---
record: 1664
origin: human
risk: low
size: low
ceremony: fast-lane
grants: [build, merge]
---
# 1664: bookkeeping-stamps gate: covered-write scope blocks composing the PR body it demands

Origin: reflect light from #1216
Defer-reason: tangential
**Related:** #1678

## Current State

During run 2026-08-29T153753-spec-1216, the bookkeeping-stamps gate (docs/hooks.md) denied a Write to the session scratchpad (`.../scratchpad/pr-early-body-1216.md`, outside the repo) that was composing the draft-PR body for the exact PR the gate's own error message demanded ("no PR is recorded for this run — open it now"). The push had already succeeded; the very next action was `gh pr create --body-file` using that file. The workaround was composing the body inline via `gh pr create --body`, which the gate does not intercept.

## Deliverables

- The gate's covered-write set excludes paths outside the repository root (session scratchpad, system temp), OR `_shared/pr-early-run-lifecycle.md` Step 3 documents inline `--body` composition as the sanctioned path so the body-file idiom is never attempted between push and PR-record.

## Acceptance Criteria

- [ ] A run at the materialize-committed/no-PR-recorded stage can compose its PR body file in the session scratchpad without a gate denial, or the documented Step 3 procedure no longer writes a body file at that stage.
- [ ] The gate still denies covered writes inside the repo at that stage (no weakening of [IL-131] protection).

_Filed by `reflect` via specShapedBody._


