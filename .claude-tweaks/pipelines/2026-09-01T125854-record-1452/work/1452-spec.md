---
record: 1452
origin: human
risk: low
size: low
ceremony: fast-lane
grants: [build, merge]
---
# 1452: archive-run refuses two clean pipeline run dirs with git-mv-failed

Origin: ledger resolve gate

Defer-reason: blocked-external

## Current State

residue.js's blast-radius sweep found two un-archived pipeline run directories whose run-state.json already reads status: clean: .claude-tweaks/pipelines/2026-08-24T183144-record-1094 and .claude-tweaks/pipelines/2026-08-25T150921-record-1420. `node plugin/bin/hooks.js archive-run --run <dir>` refuses both with `archival refused - git-mv-failed` (bin/lib/reconcile/archive-merged.js's archiveRunDir, the git mv pass over the run dir's tracked work/ entries).

## Deliverables

- [ ] Reproduce archive-run's git-mv-failed for a status:clean run dir with a committed work/{n}-spec.md entry
- [ ] Determine root cause (untracked non-work/ paths under the run dir confusing the git mv pass, a working-tree state git mv refuses, or a worktree-vs-main-checkout path mismatch)
- [ ] Fix archiveRunDir so a clean run dir with a normal work/ layout archives successfully, or document why these two specifically cannot

## Acceptance Criteria

`node plugin/bin/hooks.js archive-run --run .claude-tweaks/pipelines/2026-08-24T183144-record-1094` (and the -1420 sibling) succeed and move the run dir under .claude-tweaks/pipelines/archive/

_Filed by `ledger resolve gate` via specShapedBody._

