---
record: 1307
origin: human
risk: low
size: low
ceremony: fast-lane
grants: [build, merge]
surface: infra
---
# 1307: Stale .superpowers/sdd/ execution-plan debris (72 files, ~7 weeks old) trips wrap-up-engine's plans-ledger check

Surface: infra

## Current State

The main checkout carries a gitignored `.superpowers/sdd/` directory with 72 leftover
execution-plan artifacts (task-N-brief.md/task-N-report.md pairs, review-{sha}..{sha}.diff files,
progress.md, final-fix-report.md, final-review-fix-report.md) dated 2026-07-03/04 — roughly seven
weeks stale, from an unrelated `/superpowers:subagent-driven-development` run that predates and
has nothing to do with record #171 (a code-health fix). `wrap-up-engine.js verify`'s plans-ledger
check flags it as leftover artifacts on every run's Phase 4 closure gate that happens to check it
— repo-wide noise rather than something any one record's own wrap-up caused or can clean up in
scope.

**Shaping-time investigation (2026-08-23):** confirmed the 72-file count and 2026-07-03/04 dates
directly on the main checkout's filesystem. `final-review-fix-report.md` names the source branch
(`github-pr-awareness`) and describes specific fixes it landed — e.g. `skills/help/SKILL.md`
documenting "Stage 4.5" and `status-scan.md`'s "Stage 4.5: Current PR (GitHub)" section. Both were
found present verbatim in the current codebase (`grep -rn "Stage 4.5" plugin/skills/help/`), and
`github-pr-awareness` no longer exists as a branch (`git branch -a` shows no match). **This
confirms the work fully landed weeks ago and the debris has zero remaining value** — it answers
Deliverable 1's open question definitively: safe to delete outright, not an unfinished build.

## Deliverables

- Delete `.superpowers/sdd/` in full — confirmed safe per the shaping-time investigation above (the source work already landed on main; the directory holds no unique, un-landed content).
- Re-run `wrap-up-engine.js verify`'s plans-ledger check (or the equivalent `bin/residue.js`/verification path it uses) to confirm it no longer flags this path.

## Acceptance Criteria

- `.superpowers/sdd/` no longer exists.
- `wrap-up-engine.js verify`'s plans-ledger check reports no findings for this path on a subsequent run.

## Technical Approach

A plain deletion — `.superpowers/sdd/` is gitignored (confirmed via its own `.gitignore` file), so no `git rm` is needed, just `rm -rf .superpowers/sdd/`. Re-run whatever invokes `wrap-up-engine.js verify`'s plans-ledger check (a `/claude-tweaks:wrap-up` Phase 4 closure gate, or a direct CLI invocation if one exists) to confirm the check passes clean afterward.

### Key Files

- `.superpowers/sdd/` — the directory to delete (gitignored, no source-file changes elsewhere)
- `plugin/bin/wrap-up-engine.js` — the `verify`/plans-ledger check that flags this path; referenced for the verification step only, no change needed here

## Gotchas

- Re-verify the directory's contents haven't changed since shaping time (2026-08-23) before deleting — the investigation above establishes this specific snapshot was safe, not a standing guarantee.
- This is gitignored, per-clone local state — the debris may or may not exist identically on other machines/clones; this record addresses the main checkout on this machine.

## Original request

Stale .superpowers/sdd/ execution-plan debris (72 files, ~7 weeks old) trips wrap-up-engine's plans-ledger check

Origin: /claude-tweaks:wrap-up #171 -- discovered via wrap-up-engine.js verify's plans-ledger check, unrelated to #171 itself.
Defer-reason: pre-existing-outside-diff

## Current State

The main checkout carries a gitignored `.superpowers/sdd/` directory with 72 leftover execution-plan artifacts (task-N-brief.md/task-N-report.md pairs, review-{sha}..{sha}.diff files, progress.md, final-fix-report.md, final-review-fix-report.md) dated 2026-07-03/04 -- roughly seven weeks stale, from an unrelated /superpowers:subagent-driven-development run that predates and has nothing to do with record #171 (a code-health fix). wrap-up-engine.js verify's plans-ledger check flags it as leftover artifacts on every run's Phase 4 closure gate that happens to check it, which is repo-wide noise rather than something any one record's own wrap-up caused or can clean up in scope.

## Deliverables

- [ ] Determine whether these 72 files still have value (an in-progress build nobody finished?) or are safe to delete outright.
- [ ] Clean up .superpowers/sdd/ if abandoned, or document why it's kept if not.

## Acceptance Criteria

- [ ] .superpowers/sdd/ no longer trips wrap-up-engine.js verify's plans-ledger check with stale content, or the check is confirmed to correctly scope only to a given run's own artifacts (so unrelated old debris stops being reported against every run that happens to trigger this check).

