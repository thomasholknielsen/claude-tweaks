---
record: 2472
origin: human
risk: low
size: low
ceremony: standard
grants: [build]
---
# 2472: pr-first run reached wrap-up with no PR/run-state and no logged degrade trace

# Reflect — staged finding 1

**Category:** tangential

Origin: reflect light from #2221

Defer-reason: tangential

## Current State

This run (for #2221) reached /claude-tweaks:wrap-up with no run-state.json and no PR, despite this project resolving integration-model: pr-first and the branch already being pushed to origin. pr-early-run-lifecycle.md Step 2/3 both mandate a logged AUTO ... FAILED line on any push/create failure, and none existed in decisions.md, meaning the step most likely never ran at all for this run's original build. Wrap-up's own pr-first-merge.md precondition check caught the gap only because it explicitly requires run-state.json.pr before proceeding; this session recovered by retroactively opening PR #2470 and recording it.

## Deliverables

Consider whether flow/materialize.md's Resolution + Materialization hard gate (or build/worktree-setup.md Step 6) should assert that a pr-first run either recorded a PR or logged an explicit degrade line before the pipeline is allowed to proceed past build.

**Architecture-alignment note (Beneficial deviation, `/build` Common Step 4.5):** implemented as a new phase-boundary check at `/claude-tweaks:test`'s own entry (a new "Step 0: PR-Bookkeeping Precondition Check" in `plugin/skills/test/SKILL.md`, backed by `plugin/bin/check-pr-bookkeeping.js` / `plugin/bin/lib/pr-bookkeeping/precondition.js`) rather than inside `flow/materialize.md`'s hard gate or `build/worktree-setup.md` Step 6 as originally suggested. Rationale: `materialize.md`'s gate runs *before* build ever starts, so it structurally cannot observe a bookkeeping failure that only manifests *during* build; `worktree-setup.md` Step 6 is the very procedure whose silent failure this record is about (an existing mechanical safeguard for it — `pre-tool-use.js`'s `checkBookkeepingStampsGate` PreToolUse hook — already exists and this build reuses its helpers rather than duplicating its logic there). Placing the check at `/test`'s entry — the next phase boundary after build, with a known `$PIPELINE_RUN_DIR` rather than an ambiguously-resolved one — satisfies the Acceptance Criteria's literal wording ("caught by a gate before reaching test/review/wrap-up") as a second, independent layer of defense-in-depth.

## Acceptance Criteria

A pr-first run whose build phase never invokes pr-early-run-lifecycle.md (or invokes it and it fails silently) is caught by a gate before reaching test/review/wrap-up, rather than being discovered only by wrap-up's own merge-time precondition check.

_Filed by `reflect` via specShapedBody._

