---
record: 1250
origin: harness-health
risk: low
size: low
ceremony: fast-lane
grants: [build, merge]
fingerprint: harnesshealth-ecb57acf
surface: backend
---
# 1250: CLAUDE.md drift: CLAUDE — Subagent Contract (v4.2+)

Surface: backend

## Current State

Grep evidence confirms harness-health/SKILL.md:56, docs-health/SKILL.md:49, simplify/SKILL.md:67, and wrap-up/curation-engine.md:125-129 each explicitly cite and follow the full Subagent Contract (status line, model profile resolution, inlined output template) for genuine multi-agent or subagent dispatches, but none appear in CLAUDE.md's canonical consumer list — the list has drifted behind the codebase as these dispatch sites were added.

## Deliverables

Replace CLAUDE.md's Subagent Contract (v4.2+) "Used by" sentence:

**Current:**
```
Used by `/browse`, `/design-wrapper` (`review` mode Step 3.8 — craft critics), `/dispatch` (`build,test` then `review,polish,wrap-up`, two Task calls per file-overlap group, own GROUP/OUTCOME/MANIFEST template — none of A/B/C fit; status line + input discipline still apply, `plugin/skills/dispatch/SKILL.md` Step 5), `/help`, `/init`, `/review`, `/specify`, `/test` (qa-prompts), `/tidy`, and `/visual-review`.
```

**Proposed:**
```
Used by `/browse`, `/design-wrapper` (`review` mode Step 3.8 — craft critics), `/dispatch` (`build,test` then `review,polish,wrap-up`, two Task calls per file-overlap group, own GROUP/OUTCOME/MANIFEST template — none of A/B/C fit; status line + input discipline still apply, `plugin/skills/dispatch/SKILL.md` Step 5), `/docs-health` and `/harness-health` (parallel per-target READ+JUDGE dispatch under `--budget > 1`), `/help`, `/init`, `/review`, `/simplify` (single-subagent dispatch to `code-simplifier:code-simplifier`), `/specify`, `/test` (qa-prompts), `/tidy`, `/visual-review`, and `/wrap-up` (Phase 2 ROUTE fan-out for 3+ open worklist rows).
```

## Acceptance Criteria

The Subagent Contract's 'Used by' list omits four skills that genuinely dispatch agents under the full contract: docs-health and harness-health both do parallel per-target READ+JUDGE Task-agent fan-out under `--budget > 1` citing `_shared/subagent-output-contract.md` directly (harness-health/SKILL.md line 56, docs-health/SKILL.md line 49); simplify/SKILL.md line 67 explicitly says 'Follow the Subagent Contract' for its code-simplifier dispatch with model profile + inlined output template; wrap-up/curation-engine.md lines 125-129 dispatch each open worklist row as a parallel Task agent 'per skills/_shared/subagent-output-contract.md' at `[Use: Capable]`. None of these four are named in CLAUDE.md's list, even though the same paragraph instructs 'When adding a new dispatch site, follow the full pattern' -- implying the list is meant to stay exhaustive.

## Technical Approach

Edit CLAUDE.md's "Subagent Contract (v4.2+)" section, replacing the "Used by" sentence verbatim with the Proposed text above — a one-line prose swap, no other section of CLAUDE.md changes.

### Key Files

- `CLAUDE.md` — replace the Subagent Contract (v4.2+) section's "Used by" sentence with the proposed text listing all current dispatch-site consumers

## Gotchas

None — documentation-only change with no behavioral surface; the four added consumer citations (harness-health/SKILL.md:56, docs-health/SKILL.md:49, simplify/SKILL.md:67, wrap-up/curation-engine.md:125-129) are already verified by the grep evidence in Current State.

_Filed by `/claude-tweaks:harness-health`. Close to resolve; label `wontfix` to suppress future reports of this finding._

<!-- work-fingerprint: harnesshealth-ecb57acf -->

## Original request

CLAUDE.md drift: CLAUDE — Subagent Contract (v4.2+)

**CLAUDE.md:** CLAUDE | **Section:** Subagent Contract (v4.2+) | **Category:** drift | **Classification:** additive | **Confidence:** high

## Current State

Grep evidence confirms harness-health/SKILL.md:56, docs-health/SKILL.md:49, simplify/SKILL.md:67, and wrap-up/curation-engine.md:125-129 each explicitly cite and follow the full Subagent Contract (status line, model profile resolution, inlined output template) for genuine multi-agent or subagent dispatches, but none appear in CLAUDE.md's canonical consumer list — the list has drifted behind the codebase as these dispatch sites were added.

## Deliverables

**Current:**
```
Used by `/browse`, `/design-wrapper` (`review` mode Step 3.8 — craft critics), `/dispatch` (`build,test` then `review,polish,wrap-up`, two Task calls per file-overlap group, own GROUP/OUTCOME/MANIFEST template — none of A/B/C fit; status line + input discipline still apply, `plugin/skills/dispatch/SKILL.md` Step 5), `/help`, `/init`, `/review`, `/specify`, `/test` (qa-prompts), `/tidy`, and `/visual-review`.
```

**Proposed:**
```
Used by `/browse`, `/design-wrapper` (`review` mode Step 3.8 — craft critics), `/dispatch` (`build,test` then `review,polish,wrap-up`, two Task calls per file-overlap group, own GROUP/OUTCOME/MANIFEST template — none of A/B/C fit; status line + input discipline still apply, `plugin/skills/dispatch/SKILL.md` Step 5), `/docs-health` and `/harness-health` (parallel per-target READ+JUDGE dispatch under `--budget > 1`), `/help`, `/init`, `/review`, `/simplify` (single-subagent dispatch to `code-simplifier:code-simplifier`), `/specify`, `/test` (qa-prompts), `/tidy`, `/visual-review`, and `/wrap-up` (Phase 2 ROUTE fan-out for 3+ open worklist rows).
```

## Acceptance Criteria

The Subagent Contract's 'Used by' list omits four skills that genuinely dispatch agents under the full contract: docs-health and harness-health both do parallel per-target READ+JUDGE Task-agent fan-out under `--budget > 1` citing `_shared/subagent-output-contract.md` directly (harness-health/SKILL.md line 56, docs-health/SKILL.md line 49); simplify/SKILL.md line 67 explicitly says 'Follow the Subagent Contract' for its code-simplifier dispatch with model profile + inlined output template; wrap-up/curation-engine.md lines 125-129 dispatch each open worklist row as a parallel Task agent 'per skills/_shared/subagent-output-contract.md' at `[Use: Capable]`. None of these four are named in CLAUDE.md's list, even though the same paragraph instructs 'When adding a new dispatch site, follow the full pattern' -- implying the list is meant to stay exhaustive.

_Filed by `/claude-tweaks:harness-health`. Close to resolve; label `wontfix` to suppress future reports of this finding._

<!-- work-fingerprint: harnesshealth-ecb57acf -->

