---
record: 1385
origin: capture
risk: low
size: medium
ceremony: standard
grants: [build]
---
# 1385: grant-mode.md's snippets require reading bin/lib source mid-skill

**Related:** #1384

## Current State

Executing skills/backlog/grant-mode.md's Step 0-2 procedure by hand required reading roughly 10 full skill/lib files to reconstruct the intended shell pipeline — grant-mode.md, _shared/trust-table.md, _shared/record-queue-fetch.md, _shared/pipeline-run-dir.md, plus bin/lib/issues/grant-gate.js, trust.js, merge-lane-breaker.js, record.js, backlog.js source — because the skill's own snippets reference module internals (e.g. evaluateGrantGate's exact input shape, trustRows' hasParent/gitLog expectations) without stating them self-sufficiently in the prose. Observed in a live run (2026-08-24): roughly 192KB of tool-result payload across 60 tool calls to execute one grant sweep that filed nothing.

## Deliverables

- Either the CLI consolidation proposed in the companion "grant mode has no consolidated CLI" finding (which removes most of the need to read bin/lib source mid-skill), or a self-sufficient snippet contract added to _shared/trust-table.md / grant-mode.md that states each snippet's exact input/output shape (function signatures, expected object keys) inline, so a session following the prose does not need to open the module source to proceed.

## Acceptance Criteria

- A fresh session following grant-mode.md's Step 0-2 procedure end-to-end does not need to Read any bin/lib/issues/*.js source file to determine a snippet's expected input shape.
- Total tool-result payload for one grant-mode Step 0-2 pass measurably drops relative to the ~192KB baseline observed in the 2026-08-24 run (exact target left to implementation).

_Filed by `capture` via specShapedBody._
