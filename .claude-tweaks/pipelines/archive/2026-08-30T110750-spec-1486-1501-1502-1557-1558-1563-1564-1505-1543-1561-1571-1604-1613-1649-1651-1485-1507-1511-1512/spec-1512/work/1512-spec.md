---
record: 1512
origin: #1488's whole-branch review (2026-08-26)
risk: medium
size: medium
ceremony: standard
grants: []
surface: infra
---

# Extract another self-contained section out of backlog/refine-mode.md for byte-ceiling headroom

## Current State

`plugin/skills/backlog/refine-mode.md` sat at 40,952/40,960 bytes for most of #1488's lifetime (8 bytes headroom); its final-review fix round needed just-in-time trimming to stay under the ceiling. At this record's build time it stood at 40,902/40,960.

## Deliverables

- Identify the next self-contained, low-churn section suited to the established extraction pattern (`_shared/work-record-permission-matrix.md`, `backlog/grant-lane-decision.md` prior art) — re-evaluate Step 5's write mechanics against the file's current shape rather than assuming it's still the best cut.
- Extract into a new sibling file under `plugin/skills/backlog/`, following `shared-contract-extraction`'s pattern.
- Update `docs/skill-graph.md` with the new edge(s).

## Acceptance Criteria

- `refine-mode.md` has materially more than 8 bytes of headroom after extraction (≥5,000 bytes is a reasonable bar).
- The extracted file passes its own byte-ceiling check.
- Every existing pinned test referencing the extracted content still passes, repointed where needed.
- `docs/skill-graph.md` gains the new edge(s) if the extracted content is cross-skill.
- Full `npm test` suite green.

## Investigation Findings

**Re-evaluated the candidate from scratch, per the Gotcha's explicit instruction.** `grep -n "apply-refine-labels" plugin/skills/backlog/refine-mode.md` returns nothing — the script name #1488's review named as the leading candidate no longer exists anywhere in the file. Step 5's write mechanics today are raw `gh issue edit`/`writeRecord` calls directly in prose, not delegated to any such script. The file's shape has materially changed since the review-time snapshot, exactly as the Gotcha warned.

**Measured every top-level `## ` section's byte size** to find the actual largest, most self-contained candidate:

| Section | Bytes |
|---|---|
| Step 1: Fetch | 10,200 |
| Step 2: Priority/Related synthesis | 2,463 |
| Step 3: Grant-check | 3,470 |
| Step 3.5: Body-shape re-verification | 2,740 |
| Step 3.6: Ceiling-authorized born-ready | 1,364 |
| Step 4: Decision lanes | 671 |
| **Step 5: Apply** | **19,123** |
| Concurrency | 659 |

Step 5 is still the largest section by far (19,123 of 40,902 bytes — ~47% of the whole file), confirming it as the right neighborhood, but its own internal shape has changed: it now reads as a sequence of bold-labeled sub-paragraphs (Pre-write reverify, Priority/Related rows, Grant rows, Dependency-repair rows, Flag-back rows, Needs-decision rows, then the logging/closing-summary/run-closure tail) rather than any script-delegated block.

**Selected the logging + closing-summary + run-closure tail** (the "Check each write's own result…" paragraph through the end of "Close the run dir.") as the extraction target — 5,543 bytes, self-contained (it consumes only the already-logged `decisions.md` outcomes from the write-lane paragraphs above it, referencing them by name/shape rather than by internal cross-reference), and low-churn (a reporting/teardown concern, not per-lane write-mechanics logic that changes whenever a new lane is added).

**Extracted to `plugin/skills/backlog/refine-closing-summary.md`** (6,235 bytes — well under its own 40,960-byte ceiling), following the `grant-lane-decision.md` precedent's structure: a title, an intro paragraph naming the citing skill/step and the reason for the split, then the moved content verbatim. Left a one-paragraph citing pointer in `refine-mode.md`'s Step 5 in place of the removed text.

**Headroom achieved:** `refine-mode.md` is now 35,730 bytes — **5,230 bytes of headroom**, clearing the AC's ≥5,000-byte bar (up from 58 bytes before this record).

**`docs/skill-graph.md` — no new edge added, and this is the correct outcome, not a skipped deliverable.** `docs/skill-graph.md`'s own stated scope (`docs/skill-graph.md:3`, `:15-21`) is "every relationship *between claude-tweaks skills*" — cross-skill edges only; same-skill internal sub-file splits get no row there. Confirmed against the exact precedent this record cites as prior art #2: `grant-lane-decision.md` (also a same-skill `refine-mode.md` → `backlog/*.md` split) has **no dedicated row** in `docs/skill-graph.md` either — it appears only incidentally inside another row's prose (line 57, as a co-editor of `_shared/work-record-permission-matrix.md`'s content, a genuinely cross-skill contract). `refine-closing-summary.md` is cited only by `refine-mode.md`'s Step 5 — both files belong to `/backlog` — so it creates no cross-skill relationship, matching `grant-lane-decision.md`'s precedent exactly. The AC's edge-update requirement is itself conditional ("if the extracted content is shared across multiple skills"), which doesn't hold here.

**`docs/plugin-structure.md`'s per-skill sub-file table updated instead** (the registration site the `shared-contract-extraction` skill's step 3 names for exactly this situation): added `refine-closing-summary.md` to `/backlog`'s file list and its own descriptive clause, matching the existing `trust-signal.md`/`merge-lane-reset.md` clause pattern. No pinned test asserts this table's per-file list is exhaustive (`tests/skill-catalog-completeness.test.js` checks per-*skill* section presence, not per-sub-file list contents), so this was a documentation-accuracy improvement with nothing to break.

**Consumer sweep before writing** (per `shared-contract-extraction`'s step 1): grepped for every distinctive string in the candidate span (`"Closing summary (required"`, `"priority set ·"`, `"AUTO {time} — Backlog refine"`, `"Close the run dir\."`) across `tests/`, `docs/`, `plugin/`. Found exactly one real consumer: `tests/backlog-refine-closing-render.test.js` (8 assertions pinning the closing-summary prose verbatim against `refine-mode.md`'s own text). Two other grep hits were false positives out of scope: `plugin/skills/backlog/merge-lane-reset.md` matched only the generic `AUTO {time} — Backlog refine:` prefix on an unrelated circuit-breaker-reset log line, and `docs/journeys/refine-the-backlog-through-decision-lanes.md` narrates the closing-summary behavior in prose (still accurate after the move — no literal citation to repoint). That journey doc separately references a stale `apply-refine-labels.js` script name that no longer exists anywhere in `refine-mode.md`; nothing in this record asks for a journey-doc audit, and fixing unrelated staleness there is out of scope, so it was left untouched.

**Retargeted `tests/backlog-refine-closing-render.test.js`**: its 7 original pinned assertions now read `refine-closing-summary.md`'s content instead of `refine-mode.md`'s, matching the go-red-proof convention (`PRE_CHANGE_STEP_5_TAIL` control unchanged — still proves each pattern can go red). Added one new test pinning `refine-mode.md`'s citation of the new file, matching `backlog-grant-lane-decision.test.js`'s own established "source cites the extracted file" pattern — 8 tests total.

**Test results.** `tests/backlog-refine-closing-render.test.js`: 8/8 pass (7 retargeted + 1 new citation test). `tests/backlog-grant-lane-decision.test.js` + `tests/bin-lib/skill-audit/context-cost.test.js`: 32/32 pass (no regression from the byte-ceiling or grant-lane-decision angle). `tests/skill-catalog-completeness.test.js` + `tests/skill-graph-table-structure.test.js` + `tests/flow-subfile-table-completeness.test.js`: 11/11 pass. Full `npm test` — see this record's commit history for the final pass/fail counts.
