---
record: 1649
origin: reconcile-residue (filed automatically, see #644)
risk: medium
size: low
ceremony: fast-lane
grants: []
surface: backend
---

# reconcile: move-failed stuck on .claude-tweaks/pipelines/2026-08-25T143037-record-1348

## Current State

Reconcile had failed `move-failed` for 3 consecutive passes on `.claude-tweaks/pipelines/2026-08-25T143037-record-1348`. Live directory contained only a `work/` subdirectory; its archive twin at `.claude-tweaks/pipelines/archive/2026-08-25T143037-record-1348` had the full run bookkeeping but no `work/` — consistent with a prior `archiveRunDir` move that completed for every file except `work/`.

## Deliverables

- Confirm the diagnosis against live filesystem state.
- Reconcile the two directories: move `work/` into the archive twin, remove the now-empty live directory.
- Confirm no other run directory shares this same split shape; resolve identically if found.
- Verify reconcile no longer reports `move-failed` for this path.

## Acceptance Criteria

- `.claude-tweaks/pipelines/2026-08-25T143037-record-1348` no longer exists as a separate live directory; its `work/` contents are preserved under the archive twin.
- A `node bin/hooks.js reconcile` pass after the fix does not report `move-failed` for this path.
- No sibling run directory is left in the same split state undiagnosed.

## Investigation Findings

**Diagnosis confirmed.** Live dir at `.claude-tweaks/pipelines/2026-08-25T143037-record-1348` contained only `work/` (a single `1348-spec.md`); its archive twin held `config.yml`, `decisions.md`, `engine-state.json`, `events.jsonl`, `run-state.json`, `staged/` — no `work/`. Exactly the split shape described.

**Why manual repair over relying on automated reconcile to succeed retroactively:** a `reconcile --dry-run` pass reported this path's action as `"archived"` (dry-run only evaluates gates, never attempts the actual move), suggesting the underlying conflict had already cleared. A real (non-dry-run) `reconcile` pass was run to test this — it reproduced `move-failed` for 1348 again. Root cause was never isolated further (the record's own Technical Approach explicitly deprioritizes root-causing the automated path in favor of the manual merge, deferring the systemic "why did this split happen and how do we prevent recurrence" question to #1613's structurally-stuck escalation, which will now track any future instance of this same reason class automatically). The manual repair — `git mv {live}/work {archive-twin}/work`, commit, then `rmdir` the emptied live directory — was applied directly, following `archiveRunDir`'s own move+commit convention (`plugin/bin/lib/reconcile/archive-merged.js:410-445`) so the resulting git history is indistinguishable from what the automated path would have produced on success.

**Sibling sweep.** A full scan of every `.claude-tweaks/pipelines/{id}` directory against its `archive/{id}` twin found dozens of dirs where both sides exist — the overwhelming majority are ordinary already-fully-resolved multi-spec run dirs (own historical `spec-N/` subtrees, not the described split shape) and out of this record's scope. Narrowing to the exact shape reconcile currently reports as actionable (via `node bin/hooks.js reconcile --json --checks archive`, `runs[].reason === 'move-failed'`) surfaced 4 additional dirs sharing the identical split (live has only `work/`, archive twin has everything else, no `work/`): `2026-08-27T055510-record-1368`, `2026-08-27T051317-record-1356`, `2026-08-26T055917-record-1219`, `2026-08-26T054601-record-1214`. Per this record's own instruction ("if others exist, resolve them the same way"), all 4 were resolved identically: `git mv work → twin/work`, one combined commit, then `rmdir` each emptied live directory.

**Pre-existing, unrelated residue left untouched.** The real reconcile run also surfaced an unrelated uncommitted state for `.claude-tweaks/pipelines/2026-08-28T201841-record-1100` (unstaged deletions + an untracked archive-twin directory) — this is `archiveOrphanedMint`'s designed behavior (a plain `fs.renameSync`, no git operations at all — `archive-merged.js:111-121`), not a defect and not this record's split shape (no live directory remains at all for 1100; its full content already sits under the archive twin). Left as-is: staging/committing it is a separate, pre-existing housekeeping gap unrelated to `archiveRunDir`'s git-mv path this record concerns.

**Stale residue-cache entries not purged.** `.claude-tweaks/reconcile-cache.json`'s `residueFailures` map retains dead `move-failed:{path}` keys for all 5 resolved paths (and several older, unrelated ones from mid-August). These are inert — no future reconcile pass can touch a key whose run directory no longer exists — and purging them isn't part of this record's fix; left alone rather than hand-editing the cache file outside the tool's own API.

**Verification.** `node bin/hooks.js reconcile --json --checks archive` after all 5 repairs: `move-failed` count is 0 (previously 5 across the two passes), and none of the 5 resolved paths appear in the `runs[]` output at all (their live directories no longer exist for `iterRunDirsWithState` to enumerate). AC1-AC3 all satisfied. No code changes were required — this was a pure data-repair record; the worktree's own `git status` is clean apart from this materialized spec file.

**Incidental validation of #1613:** the residue cache also now shows several `structurally-stuck:{path}` entries accumulating from real production reconcile passes (predating this session) — confirms #1613's `trackStuckSkip`/`isStructurallyStuck` mechanism, implemented and tested earlier in this same pipeline run, is active and functioning against real data, not just its own test fixtures.
