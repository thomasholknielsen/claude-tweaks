---
record: 1973
origin: human
risk: low
size: low
ceremony: fast-lane
grants: [build, merge]
---
# 1973: dispatch/SKILL.md: report why #N/#N,#M was excluded by the open-linked-PR filter (needs 40KB trim first)

## Current State

`dispatch/SKILL.md`'s `#N` and `#N,#M,...` selection forms currently fail silently (as a plain "not found") when a named record was dropped from `dispatch-groups.json` by the open-linked-PR exclusion (#1224) — the record clears the label checks but is simply absent from the file the selection form reads next, with no explanation of why.

Record #1821's own review found and fixed this exact gap (report the specific reason: `#{N} already has an open PR (#{pr}) — not re-dispatch-eligible until that PR merges or closes`, read from the run's `dispatch-open-pr-excluded.json`), but that fix could not be ported forward in #1972 (the cleanup PR that superseded #1821/#1942 after #1852 landed the underlying exclusion first): `dispatch/SKILL.md` is currently 40,948 bytes against a hard 40,960-byte ceiling enforced by `tests/dispatch-budget-drain.test.js` — only 12 bytes of headroom, not enough to add the fix's ~2 sentences.

The exact wording (from #1821/#1942's branch, `worktree-dispatch-record-1821`, commit `5a8c2a1c1`) is available in that PR's diff for `SKILL.md`'s `#N` / `#N,#M,...` bullets if useful as a starting point — but re-derive rather than copy verbatim if the surrounding text has moved since.

## Deliverables

- Trim `plugin/skills/dispatch/SKILL.md` (or extract another paragraph to a sub-file, following the pattern already used for `cross-pr-overlap-report.md`/`oversized-group-report.md`/`open-pr-exclusion-report.md`) to create enough headroom under the 40KB ceiling.
- Add the open-linked-PR exclusion reporting to the `#N` and `#N,#M,...` selection-form bullets: when a record clears the label checks but is absent from `dispatch-groups.json`, check `dispatch-open-pr-excluded.json` and report the specific reason instead of a generic "not found".

## Acceptance Criteria

- `tests/dispatch-budget-drain.test.js`'s 40KB ceiling test still passes after the addition.
- A record dropped by the open-linked-PR exclusion and then named via `#N` or `#N,#M,...` is reported with the specific open-PR reason, not a generic not-found message.

Origin: split out of #1958's reconciliation of duplicate PRs #1852 (#1224) and #1942 (#1821) — full context in docs/plans/2026-09-05-dispatch-reselects-open-pr-record-1821-ledger.md item #4/#7 and PR #1972.
Defer-reason: needs-human-decision (trim approach is a judgment call)
