---
record: 1368
origin: human
risk: low
size: low
ceremony: standard
grants: [build]
surface: backend
---
# 1368: Investigate subagent contract-line violation observed during #1259's build dispatch

# Reflect — staged finding 1

**Category:** tangential

Origin: reflect light from #1259
Defer-reason: tangential

## Current State

During #1259's build phase, this run's `events.jsonl` recorded a `contract-violation` event at
`2026-08-24T05:08:36.040Z`, firstLine: "Still waiting on the final whole-branch review agent
(retry attempt 3) to complete." This event type is written by
`plugin/bin/lib/hooks/subagent-stop.js:50`, which fires when a dispatched Task agent's first
reply line does not match the required `DONE`/`DONE_WITH_CONCERNS`/`NEEDS_CONTEXT`/`BLOCKED`
status-line vocabulary (`_shared/subagent-output-contract.md`). The violating dispatch site and
its purpose are not otherwise identified in this run's artifacts — the timing (between the
`996af97e` materialize commit and the `37b5b3da` implementation commit, immediately after a
`superpowers:writing-plans` skill_invoked event) suggests it originated from plan-writing or an
associated whole-branch review of #991 (this record's origin), not from #1259's own build
dispatch directly, but this is not confirmed.

## Deliverables

- Identify which dispatch site produced this violation (a claude-tweaks-owned dispatch inside
  `/claude-tweaks:build` or `superpowers:writing-plans`'s own subagent use) and whether it is a
  claude-tweaks-owned dispatch site missing the subagent-output-contract, or a third-party
  (superpowers) dispatch exempt from it per that contract's exemption clause.
- If claude-tweaks-owned: fix the dispatch prompt so its first reply line is always the required
  status line.

## Acceptance Criteria

- The dispatch site is identified by skill/file name.
- Either a fix lands (status line enforced) or the site is confirmed exempt (third-party) with a
  one-line note in the finding explaining why.

_Filed by `reflect` via specShapedBody._
