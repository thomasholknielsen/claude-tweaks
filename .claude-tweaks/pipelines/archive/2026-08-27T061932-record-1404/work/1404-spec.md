---
record: 1404
origin: human
risk: low
size: low
ceremony: fast-lane
grants: [build, merge]
---
# 1404: pr-state.test.js event-loop test still flakes under sibling load after #1127's margin fix

Origin: reflect full from #1365
Defer-reason: tangential

## Current State

tests/bin-lib/reconcile/pr-state.test.js's "resolvePrStateAsync: does not block the event loop (real concurrency, not execFileSync in disguise)" asserts concurrent wall-clock time < sequential wall-clock time. #1127 ("tight timing margin under concurrent load") closed COMPLETED on 2026-08-21, but on 2026-08-24 the assertion failed in both of run 2026-08-24T144942-record-1365's full `npm test` executions under sibling-session load (e.g. "expected concurrent (470ms) under sequential (505ms)"), passing 18/18 when the file re-ran in isolation each time. Every loaded machine pays an isolated re-run + flake adjudication on this one test.

## Deliverables

Make the event-loop-blocking check robust to machine load: assert a structural property of concurrency rather than a wall-clock ratio (e.g. interleaving evidence — a timer/tick observed progressing while the calls are in flight — or a mocked-latency harness where sequential is deterministically 2x), or widen the margin/retry internally so sibling-load variance cannot invert the comparison.

## Acceptance Criteria

- The test still fails against a genuinely blocking (execFileSync-style) implementation.
- The test passes deterministically under concurrent CPU load (e.g. repeated runs alongside a parallel `npm test`), with no isolated re-run needed.
- No change to resolvePrStateAsync's production behavior.

_Filed by `reflect` via specShapedBody._

