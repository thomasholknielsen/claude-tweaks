---
record: 1876
origin: human
risk: low
size: low
ceremony: fast-lane
grants: [build, merge]
---
# 1876: pre-tool-use worktree-verification gate false-positives on plain non-git Bash commands

# Reflect — staged finding (queue write 2)

**Category:** tangential

Origin: reflect full mode from #1687
Defer-reason: tangential
**Related:** #1867

## Current State

During this run's wrap-up, the worktree-isolated session's "too complex to verify that it stays inside the worktree" Bash-call refusal (documented as harness-level in `docs/hooks.md`'s "Not covered here" note, and empirically logged in `plugin/skills/_shared/scratch-worktree.md` Section 7's dated addenda) fired on several plain, non-git commands: an env-var assignment whose name merely contained the substring `GIT` (`CT_HOOKS_GIT_TIMEOUT_MS=60000 node --test $(...)`), a `printf` whose payload was a block of `-`-prefixed bullet lines (no flags at all, just dash-led text content), and a multi-statement compound command (redirect + `mv`) with no git verb anywhere in it. Each cost a retry with a rephrased/simplified command.

## Deliverables

- [ ] Add these three trigger shapes as new dated addenda to `plugin/skills/_shared/scratch-worktree.md` Section 7, alongside the existing `${VAR:-default}`, `VAR=val && command`, bare `=`-led token, and literal-`complete` entries — so future sessions have a documented workaround rather than rediscovering it live.
- [ ] If this project owns the underlying gate (as opposed to it being pure harness code, per `docs/hooks.md`'s disclaimer), consider whether the heuristic can be tightened to key on an actual git argv token rather than substring-matching "GIT" in the command text.

## Acceptance Criteria

1. The three trigger shapes above are recorded somewhere a future session will find them before hitting the same false positive live.

_Filed by `reflect` via specShapedBody._


