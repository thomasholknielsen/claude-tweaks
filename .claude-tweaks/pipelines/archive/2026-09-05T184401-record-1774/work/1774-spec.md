---
record: 1774
origin: harness-health
risk: low
size: low
ceremony: fast-lane
grants: [build]
fingerprint: harnesshealth-6bd36d8a
---
# 1774: Skill best-practice: skill-prose-conformance-tests — Anti-Patterns

**Skill:** skill-prose-conformance-tests | **Section:** Anti-Patterns | **Category:** best-practice | **Classification:** additive | **Confidence:** high

Verified-as-of: 4c18d38c7acd13c08fdc36bff84ae99cc88b954e

## Current State

Step 1 check 9 (context-cost bloat scan) flagged this row at 901 bytes against a corpus median of 197B and a 394B flag threshold — the largest outlier in the file by nearly 2x over the next-largest flagged row. Per the bloat-signal guard, a row is legitimately long only when it carries an irreducible constraint; this one instead narrates the specific incident history that produced the rule (naming the exact fixture constants involved, quoting a commit message verbatim, and recounting how #1276 fixed a third instance) rather than stating the anti-pattern once. The trimmed version keeps the generalizable rule and one grounding example, cutting the row from 901B to roughly 610B.

## Deliverables

**Current:**
```
| Reusing a fixture already frozen in the file as a new pin's go-red control, because it is sitting right there | A control proves the pin can go red only when it is the pre-change text of the **same** region the pin targets. An excerpt frozen from another file or section can never contain the new needle, so `!control.includes(needle)` was already true before the pin was written and stays true if the pinned prose is later deleted — vacuous in both directions. `tests/untrusted-record-content-conformance.test.js` accumulated two of these against `FROZEN_NEXT_MODE_BOUNDARY` (a frozen `next-mode.md` excerpt used to "prove" pins that target `untrusted-record-content.md`), one of whose messages concedes the point — *"lacks the row either way"* — and #1276 shipped a third by inheriting the idiom before replacing it with `FROZEN_PRE_1276_SCOPE`, the exact Scope sentence its edit replaced |
```

**Proposed:**
```
| Reusing a fixture already frozen in the file as a new pin's go-red control, because it is sitting right there | A control proves the pin can go red only when it is the pre-change text of the **same** region the pin targets — an excerpt from another file or section can never contain the new needle, so `!control.includes(needle)` was already true before the pin existed and stays true after the pinned prose is deleted, vacuous either way. `tests/untrusted-record-content-conformance.test.js` shipped this pattern three times before correcting to `FROZEN_PRE_1276_SCOPE`, the pin's own pre-change text. |
```

## Acceptance Criteria

The Anti-Patterns table row on reusing an already-frozen fixture as a go-red control has absorbed incident narrative (specific test-file history, a quoted commit message, and a blow-by-blow of #1276's fix) rather than stating the reusable anti-pattern, making it a context-cost outlier re-read on every invocation of this skill

_Filed by `/claude-tweaks:harness-health`. Close to resolve; label `wontfix` to suppress future reports of this finding._

<!-- work-fingerprint: harnesshealth-6bd36d8a -->
