---
record: 1471
origin: harness-health
risk: low
size: low
ceremony: fast-lane
grants: [build]
fingerprint: harnesshealth-4e263247
---
# 1471: Skill best-practice: skill-prose-conformance-tests — Anti-Patterns

**Skill:** skill-prose-conformance-tests | **Section:** Anti-Patterns | **Category:** best-practice | **Classification:** additive | **Confidence:** high

Verified-as-of: a8023e6a1ffac0852b0ec0cdc515f9651eaa1e92

## Current State

harness-health's context-cost bloat scan (bin/lib/skill-audit/bloat.js) flags this row at 668 B against a corpus median of 210 B over 23 rows (ceiling 420 B) — more than 3x median. The row has absorbed the reason it was added (an incident narrative citing 'near-miss, spec #1263, 2026-08-22' and enumerating specific failure causes) rather than stating the load-bearing constraint alone; per the bloat-signal guard, that narrative belongs in the project's incident log, not a table re-read on every invocation.

## Deliverables

**Current:**
```
| Proving a prose pin discriminates by mutating the tree (revert the fix, delete the pinned sentence) and re-running | The mutation is live working-tree state with no owner but the running agent: an agent killed mid-cycle — a usage limit, a platform stall, a manual stop — leaves it sitting in the tree, and it reads as ordinary work in progress to whoever looks next (near-miss, spec #1263, 2026-08-22). `git show {base}:{file}` proves the same thing with zero mutation — see "Proving discrimination without editing the tree" below. Where a real mutation is unavoidable, commit first and drive the whole cycle from one script, and `git status` after any agent death |
```

**Proposed:**
```
| Proving a prose pin discriminates by mutating the tree (revert the fix, delete the pinned sentence) and re-running | The mutation is live working-tree state with no owner but the running agent — one killed mid-cycle leaves it sitting in the tree, reading as ordinary work in progress to whoever looks next. `git show {base}:{file}` proves the same thing with zero mutation — see "Proving discrimination without editing the tree" below. Where a real mutation is unavoidable, commit first and drive the whole cycle from one script, and `git status` after any agent death |
```

## Acceptance Criteria

The Anti-Patterns row about proving a prose pin discriminates by mutating the tree has absorbed a specific incident narrative (near-miss citation with spec number and date) instead of stating just the reusable constraint

_Filed by `/claude-tweaks:harness-health`. Close to resolve; label `wontfix` to suppress future reports of this finding._

<!-- work-fingerprint: harnesshealth-4e263247 -->
