---
record: 1900
origin: capture
risk: low
size: low
ceremony: fast-lane
grants: [build, merge]
---
# 1900: npm test: reap-merged #1793 macOS realpath mismatch + impeccable-cli-contract version pin

Origin: pre-existing npm test failures discovered while executing headless dispatch build,test for #1503 (run 2026-09-05T123253-record-1503)

Defer-reason: pre-existing-outside-diff

## Current State

Two npm test failures reproduce deterministically on this host, confirmed unrelated to #1503's diff (which only touches `plugin/agents/qa-agent.md` and `plugin/skills/browse/agent-browser-reference.md`) by diffing the failing test files' paths against the changed-file set, and by confirming the identical test source already exists byte-for-byte at origin/main tip (`201ec06f2`):

1. `tests/bin-lib/reconcile/reap-merged.test.js` — the three `#1793` port-lease-release tests fail with a `/private/var/...` (actual) vs `/var/...` (expected) path mismatch. This is a macOS-specific symlink-normalization bug: `os.tmpdir()` on macOS returns a path through the `/var` -> `/private/var` symlink, but somewhere in the `reapMerged`/`releasePorts` call path the value gets `fs.realpathSync`-resolved before being passed to the injected `releasePorts` callback, so the assertion's literal (non-realpath'd) expected value never matches on a real Mac.
2. `tests/impeccable-cli-contract.test.js` — `the installed CLI matches the pinned version` fails with `Installed Impeccable CLI is 4.0.1, pinned is 3.6.0` — this host has a newer globally-installed Impeccable CLI than the fixtures are pinned to. Distinct from #1853's `tests/impeccable-plugin-contract.test.js` (plugin version, not CLI version) — not the same test, not covered by that issue.

Both reproduce identically in isolation (`node --test <file>`), so per verification.md's Flake adjudication these are regressions, not load-related flake — just pre-existing ones, not caused by any particular diff.

## Deliverables

- Fix `reapMerged`'s (or the test fixture's) realpath handling so the expected value is derived the same way as the actual (both realpath'd, or neither) — likely a one-line fix in the test fixture or in whichever call resolves the removed path before invoking `releasePorts`.
- Either bump the pinned Impeccable CLI fixture version to match a currently-supported release, or document/enforce the exact CLI version contributors must have installed locally so `npm test` is deterministic across dev machines.

## Acceptance Criteria

- [ ] `node --test tests/bin-lib/reconcile/reap-merged.test.js` passes on macOS (verified against a real `/var` symlink, not just Linux CI).
- [ ] `tests/impeccable-cli-contract.test.js`'s pinned-version check either passes on a fresh `npm install`-only checkout, or the version-mismatch condition is documented as an expected local-environment caveat rather than a silent red.

_Filed by `capture` via specShapedBody._
