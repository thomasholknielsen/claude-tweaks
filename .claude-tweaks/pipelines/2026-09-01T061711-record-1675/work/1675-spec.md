---
record: 1675
origin: human
risk: low
size: low
ceremony: fast-lane
grants: [build, merge]
surface: backend
---
# 1675: Point next-mode-shape.md and assess-agent-autonomy/SKILL.md at #1276's declined-isolation rationale

Surface: backend

Origin: ledger resolve gate

Defer-reason: genuinely-larger

## Current State

#1276's build encoded the declined structural-isolation rationale into plugin/skills/challenge/SKILL.md and plugin/skills/_shared/untrusted-record-content.md, but AC 3 forbade touching plugin/skills/specify/. The whole-branch review found two sibling files still stating the pre-#1276 cost-only rationale for framing-check's inline invocation: plugin/skills/specify/next-mode-shape.md:46-48 ("challenge/SKILL.md's own contract: the caller already holds the body, so a subagent would only pay to re-derive it") and plugin/skills/assess-agent-autonomy/SKILL.md:96 (its Anti-Patterns row, "a subagent only pays to re-derive it"). Both cite the canonical home correctly (state-it-once holds), but a maintainer reading either call site sees only the cost argument, not the security case #1276 decided.

## Deliverables

- [ ] Update plugin/skills/specify/next-mode-shape.md:46-48 to cite challenge/SKILL.md's three-ground declined-isolation rationale (#1276) rather than restating the cost-only half.
- [ ] Update plugin/skills/assess-agent-autonomy/SKILL.md:96's Anti-Patterns row the same way.
- [ ] Sweep for any other restatement of the pre-#1276 cost-only phrasing across the repo (grep for "only pay(s) to re-derive it" outside challenge/SKILL.md).
- [ ] Confirm no test pins the exact restated phrasing at either site before editing (re-derive the pin set fresh — a prior grep found none, but re-verify at build time).

## Acceptance Criteria

1. Neither cited call site restates the cost-only rationale without pointing at challenge/SKILL.md's declined-isolation paragraph.
2. `npm test` passes.
3. No file outside the two named sites plus any newly discovered sweep hits is touched.

_Filed by `ledger resolve gate`. Close to resolve; label `wontfix` to suppress future reports of this finding._
