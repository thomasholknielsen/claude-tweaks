---
record: 1198
origin: docs-health
risk: low
size: low
ceremony: fast-lane
grants: [build, merge]
fingerprint: docshealth-cac63296
---
# 1198: Doc staleness: decisions/0014-hook-read-key-renames-carry-a-transitional-twin — Decision

**Doc:** decisions/0014-hook-read-key-renames-carry-a-transitional-twin | **Section:** Decision | **Category:** staleness | **Misleads:** human engineer + coding agent | **Classification:** additive | **Confidence:** high

## Current State

Live-repo check: .claude-tweaks/policy.yml currently contains only `worktree-always: true` (confirmed by direct read and `grep -nF "worktree.always:" .claude-tweaks/policy.yml`, exit 1 / no match) — no `worktree.always: true` twin, commented or otherwise. `git log -p --all -- .claude-tweaks/policy.yml` shows the dotted-spelling line was never present in the file's tracked history on this branch either. ADR 0014's own Decision section states in present tense: "This repo's policy.yml carries both spellings — worktree-always: true and worktree.always: true, the old line commented as a transitional twin" — a directly checkable factual claim that is now false. The doc's own removal condition (a fresh session started on a running build at or above the release that shipped #602, per plugin/skills/_shared/policy-deprecations.md's worktree.always entry) explains why the twin is gone, and this session's own resolved plugin build is v6.96.0, well past #602 (confirmed shipped and stable per docs/journeys/resolve-a-policy-key.md and plugin/bin/lib/policy-schema.js's RENAMED_KEYS comment) — so this looks like the decision executing exactly as designed, not a bug. But the ADR text itself was never updated to say so. A reader or retrieval agent consuming just the Decision section would confidently but incorrectly conclude the twin currently exists in policy.yml and go looking for 'the comment on the old line' that policy-deprecations.md's own worktree.always entry also points at — which no longer exists either. This repo's own established convention for a decision whose described state has since moved on is a scope-note blockquote added without editing the original decision text (docs/decisions/0009's 6.70.0 scope note, docs/decisions/0013's superseded-rather-than-edited note) — not silent staleness.

## Deliverables

**Current:**
```
- **Context:** #602, applying #332's naming rule (`skills/_shared/policy-key-naming.md`) to `worktree.always`, the last dotted key

## Context
```

**Proposed:**
```
- **Context:** #602, applying #332's naming rule (`skills/_shared/policy-key-naming.md`) to `worktree.always`, the last dotted key

> **Resolved.** The transitional twin this decision installs has since been removed from this
> repo's `.claude-tweaks/policy.yml` — a fresh session detected a running build at or above the
> release that shipped #602 and deleted the old-spelling line, per the removal condition in the
> Decision section below. Only `worktree-always: true` remains; the Decision section describes
> the now-closed transitional window, not current file contents. Left unedited below on purpose,
> per `[ADR-0013]`'s superseded-rather-than-edited convention.

## Context
```

## Acceptance Criteria

docs/decisions/0014-hook-read-key-renames-carry-a-transitional-twin.md gains a scope-note blockquote (matching the precedent set in docs/decisions/0009 and 0013) stating that the transitional twin this decision installs has since been removed from .claude-tweaks/policy.yml, so the Decision section's present-tense 'carries both spellings' claim is understood as describing the now-closed transitional window rather than current file contents. The blockquote is inserted between the frontmatter list and the '## Context' heading, following 0009's exact wording pattern including the 'Left unedited below on purpose, per [ADR-0013]'s superseded-rather-than-edited convention' closing line.

**Related:** #1197

_Filed by `/claude-tweaks:docs-health`. Close to resolve; label `wontfix` to suppress future reports of this finding._

<!-- work-fingerprint: docshealth-cac63296 -->

