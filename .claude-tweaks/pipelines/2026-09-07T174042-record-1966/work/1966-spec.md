---
record: 1966
origin: capture
risk: medium
size: medium
ceremony: standard
grants: [build]
---
# 1966: Review Console consoleAutoResolve merges auto:build-only records inconsistently at autonomy unattended

Defer-reason: tangential

Origin: /claude-tweaks:dispatch --budget all drain, run 2026-09-05T090744-dispatch-standalone (14 groups attempted)

## Current State

Under autonomy unattended with consoleAutoResolve, records carrying auto:build but no auto:merge reached different merge outcomes in the same firing with the same policy: 1302, 733, 1442, 1503, 1580, 1774, 1822, 1864 rested at pending-review (merge-authorization resolved ask, or merge-check needs-human), while 1765 and 1775 merged via the console's ordinary "Approve all + merge" resolution (1775 had auto:merge-pending that matured; 1765 had auto:build only). At least one path lets consoleAutoResolve merge a record with no merge grant, contradicting _shared/auto-mode-contract.md's rule that machinery never originates a merge grant.

## Deliverables

- [ ] Trace wrap-up/review-console.md's dispatch-claim branch vs the ordinary console path and _shared/autonomy-ceiling.md's consoleAutoResolve scope; state in one place whether the merge action is in scope for a record without auto:merge.
- [ ] Make the outcome deterministic: an auto:build-only record either always rests pending-review under consoleAutoResolve or always merges when merge-check says auto-merge, and the contract text says which.
- [ ] Test: console fixture with auto:build only, unattended, merge-check auto-merge, asserts the chosen behavior.

## Acceptance Criteria

1. Two runs of the console on identical label/policy inputs produce the same merge decision.
2. The contract text and the console implementation agree on whether consoleAutoResolve may merge an auto:build-only record.

