# #1297: Doc staleness: donts — IL-141

**Doc:** donts | **Section:** IL-141 | **Category:** staleness | **Misleads:** human engineer + coding agent | **Classification:** additive | **Confidence:** high

## Current State

The rule cites `plugin/bin/lib/hooks/git-command.js:293` for `gitTargets()`'s commit/push-only classification behavior. `gitTargets()` is actually defined at line 365 (body confirmed: `if (sub === 'commit' || sub === 'push') targets.push(...)`, matching the rule's substantive claim). Line 293 falls inside an unrelated `-C`-flag-parsing branch of a different function (`skipGlobalFlags`).

## Deliverables

**Current:**
```
`gitTargets()` classifies only the `commit` and `push` subcommands (`plugin/bin/lib/hooks/git-command.js:293`) and `WRITE_SHAPES` names no `git` entry
```

**Proposed:**
```
`gitTargets()` classifies only the `commit` and `push` subcommands (`plugin/bin/lib/hooks/git-command.js:365`) and `WRITE_SHAPES` names no `git` entry
```

## Acceptance Criteria

Correct the stale `git-command.js:293` line-number citation in the IL-141 rule (`docs/donts.md`, the `gitTargets()` bullet) to point at `gitTargets()`'s actual definition, `plugin/bin/lib/hooks/git-command.js:365`.

Filed by `/claude-tweaks:docs-health`.
