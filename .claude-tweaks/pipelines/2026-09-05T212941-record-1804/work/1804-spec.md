---
record: 1804
origin: docs-health
risk: low
size: low
ceremony: fast-lane
grants: [build]
fingerprint: docshealth-b5131791
---
# 1804: Doc staleness: plans/2026-08-27-1368-subagent-contract-violation-investigation-ledger — Open Items — Investigate subagent contract-line violation observed during #1259's build dispatch

**Doc:** plans/2026-08-27-1368-subagent-contract-violation-investigation-ledger | **Section:** Open Items — Investigate subagent contract-line violation observed during #1259's build dispatch | **Category:** staleness | **Misleads:** human engineer + coding agent | **Classification:** additive | **Confidence:** high

Verified-as-of: b47707ca31cc8eae762005f3a365e5dc15e79852

## Current State

Row 1's Item column cites `plugin/bin/lib/hooks/subagent-stop.js:50` as the emission site. Live grep of that file shows the `contract-violation` event is now appended at line 77 (`ctxLib.appendEvent(ownedRun.dir, 'contract-violation', ...)`); line 50 now falls inside an unrelated `tool_use` guard clause added later. A reader following the citation today lands on the wrong code.

## Deliverables

**Current:**
```
`plugin/bin/lib/hooks/subagent-stop.js:50`
```

**Proposed:**
```
`plugin/bin/lib/hooks/subagent-stop.js:77` (line at time of this ledger's writing was 50; the event-emission line has since moved due to unrelated comment additions)
```

## Acceptance Criteria

The doc's cited source-line reference for the contract-violation event emission should point at the line where that code currently lives.

_Filed by `/claude-tweaks:docs-health`. Close to resolve; label `wontfix` to suppress future reports of this finding._

<!-- work-fingerprint: docshealth-b5131791 -->

## Investigation outcome

The record's cited doc — `docs/plans/2026-08-27-1368-subagent-contract-violation-investigation-ledger.md` —
no longer exists in this checkout. It was deleted 2026-09-04 by commit `eda4fd7aa1b`
("Tidy: delete 2 completed execution plans + 8 orphaned pipeline ledgers (#1819)"), because its
one investigation row already carried `Status: accepted` with its resolution already posted as an
issue comment (see the pre-deletion content at `eda4fd7aa1b^:docs/plans/2026-08-27-1368-...md`).

The record's Deliverables (updating a stale `subagent-stop.js:50` → `:77` citation inside that
ledger) therefore has no remaining target — the file to edit is gone, and no substitute location
carries the same citation. No code or doc change is possible or needed.

**Recommendation for review/wrap-up:** close record #1804 as moot (the record's own body already
authorizes this: "Close to resolve; label `wontfix` to suppress future reports of this finding.").
Do not merge this PR as a code change — there is no diff to merge beyond the materialized spec
file itself.
