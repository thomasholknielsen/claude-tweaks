# Staged proposal — 2 parked records with met triggers

**Source:** /claude-tweaks:tidy, 2026-08-13, Step 1 (real trigger evaluation)

1. **#113** — "/tidy has no scan step for orphaned ledger files in docs/plans/". Trigger:
   on-disk presence of orphaned ledgers — confirmed true (~41 unspecified design docs, ~70
   legacy files in docs/superpowers/plans/ still exist). Recommend: **Promote** —
   `/claude-tweaks:specify #113`.

2. **#332** — "[Parked] Policy key rename program — decide after the collapse ships". Trigger:
   #331 (policy key collapse) ships — confirmed closed 2026-08-12. Recommend: re-evaluate the
   program's remaining scope against the now-shipped schema.
