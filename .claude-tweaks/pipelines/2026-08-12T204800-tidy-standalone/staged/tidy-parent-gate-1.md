# Staged proposal — Open parent gate on 6 due parents

**Source:** /claude-tweaks:tidy, 2026-08-13, Step 4.8 (corrected, post plugin-refresh)

| Parent | Title |
|---|---|
| #338 | Parent-issue vocabulary rename — retire family/leaves for parent-issue/sub-issue (ironically, this family's own gate) |
| #306 | Worktree concurrency hardening — one canonical catch-up, not five restatements |
| #293 | Dispatch autonomy — serialized execution, blind review, and branch durability |
| #288 | Merge autonomy/unattended-tier policy levers and batch interactive drills for developer friction |
| #284 | Align /flow's multi-spec Review Console to wrap-up's trace contract |
| #263 | Causal-depth contract: 5-Whys why-chain at confirmed-cause and near-miss time |

All confirmed OPEN, all with `parent-issue` label, all sub-issues closed. Never auto-applied at
any aggressiveness tier — posts a Verification Brief comment + `demo:pending` label, an
outward-facing GitHub write. Re-verify gate state as `due` immediately before acting (don't trust
this scan's snapshot — time has passed). Does not apply `demo:approved`/`demo:changes-requested`;
that stays `/claude-tweaks:demo`'s job per parent.
