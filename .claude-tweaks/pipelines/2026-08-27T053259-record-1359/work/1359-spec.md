---
record: 1359
origin: harness-health
risk: low
size: low
ceremony: fast-lane
grants: [build, merge]
fingerprint: harnesshealth-7aa972e9
---
# 1359: Skill drift: skill-prose-conformance-tests — Reference

**Skill:** skill-prose-conformance-tests | **Section:** Reference | **Category:** drift | **Classification:** additive | **Confidence:** high

Verified-as-of: dc73207933e9b5540d51faf09ee4ee519b5c8751

## Current State

tests/skill-prose-plugin-root-invocations.test.js (added for #1170) sweeps every plugin/skills/**/*.md file for a banned literal (`node plugin/bin/`) against an explicit exemption Map, plus a companion 'stale exemption' test — the identical structural shape already documented as the 'whole-file sweep with allowlist' pattern and cited via tests/frontier-unattended-literal.test.js, but this second instance was never added to the Instances list.

## Deliverables

**Current:**
```
`tests/frontier-unattended-literal.test.js` (whole-file sweep with allowlist), `tests/compare-shell-tweak-lever.test.js`
```

**Proposed:**
```
`tests/frontier-unattended-literal.test.js` (whole-file sweep with allowlist), `tests/skill-prose-plugin-root-invocations.test.js` (a second whole-file-sweep-with-allowlist instance — #1170's ban on repo-relative `node plugin/bin/` invocations in skill prose), `tests/compare-shell-tweak-lever.test.js`
```

## Acceptance Criteria

The Reference > Instances list omits tests/skill-prose-plugin-root-invocations.test.js, a second live instance of the 'whole-file sweep with allowlist' pattern already cited via tests/frontier-unattended-literal.test.js

_Filed by `/claude-tweaks:harness-health`. Close to resolve; label `wontfix` to suppress future reports of this finding._

<!-- work-fingerprint: harnesshealth-7aa972e9 -->
