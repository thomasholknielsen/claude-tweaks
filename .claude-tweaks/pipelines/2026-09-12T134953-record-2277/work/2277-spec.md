---
record: 2277
origin: capture
risk: low
size: low
ceremony: fast-lane
grants: [build]
---
# 2277: tests/impeccable-cli-contract.test.js: pinned CLI version keeps drifting (recurrence of #1900, now 4.1.0)

Origin: pre-existing npm test failure discovered while executing claude-tweaks build,test for #2067 (run 2026-09-11T233824-record-2067)

Defer-reason: pre-existing-outside-diff

## Current State

`tests/impeccable-cli-contract.test.js`'s "the installed CLI matches the pinned version" test fails deterministically on this host: the globally-installed Impeccable CLI is now version 4.1.0, while the test fixtures are pinned to 3.6.0. Confirmed unrelated to #2067's diff (which only touches `plugin/bin/lib/issues/decomposition-crossref.js` and its test file) by diffing the failing test's file path against #2067's changed-file set, and by re-running the file in isolation (`node --test tests/impeccable-cli-contract.test.js`) — it fails identically outside the full suite, so per verification.md's Flake adjudication this is a regression, not load-related flake, just a pre-existing one not caused by any particular diff.

This is a recurrence of the same underlying condition previously filed and closed as #1900 (which recorded installed 4.0.1 vs pinned 3.6.0) — the globally-installed Impeccable CLI on this shared host keeps drifting ahead of the repo's pinned fixture version, so whatever #1900's resolution was, it did not durably prevent the CLI from being upgraded again.

## Deliverables

- [ ] Investigate why the globally-installed `impeccable` CLI on this shared host keeps drifting ahead of the pinned test fixture version (3.6.0) after #1900 — is something (a setup script, a routine, another session) reinstalling/upgrading it, or is 3.6.0 simply stale and due for a deliberate re-pin?
- [ ] Either re-pin the fixtures to the currently-installed version deliberately (re-recording `tests/impeccable-cli-contract.test.js`'s fixtures against 4.1.0, or whatever the current version is by the time this is picked up), or make the drift detection itself resilient to this shared-host recurrence (e.g., a version-floor check instead of an exact-pin check, if that is an intentional contract change) — pick whichever approach the investigation above points to.

## Acceptance Criteria

1. `node --test tests/impeccable-cli-contract.test.js` passes on this host without requiring a manual `npm install -g impeccable@3.6.0` beforehand.
2. A note (commit message or PR description) states which of the two Deliverables approaches was taken and why.

_Filed by `capture` via specShapedBody._
