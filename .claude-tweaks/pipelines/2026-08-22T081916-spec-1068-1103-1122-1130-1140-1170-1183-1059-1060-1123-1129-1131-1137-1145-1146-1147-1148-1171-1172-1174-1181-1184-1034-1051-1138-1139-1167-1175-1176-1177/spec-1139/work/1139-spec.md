---
record: 1139
origin: human
risk: low
size: low
ceremony: fast-lane
grants: []
fingerprint: wrapup-1065-two-rule-paragraph-pin
surface: infra
---
# 1139: Conformance-pin the two-rule CLI-argument-boundary paragraph in pipeline-run-dir.md

Surface: infra
Defer-reason: genuinely-larger

## Current State

`plugin/skills/_shared/pipeline-run-dir.md`'s rewritten CLI-argument-boundary paragraph (in `## Anchoring`, landed with #1065) makes testable factual claims about six binaries — which guard rule each carries, which exit code, which helper (`checkRunDirAnchoredOrOutside` vs `mainCheckoutRoot`/`isAnchoredUnderRoot`) — but no conformance test pins any of it; `tests/pipeline-run-dir-adoption-anchoring.test.js` pins only the Bash-snippet section. `[IL-127]` recurred twice from prose-only defenses, and this paragraph now *describes* a runtime split it cannot enforce (#1065 whole-branch review, Minor #11).

## Deliverables

- [ ] A `node --test` conformance test asserting the paragraph's two bullets exist and name their binaries and exit codes (follow the `skill-prose-conformance-tests` skill's conventions for live-corpus prose pins)
- [ ] The test discriminates: deleting either bullet, or renaming a listed binary, goes red

## Acceptance Criteria

1. The new test fails when the two-rule paragraph's binary lists or exit codes are edited out of agreement with the stated claims, and passes against current prose
2. `npm test` passes in full

## Technical Approach

Follow the `skill-prose-conformance-tests` skill's live-corpus prose-pin conventions directly — read the current `## Anchoring` paragraph's two bullets, and write a test that reads the live file and asserts each bullet still names its binary and exit code. Model it as a sibling of `tests/pipeline-run-dir-adoption-anchoring.test.js` (same file, adjacent test block, or a new file in the same directory) rather than a new pattern.

### Key Files

- `plugin/skills/_shared/pipeline-run-dir.md` — the pinned paragraph
- `tests/` — new conformance test (sibling of `pipeline-run-dir-adoption-anchoring.test.js`)

## Gotchas

- Per the `skill-prose-conformance-tests` skill's own guidance, verify the new test can actually go red before considering it done — write it, then temporarily revert the target paragraph (or a copy) to confirm the test fails, rather than trusting it reads correctly.
- This paragraph exists specifically because `[IL-127]` recurred twice from prose-only defenses — treat the discrimination requirement (deleting a bullet or renaming a binary must go red) as load-bearing, not optional polish.

<!-- work-fingerprint: wrapup-1065-two-rule-paragraph-pin -->

## Original request

Conformance-pin the two-rule CLI-argument-boundary paragraph in pipeline-run-dir.md

Surface: backend
Defer-reason: genuinely-larger

## Current State

`plugin/skills/_shared/pipeline-run-dir.md`'s rewritten CLI-argument-boundary paragraph (in `## Anchoring`, landed with #1065) makes testable factual claims about six binaries — which guard rule each carries, which exit code, which helper (`checkRunDirAnchoredOrOutside` vs `mainCheckoutRoot`/`isAnchoredUnderRoot`) — but no conformance test pins any of it; `tests/pipeline-run-dir-adoption-anchoring.test.js` pins only the Bash-snippet section. `[IL-127]` recurred twice from prose-only defenses, and this paragraph now *describes* a runtime split it cannot enforce (#1065 whole-branch review, Minor #11).

## Deliverables

- [ ] A `node --test` conformance test asserting the paragraph's two bullets exist and name their binaries and exit codes (follow the `skill-prose-conformance-tests` skill's conventions for live-corpus prose pins)
- [ ] The test discriminates: deleting either bullet, or renaming a listed binary, goes red

## Acceptance Criteria

1. The new test fails when the two-rule paragraph's binary lists or exit codes are edited out of agreement with the stated claims, and passes against current prose
2. `npm test` passes in full

### Key Files

- `plugin/skills/_shared/pipeline-run-dir.md` — the pinned paragraph
- `tests/` — new conformance test (sibling of `pipeline-run-dir-adoption-anchoring.test.js`)

<!-- work-fingerprint: wrapup-1065-two-rule-paragraph-pin -->

