---
record: 1051
origin: capture
risk: low
size: low
ceremony: fast-lane
grants: []
surface: infra
---
# 1051: Housekeeping: 3 merged-undeleted remote branches + 5 un-archived clean pipeline run dirs (found via #590's residue sweep)

Surface: infra
Origin: capture

Surfaced by #590's wrap-up residue sweep (`node bin/residue.js --scope blast-radius`, base `25ddb832`, run 2026-08-20T135224-record-590) — mechanical repo housekeeping unrelated to #590's own change, deliberately not fixed inline (`genuinely-larger`: spans other records' worktrees/branches, well beyond this build's scope) and routed to backlog per the ledger's `ledgerRouteRemainder` auto-narrowing capability (`autonomy: unattended`).

## Current State

At sweep time (2026-08-20 ~14:22 UTC), the residue probe reported 8 items, all `remedy: auto`:

**Merged, undeleted remote branches** (`git branch -r --merged origin/main`):
- `origin/worktree-dispatch-record-639`
- `origin/worktree-record-522`
- `origin/worktree-record-737`

**Un-archived, already-clean pipeline run directories** (`run-state.json` status: `clean`, not under `.claude-tweaks/pipelines/archive/`):
- `.claude-tweaks/pipelines/2026-08-20T044330-record-636`
- `.claude-tweaks/pipelines/2026-08-20T044958-spec-967-968-969-970`
- `.claude-tweaks/pipelines/2026-08-20T071039-record-545`
- `.claude-tweaks/pipelines/2026-08-20T073254-record-658`
- `.claude-tweaks/pipelines/2026-08-20T133857-record-203`

Given this repo had 25+ concurrent worktree sessions active at sweep time, act only after re-confirming each item is still stale (re-run `git branch -r --merged origin/main` and re-check each `run-state.json`) — this snapshot will have drifted by the time anyone picks this up.

## Deliverables

- Delete each remote branch above that is still fully merged into `origin/main` at pickup time (`git push origin --delete {branch}`).
- Move each pipeline dir above that still shows `run-state.json` status `clean` into `.claude-tweaks/pipelines/archive/{run-id}/` (`wrap-up/cleanup-procedures-execution.md` Section B's archival move), for any not already archived by their own run or a `/tidy` sweep since.

## Acceptance Criteria

- No merged-and-still-undeleted remote branch remains for any of the three listed (re-verified live, not from this issue's snapshot).
- No `clean`-status pipeline run directory remains outside `.claude-tweaks/pipelines/archive/` for any of the five listed (re-verified live).
- `/claude-tweaks:tidy`'s own orphaned-worktree/branch sweep is the natural home for this if it fires again before a human picks this up — in that case, close this as a duplicate of whatever it files.

## Technical Approach

Re-run the two source commands fresh before acting on anything: `git branch -r --merged origin/main` for the branch list, and a `run-state.json` status check for each named pipeline directory. Only act on items that still match the original snapshot's criteria — branches genuinely merged, directories genuinely `clean`-status and not yet archived. This is pure mechanical cleanup with no code change; `hooks.js archive-run` (or the equivalent `reconcile` convergence pass) is the sanctioned mechanism for the directory moves, not a manual `git mv`.

### Key Files

- No source files — this is repository housekeeping (remote branch deletion, pipeline run directory archival), not a code change.

## Gotchas

- The listed items are a point-in-time snapshot from a repo with 25+ concurrent worktree sessions active — re-verify every item live before acting; do not trust this issue's lists as still-current.
- If `/claude-tweaks:tidy`'s own sweep already picked up and resolved any of these items independently, close this record as a duplicate of whatever `/tidy` filed rather than re-doing the same cleanup.

Defer-reason: genuinely-larger

## Original request

Housekeeping: 3 merged-undeleted remote branches + 5 un-archived clean pipeline run dirs (found via #590's residue sweep)

Origin: capture

Surfaced by #590's wrap-up residue sweep (`node bin/residue.js --scope blast-radius`, base `25ddb832`, run 2026-08-20T135224-record-590) — mechanical repo housekeeping unrelated to #590's own change, deliberately not fixed inline (`genuinely-larger`: spans other records' worktrees/branches, well beyond this build's scope) and routed to backlog per the ledger's `ledgerRouteRemainder` auto-narrowing capability (`autonomy: unattended`).

## Current State

At sweep time (2026-08-20 ~14:22 UTC), the residue probe reported 8 items, all `remedy: auto`:

**Merged, undeleted remote branches** (`git branch -r --merged origin/main`):
- `origin/worktree-dispatch-record-639`
- `origin/worktree-record-522`
- `origin/worktree-record-737`

**Un-archived, already-clean pipeline run directories** (`run-state.json` status: `clean`, not under `.claude-tweaks/pipelines/archive/`):
- `.claude-tweaks/pipelines/2026-08-20T044330-record-636`
- `.claude-tweaks/pipelines/2026-08-20T044958-spec-967-968-969-970`
- `.claude-tweaks/pipelines/2026-08-20T071039-record-545`
- `.claude-tweaks/pipelines/2026-08-20T073254-record-658`
- `.claude-tweaks/pipelines/2026-08-20T133857-record-203`

Given this repo had 25+ concurrent worktree sessions active at sweep time, act only after re-confirming each item is still stale (re-run `git branch -r --merged origin/main` and re-check each `run-state.json`) — this snapshot will have drifted by the time anyone picks this up.

## Deliverables

- Delete each remote branch above that is still fully merged into `origin/main` at pickup time (`git push origin --delete {branch}`).
- Move each pipeline dir above that still shows `run-state.json` status `clean` into `.claude-tweaks/pipelines/archive/{run-id}/` (`wrap-up/cleanup-procedures-execution.md` Section B's archival move), for any not already archived by their own run or a `/tidy` sweep since.

## Acceptance Criteria

- No merged-and-still-undeleted remote branch remains for any of the three listed (re-verified live, not from this issue's snapshot).
- No `clean`-status pipeline run directory remains outside `.claude-tweaks/pipelines/archive/` for any of the five listed (re-verified live).
- `/claude-tweaks:tidy`'s own orphaned-worktree/branch sweep is the natural home for this if it fires again before a human picks this up — in that case, close this as a duplicate of whatever it files.

Defer-reason: genuinely-larger

## Resolution

Re-verified every item live, per this record's own explicit Gotchas instruction, before acting on anything:

1. **Remote branches:** `git fetch origin` then `git branch -r --merged origin/main` shows none of the three listed branches (`worktree-dispatch-record-639`, `worktree-record-522`, `worktree-record-737`). `git ls-remote --heads origin` for all three returns empty — confirmed they no longer exist on origin at all, not merely unmerged.
2. **Pipeline run directories:** all five listed directories are absent from their original `.claude-tweaks/pipelines/{run-id}/` path and present at `.claude-tweaks/pipelines/archive/{run-id}/` instead — already archived.

Every item this record named was already independently resolved by the time this build session picked it up — almost certainly `reconcile`'s own automatic convergence checks (`archive-merged.js` for run-dir archival, `prune-remote.js` for merged-branch deletion), which run continuously across this repo's many concurrent sessions, per the record's own anticipation ("If `/claude-tweaks:tidy`'s own sweep already picked up and resolved any of these items independently, close this as a duplicate"). No specific duplicate issue was found to close against — this resolved via ongoing background automation rather than a single filed fix.

**Ruling:** no action needed. Both Acceptance Criteria are satisfied as of this verification — closing.

