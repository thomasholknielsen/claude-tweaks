---
record: 1167
origin: human
risk: low
size: low
ceremony: fast-lane
grants: []
surface: infra
---
# 1167: Archive 6 un-archived clean pipeline run directories

Surface: infra

## Current State

`residue.js --scope blast-radius` found pipeline run directories under `.claude-tweaks/pipelines/` whose `run-state.json` already reached `status: "clean"` (worktree: null, terminal) but were never moved to `.claude-tweaks/pipelines/archive/{run-id}/` per `wrap-up/cleanup-procedures-execution.md` Section B:

- `.claude-tweaks/pipelines/2026-08-20T193627-record-355` (PR #1149)
- `.claude-tweaks/pipelines/2026-08-20T193632-record-345` (PR #1155)
- `.claude-tweaks/pipelines/2026-08-20T193642-record-231` (PR #1165)
- `.claude-tweaks/pipelines/2026-08-20T204530-record-951` (PR #1157)
- `.claude-tweaks/pipelines/2026-08-20T211123-record-1011` (PR #1159)
- `.claude-tweaks/pipelines/2026-08-20T193645-record-230` (PR #1166, this record's own run — gitignored content already moved to `archive/2026-08-20T193645-record-230/`; only the tracked `work/230-spec.md` subdirectory remains, needing `git mv`)

`remedy: auto` per `residue.js`, but the run that found this (#230, `worktree-record-230`) could not fix it: Section B step 4's archival requires `git mv` on each run's tracked `work/*-spec.md` into the archive path, from the **main checkout** — a git operation a worktree-isolated session cannot legally perform (confirmed: a git command targeting the main checkout is refused there), and that run was explicitly instructed not to provision a scratch worktree.

## Deliverables

For each of the 6 run directories: `git mv {run-dir}/work/ .claude-tweaks/pipelines/archive/{run-id}/work/`, then `mv` any remaining gitignored contents (`config.yml`, `decisions.md`, `events.jsonl`, `engine-state.json`, `staged/`) into the same archive path, from the main checkout (or any session not worktree-isolated from it) — per `wrap-up/cleanup-procedures-execution.md` Section B steps 4-6. Note: `staged/` for at least one of these (`2026-08-20T193627-record-355`) already holds a leftover proposal of this exact same class (`ledger-record-archive-clean-pipeline-runs.md`) — preserve it during the move per Section B step 6 ("skipped staged items remain in the archive; NOT silently dropped").

## Acceptance Criteria

- All 6 directories exist under `.claude-tweaks/pipelines/archive/{run-id}/` instead of `.claude-tweaks/pipelines/{run-id}/`
- Each `work/` subdirectory's git history is preserved (rename, not delete+recreate)
- No staged proposal is dropped in the process

## Technical Approach

Run `wrap-up/cleanup-procedures-execution.md` Section B steps 4-6 verbatim for each of the 6 listed run directories, from the main checkout (never worktree-isolated, since step 4's `git mv` targets the main checkout by construction). Re-verify each directory's `run-state.json` status is still `clean` immediately before moving it — this snapshot is from an earlier point and may have partially resolved already (via `/tidy`, `reconcile`, or another session).

### Key Files

- No source files — this is repository housekeeping (`git mv` + gitignored-content moves into `.claude-tweaks/pipelines/archive/`), following `wrap-up/cleanup-procedures-execution.md` Section B's documented procedure exactly.

## Gotchas

- Re-verify each of the 6 directories' `run-state.json` status live before acting — this record's list is a snapshot from #230's own wrap-up sweep, and `/tidy`'s own sweep (or `reconcile`'s convergence pass) may have already archived some of them independently since.
- `2026-08-20T193627-record-355`'s `staged/` directory holds a pre-existing proposal for this exact same archival class — preserve it during the move, don't overwrite or drop it.
- This must run from the main checkout (or any non-worktree-isolated session) — the `git mv` on each run's tracked `work/` subdirectory is refused from inside a linked worktree, which is exactly why record #230's own run couldn't fix this inline.

## Decision-log reference

AUTO — record #230's `/wrap-up` Phase 3 Residue Sweep: 5 (later 6, including #230's own) un-archived clean pipeline run dirs found, fix-now rejected (worktree-isolation), classified blocked-external, routed to backlog via ledgerNarrowing (ceiling: unattended).

## Original request

Archive 6 un-archived clean pipeline run directories

`residue.js --scope blast-radius` found pipeline run directories under `.claude-tweaks/pipelines/` whose `run-state.json` already reached `status: "clean"` (worktree: null, terminal) but were never moved to `.claude-tweaks/pipelines/archive/{run-id}/` per `wrap-up/cleanup-procedures-execution.md` Section B:

- `.claude-tweaks/pipelines/2026-08-20T193627-record-355` (PR #1149)
- `.claude-tweaks/pipelines/2026-08-20T193632-record-345` (PR #1155)
- `.claude-tweaks/pipelines/2026-08-20T193642-record-231` (PR #1165)
- `.claude-tweaks/pipelines/2026-08-20T204530-record-951` (PR #1157)
- `.claude-tweaks/pipelines/2026-08-20T211123-record-1011` (PR #1159)
- `.claude-tweaks/pipelines/2026-08-20T193645-record-230` (PR #1166, this record's own run — gitignored content already moved to `archive/2026-08-20T193645-record-230/`; only the tracked `work/230-spec.md` subdirectory remains, needing `git mv`)

`remedy: auto` per `residue.js`, but the run that found this (#230, `worktree-record-230`) could not fix it: Section B step 4's archival requires `git mv` on each run's tracked `work/*-spec.md` into the archive path, from the **main checkout** — a git operation a worktree-isolated session cannot legally perform (confirmed: a git command targeting the main checkout is refused there), and that run was explicitly instructed not to provision a scratch worktree.

## Deliverables

For each of the 6 run directories: `git mv {run-dir}/work/ .claude-tweaks/pipelines/archive/{run-id}/work/`, then `mv` any remaining gitignored contents (`config.yml`, `decisions.md`, `events.jsonl`, `engine-state.json`, `staged/`) into the same archive path, from the main checkout (or any session not worktree-isolated from it) — per `wrap-up/cleanup-procedures-execution.md` Section B steps 4-6. Note: `staged/` for at least one of these (`2026-08-20T193627-record-355`) already holds a leftover proposal of this exact same class (`ledger-record-archive-clean-pipeline-runs.md`) — preserve it during the move per Section B step 6 ("skipped staged items remain in the archive; NOT silently dropped").

## Acceptance Criteria

- All 6 directories exist under `.claude-tweaks/pipelines/archive/{run-id}/` instead of `.claude-tweaks/pipelines/{run-id}/`
- Each `work/` subdirectory's git history is preserved (rename, not delete+recreate)
- No staged proposal is dropped in the process

## Decision-log reference

AUTO — record #230's `/wrap-up` Phase 3 Residue Sweep: 5 (later 6, including #230's own) un-archived clean pipeline run dirs found, fix-now rejected (worktree-isolation), classified blocked-external, routed to backlog via ledgerNarrowing (ceiling: unattended).

_Filed by `wrap-up` via specShapedBody._


**Related:** #1164 #1152

## Resolution

Re-verified all 6 listed run dirs' live state from the main checkout (plain `node -e` fs reads, no git — the Gotchas explicitly call for a live re-check before acting). Result:

- 4/6 (`record-355`, `record-345`, `record-951`, `record-230`) are already correctly archived at `.claude-tweaks/pipelines/archive/{run-id}/`, single level, expected contents (`config.yml`, `decisions.md`, `work/`, etc.) — resolved independently since this record's snapshot, presumably by a `/tidy` or `reconcile` sweep. `record-355`'s pre-existing `staged/ledger-record-archive-clean-pipeline-runs.md` proposal is confirmed still present, per the Deliverables' preserve-it note.
- 2/6 (`record-231`, `record-1011`) are archived but **double-nested** (`archive/{id}/{id}/...` instead of `archive/{id}/...`) — the exact corruption shape #1103 fixed the root cause of; this is pre-existing residual corruption predating that fix, not a recurrence. This is a different defect than "un-archived," so this record's literal ask (archive 6 un-archived dirs) is now moot for all 6 — none are un-archived.

Attempting the fix myself confirmed the same structural blocker this record's own Decision-log documents: a plain `git -C <main-checkout> status` is refused outright by the worktree-isolation guard (not just a complexity limit — a direct, minimal command redirecting to the main checkout is rejected). This session cannot perform the required `git mv` on the git-tracked `work/*.md` content.

Filed #1311 to track the 2 corrupted directories specifically (Defer-reason: blocked-external, same classification as this record's own precedent), rather than silently dropping them. #1167 itself is resolved — its specific scope (6 un-archived dirs) no longer exists.

