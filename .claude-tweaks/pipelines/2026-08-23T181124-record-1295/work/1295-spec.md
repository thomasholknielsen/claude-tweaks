---
record: 1295
origin: human
risk: low
size: medium
ceremony: standard
grants: [build]
---
# 1295: capture: split SKILL.md off the byte ceiling; finish the local-files absorb mapping and ordering prose

**Related:** #1296

## Overview

Spec #1264 (absorb-by-default) landed with `plugin/skills/capture/SKILL.md` at 40,956 of the 40,960-byte hard ceiling (`tests/bin-lib/skill-audit/context-cost.test.js`) — 4 bytes of headroom. Its final review's fix wave had to fund every addition by trimming other sentences in the same file, and two of its findings could only be partially closed inside that budget. This record covers the split that restores headroom plus the two byte-deferred completions.

Defer-reason: genuinely-larger

Origin: spec #1264's final whole-branch review (findings F8 and N2, byte-deferred by controller ruling; refs #1264)

## Current State

- `plugin/skills/capture/SKILL.md` is 40,956/40,960 bytes. Any future edit must trim first; two consecutive fix rounds during #1264 spent hours funding spec-mandated sentences byte-by-byte. This is the same disease #611 documents for `specify/decomposition-mode.md` + `record-creation.md` — capture is now the third file at the wall.
- The absorb rules are `github-issues`-shaped. The shipped byte-funded fallback is one scoping sentence ("The recommended-absorb ordering applies under `github-issues` only"), but the headless bar (criterion (b) keyed on the `type:{t}` label) and the three exclusions (`parent-issue`/`bot:in-progress` labels) read as driver-agnostic while being unevaluable under `local-files` — AC3's exclusions forbid nothing on that driver.
- The filing-time ordering of the headless absorb decision is stated once, inside the Immediate Routing section's headless-bar sentence — but `## Immediate Routing`'s own opener still says "After adding the record, route the item…" and Workflow Step 1 files-then-chains with no forward pointer to the bar (review finding N2: correct sentence, un-brought-along neighbors).

## Deliverables

- [ ] Split `plugin/skills/capture/SKILL.md` — the natural seam is Immediate Routing + absorb mechanics into a lazy-loaded sub-file (`capture/routing.md` or similar), mirroring how other skills hive off procedure files; SKILL.md keeps the summary + pointer. Target ≥4KB headroom in every resulting file.
- [ ] With bytes freed, replace the `github-issues`-only scoping sentence with the full local-files mapping: criterion (b) reads `facets.type`; the exclusions read the local facets/closed state; the recency tie-break uses the record file's mtime; the `size:` re-judge and output-naming rules apply on both drivers.
- [ ] Align the ordering prose: `## Immediate Routing`'s opener and Workflow Step 1 acknowledge the headless bar's filing-time pre-emption (an absorbing capture never files or chains) instead of asserting unconditional file-then-route.
- [ ] `tests/capture-absorb-default.test.js` extended to pin the local-files mapping and the opener alignment; existing 17 pins stay green.

## Acceptance Criteria

1. Every `plugin/skills/capture/**` file ≤ 36,000 bytes (real headroom, not ceiling-grazing); `tests/bin-lib/skill-audit/context-cost.test.js` green.
2. `grep -in "github-issues\` only" plugin/skills/capture/` returns no absorb-scoping hit (the fallback sentence is replaced by the mapping).
3. The 17 existing capture-absorb pins pass unmodified except where the split moves their target file (path updates only, no assertion weakening).
4. `npm test` passes.

_Filed by the spec #1264 pipeline's final-review adjudication (controller), via the deferral gate. Refs #1264, #611._

