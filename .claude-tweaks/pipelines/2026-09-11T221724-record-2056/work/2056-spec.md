---
record: 2056
origin: harness-health
risk: low
size: low
ceremony: fast-lane
grants: [build, merge]
fingerprint: harnesshealth-00c84acc
---
# 2056: Skill drift: run-directory-fact-packs — The shape

**Skill:** run-directory-fact-packs | **Section:** The shape | **Category:** drift | **Classification:** additive | **Confidence:** high

Verified-as-of: b497a80931452e03ceda09f1a1f5d2119acf850b

## Current State

grep confirms wrap-up-pack.js's USAGE string includes `[--only <probe,...>]` and pack.js exports/filters by PROBE_NAMES, while flow-preflight.js's USAGE string is `--run <dir> --steps <a,b,c> [--json <path>]` (no --only) and flow/preflight.js's gatherPreflight runs `Promise.all(names.map(...))` over every key of `probes` with no filtering — its own parseArgs error text states --steps is 'metadata — every field is computed regardless.'

## Deliverables

**Current:**
```
- **`--only <probe,...>`** so a consumer that needs one field does not pay for eight.
```

**Proposed:**
```
- **`--only <probe,...>`** so a consumer that needs one field does not pay for eight — `wrap-up-pack.js`/`pack.js` only; `flow-preflight.js` has no `--only` flag, and `flow/preflight.js`'s `gatherPreflight` computes every probe unconditionally regardless of `--steps` (its own parse-error text calls `--steps` "metadata — every field is computed regardless"), since `/flow`'s second call always needs the full set.
```

## Acceptance Criteria

The skill claims the two shipped fact packs "agree on every rule below," including the `--only <probe,...>` selective-probe rule, but `flow-preflight.js`/`flow/preflight.js` implements no such flag and always computes every probe. Fix the `--only` bullet to scope the rule to `pack.js`/`wrap-up-pack.js`, and soften the opening paragraph's blanket "agree on every rule below" claim to note this one exception.

_Filed by `/claude-tweaks:harness-health`. Close to resolve; label `wontfix` to suppress future reports of this finding._

<!-- work-fingerprint: harnesshealth-00c84acc -->
