---
record: 1413
origin: human
risk: medium
size: low
ceremony: fast-lane
grants: [build]
---
# 1413: hooks: checkBookkeepingStampsGate record-pr branch deadlocks the sanctioned PR-early Step 2 push

Defer-reason: tangential

Origin: reflect full from #1376 (run 2026-08-24T163411-record-1376)

## Current State

`bin/lib/hooks/pre-tool-use.js`'s `checkBookkeepingStampsGate` (record-pr branch, ~line 983) denies covered git pushes while `run-state.json`'s `pr` field is unset — but `_shared/pr-early-run-lifecycle.md` Step 2's `git push` is itself the bootstrap write that must land before any draft PR can be created and recorded, so the sanctioned PR-early lifecycle deadlocks in the owning session. The only escape today is logging the FAILED degrade line (`hasLoggedPrDegrade`) and retrying, which run 2026-08-24T163411-record-1376 hit live during #1376's build (decisions.md 18:42:41 FAILED then 18:43:17 retry success; two `bookkeeping-stamp-deny` events in events.jsonl). Causal verdict from the wrap-up near-miss walk: systemic — the gate's coverage condition was authored without exempting the lifecycle's own bootstrap step.

## Deliverables

- An exemption (or equivalent redesign) in `checkBookkeepingStampsGate`'s record-pr branch for the PR-early Step 2 bootstrap push, so a pr-first run's first push proceeds without a logged FAILED degrade + blind retry.
- A regression test pinning both sides: the bootstrap push (no `pr` recorded yet) is allowed; a covered push after `pr` is recorded remains gated exactly as today.

## Acceptance Criteria

- A pr-first run's first branch push with `run-state.json.pr` unset is not denied by `checkBookkeepingStampsGate`, and no FAILED degrade line is logged for it.
- Post-PR covered pushes are still gated (existing behavior pinned by test).
- `npm test` green including the new pin test.

_Filed by `reflect` via specShapedBody._
