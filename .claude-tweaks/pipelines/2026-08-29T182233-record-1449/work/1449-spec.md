---
record: 1449
origin: human
risk: low
size: low
ceremony: fast-lane
grants: [build]
---
# 1449: review lens dispatch: name the degrade path when a reproduction-pair agent dies to a session/usage limit

Origin: reflect full from #1391
Defer-reason: tangential

## Current State

`review/step3-lens-dispatch.md` dispatches each lens as a 2-agent reproduction pair and says a failed dispatch must never aggregate as a clean result (`_shared/subagent-output-contract.md`'s "Failed-agent retrieval"), but neither file says what the dispatcher does when one half of a pair terminates on an account session/usage limit (`Agent terminated early due to an API error: You've hit your session limit`). On #1391's review two agents (3a-B, 3b-A) died that way, a retry died the same way, and the run proceeded by applying the Direct-verification override to the surviving partner's findings — a reasonable call, but improvised, unlogged as a coverage gap until the summary, and not what a headless dispatch firing would know to do.

## Deliverables

- [ ] `review/step3-lens-dispatch.md`: one paragraph after "Failed-agent retrieval" naming the limit-termination case — retry once; on a second failure treat the surviving agent as a Low-tier single read for that lens (its findings enter `unconfirmed` unless direct-verified), log `STAGED … lens {lens}: partner agent terminated on a session limit twice; single-read coverage` to `decisions.md`, and carry a coverage-caveat line into the Step 7 summary and the PR verdict comment.
- [ ] `_shared/subagent-output-contract.md` "Failed-agent retrieval": name the session-limit signature as a terminal (non-retryable-now) failure class distinct from a transient 5xx.
- [ ] A prose-conformance pin for the new paragraph with a frozen control and a go-red proof.

## Acceptance Criteria

1. `grep -n -i "session limit" plugin/skills/review/step3-lens-dispatch.md plugin/skills/_shared/subagent-output-contract.md` is non-empty in both files.
2. The new literal prints 0 on `git show {base}:{file} | grep -c -F` and ≥ 1 at HEAD.
3. `npm test` passes.

_Filed by `reflect` via specShapedBody._

