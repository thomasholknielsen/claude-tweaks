---
record: 1429
origin: harness-health
risk: low
size: low
ceremony: standard
grants: [build]
fingerprint: harnesshealth-d636c859
---
# 1429: CLAUDE.md drift: CLAUDE — Subagent Contract (v4.2+)

**CLAUDE.md:** CLAUDE | **Section:** Subagent Contract (v4.2+) | **Category:** drift | **Classification:** additive | **Confidence:** high

Verified-as-of: d6b7e03cdd222d6a102d6faa42f1fca39d40b02d

## Current State

plugin/skills/research/source-registry.md's Dispatch section explicitly cites subagent-output-contract.md for its parallel Task-agent fan-out (one agent per question×source pair), resolves a model profile per the contract's Model Selection section, and requires the same four-value status line as its first reply line — but /research is absent from CLAUDE.md's enumerated 'Used by' list of dispatch sites, which the file's own closing sentence ('When adding a new dispatch site, follow the full pattern') implies should be exhaustive.

## Deliverables

**Current:**
```
`/help`, `/init`, `/review`, `/simplify`
```

**Proposed:**
```
`/help`, `/init`, `/research` (question×source pair fan-out, a define-in-prompt format inlined verbatim per the contract's "Not every consumer uses A/B/C" allowance — `plugin/skills/research/source-registry.md`'s Dispatch section), `/review`, `/simplify`
```

## Acceptance Criteria

The Subagent Contract's 'Used by' roster omits /claude-tweaks:research, which dispatches parallel Task agents (one per question×source pair) and explicitly follows plugin/skills/_shared/subagent-output-contract.md

_Filed by `/claude-tweaks:harness-health`. Close to resolve; label `wontfix` to suppress future reports of this finding._

<!-- work-fingerprint: harnesshealth-d636c859 -->
