---
record: 1865
origin: human
ceremony: fast-lane
grants: [build]
---
# 1865: Committed #703 spec + implementation plan name the shipped handler checkPostTeardownPin; it shipped as checkPostTeardownReanchor

## Current State

Two committed historical artifacts from #703's continuation (commit range `42a8bbb05..afd33e3e9`) name the post-teardown handler `checkPostTeardownPin` throughout, but the function that actually shipped in `plugin/bin/lib/hooks/post-tool-use.js:578` is `checkPostTeardownReanchor`:

- `.claude-tweaks/pipelines/2026-09-04T095553-record-703/work/703-spec.md` line 89 (Investigation Findings / Task 0 section)
- `docs/superpowers/plans/2026-09-04-worktree-teardown-pin-backstop.md` lines 7, 29, 128, 172, 208-209, 224, 250, 269 (including a full function signature)

Found by the whole-branch review gating the v6.116.0 release. Low severity — pure name drift in historical audit-trail documents, no functional impact.

## Deliverables

- Update both files to name the handler `checkPostTeardownReanchor` (or, if these are treated as immutable historical spec/plan artifacts per this repo's convention, add a one-line correction note instead of rewriting history).

## Acceptance Criteria

- Grepping either file for the actual shipped symbol name returns a hit.

## Failure scenario this fixes

A future maintainer greps for `checkPostTeardownPin` (per these permanently-committed documents' own text) to find or modify the post-teardown handler, gets zero hits in any `.js` source, and wrongly concludes the feature was never shipped or was silently removed.
