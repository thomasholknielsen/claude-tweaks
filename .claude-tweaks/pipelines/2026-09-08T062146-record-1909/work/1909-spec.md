---
record: 1909
origin: capture
risk: low
size: high
ceremony: standard
grants: [build]
---
# 1909: Instruction-prose diet: operative skill text states the rule and the IL tag, provenance narrative moves out, per-pipeline-step loaded-bytes budget

Origin: brainstorm on #1904 (docs/superpowers/specs/2026-09-05-pipeline-ceremony-cost-design.md, "Filed separately")
Defer-reason: genuinely-larger

**Related:** #1987

## Current State

A `/claude-tweaks:flow` second call (`review,polish,wrap-up`) loads several hundred KB of skill prose before doing any work: `plugin/skills/_shared/` is 1.3 MB, `wrap-up/` alone is 408 KB, `flow/` 348 KB, and each 40 KB file is roughly 10k tokens. Reading the operative files shows that roughly half of most of them is provenance rather than procedure: which record moved a paragraph ("extracted for #887", "split out per #635"), which commit deleted a design doc ("deleted `70849915`"), and retold incident narratives that `docs/incident-log.md` already holds under their `[IL-nn]` tag. The context-cost test (`bin/lib/skill-audit/tests/context-cost.test.js`) caps bytes per file but nothing measures bytes loaded per pipeline step, so the procedure-vs-provenance ratio is invisible. #1765 (fast-lane digest) addresses the same waste for the fast-lane path only; this record is the general case. Tokens, not minutes: prose loading costs money and context headroom (compaction pressure), not wall-clock.

This record is Phase 5 of #1987 (Skill context composer — per-run bundles composed by resolved condition, not per-file ceilings): #1987's Phases 1-4 ship the composer mechanism and its per-step measurement; this record consumes that measurement and does the prose-diet and scaffolding-dedupe pass on top of it.

## Deliverables

- [ ] A stated authoring rule in `docs/skill-authoring.md`: operative text (`## Step N` bodies, gate tables, Next Actions) states the rule and cites the `[IL-nn]` tag or record number; the narrative of why lives in `docs/incident-log.md` or `docs/decisions/`, never restated inline. One sentence of rationale per rule is allowed; a paragraph is not.
- [ ] Per-step composed-bytes measurement is delivered by #1990 (Per-step composed-bytes measurement in context-cost.js) — this record consumes that measurement, not builds it.
- [ ] A first trimming pass over the five pipeline skills plus the `_shared` files they cite, moving provenance out per the rule, with the conformance tests that pin the moved prose updated in the same change (the real cost of this record — every trim moves pins).
- [ ] Before/after bytes per step recorded in the PR description.
- [ ] Inject the Interaction-style directive once from the `SessionStart` hook (`plugin/bin/lib/hooks/session-start.js`) and delete the 35 verbatim copies (501 B each) from every SKILL.md, with the conformance tests that pin that paragraph retargeted to the hook output.
- [ ] Audit the per-skill `## Input`/`## Anti-Patterns`/`## Component-Skill Contract`/`## Next Actions` scaffolding (~260 KB of the 888 KB SKILL.md payload) with `bloat.js`'s existing provenance signals and trim author-facing narration.

## Acceptance Criteria

- [ ] `docs/skill-authoring.md` carries the rule and `harness-health` reports loaded bytes per pipeline step.
- [ ] The five pipeline skills' default-path loaded bytes drop by at least 25% with no change to which steps execute, verified by the existing conformance suites passing after pin updates.
- [ ] No `[IL-nn]` narrative is lost: every trimmed retelling still resolves to its incident-log entry by tag.

## Technical Approach

### Key Files

- `docs/skill-authoring.md`
- `plugin/bin/lib/skill-audit/`
- `plugin/skills/flow/SKILL.md`, `plugin/skills/flow/steps-and-gates.md`
- `plugin/skills/build/SKILL.md`
- `plugin/skills/review/SKILL.md`, `plugin/skills/review/code-mode-steps.md`
- `plugin/skills/wrap-up/SKILL.md`
- `plugin/skills/test/verification.md`
- `plugin/skills/_shared/subagent-output-contract.md`, `plugin/skills/_shared/pipeline-run-dir.md`
- `plugin/bin/lib/hooks/session-start.js`
- `plugin/bin/lib/skill-audit/bloat.js`
- `tests/` (prose conformance suites pinning the trimmed text)

## Blocked / Future Work

Shipped this build (verified, tests passing):

- Authoring rule added to `docs/skill-authoring.md` ("Instruction-prose diet: operative text vs. provenance narrative" section) — operative text states the rule + cites `[IL-nn]`/record number, narrative stays in `docs/incident-log.md`/`docs/decisions/`.
- The Interaction-style directive (was 36 byte-identical ~501B copies, one per `SKILL.md`) moved to a single source (`plugin/bin/lib/hooks/interaction-style.js`) injected unconditionally by the SessionStart hook (`plugin/bin/lib/hooks/session-start.js`). Every `SKILL.md` had its inline copy removed (~18 KB total across 36 files). Five conformance-test sites retargeted from "every skill carries it" to "no skill carries it inline" (`tests/skill-conventions.test.js`, `tests/bin-lib/skill-audit/house-structure.test.js`, `tests/research/skill-md.test.js`, `tests/routine-kickoff.test.js`, `tests/bin-lib/health-core/skill-md-house-checks.js`), plus a new pin in `tests/hooks-session-start.test.js` asserting the hook always injects it.
- `harness-health` now reports composed bytes per pipeline step (AC1): a new `plugin/bin/context-cost-report.js` CLI wraps `context-cost.js`'s existing `composedBytesReport()` (#1990), wired into `harness-health/SKILL.md`'s Workflow (a new reporting-only check, no issue filed — the hard gate is the existing `context-cost.test.js` suite) and surfaced in Step 8's SUMMARIZE line.
- A stray line-number-pinned test (`tests/sweep-orchestrator.test.js`, backlog/SKILL.md line 104→103) updated for the line shift the directive removal caused; content unchanged, only its line number moved.

**Not done this build — deferred, not silently dropped:**

- **AC2 (≥25% loaded-bytes drop across the five pipeline skills, `flow`/`build`/`test`/`review`/`wrap-up`) is unmet.** Measured: the Interaction-style dedup above removes ~18 KB total from the whole 36-skill `SKILL.md` corpus — real, but a small fraction of any one pipeline skill's own footprint (e.g. `wrap-up/` alone is ~408 KB). Checked whether `composedBytesReport()`'s per-compose-call-site bytes moved for the affected steps (`worktree-setup`, `manifesto`, `claims`, `pr-scan`, `merge`) — they did not: none of those compose bundles include a `SKILL.md` file as a source, so this measurement doesn't capture the dedup's effect at all. The AC's "loaded bytes per pipeline step" appears to mean the fuller footprint the record's own Current State describes (a skill directory's total bytes read across a `/flow` step, not just its `compose-context.js` bundles) — a different, broader measurement than what #1990 built, and reaching a 25% reduction on it needs the actual "first trimming pass" deliverable below, done for real across the five skills plus every `_shared` file they cite, each trim paired with its own conformance-test pin update (the same one-file-at-a-time process this build used for the Interaction-style directive, at roughly 40x the file count and with far less mechanical, more judgment-dependent edits — provenance narrative is prose, not a byte-identical string to grep for).
- **Deliverable "first trimming pass over the five pipeline skills + `_shared` files they cite"** — not attempted beyond the Interaction-style dedup above. Scoping a real pass safely (moving provenance narrative to `docs/incident-log.md`/`docs/decisions/` citations without losing content, and updating every prose-conformance test that pins the moved text) is itself a multi-session effort; attempting it inside this single build risked either an unsafe rush or leaving it half-edited mid-file.
- **Deliverable "scaffolding audit with `bloat.js`'s provenance signals"** — not attempted.

Filed as a follow-up: `/claude-tweaks:capture` — see the build handoff for the record link. `Defer-reason: genuinely-larger` (same reason this record's own parent used) — the remaining scope is the bulk of the original record and needs its own `/claude-tweaks:specify` pass to decompose per-skill, not a single sub-issue.

