---
record: 1726
origin: docs-health
risk: low
size: low
ceremony: fast-lane
grants: [build]
fingerprint: docshealth-b998cde1
---
# 1726: Doc staleness: plans/2026-08-25-record-1094-ledger — Open Items

**Doc:** plans/2026-08-25-record-1094-ledger | **Section:** Open Items | **Category:** staleness | **Misleads:** human engineer + coding agent | **Classification:** additive | **Confidence:** high

Verified-as-of: e0d4adedacc8dd7b8ab2a0e23930b6353cfeb049

## Current State

Live code at plugin/bin/lib/feedback/file-feedback.js:168 already reads `'--limit', '10000'` with an adjacent comment block (lines 161-166) stating '#1094 review finding: `--limit 500` already truncated ~half of this repo's then-998 issues' as the rationale for the widened limit — introduced in commit d51e0863c ('Resolve run-ledger row 7 — both dispatch-descriptor halves landed', 2026-08-30, after this ledger's 2026-08-25 date). The ledger's row 1 still lists Status: open, Resolution: — even though the fix it describes has shipped.

## Deliverables

**Current:**
```
| 1 | review | `findDuplicate`'s new list-then-filter query (`plugin/bin/lib/feedback/file-feedback.js:144`) has no `--label` scope, unlike the `by:{caller}`-scoped precedent it cites (`_shared/headless-self-report.md:43`) — an unscoped `--state all --limit 500` window can truncate away an older duplicate in this repo's 1400+-issue history, silently reintroducing dedup-miss via a different mechanism than the `--search` staleness this record fixes | open | — |
```

**Proposed:**
```
| 1 | review | `findDuplicate`'s new list-then-filter query (`plugin/bin/lib/feedback/file-feedback.js:144`) has no `--label` scope, unlike the `by:{caller}`-scoped precedent it cites (`_shared/headless-self-report.md:43`) — an unscoped `--state all --limit 500` window can truncate away an older duplicate in this repo's 1400+-issue history, silently reintroducing dedup-miss via a different mechanism than the `--search` staleness this record fixes | resolved | `--limit` widened to 10000 (label-scoping deliberately not applied — this caller has no reliable fixed label to scope by); see `plugin/bin/lib/feedback/file-feedback.js`'s `findDuplicate` comment |
```

## Acceptance Criteria

Update row 1's Status/Resolution to reflect that the underlying finding was already fixed: `findDuplicate` in `plugin/bin/lib/feedback/file-feedback.js` now calls `gh issue list` with `--limit 10000` (not 500), and the surrounding comment explicitly cites this ledger's #1094 review finding as the reason, noting `--label` scoping was deliberately not applied because this caller has no reliable fixed label to scope by.

_Filed by `/claude-tweaks:docs-health`. Close to resolve; label `wontfix` to suppress future reports of this finding._

<!-- work-fingerprint: docshealth-b998cde1 -->
