---
record: 1967
origin: capture
risk: medium
size: low
ceremony: standard
grants: [build]
---
# 1967: task-prompt.md should forbid git stash in dispatched calls: the stash stack is shared across worktrees

Defer-reason: tangential

Origin: /claude-tweaks:dispatch --budget all drain, run 2026-09-05T090744-dispatch-standalone (14 groups attempted)

## Current State

The 1864 review call ran `git stash` / `git stash pop` to prove test discrimination and popped a sibling worktree's WIP stash (the stash stack is repository-wide, shared by every worktree). It recovered without loss, but memory git-stash-shared-across-worktrees already records this hazard and nothing in task-prompt.md or the build/review templates warns a dispatched agent. Four other calls this firing also used stash for baseline comparison.

## Deliverables

- [ ] Add to both task-prompt.md templates and build/dispatch.md's implementer prompt: never use git stash; use `git show <rev>:<path>` or a temporary WIP commit to compare against a baseline.
- [ ] Add a PreToolUse warn (not deny) in bin/lib/hooks/pre-tool-use.js for bare `git stash` / `git stash pop` from a linked worktree, pointing at the tagged-push-then-apply-by-sha form.
- [ ] Pin the template text with a conformance test.

## Acceptance Criteria

1. Both dispatch templates contain the no-stash instruction.
2. A bare `git stash pop` from a linked worktree emits the warning line.
3. Existing pre-tool-use tests pass.

