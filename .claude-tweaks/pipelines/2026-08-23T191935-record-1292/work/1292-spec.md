---
record: 1292
origin: human
risk: low
size: low
ceremony: fast-lane
grants: [build]
surface: backend
---
# 1292: harness-health-analysis check-9 snippet: "glob matching nothing is skipped" claim is bash-only — zsh nomatch aborts the command

## Current State

`plugin/skills/_shared/harness-health-analysis.md` (line ~147, check-9's path-existence snippet)
instructs listing both skill layouts (`"<root>"/skills/*.md` and `"<root>"/skills/*/*.md`) on the
stated premise that "a glob matching nothing is skipped rather than erroring." That premise is
bash-specific: under zsh (this project's shell), an unmatched glob triggers `no matches found` and
aborts the whole command before it runs. Observed live during record #1164's wrap-up
skills-curation pass: the check failed twice until the flat-layout glob was dropped by hand. The
procedure is consumed by /claude-tweaks:wrap-up (Skills row), /claude-tweaks:init, and
/claude-tweaks:harness-health, so every consumer inherits the failure on zsh machines with
single-layout skill dirs.

## Deliverables

- Fix the snippet so it is shell-safe: either guard each glob (e.g. a per-layout probe that
  cannot nomatch-abort, or a `find`-based enumeration), or correct the prose to state the zsh
  behavior and instruct probing each layout separately.
- Sweep the same "glob matching nothing" premise across `_shared/*.md` for other occurrences.

## Acceptance Criteria

- The check-9 snippet, run verbatim under zsh in a project with only one skill layout present,
  exits 0 and lists that layout's files (no `no matches found` abort).
- No remaining `_shared/*.md` prose asserts unmatched-glob skipping without naming the shell it
  holds for.

Defer-reason: genuinely-larger

_Filed by `/claude-tweaks:wrap-up` (record #1164 skills-curation observation, queueWriteAutoFile at autonomy: unattended) via specShapedBody._
