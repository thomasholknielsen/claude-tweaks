---
record: 1197
origin: docs-health
risk: low
size: low
ceremony: fast-lane
grants: [build, merge]
fingerprint: docshealth-adcbda89
---
# 1197: Doc staleness: decisions/0013-plugin-conventions-detect-prior-art-before-writing — Decision

**Doc:** decisions/0013-plugin-conventions-detect-prior-art-before-writing | **Section:** Decision | **Category:** staleness | **Misleads:** human engineer + coding agent | **Classification:** additive | **Confidence:** high

## Current State

The Decision section states the answer is recorded in `doc-convention.{genre}` (dotted). Live code (`plugin/bin/lib/policy-schema.js` line 250) lists `{ key: 'doc-convention.adr', replacedBy: 'doc-convention-adr', migrate: ... }` in RENAMED_KEYS, and `plugin/skills/_shared/policy-deprecations.md` line 79 confirms: '## `doc-convention.adr` (renamed to `doc-convention-adr`, #332)'. The dotted spelling this ADR records is now the deprecated legacy alias, not the live key. This is the same class of drift the doc's own existing 'Naming note (6.68.1)' blockquote already documents for the 'prior art' -> 'existing convention' rename — that note's own text ('an accepted decision record is dated evidence... superseded rather than edited') establishes the precedent of adding a naming note rather than editing the decision text itself.

## Deliverables

**Current:**
```
> **Naming note (6.68.1).** This record's title and filename say "prior art"; the contract it
> decided is now `_shared/existing-convention-detection.md`, and the concept is "the repo's
> existing convention". Both are unchanged here on purpose — an accepted decision record is
> dated evidence of what was decided and in what words, superseded rather than edited. The
> decision itself is untouched by the rename.

## Context
```

**Proposed:**
```
> **Naming note (6.68.1).** This record's title and filename say "prior art"; the contract it
> decided is now `_shared/existing-convention-detection.md`, and the concept is "the repo's
> existing convention". Both are unchanged here on purpose — an accepted decision record is
> dated evidence of what was decided and in what words, superseded rather than edited. The
> decision itself is untouched by the rename.

> **Naming note (6.96.0).** The Decision section below records the answer key as
> `doc-convention.{genre}`. That dotted spelling was renamed to `doc-convention-{genre}`
> (dash, matching this schema's flat kebab-case keys) in the naming-convention rename program;
> `doc-convention.adr` now resolves only via the legacy alias in `bin/lib/policy-schema.js`'s
> `RENAMED_KEYS` (removal condition: `_shared/policy-deprecations.md`). Unchanged here for the
> same reason as the note above — read `doc-convention-{genre}` as current.

## Context
```

## Acceptance Criteria

Add a naming note after the existing one, noting that the `doc-convention.{genre}` key name recorded in the Decision section was renamed to `doc-convention-{genre}` (dash) in the naming-convention rename program, and that the dotted form now resolves only via the legacy alias in `bin/lib/policy-schema.js`'s RENAMED_KEYS.

**Related:** #1198

_Filed by `/claude-tweaks:docs-health`. Close to resolve; label `wontfix` to suppress future reports of this finding._

<!-- work-fingerprint: docshealth-adcbda89 -->

