---
record: 2009
origin: human
risk: low
size: low
ceremony: fast-lane
grants: [build, merge]
---
# 2009: Split dispatch/SKILL.md's Reporting and Configuration sections into sub-files — the file sits within 15 bytes of the 40,960 ceiling

# Reflect — staged finding 1

**Category:** tangential

Origin: reflect full (batch pass) from the multi-spec run 2026-09-05T193518 (#1928, #1929)
Defer-reason: tangential

## Current State

`plugin/skills/dispatch/SKILL.md` is 40,945 bytes against the 40,960-byte skill-file ceiling. Two specs in this run had to add one line each to its Reporting section (#1928's per-group timing line, #1929's token clause) and each turned into a trim negotiation: #1928's plan named a trim that fell 103 bytes short and the implementer shortened its own new prose; #1929 had to move flags inside a literal and cut a sentence to land at 40,945. `flow/multi-spec.md` had the same shape against its own 20,480-byte read-budget pin until #1928 split its Run directory layout tail into `multispec-run-dir-layout.md` (ledger item 19 → resolved by that split). Evidence: `wc -c` at #1928 Task 7 (40,948 → 40,875) and #1929 Task 4 (40,875 → 40,955 → 40,945). Cost this session: two extra review rounds and one NEEDS-CONTEXT-class shortfall across two specs.

## Deliverables

- Move `dispatch/SKILL.md`'s `## Reporting` (and, if still needed for headroom, `## Configuration` / `## Routine Configuration`) into `dispatch/reporting.md` (and `dispatch/configuration.md`), leaving a one-paragraph pointer in SKILL.md, the same shape `flow/multispec-run-dir-layout.md` took.
- Keep every pinned literal (`timing: call-1 …`, the `--transcript` command, the `pending-review` paragraph) in whichever file the pins now point at; update `tests/timing-prose-conformance.test.js` and any other pin to the new path.
- Add the new sub-file(s) to `docs/plugin-structure.md`'s dispatch row.

## Acceptance Criteria

1. `wc -c plugin/skills/dispatch/SKILL.md` ≤ 36,864 (≥ 10% headroom under the ceiling).
2. Every test that pinned Reporting text passes against the new location; `npm test` shows no new failures.
3. `docs/plugin-structure.md`'s dispatch sub-file row lists the new file(s) and `tests/flow-subfile-table-completeness.test.js`-style completeness holds for dispatch if such a pin exists.

_Filed by `reflect` via specShapedBody._


_Created by the consolidated Review Console of multi-spec run 2026-09-05T193518 (auto-resolved under the unattended ceiling; proposal: `staged/reflect-1.md`)._

https://claude.ai/code/session_01L9hhTyzis8dqW87Qhy96DB
