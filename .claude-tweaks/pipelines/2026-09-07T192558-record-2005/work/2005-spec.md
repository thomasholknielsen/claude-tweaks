---
record: 2005
origin: human
risk: low
size: low
ceremony: fast-lane
grants: [build, merge]
---
# 2005: test/design-gate.md's Design CLI gate reads git diff --name-only, which is empty on every committed pipeline diff — read verify.js --changed-files instead

Title: test/design-gate.md's Design CLI gate reads git diff --name-only, which is empty on every committed pipeline diff — read verify.js --changed-files instead
Type: bug
Labels: risk:low, size:low, ready
Defer-reason: genuinely-larger

Origin: ledger resolve gate (spec #1923 build, whole-branch review finding 12)

Defer-reason: genuinely-larger

## Current State

`plugin/skills/test/design-gate.md` resolves the Design CLI gate's `<changed-files>` argument from `git diff --name-only`. In a pipeline every change is already committed by the time `/claude-tweaks:test` runs, so that list is empty, `design-detect.js`'s Layer 3 sniff sees zero files, and the gate returns `decision: "skip"` on every committed frontend diff — the #808 merge precondition's own deterministic gate silently self-skips. Found by #1923's whole-branch review (finding 12, pre-existing). Since #1923 the same skill states "never hand-roll `git diff --name-only` — it is empty for every committed pipeline diff" and its `affected`/QA paths read `verify.js --changed-files`.

## Deliverables

- [ ] `plugin/skills/test/design-gate.md`: resolve `<changed-files>` from `node "${CLAUDE_PLUGIN_ROOT}/bin/verify.js" --changed-files --integration-branch {ref}` (`{ref}` via `_shared/integration-branch.md`), joining `files` with commas for `--files`; an exit 1 (no resolvable base) falls back to `git diff --name-only HEAD~1..HEAD` ∪ the working tree, never an empty list.

- [ ] A prose pin in `tests/test-skill-affected-conformance.test.js` (or a sibling): `design-gate.md` cites `--changed-files` and no longer cites bare `git diff --name-only` for the gate input.

- [ ] `docs/skill-graph.md` `## test` ↔ `/design-wrapper` row mentions the shared changed-file set.

## Acceptance Criteria

1. `grep -n "git diff --name-only" plugin/skills/test/design-gate.md` returns nothing for the gate-input step; `grep -n -- "--changed-files" plugin/skills/test/design-gate.md` returns at least one line.

2. In a temp repo with a committed `src/App.tsx` change since the stamp anchor, following `design-gate.md`'s step yields a non-empty `--files` value for `design-detect.js --mode test` (a `node -e` probe in the conformance test can assert the documented command shape).

3. `npm test` passes with no new failures relative to `main`.

_Filed by `ledger resolve gate` via specShapedBody._


_Created by the consolidated Review Console of multi-spec run 2026-09-05T193518 (auto-resolved under the unattended ceiling; proposal: `spec-1923/staged/ledger-record-design-gate-changed-files.md`)._

https://claude.ai/code/session_01L9hhTyzis8dqW87Qhy96DB
