---
record: 1485
origin: wrap-up leftover from #1302
risk: medium
size: low
ceremony: fast-lane
grants: []
surface: infra
---

# Live-verify create_webhook_trigger request/response shape (#1302 follow-up)

## Current State

Record #1302 built the WEBHOOK-TRIGGER mode (`plugin/skills/routine/webhook-trigger.md`) documented entirely from `create_webhook_trigger`'s own tool description, since `RemoteTrigger` was confirmed unavailable via `ToolSearch select:RemoteTrigger` across 3 consecutive interactive sessions (2026-08-23 x2, 2026-08-26). AC2 ("verified against a real or explicitly documented dry-run call") was never met.

## Deliverables

- Re-run `ToolSearch select:RemoteTrigger` in this session.
- If reachable, call `create_webhook_trigger` against a real (or explicit, logged dry-run) routine; record actual required fields, response shape, App-not-installed error text.
- Diff the actual response against `webhook-trigger.md`'s current documentation and correct any mismatch.

## Acceptance Criteria

- A live (or explicit dry-run) `create_webhook_trigger` call has been made and its actual request/response shape is confirmed to match (or corrected to match) `webhook-trigger.md`.

## Investigation Findings

**`RemoteTrigger` is reachable in this session** — `ToolSearch select:RemoteTrigger` resolved the full tool schema on the first attempt. This is a 4th consecutive session checking, and the **first success** after 3 straight misses (2026-08-23 x2, 2026-08-26). A useful data point on its own: the earlier hypothesis that this is a session-type-structural gap (interactive vs. cloud/routine) doesn't fully hold — this build is running in the same kind of interactive worktree session as the prior 3 misses, and it resolved this time. Availability looks intermittent/environmental rather than strictly session-type-gated.

**Live `create_webhook_trigger` call deliberately NOT made — AC2 remains unmet, by explicit user decision, not by tool unavailability.** Before firing the call, I read the tool's own description closely: `create_webhook_trigger` "attaches an event source to an existing routine" via `POST /v1/code/webhook-triggers`, and the `RemoteTrigger` action set has no corresponding delete action (`list`, `get`, `create`, `update`, `run`, `create_webhook_trigger`, `list_runs`, `get_run_log` — no `delete`/`remove`). A real call therefore creates a permanent, un-revocable-via-this-API external side effect: a live GitHub event source wired to fire an existing routine on the user's account.

Pulled a read-only `RemoteTrigger action:list` (safe — GET, no mutation) to check for a plausible scratch/test target before deciding how to proceed: all 20 existing routines are one-off, already-fired (`ended_reason: run_once_fired`, `enabled: false`) check-in routines wired to the real `memenu-io/memenu-app` repository — none are a disposable scratch target, and attaching a live webhook to any of them means wiring a real GitHub event stream against a real production-adjacent repo to re-arm an already-completed routine.

Given the irreversibility (no delete lever) and the lack of any safe throwaway target among existing routines, this was surfaced to the user directly via `AskUserQuestion` rather than decided unilaterally — offering: skip the live call and leave AC2 unmet (documented, not fabricated); go ahead against a named existing routine; or create a disposable routine first specifically for this test. **The user chose to skip the live call.** AC2 is therefore left explicitly unmet, per this record's own Gotchas ("do not fabricate a 'verified' result... leave the AC unmet rather than closing it on documentation-only reasoning") — the same honest-non-completion shape the record anticipated for the unavailable case, now applied to a deliberate-decline case instead.

**No code changes.** `plugin/skills/routine/webhook-trigger.md` is untouched — nothing to diff without a captured real response.

**Recommendation for a future attempt:** the "create a throwaway routine first, then attach the webhook to that" option remains available and was not exercised this run — a future session with explicit authorization to spend an extra routine slot could take that path instead of touching any of the 20 existing check-in routines.
