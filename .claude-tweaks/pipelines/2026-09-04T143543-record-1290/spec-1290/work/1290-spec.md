---
record: 1290
origin: human
risk: low
size: low
ceremony: fast-lane
grants: [build, merge]
surface: infra
---
# 1290: reconcile: move-failed stuck on /Users/thomasholknielsen/Code Workspaces/claude-tweaks/.claude-tweaks/pipelines/2026-08-09T122833-spec-271-267

Surface: infra

## Current State

Reconcile's `archive-merged.js` convergence check has failed `move-failed` on
`.claude-tweaks/pipelines/2026-08-09T122833-spec-271-267` for 3 consecutive passes (first
observed 2026-08-22T11:19:58.383Z), auto-filed via `escalate-residue.js`'s fingerprint-deduped
filing path (`bin/lib/reconcile`, see #644).

Investigated at shaping time (2026-08-23) directly against this machine's main checkout:
- The run directory still exists un-archived, with its git-tracked `work/` subdirectories under
  `spec-267/` and `spec-271/` already `git mv`'d into the archive twin and committed (confirmed:
  `archive/2026-08-09T122833-spec-271-267/spec-267/work` and `.../spec-271/work` exist; the
  originals no longer carry `work/`). What remains un-moved: the top-level `events.jsonl`,
  `run-state.json`, and `staged/` entries, plus each spec dir's own now-orphaned `staged/`
  subdirectory — i.e. `archive-merged.js`'s top-level plain-move loop (which runs before the
  per-spec-dir loop) is the step returning `move-failed`, since the per-spec-dir loop's own work
  (moving `staged/` inside each spec dir) has clearly never been reached.
- A direct reproduction — `fs.renameSync` on `run-state.json` from the run dir into its archive
  twin, then immediately back — **succeeded cleanly** with no thrown error: no restrictive
  permissions (`runDir writable: yes`), no BSD `chflags` (`ls -lO@` shows none set, only a benign
  `com.apple.provenance` xattr on each entry), and the destination directory already exists with
  room for these entries (no name collision).
- This means the failure is not currently reproducible by directly exercising the same syscall —
  either it was transient (e.g. one of these files briefly held open by a concurrent writer during
  the specific pass(es) that recorded the streak), or reconcile simply hasn't re-attempted this
  path since the streak was recorded and escalated, leaving a stale failure count.
- Separately: `archive-merged.js`'s `move-failed` reason is a bare string — the `catch` blocks
  around `fs.renameSync` (`archive-merged.js:330-334`, `:376-380`) discard the actual thrown
  error (`err.code`/`err.message`) entirely, so neither this filed issue nor `bin/residue.js`'s
  own findings carry the real OS-level cause. That gap is what made this investigation require a
  manual reproduction instead of reading a logged error.

## Deliverables

1. Capture and surface the actual filesystem error (`err.code` at minimum, e.g. `EACCES`/`EBUSY`/`ENOTEMPTY`) when a `move-failed`/`move-failed-partial-revert` outcome fires in `archive-merged.js`, so a future occurrence is diagnosable from `decisions.md`/the filed issue body without manual reproduction.
2. Re-run reconcile against this specific path (or trigger the next scheduled reconcile pass) to determine whether it now succeeds — clearing the stale streak via `recordResidueSuccess` — or reproduces the failure with the newly-captured error detail.
3. If the failure reproduces with a real cause, fix it; if it was transient/stale, close this record noting the streak self-cleared, and consider whether `escalate-residue.js` should have closed or commented on its own auto-filed issue once the streak resets (not confirmed to exist today — a related gap, not necessarily in scope here).

## Acceptance Criteria

- `archive-merged.js`'s move-failed paths log the underlying OS error (code + message), verifiable via a test that forces a `fs.renameSync` failure and asserts the captured detail.
- This path (`.claude-tweaks/pipelines/2026-08-09T122833-spec-271-267`) is confirmed either fully archived (no entries remain outside `archive/`) or reproducibly still failing with a now-diagnosable cause.
- If fully archived, this issue is closed with a note on what resolved it (stale streak vs. real fix).

## Technical Approach

Wrap the `fs.renameSync` calls in `archive-merged.js`'s top-level loop (`:325-334`) and per-spec-dir
loop (`:371-380`) so the caught error's `code`/`message` is threaded into the `{ ok: false, reason,
detail }` result rather than discarded — `trackArchiveResult` (`:424-436`) already has the
plumbing to pass extra detail into `escalate()`'s filed-issue body and/or `recordResidueFailure`'s
cache entry. Then re-run reconcile (`node bin/hooks.js reconcile`, or wait for the next scheduled
pass) against this path specifically and read the outcome: a clean archive confirms the streak was
stale; a repeat `move-failed` now carries the real error for a targeted fix.

### Key Files

- `plugin/bin/lib/reconcile/archive-merged.js` — the top-level (`:325-338`) and per-spec-dir (`:340-382`) plain-move loops whose `catch` blocks discard the thrown error
- `plugin/bin/lib/reconcile/cache.js` — `recordResidueFailure`/`recordResidueSuccess`, the streak counter this bug's escalation is keyed on
- `plugin/bin/lib/reconcile/escalate-residue.js` — the auto-filing path that produced this issue, fingerprint-deduped
- `.claude-tweaks/pipelines/2026-08-09T122833-spec-271-267` — the specific stuck run dir

## Gotchas

- The failure was not reproducible via a direct, isolated `fs.renameSync` test at shaping time (2026-08-23) — this record may resolve as "streak was stale, reconcile now succeeds on its own" rather than requiring a real bug fix; the diagnostic-logging deliverable (#1) has value regardless of that outcome, since the next occurrence (this path or another) will otherwise be equally undiagnosable.
- No evidence found (in a quick read of `escalate-residue.js`) that reconcile auto-closes a previously-filed residue issue once the streak clears — this record's own resolution may need to close itself manually rather than relying on reconcile to do it.
- `.claude-tweaks/pipelines/` is gitignored, per-clone local state — this path's current state (partially archived: `work/` committed, `staged/`+top-level entries not) is specific to this machine's checkout and may not reproduce identically elsewhere.

## Original request

reconcile: move-failed stuck on /Users/thomasholknielsen/Code Workspaces/claude-tweaks/.claude-tweaks/pipelines/2026-08-09T122833-spec-271-267

Reconcile has failed `move-failed` on this path for 3 consecutive passes (first observed 2026-08-22T11:19:58.383Z).

**Path:** `/Users/thomasholknielsen/Code Workspaces/claude-tweaks/.claude-tweaks/pipelines/2026-08-09T122833-spec-271-267`
**Reason:** `move-failed`

Filed automatically by `bin/lib/reconcile` — see #644.

<!-- fingerprint: reconcile-residue-b797867b -->

