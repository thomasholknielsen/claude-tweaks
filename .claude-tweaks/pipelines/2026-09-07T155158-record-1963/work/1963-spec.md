---
record: 1963
origin: capture
risk: medium
size: low
ceremony: fast-lane
grants: [build, merge]
---
# 1963: Settle retry count splits across issue and PR comment sources, so the retry ceiling never trips

Defer-reason: tangential

Origin: /claude-tweaks:dispatch --budget all drain, run 2026-09-05T090744-dispatch-standalone (14 groups attempted)

## Current State

dispatch/settle-and-merge.md Step 6 step 4 counts "Attempt N failed" comments from the PR when run-state.json carries a pr object, else from the issue. A record whose attempts alternate between runs that opened a PR and runs that did not (or whose PRs were closed) gets its attempts split across sources. Record 1302 shows two independent `attempt=1` markers (2026-09-02 on PR 1783, 2026-09-05 on the issue) despite four consecutive blocked-dependency failures since 2026-08-23; the ceiling of 3 was never reached and the record kept re-entering the auto:build queue.

## Deliverables

- [ ] Count attempts across both the issue's comments and every linked PR's comments (open and closed) in bin/lib/issues/retry.js's caller, or persist the count on the issue regardless of where the full failure comment is posted.
- [ ] Post the trust-negative-evidence marker and the "Attempt N failed" line to the issue always, with the PR getting a pointer copy under pr-first.
- [ ] Test: comments spread over one issue and two closed PRs count as N attempts total.

## Acceptance Criteria

1. `countFailedAttempts` sees every prior attempt for a record regardless of which PR each attempt posted to.
2. A record with three consecutive failures across three separate runs gets bot:blocked on the third.
3. Existing retry.js tests still pass.

