---
record: 2270
origin: human
risk: low
size: low
ceremony: standard
grants: [build, merge]
---
# 2270: Make test:perf portable to Windows the same way #2043 fixed npm test

# Reflect — staged finding 1

**Category:** tangential

Origin: reflect hindsight from #2043
Defer-reason: tangential

## Current State
package.json's `test:perf` script still reads `node --test $(find perf -name '*.test.js' | sort)` — the same `$(...)` command-substitution construct #2043 just replaced in the `test` script for Windows cmd.exe portability, using the new tools/run-tests.js. `test:perf` is intentionally excluded from npm test/CI (CLAUDE.md: "deliberately excluded from npm test"), so it was correctly out of #2043's scope, but it carries the identical Windows failure mode for anyone running `npm run test:perf` on a Windows machine or CI runner.

## Deliverables
- [ ] Extend tools/run-tests.js (a `roots` parameter, or a small sibling script reusing its collectTestFiles/listTestFiles helpers) to also cover the `perf` root, and point package.json's test:perf script at it the same way `test` now is.

## Acceptance Criteria
1. `npm run test:perf`'s script contains neither a POSIX env-prefix nor `$(...)` command substitution.
2. The resolved perf test file list is identical (verified by diff) to `find perf -name '*.test.js' | sort`'s current output.

_Filed by `reflect` via specShapedBody._

## Decision-log reference

STAGED — Reflect Step 3: tangential idea "test:perf carries the same Windows-portability gap #2043 just fixed for test" — backlog candidate — landing: born-ready (defer-reason: tangential). Surface at the Queue writes gate.

