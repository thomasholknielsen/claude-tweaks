---
record: 1997
origin: human
risk: low
size: medium
ceremony: standard
grants: []
fingerprint: 2026-09-06-skill-context-composer-design:retire-per-file-byte-pins-retarget-or-delete-the-ad-hoc-40-k
surface: backend
---
# 1997: Retire per-file byte pins: retarget or delete the ad-hoc 40 KB assertions, rewrite donts.md's three ceiling rules to composition, plan-audit headroom check on composed bytes
Surface: backend

## Overview

Retire the ad-hoc per-file byte pins the parent design's composition mechanism makes obsolete: retarget or delete every `<= 40960`-style assertion outside `context-cost.test.js` (U3's own hard gate), rewrite `docs/donts.md`'s three ceiling rules (the `[IL-70]` extraction rule, the "draft the sub-file first" rule, and the `_shared` `wc -c` rule) to state the composition rule instead, and extend `plugin/bin/lib/plan-audit/checks.js`'s `headroomCheck` to measure composed bytes for any compose call site whose sources a plan touches — closing `[IL-140]`'s missing multi-spec pre-flight.

**Complexity:** Medium
**Estimated tasks:** 7

## Non-Goals

- Adding any new markers or compose call sites — this record is pure retirement/retargeting of tests and docs already made obsolete by U1-U9's landed work; it fences nothing new.
- Building U3's composed-bytes measurement itself — this record consumes it (`headroomCheck` calls into the same measurement U3 already shipped).
- Fully closing #1709 or #1881 itself with a merge/label change beyond naming them — this record closes them **by reference** in the PR description (`refs #1709, refs #1881`) and, per the parent design's instruction, closes them with a comment after this record's own PR merges; it does not silently resolve them without that comment.

## Current State

- `tests/run-dir-timestamp-utc.test.js`'s BUDGETS map (manifesto.md 21504, multi-spec.md 20480) uses its own distinct literal numbers, separate from the grep below.
- The authoritative enumeration of every ad-hoc `<= 40960`/`40 * 1024` per-file pin, run fresh at spec time (`grep -rln "40960\|40 \* 1024" tests/`), returns 18 files: `tests/reflect-transcript-judge-prose.test.js`, `tests/policy-schema-metadata.test.js`, `tests/materiality-floor-conformance.test.js`, `tests/specify-decomposition-collapse.test.js`, `tests/console-on-pr.test.js`, `tests/tidy-residue-markers.test.js`, `tests/sweep-backstop.test.js`, `tests/deferred-live-verification-ac-class.test.js`, `tests/capture-absorb-default.test.js`, `tests/transcript-judge-prose.test.js`, `tests/merge-size-probe-cli-e2e.test.js`, `tests/dispatch-budget-drain.test.js`, `tests/tidy-report-rules.test.js`, `tests/dev-url-detection-lease-conformance.test.js`, `tests/bin-lib/merge-size-probe.test.js`, `tests/bin-lib/plan-audit/cli.test.js`, `tests/bin-lib/harness-health/skill-md.test.js`, `tests/bin-lib/code-health/scope.test.js`. This list (not any hand-typed subset) is the actual worklist for the first Deliverable below — re-run the grep at implementation time rather than trusting this enumeration, since concurrent work can add or remove pins between spec time and build time.
- `docs/donts.md` currently carries three ceiling rules: the `[IL-70]` extraction rule, the "draft the sub-file first" rule, and the `_shared` `wc -c` rule — all three predate this decomposition's composition mechanism and currently point authors toward fragmentation (more sub-files) as the response to ceiling pressure.
- `plugin/bin/lib/plan-audit/checks.js`'s `headroomCheck` currently measures per-file headroom against the raw `CEILING_BYTES` import from `context-cost.js`; it has no per-compose-call-site awareness, which is exactly `[IL-140]`'s "missing multi-spec pre-flight" gap. The call-site-to-source-files mapping this extension needs is U3's own `findComposeCallSites`/`measureComposed` pair — `headroomCheck` calls into those directly rather than deriving its own separate mapping.
- #1709 (a per-file pin on `issue-claims.md`, made moot by U5) and #1881 (three files in the WARN band, resolved by U3's per-file-test-as-warning change and Phase 2's markers) are assumed to be real, open records pending verification — confirm both with `gh issue view 1709` and `gh issue view 1881` before writing any `refs #N` text; if either has already been closed or doesn't resolve, drop that reference rather than asserting it in the PR.
- This record's `Blocked by` link is U3 (#1990) — verify it has actually merged (e.g. `gh pr list --search "1990" --state merged`) before starting, since this record's docs rewrite and test retirement build directly on `context-cost.js`'s composed-bytes measurement and marker-stripped per-file warning existing. U4 (#1991) is a sibling, not a blocker, but may have already made its own interim adjustment to `manifesto.md`'s BUDGETS pin — check its current state too (it's fine either way; see the reconciliation Deliverable below).

## Deliverables

- [ ] For each of the 18 test files enumerated in Current State (re-grepped fresh at implementation time), determine whether its byte pin protects content now covered by a compose call site's own hard gate (U3's `measureComposed`) — if so, retarget the pin to reference the composed step instead of the raw file; if the pin protects content with no corresponding compose call site (not yet migrated, or never going to be), delete the pin with a recorded removal condition. Record a per-file disposition table (file → retarget/delete → one-line reason) in the PR description, so the outcome for each of the 18 files is auditable rather than inferred from the diff alone. The removal-condition note itself has one canonical home for a deleted pin: a code comment in the test file itself, naming the `[IL-nn]` tag (see the `docs/incident-log.md` deliverable below) it cross-references — the PR description repeats it for visibility, but the in-file comment is authoritative and the one that survives independently of any single PR's description.
- [ ] `tests/run-dir-timestamp-utc.test.js`'s BUDGETS map: reconcile against whatever value U4 already established for `manifesto.md` (U4 may have raised this pin mid-flight to accommodate marker-stripped measurement) — this record is the single place that value gets its final, considered form, rather than U4's own interim adjustment.
- [ ] Rewrite `docs/donts.md`'s `[IL-70]` extraction rule, the "draft the sub-file first" rule, and the `_shared` `wc -c` rule to state the composition rule (a file nearing budget gets markers or a compose call site, not another sub-file, unless the sub-file is genuinely a lazy unit some runs never read) — keep each rule's own `[IL-nn]` tag intact; do not renumber or remove the incident-log entries themselves, only the guidance rule text.
- [ ] `plugin/bin/lib/plan-audit/checks.js`'s `headroomCheck`: extend it to measure composed bytes (via U3's `measureComposed`) for any compose call site whose sources a plan under audit actually touches, alongside its existing per-file measurement — this is the fix for `[IL-140]`'s missing multi-spec pre-flight.
- [ ] `docs/incident-log.md`: append a removal-condition note for the retired pins (which pins were removed, why, and what observable state would justify reintroducing a per-file pin) — no renumbering of existing entries. This is the entry each deleted pin's in-file comment cross-references by tag.
- [ ] After confirming #1709 and #1881 are real, open records (see Current State), note them by reference in the PR description (`refs #1709, refs #1881`); closing both with a comment is a manual post-merge step performed by whoever merges this record's PR (there is no automated hook that does this), and is the last action this record's own scope covers.

## Acceptance Criteria

1. `grep -rn "40960\|40 \* 1024" tests/` returns only hits inside `tests/bin-lib/skill-audit/context-cost.test.js` and the plan-audit suite (`tests/bin-lib/plan-audit/checks.test.js` and `tests/bin-lib/plan-audit/cli.test.js` — both explicitly named, since "the plan-audit test suite" is otherwise ambiguous about which of its files are exempt) — every other per-file pin found by that grep today is either retargeted or deleted with a recorded removal condition. This literal-string grep is a known-incomplete proxy (it can't catch a pin expressed via an imported constant or computed arithmetic) — as a secondary check, also confirm no test file outside `context-cost.test.js`/the plan-audit suite imports or duplicates `CEILING_BYTES` directly for its own per-file assertion.
2. Each retargeted pin has a red-proof: reverting the corresponding U1-U9 migration (or a synthetic equivalent) makes the retargeted test fail, proving it still discriminates.
3. `docs/donts.md`'s three ceiling rules are rewritten to state the composition rule, with their original `[IL-nn]` tags intact and unchanged.
4. `headroomCheck`'s new composed-bytes measurement is proven with a fixture plan that touches a compose call site's sources and correctly reports composed-byte headroom, not just raw per-file headroom.
5. `npm test` passes.
6. The PR description names `refs #1709, refs #1881`; both are closed with a comment once this record's PR merges.

## Technical Approach

### Key Files

- `tests/run-dir-timestamp-utc.test.js` — retarget the BUDGETS map.
- `tests/capture-absorb-default.test.js`, `tests/materiality-floor-conformance.test.js`, `tests/tidy-residue-markers.test.js`, `tests/dispatch-budget-drain.test.js`, `tests/console-on-pr.test.js`, `tests/sweep-backstop.test.js`, `tests/specify-decomposition-collapse.test.js`, `tests/deferred-live-verification-ac-class.test.js`, `tests/policy-schema-metadata.test.js`, `tests/transcript-judge-prose.test.js`, `tests/reflect-transcript-judge-prose.test.js`, `tests/bin-lib/harness-health/skill-md.test.js`, `tests/merge-size-probe-cli-e2e.test.js`, `tests/tidy-report-rules.test.js`, `tests/dev-url-detection-lease-conformance.test.js`, `tests/bin-lib/merge-size-probe.test.js`, `tests/bin-lib/code-health/scope.test.js` — retarget or delete each ad-hoc `<= 40960` assertion, per file, with its own removal condition where deleted.
- `docs/donts.md` — edit: rewrite the three ceiling rules.
- `plugin/bin/lib/plan-audit/checks.js` — edit: extend `headroomCheck` with composed-bytes measurement.
- `tests/bin-lib/plan-audit/checks.test.js` — edit: add the fixture test proving the extended `headroomCheck`.
- `docs/incident-log.md` — edit: append the removal-condition note (no renumbering).

## Gotchas

- Never renumber or delete an existing `[IL-nn]` entry while rewriting `donts.md`'s rules — the tag is the durable cross-reference other files cite; only the guidance text changes.
- A pin's removal condition must be recorded, not just asserted — state precisely what observable state (a specific file exceeding a specific ceiling with no compose call site protecting it) would justify reintroducing a per-file pin, per this project's "no implicit deferrals" philosophy.
- `headroomCheck`'s extension is the actual fix for `[IL-140]` (the missing multi-spec pre-flight) — verify this claim with a fixture plan spanning multiple specs that touch the same compose call site's sources, not just a single-spec fixture, since the multi-spec case is specifically what `[IL-140]` was about.
- U4 may have already made an interim adjustment to `manifesto.md`'s BUDGETS pin while landing its own markers — reconcile with that interim value rather than assuming the pin is still at its original number; read the current test file before editing, not from this record's own Current State snapshot.
- Closing #1709/#1881 happens only after this record's own PR merges (a comment, not a merge-time auto-close) — verify the record numbers are real, open records before writing `refs #N` (per this project's own convention of not reflexively writing a reference without confirming the backing issue).


<!-- work-fingerprint: 2026-09-06-skill-context-composer-design:retire-per-file-byte-pins-retarget-or-delete-the-ad-hoc-40-k -->
