---
record: 1991
origin: human
risk: medium
size: high
ceremony: standard
grants: []
fingerprint: 2026-09-06-skill-context-composer-design:mode-and-attendance-markers-pipeline-run-dir-md-auto-mode-co
surface: backend
---
# 1991: Mode and attendance markers: pipeline-run-dir.md, auto-mode-contract.md, flow/manifesto.md, flow/materialize.md fenced by mode and attendance; flow's steps compose per resolved mode

Surface: backend

## Overview

Apply U2's fence-and-switch procedure to the mode/attendance dimension: fence the existing auto-vs-interactive and headless-vs-attended prose already present in `plugin/skills/_shared/pipeline-run-dir.md` (30,673 B), `plugin/skills/_shared/auto-mode-contract.md` (39,126 B — 1.8 KB of raw headroom, requiring U3's marker-stripped measurement to even attempt), `plugin/skills/flow/manifesto.md` (21,434 B), and `plugin/skills/flow/materialize.md` (30,347 B), then switch `plugin/skills/flow/steps-and-gates.md` (composes a `flow-run` bundle from `pipeline-run-dir.md` + `auto-mode-contract.md`) and `plugin/skills/flow/SKILL.md`'s Manifesto step (composes a `manifesto` bundle from `manifesto.md`) to read the composed bundles.

**Complexity:** High
**Estimated tasks:** 7

## Non-Goals

- Fencing `worktree-setup.md`, `issue-claims.md`, `github-pr-scan.md`, or any other Phase 2 file — those are the sibling U5/U6 records.
- Retargeting any pin beyond `tests/run-dir-timestamp-utc.test.js`, `tests/auto-mode-flow-two-stop-budget.test.js`, and `tests/flow-run-dir-anchoring.test.js` — the three named here are the ones this record's own edits touch.
- Making any `/flow` call site depend on `config.yml` existing — `mode`/`attendance` are legitimately unresolved on a standalone run with no run directory, and both branches must stay in that case; this record must not introduce a new failure mode for that case.
- Retiring `manifesto.md`'s own byte pin in `tests/run-dir-timestamp-utc.test.js`'s BUDGETS map — that retirement is U10's job; this record only raises or retargets it if marker-stripped measurement requires it to unblock this record's own work, and notes the change for U10 to reconcile.

## Current State

- `plugin/skills/_shared/pipeline-run-dir.md` (30,673 B) already contains distinct auto-mode and interactive-mode prose for run-dir resolution.
- `plugin/skills/_shared/auto-mode-contract.md` (39,126 B) already contains distinct auto/interactive rows and headless/attended rows across its mode-state and decision-precedence tables; at 1.8 KB of raw headroom under the 40 KB ceiling, this file cannot safely take on marker overhead until this record's own U3 blocking dependency has actually landed and its marker-stripped-bytes measurement is what the ceiling test checks — verify this directly (confirm `context-cost.js` exports `measureComposed`/marker-stripped `measureSkills` and the ceiling test uses them, e.g. `grep -n "measureComposed\|stripMarkers" plugin/bin/lib/skill-audit/context-cost.js`) before touching this file, never assume it from this body's own prose.
- `plugin/skills/flow/manifesto.md` (21,434 B) has a byte pin in `tests/run-dir-timestamp-utc.test.js`'s BUDGETS map (21504) — tighter than the general 40 KB ceiling — which this record's marker insertion must either respect (measuring marker-stripped, not raw) or explicitly raise in the same change.
- `plugin/skills/flow/materialize.md` (30,347 B) contains ceremony-check and other mode-sensitive prose.
- `plugin/skills/flow/steps-and-gates.md` and `plugin/skills/flow/SKILL.md` are today's call sites that read `pipeline-run-dir.md`/`auto-mode-contract.md` and `manifesto.md` directly.
- This record's `Blocked by` link is U2 and U3 — the dependency-edge mechanism ensures both are built and merged before this record's own build starts. Verify directly that both have actually merged (e.g. `gh pr list --search "1989" --state merged` and the same for U3/#1990) before beginning implementation; do not treat this body's own descriptive prose about what U2/U3 "establish" as proof they have already landed at the moment this record is picked up.
- `#1997` is U10 (the systematic per-file-pin-retirement record this decomposition also produced) — every "U10" reference below means #1997, named explicitly here once so the informal label never needs re-deriving.

## Deliverables

- [ ] Add `when:` markers around the existing auto-vs-interactive prose in `pipeline-run-dir.md`, fenced by `mode=auto`/`mode=interactive` (and `mode=confirm`/`mode=hybrid` only where the source text names a mechanism specific to that mode by name — e.g. a `confirm`-only approval gate or a `hybrid`-only partial-stop behavior; a sentence that reads correctly under every mode value, with no mode-specific mechanism named, is left unconditional). Record the list of paragraphs classified as mode-specific vs. left-unconditional in the PR description — this is also the source list the grep-negative proofs (below) check against.
- [ ] Add `when:` markers around the existing auto/interactive and headless/attended rows in `auto-mode-contract.md`, fenced by `mode` and `attendance` respectively (nesting where a passage genuinely depends on both, at the grammar's one-level nesting limit; a passage that would need a third independent axis is split into two separate passages — one per value of the third axis — rather than exceeding the one-level nesting limit).
- [ ] Add `when:` markers around `manifesto.md`'s mode-sensitive prose; if this pushes the file's marker-stripped bytes past its existing `21504`-byte BUDGETS pin in `tests/run-dir-timestamp-utc.test.js`, raise that specific pin in the same change (measuring marker-stripped bytes, not raw) and record the new value in this record's own closing PR description, cross-referenced with a `refs #1997` comment on that record so U10 (#1997) picks it up during its own reconciliation pass — a code comment or test-file note alone is not enough; the cross-reference must be visible from #1997 itself.
- [ ] Add `when:` markers around `materialize.md`'s mode-sensitive prose.
- [ ] Switch `plugin/skills/flow/steps-and-gates.md` to compose a `flow-run` bundle from `pipeline-run-dir.md` + `auto-mode-contract.md` and read `{run}/context/flow-run.md`, with the mandatory fallback sentence, verbatim: "if the compose command is unavailable or exits non-zero, read the named source files directly."
- [ ] Switch `plugin/skills/flow/SKILL.md`'s Manifesto step to compose a `manifesto` bundle from `manifesto.md` and read `{run}/context/manifesto.md`, with the same mandatory fallback sentence verbatim.
- [ ] Byte-identity proof (plan-time verification, pasted into the PR description, not a committed fixture — the same deliberate PR-description-only method U2 uses for the identical reason: a committed fixture would freeze these files against every later edit) for all four marked files: confirm `stripMarkers(new file) === git show origin/main:{file}` for each.
- [ ] Two grep-negative proofs, checked against the paragraph classification recorded above: the composed `flow-run` bundle under `mode=auto` contains none of the interactive-only paragraphs identified during fencing, and under `mode=interactive` contains none of the auto-only paragraphs.
- [ ] Retarget `tests/run-dir-timestamp-utc.test.js`, `tests/auto-mode-flow-two-stop-budget.test.js`, and `tests/flow-run-dir-anchoring.test.js` only where a pin spans a line this record fences.

## Acceptance Criteria

1. The composed `flow-run` bundle under `mode=auto` excludes every interactive-only paragraph identified during fencing (grep-negative proof #1).
2. The composed `flow-run` bundle under `mode=interactive` excludes every auto-only paragraph identified during fencing (grep-negative proof #2).
3. A standalone run with no `config.yml` composes `flow-run` and `manifesto` with `mode` and `attendance` both listed under `unresolved`, and both branches present in the output — proven with a fixture run directory that has no `config.yml`.
4. Byte-identity proof (`stripMarkers(new file) === git show origin/main:{file}`) is shown for all four files in the PR description.
5. `npm test` passes, including the three retargeted test files.

## Technical Approach

### Key Files

- `plugin/skills/_shared/pipeline-run-dir.md` — add markers around existing auto/interactive prose.
- `plugin/skills/_shared/auto-mode-contract.md` — add markers around existing auto/interactive and headless/attended prose (only after confirming marker-stripped bytes stay within the ceiling, per U3).
- `plugin/skills/flow/manifesto.md` — add markers around existing mode-sensitive prose; raise the `run-dir-timestamp-utc.test.js` BUDGETS pin in the same change if needed.
- `plugin/skills/flow/materialize.md` — add markers around existing mode-sensitive prose.
- `plugin/skills/flow/steps-and-gates.md` — edit: compose and cite the `flow-run` bundle.
- `plugin/skills/flow/SKILL.md` — edit: compose and cite the `manifesto` bundle at the Manifesto step.
- `tests/run-dir-timestamp-utc.test.js` — retarget the BUDGETS map and any pin spanning a fenced line.
- `tests/auto-mode-flow-two-stop-budget.test.js` — retarget any pin spanning a fenced line.
- `tests/flow-run-dir-anchoring.test.js` — retarget any pin spanning a fenced line.

## Gotchas

- `auto-mode-contract.md` sits at only 1.8 KB of raw headroom under the 40 KB ceiling — do not attempt this file's markers until confirming (via U3's shipped `context-cost.js` changes) that the ceiling test now measures marker-stripped bytes, or the file will fail the raw-byte hard gate the moment markers are inserted, even though no prose content grew.
- `mode` and `attendance` are legitimately unresolved on a standalone run (no `config.yml`) — never make a call site's correctness depend on `config.yml` existing; both branches must render correctly in that case, and this is the case AC 3 exists to prove.
- `manifesto.md`'s BUDGETS pin (`tests/run-dir-timestamp-utc.test.js`) is tighter than the general ceiling and specific to this file — raising it here (if needed) is a narrow, single-file, marker-stripped-measurement change, not a general test-suite loosening; flag the new value explicitly for U10 (#1997), which does the systematic pin retirement later, via the cross-reference comment named in the Deliverables above.
- This record is sized High and 6-8 tasks specifically because it touches the two largest/most-cited files in the flow lifecycle (`auto-mode-contract.md`'s decision-precedence table has multiple citers) — do the byte-identity proof per file before moving to the next file, rather than fencing all four and proving identity once at the end, so a mistake is caught against a smaller diff.

## Build deviations (architecture alignment, /build Common Step 4.5)

Two deviations from the Deliverables above shipped, both classified **Update the spec** (the spec's premise was measured false; the build is correct) and staged for the consolidated Review Console as `spec-1991/staged/build-deviation-{1,2}.md` — each carries the proposed amendment text. What was built is the record's `manifesto` half exactly as specified: four `when:` fences in `manifesto.md` (1,401 B of single-value prose), the `BUDGETS` pin raised 21504 → 21760 as a raw single-Read budget with a `refs #1997` cross-reference, and `flow/SKILL.md`'s Step 3 composing and reading the `manifesto` bundle (21,579 B under any mode — under the composed ceiling with no exception).

1. **Three of the four named sources are not fenced** (staged, build-deviation-1). Four read-only fence surveys measured the mode/attendance branch-specific share before the plan promised anything: `pipeline-run-dir.md` 0 B (its one `auto`-only paragraph is the antecedent for five unconditional siblings; the bulk branches on `integration-model`/`worktree-policy`/`transport`), `materialize.md` 0 B (every branch is `work-backend`, `work-links`, `ceremony`, or the worktree-vs-current-branch execution mode — a name collision with the composer's `mode` key, never a legal value), `auto-mode-contract.md` 994 B (four Mode-states table rows, no `attendance` prose) with no consumer composing it — marker lines there would be raw overhead on a file 1,834 B under the merge-time hard gate for zero composed saving.
2. **The `flow-run` bundle is not created** (staged, build-deviation-2). `pipeline-run-dir.md` + `auto-mode-contract.md` compose to ~70 KB under every mode with under 1 KB branch-specific; #1990's composed-bytes gate fails any call site over 40,960 B unless it joins `COMPOSED_STEP_EXCEPTIONS`, and a second exception on the first sibling record would hollow out the gate. Carried to #1987 as Cross-Spec Promise F5 (viable once those files are fenced on the axes they branch on, or split along reader need). AC1-AC3 are proven against the `manifesto` bundle instead.

Also as built: the `interactive` bullet in `manifesto.md` stays unfenced (under `interactive` the file is never read, so that fence would never compose); passages true under `confirm` or `hybrid` stay unconditional (the grammar has no OR); zero pins needed retargeting beyond the `BUDGETS` constant. AC3's proof shows `unresolved: ["mode"]` rather than both keys: `attendance` resolves from the project's `.claude-tweaks/policy.yml` (`autonomy: unattended` → `headless`), the resolver's documented second source, so "both unresolved" holds only in a policy-less repo — the criterion's substance (no `config.yml` → `mode` unresolved, both branches present) is met, and `manifesto.md` carries no `attendance` fence.


<!-- work-fingerprint: 2026-09-06-skill-context-composer-design:mode-and-attendance-markers-pipeline-run-dir-md-auto-mode-co -->
work-fingerprint: 2026-09-06-skill-context-composer-design:mode-and-attendance-markers-pipeline-run-dir-md-auto-mode-co
