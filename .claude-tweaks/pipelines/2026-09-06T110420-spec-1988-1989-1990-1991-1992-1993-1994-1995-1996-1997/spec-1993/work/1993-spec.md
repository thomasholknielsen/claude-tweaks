---
record: 1993
origin: human
risk: low
size: medium
ceremony: standard
grants: []
fingerprint: 2026-09-06-skill-context-composer-design:worktree-policy-markers-shared-worktree-setup-md-and-build-w
surface: backend
---
# 1993: Worktree-policy markers: _shared/worktree-setup.md and build/worktree-setup.md fenced by worktree-always; build composes `worktree-setup` once
Surface: backend

## Overview

Apply U2's fence-and-switch procedure to the worktree-policy dimension: fence the existing `worktree-always: true`-vs-optional branches and the `EnterWorktree`-vs-`git worktree add` fallback text already present in `plugin/skills/_shared/worktree-setup.md` (the most re-read shared file in main-agent sessions — 22 reads across 40 sessions) and `plugin/skills/build/worktree-setup.md`, then switch `plugin/skills/build/SKILL.md`'s worktree step to compose a `worktree-setup` bundle from both and read it once.

**Complexity:** Medium
**Estimated tasks:** 5

## Non-Goals

- Fencing any file outside the two named worktree-setup files — `issue-claims.md`, `github-pr-scan.md`, `pipeline-run-dir.md`, and the rest are the sibling U4/U5 records.
- Fencing or moving `_shared/scratch-worktree.md`'s Post-creation catch-up section — that section lives in a different file and is only cited by heading name; this record's job is to keep that heading resolvable, not to touch it.
- Changing worktree-creation behavior itself (which tool runs, `EnterWorktree` vs `git worktree add`) — this record fences existing prose describing that behavior; it does not alter the behavior.

## Current State

- `plugin/skills/_shared/worktree-setup.md` is read 22 times across 40 sessions by main agents — the single most-read shared file measured in the parent design's audit — and already contains distinct `worktree-always: true` and optional-worktree prose, plus distinct `EnterWorktree`-vs-`git worktree add` fallback text.
- `plugin/skills/build/worktree-setup.md` mirrors the same *shape* of `worktree-always`/optional and `EnterWorktree`/fallback distinction for the build-specific entry path — its own prose is not assumed word-for-word identical to `_shared/worktree-setup.md`'s; read and classify each file's actual text independently rather than copy-pasting fence placement from one to the other.
- `plugin/skills/build/SKILL.md`'s worktree step currently reads both files directly — today's call site this record switches.
- `_shared/scratch-worktree.md` cites `worktree-setup.md`'s "Post-creation catch-up" section by heading name — that heading must remain outside any fence this record adds, per the `[IL-144]` guard U2 establishes.
- `tests/build-worktree-setup-dispatch-stamp.test.js` and `tests/dispatch-worktree-anchoring.test.js` currently pin prose in these files.
- This record's `Blocked by` link is U2 (#1989) and U3 (#1990) — verify both have actually merged (e.g. `gh pr list --search "1989" --state merged`, same for #1990) before starting; the dependency link enforces build order, but confirm it directly.
- `worktree-policy` resolves via the same `resolvePolicyKeys(['worktree-always'], {policyRaw, runConfigRaw})` mechanism U1 defines for every policy-backed condition key — the composer resolves it once per `compose-context.js` invocation, not something `build/SKILL.md`'s worktree step re-derives itself.

## Deliverables

- [ ] Add `when:` markers around the existing `worktree-always: true`-vs-optional branches in `_shared/worktree-setup.md`, fenced by `worktree-policy=always`/`worktree-policy=optional`.
- [ ] Add `when:` markers around the existing `EnterWorktree`-vs-`git worktree add` fallback text in the same file, fenced by the same key — a sentence is left unconditional only when it names no worktree-policy-specific mechanism at all (e.g. a general "then proceed with the build" sentence); a sentence naming `EnterWorktree` or `git worktree add` specifically, or the always-vs-optional prompt itself, is branch-specific and gets fenced.
- [ ] Apply the identical fencing *approach* (not identical marker placement) to `plugin/skills/build/worktree-setup.md`'s own mirrored branches, reading and classifying that file's actual prose independently.
- [ ] Confirm the "Post-creation catch-up" heading in `_shared/worktree-setup.md` (cited by `_shared/scratch-worktree.md`) stays entirely outside any fence — verify with U2's own citation-enumeration procedure: grep every file under `plugin/skills/**/*.md` for the heading's literal text and for markdown anchor links to it, confirm none falls inside a fenced span.
- [ ] Switch `plugin/skills/build/SKILL.md`'s worktree step to compose a `worktree-setup` bundle from both marked files, in this order: `plugin/skills/_shared/worktree-setup.md` first (the general contract), then `plugin/skills/build/worktree-setup.md` (the build-specific addendum) — and read `{run}/context/worktree-setup.md`, with the mandatory fallback sentence, verbatim: "if the compose command is unavailable or exits non-zero, read the named source files directly."
- [ ] Byte-identity proof (plan-time verification, PR description only — U2's non-fixture method): confirm `stripMarkers(new file) === git show origin/main:{file}` for both marked files.
- [ ] Alongside the re-read-count measurement below, also report the composed bundle's byte count under each `worktree-policy` value (`always` vs `optional`) — the re-read count reflects the architecture switch (one bundle read instead of two file reads) regardless of fencing quality, while the per-branch byte count is what actually shows the fencing removed the untaken branch's content.
- [ ] Retarget `tests/build-worktree-setup-dispatch-stamp.test.js` and `tests/dispatch-worktree-anchoring.test.js` only where a pin spans a line this record fences.
- [ ] Measure and report the re-read count for both files on the next ten sessions after release, using the parent design audit's own transcript method, in the PR description.

## Acceptance Criteria

1. The composed `worktree-setup` bundle under `worktree-policy=always` omits every optional-worktree prompt/fallback sentence identified during fencing.
2. `_shared/scratch-worktree.md`'s citation of the "Post-creation catch-up" heading still resolves correctly after fencing (the heading and its content are unfenced).
3. Byte-identity proof shown for both files in the PR description.
4. The re-read count for `_shared/worktree-setup.md` and `build/worktree-setup.md`, measured on the next ten sessions after release using the audit's transcript method, is reported in the PR (a drop is expected but not gated — this is a measurement AC, not a threshold AC, since ten future sessions can't be observed before merge).
5. `npm test` passes, including the two retargeted test files.

## Technical Approach

### Key Files

- `plugin/skills/_shared/worktree-setup.md` — add markers around existing `worktree-always`/optional and `EnterWorktree`/fallback prose.
- `plugin/skills/build/worktree-setup.md` — add markers around the mirrored branches.
- `plugin/skills/build/SKILL.md` — edit the worktree step to compose and cite the `worktree-setup` bundle.
- `tests/build-worktree-setup-dispatch-stamp.test.js`, `tests/dispatch-worktree-anchoring.test.js` — retarget only pins spanning fenced lines.

## Gotchas

- `_shared/scratch-worktree.md` cites `worktree-setup.md`'s "Post-creation catch-up" section by heading — that heading must stay entirely outside any fence; verify this explicitly rather than assuming the fencing pass naturally avoids it, since this is the single most load-bearing citation into this file.
- This file is the single most-read shared file in the entire audit (22 reads/40 sessions) — treat the byte-identity proof with extra care here; an accidental content loss in this file would be the highest-traffic regression this whole decomposition could introduce.
- The re-read-count AC (AC 4) cannot be satisfied at merge time — it requires ten sessions to run after release. Report it as a measurement commitment in the PR description (how and when it will be measured), not as a number available before merge.

## Build deviations (architecture alignment, /build Common Step 4.5)

One deviation, classified **Update the spec** and staged for the consolidated Review Console as `spec-1993/staged/build-deviation-1.md`: a plan-time fence survey measured 0 B of `worktree-policy`-specific prose in either named file (16,671 / 20,032 B unconditional) — the differences are by build mode (`worktree` vs `current-branch`), by tool mechanism (`EnterWorktree` vs `git worktree add`), and by auto vs interactive (the consent section, on the `mode` axis and not `hybrid`-complete); the policy key appears only in two mid-sentence clauses. No markers were added, so Deliverables 1–4 and 6–8 and AC1–AC3 have nothing to attach to; neither named test pins a fenced line. What was built: Deliverable 5 as specified — `build/SKILL.md` Common Step 1 composes a `worktree-setup` bundle (shared contract first, build addendum second) and reads it once with the verbatim fallback sentence, whenever a parent `/flow` has already exported `$PIPELINE_RUN_DIR` (a standalone `/build` reaches Common Step 1 before Spec Step 1's materialize mints its run directory — `_shared/pipeline-run-dir.md`'s record-mode exception — so it reads the two files directly rather than tripping the exit-2 fallback on every run; a second deviation the record's "reads both files directly … today's call site" premise did not anticipate, folded into the same staged amendment); with zero fences the bundle is a ~36.8 KB concatenation under the 40,960 B composed gate, which couples both files' growth budgets at this call site (flow ledger row 20, trigger #1997). AC4 is stated in the PR as a measurement commitment; AC5 holds.


<!-- work-fingerprint: 2026-09-06-skill-context-composer-design:worktree-policy-markers-shared-worktree-setup-md-and-build-w -->
