---
record: 1995
origin: human
risk: medium
size: medium
ceremony: standard
grants: []
fingerprint: 2026-09-06-skill-context-composer-design:subagent-boundary-dispatch-task-prompts-and-review-lens-prom
surface: backend
---
# 1995: Subagent boundary: dispatch task prompts and review lens prompts cite composed bundles; the Subagent Contract forbids `_shared/` paths in dispatch input
Surface: backend

## Overview

Close the subagent boundary: have the orchestrator compose the bundle a dispatched agent needs *before* dispatch, and change `plugin/skills/dispatch/task-prompt.md`'s two call templates and `plugin/skills/review/step3-lens-dispatch.md`'s lens prompts to cite `{run}/context/{step}.md` paths in the dispatch prompt itself, never a `_shared/…` path. `plugin/skills/_shared/subagent-output-contract.md`'s input discipline section gains the sentence "cite the run's composed bundle, never a `_shared/` path", plus the fallback rule. Dispatch's second-call group agent (which legitimately runs whole skills inside a subagent, per #296's two-call isolation) still reads SKILL.md bodies directly — it is not switched — but reads composed bundles for everything those skills themselves cite.

**Complexity:** Medium
**Estimated tasks:** 5

## Non-Goals

- Building the enforcement gate that denies a subagent's `_shared/` reads — that is the sibling U9 record, explicitly gated on this record's own measurement first.
- Switching `dispatch/task-prompt.md`'s second-call group agent's own SKILL.md reads — those are sanctioned by #296's two-call isolation design and stay as direct reads; only the shared-contract citations *inside* the skills that agent runs switch to composed bundles.
- Editing `plugin/skills/dispatch/task-prompt.md`'s prose beyond the two call templates named here, or `review/step3-lens-dispatch.md`'s prose beyond its lens prompts.
- Resolving #1863 (design-wrapper critic prompts inline ~70 KB per critic) — same boundary, explicitly filed as a separate record; not folded in here.

## Current State

- `plugin/skills/dispatch/task-prompt.md` has two call templates whose dispatch prompts currently cite `_shared/*.md` paths directly.
- `plugin/skills/review/step3-lens-dispatch.md`'s lens prompts currently cite `_shared/*.md` paths directly for the per-lens agent prompt.
- `plugin/skills/_shared/subagent-output-contract.md`'s input discipline section documents minimal input (scope + paths + literal output template) but has no explicit rule against citing `_shared/` paths in dispatch input yet.
- `plugin/skills/dispatch/SKILL.md`'s second-call group agent runs whole skills inside a subagent by design (#296) — the one dispatch that legitimately reads SKILL.md bodies; this record does not change that.
- This record's `Blocked by` link is U2 (#1989) and U7 (#1994) — verify both have actually merged (e.g. `gh pr list --search "1989" --state merged`, same for #1994) before starting: they establish the concrete call sites (`merge`, `review-code`, `test-verify`) this record's dispatch prompts switch to citing instead of raw `_shared/` paths. U4 (#1991), U5 (#1992), and U6 (#1993) are siblings, not blocking dependencies here, but this record's dispatch-prompt switch also depends on whichever of their own step names (e.g. `flow-run`, `claims`, `worktree-setup`) a given dispatch prompt cites — check each one's actual merge state individually before switching that specific citation, and leave a citation pointing at an unmerged sibling's step name reading the raw `_shared/` source directly (with the standard fallback sentence) until that sibling lands.
- Related, filed separately, not touched here: #1863 (design-wrapper critic prompts), #1912, #1868 (both also touch `subagent-output-contract.md` — rebase this record's edit onto whichever lands first).

## Deliverables

- [ ] Enumerate every `_shared/*.md` citation inside `plugin/skills/dispatch/task-prompt.md`'s two call templates and `plugin/skills/review/step3-lens-dispatch.md`'s lens prompts (a plain grep for `_shared/` within each file), and confirm each has a resolved compose step name from U2/U4/U5/U6/U7 before switching it — flag any citation with no matching step name yet as an explicit, named gap in the PR description (not switched, left reading the raw source with the fallback sentence) rather than silently switching to a step name that doesn't exist.
- [ ] Replace each citation that does have a resolved step name with a citation to the orchestrator-composed `{run}/context/{step}.md` bundle path instead (the orchestrator composes before dispatch, per U1 (#1988)'s CLI).
- [ ] Add the sentence "cite the run's composed bundle, never a `_shared/` path — except inside the fallback sentence itself, which is expected to name the underlying source files it falls back to" to `plugin/skills/_shared/subagent-output-contract.md`'s input discipline section, plus the fallback rule (dispatch prompt states that a bundle-read failure falls back to the named sources, matching every other call site's fallback sentence verbatim: "if the compose command is unavailable or exits non-zero, read the named source files directly" — see `plugin/skills/wrap-up/cleanup-procedures-execution.md` Section C's merge row, from U2, as one concrete existing example to match wording against).
- [ ] Confirm the second-call group agent's own SKILL.md reads are untouched (still direct) — it is the one sanctioned exception per #296 — while confirming the shared-contract citations *inside* the skills it runs are the composed-bundle form (inherited from U2/U4/U5/U6/U7's own edits, not re-done here) wherever those upstream edits have actually landed; where one hasn't, that citation stays a direct read until it does (same gap-flagging rule as above).
- [ ] New conformance test asserting no dispatch prompt template under `plugin/skills/dispatch` or `plugin/skills/review` cites a `_shared/*.md` path as its PRIMARY citation — the regex excludes the one line carrying the mandatory fallback sentence (which legitimately names the `_shared/` sources it falls back to), matching that sentence's exact wording so the exclusion can't be abused to hide an unswitched primary citation.
- [ ] Cardinality rule for the compose-call switch: when one dispatch-prompt call template cites multiple distinct `_shared/*.md` files under what is already one step name (e.g. `review-code` citing all three of U7's sources), those citations collapse into ONE `{run}/context/{step}.md` reference for that step, never one file per source — a call template needing sources from two *different* step names (e.g. both `review-code` and `claims`) cites both bundle paths, one per step, never a manufactured third combined bundle.
- [ ] Measure subagent reads of the switched shared-contract files per session, before and after, using the parent design audit's method (Read tool_use + Bash cat/sed on `skills/**/*.md` in `~/.claude/projects/<slug>/<session>/subagents/*.jsonl`), and report both numbers in the PR — this measurement is the explicit go/no-go input the sibling U9 record consumes.

## Acceptance Criteria

1. No dispatch prompt template under `plugin/skills/dispatch` or `plugin/skills/review` cites a `_shared/*.md` path — proven by the new conformance test.
2. Subagent reads of the switched files drop by at least 10× on the next ten sessions after release, measured by the audit's method and reported in the PR (a measurement-and-report AC, since ten future sessions can't be observed before merge — state the pre-release baseline and the measurement commitment). If the measured drop later falls short of 10×, that is not a silent miss: file a narrow follow-up record naming the actual measured ratio, which the sibling U9 record's go/no-go decision (or a re-opened version of it, if U9 already closed as not-needed) then consumes.
3. The second-call group agent's own sanctioned SKILL.md reads are unaffected — proven by a fixture dispatch showing it still reads the skill body directly while any shared-contract citation inside that skill resolves to a composed bundle.
4. `npm test` passes, including the new conformance test.

## Technical Approach

### Key Files

- `plugin/skills/dispatch/task-prompt.md` — edit: switch both call templates' `_shared/` citations to composed-bundle citations.
- `plugin/skills/review/step3-lens-dispatch.md` — edit: switch the lens prompts' `_shared/` citations to composed-bundle citations.
- `plugin/skills/_shared/subagent-output-contract.md` — edit: add the "cite the run's composed bundle" sentence and its fallback rule to the input discipline section.
- `plugin/skills/dispatch/SKILL.md` — read only: confirm the second-call group agent's exemption is unaffected.
- `tests/multi-agent-coordination.test.js`, `tests/dispatch-flow-rundir-handoff.test.js` — edit: add or extend the conformance assertion; retarget only pins spanning switched lines.

## Gotchas

- Agents only see what's in their prompt — a reference to a sibling file never reaches them (the Subagent Contract's own founding premise). This is exactly why the switch matters: a dispatch prompt citing `_shared/pr-first-merge.md` sends the agent to read that whole file with both branches; citing the composed bundle path sends it to read only the resolved branch.
- #1912 and #1868 both touch `subagent-output-contract.md` — rebase this record's one-sentence addition onto whichever lands first, the same rebase discipline U2 already established for its own overlapping records.
- #1863 (design-wrapper critic prompts, ~70 KB inline per critic) is the same class of boundary violation but is filed and worked separately — do not fold its fix into this record even though the fix pattern is identical; the file it touches (`design-wrapper` critic prompts) is out of this record's Key Files.
- The measurement in this record's last Deliverable is the explicit input the sibling U9 record's go/no-go decision consumes — report the before/after numbers precisely enough (not just "reads dropped") that U9 can decide whether its own enforcement gate is still needed.


<!-- work-fingerprint: 2026-09-06-skill-context-composer-design:subagent-boundary-dispatch-task-prompts-and-review-lens-prom -->
