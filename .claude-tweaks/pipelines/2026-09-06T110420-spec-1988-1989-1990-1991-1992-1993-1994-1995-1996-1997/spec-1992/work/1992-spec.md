---
record: 1992
origin: human
risk: medium
size: medium
ceremony: standard
grants: []
fingerprint: 2026-09-06-skill-context-composer-design:transport-markers-issue-claims-md-github-pr-scan-md-github-p
surface: backend
---
# 1992: Transport markers: issue-claims.md, github-pr-scan.md, github-pr-scan-acceptance.md fenced by gh/MCP transport; tidy and dispatch compose per resolved transport

Surface: backend

## Overview

Apply U2's fence-and-switch procedure to the transport dimension: fence the existing `gh`/MCP branches already present in `plugin/skills/_shared/issue-claims.md` (39,156 B), `plugin/skills/_shared/github-pr-scan.md` (40,045 B — 915 B of raw headroom, requiring U3's marker-stripped measurement to attempt at all), and `plugin/skills/_shared/github-pr-scan-acceptance.md` (38,853 B), then switch `plugin/skills/tidy/scan-procedures.md` Steps 4.7/4.8 (composes a `claims-scan` bundle) and `plugin/skills/dispatch/SKILL.md`'s claim step (composes a `claims` bundle) to read the composed bundles. `_shared/github-write-transport.md` is the transport-resolution contract itself and stays unfenced.

**Complexity:** Medium
**Estimated tasks:** 6

## Non-Goals

- Fencing `_shared/github-write-transport.md` — it is the resolution contract this record's markers key off of, and stays whole and unfenced.
- Fencing `pipeline-run-dir.md`, `auto-mode-contract.md`, `manifesto.md`, or `materialize.md` — those are the sibling U4 record.
- Closing #1709 (a per-file pin on `issue-claims.md` that becomes moot once this record's markers land) — this record notes it moot for U10 to close by reference; it does not close it itself.
- Building a new transport-resolution mechanism — the resolver is U1 (#1988)'s existing `command -v gh` semantics check; this record only fences prose by its output. `transport` is a presence check (is `gh` on PATH), never a policy read, so unlike `mode`/`attendance`/`worktree-policy` it has no `unresolved` state to plan for here — it always resolves to exactly `gh` or `mcp`.
- Deliverable-name-collisions section (inside `issue-claims.md`) — untouched by this record; it carries no transport-specific branches and is neither fenced nor moved.

## Current State

- `plugin/skills/_shared/issue-claims.md` (39,156 B) already contains a `gh`-specific claim procedure and an MCP-specific one, plus the Deliverable-name-collisions section (out of scope here, per Non-Goals).
- `plugin/skills/_shared/github-pr-scan.md` (40,045 B) already contains `gh`/MCP branches, at 915 B of raw headroom under the 40 KB ceiling — this record cannot proceed on this file until this record's own U3 (#1990) blocking dependency has actually landed and its marker-stripped measurement governs the ceiling test instead of raw bytes; verify directly (`grep -n "measureComposed\|stripMarkers" plugin/bin/lib/skill-audit/context-cost.js`) before starting this file, never assume it from this body's own prose. If that precondition still isn't met when this record is otherwise ready to ship, close this record with `issue-claims.md` and `github-pr-scan-acceptance.md` migrated (2 of 3) and file a narrow follow-up for `github-pr-scan.md` alone, rather than blocking the whole record indefinitely.
- `plugin/skills/_shared/github-pr-scan-acceptance.md` (38,853 B) already contains `gh`/MCP acceptance-criteria branches.
- `plugin/skills/tidy/scan-procedures.md` Steps 4.7/4.8 and `plugin/skills/dispatch/SKILL.md`'s claim step currently read the claims-related sources directly — today's call sites this record switches.
- #1709 is an existing per-file pin on `issue-claims.md` that this record's fencing makes moot (the pin targets text that moves inside a marker) — flagged here for U10 (#1997) to close by reference, not closed by this record.
- Cloud/MCP-only sandboxes (Routines) resolve `transport=mcp` at runtime — the gh-absent path is a real, exercised branch, not a hypothetical.
- This record's `Blocked by` link is U2 (#1989) and U3 (#1990) — verify both have actually merged before starting (e.g. `gh pr list --search "1989" --state merged`, same for #1990); the dependency link enforces build order, but confirm it directly rather than trusting this body's own descriptive prose about what they establish.

## Deliverables

- [ ] Add `when:` markers around the existing `gh`-specific and MCP-specific prose in `issue-claims.md`, fenced by `transport=gh`/`transport=mcp`.
- [ ] Add `when:` markers around the existing `gh`/MCP branches in `github-pr-scan.md` (only once U3's marker-stripped measurement governs this file's ceiling test).
- [ ] Add `when:` markers around the existing `gh`/MCP acceptance-criteria branches in `github-pr-scan-acceptance.md`.
- [ ] Never fence a heading, Step label, or anchor another file cites by name (the `[IL-144]` guard, same as U2/U4) — apply U2's exact enumeration procedure: grep every file under `plugin/skills/**/*.md` for these three files' section headings, both as literal heading text and as markdown anchor links, before fencing.
- [ ] Switch `plugin/skills/tidy/scan-procedures.md` Steps 4.7/4.8 to compose a `claims-scan` bundle from the three marked sources and read `{run}/context/claims-scan.md`, with the mandatory fallback sentence, verbatim: "if the compose command is unavailable or exits non-zero, read the named source files directly."
- [ ] Switch `plugin/skills/dispatch/SKILL.md`'s claim step to compose a `claims` bundle from all three marked sources (`issue-claims.md`, `github-pr-scan.md`, `github-pr-scan-acceptance.md` — the same set as `claims-scan`, since dispatch's claim step needs the same claim procedure `tidy` does) and read `{run}/context/claims.md`, with the same mandatory fallback sentence verbatim.
- [ ] Byte-identity proof (plan-time verification, PR description only — same deliberate non-fixture method U2 uses): confirm `stripMarkers(new file) === git show origin/main:{file}` for all three marked files.
- [ ] Two grep-negative proofs: the composed `claims` bundle under `transport=gh` contains no MCP tool names, and under `transport=mcp` contains no `gh api` invocations.
- [ ] Test both compositions end to end (not just the grep negatives) — confirm a `gh`-absent sandbox still resolves and reads a correct, complete `claims`/`claims-scan` bundle under `transport=mcp`.
- [ ] Retarget `tests/console-on-pr.test.js`, `tests/sweep-backstop.test.js`, and `tests/dispatch-budget-drain.test.js` only where a pin spans a line this record fences.
- [ ] Note #1709 as moot in the PR description, for U10 to close by reference.

## Acceptance Criteria

1. The composed `claims` bundle under `transport=gh` contains no MCP tool names (grep-negative proof).
2. The composed `claims` bundle under `transport=mcp` contains no `gh api` invocations (grep-negative proof).
3. Both compositions (`transport=gh` and `transport=mcp`) are exercised end to end against a fixture run, confirmed complete and not merely grep-clean.
4. Byte-identity proof shown for all three files in the PR description.
5. `npm test` passes, including the three retargeted test files.

## Technical Approach

### Key Files

- `plugin/skills/_shared/issue-claims.md` — add markers around existing `gh`/MCP prose.
- `plugin/skills/_shared/github-pr-scan.md` — add markers around existing `gh`/MCP prose (only after U3's marker-stripped ceiling measurement is in effect).
- `plugin/skills/_shared/github-pr-scan-acceptance.md` — add markers around existing `gh`/MCP acceptance-criteria prose.
- `plugin/skills/tidy/scan-procedures.md` — edit Steps 4.7/4.8 to compose and cite the `claims-scan` bundle.
- `plugin/skills/dispatch/SKILL.md` — edit the claim step to compose and cite the `claims` bundle.
- `tests/console-on-pr.test.js`, `tests/sweep-backstop.test.js`, `tests/dispatch-budget-drain.test.js` — retarget only pins spanning fenced lines.

## Gotchas

- `github-pr-scan.md` sits at only 915 B of raw headroom under the ceiling — the smallest margin of any file in this decomposition — do not attempt its markers until U3's marker-stripped measurement change is confirmed live; a naive marker insertion will fail the raw-byte hard gate immediately even though real content didn't grow.
- MCP-only sandboxes (cloud Routines) genuinely resolve `transport=mcp` — this is not a rare edge case to leave under-tested; the gh-absent path must read correctly, verified by actually running the composition, not just by inspecting the fenced source.
- `_shared/github-write-transport.md` is the transport-resolution contract and stays unfenced by design — do not add markers to it even though it's transport-adjacent; it's the thing the markers key off of, not branch-specific prose itself.
- #1709 becomes moot once this record's markers land (the pin's target text moves inside a marker) — note it, don't close it; closing existing records outside this decomposition's own scope is U10's consolidated retirement pass, so a premature close here would fragment that pass's bookkeeping.

## Build deviations (architecture alignment, /build Common Step 4.5)

Four deviations from the Deliverables above shipped, all classified **Update the spec** (premises measured false, or wording that could not hold) and staged for the consolidated Review Console as `spec-1992/staged/build-deviation-{1..4}.md` — each carries the proposed amendment text. What was built: `issue-claims.md` fenced at eleven paired `gh`/MCP spans (2,077 B gh-only, 2,491 B mcp-only; raw 39,635 B) and `github-pr-scan.md` at five spans (10,552 B gh-only — the PR-backed items' procedures, item 9's label kept outside its fence — and its gh-absent Transport bullet; raw 40,261 B against the 40,960 B merge-time gate), both byte-identical under `stripMarkers`; two single-source bundles wired — `claims` (issue-claims.md) at `tidy/scan-procedures.md` Step 4.7 and `flow/claim-targets.md`'s MCP path, `pr-scan` (github-pr-scan.md) at `tidy/scan-procedures.md` Step 4.8's dispatcher read — each under the composed ceiling in both transports with no exception.

1. **No three-file `claims`/`claims-scan` bundle** (staged, build-deviation-1). The three named sources total 118,054 B raw and compose to roughly 88 KB at best on this axis — over twice #1990's composed-bytes gate. Two single-source bundles replace them.
2. **`github-pr-scan-acceptance.md` is not fenced** (staged, build-deviation-2). It is `gh`-literal throughout with no MCP prose, its scopes hard-skip on `gh`-absence at the detection ladder, and its sub-sections are contract-inlined whole by two other files; it stays a raw read everywhere.
3. **The claim-side call site is `flow/claim-targets.md`'s MCP path, not `dispatch/SKILL.md`** (staged, build-deviation-3). Dispatch no longer claims (flow's Step 2.8 does, via `claim-targets.js`), cites neither PR-scan source, and sits 12 B under its merge-time raw gate.
4. **The named test list and AC2's literal wording were stale** (staged, build-deviation-4). Two of the three named tests read neither source; the live citer sets were run (every one reads the raw file, so a marker line cannot break a substring assertion). Unconditional passages name `gh` commands that are not transport branches, so AC2 is proven against the classification's gh-only spans.

Also as built: mid-line transport clauses (Step 4's sentences, the `release-claim.js` description, the "Primary path — the CLI" line) and the mirror-comment code block stay unconditional — the latter has no MCP equivalent to fall back to, a contract gap on the exercised MCP path recorded in the flow ledger (row 16); #1709 is noted moot in the PR for #1997.


<!-- work-fingerprint: 2026-09-06-skill-context-composer-design:transport-markers-issue-claims-md-github-pr-scan-md-github-p -->
work-fingerprint: 2026-09-06-skill-context-composer-design:transport-markers-issue-claims-md-github-pr-scan-md-github-p
