---
record: 1989
origin: human
risk: medium
size: medium
ceremony: standard
grants: []
fingerprint: 2026-09-06-skill-context-composer-design:merge-path-markers-fence-pr-first-merge-md-and-pr-early-run
surface: backend
---
# 1989: Merge-path markers: fence pr-first-merge.md and pr-early-run-lifecycle.md by integration-model and transport; wrap-up's merge row reads one composed `merge` bundle

Surface: backend

## Overview

Migrate the merge path — the vertical slice the parent design (#1987) measures Phase 1 against — onto U1's composer: fence the existing `pr-first`/`local-merge` branches inside `plugin/skills/_shared/pr-first-merge.md` (30,166 B) and `plugin/skills/_shared/pr-early-run-lifecycle.md` (28,446 B) with `when:` markers, without rewriting a single sentence of the prose being fenced, then switch the two consumer files that run the merge procedure — `plugin/skills/wrap-up/cleanup-procedures-execution.md` Section C's merge row and `plugin/skills/wrap-up/auto-merge-short-circuit.md` — to compose a `merge` bundle and read it instead of reading both sources directly. Every other citing file (roughly 30 per source) keeps reading the sources unchanged.

**Complexity:** Medium
**Estimated tasks:** 6

## Non-Goals

- Fencing any file other than the two named sources — `pipeline-run-dir.md`, `auto-mode-contract.md`, `worktree-setup.md`, `issue-claims.md`, `github-pr-scan.md`, and the rest are later Phase 2 sub-issues.
- Switching any citing file besides the two named consumers — every other citer of `pr-first-merge.md`/`pr-early-run-lifecycle.md` (roughly 30 files) keeps reading the sources directly, unchanged, per the parent design's stated scope.
- Rewriting or improving the fenced prose — this record fences text that already exists; the review question per fence is "is this sentence really pr-first-only / gh-only", never "can this sentence be improved".
- Building the composed-bytes measurement itself — that is the sibling U3 record; this record's byte-identity proof is a plan-time verification step, not a permanent measurement.
- Resolving #1728, #1875, #1793, or #1795 — these `needs:decision` records also touch the same two files; this record's markers are rebased onto whatever lands first from them, not blocked on their resolution.

## Current State

- `plugin/skills/_shared/pr-first-merge.md` (30,166 B) already contains a `## Local-merge fallback` section and local-merge-specific sentences inside Steps 2.5 and 3 — the exact prose this record fences, not rewrites.
- `plugin/skills/_shared/pr-early-run-lifecycle.md` (28,446 B) already contains a `## Skip / degrade behavior` section, a `## Root cause: MCP PR-body sanitization…` section, and per-transport sentences under Steps 1-4 and the Phase-checklist update — the exact prose this record fences.
- `plugin/skills/wrap-up/cleanup-procedures-execution.md` Section C's merge row currently reads `pr-first-merge.md`'s procedure directly; `plugin/skills/wrap-up/auto-merge-short-circuit.md` runs the same procedure the same way — both are today's call sites this record switches.
- U1 (#1987's Phase 1 composer record) ships `bin/compose-context.js`, `stripMarkers(text)`, and `compose(sources, conditions)` — this record is the first production consumer of both the CLI and the pure functions.
- The following test files currently pin prose in the two source files and must be checked for pins spanning a marker line: `tests/pr-first-merge.test.js`, `tests/pr-early-run-lifecycle.test.js`, `tests/pr-early-run-lifecycle-degrade-warning-conformance.test.js`, `tests/merge-verification-gate-conformance.test.js`, `tests/local-merge-auto-finish.test.js`, `tests/auto-merge-short-circuit-ledger-deletion.test.js`, `tests/dev-url-detection-lease-conformance.test.js`, `tests/pr-run-comments.test.js`, `tests/sweep-backstop.test.js`, `tests/dispatch-flow-rundir-handoff.test.js`, `tests/build-skip-degrade-trace-adoption.test.js`, `tests/ports-release-sites-conformance.test.js`.

## Deliverables

- [ ] Add `when:` markers around the branch-specific text already present in `plugin/skills/_shared/pr-first-merge.md`: the BODY of its `## Local-merge fallback` section (the heading line itself stays outside the fence, unconditional, so any existing citation to that heading by name keeps resolving — only the section's content is fenced) and the local-merge sentences inside Steps 2.5 and 3, fenced by `integration-model=local-merge` (the pr-first-only prose around them fenced by `integration-model=pr-first` where it is genuinely pr-first-only — a sentence that reads correctly regardless of integration model, e.g. "then merge" with no branch-specific mechanism named, is left unconditional; a sentence naming a pr-first-specific artifact (a draft PR, `gh pr merge`) or a local-merge-specific one (a direct branch merge with no PR) is branch-specific and gets fenced).
- [ ] Add `when:` markers around the branch-specific text already present in `plugin/skills/_shared/pr-early-run-lifecycle.md`: the BODY (not the heading line) of `## Skip / degrade behavior`, fenced by `integration-model=local-merge` (it describes what happens when there is no PR to react to); the BODY of the MCP sanitization root-cause section (`## Root cause: MCP PR-body sanitization…`), fenced by `transport=mcp`; and the per-transport sentences under Steps 1-4 and the Phase-checklist update, fenced by `transport=gh`/`transport=mcp` as each sentence's own content dictates.
- [ ] Never fence a heading, Step label, or anchor another file cites by name (the `[IL-144]` guard) — enumerate every citing file's citations before fencing and confirm none targets a heading now inside a fence; concretely, grep every file under `plugin/skills/**/*.md` for the two source files' section headings (`## Local-merge fallback`, `## Skip / degrade behavior`, `## Root cause: MCP PR-body sanitization`) both as literal heading text and as markdown anchor links (`](...#local-merge-fallback)`-style slugs), and confirm none of the matches falls inside a span this record fences — a heading is always left outside the fence regardless (per the two Deliverables above), so this check is really about the fenced content's Step-label lines, not the section headings themselves.
- [ ] Switch `plugin/skills/wrap-up/cleanup-procedures-execution.md` Section C's merge row to carry the compose call `node "${CLAUDE_PLUGIN_ROOT}/bin/compose-context.js" --run "$PIPELINE_RUN_DIR" --step merge plugin/skills/_shared/pr-first-merge.md plugin/skills/_shared/pr-early-run-lifecycle.md`, followed by "read `$PIPELINE_RUN_DIR/context/merge.md`" and the mandatory fallback sentence, verbatim: "if the compose command is unavailable or exits non-zero, read the named source files directly."
- [ ] Switch `plugin/skills/wrap-up/auto-merge-short-circuit.md` to the identical compose call (same step name `merge`, same two sources, same fallback sentence verbatim) — it runs the same procedure and must switch with it.
- [ ] New permanent marker-conformance test, `tests/compose-markers-conformance.test.js`: over the live corpus, every `when:` opens and closes in-file, every key/value is from the allowed vocabulary (the six-key/value list U1's `plugin/bin/lib/compose-context/resolve-conditions.js` defines — cite that module directly, don't restate the list a second place), nesting is at most 1 deep, and no fenced block contains a line matching `^#{1,6} ` or `^\*\*Step \d` — headings are structurally excluded from every fence by the Deliverables above, so this assertion is a backstop, not the primary defense.
- [ ] Plan-time-only byte-identity verification: for both marked files, confirm `stripMarkers(new file) === git show origin/main:{file}` — i.e., stripping only the newly-inserted marker lines from the edited file reproduces the pre-migration content exactly; this is what "byte-identity" means here, since the raw edited file is never expected to equal `origin/main` verbatim once markers are inserted (pasted into the PR description, never committed as a fixture — a permanent fixture would freeze the files against every later edit).
- [ ] Retarget the twelve prose-pinning test files listed in Current State only where a pin spans a line this record fences; leave every pin on unchanged text as-is.
- [ ] Rebase the new markers onto whatever lands first among #1728, #1875 (both edit `pr-early-run-lifecycle.md`) and #1793, #1795 (both edit `cleanup-procedures-execution.md`) if any of them merges before this record does; re-run the byte-identity proof against `origin/main` at PR time regardless. If one of those records introduces genuinely new prose (not just an edit to existing fenced/unfenced text), that new prose lands unconditional (unfenced) as part of this record's rebase — extending it with its own `when:` markers is out of scope here and is a follow-up, not a blocker.
- [ ] Before starting implementation, confirm U1 (#1988) has actually merged — `gh pr list --search "1988" --state merged` or an equivalent check — since this record's own compose call sites and byte-identity method depend on `stripMarkers`/`compose-context.js` existing; this record's own `Blocked by` link is the enforcement mechanism, but verify it directly rather than trusting this body's own prose about U1's state.

## Acceptance Criteria

1. Both `pr-first-merge.md` and `pr-early-run-lifecycle.md` parse through U1's composer with zero unresolved-marker errors.
2. The composed `merge` bundle under `integration-model=pr-first` + `transport=gh` is under 35 KB and reads, end to end, as one coherent procedure (no orphaned reference to a fenced-out section) — checked once, manually, via the CLI's own printed `bytes` value at PR time (not a new automated test; the permanent automated gate on composed bytes is U3's job).
3. Both `cleanup-procedures-execution.md` Section C and `auto-merge-short-circuit.md` cite the `merge` bundle with the exact compose call and the mandatory fallback sentence, verbatim: "if the compose command is unavailable or exits non-zero, read the named source files directly."
4. The byte-identity proof (`stripMarkers(new file) === git show origin/main:{file}`, for both files) is shown verbatim in the PR description.
5. The new `tests/compose-markers-conformance.test.js` suite is proven capable of failing: insert a fenced heading into a fixture and show the test goes red, then remove it.
6. `npm test` passes, including the retargeted pins.

## Technical Approach

Fencing is purely additive to the two source files — every fenced block's content is byte-identical to what already exists; only marker lines are inserted around it. The review for each fence is "is this sentence really pr-first-only / gh-only" — a sentence that reads correctly under both branches is left unconditional, not fenced defensively.

### Key Files

- `plugin/skills/_shared/pr-first-merge.md` — add `when:` markers around existing local-merge-only prose.
- `plugin/skills/_shared/pr-early-run-lifecycle.md` — add `when:` markers around existing skip/degrade, MCP-sanitization, and per-transport prose.
- `plugin/skills/wrap-up/cleanup-procedures-execution.md` — edit Section C's merge row to compose and cite the `merge` bundle.
- `plugin/skills/wrap-up/auto-merge-short-circuit.md` — edit to compose and cite the same `merge` bundle.
- `tests/compose-markers-conformance.test.js` — new: live-corpus marker conformance test.
- `tests/pr-first-merge.test.js`, `tests/pr-early-run-lifecycle.test.js`, `tests/pr-early-run-lifecycle-degrade-warning-conformance.test.js`, `tests/merge-verification-gate-conformance.test.js`, `tests/local-merge-auto-finish.test.js`, `tests/auto-merge-short-circuit-ledger-deletion.test.js`, `tests/dev-url-detection-lease-conformance.test.js`, `tests/pr-run-comments.test.js`, `tests/sweep-backstop.test.js`, `tests/dispatch-flow-rundir-handoff.test.js`, `tests/build-skip-degrade-trace-adoption.test.js`, `tests/ports-release-sites-conformance.test.js` — retarget only the pins that span a fenced line.

## Gotchas

- `[IL-144]`: never delete text while fencing — every fenced block's content must remain byte-for-byte what it was before this change; the byte-identity proof exists specifically to catch an accidental deletion.
- Never fence a heading, a `**Step N**` label, or an anchor another file cites by name — grep every known citer of both source files for the heading text before fencing, not after.
- #1728 and #1875 (both `needs:decision`, both edit `pr-early-run-lifecycle.md`) and #1793/#1795 (both `needs:decision`, both edit `cleanup-procedures-execution.md`) are gotchas to rebase around, not blockers — do not wait for them; re-verify the byte-identity proof against `origin/main` at PR time since one of them may have landed first.
- The byte-identity proof is deliberately a plan-time verification step, never a committed fixture — a fixture would freeze these two files against every future edit, defeating the point of migrating them onto a living composition mechanism.
- Assumption carried from the design doc: shared contracts carry every branch of a condition the run has already resolved once (883 KB of files carry both `pr-first` and `local-merge` prose repo-wide) — this record is the first of several planned migrations (Phase 2) that generalize the same fix; treat this record's own approach as the template later Phase 2 records copy, not as a one-off.

## Build deviations (architecture alignment, /build Common Step 4.5)

Five deviations from the Deliverables and Acceptance Criteria above shipped. Four are classified **Update the spec** (the spec was wrong or incomplete; the build is correct) and are staged for the consolidated Review Console as `spec-1989/staged/build-deviation-{1..4}.md` — each carries the proposed amendment text; the code stays as built either way. One is **Beneficial** and applied as-built.

1. **AC2's 35 KB target is infeasible by additive fencing** (staged, build-deviation-1). The two sources total 58,751 B on origin/main; the Non-Goals forbid rewriting prose, and the genuinely branch-specific passages come to 3-5 KB. Measured composed `merge` bundle under `pr-first` + `gh` at this record's head: **55,995 B** (the CLI's printed `bytes`). The byte budget, and any restructuring needed to meet one, is carried to #1990 as a Cross-Spec Promise on #1987.
2. **The pr-first merge site is `review-console.md` step 6, not `cleanup-procedures-execution.md` Section C** (staged, build-deviation-2). Section C cites the procedure inside a "skip this step entirely" branch — the merge has already happened by teardown — so the compose sentence was removed there (Section C is byte-identical to its pre-record text) and placed at the "Approve all + merge" step that actually runs `_shared/pr-first-merge.md`. `auto-merge-short-circuit.md` switched as specified. Non-Goals bullet 2's exclusion rested on the premise that Section C was the run site.
3. **Compose source paths are `${CLAUDE_PLUGIN_ROOT}`-rooted, not repo-relative** (staged, build-deviation-3). The verbatim call in Deliverables 4-5 passed `plugin/skills/_shared/…`, which resolves only from a claude-tweaks checkout; in every consumer install the composer would ENOENT and the fallback sentence would fire on every merge. Rule stated in `docs/skill-authoring.md`'s call-site paragraph, pinned by `tests/skill-prose-plugin-root-invocations.test.js`.
4. **Fence scope is narrower than Deliverables 1-2 state** (staged, build-deviation-4). `pr-first-merge.md`: the `## Local-merge fallback` body is `integration-model=local-merge`; Step 2.5's paragraph is a gh-absent paragraph (`transport=mcp`); Step 3 has no local-merge sentence. `pr-early-run-lifecycle.md`: only the local-merge paragraph, its SKIP block, and the Standalone line of `## Skip / degrade behavior` are fenced (rows 2-7 of the degrade table are pr-first paths the bookkeeping-stamps gate reads); of the MCP root-cause section only the `**Scope extends…**` paragraph is fenced, because three later sentences cite "Root cause above" under every transport. Mid-line branch-specific clauses stay unfenced (line-anchored grammar). Byte-identity holds for both files.
5. **Two additions the spec did not name** (Beneficial, applied): `parseMarkers` gains an additive `fenced: true` flag on code-fenced text tokens so the conformance suite reuses the parser's fence state instead of re-deriving it; the #1170 plugin-root test gains a compose-context source-path offender class with a fixture proof.

Deliverable 8 (retarget pin files) needed zero retargets — no existing pin spanned a fenced line. Deliverable 10 (confirm U1 merged) is satisfied by the shared branch: #1988's commits precede this record's on the same PR.


<!-- work-fingerprint: 2026-09-06-skill-context-composer-design:merge-path-markers-fence-pr-first-merge-md-and-pr-early-run -->
work-fingerprint: 2026-09-06-skill-context-composer-design:merge-path-markers-fence-pr-first-merge-md-and-pr-early-run
