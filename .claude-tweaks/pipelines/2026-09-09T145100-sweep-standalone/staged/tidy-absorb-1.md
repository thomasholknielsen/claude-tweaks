# Staged: Absorb — #1812, #1813, #1814, #1815, #1816, #1817 → #1811

**Finding:** six auto-filed `reconcile: structurally-stuck` records (#1613's per-directory escalation), one defect. `/claude-tweaks:specify`'s sweep drain shaped #1811 as the consolidated record: its Current State table names all seven run directories (`record-872`, `record-605`, `record-1317`, `record-1315`, `record-1292`, `record-1179` plus #1811's own `record-1378-816`), and its Deliverables cover archiving every one, the `archive-merged.js` branchless MERGED-by-number path, the no-`run-state.json` terminal path, and consolidating the escalation itself.

**Proposed:** Absorb #1812–#1817 into #1811 — the scope is already integrated first-class into #1811's Current State / Deliverables / Acceptance Criteria (no appendix); on approval comment `Absorbed into #1811.` on each and close each with `--reason "not planned"`.

**Why staged:** Absorb of a `github-issues` record closes an issue — Stage at every tier (outward-facing write).

**Commands:**
gh issue comment 1812 --body "Absorbed into #1811 (consolidated structurally-stuck record covering this run dir)."
gh issue close 1812 --reason "not planned"
gh issue comment 1813 --body "Absorbed into #1811 (consolidated structurally-stuck record covering this run dir)."
gh issue close 1813 --reason "not planned"
gh issue comment 1814 --body "Absorbed into #1811 (consolidated structurally-stuck record covering this run dir)."
gh issue close 1814 --reason "not planned"
gh issue comment 1815 --body "Absorbed into #1811 (consolidated structurally-stuck record covering this run dir)."
gh issue close 1815 --reason "not planned"
gh issue comment 1816 --body "Absorbed into #1811 (consolidated structurally-stuck record covering this run dir)."
gh issue close 1816 --reason "not planned"
gh issue comment 1817 --body "Absorbed into #1811 (consolidated structurally-stuck record covering this run dir)."
gh issue close 1817 --reason "not planned"
