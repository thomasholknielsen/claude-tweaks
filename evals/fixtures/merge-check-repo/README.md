# merge-check-repo

A minimal fixture repository standing in for a plugin-shaped project under test — it carries its
own policy config, an agent-instruction skill file, a moved reference target, and a small pair of
library modules, so merge-check eval cases can plant diffs across every one of those surfaces.
