---
name: claude-tweaks:next
description: Use when you want workflow status or recommendations. Redirects to /claude-tweaks:help.
---

# Next → Help (Redirect)

`/claude-tweaks:next` has been absorbed into `/claude-tweaks:help`, which combines command reference, pipeline status scanning, and next-step recommendations in a single skill.

**Run `/claude-tweaks:help` instead.** Pass any arguments through:

- `/claude-tweaks:help` — full dashboard (cheat sheet + status + recommendation)
- `/claude-tweaks:help status` — pipeline scan only
- `/claude-tweaks:help commands` — quick reference only
- `/claude-tweaks:help {spec or topic}` — targeted status

## Relationship to Other Skills

| Skill | Relationship |
|-------|-------------|
| `/claude-tweaks:help` | Replaces /claude-tweaks:next — all functionality moved there |
