# Mode/attendance markers Implementation Plan (#1991)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Fence the mode-specific prose that actually exists on this record's axis — `plugin/skills/flow/manifesto.md`'s three per-mode bullets and its auto-FYI paragraph — with `<!-- when: mode=… -->` markers, inserting marker lines only; switch `plugin/skills/flow/SKILL.md`'s Manifesto step to compose and read one `manifesto` bundle; raise `manifesto.md`'s raw read-budget pin by the marker overhead and cross-reference #1997; and prove byte-identity, the two grep-negative compositions, and the standalone both-branches read.

**Architecture:** Four marker pairs in one file; one compose sentence at one call site (the pinned "read `manifesto.md` … after Step 2.8 passes" phrasing kept intact); one `BUDGETS` constant raised with its rationale; three plan-time proofs pasted into PR #1998's description. The other three named sources are not fenced and the `flow-run` bundle is not created — the fence surveys measured why (rulings below), and each is an *Update the spec* deviation staged at Common Step 4.5.

**Tech Stack:** Markdown, Node 18+ (`node --test`), `plugin/bin/lib/compose-context/compose.js` (`parseMarkers`, `stripMarkers`, `compose`), `plugin/bin/compose-context.js`, `plugin/bin/lib/skill-audit/context-cost.js` (`composedBytesReport` — the gate that now sees every call site).

**Spec:** `.claude-tweaks/pipelines/2026-09-06T110420-spec-1988-1989-1990-1991-1992-1993-1994-1995-1996-1997/spec-1991/work/1991-spec.md` (materialized record #1991)

## Global Constraints

- **[IL-144] — never delete or alter a sentence while fencing.** Only `<!-- when: mode=… -->` / `<!-- /when -->` lines are inserted, each on its own line. Proof: `stripMarkers(new file) === git show origin/main:plugin/skills/flow/manifesto.md` (Task 1, pasted into the PR by the controller).
- **Never fence a heading, a `**Step N**` label, a table header row, a code block that other prose cites, or an anchor another file cites by name.** `## Present the Manifesto` (cited by `manifesto-confirm.md`), `## Path conventions` (cited by `multi-spec.md`, `steps-and-gates.md`, `pipeline-run-dir.md`), and the Recommendation-defaults table (row cited by `auto-mode-contract.md`) stay unconditional; the `` ```markdown `` template block and the `On approval` yaml block are pinned as needed by every mode.
- **The marker grammar has no OR.** A passage true under `confirm` *or* `hybrid` (lines 66, 115, 180, the tail of 150-156) stays unconditional — duplicating it under two markers costs more than it saves. Mid-line clauses stay unfenced.
- **The `mode=interactive` bullet (line 12) stays unfenced:** under `interactive` this file is never read (Step 3 is skipped), so that fence would never compose — marker bytes for nothing.
- **Raw-size pins that stay hard:** `tests/run-dir-timestamp-utc.test.js`'s `BUDGETS` (raw `statSync`, 21,504 B today, 70 B of headroom) — Task 1 raises it; `plugin/bin/lib/merge-size-probe.js` gates `SKILL.md` files at 40,960 raw bytes at merge time — `flow/SKILL.md` is 40,138 B, so Task 2's compose sentence must stay under ~700 B and the task `wc -c`s the result.
- Compose call form (install-safe, single line, per `docs/skill-authoring.md`'s Call-site form): `node "${CLAUDE_PLUGIN_ROOT}/bin/compose-context.js" --run "$PIPELINE_RUN_DIR" --step manifesto "${CLAUDE_PLUGIN_ROOT}/skills/flow/manifesto.md"`, then read `$PIPELINE_RUN_DIR/context/manifesto.md`, then the verbatim fallback: **"if the compose command is unavailable or exits non-zero, read the named source files directly."**
- Repo conventions: imperative commit messages, `refs #1991` (never `closes`/`fixes`), trailing `Claude-Session: https://claude.ai/code/session_01AU9zM5ZMdZaeTJV4GtBjZj` line; every git command `git -C "/Users/thomasholknielsen/Code Workspaces/claude-tweaks/.claude/worktrees/skill-context-composer-design" …`; one plain command per Bash call (no `&&`, `;`, pipes, heredocs, `cd`, shell variables, `git stash`); Edit/Write tools for file content; never `git add -A`.
- Pre-existing baseline failures (never regressions): `tests/bin-lib/reconcile/reap-merged.test.js` 3/15 fail on macOS realpath (tracked by #1900); `tests/reconcile.test.js`'s #872 wall-clock test, `tests/statusline.test.js`, and `tests/bin-lib/hooks/reconcile-background.test.js`'s #1687 control are load-sensitive flakes.

## Plan-authoring rulings (recorded per `build/plan-authoring-checks.md`)

Four read-only fence surveys (one per named source, 2026-09-06) measured the branch-specific share on the `mode`/`attendance` axis before anything was promised — ledger row 10's first lesson:

- **Ruling — `pipeline-run-dir.md` is not fenced.** Its only `mode=auto`-specific paragraph (resolution-order step 4, ~840 B) is the antecedent for five unconditional sibling paragraphs ("this allowlist", "the allowlist above"); fencing it orphans them — the exact #1989 lesson. Step 3 declares itself "regardless of mode", step 5 must render under every mode, and the file's bulk (`## Anchoring` onward) branches on `integration-model`/`worktree-policy`/`transport`, not this record's axis. Fenceable share: 0 B. Cost if wrong: a later record fences it on its real axis.
- **Ruling — `materialize.md` is not fenced.** Every branch point in it is on another axis (`work-backend`, `work-links`, `ceremony`, worktree-vs-current-branch execution mode — a name collision with the composer's `mode` key, never a legal value); `attendance`/`headless`/`interactive`/`confirm`/`hybrid` do not occur in the file; its Standalone-fallback paragraph declares itself mode-invariant. Fenceable share: 0 B.
- **Ruling — `auto-mode-contract.md` is not fenced.** Its only single-value passages are the four `## Mode states` table rows (994 B, 2.5 % of 39,126 B); no `attendance` prose exists (its two "headless" hits are the `autonomy` posture). No call site composes this file after the next ruling, so 172 B of marker lines would be pure raw overhead on a file 1,834 B under the merge-time hard ceiling, for zero composed saving. Cost if wrong: four fence pairs later, when a consumer composes it.
- **Ruling — the `flow-run` bundle (Deliverable 5) is not created.** `pipeline-run-dir.md` (31,619 B) + `auto-mode-contract.md` (39,126 B) compose to ~70 KB under every `mode`/`attendance` value — less than 1 KB is branch-specific — and `#1990`'s composed-bytes gate fails any call site over 40,960 B unless it is added to `COMPOSED_STEP_EXCEPTIONS`. Adding a second exception on the first sibling record would hollow out the gate the parent design made the hard test; the reads in `steps-and-gates.md`/`SKILL.md` stay as they are. Carried to #1987 as a Cross-Spec Promise (F5): a `flow-run` bundle is viable only once those two sources are fenced on the axes they actually branch on or split along reader need. Cost if wrong: one compose sentence later.
- **Ruling — `manifesto.md` is fenced at exactly four spans** (survey lines 9, 10, 11, 68: the `auto`/`confirm`/`hybrid` bullets under `## When to run` and the `**In default `auto` mode, render the FYI variant instead:**` paragraph): 1,401 B of single-value prose; every other candidate is a `confirm`-or-`hybrid` OR, a both-modes-pinned block, or a cited section. The `manifesto` bundle then measures ~21.4 KB under any mode — under the composed ceiling with no exception.
- **Ruling — the `BUDGETS` pin is raised, not switched to marker-stripped bytes.** The test's own rationale is the ~20 KB single-Read budget, i.e. raw bytes the one reader pays; today that reader is a plain Read, and with the compose call it reads the bundle instead — so the raw pin remains the honest measure of the fallback read. Raise `21504` → `21760` (raw after markers ≈ 21,607 B; 153 B margin), record the reason in the test comment, and cross-reference #1997 with a `refs #1997` comment on that issue (controller, Task 3).
- **Ruling — AC1/AC2's grep-negative proofs and AC3's standalone proof are plan-time, pasted into the PR** (like #1989's byte-identity proof), never a committed fixture that freezes the bullets' wording; `tests/compose-markers-conformance.test.js` already guards well-formedness and the composed-bytes gate discovers the new call site on its own.
- Verbatim-command run-once check: every proof command in Task 3 is run once by the implementer and its output quoted. Degrade-clause check: the fallback sentence is the record's own mandated wording. Gate-over-producers: `findComposeCallSites` scans every `plugin/skills/**/*.md`. Renumbering: none.

---

### Task 1: Fence `manifesto.md`, raise its read-budget pin, prove byte-identity

**Files:**
- Modify: `plugin/skills/flow/manifesto.md` (marker lines only around survey lines 9, 10, 11, 68 — re-derive the line numbers from the live file by their opening text: `- **`auto` mode (flow's default)**`, `- **`confirm` mode**`, `- **`hybrid` mode**`, `**In default `auto` mode, render the FYI variant instead:**`)
- Modify: `tests/run-dir-timestamp-utc.test.js` (`BUDGETS['plugin/skills/flow/manifesto.md']` 21504 → 21760, with a comment)

**Steps:**
- [ ] **Step 1: Insert the four fences.** Each bullet gets `<!-- when: mode=auto -->` / `<!-- when: mode=confirm -->` / `<!-- when: mode=hybrid -->` on the line immediately before it and `<!-- /when -->` on the line immediately after — the `interactive` bullet that follows stays outside every fence. The FYI paragraph gets `<!-- when: mode=auto -->` before and `<!-- /when -->` after (the paragraph is one physical line; keep the blank lines around the fenced span as they were so the markdown paragraph boundary is unchanged). No other byte changes.
- [ ] **Step 2: Byte-identity proof** — run `node -e 'const {stripMarkers}=require("/Users/thomasholknielsen/Code Workspaces/claude-tweaks/.claude/worktrees/skill-context-composer-design/plugin/bin/lib/compose-context/compose.js");const fs=require("fs");const cp=require("child_process");const f="plugin/skills/flow/manifesto.md";const now=stripMarkers(fs.readFileSync(f,"utf8"));const base=cp.execFileSync("git",["-C","/Users/thomasholknielsen/Code Workspaces/claude-tweaks/.claude/worktrees/skill-context-composer-design","show","origin/main:"+f],{encoding:"utf8"});console.log(JSON.stringify({file:f,identical:now===base,nowBytes:Buffer.byteLength(now),baseBytes:Buffer.byteLength(base)}))'` — expected `"identical":true`; quote it.
- [ ] **Step 3: Raise the pin.** `wc -c plugin/skills/flow/manifesto.md` (expected ≈ 21,607). In `tests/run-dir-timestamp-utc.test.js`, change `21504` to `21760` in `BUDGETS` and extend the comment above it: "21504 -> 21760, #1991: four `when:` marker pairs (~170 B of marker lines) fence the per-mode bullets and the auto-FYI paragraph; the pin stays a raw-bytes single-Read budget because `flow/SKILL.md`'s fallback path still reads this file directly — #1997 retires it." Run `node --test tests/run-dir-timestamp-utc.test.js`.
- [ ] **Step 4: Run every suite that reads this file**, each alone, and quote tails: `tests/run-dir-timestamp-utc.test.js`, `tests/manifesto-lever-conformance.test.js`, `tests/manifesto-auto-fast-path.test.js`, `tests/auto-mode-flow-two-stop-budget.test.js`, `tests/flow-run-dir-anchoring.test.js`, `tests/merge-verification-gate-conformance.test.js`, `tests/hooks-run-dir-resolve.test.js`, `tests/bin-lib/set-config/write.test.js`, `tests/compose-markers-conformance.test.js` (must now report three marked files). A pin that fails ONLY because it spans an inserted marker line is retargeted to the unchanged text on either side; any other failure is a defect in the fence.
- [ ] **Step 5: Commit** — `Fence manifesto.md's per-mode bullets and auto-FYI paragraph with when: markers; raise its raw read-budget pin by the marker overhead (refs #1991)`.

### Task 2: Wire the `manifesto` compose call site in `flow/SKILL.md`

**Files:**
- Modify: `plugin/skills/flow/SKILL.md` (Step 3's last paragraph — the one beginning `For the complete Manifesto content`)

**Steps:**
- [ ] **Step 1: Edit the paragraph.** Keep its first two sentences byte-identical (the second — `Read `manifesto.md` only after Step 2.8 passes — a run stopped at pre-flight never consumes it (#724).` — is pinned by `tests/run-dir-timestamp-utc.test.js`'s `/read `manifesto\.md`[^.]*after Step 2\.8 passes/i`). Immediately after that sentence insert, on the same line: `Read it as one composed bundle: `node "${CLAUDE_PLUGIN_ROOT}/bin/compose-context.js" --run "$PIPELINE_RUN_DIR" --step manifesto "${CLAUDE_PLUGIN_ROOT}/skills/flow/manifesto.md"`, then read `$PIPELINE_RUN_DIR/context/manifesto.md`; if the compose command is unavailable or exits non-zero, read the named source files directly.` Then the existing third sentence (`manifesto.md` is everything an `auto`-mode run needs …) follows unchanged. Note in the same paragraph, if not already implied, that the compose runs after `config.yml` is written and `PIPELINE_RUN_DIR` exported (the sentences above it already establish that order).
- [ ] **Step 2: Size and gate.** `wc -c plugin/skills/flow/SKILL.md` — must stay under 40,960 (merge-time raw gate; expected ≈ 40,520). Then `node -e 'const c=require("./plugin/bin/lib/skill-audit/context-cost.js");for(const r of c.composedBytesReport("plugin"))console.log(JSON.stringify({step:r.step,file:r.file,line:r.line,max:r.max,error:r.error}))'` — expected: the two `merge` rows plus one `manifesto` row at `flow/SKILL.md`, `max` ≈ 21,4xx, no `error`; and `node -e 'const c=require("./plugin/bin/lib/skill-audit/context-cost.js");console.log(JSON.stringify(c.overComposedCeiling(c.composedBytesReport("plugin"))))'` → `[]`.
- [ ] **Step 3: Suites** — `tests/run-dir-timestamp-utc.test.js`, `tests/skill-prose-plugin-root-invocations.test.js`, `tests/bin-lib/skill-audit/context-cost.test.js` (the hard gate sees the new site), `tests/flow-cleanup-only-conformance.test.js`, `tests/dispatch-flow-rundir-handoff.test.js`, `tests/flow-claim-preflight.test.js`, `tests/multi-spec-config-scaffold.test.js`, `tests/batch-ref-argument.test.js`, `tests/manifesto-auto-fast-path.test.js`.
- [ ] **Step 4: Commit** — `Compose the manifesto bundle at flow's Step 3 — one call site, install-safe source path, read-the-source fallback (refs #1991)`.

### Task 3: Proofs, full verification

**Files:** none modified (proof outputs go in the report; the controller pastes them into PR #1998 and posts the #1997 cross-reference comment).

**Steps:**
- [ ] **Step 1: Grep-negative proofs (AC1/AC2).** Write a scratch run dir under your scratch directory with `config.yml` = `mode: auto\nintegration-model: pr-first\n`; run `node "/Users/thomasholknielsen/Code Workspaces/claude-tweaks/.claude/worktrees/skill-context-composer-design/plugin/bin/compose-context.js" --run <scratch-run> --step manifesto "/Users/thomasholknielsen/Code Workspaces/claude-tweaks/.claude/worktrees/skill-context-composer-design/plugin/skills/flow/manifesto.md"`; then `grep -c` the bundle for each fenced opening — expected under `mode: auto`: `**`auto` mode (flow's default)**` 1, `render the FYI variant instead` 1, `**`confirm` mode**` 0, `**`hybrid` mode**` 0. Rewrite `config.yml` with `mode: confirm` and re-run: `auto` bullet 0, FYI paragraph 0, `confirm` 1, `hybrid` 0. Quote the JSON line (`bytes`) each time.
- [ ] **Step 2: Standalone proof (AC3).** Delete the scratch `config.yml` (no policy.yml, no CLAUDE.md in that scratch tree); re-run; expected JSON `unresolved` contains `mode` and `attendance`; the bundle's first line lists `mode=unresolved`; all four fenced openings present (count 1 each).
- [ ] **Step 3: Full suite** — `npm test > <scratch>/npm-test-1991.log 2>&1` then `grep -n "^not ok\|^# tests\|^# pass\|^# fail" <that file>`; expected only the baseline failures listed in Global Constraints (re-run any other failing file alone once before calling it a regression).
- [ ] **Step 4: Report** — the three proofs verbatim, the `composedBytesReport` rows, the final sizes of `manifesto.md` and `flow/SKILL.md`, the suite tail.

## Verification (whole plan)

- `stripMarkers(manifesto.md) === origin/main` — identical (AC4 for the one fenced file; the other three are unchanged and need no proof).
- `mode=auto` bundle excludes the `confirm`/`hybrid` bullets; `mode=confirm` bundle excludes the `auto` bullet and the FYI paragraph (AC1/AC2, amended to the `manifesto` bundle).
- No `config.yml` → `mode`/`attendance` unresolved, both branches present (AC3).
- `composedBytesReport` shows the `manifesto` site under 40,960 B with no exception; `overComposedCeiling` is `[]`.
- `npm test` — only the baseline failures (AC5, including the raised pin).
