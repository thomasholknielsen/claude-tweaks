# Transport markers Implementation Plan (#1992)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Fence the transport-specific prose that actually exists — the eleven paired `gh`/MCP spans in `plugin/skills/_shared/issue-claims.md` and the five PR-backed spans in `plugin/skills/_shared/github-pr-scan.md` — with `<!-- when: transport=gh|mcp -->` markers, inserting marker lines only; wire two single-source bundles at the sites that read those files' branch-specific prose (`claims` from `issue-claims.md` at `tidy/scan-procedures.md` Step 4.7 and `flow/claim-targets.md`'s MCP path; `pr-scan` from `github-pr-scan.md` at `tidy/scan-procedures.md` Step 4.8's dispatcher read); prove byte-identity, the grep-negative compositions, and a real `gh`-absent composition; note #1709 moot.

**Architecture:** Marker lines only in two `_shared` files, both merge-time raw-gated at 40,960 B (`issue-claims.md` 39,156 → ~39,646 B; `github-pr-scan.md` 40,045 → ~40,261 B — every fence pair is counted before it saves anything). Two bundles, one source each, each under the composed ceiling in both transports (`claims` ≈ 36.7 / 37.1 KB; `pr-scan` ≈ 39.2 / 29.5 KB) — the three-file `claims`/`claims-scan` bundles the record names total 118,054 B raw and cannot fit any axis's gate (rulings below). `github-pr-scan-acceptance.md` is not fenced (0 B transport-specific; its scopes hard-skip on `gh`-absence at the detection ladder) and keeps being read raw. `dispatch/SKILL.md` gets no call site: it no longer claims (flow's Step 2.8 does, via `claim-targets.js`) and sits 12 B under its raw gate.

**Tech Stack:** Markdown, Node 18+ (`node --test`), `plugin/bin/lib/compose-context/compose.js`, `plugin/bin/compose-context.js`, `plugin/bin/lib/skill-audit/context-cost.js`'s `composedBytesReport`.

**Spec:** `.claude-tweaks/pipelines/2026-09-06T110420-spec-1988-1989-1990-1991-1992-1993-1994-1995-1996-1997/spec-1992/work/1992-spec.md` (materialized record #1992)

## Global Constraints

- **[IL-144] — never delete or alter a sentence while fencing.** Only `<!-- when: transport=gh -->` / `<!-- when: transport=mcp -->` / `<!-- /when -->` lines are inserted, each on its own line. Proof per file: `stripMarkers(new file) === git show origin/main:{file}` (Tasks 1 and 2; the controller pastes both into PR #1998).
- **Never fence a heading, a `**Step N**` label, a numbered-item label another passage cites by number, a table header row, or an anchor another file cites by name.** In `github-pr-scan.md`, item 9's label line (`9. **Unarmed ready PR** …`) stays outside the fence — five files and this file's own Findings/Output-Contract/severity rows cite "item 9" — only its procedure body is fenced; the `## Transport (gh-absent fallback)` heading and its scene-setting bullets stay unconditional (cited by name from `github-write-transport.md` and six in-file `see Transport above` pointers). In `issue-claims.md`, "The lock" heading and step numbers stay; only the paired `- **gh CLI:**` / `- **MCP:**` illustration bullets, the bootstrap and list-all-claims bullet pairs, and the MCP-only "Repairing an unreadable claim blob" numbered steps (survey lines 215-232, scoped by the unconditional "Primary path — the CLI" sentence as the gh-absent fallback) move; the `**In-flight detection at claim time (#315).**` paragraph stays unconditional because Step 4's unconditional text cites it by name.
- **Mid-line branch-specific clauses stay unfenced** (the grammar is line-anchored; splitting a sentence is a rewrite): `issue-claims.md`'s Step-4 sentences 145-160, the `release-claim.js` description 180-192, line 207, and the mirror-comment code block 252-258 (it has no MCP equivalent — fencing it would delete the only guidance an MCP reader has; ledgered as a doc gap).
- **Raw merge-time gate:** `plugin/bin/lib/merge-size-probe.js` fails the merge if any `_shared/*.md` or `SKILL.md` exceeds 40,960 raw bytes. Task 1 and Task 2 each `wc -c` their file after fencing and report the number; `github-pr-scan.md` must land ≤ 40,300 B (core proposal ≈ 40,261 — the item-1/item-9-table extensions the survey listed are rejected: coupled fences 200 lines apart with no pin to hold them together).
- **`transport` always resolves** (`resolve-conditions.js` probes `gh` on PATH — `gh` or `mcp`, never `unresolved`), so there is no both-branches standalone case to prove; AC3's `gh`-absent composition is proven by running the real CLI with `gh` removed from `PATH` (Task 4).
- Compose call form (install-safe, single physical line, per `docs/skill-authoring.md`'s Call-site form): `node "${CLAUDE_PLUGIN_ROOT}/bin/compose-context.js" --run "$PIPELINE_RUN_DIR" --step {claims|pr-scan} "${CLAUDE_PLUGIN_ROOT}/skills/_shared/{issue-claims|github-pr-scan}.md"`, then read `$PIPELINE_RUN_DIR/context/{step}.md`, then the verbatim fallback: **"if the compose command is unavailable or exits non-zero, read the named source files directly."** A call site placed where no run dir exists (a standalone `/tidy` has one — `*-tidy-standalone*` — a standalone `/flow` creates one at Step 3 before Step 2.8? No: Step 2.8 claims *before* Step 3 creates the run dir) must say what to do without `$PIPELINE_RUN_DIR` — read the file directly (Task 3's `claim-targets.md` wording).
- Repo conventions: imperative commit messages, `refs #1992` (never `closes`/`fixes`), trailing `Claude-Session: https://claude.ai/code/session_01AU9zM5ZMdZaeTJV4GtBjZj` line; every git command `git -C "/Users/thomasholknielsen/Code Workspaces/claude-tweaks/.claude/worktrees/skill-context-composer-design" …`; one plain command per Bash call (no `&&`, `;`, pipes, heredocs, `cd`, shell variables, `git stash`); Edit tool with the exact existing lines for every marker insertion; never `git add -A`.
- Pre-existing baseline failures (never regressions): `tests/bin-lib/reconcile/reap-merged.test.js` 3/15 fail on macOS realpath (#1900); `tests/reconcile.test.js`'s #872 wall-clock test, `tests/statusline.test.js`, and `tests/bin-lib/hooks/reconcile-background.test.js`'s #1687 control are load-sensitive flakes.

## Plan-authoring rulings (recorded per `build/plan-authoring-checks.md`)

Three read-only fence surveys (one per named source, 2026-09-06) measured the `transport` branch-specific share before anything was promised (ledger rows 10 and 13):

- **Ruling — no three-file `claims` or `claims-scan` bundle.** `issue-claims.md` + `github-pr-scan.md` + `github-pr-scan-acceptance.md` = 118,054 B raw; the best transport-axis composition of all three is ~88 KB — over twice #1990's 40,960 B composed gate. Two single-source bundles replace them: `claims` (issue-claims.md alone: ≈36,665 B under `gh`, ≈37,079 B under `mcp`) and `pr-scan` (github-pr-scan.md alone: ≈39,161 B under `gh`, ≈29,493 B under `mcp`). Both fit with no `COMPOSED_STEP_EXCEPTIONS` entry. Update-the-spec deviation (Deliverables 5-6, AC1-AC3's bundle name). Cost if wrong: one more compose sentence.
- **Ruling — `github-pr-scan-acceptance.md` is not fenced.** 0 B is `transport`-specific: the file is `gh`-literal throughout and its `acceptance-gap`/`parent-gate` scopes hard-skip on `gh`-absence at `forge-detection.md`'s ladder (`scan-procedures.md` line 248 says so), so no MCP reader ever needs any of it and no paragraph is droppable under `gh`. Its `work-links resolution` and `Fetch limit` sub-sections are contract-inlined whole by two other files. It stays a raw read everywhere. Update-the-spec (Deliverable 3, the byte-identity proof's third file). Cost if wrong: fences later on a different axis.
- **Ruling — `dispatch/SKILL.md` gets no call site.** It does not claim (`docs/plugin-structure.md`: dispatch "mints the run directory but no longer claims"; `/flow`'s Step 2.8 claims via `claim-targets.js`), cites neither PR-scan file, and is 40,948 B against its 40,960 B raw gate. The record's "dispatch's claim step" is `flow/claim-targets.md`, whose MCP path (its line ~149: "The lock steps 1-6 directly via the MCP contents-API calls") is the one skill-prose reader of `issue-claims.md`'s transport bullets on the claim side; it gets the `claims` call site. Update-the-spec (Deliverable 6). Cost if wrong: one sentence moves.
- **Ruling — the record's test list is stale.** `tests/console-on-pr.test.js` and `tests/dispatch-budget-drain.test.js` read neither source; `tests/sweep-backstop.test.js` reads `github-pr-scan.md` (item 9 by boundary strings and the `node -e` bodies inside it — all inside or outside the proposed fence, none straddling). The live citer sets are: `issue-claims.md` → `integration-branch-conformance`, `flow-claim-preflight`, `github-rate-limit-conformance`, `bin-lib/release-claim/release`, `bin-lib/repair-claim/cli`; `github-pr-scan.md` → `sweep-backstop`, `tidy-report-rules`, `session-tmp-root-migration`, `policy-schema`, `policy-schema-metadata`, `bin-lib/residue/probes-forge`. Every one reads the raw file, so an added marker line cannot break a substring assertion; each task runs its file's live set anyway.
- **Ruling — AC2 is amended to the classification.** "No `gh api` invocations under `transport=mcp`" cannot hold literally: unconditional passages (`## Close-via-merge`, the failure-posture table, line 207's CLI sentence) name `gh` commands that are not transport branches. The proof checks that none of the gh-only spans' distinctive lines survive under `mcp`, and none of the mcp-only spans' tool names (`create_branch`, `create_or_update_file`, "get file contents", "read-tree") survive under `gh`.
- **Ruling — #1709 is noted moot in the PR, not closed.** It asks for a raw-byte pin on `issue-claims.md`; #1990 made per-file bytes a warning tier and the merge-time probe already enforces the raw gate this record stays under (39,646 B); #1997 closes it by reference.
- Verbatim-command run-once check: every proof command in Task 4 is run once and quoted. Degrade-clause check: the fallback sentence is the record's own mandated wording. Gate-over-producers: `findComposeCallSites` scans every `plugin/skills/**/*.md`; `composedBytesReport` will show three rows (`claims` ×2, `pr-scan` ×1). Renumbering: none.

---

### Task 1: Fence `issue-claims.md`, prove byte-identity

**Files:**
- Modify: `plugin/skills/_shared/issue-claims.md` (marker lines only, at the eleven spans below — re-derive each line by its opening text on the live file)

**Spans (survey, file-native lines at plan time):** `transport=gh` — 68-71 (`  - **gh CLI:** \`gh api "repos/{owner}/{repo}/git/refs/heads/$` bootstrap), 88-106 (Step 1 `- **gh CLI:**` bullet with its ```bash block and trailing sentences), 137-139 (Step 3 `- **gh CLI:**`), 162-164 (Step 4 `- **gh CLI:**`), 194 (list-all-claims `- **gh CLI:**`); `transport=mcp` — 72-74, 107-110, 140-142, 165, 195-196 (each the paired `- **MCP:**` bullet), 215-232 (the "Repairing an unreadable claim blob" numbered steps 1-4, from the `1. Read the blob at` line to the last step's line, bounded by the blank line / heading). Each fence opens on the line before the span's first line and closes on the line after its last; bullets in one list stay one list when a branch is stripped.

**Steps:**
- [ ] **Step 1: Insert the eleven fences** with Edit (exact existing line as `old_string`). No other byte changes.
- [ ] **Step 2: Byte-identity proof** — `node -e 'const {stripMarkers}=require("/Users/thomasholknielsen/Code Workspaces/claude-tweaks/.claude/worktrees/skill-context-composer-design/plugin/bin/lib/compose-context/compose.js");const fs=require("fs");const cp=require("child_process");const f="plugin/skills/_shared/issue-claims.md";const now=stripMarkers(fs.readFileSync(f,"utf8"));const base=cp.execFileSync("git",["-C","/Users/thomasholknielsen/Code Workspaces/claude-tweaks/.claude/worktrees/skill-context-composer-design","show","origin/main:"+f],{encoding:"utf8"});console.log(JSON.stringify({file:f,identical:now===base,nowBytes:Buffer.byteLength(now),baseBytes:Buffer.byteLength(base),rawBytes:fs.statSync(f).size}))'` — expected `"identical":true`, `rawBytes` ≈ 39,646 (< 40,960).
- [ ] **Step 3: Compose probe** — `node -e` with `compose()` over the file under `transport=gh` and `transport=mcp` (other keys `VOCAB[k][0]`): under `gh`, `create_branch` / `create_or_update_file` / `get file contents` / `read-tree` counts 0 and the gh bullets present; under `mcp`, the gh bullets' `gh api "repos/{owner}/{repo}/git/refs/heads/$` and `gh api --method PUT` lines absent and the MCP bullets plus the repair steps present. Quote the counts and both composed byte sizes.
- [ ] **Step 4: Suites** (each alone, raw tails): `tests/flow-claim-preflight.test.js`, `tests/github-rate-limit-conformance.test.js`, `tests/integration-branch-conformance.test.js`, `tests/bin-lib/release-claim/release.test.js`, `tests/bin-lib/repair-claim/cli.test.js`, `tests/compose-markers-conformance.test.js` (four marked files now), `tests/bin-lib/skill-audit/context-cost.test.js`. A pin that fails ONLY because it spans an inserted marker line is retargeted to the unchanged text on either side; any other failure is a defect in the fence.
- [ ] **Step 5: Commit** — `Fence issue-claims.md's paired gh/MCP bullets and the MCP-only repair steps with transport markers (refs #1992)`.

### Task 2: Fence `github-pr-scan.md`, prove byte-identity

**Files:**
- Modify: `plugin/skills/_shared/github-pr-scan.md` (marker lines only, five spans)

**Spans (survey, plan-time lines):** `transport=gh` — 34-42 (`current-pr` items 1-4 with their code fence; the scope intro at line 32 stays), 57 (`repo-wide` item 4 `4. **Merged/closed PRs with local remnants**`), 88-175 (item 9's procedure body — the label line 87 stays outside; the fence opens on the line after 87 and closes after the last body line before `10. **Unsettled run**`), 313-332 (`triage-queue` item 3 `3. **Auto-merged this week**` with its bash block); `transport=mcp` — 16 (the Transport section's `- **PR-backed items**` bullet). `tests/sweep-backstop.test.js` slices item 9 by the strings `9. **Unarmed ready PR**` and `10. **Unsettled run**` and extracts the `node -e` bodies inside — the fence lines sit between those boundaries and outside every code fence, so nothing moves.

**Steps:**
- [ ] **Step 1: Insert the five fences** with Edit. No other byte changes.
- [ ] **Step 2: Byte-identity proof** — the Task 1 Step 2 command with `f="plugin/skills/_shared/github-pr-scan.md"`; expected `"identical":true`, `rawBytes` ≈ 40,261 — **must be ≤ 40,300**; if higher, report and stop (do not trim prose).
- [ ] **Step 3: Compose probe** — under `mcp`: `Resolve this item's session-scoped temp paths` (item 9's body) count 0, `9. **Unarmed ready PR**` count 1 (the label survives), `3. **Auto-merged this week**` 0, `1. **PR lookup**` 0, `- **PR-backed items**` 1; under `gh`: all four gh spans present, `- **PR-backed items**` 0. Quote counts and both composed sizes (expected ≈ 39,161 / 29,493).
- [ ] **Step 4: Suites**: `tests/sweep-backstop.test.js`, `tests/tidy-report-rules.test.js`, `tests/session-tmp-root-migration.test.js`, `tests/policy-schema.test.js`, `tests/policy-schema-metadata.test.js`, `tests/bin-lib/residue/probes-forge.test.js`, `tests/compose-markers-conformance.test.js`, `tests/merge-size-probe-cli-e2e.test.js`, `tests/bin-lib/skill-audit/context-cost.test.js`. Same retarget rule.
- [ ] **Step 5: Commit** — `Fence github-pr-scan.md's PR-backed spans and its gh-absent Transport bullet with transport markers (refs #1992)`.

### Task 3: Wire the `claims` and `pr-scan` call sites

**Files:**
- Modify: `plugin/skills/tidy/scan-procedures.md` (Step 4.7's `**Primary: list the \`claims/\` blob keyspace**` paragraph; Step 4.8's dispatcher paragraph beginning `Scan per \`_shared/github-pr-scan.md\``)
- Modify: `plugin/skills/flow/claim-targets.md` (the MCP-path sentence citing `_shared/issue-claims.md`'s "The lock" steps 1-6 directly, ~line 149)

**Steps:**
- [ ] **Step 1: `scan-procedures.md` Step 4.7.** After the sentence that cites `_shared/issue-claims.md`'s "The lock" — "List all claims" (the `**Primary: list …**` paragraph), insert on the same line: `Read that file as one composed bundle rather than opening it directly: `node "${CLAUDE_PLUGIN_ROOT}/bin/compose-context.js" --run "$PIPELINE_RUN_DIR" --step claims "${CLAUDE_PLUGIN_ROOT}/skills/_shared/issue-claims.md"`, then read `$PIPELINE_RUN_DIR/context/claims.md` (the gh-absent MCP path composes without the `gh` bullets; a `gh` run without the MCP ones); if the compose command is unavailable or exits non-zero, read the named source files directly.` Keep the existing "(gh path shown above; use … MCP-path …)" parenthetical unchanged.
- [ ] **Step 2: `scan-procedures.md` Step 4.8.** In the `Scan per \`_shared/github-pr-scan.md\`` paragraph, immediately after "The dispatcher inlines all three scope sections (…), `github-pr-scan.md`'s Output Contract, and `_shared/forge-detection.md`'s Detection Ladder into this agent's prompt." insert on the same line: `Take `github-pr-scan.md`'s text from one composed bundle rather than the file: `node "${CLAUDE_PLUGIN_ROOT}/bin/compose-context.js" --run "$PIPELINE_RUN_DIR" --step pr-scan "${CLAUDE_PLUGIN_ROOT}/skills/_shared/github-pr-scan.md"`, then read `$PIPELINE_RUN_DIR/context/pr-scan.md` — under `transport=mcp` the PR-backed items' procedures are already gone, so the agent inherits only what it can run; `github-pr-scan-acceptance.md` is read as-is (nothing in it branches on transport); if the compose command is unavailable or exits non-zero, read the named source files directly.` `wc -c scan-procedures.md` after (a sub-file: the warning tier applies, not the merge gate; report the number).
- [ ] **Step 3: `claim-targets.md`.** In the MCP-path sentence (`_shared/issue-claims.md`'s "The lock" steps 1-6 directly via the MCP contents-API calls …), append on the same line: `— read them from the composed `claims` bundle when a run directory already exists (`node "${CLAUDE_PLUGIN_ROOT}/bin/compose-context.js" --run "$PIPELINE_RUN_DIR" --step claims "${CLAUDE_PLUGIN_ROOT}/skills/_shared/issue-claims.md"`, then `$PIPELINE_RUN_DIR/context/claims.md`); Step 2.8 runs before Step 3 creates a fresh run's directory, so with no `$PIPELINE_RUN_DIR` (or if the compose command is unavailable or exits non-zero) read the named source files directly.` Verify the fallback sentence's exact wording survives (`if the compose command is unavailable or exits non-zero, read the named source files directly` must appear verbatim — rephrase the surrounding clause, never the sentence).
- [ ] **Step 4: Gate and pins** — `node -e 'const c=require("./plugin/bin/lib/skill-audit/context-cost.js");for(const r of c.composedBytesReport("plugin"))console.log(JSON.stringify({step:r.step,file:r.file,line:r.line,max:r.max,error:r.error}))'` → three new rows (`claims` at `tidy/scan-procedures.md` and `flow/claim-targets.md`, `pr-scan` at `tidy/scan-procedures.md`), all `max` < 40,960, no `error`; `overComposedCeiling` `[]`. Suites: `tests/skill-prose-plugin-root-invocations.test.js`, `tests/bin-lib/skill-audit/context-cost.test.js`, `tests/flow-claim-preflight.test.js`, `tests/run-dir-timestamp-utc.test.js`, `tests/sweep-backstop.test.js`, `tests/tidy-report-rules.test.js`, plus every file `grep -rl "scan-procedures.md\|claim-targets.md" tests` returns.
- [ ] **Step 5: Commit** — `Compose the claims and pr-scan bundles at tidy's claim sweep and PR scan and at flow's MCP claim path (refs #1992)`.

### Task 4: Proofs, full verification

**Files:** none modified.

**Steps:**
- [ ] **Step 1: Grep-negative proofs (AC1/AC2, amended).** With a scratch run dir under your scratch directory carrying `config.yml` = `mode: auto\nintegration-model: pr-first\n`, run the real CLI for `--step claims` (source `issue-claims.md`) and `--step pr-scan` (source `github-pr-scan.md`); `transport` resolves `gh` on this machine. Quote each JSON line and the counts: `claims` under gh — `create_branch` 0, `create_or_update_file` 0, `get file contents` 0, `read-tree` 0, `1. Read the blob at` 0; `pr-scan` under gh — `- **PR-backed items**` 0, `Resolve this item's session-scoped temp paths` 1.
- [ ] **Step 2: Real `gh`-absent composition (AC3).** Run the same two CLI calls with `gh` removed from PATH: `env PATH=/usr/bin:/bin node "<compose-context.js>" --run <scratch> --step claims "<issue-claims.md>"` (one plain command; confirm first that `env PATH=/usr/bin:/bin gh --version` fails). Expected: the bundle's first line shows `transport=mcp`; `claims` — the gh bullets' `gh api "repos/{owner}/{repo}/git/refs/heads/$` and `--method PUT` lines 0, `create_or_update_file` ≥ 1, `1. Read the blob at` 1; `pr-scan` — item 9's body 0, `9. **Unarmed ready PR**` 1, `- **PR-backed items**` 1. Quote the JSON lines and counts; then read each mcp bundle once end to end and state whether it reads as a complete procedure (every "step N above/below" pointer still resolves).
- [ ] **Step 3: Full suite** — `npm test > <scratch>/npm-test-1992.log 2>&1`, then `grep -n "^not ok\|^# tests\|^# pass\|^# fail" <log>`; expected only the baseline failures (re-run any other failing file alone once before calling it a regression).
- [ ] **Step 4: Report** — every proof verbatim, `wc -c` of the four edited skill files, the `composedBytesReport` rows, the suite tail.

## Verification (whole plan)

- `stripMarkers(file) === origin/main` for `issue-claims.md` and `github-pr-scan.md` (AC4, amended to the two fenced files).
- `claims` under gh carries no MCP tool names; `pr-scan` under mcp carries no PR-backed procedure (AC1/AC2, amended).
- The real CLI under a `gh`-less PATH resolves `transport=mcp` and composes complete bundles (AC3).
- `composedBytesReport` shows three new rows under 40,960 B with no exception; `overComposedCeiling` `[]`; both `_shared` files under the 40,960 B raw merge gate.
- `npm test` — only the baseline failures (AC5).
