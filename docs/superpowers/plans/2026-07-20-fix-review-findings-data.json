{
  "bin/code-health.js": [
    {
      "category": "altitude",
      "line": 316,
      "summary": "The \"pick up to --budget distinct targets, simulating post-audit cursor state in-memory between picks\" loop is hand-rolled separately in cmdNextSlice and all three sibling cmdNextTargets, instead of being generalized into bin/lib/health-core/ the way the adjacent single-pick selection logic already was.",
      "failure_scenario": "bin/lib/health-core/rotation.js's own header comment records that exactly this \"four engines independently reimplemented the identical shape\" problem was already solved once, for the underlying stale-then-churn single-pick selector. The budget-loop layer directly on top of it was never given the same treatment: docs-health.js:82-90, harness-health.js:122-141, and journey-health.js:81-104 each re-derive the identical `budget = Number.isFinite(args.budget) && args.budget > 0 ? args.budget : 1` line plus the same loop/break/cursor-simulation shape, only the cursor-key derivation and selectFn args differing \u2014 exactly the kind of per-engine variance rotation.js's own extraction already shows how to parameterize around."
    },
    {
      "category": "cross-file",
      "line": 89,
      "summary": "cmdStatus (lines 89 and 91) and cmdChurnReport (line 115) read `args.root` bare, without the `args.root || process.cwd()` fallback every other command in this file and all three sibling health CLIs use, so a malformed --root (e.g. a trailing flag with no value, which parseArgs turns into `undefined`) silently makes both commands report empty state instead of erroring or falling back to cwd.",
      "failure_scenario": "Empirically confirmed with real data: with `.claude-tweaks/code-health/cache.json` containing one regressed finding, `code-health.js status --fail-on regressed --root <realdir>` correctly prints `FAIL: 1 regressed finding(s)` and exits 1. The identical command run from inside that same real directory, but with --root as a trailing flag (no value, so args.root becomes undefined), prints `open:0 regressed:0 ...` and exits 0 \u2014 silently defeating the --fail-on regressed CI/Routine gate even though the regressed finding is sitting in the very directory the command runs from."
    },
    {
      "category": "language-pitfall",
      "line": 199,
      "summary": "The --min-risk (line 199) and --min-severity (line 152) validation guards use `!(value in RISK_RANK)` against the plain object literal `{ high: 0, medium: 1, low: 2 }`, so any Object.prototype property name (constructor, toString, hasOwnProperty, ...) passes validation as if it were a real risk tier.",
      "failure_scenario": "Empirically confirmed: `\"constructor\" in RISK_RANK` is true, so `--min-risk constructor` sails past the guard meant to reject unrecognized tiers. decide() then reads `RISK_RANK['constructor']` (the Object constructor function, not a number); the numeric rank comparison is never true for any finding, so every finding \u2014 confirmed even a `risk: \"high\"` one \u2014 silently resolves to 'remember' instead of 'file'. This is exactly the \"an unrecognized value silently remembers every finding instead of filing it, including high-risk ones\" failure the guard's own error message warns about, defeated by the one input class `in` mishandles on a plain object."
    }
  ],
  "bin/lib/journey-health/fingerprint.js": [
    {
      "category": "altitude",
      "line": 8,
      "summary": "The per-skill fingerprint wrapper (normalizeDescription + fingerprint(prefix, basis)) is reimplemented byte-for-byte in docs-health/fingerprint.js, harness-health/fingerprint.js, and journey-health/fingerprint.js instead of being one parametrized factory in health-core/fingerprint.js, unlike the sibling createCache(skillName)/createDurableState(skillName, opts) factories already established in the same per-skill cache.js files.",
      "failure_scenario": "The three files already differ only in one string literal ('docshealth'/'harnesshealth'/'journeyhealth') and one destructured field list with nothing structural distinguishing them further \u2014 a change to how descriptions are normalized, or a fifth health engine being added, requires hand-copying and hand-syncing this same 12-line wrapper across three-plus files instead of calling one shared factory."
    }
  ],
  "bin/lib/code-health/tests/cli-validate-findings.test.js": [
    {
      "category": "altitude",
      "line": 225,
      "summary": "applyConfidenceFloor and deriveDocId are pure business-logic functions defined inside their skill's CLI entrypoint script (bin/code-health.js, bin/docs-health.js) rather than in the bin/lib/{skill}/ module directory where every sibling pure function lives, forcing their tests to reach outside lib/ to import them.",
      "failure_scenario": "cli-validate-findings.test.js:225 does `require('../../../code-health')` and docs-health's derive-doc-id.test.js:4 does `require('../../../docs-health')` \u2014 climbing past bin/lib/{skill}/ entirely \u2014 while every other domain rule in these suites (computeRisk, fingerprint, validateFindingV2, decide, criteriaForArea) is imported from a same-directory lib module. A contributor adding the next pure gating/derivation function to either CLI has no 'lib/ is where this goes' precedent for this specific kind of logic and is more likely to keep bolting it onto the CLI script, growing bin/code-health.js and bin/docs-health.js into a second, inconsistent home for domain logic."
    }
  ],
  "skills/capture/SKILL.md": [
    {
      "category": "altitude",
      "line": 121,
      "summary": "Slug derivation (lowercase, collapse non-alphanumeric runs, trim, truncate to 60 chars, dedupe with a numeric suffix) is spelled out as a natural-language algorithm for the model to hand-execute, instead of being a small function in bin/lib/issues/local-store.js alongside the allocateId/createRecord helpers it is always used together with.",
      "failure_scenario": "Every work-backend: local-files write in /capture (plus the GitHub-failure fallback at line 91, which explicitly reuses 'the same slug derivation rule') re-derives the slug from prose rather than calling one deterministic implementation. There is no single place to fix or extend the collision/truncation rule if it ever needs to change, and two invocations following the instructions slightly differently can legitimately produce different filenames for the same title."
    },
    {
      "category": "cross-file",
      "line": 111,
      "summary": "capture/SKILL.md (and specify/SKILL.md's parent/leaf creation) instruct creating brand-new local-files records via allocateId() + writeRecord(), the exact pattern bin/lib/issues/local-store.js's own header comments say is 'NOT safe for concurrent record creation' and that the newer createRecord() (added in commit bc82643, 'Fix cross-slug issue id collision race in local-store.js') exists specifically to replace for callers creating brand-new records.",
      "failure_scenario": "Two Claude Code sessions on the same project (work-backend: local-files) each run /capture (or /specify decomposing a design doc) at nearly the same time. Both allocateId('specs') calls read the same directory listing before either write lands, both compute e.g. id=13, and both writeRecord() calls succeed under different slugs (specs/13-idea-a.md and specs/13-idea-b.md) \u2014 two records silently sharing one numeric id, corrupting any later facets.parent/facets.blockedBy reference that assumes id uniqueness. This is the identical bug local-store.js's createRecord() was written and unit-tested to close; capture/SKILL.md (lines 94-101, 111-119) and specify/SKILL.md (lines 444-452, 513-521) never adopted it."
    },
    {
      "category": "line-by-line",
      "line": 272,
      "summary": "The Relationship-to-Other-Skills table claims /claude-tweaks:visual-review files UI ideas as new backlog records \"via /capture,\" directly contradicting this same file's Component-Skill Contract section (line 238), which explicitly lists /claude-tweaks:visual-review among the skills that \"file a new backlog record directly without going through this skill, so they are NOT capture parents.\"",
      "failure_scenario": "An agent implementing or reasoning about /claude-tweaks:visual-review's backlog-filing behavior reads the Relationship table row (line 272) and concludes it must invoke `/claude-tweaks:capture` as a subroutine to file UI-idea records, when the canonical Component-Skill Contract paragraph 34 lines earlier says the opposite \u2014 visual-review files directly (recordPayload/gh issue create equivalent) and only *recommends* /capture to the user afterward. The sibling row for /claude-tweaks:reflect (line 271) correctly states \"files new backlog records directly, not via /capture,\" showing the visual-review row is the outlier that drifted from the pattern."
    },
    {
      "category": "structural-contract",
      "line": 257,
      "summary": "capture's Relationship-to-Other-Skills table has no row for /claude-tweaks:triage, even though triage/SKILL.md lists capture as an upstream feeder \u2014 a one-directional cross-reference.",
      "failure_scenario": "triage/SKILL.md line 225 states '/claude-tweaks:capture | Feeder \u2014 files raw backlog records; they reach this gate's worklist only after /claude-tweaks:specify shapes them to ready.' but capture/SKILL.md's Relationship table (starting line 257) has no corresponding row for /claude-tweaks:triage (confirmed by grepping the full file: the only 'triage' hits are unrelated prose like 'review and triage backlog' and 'get human triage before promotion', never a table row). A reader relying on capture's own table to understand its downstream consumers will not learn that captured records eventually feed triage's authorization worklist."
    }
  ],
  "skills/build/failure-recovery.md": [
    {
      "category": "altitude",
      "line": 19,
      "summary": "The \"reproduce a behavioral bug before fixing it\" discipline is a cross-cutting concern independently restated with drifting wording in at least four places (build/SKILL.md:235, build/failure-recovery.md:19, test/SKILL.md:225, review/SKILL.md:508) rather than centralized in one `_shared/*.md` fragment, unlike other repeated cross-cutting concerns in this codebase (Working Directory Discipline, git rules, auto-mode routing) which do have a canonical shared home.",
      "failure_scenario": "A future refinement to the reproduce-first protocol (e.g., adding an 'escalate after N failed reproduction attempts' rule, or changing what counts as a valid repro) has to be manually propagated across all four call sites; missing one leaves that skill's copy silently inconsistent with the others \u2014 the exact drift pattern CLAUDE.md's own Don'ts list repeatedly flags for duplicated cross-file prose (e.g., the cardinality-restatement and Relationship-table entries)."
    }
  ],
  "tests/multi-agent-coordination.test.js": [
    {
      "category": "assertions-cannot-fail",
      "line": 58,
      "summary": "Multiple tests hand-construct the exact decision-log-entry string they then assert against with a self-authored regex, without ever calling a coordination.js (or any other) function that actually produces that string, so the assertion can never fail regardless of real behavior.",
      "failure_scenario": "At line 58-62 the test builds `entry` from `unconfirmed[0].path`/`.line` plus literal boilerplate text, then matches it against `/^- (AUTO|STAGED|KEPT-PROMPT) \\d{2}:\\d{2}:\\d{2} \u2014 .+\\. .+\\.$/` \u2014 a pattern written by the same author to match the same string. No exported coordination.js function is invoked to produce this line. The identical pattern recurs at lines 187, 193, 202, 305-316, 340-354, 504-510, and 616-620 (roughly 8 tests total). If the documented decision-log format in skills/review/SKILL.md or skills/_shared/multi-agent-coordination.md is later reworded (e.g. dropped trailing period, changed 'Stage path:' phrasing), or if a helper is later added to coordination.js to actually emit these lines and ships with a formatting bug, none of these tests would ever fail \u2014 they only verify that the test author's regex matches the test author's own string."
    },
    {
      "category": "simplification",
      "line": 515,
      "summary": "A second, independently-written local `applyRedTeamFindings` function (lines 515-523) duplicates the first one (lines 457-478), even though its only call site (line 524, `applyRedTeamFindings(draftSpec, [])`) exercises exactly the empty-findings path the first definition already handles correctly.",
      "failure_scenario": "Calling the first definition (line 457) with `findings = []` skips its `for` loop entirely, leaves `openQuestions` empty, and its `if (openQuestions.length > 0)` guard is false \u2014 so it returns `specBody` unchanged, byte-identical to what the second definition's explicit `if (findings.length === 0) return specBody;` produces. The second copy is provably redundant given its actual usage; a future change to the write-back rule (e.g. how the Open Questions header is formatted) applied to the first definition has no mechanical reason to also touch the second, so the two definitions can silently diverge."
    }
  ],
  "bin/lib/issues/record.js": [
    {
      "category": "conventions",
      "line": 280,
      "summary": "The specShapedBody comment claims 'the three health builders' share this skeleton, but grep confirms four issue-payload builders (code-health, harness-health, journey-health, docs-health) call it, violating CLAUDE.md's rule against restating a list's cardinality as a literal number in prose.",
      "failure_scenario": "grep -rn specShapedBody bin/lib/*/issue-payload.js shows four call sites (code-health, harness-health, journey-health, docs-health), since docs-health was added after this comment was written and after the 'three health producers' phrasing at line 37 (which is accurate for its own, narrower legacy-fingerprint-regex context) was reused loosely here. CLAUDE.md explicitly documents this exact drift shape recurring 8+ times across specs; a reader trusting this comment's literal count would undercount which builders are coupled to this shared skeleton when scoping a future change to it."
    },
    {
      "category": "line-by-line",
      "line": 127,
      "summary": "normalizeLabelNames's `l.name` branch has no null-guard, unlike the two near-identical sibling label-normalization helpers in the same module family (grouping.js's labelNames uses `l && l.name`; metrics.js's labelName uses `(label && label.name) || ''`).",
      "failure_scenario": "If any caller of parseRecordFacets(labels) \u2014 used across dispatch, triage, demo, tidy, review-backlog, help/status-scan, specify, github-pr-scan, assess-agent-autonomy, and bin/lib/code-health/pull-issues.js \u2014 is ever handed a labels array containing a null/undefined entry (e.g. a malformed or partially-redacted `gh api`/`gh issue list --json labels` response), `l.name` throws `TypeError: Cannot read properties of null (reading 'name')` and aborts that skill's entire facet-parsing pass, where grouping.js's and metrics.js's equivalent helpers would instead just skip the bad entry."
    }
  ],
  "bin/lib/harness-health/scope.js": [
    {
      "category": "conventions",
      "line": 30,
      "summary": "parseRulePaths (harness-health/scope.js), parseFilesField (docs-health/freshness.js), and parseJourneyFiles (journey-health/scope.js) are three independently hand-written ~15-line functions that all parse a YAML-style bullet list under a named frontmatter key, an unextracted N>=2 near-identical-consumer duplication.",
      "failure_scenario": "CLAUDE.md's Don'ts list states: \"Don't accept a plan's 'duplicate this across N\u22652 near-identical consumers, no shared module exists yet' framing as final \u2014 extract the shared logic anyway,\" citing this repo's own retry-cli.js extraction (bin/lib/health-core/retry-cli.js, already shared by code-health/harness-health/journey-health/docs-health) as the model to follow. These three parsers ship the violation instead, and have already drifted apart in the wild: docs-health's list-item regex requires `\\s+` after the dash (`/^\\s*-\\s+(.+?)\\s*$/`) while harness-health's and journey-health's accept `\\s*` (`/^\\s*-\\s*(.+?)\\s*$/`), with no shared test to catch further divergence as each is edited independently."
    }
  ],
  "skills/_shared/label-bootstrap.md": [
    {
      "category": "conventions",
      "line": 32,
      "summary": "The work-record label count is hand-typed here as \"23 core + 3 optional\" (and again as \"26\" two lines later), even though CLAUDE.md's own Structure table documents work-record.md's taxonomy as something every consumer \"cites rather than restates.\"",
      "failure_scenario": "A future label is added to or removed from work-record.md's Label taxonomy table (e.g. a new priority:critical). Three independently hand-typed counts \u2014 label-bootstrap.md:32 (\"23 core + 3 optional\"), label-bootstrap.md:34 (\"26\"), and work-record.md:62 (\"23 core labels + 3 optional\") \u2014 all need updating in lockstep with the table, and nothing forces that; this is exactly the recurring failure CLAUDE.md's Don'ts list calls out by name: \"Don't restate a list's cardinality as a literal number in prose ... no single keyword grep catches every differently-worded restatement.\""
    }
  ],
  "skills/init/bootstrap-steps.md": [
    {
      "category": "conventions",
      "line": 395,
      "summary": "Several /init decision prompts (Step 11 here; also Step 10 ~lines 306-317, Step 12 ~451-463, Step 14 ~638-648, Step 15's gate-fails case at 717, and Phase 8 in skills/init/SKILL.md lines 320-328) render a single decision as a plain markdown numbered list inside a 'Present:' block with no accompanying AskUserQuestion call.",
      "failure_scenario": "This directly contradicts the skill's own frontmatter directive (init/SKILL.md line 5): 'Present single decisions via the AskUserQuestion tool ... instead of a plain-text numbered list.' Following these steps literally renders the diagram-suggestions/Impeccable/shadcn/issue-tracking/journey-discovery choices as a bare numbered list requiring the user to type a digit back, even though Step 6 (line 149) and Step 13 (line 610) in the same file show the correct AskUserQuestion pattern was known and available for equivalent single decisions."
    },
    {
      "category": "cross-file",
      "line": 91,
      "summary": "The suggested .gitignore block only lists `.claude-tweaks/code-health/` for the health skills' local cache, omitting the equivalent `.claude-tweaks/harness-health/`, `.claude-tweaks/journey-health/`, and `.claude-tweaks/docs-health/` directories that also hold a per-skill local `cache.json` per `_shared/health-state.md`.",
      "failure_scenario": "A project runs /init and accepts this .gitignore suggestion (or /harness-health, /journey-health, or /docs-health fire before code-health ever does): `.claude-tweaks/harness-health/cache.json`, `.claude-tweaks/journey-health/cache.json`, and `.claude-tweaks/docs-health/cache.json` (confirmed present in bin/lib/harness-health/cache.js, bin/lib/journey-health/cache.js, bin/lib/docs-health/cache.js \u2014 each documents the identical canonical path pattern `<root>/.claude-tweaks/{skill}/cache.json`) are never gitignored and get accidentally committed or flagged as untracked cruft, exactly the class of bug CLAUDE.md's own Don'ts section warns about for this file (\"blanket .claude-tweaks/ suggestion\" bug). The file's own explanatory sentence calls code-health's cache.json 'the only file still written under this path,' which is only true for the code-health subdirectory specifically and reads as if no sibling health skill needs the same treatment."
    },
    {
      "category": "reuse",
      "line": 304,
      "summary": "Step 10 and Step 12 each hardcode the same abbreviated frontend-signal detection list inline instead of referencing the existing canonical skills/design-wrapper/frontend-detection.md, which skills/specify/design-pre-steps.md (line 15, same review scope) already cites correctly for the identical question.",
      "failure_scenario": "The file self-flags the risk at line 444 ('if that list is ever tightened, update it in both places'), and the divergence is already live: the canonical file also matches .scss/.sass/.less/.astro/.mdx, so a Sass- or Astro-only frontend project already fails the abbreviated Impeccable (Step 10) and shadcn (Step 12) sniffs in /init while /specify and /design-wrapper, which read the canonical file, detect it correctly \u2014 a present 3-way inconsistency, not just a future one."
    }
  ],
  "skills/test/qa-prompts.md": [
    {
      "category": "conventions",
      "line": 152,
      "summary": "The 'Legacy format prompt' template \u2014 which the file's own header (line 7) requires to be 'inline[d] ... verbatim per agent (no references to sibling files)' \u2014 tells the dispatched agent to emit REPORT_JSON 'with the same shape documented in the structured-format prompt above,' a reference the agent never actually receives.",
      "failure_scenario": "For a QA run consisting entirely of legacy-format stories, the structured-format prompt (lines 58-117) is never composed or dispatched in that run at all. The agent executing the Legacy format prompt therefore has no REPORT_JSON schema to follow beyond the meaningless-to-it phrase 'the same shape documented ... above' \u2014 it must guess the `caveats`/`recovered_locators`/`page_inventories` shape rather than being given it verbatim, exactly the failure mode this file's own dispatch contract (line 7) and CLAUDE.md's general Don't about inlining literal output templates both exist to prevent."
    }
  ],
  "skills/build/worktree-setup.md": [
    {
      "category": "conventions",
      "line": 53,
      "summary": "The worktree base-mismatch warning template embeds a literal \u26a0 emoji character, violating CLAUDE.md's explicit \"Don't use emojis in skill files \u2014 use **(Recommended)** bold text for emphasis instead.\"",
      "failure_scenario": "Whenever `worktree.baseRef: fresh` pulls a stale `origin/<default-branch>` and the base-ref mismatch check fires, the rendered output is literally \"\u26a0 Worktree base mismatch \u2014 branched from {ACTUAL_BASE short} but expected {EXPECTED_BASE short} ({BASE_BRANCH}).\" \u2014 shipping the exact emoji-in-skill-output pattern the Don'ts list forbids, with no carve-out for user-facing message text."
    }
  ],
  "skills/research/reference/methodology.md": [
    {
      "category": "conventions",
      "line": 51,
      "summary": "Step 4's parallel-subagent dispatch names \"Template C\" by reference instead of inlining its literal 4-line output format, violating CLAUDE.md's explicit rule that dispatched output templates must be inlined verbatim in the agent prompt.",
      "failure_scenario": "CLAUDE.md: \"Don't dispatch parallel Task agents without inlining a literal output template (Template A/B/C) from skills/_shared/subagent-output-contract.md in the agent prompt \u2014 references won't reach the agent.\" A verification subagent spawned from this Step 4 prompt never receives Template C's actual format (\"First line: yes or no / Second line onward: up to 3 bullet lines of evidence... Maximum 200 tokens\") because it has no access to _shared/subagent-output-contract.md \u2014 only the parenthetical gloss \"Template C (yes/no + \u22643 evidence bullets)\" reaches it, leaving the agent free to return an unparseable shape the caller can't mechanically fold into a verdict."
    }
  ],
  ".claude-plugin/plugin.json": [
    {
      "category": "conventions",
      "line": 4,
      "summary": "The plugin's version (6.11.1) has drifted out of sync with CLAUDE.md's own hardcoded restatement of it in prose ('A Claude Code plugin (v5.7.0) containing markdown skill files...', CLAUDE.md line 5), even though CLAUDE.md's own Versioning section states '- Version lives in `.claude-plugin/plugin.json`' (CLAUDE.md line 133) as the single source of truth.",
      "failure_scenario": "Anyone (human or agent) reading CLAUDE.md's opening paragraph to learn what version of the plugin they're working with is told v5.7.0 \u2014 roughly 50+ releases behind the real 6.11.1 in plugin.json. This is the exact silently-desyncing-hardcoded-number pattern CLAUDE.md's own Don'ts list repeatedly warns against for cardinality/counts restated in prose (see the '8-lever'/'17 core labels' Don't entry), just applied to a version string instead of a count."
    },
    {
      "category": "cross-file",
      "line": 4,
      "summary": "plugin.json declares version 6.11.1 while the project's own CLAUDE.md still describes the plugin as 'v5.7.0' in its opening line, a stale cross-file version reference.",
      "failure_scenario": "Any reader (human or agent) who trusts CLAUDE.md's header line ('A Claude Code plugin (v5.7.0)...') as the current version \u2014 rather than checking plugin.json \u2014 will under-report the installed version by dozens of releases; this is the exact recurring failure mode CLAUDE.md's own Don'ts section already documents ('Don't let a phase's version bump depend on remembering to add it... only Phase 1's plan included a version-bump step')."
    }
  ],
  "bin/lib/issues/retry.js": [
    {
      "category": "cross-file",
      "line": 22,
      "summary": "hasHitRetryCeiling(comments, ceiling) counts only already-posted failure comments, so it is off by one relative to what its name promises for the attempt about to run; the repo's only live caller (skills/dispatch/SKILL.md) already knows this and inlines `attemptNumber >= ceiling` instead of calling it, but the caveat is never written into retry.js itself.",
      "failure_scenario": "A future caller that (reasonably, given the name) calls hasHitRetryCeiling(comments, ceiling) before deciding whether to attempt one more retry gets false all the way through the attempt that IS the ceiling-hitting one, because `comments` reflects only prior attempts, not the one about to run \u2014 silently permitting one extra retry beyond the configured ceiling. skills/dispatch/SKILL.md line 297 has an inline comment acknowledging this ('equivalent to hasHitRetryCeiling if comments included this attempt's own (not-yet-posted) comment') and deliberately avoids calling it, but the exported function remains uncorrected and undocumented in its own source for the next caller who won't know to route around it."
    }
  ],
  "bin/lib/journey-health/qa-evidence.js": [
    {
      "category": "cross-file",
      "line": 51,
      "summary": "findingsByStoryId is built as a Map keyed by story_id, so when a story has more than one entry in report.findings[] only the last one survives, silently discarding the rest before the regression check runs.",
      "failure_scenario": "skills/test/qa-reporting.md's own schema allows multiple findings per story_id (a story that auto-recovers a locator gets a separate `stale-selector`/auto-fixed finding, and a PASS_WITH_CAVEATS story with 2+ caveats gets one `ux-issue` finding per caveat) \u2014 nothing about report.findings[] is 1:1 with stories. Concretely: a story auto-recovers a locator early (logged as a non-regression `stale-selector` finding) then genuinely fails later for an unrelated reason (logged as a `code-bug` finding, which IS in REGRESSION_CATEGORIES) \u2014 both entries share the same story_id. If the `stale-selector` entry lands later in report.findings[], `new Map(...)` overwrites the real `code-bug` entry with it, `REGRESSION_CATEGORIES.has('stale-selector')` is false, and evaluateQaEvidence returns `verdict: 'inconclusive'` (\"attributed to QA tooling\") for what is actually a genuine product regression \u2014 journey-health never surfaces it."
    }
  ],
  "bin/lib/code-health/tests/durable-integration.test.js": [
    {
      "category": "cross-file",
      "line": 17,
      "summary": "This file only exercises `retry-queue drain` end-to-end through the real code-health.js CLI; unlike its docs-health sibling (bin/lib/docs-health/tests/durable-integration.test.js, which also tests `retry-queue update <results.json>`), there is no equivalent CLI-level test here for `retry-queue update`, so bin/code-health.js's own main() dispatch line `retryQueueCommands.update({ ...args, _: args._.slice(1) })` is never exercised through the real argv parser.",
      "failure_scenario": "A typo in that rebasing (e.g. `.slice(2)` instead of `.slice(1)`, or dropping the rebase entirely) would make `args._[1]` resolve to the literal string 'update' instead of the results-file path once it reaches makeRetryQueueCommands's update(), causing `fs.readFileSync('update', 'utf8')` to throw ENOENT and the command to exit(1) \u2014 silently breaking the retry-queue step of any scheduled code-health Routine in production. No test in the code-health suite would catch this: the unit-level coverage in bin/lib/health-core/tests/retry-cli.test.js calls makeRetryQueueCommands directly and bypasses argv parsing entirely, and this file (the only CLI-level integration test for code-health's durable-state commands) never invokes 'retry-queue update' at all."
    }
  ],
  "bin/lib/code-health/tests/scope.test.js": [
    {
      "category": "cross-file",
      "line": 8,
      "summary": "MAX_STALE_DAYS is hardcoded to 30 locally with a comment saying it \"mirrors score.js constant\" instead of being imported from ../score, unlike the equivalent docs-health test file (bin/lib/docs-health/tests/scope.test.js, which imports the real STALE_DAYS value directly via `const { STALE_DAYS } = require('../score');`).",
      "failure_scenario": "If bin/lib/code-health/score.js's actual MAX_STALE_DAYS is ever changed (a one-line edit), this file's boundary tests ('selectSlice force-picks a slice unjudged past MAX_STALE_DAYS' and 'selectSlice returns null when the only slice was recently judged') keep computing fixture ages from the stale local literal 30 rather than the real constant selectSlice() actually enforces via score.js. Depending on the direction of drift this either produces a confusing failure that looks unrelated to the actual source change (wasting debugging time) or \u2014 if the new real threshold is smaller than the hardcoded 30 used to build the 'past threshold' fixture \u2014 keeps passing without ever probing where the real new boundary actually is. This is exactly the literal-value-mirroring drift pattern this repo's own CLAUDE.md repeatedly flags as a recurring source of bugs."
    }
  ],
  "bin/journey-health.js": [
    {
      "category": "cross-file",
      "line": 82,
      "summary": "cmdNextTarget calls readDurableState(root) twice per invocation (lines 71 and 82) instead of once, and no test in bin/lib/journey-health/tests/cli-next-target.test.js catches it \u2014 unlike its harness-health sibling, which was fixed for this exact bug and has a regression test for it.",
      "failure_scenario": "Every `journey-health.js next-target` call without --target (the CLI's default path, exercised by tests like 'defaults to the light tier' and '--budget 2') issues two separate `git fetch origin health-state` calls plus duplicate `git show` calls instead of one, doubling network/subprocess cost on every routine firing. Since up to four health engines (code-health/harness-health/journey-health/docs-health) write to the same health-state branch, a concurrent write landing between the two fetches lets coverageScanDue (read at line 71) and the cursors used for target selection (read again at line 82) reflect two different points in time \u2014 exactly the inconsistency commit 2d56e7d fixed in bin/harness-health.js's cmdNextTarget ('so gapScanDue and cursors answer the same point in time... the CLI doesn't pay for a second git fetch/show round-trip'). That same commit added bin/lib/harness-health/tests/cli-next-target.test.js's makeGitSpy-based 'fetches durable state exactly once, not twice' tests and touched bin/lib/journey-health/scope.js in the same diff, but never touched bin/journey-health.js or bin/lib/journey-health/tests/cli-next-target.test.js \u2014 confirmed by `git show 2d56e7d --stat` and by grepping readDurableState(root) call sites in both CLI files (harness-health: 1 call in cmdNextTarget; journey-health: 2)."
    },
    {
      "category": "efficiency",
      "line": 71,
      "summary": "cmdNextTarget calls readDurableState(root) twice per invocation instead of once, and each call performs a real `git fetch origin health-state` network round-trip.",
      "failure_scenario": "Every `journey-health.js next-target` call made without --target (the normal scheduled-routine path) pays for two full git-fetch + three-git-show sequences against the health-state branch instead of one: line 71 reads it to compute coverageScanDue, then line 82 reads it again for the budget loop's cursors. The sibling function in bin/harness-health.js (lines 67-77) explicitly reads durable state once into `durableCursors` and reuses it for the same two purposes, with a comment stating this is precisely so the CLI 'doesn't pay for a second... round-trip per invocation' \u2014 journey-health.js's otherwise near-identical function doesn't follow that pattern."
    }
  ],
  "bin/lib/issues/tests/labels.test.js": [
    {
      "category": "cross-file",
      "line": 38,
      "summary": "REAL_LABEL_DESCRIPTIONS still cap-checks six retired code-health:risk-*/code-health:effort-* label names that no code anywhere in the repo emits anymore, while the live risk:*/effort:*/by:* labels that replaced them carry zero 100-char-cap regression coverage.",
      "failure_scenario": "A repo-wide `grep -rn 'code-health:risk-\\|code-health:effort-' bin/ skills/` returns zero hits outside this one test file \u2014 the taxonomy migrated to shared risk:*/effort:*/by:* labels (spec 13/15, confirmed in bin/lib/issues/record.js, skills/_shared/label-bootstrap.md, and every health engine's issue-payload.js) after this array was authored on 2026-07-11 (commit 8289fb6, when code-health:risk-*/effort-* were still real). A later commit (c95b8d5, 'retire tier rows from the cap test') pruned the equally-dead tier:* rows from this same array but left these six untouched. If a maintainer now edits label-bootstrap.md's live risk:low/by:code-health/etc. description text past GitHub's 100-char cap, this test \u2014 whose own comment states its purpose is 'a single place that would have caught... 100-char overruns before they shipped' \u2014 stays green, and the overrun would only surface later as a live 422 from `gh label create`, the exact failure mode ensureLabelPayload and this test exist to prevent."
    },
    {
      "category": "reuse",
      "line": 48,
      "summary": "REAL_LABEL_DESCRIPTIONS hand-copies 7 of its 18 entries verbatim from skills/_shared/label-bootstrap.md's own canonical LABELS_JSON instead of reading that file, so the two can silently drift apart.",
      "failure_scenario": "auto:build, auto:merge, bot:in-progress, bot:blocked, wontfix, ready, and parked at labels.test.js:48-54 are currently byte-identical to the corresponding rows in skills/_shared/label-bootstrap.md:52-61's 'Canonical LABELS_JSON' \u2014 the actual source substituted into every real `gh label create` bootstrap flow (confirmed by direct comparison of both files). If a future edit rewords one of those descriptions in label-bootstrap.md without a matching edit here \u2014 nothing forces the two together, one is markdown prose and the other a Node test file \u2014 this test keeps validating its own stale copy and stays green, while the real `gh label create --description` built from the changed label-bootstrap.md text could exceed GitHub's 100-char cap and fail at runtime with a 422 \u2014 exactly the class of bug the array's own comment (lines 33-35) says it exists to catch 'before they shipped.'"
    }
  ],
  "skills/_shared/auto-mode-contract.md": [
    {
      "category": "cross-file",
      "line": 29,
      "summary": "The Bookend Architecture's \"Begin stop\" policy-lever list omits `ceremony-profile` (lever #10), even though this same file's own \"Adding a new policy lever\" checklist later in the file names it as an already-added lever and instructs updating this exact list.",
      "failure_scenario": "A reader or agent consulting this file's canonical lever list \u2014 the place every pipeline skill is told to look instead of restating policy semantics inline (per this file's own \"Per-skill compliance\" section) \u2014 sees only 8 levers ending at `unattended-tier` and would conclude `ceremony-profile` is not part of the Manifesto's begin-stop policy set. This contradicts `flow/SKILL.md` Step 3 (which lists it explicitly), `flow/manifesto.md` (whose canonical numbering states \"10=Ceremony profile\"), `help/reference-card.md`, and `help/context-flow.md` \u2014 all four sibling consumer files were correctly updated when the lever was added, but this file's own line 212 checklist item 1 (\"This file \u2014 add the lever name to the Bookend Architecture's computed-levers list\") was not followed for its own list."
    }
  ],
  "skills/_shared/design-wrapper-handling.md": [
    {
      "category": "cross-file",
      "line": 14,
      "summary": "Asserts survey mode returns a `{deferred: {reason: ...}}` shape (\"currently: only `survey` mode is ever deferred\"), but `design-wrapper/modes/survey.md`'s own complete \"Output to caller\" contract never documents or returns any `deferred` shape anywhere.",
      "failure_scenario": "Caller skills (/build's design-prebuild.md, /test, /review) each carry a \"should not happen for this mode, treat as skip\" branch for a `{deferred: ...}` reply, trusting this file's premise that survey mode can legitimately produce one. But a case-insensitive grep of the entire skills/design-wrapper/ tree (SKILL.md, command-map.md, modes/survey.md) for \"defer\" turns up zero instances of the wrapper actually emitting this shape \u2014 survey.md's documented Output to caller is only `{mode, result: \"ok\", context, recommendations, suppressed}` or a skip object (Step 1). The `deferred` return-shape category this file defines and every caller special-cases is dead prose no caller can ever actually observe from the real implementation."
    }
  ],
  "skills/wrap-up/review-console.md": [
    {
      "category": "cross-file",
      "line": 258,
      "summary": "\"On approval\"/\"On override\" (and the Empty-console fast path) instruct proceeding to \"Step 10 (Consolidated Summary)\", but per wrap-up/SKILL.md's actual headings Step 9 is \"Present Consolidated Summary\" and Step 10 is \"Execute Approved Actions\" \u2014 the parenthetical names the wrong step.",
      "failure_scenario": "After resolving the Review Console (Step 8.6), an executor following \"Proceed to Step 10 (Consolidated Summary)\" literally jumps straight into Step 10's cleanup execution and skips Step 9 entirely \u2014 including Step 9's mandatory per-spec summary template and the \"Work archived...\" closure line that Step 10's own \"Verify execution\" checklist expects to already have run. Line 255 two lines earlier in the same file correctly treats Step 10 as the execution step (\"Execute cleanup actions... \u2014 Step 10 picks these up\"), directly contradicting line 258's \"(Consolidated Summary)\" label; the same mislabel recurs at line 267 and line 275."
    },
    {
      "category": "cross-file-promise",
      "line": 112,
      "summary": "The Review Console's \"Numbering rules\" state \"Sections 1\u20136 (Auto-applied through Cleanup)\" share one global row sequence and label Queue writes as \"Section 7,\" but the actual template lists 7 named batch sections before Queue writes (Auto-applied, Pending review, Low-confidence findings, Contested findings, Skill updates, Configuration updates, Cleanup actions), making Queue writes really the 8th section \u2014 contradicting wrap-up/SKILL.md line 303's own correct \"up to seven sections\" description of the same console.",
      "failure_scenario": "The stale \"Section 7\" label has already propagated: skills/flow/multispec-review-console.md cites \"wrap-up/review-console.md's Section 7\" twice (lines 40 and 199) as the canonical reference point for the Queue-writes-is-never-bulk-approved rule, so any future reader or automated check that keys off \"Section 7\" to locate Queue writes in the numbering scheme will point at the wrong section."
    },
    {
      "category": "delegation",
      "line": 275,
      "summary": "The canonical \"Empty-console fast path\" here requires 5 conditions (adds \"no cleanup actions apply\" and \"no queue writes are pending\" to the base three), but wrap-up/SKILL.md's own inline restatement of the same rule (line 307) lists only 3 conditions \u2014 a duplicated rule drifted from the source it summarizes.",
      "failure_scenario": "A run with an empty `decisions.md`, empty `staged/`, and no skill/config updates, but with a pending queue write (e.g. a leftover-routing proposal) or an applicable cleanup row (pipeline-run-directory archival always applies once a run dir exists), satisfies SKILL.md's narrower 3-condition test and is routed to \"skip the console and proceed to Step 9,\" while review-console.md's own fuller rule would keep the console open \u2014 silently dropping the queue-write approval prompt or cleanup visibility."
    },
    {
      "category": "line-by-line",
      "line": 112,
      "summary": "\"Numbering rules\" states sections 1\u20136 span \"Auto-applied through Cleanup,\" but the worked example immediately below uses one continuous 1\u201315 sequence across seven named sections (Auto-applied, Pending review, Low-confidence findings, Contested findings, Skill updates, Configuration updates, Cleanup actions) before Queue writes starts its own Q-prefixed count.",
      "failure_scenario": "The example's own rows require 7 sections before Queue writes (rows 1-4 Auto-applied, 5-7 Pending review, 8-9 Low-confidence, 10 Contested, 11-12 Skill updates, 13-14 Configuration updates, 15 Cleanup actions) \u2014 contradicting \"Sections 1\u20136\" two lines above in the same file, and contradicting wrap-up/SKILL.md line 303's own \"up to seven sections\" claim, which correctly lists all seven names. An implementer following the \"Sections 1-6\"/\"Section 7 is Queue writes\" rule would mislabel Cleanup actions as Section 6 and misnumber Queue writes as Section 7 instead of 8."
    }
  ],
  "skills/wrap-up/SKILL.md": [
    {
      "category": "cross-file",
      "line": 207,
      "summary": "Step 8's `work-backend: github-issues` \"newly unblocked records\" check finds dependents solely via `parseDependencies(r.body)` (matches only literal `Blocked by #N` body-text lines) with no branch for `work-links: native`, even though `bin/lib/issues/record.js` ships `buildNativeDependencyQuery`/`hasOpenNativeBlocker` specifically for that mode.",
      "failure_scenario": "Under `work-links: native`, per specify/SKILL.md's own decomposition Linking-pass description (\"native's Linking pass writes no body text at all\"), a leaf's dependency is recorded only as a GraphQL sub-issue/blockedBy relationship \u2014 so `parseDependencies(r.body)` returns `[]` for every open record's body, and this Step 8 feature silently reports zero newly-unblocked records on every record closure, forever, under that configuration, even when real dependents exist."
    },
    {
      "category": "cross-file",
      "line": 185,
      "summary": "Step 7's summary sentence says skill-curation.md's 7.3 substep is a \"6-dimension analysis,\" but skill-curation.md's own 7.3-7.5 section (and CLAUDE.md's Structure table) both describe it as applying `_shared/harness-health-analysis.md`'s documented \"8-dimension check.\"",
      "failure_scenario": "Anyone auditing or extending Step 7's skill-curation procedure against this SKILL.md's own prose (without cross-checking skill-curation.md or harness-health-analysis.md) will believe only 6 of the 8 documented dimensions apply during wrap-up's skill curation, potentially causing a future edit to skill-curation.md's dimension list to be mis-validated against the wrong count, or a reviewer to flag a correct 8-dimension implementation as over-scoped relative to this stated 6."
    },
    {
      "category": "factual-claim",
      "line": 117,
      "summary": "Step 6's fast-lane pre-check says to feed `git diff --name-only` output into `bin/lib/issues/blast-radius.js`'s `classifyDiffFiles(files, sensitivePaths)`, but that function reads `f.path` off each element of `files` \u2014 bare filename strings have no `.path`, so every entry silently classifies as `isSensitive: false` regardless of content.",
      "failure_scenario": "`git diff --name-only` returns bare path strings (e.g. `[\"skills/build/SKILL.md\"]`). Passed directly as `files`, each mapped entry becomes `{path: undefined, isSensitive: false, ...}` since a string has no `.path` property \u2014 so a diff that clearly touches a `docs/REGISTRY.md`-matched path still evaluates \"matches none of the Auto-detect patterns,\" letting the fast-lane pre-check wrongly conclude the condition holds and skip the doc-update sub-scans it exists to gate."
    }
  ],
  "skills/help/SKILL.md": [
    {
      "category": "cross-file",
      "line": 54,
      "summary": "SKILL.md Section 2 summarizes status-scan.md as covering 'sub-stages 4.5 and 4.6,' omitting Stage 4.7 (Acceptance Queue), which status-scan.md's own header (line 5) and body fully define.",
      "failure_scenario": "status-scan.md line 5 states 'Dispatch Stages 1-7 including sub-stages 4.5, 4.6, and 4.7' and later documents a complete '## Stage 4.7: Acceptance Queue' section (demo:pending count, rendered as a dashboard 'Acceptance Queue' block). SKILL.md's own summary of that same sub-file (line 54) undercounts it as only 'sub-stages 4.5 and 4.6.' Anyone trusting SKILL.md's summary instead of fully reading status-scan.md \u2014 including a future editor deciding what to update when adding an 8th stage \u2014 could reasonably conclude Stage 4.7/Acceptance Queue doesn't exist and drop it from the dashboard."
    },
    {
      "category": "cross-file-promise",
      "line": 141,
      "summary": "/help's and /claude-tweaks:ledger's Relationship tables both claim \"/help ... scans for active ledgers with open items and surfaces them in the status dashboard,\" but /help's actual Stage 1-7 scan procedure (skills/help/status-scan.md) never reads any ledger file and the Present Dashboard template has no ledger row.",
      "failure_scenario": "skills/build/SKILL.md Common Step 4 appends blocked items to the open items ledger (`docs/plans/*-ledger.md`, phase `build/*`, status `open`) and explicitly tells the implementer \"These will be picked up by /claude-tweaks:help when scanning for actionable work\" (build/SKILL.md line 221) \u2014 but running `/claude-tweaks:help` afterward never surfaces those blocked items on the dashboard, so they silently sit unnoticed until a `/wrap-up` run (if any) eventually resolves the ledger."
    },
    {
      "category": "line-by-line",
      "line": 34,
      "summary": "The Input table says the `status` argument produces 'Pipeline status scan only,' but Section 3 (Recommendation)'s own skip condition ('Always included unless `$ARGUMENTS` = `commands`') means Section 3 renders for `status` too, contradicting the table.",
      "failure_scenario": "Running `/claude-tweaks:help status`: Section 1 skips (its stated condition is `$ARGUMENTS = status`), Section 2 does not skip (its condition is `$ARGUMENTS = commands`), and Section 3 does not skip either (its condition is 'unless `$ARGUMENTS = commands`', and `status != commands`). So both the status scan and the full Recommendation/Priority-Order/Tie-Breaking section render \u2014 directly contradicting the Input table's promise of 'Pipeline status scan only' for that argument. Whoever executes the skill has no way to tell from the doc alone whether the table or the skip-condition is the intended behavior."
    },
    {
      "category": "line-by-line",
      "line": 36,
      "summary": "The Input table promises 'Targeted status for that specific spec/topic' when `$ARGUMENTS` is a spec number or topic, but no section-skip condition or stage in status-scan.md implements any spec/topic filtering \u2014 the argument falls through to the same full, generic rendering as no argument at all.",
      "failure_scenario": "Running `/claude-tweaks:help 42` or `/claude-tweaks:help checkout-flow`: none of the three section-skip conditions (`= status`, `= commands`, `unless commands`) match a bare spec number or topic string, so Section 1 (cheat sheet), Section 2 (full Stages 1-7 scan), and Section 3 (recommendation) all render in full \u2014 identical to the no-argument case. status-scan.md's Stages 1-7 procedure and Present-Dashboard template contain no per-spec/per-topic filter anywhere. The user asking for a targeted view of one spec instead receives the entire untargeted dashboard, with no documented mechanism ever narrowing it."
    }
  ],
  "skills/triage/SKILL.md": [
    {
      "category": "cross-file",
      "line": 236,
      "summary": "The Relationship-to-Other-Skills table lists `bin/lib/issues/record.js` as two separate, differently-worded rows (lines 236 and 237) instead of one consolidated entry.",
      "failure_scenario": "A future edit to how record.js is used by triage (e.g. adding/removing a function, changing parseRecordFacets' role) is applied to only one of the two rows since they read as unrelated entries, leaving the other stale \u2014 exactly the 'same fact restated in two places drifts independently' failure this project's own CLAUDE.md documents as having recurred multiple times elsewhere (paired Relationship tables, cardinality restatements). Line 236 describes parseRecordFacets' risk/effort fields feeding display-tier computation and grant-check's input; line 237 describes the same function as Step 1's ready-queue filter \u2014 a maintainer skimming for 'the record.js row' to update is likely to find and edit only one."
    },
    {
      "category": "simplification",
      "line": 236,
      "summary": "The Relationship-to-Other-Skills table has two separate rows for the same file, `bin/lib/issues/record.js`, instead of one consolidated row.",
      "failure_scenario": "A reader scanning the Relationship table for what `bin/lib/issues/record.js` does sees two adjacent-but-not-merged entries (lines 236 and 237) describing different facets of the same dependency \u2014 the kind of redundant table row the codebase's own cross-reference conventions exist to avoid; a future edit to one row (e.g. correcting or extending the description) has no structural reason to also touch the other, so the two descriptions can drift apart over time."
    }
  ],
  "bin/harness-health.js": [
    {
      "category": "cross-file",
      "line": 144,
      "summary": "cmdValidateFindings in harness-health.js, docs-health.js, and journey-health.js has no hard-gate requiring --target (or --slice) on a non-dry-run invocation, unlike code-health.js which explicitly exits with an error in this situation.",
      "failure_scenario": "code-health.js:190-197 hard-errors ('validate-findings: --slice is required for a real (non-dry-run) run \u2014 without it, the round-robin cursor for this slice never persists and rotation state silently drifts') when --slice is omitted on a real run. The sibling engines have no such guard: bin/harness-health.js's cmdValidateFindings, bin/docs-health.js's cmdValidateFindings, and bin/journey-health.js's cmdValidateFindings all accept a missing --target silently. buildValidateFindingsUpdate in each engine's cache.js only patches the per-target cursor 'if (target) {...}' (or 'if (target && kind)' for harness-health), so a caller that forgets --target (a flag typo, or a skill-prompt drift) still writes the run record and dedup cache correctly but never advances that target's rotation cursor \u2014 the exact 'rotation state silently drifts' failure code-health explicitly protects against, silently reproduced in the other three engines. The target gets perpetually re-selected as stale/overdue by selectByStaleThenChurn's Phase 1 on every future run, repeatedly re-judging it and generating duplicate (deduped, but LLM-judge-cost-wasting) findings."
    },
    {
      "category": "reuse",
      "line": 41,
      "summary": "loadIssueIndex, cmdChurnReport, and cmdMark are each duplicated near-verbatim (differing only in a bracketed tool-name/filename string) across 3-4 of the four health-suite CLI files, none relocated to bin/lib/health-core/ alongside the computeChurn/retry-queue helpers these same files already import from there.",
      "failure_scenario": "The identical blocks recur at docs-health.js:36-60 (loadIssueIndex), docs-health.js:167-198 (cmdChurnReport), docs-health.js:202-214 (cmdMark), journey-health.js:41-65/186-217/221-233 (same three), and code-health.js:62-86/114-144 (loadIssueIndex + cmdChurnReport only). A behavior change to any of the three \u2014 e.g. a new mark-able status, or an added churn-report column \u2014 requires hand-editing 3-4 files identically; missing one leaves that engine silently on the old behavior with no test forcing the drift to surface."
    }
  ],
  "bin/lib/docs-health/tests/skill-md.test.js": [
    {
      "category": "cross-file",
      "line": 41,
      "summary": "The house-section-ordering test's idx() helper resolves '## Next Actions' to its first occurrence \u2014 the boilerplate '> **Interaction style:**' directive's backticked mention near line 5 \u2014 not the real '## Next Actions' heading further down, so the ordering assertions never actually check the real heading's position; this identical bug (already flagged for bin/lib/code-health/tests/skill-md.test.js) independently recurs, unflagged, in this file and in bin/lib/harness-health/tests/skill-md.test.js:42 and bin/lib/journey-health/tests/skill-md.test.js:42.",
      "failure_scenario": "If skills/docs-health/SKILL.md's real '## Next Actions' heading were ever moved to after '## Anti-Patterns' or '## Relationship to Other Skills' (a genuine structural regression this test exists to catch), the test would still pass, because idx('## Next Actions') is pinned to the early directive-text mention (which necessarily precedes every real section heading) rather than the actual heading position \u2014 the same false-positive-proof gap already identified in the code-health sibling, but here present in three additional, independently-executed test files."
    },
    {
      "category": "reuse",
      "line": 33,
      "summary": "docs-health/tests/skill-md.test.js duplicates the majority of code-health/tests/skill-md.test.js's assertion bodies verbatim instead of sharing one parameterized SKILL.md house-structure checker.",
      "failure_scenario": "The 'has the required house sections in order' test body (idx()-based ordering checks for When to Use / Anti-Patterns / Component-Skill Contract / Relationship to Other Skills / Next Actions) is byte-for-byte identical between code-health/tests/skill-md.test.js:29-41 and docs-health/tests/skill-md.test.js:33-44, as are the 'carries the standard interaction-style directive', '$PIPELINE_RUN_DIR' wiring, and 'no emojis' tests, plus the identical required-token forEach-loop pattern (just different token lists). When CLAUDE.md's own documented SKILL.md house-structure convention changes (a new required section, a reordering, a new frontmatter rule), a maintainer must find and hand-patch every copy; nothing enforces the copies staying in sync, and the same shape likely recurs in harness-health's and journey-health's skill-md tests too."
    }
  ],
  "skills/_shared/subagent-output-contract.md": [
    {
      "category": "cross-file",
      "line": 106,
      "summary": "The canonical \"Used by\" list for Template A (a markdown-table findings format) claims /challenge's per-lens proposers use Template A, but challenge/SKILL.md's actual dispatch template (Process Step 3) specifies a completely different output shape \u2014 \"free-form 2-4 paragraphs,\" no severity/path/line/finding/evidence table, no \"No findings.\" sentinel.",
      "failure_scenario": "A future skill author or dispatcher trusts this file's own consumer list as the source of truth and inlines Template A's markdown-table format into a /challenge lens-proposer dispatch (per this contract's own instruction to \"inline the template literally\"), producing agents that return structured findings tables instead of the free-form debiasing prose challenge/SKILL.md's aggregator (Step 4) expects to synthesize \u2014 breaking the MoA aggregation step, which is written to consume paragraph-form perspectives, not table rows."
    }
  ],
  "skills/journey-health/SKILL.md": [
    {
      "category": "cross-file",
      "line": 313,
      "summary": "journey-health's Relationship-table row for harness-health claims 'this skill's own Step 3.6 mirrors harness-health's Step 5,' but harness-health's actual Step 5 is 'GATHER OPEN ISSUES for dedup' (fetching existing issues for dedup), not a verify-gate step \u2014 harness-health has no separate numbered VERIFY GATE step at all (its verify gate is embedded inside Step 3's application of the shared harness-health-analysis.md procedure, per that skill's own Anti-Patterns table).",
      "failure_scenario": "A maintainer reads journey-health/SKILL.md's Relationship table to understand the parallel SELECT->JUDGE->VERIFY GATE->FINGERPRINT/DEDUP->FILE pipeline shape shared across the four health skills, then opens harness-health/SKILL.md expecting a distinct 'Step 5: VERIFY GATE' \u2014 instead finds Step 5 is 'GATHER OPEN ISSUES for dedup' and no standalone verify-gate step exists in harness-health's numbered workflow, breaking the claimed step-for-step parity and misleading anyone trying to align the two skills' numbering (e.g. when adding a new sibling health skill or auditing cross-skill consistency). Contrast with the correct, verified analogous claims: docs-health/SKILL.md's own row and journey-health's docs-health row both correctly cite docs-health's Step 3.5 as the verify gate."
    },
    {
      "category": "structural-contract",
      "line": 3,
      "summary": "journey-health omits `allowed-tools` even though it never calls Write/Edit/Task and is documented as sharing code-health's exact pipeline shape and 'never edits' contract.",
      "failure_scenario": "Same pattern as harness-health: journey-health/SKILL.md's own description states 'Never edits journeys, stories, or code' and its Relationship table describes itself as a 'Sibling health skill' mirroring code-health's SELECT->JUDGE->VERIFY->FINGERPRINT/DEDUP->FILE shape, and a full-file grep confirms no Write/Edit/Task calls in the body \u2014 the same tool profile code-health restricted itself to via `allowed-tools`. journey-health declares no `allowed-tools` line at all, so unlike code-health it has no frontmatter-level backstop against an accidental Write/Edit call despite promising the same behavioral guarantee."
    }
  ],
  "CLAUDE.md": [
    {
      "category": "cross-file-promise",
      "line": 5,
      "summary": "CLAUDE.md's own opening line states the plugin is \"v5.7.0\" while the actual version in `.claude-plugin/plugin.json` (the documented single source of truth per the Versioning section) is 6.11.1.",
      "failure_scenario": "Anyone (human or agent) reading CLAUDE.md's first paragraph to gauge what capabilities the installed plugin has will conclude it predates every feature shipped between v5.7.0 and v6.11.1 \u2014 the unified GitHub-issues work-record system, ceremony tiering, health-state durable storage, assess-agent-autonomy, etc. \u2014 none of which existed at v5.7.0. The line was apparently never touched across dozens of subsequent version bumps despite the Versioning section's own instruction to treat plugin.json as canonical."
    },
    {
      "category": "cross-file-promise",
      "line": 23,
      "summary": "CLAUDE.md's Dependencies line lists label-bootstrap.md's consumers as \"capture/specify/triage/dispatch/tidy/wrap-up/init/code-health/harness-health/journey-health/docs-health/flow,\" omitting /demo and /review-backlog, both of which genuinely bootstrap labels through `_shared/label-bootstrap.md` (demo's `demo:changes-requested` check-then-create loop at demo/SKILL.md:155; review-backlog's `priority:*` bootstrap with its own explicit Relationship-table row citing the file as canonical at review-backlog/SKILL.md:274).",
      "failure_scenario": "A future change to label-bootstrap.md's check-then-create snippet or LABELS_JSON shape that greps CLAUDE.md's consumer list to find every call site needing the update will miss /demo and /review-backlog's copies, leaving them silently out of sync with the canonical snippet."
    }
  ],
  "skills/_shared/work-record.md": [
    {
      "category": "cross-file-promise",
      "line": 261,
      "summary": "CLAUDE.md's Backlog-integration note points to \"work-record.md's Config keys table\" as documenting that every consumer skill reads the legacy `backlog-backend` flag as a read-only alias of `work-backend`, but that table never mentions `backlog-backend` and instead states \"per-skill aliases and env-var renames are forbidden\"; in practice only capture, challenge, and tidy implement the alias fallback \u2014 dispatch, triage, demo, specify, flow, wrap-up, review, ledger, and review-backlog all read `work-backend` directly with no alias mention.",
      "failure_scenario": "A pre-6.0 project whose CLAUDE.md still has only `backlog-backend: github-issues` (no `work-backend:` line) will have /capture, /challenge, and /tidy correctly detect github-issues, but /dispatch and /triage will treat `work-backend` as missing, fall back to the `local-files` default, and incorrectly report the feature \"not applicable\" and refuse to run; /demo, /specify, /flow, /wrap-up, /review, and /ledger would similarly default to the wrong backend."
    }
  ],
  "skills/challenge/SKILL.md": [
    {
      "category": "delegation",
      "line": 184,
      "summary": "Step 4 claims to 'Inline the verbatim Mode 4 aggregator instruction template' from _shared/multi-agent-coordination.md, but the inlined text reads 'candidate debiasing perspectives' instead of the primitive's canonical 'candidate responses' \u2014 a paraphrase the primitive's own Anti-Patterns table explicitly forbids.",
      "failure_scenario": "_shared/multi-agent-coordination.md's Mode 4 section states the aggregator instruction is 'verbatim, across all callers' and its Anti-Patterns table lists 'Paraphrasing the MoA aggregator instruction' as a failure ('Variations produce inconsistent synthesis quality'). challenge/SKILL.md line 184 substitutes 'candidate debiasing perspectives' for the canonical 'candidate responses,' so the one caller this file inspected to check delegation already violates the 'do not paraphrase' rule it claims to follow \u2014 a maintainer diffing the two files against the stated verbatim contract finds a real mismatch."
    },
    {
      "category": "line-by-line",
      "line": 67,
      "summary": "The 'Work Record Output Handling' section states /challenge posts its debiasing findings as GitHub issue comments (or appends to the local record file) when the input is a work record reference, but no step in the actual Process (Steps 1-5) ever performs a gh issue comment / record-append call.",
      "failure_scenario": "A user runs /claude-tweaks:challenge #42 (a work record reference). Per Step 5, the skill writes the brief to docs/plans/{date}-{topic}-brief.md and appends one AUTO entry to decisions.md, then proceeds to the Brief Self-Review pass and Next Actions \u2014 nothing in the executed steps ever calls `gh issue comment` or local-store.js's write/append API to post findings back onto record #42, so the section's claim that 'the work record is the durable home for record-backed challenges' never actually happens; only the docs/plans/ brief exists."
    }
  ],
  "bin/lib/code-health/scope.js": [
    {
      "category": "efficiency",
      "line": 95,
      "summary": "contentHash() (line 62) and sliceLoc() (line 95) each independently call sourceFiles(absDir) and then fully re-read every file it returns, so selectSlice's Phase-2 scoring does two `find` subprocess spawns and two full read-passes over every source file in a slice instead of one.",
      "failure_scenario": "Every next-slice/churn-report invocation that reaches Phase 2 for a slice whose content hash changed since the last audit (the common case for any actively-developed slice, since an unchanged hash short-circuits before sliceLoc is ever called) re-spawns the `find` subprocess a second time and re-reads every one of that slice's source files from disk a second time \u2014 once as a Buffer in contentHash, once as utf8 in sliceLoc \u2014 doubling subprocess and disk I/O cost, worse the larger the slice."
    },
    {
      "category": "line-by-line",
      "line": 45,
      "summary": "sourceFiles()'s SKIP_DIRS exclusion pattern `${absDir}/${dir}/*` only matches a skip-directory when it is a direct child of the scanned slice root, not when it appears deeper in the subtree, so nested node_modules/dist/build/coverage directories are hashed and counted as source.",
      "failure_scenario": "Empirically verified with GNU find semantics: `find <absDir> -type f -not -path \"<absDir>/node_modules/*\"` excludes `<absDir>/node_modules/x.js` but does NOT exclude `<absDir>/pkg/nested/node_modules/x.js`, since the pattern requires node_modules to immediately follow absDir with no intervening path segment. For any target repo code-health is run against with a slice containing a nested vendor/build directory more than one level deep (common in monorepo packages with their own node_modules, or nested dist/coverage output), contentHash() and sliceLoc() silently include that vendor/build content as if it were source \u2014 producing spurious 'content changed' churn signals and inflated/wrong hotspot scores for that slice on every run."
    }
  ],
  "tests/statusline.test.js": [
    {
      "category": "efficiency",
      "line": 15,
      "summary": "`runStatusline()` creates a fresh temp HOME directory via `fs.mkdtempSync` on every call (line 15) but never removes it, unlike the file's other three fixture helpers (`withLedgers`, `withSpecs`, and the `renderGit` test's `repoDir`), which all clean up in a `finally` block.",
      "failure_scenario": "This file calls `runStatusline` 14 times total (7 individual end-to-end tests at lines 323, 335, 348, 363, 371, 376, 381, plus the 7-iteration timing loop at line 398), each leaving a `ct-sl-*` directory behind under the OS temp dir with no corresponding `fs.rmSync` anywhere in the file \u2014 repeated local test runs (or CI runs on a persistent temp volume) accumulate these indefinitely."
    },
    {
      "category": "setup-teardown-asymmetry",
      "line": 15,
      "summary": "The runStatusline() helper creates a fresh temp HOME directory via fs.mkdtempSync on every call and never removes it, unlike every other temp-directory helper in the same file.",
      "failure_scenario": "Every one of the roughly 10 call sites of runStatusline() in this file (9 distinct end-to-end tests, one of which loops 7 times for the timing test) leaves an orphaned 'ct-sl-XXXXXX' directory under os.tmpdir() with no matching fs.rmSync cleanup \u2014 contrast with withLedgers()/withSpecs() (try/finally + fs.rmSync) and the renderGit test's own try/finally in this same file, all of which do clean up. Each `npm test` invocation leaks 15+ directories permanently; repeated CI/dev runs over the project's lifetime accumulate unbounded temp-directory and inode usage that nothing in the suite ever reclaims."
    },
    {
      "category": "simplification",
      "line": 213,
      "summary": "`withLedgers` (213-225) and `withSpecs` (287-299) are structurally identical temp-directory fixture scaffolding \u2014 mkdtemp, mkdir a fixture subdir, write files, run `fn(cwd)`, clean up in `finally` \u2014 differing only in the mkdtemp prefix and the `docs/plans` vs `specs` subpath.",
      "failure_scenario": "Any future change to the fixture lifecycle (e.g. also asserting the temp dir was actually removed, or swapping the base tmp dir) has to be hand-applied to both copies; a single `withFixtureDir(subpathSegments, files, fn)` helper would need the fix once instead of twice, and the two have already diverged trivially (variable names `plans` vs `specs`) with no functional reason to."
    }
  ],
  "skills/_shared/github-pr-scan.md": [
    {
      "category": "efficiency",
      "line": 52,
      "summary": "The `repo-wide` scope's 8 steps \u2014 open PRs, a per-open-PR GraphQL loop, four separate health-issue-label queries, a merged-PR check, and a grant-queue count \u2014 are independent gh/bash calls with no `> **Parallel execution:**` directive, unlike the pattern this same shared-file corpus uses elsewhere (dev-url-detection.md Step 1, journey-coverage-check.md).",
      "failure_scenario": "The single agent this scope is inlined into (per this file's own \"the dispatcher inlines the relevant scope section ... into the scan agent's prompt\" contract) has no instruction to batch these calls, so it runs item 2's \"once per open PR\" loop and items 1/3/5/6/7/8's mutually-independent gh calls one at a time \u2014 every `/tidy` Step 4.8 run and every `/help` Stage 4.5-4.7 run pays N sequential round-trips that parallel tool calls would collapse to one batch."
    },
    {
      "category": "simplification",
      "line": 125,
      "summary": "The \"Findings and recommendations\" table repeats an identical stale/still-valid row-pair four times \u2014 once each for code-health, harness-health, journey-health, docs-health \u2014 with only the skill name substituted, instead of one row generalized over the by:{skill} label family.",
      "failure_scenario": "A fifth health skill is added later (this repo has already gone code-health -> harness-health -> journey-health -> docs-health), and a maintainer has to notice and copy-paste a 5th near-identical pair by hand rather than one generic row already covering it. The harness-health row also carries a one-off aside (\"harness-health never applies patches directly\") that a maintainer must decide anew for each copy, even though harness-health/SKILL.md Step 7 (cited in this same _shared corpus's harness-health-analysis.md) states it \"no longer applies anything ... it always files\" \u2014 i.e. the same report-only posture the other three rows already share without calling it out, making the divergence look skill-specific when the underlying table structure is what's actually inconsistent."
    }
  ],
  "skills/_shared/harness-health-analysis.md": [
    {
      "category": "efficiency",
      "line": 63,
      "summary": "Step 1's 8 Evidence Pre-Checks (stale-example ls, convention-drift grep, glob-resolution find, line-budget wc, unscoped-rule sed, self-referential grep, narrative-density grep+wc, bare-invocation grep) are independent read-only checks against the same target with no `> **Parallel execution:**` directive, unlike the convention this shared-file corpus establishes elsewhere.",
      "failure_scenario": "A `/claude-tweaks:wrap-up` Step 7 or `/claude-tweaks:init` Phase 3/6 run with several skills in its read set executes checks 1-8 sequentially per target instead of firing the independent bash/grep/ls calls together, adding N-times-8 avoidable round-trips to a routine sweep where none of the 8 checks depends on another's output."
    }
  ],
  "skills/specify/SKILL.md": [
    {
      "category": "efficiency",
      "line": 198,
      "summary": "Decomposition mode fetches the full issue list twice within one run \u2014 Step 1's Landscape read (--state open --json number,title,labels,body, line 198) and Step 3's Idempotency map (--state all --json number,body, line 390) \u2014 with no note that Step 3 could reuse or extend Step 1's already-fetched data.",
      "failure_scenario": "On a repo near the shared --limit 200 cap, every /specify decomposition run pays for two separate full gh issue list round-trips over substantially overlapping data, instead of one --state all fetch (with the union of needed fields) performed once at Step 1 and filtered in-memory for the open-only Landscape/Overlap-Analysis need."
    },
    {
      "category": "structural-contract",
      "line": 728,
      "summary": "specify's Relationship-to-Other-Skills table has no row for /claude-tweaks:triage, even though triage/SKILL.md names specify as 'the shaper' that gates entry to its worklist \u2014 a one-directional cross-reference.",
      "failure_scenario": "triage/SKILL.md line 226 states '/claude-tweaks:specify | The shaper \u2014 stamps ready + scoring before a record can enter this gate's worklist, and is where a flagged-back record returns for re-shaping.' but specify/SKILL.md's Relationship table (starting line 728) has no row for /claude-tweaks:triage \u2014 every 'triage' hit in the file is prose about label territory (e.g. line 190, 725), never a table entry. Someone editing specify's ready/scoring contract has no reciprocal pointer in specify's own file flagging that triage depends on exactly that stamping behavior to admit records into its gate."
    }
  ],
  "skills/build/SKILL.md": [
    {
      "category": "efficiency",
      "line": 95,
      "summary": "Spec Step 2.5's per-Manual-Steps-item CLI/credential probing (`which {tool}`, then `{tool} auth status`) carries no \"Parallel execution\" directive, unlike the structurally identical probe-then-classify procedure in `operational-checklist.md` (same skill directory, Common Step 5.5) which explicitly calls out parallel tool calls for its probes.",
      "failure_scenario": "A spec whose \"Manual Steps\" section lists several independent items (e.g., a Terraform apply, a Vercel env var, a Stripe webhook rotation) runs each item's `which`/`{tool} auth status` probe sequentially rather than as one batch of parallel Bash calls, adding avoidable wall-clock time to every build with a multi-item Manual Steps section even though the probes share no dependency \u2014 the same shape of independent-check work that `operational-checklist.md`'s own header note (\"all checks are independent Grep/Glob operations\") already parallelizes a few steps later in the same file."
    }
  ],
  "skills/design-wrapper/modes/pre-build.md": [
    {
      "category": "efficiency",
      "line": 27,
      "summary": "Steps 3-4 describe up to 8 independent file reads (up to 6 Impeccable reference docs plus PRODUCT.md/DESIGN.md or their fallback globs) with no `> **Parallel execution:**` directive, unlike every other multi-independent-file gather step in this same review scope.",
      "failure_scenario": "A grep for the directive across all 8 skill directories in scope (deepen, journeys, reflect, simplify, visual-review, visualize, assess-agent-autonomy, design-wrapper) turns up 8 uses in deepen/journeys/reflect/visual-review for structurally identical \"gather several independent files before analyzing\" steps, and zero anywhere in design-wrapper. On a spec whose body trips every \"Add X\" trigger (mentions animation, breakpoints, hover states, and copy together), Step 3 alone lazy-loads 6 reference files and Step 4 reads up to 2 more -- 8 independent Reads the executing agent has no stated basis to batch, costing up to 7 avoidable extra round-trips versus one batched Read call."
    }
  ],
  "skills/help/status-scan.md": [
    {
      "category": "efficiency",
      "line": 37,
      "summary": "Stage 1 issues two separate `gh issue list --state open` calls (line 37 and line 94) against overlapping data instead of one call whose --json field list covers both the count computation and the conflict-detection body extraction.",
      "failure_scenario": "Every /help run under work-backend: github-issues pays for a second full `gh issue list --state open --limit 200` round-trip (line 94, fetching `body` for the already-identifiable `ready` subset) purely because the first call (line 37) didn't also fetch `body`. Adding `body` to the first call's --json list and filtering client-side on the already-computed facets.stage === 'ready' would satisfy both consumers from one API call, cutting Stage 1's GitHub API cost roughly in half."
    },
    {
      "category": "factual-claim",
      "line": 30,
      "summary": "status-scan.md cites the Grant-queue-counts snippet as 'item-7' in `_shared/github-pr-scan.md`, but that snippet is numbered item 8 there.",
      "failure_scenario": "github-pr-scan.md's `repo-wide` scope numbers its steps 1 through 8, with 'Grant-queue counts' explicitly as item 8 (after item 7, 'Docs-health issues') \u2014 confirmed by that file's own Output Contract text ('item 8 above, `repo-wide` scope only') and by two correct 'item 8' references in `skills/tidy/SKILL.md` (lines 247 and 249) pointing at the same snippet. status-scan.md line 30 instead calls it 'item-7', so anyone following this pointer to cross-check or update the Grant-queue-counts logic lands on the unrelated Docs-health-issues query (item 7) instead of the actual snippet."
    },
    {
      "category": "simplification",
      "line": 44,
      "summary": "Stage 1's github-issues and local-files node scripts duplicate the same six-bucket filter chain (blocked/building/authorized/ready/parked/backlog) almost verbatim instead of computing it once against a common faceted-record shape.",
      "failure_scenario": "If a bucket definition changes (e.g. a new bot-state or grants combination is introduced), an editor updating the github-issues block's filters (lines 44-49) must remember to make the identical edit to the local-files block's copy (lines 67-72) 23 lines below; miss it, and the dashboard's ready/authorized counts diverge depending on which work-backend a project uses. tidy/scan-procedures.md's own Step 1 already demonstrates the cleaner split (fetch varies per driver, classification logic described once) that this file doesn't follow for its own Stage 1."
    }
  ],
  "skills/demo/SKILL.md": [
    {
      "category": "efficiency",
      "line": 63,
      "summary": "Verification Briefs are fetched one `gh issue view {n} --json comments` call per `demo:pending` record in an implicit sequential loop, with no parallel-execution directive, unlike this codebase's own Form A convention for independent read-only Bash operations.",
      "failure_scenario": "A sweep with many outstanding `demo:pending` records (the exact scenario this skill's own 'When to Use' describes \u2014 'several parallel threads... one place that shows everything built') issues N independent `gh issue view` calls strictly one after another instead of as parallel tool calls, adding wall-clock latency proportional to N for work that has no ordering dependency \u2014 the same class of inefficiency `dispatch/SKILL.md` explicitly calls out avoiding ('Two bulk calls, not per-issue re-fetches')."
    },
    {
      "category": "line-by-line",
      "line": 115,
      "summary": "Step 2's Suggested-verdict pre-fill rule requires checking whether a record's 'changed-file list' touches any merge-sensitive-paths glob, but no step in this skill ever fetches a changed-file list for a label-backed record.",
      "failure_scenario": "Step 1 Source A's only fetch is `gh issue list --json number,title,labels,url` plus the last issue comment containing '## Verification Brief' (a narrative digest of The ask/What shipped/Confirmed/See it yourself \u2014 not a structured file list). Step 2 then instructs pre-filling 'Approve' only when risk:low + effort:low AND 'its changed-file list doesn't touch any merge-sensitive-paths glob' \u2014 but there is no `gh pr diff`, `gh issue view --json` file field, or any other call anywhere in the skill that retrieves changed paths for a record. An agent following this literally has no defined data source to evaluate the merge-sensitive-paths condition and must either skip that half of the rule silently or fabricate a check, producing inconsistent Suggested-verdict pre-fills across runs."
    }
  ],
  "skills/tidy/SKILL.md": [
    {
      "category": "efficiency",
      "line": 58,
      "summary": "Step 5.5 is documented as having no data dependency on the parallel scan batch (\"git-log only... not a data dependency\") yet still runs sequentially after it purely for report-assembly ordering.",
      "failure_scenario": "Every full or `patterns`-scoped /tidy run pays Step 5.5's git-log scan time serially on top of the Steps 1-4.8 parallel batch's wall-clock time, even though the text admits Step 5.5 could be dispatched alongside them as an 8th parallel Task agent with its output simply slotted into the correct report section afterward \u2014 the same technique skills/help/status-scan.md already uses to run all 7 of its stages, including independent sub-stages 4.5/4.6/4.7, in one parallel batch with no sequential carve-out."
    },
    {
      "category": "line-by-line",
      "line": 182,
      "summary": "The Evidence tier is declared '(`--scope=github` routine firings only)' and gated on 'Step 4.8 ran', yet two of its four auto-apply finding shapes ('Parked record, `milestoneDueOn`...' and 'Parked record, a `watchedPaths` entry...') are Step 1 findings that a `--scope=github` firing never produces, since the Scope Selection table maps `github` to Step 4.8 only.",
      "failure_scenario": "A project instantiates the `tidy-github-triage` routine variant (`/claude-tweaks:routine create tidy --variant=github-triage`, which fires `/claude-tweaks:tidy --scope=github` on a schedule) and sets `tidy-routine-autonomy: evidence-based` in CLAUDE.md. Per the Scope Selection table (`| github | 4.8 |`), this firing runs only Step 4.8 (`repo-wide` PR/issue scan) \u2014 Step 1, the sole producer of parked-trigger findings (scan-procedures.md Shape 2), never executes. github-pr-scan.md's own `repo-wide` section also explicitly disclaims parked/backlog findings ('Backlog-record findings ... are `/tidy` Step 1's job now, not this scope's'). So the Evidence tier's two parked-record rows (and its own 'stale backlog record' example at line 186) describe an auto-apply path that can never actually fire in the exact context ('`--scope=github` routine firings only') the subsection restricts itself to \u2014 dead, unreachable documentation that misleads anyone maintaining or auditing the evidence-based auto-apply behavior into believing parked-record promotion happens through this tier."
    },
    {
      "category": "line-by-line",
      "line": 148,
      "summary": "Step 6's aggressiveness-routing table has matching `local-files`/`github-issues` rows for Absorb on a backlog record, but only a `github-issues` row for Delete on a backlog record \u2014 no row covers Delete of a stale backlog record under `work-backend: local-files`.",
      "failure_scenario": "On a project with `work-backend: local-files`, Step 1 Shape 1 recommends 'Delete \u2014 stale' for a 4+-week-old, untouched backlog record (scan-procedures.md's own stated recommendation for that age band). At Step 6, row 147 only lists non-record artifact types (temp files, symlinks, worktrees, plans), row 148 explicitly excludes 'backlog records' and points solely to 'the dedicated backlog-record Delete row below for `github-issues`-backend findings' (row 154), and no local-files-backend Delete row exists anywhere in the table \u2014 unlike Absorb, which has both a local-files row (149, Stage/Auto-apply/Auto-apply) and a github-issues row (155, Stage/Stage/Stage). An agent routing this finding under `moderate` or `aggressive` aggressiveness has no table entry to follow and must improvise, contradicting the doc's own claim that routing is deterministic ('Nothing is invented here for routines')."
    }
  ],
  "skills/_shared/decision-records.md": [
    {
      "category": "factual-claim",
      "line": 3,
      "summary": "Claims `/claude-tweaks:init` Phase 8.5 creates the `docs/decisions/` folder taxonomy, but Phase 8.5's actual documented procedure only ever creates `docs/REGISTRY.md` and backlogs a pointer to a missing doc rather than creating the doc or folder itself.",
      "failure_scenario": "Someone relying on this claim expects a fresh `/init` run to leave a real `docs/decisions/` directory on disk. It won't: `skills/init/docs-structure.md` step 7 creates only `docs/REGISTRY.md` (\"missing docs are not added until they're created\"), step 8 files a backlog work record instead of writing any file/folder, and `skills/init/bootstrap-steps.md` Step 2's directory-creation list (specs/, docs/, docs/superpowers/specs/, docs/superpowers/plans/, docs/plans/, docs/journeys/, .claude/skills/) never includes docs/decisions/ either. `skills/wrap-up/docs-health-integration.md` line 44 independently confirms \"/init Phase 8.5's missing-doc detection... only backlogs a pointer to the template.\" This repo's own docs/decisions/ folder was in fact first created by the commit that introduced the ADR contract and its first ADR file, not by any /init run."
    }
  ],
  "skills/init/SKILL.md": [
    {
      "category": "factual-claim",
      "line": 152,
      "summary": "The Scope Selection Gate, which fires immediately after Phase 0 (before Phase 2 reconnaissance has run), parenthetically claims the referenced 'Phases at a Glance' table auto-marks Phase 8 'Skip \u2014 no UI detected,' but that table (lines 52-64) contains no such annotation anywhere, and the condition (recon findings) doesn't exist yet at this point in the run.",
      "failure_scenario": "Someone executing this skill literally at the Scope Selection Gate looks at 'Phases at a Glance' (as directed) to see how Phase 8 is marked, finds a plain generic row with no 'Skip \u2014 no UI detected' text and no mechanism for it, and has no reconnaissance data available yet (Phase 2 hasn't run) to evaluate the stated condition \u2014 the instruction points to information that isn't there and can't yet be computed at the point it's given."
    },
    {
      "category": "line-by-line",
      "line": 162,
      "summary": "Option 2 (Interactive)'s per-phase gate template computes the next two phases via literal {N+1}/{N+2} integer arithmetic, but the real phase sequence has a non-integer step (\u20267, 8, 8.5, 9\u2026), so the substituted text is wrong at that boundary.",
      "failure_scenario": "Interactive mode reaches 'Phase 7 complete. Continue to Phase 8?' (N=7). If the user picks 'Skip Phase 8', the template's Option 2 description computes 'Move to Phase {N+2}' = Phase 9 \u2014 but the real next phase after 8 is 8.5 (Doc Registry), not 9, so the rendered text names the wrong phase and Phase 8.5 would be silently skipped along with Phase 8 instead of being offered its own gate. The same arithmetic also has no defined value once N itself becomes 8.5 (N+1 = 9.5, which doesn't exist)."
    },
    {
      "category": "simplification",
      "line": 415,
      "summary": "The Next Actions signal-resolution table's 'Everything is clean' row (line 414) and its 'Fallback (no row above matches)' row (line 415) recommend the identical action with identical description text, so the 5-row table only has 4 distinct outcomes.",
      "failure_scenario": "A future edit to the default recommendation (wording or target skill) has two rows to update in lockstep instead of one, with nothing in the table structure enforcing they stay identical \u2014 the exact kind of row drift the file's own caveat ('the signal rows are not exhaustive over every possible post-init state') is trying to guard against by having one well-defined catch-all outcome instead of two."
    }
  ],
  "skills/design-wrapper/frontend-detection.md": [
    {
      "category": "factual-claim",
      "line": 3,
      "summary": "The file's opening sentence claims `/specify` (\"Phase 1\") does not yet write the record's `Surface:` body-metadata line and that writing it is a future \"Phase 2\" \u2014 directly contradicted by this same file's own \"Layer 2\" section 65 lines later and by design-wrapper/SKILL.md, both of which state `/specify` already writes `Surface:` (and `Design-intent:`) on every new leaf record today.",
      "failure_scenario": "An agent or maintainer who reads only this file's intro while investigating Layer-2 detection behavior (e.g. debugging why a spec's `Surface:` value resolves a certain way) would conclude the field isn't populated yet and either distrust/ignore genuine Layer-2 results, wrongly assume every spec still falls through to the Layer-3 file-extension sniff, or file a duplicate \"implement Surface: writing\" work item for something `/specify` has already been doing on every leaf record since spec 20 shipped."
    }
  ],
  "skills/design-wrapper/impeccable-cli.md": [
    {
      "category": "factual-claim",
      "line": 75,
      "summary": "The field-reference table asserts the CLI's `detect --json` output has exactly \"9 specific rule ids\" with `advisory` severity, enumerating them by name \u2014 but the currently published Impeccable CLI (verified by downloading and inspecting the real npm packages for 3.2.0, the exact version this doc claims to be \"last verified against\", and the newer 3.2.1) has a 10th advisory rule, `design-system-font-size`, that is absent from this list. It does not exist in 3.2.0's registry (confirming the doc's count was correct on its stated verification date) but is present as `severity: 'advisory'` in 3.2.1, i.e. a patch release introduced the drift.",
      "failure_scenario": "Anyone using this doc to predict whether a `design-system-font-size` finding will fail the `/claude-tweaks:test` gate would consult this list, not find the rule, and (per the doc's own \"`warning` (default \u2014 most rules)\" framing and its Severity-to-result mapping table where \"Any `warning` -> fail\") wrongly conclude the finding is `warning`-severity and blocks the pipeline \u2014 when the live CLI actually tags it `advisory`, which does not fail the gate. It also shows the doc's own stated re-verification trigger (\"re-verify... after any future Impeccable CLI major version bump\") is insufficient, since this specific drift shipped in a patch bump (3.2.0 -> 3.2.1), not a major one."
    }
  ],
  "skills/assess-agent-autonomy/SKILL.md": [
    {
      "category": "factual-claim",
      "line": 39,
      "summary": "The Input section states `#{n}` is \"used to fetch the record body for `grant-check`/`ceremony-check`\" and \"used for reference/logging in `merge-check`/`failure-check`'s rendered output\" \u2014 but this grouping is backwards for two of the four modes: `ceremony-check`'s own Step 1 (lines ~265-268) explicitly says \"no fetch at all\" for its primary call path (reuses data already held by the caller), while `failure-check`'s own Step 1 (line 211) runs a genuine `gh api \".../issues/${N}/comments...\"` fetch keyed on `${N}`, not mere reference/logging.",
      "failure_scenario": "Someone modifying or debugging `failure-check` from the Input section's summary alone would assume the mode performs no external I/O of its own and skip handling its network/rate-limit exposure \u2014 despite failure-check's own Step 2 classification logic explicitly watching for \"`gh api` rate-limit (HTTP 429) responses\" from the exact call its own Step 1 just issued. Conversely, someone estimating `/specify` Step 3's per-record API cost from this line would wrongly expect `ceremony-check` to issue its own fetch, when its primary call path is documented as free (in-memory reuse, \"nothing to shell out for\")."
    }
  ],
  "skills/code-health/SKILL.md": [
    {
      "category": "factual-claim",
      "line": 402,
      "summary": "The Anti-Patterns table claims the confidence floor for noisy criteria is 'enforced by the skill judgment, not the engine,' but the engine (bin/code-health.js's validate-findings command) actually enforces it mechanically.",
      "failure_scenario": "An implementer reads this row and concludes the LLM must self-police confidence floors during the verify gate (Step 7), with the engine only checking shape. In reality, bin/code-health.js's cmdValidateFindings calls applyConfidenceFloor(v.value, crit.confidenceFloor) at line 234 (function defined at line 24, unit-tested in bin/lib/code-health/tests/cli-validate-findings.test.js) and drops any finding below its criterion's confidenceFloor \u2014 e.g. a security-logic finding with confidence:'medium' is silently dropped by validate-findings itself, not merely discouraged by skill judgment. The documented mental model of where the floor is enforced is factually backwards, which could mislead someone debugging why a finding they thought would file (relying on 'skill judgment' catching it) never appeared, or someone modifying the engine believing it 'validates shape, not policy.'"
    },
    {
      "category": "simplification",
      "line": 103,
      "summary": "The universal criteria list for an unknown area (`types: []`) is hardcoded as a 15-item prose enumeration, immediately followed by a live command that already computes the same list from `criteria.js`.",
      "failure_scenario": "If `bin/lib/code-health/criteria.js`'s `CRITERIA` array gains, removes, or renames a universal criterion, this hardcoded prose list silently goes stale (nothing forces it to be regenerated), and a session following the doc literally would judge unknown-type areas against the wrong criterion set \u2014 exactly the cardinality/list-restatement drift risk this project's own CLAUDE.md Don'ts section calls out repeatedly for other prose-restated lists."
    }
  ],
  "bin/claude-tweaks-statusline.js": [
    {
      "category": "language-pitfall",
      "line": 14,
      "summary": "readStdin()'s 50ms timeout resolves the read without ever detaching stdin's data/end/error listeners or pausing/destroying the stream, so if the harness doesn't close the write end of piped stdin promptly, the statusline process writes its output correctly but then never exits.",
      "failure_scenario": "Empirically confirmed: spawning `node bin/claude-tweaks-statusline.js` with JSON written to its stdin but the write end never closed (`child.stdin.write(...)` with no `.end()`) causes the child to correctly print its rendered segments after ~50ms, then hang \u2014 still alive and unexited 3+ seconds later (had to be killed). Because a readable stream in flowing mode with attached listeners keeps Node's event loop referenced until 'end'/EOF, any statusline host whose stdin-piping mechanism doesn't promptly close the pipe (a kept-open FD, a PTY, a reused stream) leaves an orphaned node process behind on every render."
    },
    {
      "category": "simplification",
      "line": 124,
      "summary": "findActiveSpec's candidates array + for loop is vestigial now that commit 2c8660f (this session) removed the only other entry (specs/INBOX), leaving a loop/try-catch wrapper around what is now a single directory lookup.",
      "failure_scenario": "Every statusline render allocates and iterates a one-element array with a nested try/catch for a check that is now unconditionally just \"read <cwd>/specs\" \u2014 no remaining input can make the loop run more than once, so a future reader has to trace the loop/candidates plumbing before realizing it's dead weight left over from the same-day INBOX-candidate removal (confirmed via `git show 2c8660f -- bin/claude-tweaks-statusline.js`)."
    }
  ],
  "agents/qa-agent.md": [
    {
      "category": "language-pitfall",
      "line": 113,
      "summary": "Story-supplied locator/value strings (name, text, value) are interpolated directly inside double-quoted agent-browser shell invocations with no escaping guidance.",
      "failure_scenario": "A story step has `locator: { role: button, name: 'Submit \"now\"' }` or `action: fill` with `value: 'O'Brien'` / a value containing a backtick or `$(...)`. Commands like `find role <role> --name \"<name>\"` (lines 97-101) and `fill <ref> \"<value>\"` (line 113) are executed via the Bash tool with the literal string spliced into the double-quoted argument \u2014 an embedded `\"` prematurely closes the argument and corrupts the command (misparsed flags or a stray shell token), and embedded `` ` ``/`$()`/`;` is shell-interpreted rather than passed through as literal text, since nothing in the workflow instructs the agent to escape or otherwise safely pass these values."
    },
    {
      "category": "line-by-line",
      "line": 225,
      "summary": "The Legacy Format workflow closes the agent-browser session twice on a failing step \u2014 once inside the FAIL branch and again in the unconditional step that follows it.",
      "failure_scenario": "A legacy-format story fails at step N: Step 3.f runs 'capture a trace, then close, then stop', already issuing `agent-browser --session <id> close`. Step 4 then unconditionally runs `agent-browser --session <session> close` again with no 'if not already closed' guard, so the agent issues a second close against an already-closed session \u2014 at best a wasted/erroring CLI call the agent must silently absorb, at worst a confusing error line leaking into the returned report. (Structured Format's Section 6 avoids this by making close the terminal action of the FAIL path only once; Legacy Format's flat step list does not.)"
    },
    {
      "category": "line-by-line",
      "line": 181,
      "summary": "The Fill-steps instruction specifies a locator-field priority order (testid > label > placeholder > role+name) that presumes multiple locator strategies can be present on one step, but Section 4 Step 1's locator mapping (and the story schema seen in skills/stories/story-examples.md) only ever populates a single-key locator object, leaving the priority rule with no defined algorithm to apply it against.",
      "failure_scenario": "If a story's `locator:` object ever does carry more than one key (e.g. a migrated or hand-authored story with both `testid` and `label` set), the agent has no described procedure for choosing between them beyond the bare priority list \u2014 Section 4 Step 1's five bullets each map exactly one field to one `find` subcommand and never address a multi-field object \u2014 so the agent must improvise, risking a different resolution choice across runs/agents for the same step."
    },
    {
      "category": "removed-behavior",
      "line": 185,
      "summary": "Teardown (Section 5) and Failure Handling (Section 6) give contradictory instructions for what happens after a step fails: Section 4 says failure triggers Section 6's trace-then-close immediately, but Section 5 says Teardown 'runs regardless of pass/fail status' after the step loop.",
      "failure_scenario": "A story with a Teardown block fails at step 2. Following Section 4 step 8 literally ('On FAIL: capture a trace BEFORE closing the session (see Section 6)... stop execution') means Section 6 fires immediately, closing the session \u2014 leaving Section 5's 'Teardown runs regardless of pass/fail status' with no open session to act on, silently dropping teardown on every failure. Conversely, if Teardown is run first (its literal document position, between Sections 4 and 6), the failure trace captured in Section 6 reflects post-teardown page state rather than the moment of failure, defeating the trace's diagnostic purpose. The doc does not resolve which reading is correct."
    },
    {
      "category": "removed-behavior",
      "line": 193,
      "summary": "The failure trace is written to `{TRACES_BASE}/<story-id>/<timestamp>.zip` with no step anywhere creating that per-story subdirectory first, unlike the screenshot directory which Setup explicitly `mkdir -p`s.",
      "failure_scenario": "On a story's first-ever failure, `agent-browser --session <story-id> trace save {TRACES_BASE}/<story-id>/<ISO-timestamp>.zip` is invoked against a `<story-id>` subdirectory under TRACES_BASE that was never created (Setup Step 2a only does `mkdir -p {SCREENSHOT_PATH}`). If the underlying CLI does not auto-create parent directories for its output path, the trace-save call fails exactly on the failure it exists to diagnose, and the report's `TRACE:` line/`trace` field point at a file that was never written."
    },
    {
      "category": "simplification",
      "line": 255,
      "summary": "The REPORT_JSON envelope \u2014 including the full nested `page_inventories` shape (interactive_elements/forms/navigation/accessibility/layout) \u2014 is spelled out in full here and, byte-for-byte on the nested part, again in skills/test/qa-prompts.md's dispatch prompt template (line 114), with neither file referencing the other as the source of truth for the schema.",
      "failure_scenario": "A future change to the page_inventories shape (e.g. adding a new accessibility field) made only in qa-agent.md's report template doesn't propagate to the inline dispatch prompt in qa-prompts.md (or vice versa, since qa-prompts.md is what's actually sent to a generic Task dispatch and independently re-specifies the schema for the agent to follow), so the two paths can silently diverge and the Phase 4 REPORT_JSON parser in qa-reporting.md ends up handling two slightly different shapes depending on which template the executing agent actually followed."
    }
  ],
  "bin/docs-health.js": [
    {
      "category": "line-by-line",
      "line": 254,
      "summary": "cmdFindRefs checks `fs.existsSync(targetPath)` using the raw, unresolved targetPath instead of resolving it against `root` the way the very next line's `deriveDocId(targetPath, root)` correctly does; cmdCheckFreshness (line 272, `fs.readFileSync(targetPath, 'utf8')`) has the identical defect.",
      "failure_scenario": "Empirically confirmed: `cd <dir-without-docs/foo.md> && node bin/docs-health.js find-refs docs/foo.md --root <other-dir-that-has-docs/foo.md>` fails with `find-refs: could not read file: docs/foo.md` and exits 1, even though the file genuinely exists under --root. The same invocation with an absolute path, or with cwd manually set to --root, succeeds. This breaks the `[--root <dir>]` contract both subcommands advertise in their own usage strings for exactly the \"audit a project elsewhere\" scenario deriveDocId's own comment says --root exists to support; check-freshness reproduces identically."
    }
  ],
  "bin/lib/issues/unattended-tier.js": [
    {
      "category": "line-by-line",
      "line": 23,
      "summary": "The scope-expansion pattern /breaks? (more than )?\\d+ unrelated tests/i matches any digit count, not just the '>10 unrelated tests' threshold that ledger/resolve-gate.md's Phase 1 (the spec this module's own docstring says it encodes 1:1) actually requires before a fix may legitimately be skipped.",
      "failure_scenario": "With unattended-tier: on, a Phase-1 blocker reason such as 'Would expand scope -- breaks 2 unrelated tests' makes clearsFloor return true, so ledger/resolve-gate.md's Phase 2 auto-routes the item straight to a backlog record with no AskUserQuestion drill and no human ever seeing the reason \u2014 even though 2 broken tests does not meet Phase 1's own >10 bar for legitimately deferring, defeating the 'only the four legitimate categories, never ambiguous ones' floor guarantee the module documents."
    }
  ],
  "skills/test/SKILL.md": [
    {
      "category": "line-by-line",
      "line": 166,
      "summary": "Step 1.5's \"Reporting\" subsection enumerates only two branches for surfacing Design CLI findings (\"Standard mode / All mode\" and \"Pipeline mode,\" naming the two `report-templates.md` pipeline templates) and never covers `skip-qa` mode's own report format, even though the Anti-Patterns table requires the Design CLI Gate to still run under `skip-qa`.",
      "failure_scenario": "`/claude-tweaks:test skip-qa` runs with `VERIFICATION_PASSED=true` (the exact scenario `/flow`'s polish-phase re-verify gate uses) and the Design CLI Gate returns a `severity: warning` finding. Step 1's own skip-qa report text (\"Verification: passed (types + lint + tests). QA: skipped (skip-qa). Set TEST_PASSED=true.\") is neither the Standard/All-mode table format nor either of the two named `report-templates.md` Pipeline-mode templates, leaving no specified location for the required \"Design CLI\" row/line."
    }
  ],
  "bin/lib/docs-health/findability.js": [
    {
      "category": "line-by-line",
      "line": 45,
      "summary": "computeInboundReferences matches an inbound reference by a bare-filename substring check (content.includes(basename)) instead of a path-qualified match, so two docs sharing a filename in different directories produce false-positive reference counts.",
      "failure_scenario": "docs/api/config.md and docs/db/config.md both exist. A page (e.g. README.md) links only to docs/db/config.md by writing 'see db/config.md'. Because the check only tests whether the file's body contains the bare basename 'config.md' (a substring test, no directory qualification), docs/api/config.md \u2014 which is never actually linked from anywhere \u2014 is counted as referenced too, since 'db/config.md'.includes('config.md') is true. A genuinely orphaned, unreachable doc that happens to share its filename with a linked doc elsewhere in the tree is reported as having inbound references, so docs-health's findability check never flags it."
    },
    {
      "category": "reuse",
      "line": 9,
      "summary": "findability.js's walkMarkdownFiles hand-rolls the same recursive docs/**.md directory walk that scope.js's listDocs already performs in the same skill, as a separately-configured private copy instead of building on the existing listDocs helper.",
      "failure_scenario": "The two walkers already diverge in scope with no comment explaining whether it's deliberate: listDocs skips dotfile directories and excludes docs/superpowers/** and docs/journeys/** from its candidate set, while walkMarkdownFiles skips neither. A future change to one walker's skip rules (e.g. adding a new excluded top-level dir to listDocs) has no mechanism to reach findability's independent copy, so the two can silently keep drifting apart."
    }
  ],
  "bin/lib/coordination.js": [
    {
      "category": "line-by-line",
      "line": 81,
      "summary": "normalizeFinding's early return when a combined path:line string fails to parse leaves findingsMatch's path/severity comparison to operate on the original (still path-less, line-less) finding, reopening the exact spurious-match hole the function's own header comment claims it closes.",
      "failure_scenario": "Two entirely different dispatched-agent findings that both lack any usable path field (e.g. a naive JSON transcription where 'path' and 'Path:Line' are both absent, or the value has no parseable trailing line number) each fail parsePathLine, so normalizeFinding hits `if (parsed.line === undefined) return finding;` and returns the finding untouched \u2014 path stays undefined, line stays undefined. findingsMatch then evaluates `na.path !== nb.path` as `undefined !== undefined` (false, doesn't short-circuit) and `Math.abs(na.line - nb.line) > tolerance` as `NaN > tolerance` (false, doesn't short-circuit), falling through to a severity-bucket comparison that also resolves equal for two undefined severities \u2014 so categoriseReproduction/detectCrossLensOverlap silently treat two unrelated, unlocated findings as the same finding, exactly the failure mode the file's lines 48-60 comment describes normalizeFinding as existing to prevent."
    }
  ],
  "bin/lib/hooks/post-tool-use.js": [
    {
      "category": "line-by-line",
      "line": 94,
      "summary": "checkClosingKeyword's fixed 20-character lookback window can slice off part of a longer word immediately before a bare issue ref, and JS regex's \\b treats the resulting slice's start as a boundary even though none exists in the real message \u2014 so a word that merely ends in a closing-keyword suffix can be misread as a genuine closing keyword and suppress the warning.",
      "failure_scenario": "A commit message contains '...unresolved            #123' (a word ending in one of the recognized keyword suffixes, glued directly to a prefix, followed by exactly 12 whitespace characters before the ref \u2014 e.g. from a copy-pasted, alignment-padded table in the commit body). idx-20 lands exactly on the 'r' of 'resolved' inside 'unresolved', so `before = message.slice(idx-20, idx+ref.length)` equals 'resolved            #123' with no trace of the disqualifying 'un' prefix. CLOSING_KEYWORD_RE's `\\b` matches at the start of this truncated slice (a word-boundary in the sliced string even though 'un' immediately precedes 'resolved' in the real message), so the ref is wrongly marked closed and checkClosingKeyword's warning never fires for a commit that in fact has no real GitHub closing keyword."
    },
    {
      "category": "reuse",
      "line": 60,
      "summary": "CLOSING_KEYWORD_RE re-encodes the same GitHub closing-keyword vocabulary (fix/close/resolve + inflections) that bin/lib/issue-branch-tracking.js already exports as ISSUE_REF_SOURCE, as an independently hand-written regex.",
      "failure_scenario": "Both patterns currently agree on the same 9 recognized forms but share no code \u2014 issue-branch-tracking.js's exported ISSUE_REF_SOURCE is never imported here. If GitHub's recognized keyword list changes, or a maintainer updates the generated-workflow pattern in issue-branch-tracking.js without knowing this second copy exists, post-tool-use.js's 'did this commit close its issue' nudge silently starts disagreeing with what the generated GitHub Actions workflow (and GitHub's own native parsing) actually recognizes."
    }
  ],
  "tests/hooks-session-start.test.js": [
    {
      "category": "line-by-line",
      "line": 48,
      "summary": "The 'close-run hint substitutes CLAUDE_PLUGIN_ROOT' test unconditionally deletes process.env.CLAUDE_PLUGIN_ROOT (line 48) without capturing/restoring any pre-existing value first, the same unsafe shape as tests/hooks-pre-tool-use.test.js's separate, already-known instance of this bug \u2014 this is an independent second occurrence in a different file, not the same defect location.",
      "failure_scenario": "If the test process is ever run with a real CLAUDE_PLUGIN_ROOT already set in the environment (e.g. invoked from inside an actual plugin session, or by a test harness that exports it globally), this test permanently destroys that value for the remainder of the process instead of restoring it in a finally block the way the file's own later assertion (line 57) at least attempts for the value it itself sets \u2014 any later code in the same process that reads CLAUDE_PLUGIN_ROOT sees it missing even though the environment originally provided it."
    },
    {
      "category": "test-coverage-gap",
      "line": 30,
      "summary": "The test titled \"capped at 3, newest first\" only ever creates 2 non-clean run directories, so the MAX_REPORTED=3 truncation logic named in its own title is never exercised.",
      "failure_scenario": "session-start.js's run() does `ctxLib.listRunDirsWithState(ctx.cwd).slice(0, MAX_REPORTED)` with MAX_REPORTED=3. The test creates one 'interrupted' run (spec-1), one 'active' run (spec-2), and one 'clean' run (spec-0) \u2014 but spec-0 is excluded by listRunDirsWithState's own clean-status filter before the slice ever runs, not by the cap. With only 2 candidates ever reaching `.slice(0, 3)`, the slice is a no-op in this fixture, so the test cannot distinguish 'the cap correctly keeps 3' from 'there is no cap at all.' A future change that dropped the `.slice()` call, set MAX_REPORTED to a wrong value, or broke newest-first ordering once more than 3 runs exist would pass this test with zero warning."
    }
  ],
  "skills/ledger/SKILL.md": [
    {
      "category": "line-by-line",
      "line": 251,
      "summary": "The Relationship-to-Other-Skills row for /claude-tweaks:wrap-up states 'deletes the ledger (Step 5),' directly contradicting this same file's own 'Delete' section three paragraphs earlier (line 185: 'Called by /claude-tweaks:wrap-up Step 10 (planned in Step 5)') and wrap-up/SKILL.md's own text, which explicitly separates planning cleanup at Step 5 from executing it at Step 10.",
      "failure_scenario": "Someone auditing when the ledger file actually disappears from disk during a `/claude-tweaks:wrap-up` run reads only the compact Relationship-table row and concludes deletion happens at Step 5 \u2014 but the ledger file in fact persists on disk through Step 5 (which only enumerates and filters the cleanup list) and is only actually removed at Step 10, five steps later; any tooling, doc, or reviewer relying on the Step-5 claim (e.g. to check 'has the ledger been cleaned up yet' right after Step 5) would be wrong."
    }
  ],
  "skills/flow/SKILL.md": [
    {
      "category": "line-by-line",
      "line": 28,
      "summary": "The 'When NOT to Use' bullet states unconditionally that a plan with tangled cross-task dependencies means 'flow's shape gate (Step 2.6) will hard-fail anyway,' but the skill's own Arguments table (line 52) states that `auto` mode \u2014 flow's default \u2014 'Silences merge-check (Step 2.5), shape-check (Step 2.6)...', and `flow/validation.md`'s Step 2.6 procedure confirms that under `auto` a hard-fail signal is auto-resolved to 'proceed anyway and accept tangled-task risk' (option 2) rather than actually stopping the pipeline.",
      "failure_scenario": "A user invokes `/claude-tweaks:flow #N` in the default `auto` mode against a work record whose plan has tangled cross-task dependencies, expecting \u2014 per the 'When NOT to Use' guidance \u2014 that the pipeline will stop and force a `/claude-tweaks:specify` pass first. Instead, per the actual default-auto mechanics, Step 2.6's hard-fail signal is silently downgraded to 'proceed anyway,' so the pipeline runs the whole way through against a genuinely coupled, tangled plan with only a buried `ops` ledger note \u2014 the opposite of what the advice implied would happen."
    }
  ],
  "bin/hooks.js": [
    {
      "category": "removed-behavior",
      "line": 45,
      "summary": "record-worktree's if/else-if chain has no branch for \"a run dir was resolved but no worktree argument was given,\" and close-run's analogous chain (line 78) has no branch for \"no run dir could be resolved and --run wasn't invalid\" \u2014 both cases print nothing and exit 0, indistinguishable from success.",
      "failure_scenario": "Empirically confirmed: `node bin/hooks.js record-worktree --run <valid-existing-dir>` (worktree path omitted) produces zero stdout output and exit 0, recording nothing. `node bin/hooks.js close-run` run where no `.claude-tweaks/pipelines/` run can be resolved also produces zero output and exit 0. A skill or human that malforms either invocation gets no diagnostic that the worktree assignment was never recorded, silently leaving E1's working-directory-discipline enforcement disabled for that run, since pre-tool-use.js unconditionally allows whenever `!ctx.runState.worktree`."
    },
    {
      "category": "simplification",
      "line": 25,
      "summary": "record-worktree (lines 25-43) and close-run (lines 65-77) both re-implement the same \"resolve --run <path>, validate it's a real directory via a try/catch IIFE, else fall back to ctxLib.resolveRunDir\" block.",
      "failure_scenario": "A future change to what counts as a valid --run path (e.g. also rejecting a directory that exists but isn't a real run dir, or resolving symlinks first) has to be hand-applied to both copies; editing only the one under active work (record-worktree, which carries the explanatory comment) leaves close-run silently validating --run differently, with no shared code path to keep them in sync."
    }
  ],
  "bin/lib/hooks/pre-tool-use.js": [
    {
      "category": "removed-behavior",
      "line": 66,
      "summary": "checkWorktreeRequired's Bash-command target detection only recognizes 'git commit' (gitTargets(...).find(t => t.action === 'commit')), never 'git push', so the worktree.always policy gate silently allows a bare push from a non-isolated checkout even though the very deny message it emits (lines 100-105) and CLAUDE.md's Hooks section both state the policy covers 'git commit/push'.",
      "failure_scenario": "With worktree.always: true set and the session cwd in the main (non-worktree) checkout, running Bash command `git push origin main` with no accompanying `git commit` in the same invocation never sets targetPath \u2014 the commit filter finds nothing and fileWriteTargets (tee/cp/mv only) also finds nothing for a push \u2014 so checkWorktreeRequired returns {} and the push proceeds unblocked, contradicting the policy's own stated guarantee."
    }
  ],
  "bin/lib/routine-template-parser.js": [
    {
      "category": "removed-behavior",
      "line": 65,
      "summary": "In parseRoutineTemplate's nested-map branch, `sawNested` is set to true for any non-blank indented line regardless of whether it actually matches the `key: value` shape the loop parses, so a YAML block list (`- item`) nested under a top-level key silently resolves to an empty object {} instead of an array or a visible parse failure.",
      "failure_scenario": "A future routine-template.yml expressing a list in block style (e.g. `targets:\\n  - foo\\n  - bar`, a natural YAML shape) instead of the currently-used inline `[foo, bar]` form would have that key's value silently become {} \u2014 every list entry vanishes with no error, and any code reading `tpl.targets` as an array gets an empty object with none of the expected entries instead of a loud failure. Currently latent: every routine-template.yml in the repo today only uses inline arrays, so this path isn't exercised yet."
    }
  ],
  "bin/lib/code-health/tests/skill-md.test.js": [
    {
      "category": "removed-behavior",
      "line": 38,
      "summary": "The house-section-ordering test's idx() helper uses body.indexOf(), which resolves '## Next Actions' to the first of three occurrences of that substring \u2014 a mention inside the boilerplate '> **Interaction style:**' directive near the top of the file \u2014 not the real '## Next Actions' section heading, so the ordering assertions never actually check the real heading's position.",
      "failure_scenario": "SKILL.md contains the string '## Next Actions' three times: once inside the standard Interaction-style directive prose near the top (index ~958), once as the real section heading further down (index ~28188), and once inside the Component-Skill Contract paragraph's own prose (index ~29339) \u2014 confirmed by direct inspection of skills/code-health/SKILL.md. `idx('## Next Actions')` always returns the first (~958), so `idx('## Next Actions') < idx('## Component-Skill Contract')` is trivially true regardless of where the real heading sits. If a future edit moved the actual `## Next Actions` heading to after `## Relationship to Other Skills` \u2014 violating the documented placement convention in CLAUDE.md \u2014 this test would keep passing because it's comparing the wrong occurrence. The identical bug is independently present in bin/lib/docs-health/tests/skill-md.test.js line 41 (verified: '## Next Actions' also occurs 3 times in skills/docs-health/SKILL.md, first at index ~1313 inside the same boilerplate directive)."
    }
  ],
  "bin/lib/issues/tests/claims.test.js": [
    {
      "category": "removed-behavior",
      "line": 70,
      "summary": "The '[1,2]' and '\"just a string\"' entries in parseClaimMarker's 'never throws and returns null on garbage' test never reach claims.js's Array.isArray(fields)/typeof-object rejection guard, since CLAIM_RE requires a literal '{' immediately after the marker prefix and rejects both inputs by regex mismatch alone.",
      "failure_scenario": "claims.js's `if (fields === null || typeof fields !== 'object' || Array.isArray(fields)) return null;` guard inside parseClaimMarker has no test that actually executes it: every '{...}'-shaped fixture in this suite parses to a well-formed plain object (hitting the happy path), and every non-'{...}'-shaped fixture \u2014 including these two garbage entries, which look purpose-built to test array/primitive JSON payloads \u2014 is rejected earlier by CLAIM_RE/RELEASE_RE's mandatory leading brace. A future change that loosens the marker regex (e.g. to tolerate a leading '[' or bare value) would ship with this specific guard's correctness completely unverified, and the full suite would stay green either way."
    }
  ],
  "bin/lib/issues/metrics.js": [
    {
      "category": "reuse",
      "line": 14,
      "summary": "labelName re-implements the exact GitHub label-normalization logic that record.js's normalizeLabelNames and grouping.js's labelNames already provide, as this file's own comment admits.",
      "failure_scenario": "The string-vs-{name} label shape is normalized identically in three places today (record.js:127, grouping.js:52, metrics.js:15); if a future fix to label normalization (e.g. trimming, or handling a new GraphQL label shape) lands in record.js's exported normalizeLabelNames but not in metrics.js's parallel labelName, computeWontfixRate's by:* origin bucketing and firstLabelTime's timeline-event matching silently normalize labels differently from the rest of the pipeline, with no shared test or import link to catch the divergence."
    }
  ],
  "bin/lib/docs-health/issue-payload.js": [
    {
      "category": "reuse",
      "line": 33,
      "summary": "fenceFor/fencedBlock (a GitHub-fence-safe code-block wrapper) and the CLASSIFICATION_SCORING risk/effort map are copy-pasted verbatim into both docs-health/issue-payload.js and harness-health/issue-payload.js instead of living once in bin/lib/issues/record.js, which both files already import for recordPayload/specShapedBody.",
      "failure_scenario": "A fix to the fence-length edge case, or a change to the additive/restructural risk/effort mapping, has to be applied by hand in two files that already depend on record.js for the adjacent body-template concern; journey-health/issue-payload.js needing either helper next (it currently reimplements neither, formatting its body without fenced blocks) is one copy-paste away from becoming a third divergent copy."
    }
  ],
  "tests/hooks-dispatcher.test.js": [
    {
      "category": "reuse",
      "line": 54,
      "summary": "The file hand-rolls `JSON.parse(fs.readFileSync(path.join(run, 'run-state.json'), 'utf8'))` ten separate times (lines 54, 58, 84, 89, 92, 102, 147, 155, 158, 166) instead of importing `readRunState(runDir)`, which `bin/lib/hooks/context.js` already exports for exactly this read.",
      "failure_scenario": "Neither this file nor tests/hooks-lifecycle.test.js (which separately reimplements the same read as a local `readState` arrow function at line 18) imports bin/lib/hooks/context.js at all. If run-state.json's read/parse ever needs a behavior change that context.js's readRunState already handles (e.g. its try/catch-to-null on a missing or corrupt file), that fix has no reason to touch these 11 independent inline copies, so they silently keep stale behavior \u2014 and any future contributor grepping for 'who reads run-state.json' has to check both the shared helper and every test file separately."
    }
  ],
  "bin/lib/code-health/tests/status-v2.test.js": [
    {
      "category": "reuse",
      "line": 31,
      "summary": "seedDurableCursors, seedDurableRuns, and seedDurableRemembered are the same ~15-line git-fixture-seeding helper copy-pasted three times across code-health/tests, differing only in the JSON filename written.",
      "failure_scenario": "cli-nextslice.test.js:24, churn-v2.test.js:23, and status-v2.test.js:31 each independently init a bare repo, init+commit a seed checkout onto a health-state branch, push it, then wire the test root's origin remote at it \u2014 identical step-by-step except for 'cursors.json' vs 'runs.json' vs 'remembered.json'. The same anti-pattern recurs in docs-health/tests as makeTmpGitRoot (cli-check-freshness.test.js:11, freshness.test.js:10) and initGitRepo (scope.test.js:14) \u2014 three more near-identical 'tmp git repo with committer identity' copies. If the seeding technique needs to change (e.g. the health-state branch layout changes), a maintainer must remember to patch all of these copies across two skill directories instead of one shared helper (naturally homed in bin/lib/health-core/tests/, the layer already shared by all four health skills)."
    }
  ],
  "bin/lib/harness-health/tests/dedup.test.js": [
    {
      "category": "reuse",
      "line": 3,
      "summary": "dedup.test.js in both harness-health and journey-health fully re-tests decide(), which their local dedup.js files define as nothing but `module.exports = require('../health-core/dedup')` \u2014 already covered (with a superset of cases) by bin/lib/health-core/tests/dedup.test.js.",
      "failure_scenario": "Reading bin/lib/harness-health/dedup.js and bin/lib/journey-health/dedup.js confirms both are one-line pass-through wrappers; bin/lib/health-core/dedup.js's own header says it is 'Shared by harness-health, journey-health, and docs-health (byte-identical wrapper across the three today)', and bin/lib/health-core/tests/dedup.test.js already exercises every branch, including a 'regressed cache-only fallback' case that neither per-subsystem copy tests. Any change to decide()'s contract now has to be updated in 3 near-identical test files for zero coverage gain \u2014 and the very same directories already state and follow the opposite rule elsewhere: harness-health/tests/cache.test.js's own comment says computeChurn 'is a pure function shared with journey-health and is fully covered by bin/lib/health-core/tests/runs.test.js \u2014 no need to duplicate it here,' a principle that was never applied to dedup.js."
    }
  ],
  "skills/stories/coverage-report.md": [
    {
      "category": "reuse",
      "line": 5,
      "summary": "`/claude-tweaks:stories` Step 6 reimplements journey\u2194story coverage and orphan-detection logic instead of using the canonical `_shared/journey-coverage-check.md` computation that `/claude-tweaks:review` (lens 3g-cov) and `/claude-tweaks:journey-health` both explicitly reuse.",
      "failure_scenario": "By Step 6, stories are already written to disk (same precondition the shared fragment expects \u2014 finalized `docs/journeys/*.md` + `stories/*.yaml`), so there's no timing reason for the duplication. If the canonical algorithm in `_shared/journey-coverage-check.md` is later refined (e.g., how an 'orphaned story with URL match' is identified, per its own Computation section), `/review` and `/journey-health` pick the change up automatically while `/stories`' independently-written version here and in `skills/stories/journey-ingest.md:54-61` silently keeps the old behavior \u2014 producing inconsistent coverage numbers across skills for the same journeys and stories."
    }
  ],
  "skills/review-backlog/SKILL.md": [
    {
      "category": "reuse",
      "line": 82,
      "summary": "The git-log-based `createdAt` derivation script (execSync per record, fallback to `new Date().toISOString()`) is duplicated verbatim in two places in the same file (unsynced-record fold-in and local-files fetch) instead of living once in `bin/lib/issues/review-backlog.js`, which already houses this skill's other pure filter/sort/split/merge helpers.",
      "failure_scenario": "A future fix to the date-derivation logic (e.g., handling a git-log format change, or a different fallback policy) made in one of the two copies (line 82-93 or line 109-120) can silently miss the other, leaving the `unsynced`-record path and the `local-files`-driver path computing `createdAt` inconsistently even though both are meant to use 'the same approach' per the file's own prose."
    }
  ],
  "skills/_shared/health-filing-mechanics.md": [
    {
      "category": "reuse",
      "line": 15,
      "summary": "The retry-queue-drain section instructs consumers to file `gh issue create` 'exactly as the Type-expression-branch below describes,' but this shared file has no Type-expression-branch section anywhere in it.",
      "failure_scenario": "A session reading this canonical shared file top-to-bottom to understand or verify the retry-drain mechanic follows the 'below' pointer and finds nothing \u2014 the file ends at line 41 with 'Keeping the four copies in sync' \u2014 so the actual Type-expression-branch content it needs is only ever available in whichever consuming skill's own SKILL.md happens to be open, defeating this file's stated purpose as 'the one place... a correctness fix made to one skill's copy can be checked against.'"
    }
  ],
  "skills/_shared/dev-url-detection.md": [
    {
      "category": "reuse",
      "line": 72,
      "summary": "Step 2.7's `git rev-parse --git-dir` vs `--git-common-dir` check reimplements in raw bash the same linked-worktree heuristic `bin/lib/hooks/worktree-detect.js`'s `repoInfo()` already provides, without that helper's submodule guard or symlink-safe path resolution.",
      "failure_scenario": "This check runs from inside a git submodule checkout: worktree-detect.js's repoInfo() explicitly treats a submodule as \"not an isolated worktree\" (checking --show-superproject-working-tree), but dev-url-detection.md's inline one-liner has no such guard and can misclassify it. The module's own header comment states its purpose is \"so the hook and the skill never disagree about what counts as isolated\" \u2014 any future correction to that shared heuristic won't reach this independently hand-rolled copy, which could be called instead via the same `node -e \"require(process.env.CLAUDE_PLUGIN_ROOT + '/bin/lib/hooks/worktree-detect.js').repoInfo(...)\"` pattern already used elsewhere in this file corpus (label-bootstrap.md, issue-claims.md, github-pr-scan.md)."
    }
  ],
  "tests/hooks-pre-tool-use.test.js": [
    {
      "category": "setup-teardown-asymmetry",
      "line": 207,
      "summary": "The CLAUDE_PLUGIN_ROOT test unconditionally deletes the real environment variable without capturing or restoring any pre-existing value, unlike the careful save/restore pattern this same suite uses for NO_COLOR elsewhere.",
      "failure_scenario": "Line 207 runs `delete process.env.CLAUDE_PLUGIN_ROOT;` with no prior `const orig = process.env.CLAUDE_PLUGIN_ROOT` capture, and the `finally` block at line 218 also just deletes rather than restoring `orig`. bin/hooks.js itself reads `process.env.CLAUDE_PLUGIN_ROOT` as a first-class runtime signal (see pluginRoot() in bin/lib/hooks/pre-tool-use.js), so this variable genuinely can be set in real Claude Code plugin-context processes. If this test suite is ever run in an environment where it legitimately holds a value, that value is irrecoverably cleared for the rest of the test process, and any later test (now or added in future) that depends on the ambient value would silently see it missing. The identical unconditional-delete pattern is duplicated at tests/hooks-session-start.test.js lines 48 and 57 in its own 'close-run hint substitutes CLAUDE_PLUGIN_ROOT' test \u2014 contrast with tests/statusline.test.js, which captures `orig` and conditionally restores NO_COLOR in four separate tests."
    },
    {
      "category": "simplification",
      "line": 57,
      "summary": "Line 57 duplicates the exact regex-escaping expression already extracted as the `esc()` helper at line 41, instead of calling `esc(wt)` like every later test in the file does.",
      "failure_scenario": "`esc` is used correctly at lines 71, 163, and 183, but line 57 \u2014 in an earlier test \u2014 still has the raw `wt.replace(/[.*+?^${}()|[\\]\\\\]/g, '\\\\$&')` inlined. A future fix to the escape set (e.g. adding a character the current regex misses) applied only at line 41 leaves line 57's assertion silently checking the old, unpatched pattern."
    }
  ],
  "bin/install-statusline-wrapper.js": [
    {
      "category": "simplification",
      "line": 6,
      "summary": "The entire generated wrapper script \u2014 including a hand-escaped /^\\d+\\.\\d+\\.\\d+$/ version regex \u2014 lives as one un-lintable, un-typechecked template literal, and its only test asserts the written bytes equal buildWrapperSource()'s own return value rather than that the generated script is valid, runnable JS.",
      "failure_scenario": "tests/install-statusline-wrapper.test.js:19 does `assert.strictEqual(contents, buildWrapperSource())` \u2014 a tautological self-comparison that always passes regardless of whether the nested script is syntactically valid. A future edit to the template that drops one backslash from \\\\d (or otherwise breaks the nested script) would still pass `npm test`; the first real signal would be a user's statusline going silently blank after a plugin upgrade regenerates the wrapper from the broken template."
    }
  ],
  "bin/lib/issues/grouping.js": [
    {
      "category": "simplification",
      "line": 91,
      "summary": "extractKeyFiles's journey-health and docs-health branches are byte-for-byte identical two-line bodies (and match harness-health's core logic too), collapsible into one shared branch.",
      "failure_scenario": "Each of the three branches independently calls BOLD_HEADER_RE.exec(body) and returns [targetHeader[1]] or []; the branch's own comments explicitly say 'the same extraction applies' for both journey-health and docs-health, so a future fifth health-origin, or a fix to the header-parsing edge case harness-health's new-skill guard already needed, is likely to be pasted in as another near-identical if block rather than extending one shared code path \u2014 the same copy-paste growth that produced today's triplication."
    }
  ],
  "bin/lib/issue-branch-tracking.js": [
    {
      "category": "simplification",
      "line": 90,
      "summary": "generateWorkflowYaml's 'Extract referenced issues' step (env block + 4-line shell script) is duplicated verbatim between the label-fix-branch and cleanup-fix-labels job arrays.",
      "failure_scenario": "Lines 53-61 and 90-98 are an exact copy of the same 9 lines of generated YAML/shell text. A future fix to the extraction shell (e.g. handling multi-line commit messages, or changing the sort -un dedup) applied to one copy and not the other leaves the two jobs silently parsing referenced issues differently, even though both exist to implement the identical 'which issues does this push reference' logic."
    }
  ],
  "bin/lib/code-health/cache.js": [
    {
      "category": "simplification",
      "line": 21,
      "summary": "code-health/cache.js's computeChurn duplicates health-core/runs.js's computeChurn wholesale just to add one derivable extra field (`stayed`), rather than computing `stayed` in the shared version and letting the other three engines ignore the extra return key.",
      "failure_scenario": "A future fix to the shared churn ratio/union math (e.g. a rounding tweak or a denominator change) lands in health-core/runs.js and has no automatic reason to also land in code-health's byte-for-byte fork in cache.js, since nothing but a comment in each file marks the two as needing to move together."
    }
  ],
  "tests/hooks-worktree-detect.test.js": [
    {
      "category": "simplification",
      "line": 17,
      "summary": "`linkedWorktreeOf()` here (lines 17-22) is byte-for-byte identical to `linkedWorktreeOf()` in tests/hooks-pre-tool-use.test.js (lines 228-233), differing only in the mkdtemp prefix string, and the same copy-paste pattern recurs for `gitRepo()`/`withPolicy()`/`tmpProject()` across tests/hooks-dispatcher.test.js, tests/hooks-session-start.test.js, and tests/policy.test.js \u2014 no shared tests/ fixture module exists.",
      "failure_scenario": "The 'same' helper has already silently drifted: this file's `gitRepo()` (lines 10-15) does `git init` + an initial empty commit, while tests/hooks-pre-tool-use.test.js's `gitRepo()` (lines 11-15) only inits, forcing that file to define a second `gitRepoWithCommit()` wrapper (lines 222-226) to get the same behavior. A `git worktree add` invocation change (e.g. a future git version deprecating the `-b` positional form used identically in both copies) requires finding and patching every one of these ~5 independent copies, and missing one leaves that file's worktree fixtures silently broken while its siblings pass."
    }
  ],
  "bin/lib/code-health/tests/fingerprint.test.js": [
    {
      "category": "simplification",
      "line": 61,
      "summary": "Five v2 tests in fingerprint.test.js locally re-require `{ fingerprint }` from '../fingerprint' even though it is already destructured once at module scope on line 3.",
      "failure_scenario": "Lines 61, 67, 75, 83, and 100 each re-run `const { fingerprint } = require('../fingerprint');` inside the test body, shadowing the top-of-file import for no behavioral reason (require() is cached, so it's redundant rather than wrong) \u2014 the v1 tests earlier in the same file just use the top-level binding directly, so the v2 block is inconsistent with its own file's convention for zero benefit, and a reader has to double-check whether the local require is actually doing something different."
    }
  ],
  "bin/lib/health-core/tests/durable-state.test.js": [
    {
      "category": "simplification",
      "line": 218,
      "summary": "Six writeState tests each re-declare the same ~7-rule fakeRunner matcher script almost verbatim, varying only the final ref-update rule's behavior.",
      "failure_scenario": "The rev-parse/fetch/show/blobs/trees/commits matcher rules are copy-pasted near-identically across lines 197-209, 218-234, 242-254, 262-274, 327-336, and 411-420. When the real writeState git/gh call sequence changes (a new blob type, a different argument shape), a maintainer must update the matcher rules identically in all six places; missing one leaves a stale mock that silently keeps passing against an outdated call shape instead of catching the drift, undermining the regression coverage these tests exist to provide."
    }
  ],
  "bin/lib/harness-health/tests/cli-next-target.test.js": [
    {
      "category": "simplification",
      "line": 26,
      "summary": "The bare-git-repo health-state-branch seeding helper (seedDurableCursors/seedDurableRuns) is hand-duplicated in 3 files within this scope, each one's own comment naming the others as precedent instead of the group extracting a shared helper.",
      "failure_scenario": "bin/lib/harness-health/tests/cli-next-target.test.js:26-42 (seedDurableCursors), bin/lib/harness-health/tests/cli-validate-findings.test.js:27-43 (seedDurableRuns), and bin/lib/journey-health/tests/cli-validate-findings.test.js:30-46 (seedDurableRuns) each independently spell out the same 8-step sequence (git init --bare, seed dir init, checkout -b health-state, write the JSON fixture, add+commit+push to the bare remote, then git init + remote add origin in the real test root) \u2014 with bin/lib/health-core/tests/ already established in this repo as the home for genuinely shared health-state test infrastructure (durable-state.test.js, runs.test.js, retry-cli.test.js). A fix to the technique applied to only one copy (the likely real-world outcome, since nothing forces the siblings to be touched in the same change) leaves the others silently exercising a stale technique until they happen to be edited for an unrelated reason."
    }
  ],
  "bin/lib/harness-health/tests/scope.test.js": [
    {
      "category": "simplification",
      "line": 15,
      "summary": "13 near-identical tmp() helpers across harness-health/tests and journey-health/tests mkdtempSync a fresh directory per test with no cleanup, while the sibling bin/lib/issues/tests/local-store.test.js's own tmp(t) registers t.after() cleanup \u2014 an unexplained divergence in an otherwise copy-pasted one-liner.",
      "failure_scenario": "grep across just 11 of these files counts ~136 mkdtempSync-backed tmp() calls (scope.test.js alone: 41, cli-next-target.test.js: 20, cli-validate-findings.test.js: 17) with no matching fs.rmSync anywhere in harness-health/tests or journey-health/tests, versus bin/lib/issues/tests/local-store.test.js:9-13's tmp(t), which does `t.after(() => fs.rmSync(dir, { recursive: true, force: true }))`. Since harness-health/tests, issues/tests, and journey-health/tests all run under the same `npm test` (per CLAUDE.md's own Commands table), every local test run and CI job leaves on the order of a hundred-plus orphaned directories under os.tmpdir() with nothing but the OS's own best-effort temp sweep to reclaim them."
    }
  ],
  "skills/review/SKILL.md": [
    {
      "category": "simplification",
      "line": 464,
      "summary": "Step 6.6 (Design Findings Routing) and Step 3-ter (Visual Findings Routing, line 474) are near-verbatim copy-paste of the same three-step routing procedure, differing only in the category label and upstream source skill name.",
      "failure_scenario": "A future change to the shared routing mechanics both blocks reference (e.g., adjusting the severity-to-route mapping, or how resolved findings fold back into the Step 7 summary) has to be hand-applied twice within this same file to stay consistent \u2014 nothing marks the two blocks as duplicated or points one at the other, so it's easy to update Step 6.6 and forget Step 3-ter (or vice versa), leaving Design-Quality and UI/Visual findings routed inconsistently."
    }
  ],
  "skills/visual-review/SKILL.md": [
    {
      "category": "simplification",
      "line": 178,
      "summary": "Step 4's \"Applying a recommendation\" apply-gate and Step 5's \"Fix flagged issues\" apply-gate hand-roll the identical three-branch procedure (AskUserQuestion apply-all/choose-individually/none -> per-item direct Skill-tool invocation -> /claude-tweaks:test skip-qa re-verify -> \"report, don't auto-revert\" framing) as two separately-written blocks instead of one referenced sub-procedure; line 221 explicitly concedes it is \"the same... approach as Step 4's apply-gate\" without factoring it out.",
      "failure_scenario": "The two copies have already drifted: Step 4's AskUserQuestion options (line 182-184) are worded \"Apply all (Recommended)\" / \"Choose individually\" / \"None -- just the report\", while Step 5's (line 218-220) are independently worded \"Apply all recommended (Recommended)\" / \"Choose individually\" / \"Skip fixes\" -- different labels for what the file itself calls the same mechanic. A future change to the shared re-verify command or option set now has to be found and applied in both places, and the next edit is as likely to touch only one copy as this one did."
    }
  ],
  "skills/reflect/hindsight-mode.md": [
    {
      "category": "simplification",
      "line": 44,
      "summary": "hindsight-mode.md's Interactive-mode `AskUserQuestion` block (lines 44-48) is a near-verbatim copy of full-mode.md's block (lines 51-55) -- identical question shape and both options, differing only in the word \"findings\"/\"insights\" -- even though light-mode.md in the same directory already establishes the pattern of pointing at full-mode.md's block (\"Same table/AskUserQuestion mechanics as full mode\") instead of restating it.",
      "failure_scenario": "A future change to the shared \"Apply all / Override specific items\" mechanic (e.g. adding a third routing option, which CLAUDE.md's batch-decision convention explicitly allows) requires editing the same boilerplate in two files (full-mode.md and hindsight-mode.md); light-mode.md would not need touching since it already just references full-mode.md, making it easy for an editor to update the two files they can see duplicated content in and never notice hindsight-mode.md silently fell out of sync, since nothing marks it as a copy rather than an independent block."
    }
  ],
  "skills/design-wrapper/modes/survey.md": [
    {
      "category": "simplification",
      "line": 53,
      "summary": "Step 4 spends a sentence describing how `decline_count` gets \"incremented in-place\" before immediately disclaiming that this mode never actually performs it -- the same fact (increment ownership belongs entirely to /flow) is then stated a second time, unambiguously, in the \"Ownership\" paragraph 29 lines later (line 82).",
      "failure_scenario": "A reader who only reads Step 4 in isolation comes away thinking `survey` mode itself performs the cache increment (\"Increment in-place when... being re-surfaced\"), which reads as contradicting the file's own later, authoritative statement that \"/flow is the only writer... This wrapper... never writes the declined cache itself.\" The two statements are not actually inconsistent once both are read, but the first is redundant scaffolding that exists only to be corrected by the second -- collapsing Step 4's clause to a one-line forward-reference to \"Ownership\" would remove the confusing first pass entirely."
    }
  ],
  "skills/flow/multispec-review-console.md": [
    {
      "category": "simplification",
      "line": 158,
      "summary": "\"On approval\" (steps 6-10) and \"On override\" (steps 5-9) duplicate five full cleanup steps almost verbatim instead of extracting them into a shared subsection, the way \"Per-issue label cleanup\" already is in the same file.",
      "failure_scenario": "Lines 159-161 (\"Finish the shared branch\", \"Release each issue claim\", \"Remove grants\") are near-word-for-word identical to lines 172-174. A future change to claim-release or grant-removal behavior (e.g. a new branch-finish outcome type) has to be hand-applied to both copies, and nothing forces the second copy to be kept in sync with the first \u2014 the file's own \"Per-issue label cleanup\" extraction shows the author already knows this pattern but didn't apply it to these five steps."
    }
  ],
  "skills/browse/SKILL.md": [
    {
      "category": "simplification",
      "line": 92,
      "summary": "SKILL.md's \"Operation Mapping\" table (lines 96-109) re-states 10 of the 13 rows in agent-browser-reference.md's canonical Operation vocabulary table almost verbatim, despite the file twice pointing readers to that sub-file as the source of truth.",
      "failure_scenario": "When agent-browser-reference.md gains or changes a command (e.g. a new `find` variant, or the `screenshot --annotate` flag changes), the editor must remember to also update this second, independently-formatted copy in SKILL.md \u2014 miss it, and the two tables silently disagree on the exact CLI syntax a consuming skill would copy."
    }
  ],
  "hooks/hooks.json": [
    {
      "category": "simplification",
      "line": 26,
      "summary": "PreToolUse and PostToolUse register 9 separate `{\"matcher\": \"Bash\", \"hooks\": [...]}` blocks (6 in PreToolUse for git commit / git push / git -C / cp / mv / tee, 3 more in PostToolUse) that differ only in the `if` string, even though the schema already supports multiple hook objects inside one matcher's `hooks` array (as every other event in this same file demonstrates with a single-item array).",
      "failure_scenario": "Widening the write-detection gate again (as already happened once in commit cab6142, 'Widen the worktree.always security gate to cover cp/mv/tee writes') means copy-pasting a whole new `{\"matcher\": \"Bash\", \"hooks\": [...]}` block instead of appending one entry to an existing array; a reader auditing 'what Bash patterns does PreToolUse guard' has to mentally merge 6 near-identical blocks instead of reading one list, and the git log shows this has already happened as incremental copy-paste additions (0445448 -> 02bb1e2 -> cab6142) rather than one edit to a shared array."
    }
  ],
  "bin/lib/hooks/git-command.js": [
    {
      "category": "simplification",
      "line": 180,
      "summary": "fileWriteTargets's cd-token handling (lines 180-188) duplicates gitTargets's cd-token handling (lines 104-116) verbatim in logic and variable names, just reformatted.",
      "failure_scenario": "pre-tool-use.js's checkWorktreeRequired calls both functions back-to-back on the same Bash command (once for git-commit detection, once for tee/cp/mv detection) and relies on them agreeing about the effective cwd for identical cd sequences. A future fix to cd-resolution edge cases (a new isUnresolvable pattern, pushd/popd support, etc.) applied to only one copy would make the worktree-required gate's git-write detection and its non-git-write detection silently disagree about the same command's target directory."
    }
  ],
  "skills/deepen/SKILL.md": [
    {
      "category": "structural-contract",
      "line": 172,
      "summary": "deepen's Relationship-to-Other-Skills table has no row for /claude-tweaks:ledger, even though ledger/SKILL.md documents deepen as a consumer \u2014 a one-directional cross-reference in violation of CLAUDE.md's bidirectional-table rule.",
      "failure_scenario": "ledger/SKILL.md line 249 states '/claude-tweaks:deepen | Appends staged and declined depth candidates when invoked in a pipeline. Uses phase: deepen.' and ledger's phase enum (line 88/107) explicitly lists `deepen` as a valid ledger-writing phase, but deepen/SKILL.md's own Relationship table (starting line 172) contains zero mention of ledger anywhere in the file (verified via full-file grep). A future editor changing deepen's staging/decline behavior has no reciprocal row in deepen's own file pointing back at the ledger phase contract, so the dependency can silently drift out of sync \u2014 exactly the one-directional-reference failure mode CLAUDE.md's 'Don't add one-directional cross-references \u2014 always update both sides' rule exists to prevent."
    }
  ],
  "skills/research/SKILL.md": [
    {
      "category": "structural-contract",
      "line": 111,
      "summary": "research's '## Component-Skill Contract' section is placed after '## Anti-Patterns' (line 101) instead of immediately before it, violating CLAUDE.md's explicit CSC placement rule.",
      "failure_scenario": "CLAUDE.md's Component-Skill Contract convention requires the paragraph be 'placed immediately before ## Anti-Patterns'. Every other CSC-bearing skill in the repo (simplify, reflect, deepen, journeys, visual-review, design-wrapper, capture, challenge, stories, plus browse/ledger/dispatch/etc.) follows this ordering \u2014 verified by scanning all 33 SKILL.md files for headings interleaved between CSC and Anti-Patterns, which found research as the sole exception. In research/SKILL.md the order is Next Actions (92) -> Anti-Patterns (101) -> Component-Skill Contract (111) -> Relationship (117). Any tooling or reviewer that locates a skill's CSC paragraph by its documented structural position (rather than a full-text search) will fail to find research's CSC section in the expected place."
    }
  ],
  "skills/harness-health/SKILL.md": [
    {
      "category": "structural-contract",
      "line": 3,
      "summary": "harness-health omits `allowed-tools` even though its tool usage and 'never edits' promise are functionally identical to code-health/SKILL.md, which explicitly restricts to Read/Grep/Glob/Bash/AskUserQuestion.",
      "failure_scenario": "code-health/SKILL.md:4 declares `allowed-tools: Read, Grep, Glob, Bash, AskUserQuestion` and its own body never calls Write/Edit/Task (verified by grep). harness-health/SKILL.md describes itself identically ('Never edits code \u2014 only harness documentation', same SELECT->JUDGE->VERIFY->FINGERPRINT/DEDUP->FILE pipeline shape per its own Relationship row for code-health) and also never calls Write/Edit/Task in its body, yet declares no `allowed-tools` at all, so it inherits the full default toolset. Per CLAUDE.md's rule 'Declare allowed-tools only to restrict... Declaring a narrow set... when the skill actually uses [more] is a bug \u2014 it either lies about the contract or silently breaks the skill', the inconsistency runs the other way here: three siblings documented as having the exact same restricted tool profile as code-health apply the restriction unevenly, so only code-health's frontmatter would actually block an accidental Write/Edit call if one crept into the skill body."
    }
  ],
  "skills/docs-health/SKILL.md": [
    {
      "category": "structural-contract",
      "line": 3,
      "summary": "docs-health omits `allowed-tools` even though it never calls Write/Edit/Task/WebFetch and is documented as sharing code-health's exact pipeline shape and 'never edits' contract.",
      "failure_scenario": "docs-health/SKILL.md's description states 'Never edits docs' and its own body implements the 'tracked-dependency freshness checks' entirely via the Bash-invoked `bin/docs-health.js check-freshness` helper (line 79) rather than WebFetch, and a full-file grep confirms no Write/Edit/Task/WebFetch calls \u2014 the identical Read/Grep/Glob/Bash/AskUserQuestion profile code-health restricted itself to. docs-health declares no `allowed-tools` line, so \u2014 like harness-health and journey-health \u2014 it has no frontmatter-level enforcement backing its own 'never edits' promise the way code-health's declared allowlist does."
    }
  ],
  "bin/lib/health-core/tests/retry-cli.test.js": [
    {
      "category": "wrapper-proxy",
      "line": 95,
      "summary": "The only test exercising the ok:true (success) branch of retry-queue update ('fresh') uses a fingerprint that was never present in the initial retryQueue, so it never actually proves dequeueRetry removes a real queued entry on success \u2014 every other test in the file only ever supplies ok:false results.",
      "failure_scenario": "If retry-cli.js's `if (r.ok) { queue = dequeueRetry(queue, r.fingerprint); }` line were accidentally deleted, or that branch's condition were inverted, a fingerprint that genuinely succeeded after prior failures would stay stuck in the persisted retry-queue.json forever (repeatedly re-drained/escalated by every subsequent firing) \u2014 yet this test would still report `escalated.length === 1` with `fingerprint === 'stuck'` exactly as before, because 'fresh' (the only ok:true entry in the whole file) was never in the queue to begin with, so dequeueRetry running or not running on it is unobservable. The file already has the technique to check this (`ds.readDurableState().retryQueue`, used in the malformed-entry test a few tests later) but never applies it to a pre-existing, now-successful fingerprint."
    }
  ]
}