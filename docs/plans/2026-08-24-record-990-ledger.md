# Open Items — archiveRunDir() late-write TOCTOU fix (record #990)

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | wrap-up | pr — PR #1392 (head worktree-dispatch-record-990, this work) — open, awaiting merge | accepted | Record #990 carries `auto:build` but not `auto:merge`, and `merge-authorization: ask` (config.yml) — this run has no authorization to merge on its own. PR left ready/open for human merge authorization or a future `auto:merge` grant; nothing further for this run to fix. |
