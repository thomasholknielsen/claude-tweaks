# Open Items — flow run spec-559-560 (merge-verification: policy key + merge-site consumers)

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | build | M1 regression test (tests/merge-verification.test.js, 'requesting integration-model and merge-verification together reuses…') uses the AC1 fixture whose integration-model is explicit, so it passes with or without the CLI dedup — add an unset-integration-model fixture (or a detection-count probe) so the dedup has real regression coverage. Parked at SDD final re-review (not load-bearing: perf-only). | deferred | → backlog (auto-routed by ledger narrowing at autonomy: unattended — blocker: expands scope, resolve-policy.js needs injectable derivation deps for a discriminating test). Staged: spec-559/staged/ledger-record-resolve-policy-dedup-test.md — record created at the consolidated console |
| 2 | build/skill | 'Derived-default policy key' is now a repeated pattern (integration-model, merge-verification): schema row with no static default + a code twin of a _shared prose ladder + resolve-policy.js computed-default block + /help policy derived-key rendering. Worth naming once in skills/_shared/policy-schema.md's Canonical read path (or the lever checklist) so the third key follows it without a whole-branch review catching the gaps. [route: D1 — project convention] | fixed | Review Step 4 hindsight (Change now, D1): "Derived-default keys" paragraph added to skills/_shared/policy-schema.md Canonical read path — commit c37a5ad7 |
| 3 | review | skills/_shared/policy-schema.md:121 (merge-verification row) says "derivation runs only when the key is absent or invalid" but the CLI it cites preserves `invalid: true` (empty `--values` line) — only the in-process helper `resolveMergeVerification` derives on invalid. Medium (Error-Handling / contract prose). Patch staged at spec-559/staged/review-1.patch. | open | — |
| 4 | review | Doc freshness (3i, informational): README.md, CLAUDE.md, docs/getting-started.md, docs/plugin-structure.md, docs/skill-graph.md, docs/skill-authoring.md match changed areas (skills/**, bin/**) but were not updated — checked at build Step 6.5: none enumerate policy keys, lever counts, or single-file bin/lib modules. | observation | Review in wrap-up; expected no update needed |
| 5 | wrap-up | pr — PR #588 open, head worktree-flow+spec-559-560 (this run's own draft PR, opened at run start per pr-early-run-lifecycle; residue sweep, remedy: record) — disposed by the multi-spec finish step (mark ready + merge/PR decision at the consolidated console), not by spec 559's own wrap-up | open | — |
| 6 | build | Two copies of the `bot:blocked` bootstrap payload (the `skills/_shared/label-bootstrap.md` fence read live by `tests/bin-lib/issues/labels.test.js`, and the duplicate in `skills/dispatch/settle-and-merge.md`'s Settle step comment) were both widened for the gate's second meaning but nothing pins them to each other — a future edit to one silently drifts the other. Surfaced by the #560 fix wave; not fixed in-run (a new pin/test is scope beyond #560). | open | — |
| 7 | test | Pre-existing at build time: `tests/changelog-coverage.test.js` fails 2× on this branch because `origin/main` shipped 6.87.0 after the branch base (`6b80ccf6`); the branch touches neither CHANGELOG.md nor docs/shipped-versions.tsv. Resolves on the pre-finish catch-up merge with `origin/main` (multi-spec.md shared-worktree step 3). | fixed | Catch-up merge efdddd5f brought 6.87.0's CHANGELOG/shipped-versions entries into the branch — re-verification on efdddd5f green (see spec-560 decisions.md) |
| 8 | build/skill | "Empirical premise-check first" — #560's Task 0 (a probe PR against a throwaway base) reversed the plan's core premise (`--auto` is an immediate merge on unprotected repos) before any prose was written; the spec's blocking Task 0 shape is worth naming as a convention for specs that encode third-party CLI behavior. [route: D4 — memory / D5 — upstream specify guidance] | observation | Head start for wrap-up Skills curation |
| 9 | review | `_shared/pr-first-merge.md` Step 2.5 (spec 560): high — park comment inline-interpolates CI-controlled check names (3b, reproduced); initial `gh pr view` has no failure branch and an empty rollup reads green under merge-when-green (3c, reproduced); empty/invalid lever has no row (3c-A, verified); `bot:blocked` add unverified while the log asserts it (3c-B, verified). Medium — wait+green lacks the headRefOid comparison; state-not-OPEN ordered last; `github-pr-scan.md:150` write-time re-read omits `bot:blocked`; the conformance test doesn't pin the other reason tokens. One combined patch staged at spec-560/staged/review-1.patch (verified applies clean; pins 31/31 with it). | open | — |
| 10 | review | Doc freshness (3i, informational, spec 560): registry docs match `skills/**` but were checked at build Step 6.5 — no update owed. 3i-diagram: the gate is a 3-value × 3-state decision table with four outcomes and no diagram; consider `/claude-tweaks:visualize state merge-verification-gate` (recommendation only). | observation | Suggest to user in wrap-up |
| 11 | review/hindsight | Structural: `bot:blocked` now carries two meanings (retry ceiling with grant revocation vs merge-verification park with grants intact) — a distinct `bot:parked` label would make the state legible and routable directly. Label taxonomy is a `_shared/work-record.md` contract change → Capture, not a hindsight fix. | deferred | Capture — record proposal staged at spec-560/staged/hindsight-record-bot-parked-label.md (backlog); created at the consolidated console |
