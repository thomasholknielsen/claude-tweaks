# Open Items — Sweep residue: needs:decision marker, comment template, needs:* worklist rule (#1488)

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | build/skill | `.claude/skills/skill-prose-conformance-tests/SKILL.md`'s "Proving discrimination without editing the tree" section documents `git show {base}:{file}` but doesn't warn that `{base}` must be a fixed, independently-verified ancestor SHA — never a moving ref like `HEAD` (self-defeating once the change lands, since HEAD becomes post-change content) — nor does it mention pairing the pin with its own ancestor-precondition test. This build hit exactly that mistake at Task 11 (caught by review, fixed to a fixed SHA) after Task 10 had already made the milder tautological-literal mistake the section does cover; Task 12 then landed the correct fixed-ancestor-SHA-plus-precondition-test form cleanly. Enrichment candidate for that section. | open | — |
| 2 | review | `plugin/skills/backlog/grant-lane-decision.md`'s Write mechanics (lines 77-81): `grant-mode.md`'s headless `needs:decision` write is two unguarded sequential `gh` calls (label add, then comment post) with no failure isolation between them — unlike `refine-mode.md`'s own batched path through `apply-refine-labels.js`, which explicitly separates edit-failure from comment-failure (#1073). A comment-post failure after a successful label write on this unattended, no-human-present path silently and permanently excludes the record from every headless worklist with no visible decision comment ever posted, violating AC1's "exactly one unresolved decision comment" guarantee. Found by `/claude-tweaks:review`'s Error Handling lens (3c), confirmed via direct source verification (`apply-refine-labels.js:192-224`'s edit/comment split read directly). Severity: High. | open | staged: `staged/review-1.patch` |
| 3 | review/hindsight | Ran all 5 hindsight evaluations (approach correctness, structural debt, missing consolidation, convention drift, skill-worthy patterns). No new findings — the two genuine observations this pass would otherwise have raised were already caught and routed earlier in this same run: the grant-mode.md/refine-mode.md write-path asymmetry (structural debt) is Step 3 Routing's staged `staged/review-1.patch` (ledger item 2), and the go-red-control-via-fixed-SHA pattern (skill-worthy) is `/build` Common Step 4.5's item 1 above. No changes made this step. | accepted | approach is sound; both real observations already routed elsewhere in this run |
