# Open Items — File-overlap bundle: #1837,#1839,#1840,#1851,#1873,#2026,#2029,#2265,#2294,#2333,#2335,#2341

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | review | HIGH (3c, confirmed via direct-verification): `plugin/bin/lib/verify/extract.js:57` — `extractFailingRegion`'s 'summary'-family keep-line regex `/^FAIL \|^Error:/` doesn't tolerate leading whitespace or vitest's `❯`/`FAILED` prefixes (unlike this file's own sibling `SUMMARY_FAIL_RE` at line 164), so a failed vitest run's `report.json.failingRegion` can come back empty. | open | — |
| 2 | review | MEDIUM (3c, confirmed via direct-verification): `plugin/bin/verify.js:39-46` — `enrich()`'s unreadable-log catch path never sets `countsFamily`, so an unreadable `tests` log silently skips the new `countsUnparsed`/CAVEAT mechanism entirely, reproducing the exact regression-comparison-silently-disabled bug #1837 was filed to fix, via a different root cause. | open | — |
| 3 | review | HIGH (3b, confirmed via direct-verification): `plugin/bin/lib/issues/record.js:684,693` — `specShapedBody`'s `templateStamp` param is type-checked only (unlike sibling `verifiedAsOf`, format-validated by `SHA_SHAPE_RE`) and spliced raw into the body; `harness-health/issue-payload.js:63-64` builds it from `finding.templateSource`, a free-text judge-set field with zero schema validation in `validate-finding.js`. A `templateStamp` containing a spoofed `## Original request` heading neutralizes `materialize.js`'s placeholder gate for everything after it. | open | — |
| 4 | review | LOW (3b, confirmed via direct-verification): `plugin/bin/lib/issues/materialize-format.js:58` — `stripCodeSpans`'s inline-code-span removal `/(`+)([\s\S]*?)\1/g` has no length cap; a long backtick-heavy issue body (bounded by GitHub's ~65KB limit) could cause slow quadratic-ish backtracking before `shapeGate` completes. | fixed | Auto-applied per review-auto-apply-ceiling: low — see commit noted in decisions.md |
| 5 | review | HIGH (3f, confirmed via direct-verification): `tests/bin-lib/verify/cli.test.js` — missing the spec-deliverable-named end-to-end test asserting the CAVEAT stdout line for an unparseable `tests` check under `--count-stamp`; the line is implemented (`verify.js:530-531`) and `report.test.js:40` covers the `countsUnparsed` field, but no `cli.test.js` case exercises the real CLI's stdout for this path. | open | — |
| 6 | review | MEDIUM (3e, confirmed by reproduction — both agents independently found it): `plugin/bin/materialize.js:159` (+ `harness-health.js:32`) — plugin-version-from-manifest resolution duplicated a third time (pre-existing `bin/lib/hooks/session-start.js:43-54`; two new copies this diff) instead of extracted to `bin/lib/**`, against CLAUDE.md's stated architecture. | open | — |
| 7 | review | LOW (3d, unconfirmed — single-agent, not reproduced): `plugin/skills/_shared/label-bootstrap.md:65` — reconcile pass's second sequential `gh label list --search` call per label instead of fetching once and diffing in-memory; gated on rare `LABEL_BOOTSTRAP_VERSION` bump. | observation | Low-confidence — routed to Wrap-Up Console |
| 8 | review | LOW (3f, unconfirmed — single-agent, not reproduced): `tests/bin-lib/verify/run.test.js:283-310` — asserts spawn-options shape (`opts.env.NO_COLOR`) rather than observable child-process env, a mild refactor-coupling risk. | observation | Low-confidence — routed to Wrap-Up Console |
| 9 | review/hindsight | [skill: NEW - safe-revert-to-prove-red] Pattern observation: verifying a fix's own regression test discriminates (revert, confirm red, restore) is safer done via an Edit-tool toggle on the uncommitted change than via `git stash`, since this repo's stash stack is shared across sibling worktrees (a `stash pop` can apply a sibling's WIP — an existing memory-level rule this session both violated once, harmlessly, and then followed correctly 3 more times). Refines the existing "verify test discrimination by reverting" convention with the worktree-safe mechanism. | deferred | STAGED to staged/reflect-1.md for the Wrap-Up Review Console |
