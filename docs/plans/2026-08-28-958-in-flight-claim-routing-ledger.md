# Open Items — 958: Auto-route an in-flight tombstone claim into dispatch's settle-the-existing-build handling

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | review | High: `claim-targets.md`'s in-flight routing passes "this run's own `{target}`" (the single stopped issue) to `resume-confirmation.md` alongside `{run-dir}` resolved from a *different*, pre-existing run's PR marker. When that found run was multi-spec (multiple issues sharing one PR — `_shared/pr-early-run-lifecycle.md`'s "Multi-spec runs share one PR"), `{target}` under-specifies the resume and never re-touches the sibling spec(s); the PR body's own `### Resume` line already carries the correct full target list but is never read. | fixed | Auto-resolved (consoleAutoResolve, unattended) — staged/review-1.patch applied, commit `2851ace72`; staged file retained as revert artifact |
| 2 | review | High: the PR-body-derived `{run-id}` is joined into `$RUN_ROOT/.claude-tweaks/pipelines/{run-id}` and handed to `resume-confirmation.md`/`check-resume-freshness` with no format/anchoring validation, unlike the sibling `link` field (`isSameRepoPrUrl`-validated before any `gh` call). `isAnchoredUnderRoot` only confirms the path is inside the main checkout, not specifically under `.claude-tweaks/pipelines/`, and `checkResumeFreshness` fails open (`no-state` → `safe: true`) on a directory that isn't a real run dir — a crafted marker in an editable PR body can point `PIPELINE_RUN_DIR` at an arbitrary in-repo path. | fixed | Auto-resolved (consoleAutoResolve, unattended) — staged/review-1.patch applied, commit `2851ace72`; staged file retained as revert artifact |
| 3 | review | Medium: `settle-and-merge.md`'s Claim-contest special case named only the "Claim contested" stop, not "Claim in-flight" — the new routing's own claim that headless self-report "already" covers the in-flight stop shape wasn't backed by that (unchanged) file. | fixed | Broadened the special case to cover both stops with distinct failing-check-names (`flow-step-2.8-claim-contest` / `flow-step-2.8-claim-in-flight`); pinning test added; commit `25257bc79` |
| 4 | review/hindsight | [skill: pr-early-run-lifecycle] Observation: external run-id resolution from a PR body should read the `### Resume` line and validate the run-id shape — a forward note for the next hook-in that resolves an identity from a PR body. | fixed | Skills curation row applied it directly (additive+high-confidence+high-reversibility), commit `d2eb3ba75`; source insight retained at staged/reflect-1.md |
| 5 | wrap-up | Near-miss (causal: terminal): dispatched review lens agent 3c-A led its reply with narrative before the required status line, a subagent-output-contract violation caught only via `friction-events.js`'s contract-violation log during wrap-up, not re-prompted live. Its finding was still trustworthy via reproduction-pair agreement with 3c-B. | accepted | No correctness impact this run — content independently reproduced. Accepted as a one-off dispatch-monitoring lapse, not a pattern warranting a skill change. |
| 7 | ops | PR #1600 open, head `worktree-dispatch-record-958` (this work) (reason-not-auto: awaiting Review Console merge-authorization decision — Medium+ review findings block auto-merge). | observation | Resolves via this same run's Phase 4 merge-authorization decision, below — not a separate drill item |
| 6 | wrap-up | Journey staleness: `act-on-a-contested-claim.md` Step 3b described the in-flight card as always requiring manual PR resolution; #958 made that conditional on a human being present. | fixed | Auto-resolved (consoleAutoResolve, unattended) — staged/wrap-up-journey-1.md applied, commit `007dd5819`; staged file retained as revert artifact |
