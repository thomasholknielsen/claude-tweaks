# Open Items — spec 686-693 (release-claim/log-decision CLIs; failed-agent retrieval; auto third-stop; draft-merge + worktree-name; phase banner; consoleAutoResolve trusted classes; resolve-run-dir; teardown ordering)

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | wrap-up | origin/worktree-flow-spec-697 — merged into origin/main, not deleted (residue sweep, remedy: auto) | fixed | Deleted stale merged remote branch (confirmed reachable from origin/main tip 538c7eef) |
| 2 | wrap-up | PR #709 (this run's own bundle PR) — open, head worktree-flow-spec-686-687-688-689-690-691-692-693 (residue sweep, remedy: record) | accepted | Intentional — stays open until all 8 specs in the 686-693 bundle complete; merged/closed via the Shared teardown at end of run (user-confirmed) |
| 3 | test | `tests/changelog-coverage.test.js` fails: version 6.90.0 shipped on origin/main with no CHANGELOG entry — a concurrent sibling session's release, unrelated to spec 687's diff (verified: pre-existing, 2 failures, 3990/3992 pass both before and independent of this spec's commits) | fixed | Merged origin/main (693dbd43, brought in v6.90.0+v6.91.0's own already-correct CHANGELOG entries plus 2 more releases' worth of upstream drift). Re-verified: 4075/4075 pass. |
| 4 | test/build | `tests/changelog-coverage.test.js` fails again at spec 692 (2 failures, 4143/4145): origin/main has now shipped v6.92.0, including a PR titled "Ship bin/ CLIs for the hand-scripted per-run procedures — materialize, claim, release-claims, log-decision" that independently built its own `bin/log-decision.js` with a different CLI signature (`--run-dir <dir> [--spec][--skill] <STATUS> <message...>`) than the one this run's own spec 686 built and every spec since has been citing in skill prose (`--run <run-dir> --status --text [--spec][--step][--reversibility][--lever][--section]`). Attempted `git merge origin/main`: 6 additional real conflicts beyond the add/add on log-decision.js itself — `bin/hooks.js`, `docs/plugin-structure.md` (x2), `skills/flow/multi-spec.md`, `skills/flow/multispec-review-console.md` (x4), `skills/flow/steps-and-gates.md`, `skills/wrap-up/review-console.md` — stemming from origin/main's own overlapping work on run-dir archival (#717) and wakeup-parking (#712). Merge aborted cleanly (`git merge --abort`, verified clean tree) rather than resolved blind — too high-stakes for Phase-1 fix-exhaust (a duplicate-CLI conflict has real, branch-wide behavioral consequences depending on which signature wins, and every citing skill in this branch was written against ours). | open — deferred to the bundle's final Resolve Gate (mandatory human drill, not silenced by `auto`) rather than resolved per-spec; the 2 changelog-coverage failures are pre-existing upstream drift, not a regression from spec 692's own diff (independently confirmed: `resolve-run-dir`/pipeline-shadow-guard code and tests are unaffected) |
