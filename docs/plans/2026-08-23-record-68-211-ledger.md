# Open Items — #211 Wire a GitHub event trigger for cloud /review; #68 routine notification heartbeat

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | build | #68 Task 0 (empirical premise-check): confirm `RemoteTrigger {action: "update", trigger_id, body: {notifications: {...}}}` actually persists the field on the guided-creation path. Requires the `RemoteTrigger` tool, which this build-time session (Claude Code CLI worktree agent) does not have loaded (`ToolSearch select:RemoteTrigger` returns no match here) — it is reachable only from a session with that tool granted (a `/claude-tweaks:routine` invocation in an entitled environment). Per #68's own AC3, this blocks only the guided-path deliverable (AC2/AC3); the direct-create path (AC1, AC4, doc note) is implemented and unaffected. | deferred → backlog | Re-confirmed genuinely blocked in this ledger-resolution session too (`ToolSearch select:RemoteTrigger` still returns no match, dispatching-orchestrator tool access does not propagate to a Task-dispatched subagent). Auto-routed via `ledgerNarrowing` (autonomy-ceiling: unattended, `clearsFloor('blocked-dependency')` = true). Staged: `staged/ledger-record-68-notifications-probe.md`. Resolves at the Review Console's Queue writes section. |
| 2 | build | #211's core deliverable — verifying `create_webhook_trigger`'s actual behavior (required fields, response shape, whether the public "web UI only" doc claim is stale) and adding a webhook-trigger creation path to `/claude-tweaks:routine` — requires empirically calling the `RemoteTrigger` tool's `create_webhook_trigger` action. That tool is not loaded in this build session (`ToolSearch select:RemoteTrigger` returns no match), so this build did not implement the webhook-trigger creation path; writing speculative skill prose for an unverified field/filter grammar would repeat the exact anti-pattern #211's own Technical Approach warns against. See #211's own materialized spec, "Blocked / Future Work" section. | deferred → backlog | Re-confirmed genuinely blocked in this ledger-resolution session too (`ToolSearch select:RemoteTrigger` still returns no match, dispatching-orchestrator tool access does not propagate to a Task-dispatched subagent). Auto-routed via `ledgerNarrowing` (autonomy-ceiling: unattended, `clearsFloor('blocked-dependency')` = true). Staged: `staged/ledger-record-211-webhook-trigger.md`. Resolves at the Review Console's Queue writes section. |

## Resolve

Both items were confirmed genuinely blocked (RemoteTrigger tool unavailable to this ledger-resolution session as well — see Resolution column) and auto-routed to backlog proposals via the `ledgerNarrowing` bookkeeping capability at `autonomy-ceiling: unattended`. Each proposal is staged for the Review Console's Queue writes approval; on approval it becomes a real backlog record (`ready`, scored) tracking the same empirical-probe work for a future session with `RemoteTrigger` available.
