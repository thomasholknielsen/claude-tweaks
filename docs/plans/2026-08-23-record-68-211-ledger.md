# Open Items — #211 Wire a GitHub event trigger for cloud /review; #68 routine notification heartbeat

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | build | #68 Task 0 (empirical premise-check): confirm `RemoteTrigger {action: "update", trigger_id, body: {notifications: {...}}}` actually persists the field on the guided-creation path. Requires the `RemoteTrigger` tool, which this build-time session (Claude Code CLI worktree agent) does not have loaded (`ToolSearch select:RemoteTrigger` returns no match here) — it is reachable only from a session with that tool granted (a `/claude-tweaks:routine` invocation in an entitled environment). Per #68's own AC3, this blocks only the guided-path deliverable (AC2/AC3); the direct-create path (AC1, AC4, doc note) is implemented and unaffected. | open | — |
| 2 | build | #211's core deliverable — verifying `create_webhook_trigger`'s actual behavior (required fields, response shape, whether the public "web UI only" doc claim is stale) and adding a webhook-trigger creation path to `/claude-tweaks:routine` — requires empirically calling the `RemoteTrigger` tool's `create_webhook_trigger` action. That tool is not loaded in this build session (`ToolSearch select:RemoteTrigger` returns no match), so this build did not implement the webhook-trigger creation path; writing speculative skill prose for an unverified field/filter grammar would repeat the exact anti-pattern #211's own Technical Approach warns against. See #211's own materialized spec, "Blocked / Future Work" section. | open | — |

## Resolve

Both items need a session with the `RemoteTrigger` tool available (a `/claude-tweaks:routine` invocation, not this build's CLI worktree agent) to run the empirical probes described above, then implement/confirm accordingly.
