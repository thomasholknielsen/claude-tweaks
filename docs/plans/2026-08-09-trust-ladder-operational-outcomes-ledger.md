# Open Items — Trust ladder: merged-unreverted operational outcomes become known-good evidence in trust.js (#267)

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | build/simplify | [HIGH, safety] `parseGitLog`'s leading-newline bug: `git log --format='%H%x1f%B%x1e'` writes a newline immediately after each `%x1e` record separator, so every parsed record but the first carried a leading `\n` on its `sha` — silently degrading `isClosingCommitReverted`'s primary trailer-based detector for every commit but the first in any real log. A reverted closing commit could wrongly count as known-good operational evidence, feeding `permittedGrants`. Caught by the code-simplifier during Common Step 3's extraction of the shared `parseGitLog` helper, verified independently, fixed with a one-line `.trim()`, regression-tested against real git-log byte shape with mutation-verified discrimination. | fixed | Commit 076765d4. Full affected suites (493 tests) re-run clean. |
| 2 | review | [MEDIUM, security] `{integration-branch}` shell-injection risk in the `git log "{integration-branch}"` calls in skills/_shared/trust-table.md, skills/backlog/refine-mode.md, skills/capture/SKILL.md — a maliciously crafted branch name substituted into the shell command could inject arbitrary shell syntax. Corroborated by both 3c review agents. Not fixed in this leaf: exploitability requires already-privileged repo/policy.yml write access; it extends a pre-existing, unaddressed risk pattern (`{resolved-limit}`, `{resolved-ceiling}` share the same shape) that predates this leaf; a robust fix needs git-ref-name validation or centralized sanitization in `_shared/integration-branch.md` — bigger than this leaf's scope. | deferred | Documented here, not silently dropped. Candidate for a follow-up leaf covering all `{resolved-*}`/`{integration-branch}` substitutions repo-wide. |
| 3 | review | [HIGH, safety] `demo:pending` fallthrough: a `demo:pending` record (an outstanding, unresolved human-review request) was silently eligible for promotion to operational known-good, since `dispositionState`'s `'pending'` branch fell into the same catch-all `else` as `'none'` in `trustRows`'s disposition switch. Corroborated independently by 3a-A and 3b-A, both HIGH. | fixed | Uncommitted at review-scan time; fixed by adding an explicit `disposition === 'none'` branch (excluding `'pending'`) with an explanatory comment; regression test added: "a demo:pending record is never promoted to operational known-good". |
| 4 | review | [HIGH, correctness] `parseGitLog` missing-FIELD_SEP fragility: a git-log fragment with no `%x1f` field separator (a truncated write, or an embedded record-separator byte splitting one commit's message into two fragments) sliced garbage text into a fabricated sha instead of being dropped — a phantom sha could be returned as a closing commit and never match a real revert trailer, letting a genuinely reverted record grade known-good. Corroborated by 3c-A (HIGH) and 3c-B (MEDIUM), same root cause. | fixed | Uncommitted at review-scan time; fixed by returning `null` (filtered out) for any fragment lacking `FIELD_SEP`; regression test added: "parseGitLog drops a fragment with no field separator instead of fabricating a garbage sha". |
| 5 | review | [MEDIUM, security] `{resolved-window}` unescaped JS-string-splice injection in all 3 production callers (trust-table.md, refine-mode.md, capture/SKILL.md) — a policy.yml value containing a quote character would break out of the JS string literal inside the embedded `node -e "..."` block. Corroborated by 3c-A + 3c-B, MEDIUM. | fixed | Switched to `process.argv[1]` passed after `--`, matching spec 271 Finding 1's precedent in focus-mode.md. Verified by read-through (markdown-embedded snippets, no dedicated test file). |
| 6 | review | [LOW, docs] capture/SKILL.md's fail-closed contract ("file without `ready` on any failure") is prose-only — no test enforces it since the block is a markdown-embedded shell/node snippet, not a unit-testable function. Corroborated by 3c-A + 3c-B. | accepted | Accepted as-is: matches the existing verification approach for all of Task 7's markdown wiring (manual read-through); a dedicated test would require extracting the snippet into a testable module, out of this leaf's scope. |
| 7 | review | [LOW, observability] The new `git log` fetch in the 3 production callers has no exit-code or truncation diagnostic — a failed or truncated fetch silently produces an incomplete gitLog rather than a visible error. Noted by 3c-A. | accepted | Accepted as-is: the surrounding prose already documents "fails toward the default, never toward the grant" for capture/SKILL.md; the other two callers are informational (trust table render, refine-mode advisory column), not grant-authorizing. |
| 8 | review | [LOW, test-coverage] `resolveValue`'s boolean-typed-key branch is untested (only numeric-key coercion has regression tests). Noted by 3f-A. | accepted | Accepted as-is: no current policy key is boolean-typed, so the branch is unexercised by any real caller; capturing as a note rather than adding a test for hypothetical input. |
| 9 | review | [LOW, test-coverage] SHA-prefix-matching in `isClosingCommitReverted`'s revert-trailer detection is untested against a short (abbreviated) SHA in the trailer vs. a full SHA in gitLog. Corroborated by 3f-A + 3f-B. | deferred | Candidate for a small follow-up test; not blocking since real `git log` output and this module's own SHA extraction are both full-length, so the mismatch is a defense-in-depth gap, not an observed failure mode. |
| 10 | review | [LOW, coupling] Exporting `parseGitLog` as an internal helper (for reuse across the 3 production callers) couples those callers to trust.js's internal parsing format — a future format change must update all 3 markdown files in lockstep. Noted by 3f-B. | accepted | Accepted as-is: the alternative (each caller re-implementing git-log parsing) is worse; the coupling is explicit and grep-discoverable via the shared `parseGitLog` import. |
| 11 | review | [observation, skill-worthy] `resolveValue`'s pattern (read a policy key, coerce with a typed fallback to a documented default, never throw on malformed input) is a reusable shape beyond just `trust-revert-window-days` — corroborated by 3a-A + 3a-B. Candidate for a `[skill: NEW - ...]`-tagged wrap-up Skills curation row, similar to spec 271's IL-118 precedent. | deferred | Routed to wrap-up's reflect/curation pass rather than resolved here — this ledger row exists so it isn't silently dropped before wrap-up reads it. |
