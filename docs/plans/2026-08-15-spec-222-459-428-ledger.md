# Open Items — Spec 222/459/428 (dispatch-site sweep, ADR-0008 citation fix, health-parity contract test)

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | build | Spec #222's "/journeys and /stories dispatch blocks declare [Use: Fast]" deliverable does not apply — neither skill currently issues a Task-agent fan-out dispatch (journeys runs inline; stories runs agent-browser as separate OS processes, explicitly "not a Task agent"). The contract's Template B "Used by" line naming these two skills as per-journey/per-flow dispatchers is stale against current implementation. | accepted | Verified via grep — no Task()/"Task agent" construction site in either file (confirmed twice, before and after Tasks 1-3 landed). Filing a follow-up for the contract's stale "Used by" line is out of scope for #222 (contract file is #216's, a Non-Goal here) — surfaced instead at Common Step 4.5 (Architecture Alignment). |
| 2 | build | The plan's Global Constraints dropped `skills/specify/red-team.md` from the spec's own Non-Goals list ("`skills/_shared/multi-agent-coordination.md` and `skills/specify/red-team.md` — owned by #220; excluded here") and swept it as part of Task 2. | accepted | Deliberate, correct deviation: #220 closed 2026-08-13, before this build started, and AC1 could not pass with `red-team.md`'s pre-sweep `Tier: **Standard** (Sonnet)` grammar left in place — a live, in-scope dispatch site by the time #222 actually ran, not the design-doc-coupled internals of the *different*, still-excluded file `skills/_shared/multi-agent-coordination.md`. Only the one grammar line was touched, per the plan. Flagged by the final whole-branch review as needing this ledger entry (same class as item #1) — not a defect, a missing audit trail entry. |
| 3 | build | Final whole-branch review found the sweep is not actually "repo-wide" for the model-selection vocabulary axis as the spec's Overview claimed: `skills/_shared/multi-agent-coordination.md:66,205` (owned by #220, correctly a Non-Goal here) still carry the retired `[Use: Standard model — {reason}]` grammar, and `skills/build/SKILL.md` / `build-options.md` (owned by #217/#223, correctly Non-Goals) still expose the `tier=<fast|standard|capable|frontier>` CLI token and a "Model tier override" heading for this same axis — plus four `docs/` sites outside this sweep's `find skills agents bin` scope entirely. All three owning records (#216, #217, #220, #223) are now closed, so nothing currently tracks finishing this. Also folds in two Minor findings from the same review: an intra-file grammar inconsistency in `skills/review/step3-debate-and-refutation.md` (Frontier sites keep an in-bracket reason clause the Capable site's bare-bracket form dropped) and a bespoke citation phrasing at `skills/tidy/SKILL.md:65`. | deferred | Filed as backlog record **#478** via `/claude-tweaks:capture` (routed `keep`, auto default) — scope: the two `multi-agent-coordination.md` sites, the `build/SKILL.md`/`build-options.md` `tier=` CLI-token surface, the four `docs/` sites, and the two Minor grammar-consistency notes above. Includes the observation that AC1-style regexes structurally miss the trailing-reason-before-bracket and hyphenated "model-tier" variants — any successor sweep needs a broader enumerator from the start, not a repeat of Tasks 1-3/6's after-the-fact discovery. |
| 4 | wrap-up | Reflect (Near-misses, systemic): `bin/lib/skill-audit/context-cost.js`'s 40KB per-invocation ceiling test only fails on overage — it has no early-warning threshold (e.g. flag at 90% of ceiling). `skills/init/SKILL.md` was already only 15 bytes under the 40960-byte ceiling before this build touched it at all (a prior extraction, commit referenced in the repo's own history as #333, addressed `flow/SKILL.md` and `dispatch/SKILL.md` for the same reason but left `init/SKILL.md` at the edge). This build's own additions then pushed it over, caught only because the test happened to run before commit — a build that skipped `npm test` before committing would have shipped a broken ceiling silently until the next full suite run. CAUSAL: systemic — the test design has no proactive warning tier, only a reactive hard-fail. | deferred | Staged for the Skills/Docs curation registry rows at this run's Wrap-Up Review Console (`/claude-tweaks:wrap-up` Phase 2) — a candidate improvement to `bin/lib/skill-audit/context-cost.js` (add a warning-tier check, e.g. `no SKILL.md exceeds 90% of the per-invocation ceiling`) is not this build's own scope to implement. |
| 5 | wrap-up | Reflect (Fresh start): if re-run, this build would front-load Task 6's centralized closing grep as a discovery pass *before* decomposing Tasks 1-3, not just as a verification pass after. Three of five implementation tasks (1, 2, 3) each independently discovered real old-grammar sites their own briefs' literal citations missed, at each task's own after-the-fact self-review cost, before Task 6 finally ran the widened check that would have caught all of them up front. | deferred | General process insight for future grep-based-acceptance-criteria sweeps (not specific to this repo's code) — noted here rather than routed to a formal skill update, since claude-tweaks has no dedicated "vocabulary sweep" skill this would attach to; worth keeping in mind next time `/claude-tweaks:specify` decomposes a similar spec. |
| 6 | build | Spec #459's acceptance criterion (correct the dead `docs/superpowers/plans/2026-08-02-dispatch-mcp-bridge.md` citation in ADR-0008's Consequences section) was already satisfied on `main` before this flow run started — commit `cea9f2ea` (pre-release whole-branch review, 2026-08-15T09:44:59+02:00) rewrote the citation to note `(deleted \`d83f0720\`)`. Wording differs from #459's literal Proposed text but the underlying criterion (no dead-path citation) is met. | accepted | No code change made for #459 — re-editing an already-resolved citation to match different literal wording would be pure churn. Verified against actual file content (`docs/decisions/0008-...md:77`) and `git log`, not assumed. Record #459 closes via this run's carrier commit same as #222/#428. |
| 7 | wrap-up | Run-level scaffold gap: the multi-spec parent run directory (`.claude-tweaks/pipelines/2026-08-15T100542-spec-222-459-428/`) held only its three `spec-{N}/` subdirectories — `config.yml`, `manifest.yml`, top-level `decisions.md`, and `staged/` (per `flow/multi-spec.md`'s documented Run Directory Layout) were never created at run start. Repaired retroactively during spec #222's Phase 4 (manifest.yml reconstructed from current state; config.yml's original Manifesto answers were not reconstructed, to avoid fabricating values — per-spec `decisions.md` files remain the source of truth for what was actually decided). | deferred | Worth a follow-up capture after this run closes (likely cause: the parent-scaffold creation step early in `/flow`'s multi-spec path was skipped or mis-targeted) — not filed as a new backlog record mid-run, to stay in scope. |
