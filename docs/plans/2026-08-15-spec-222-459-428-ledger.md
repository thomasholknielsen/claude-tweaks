# Open Items — Spec 222/459/428 (dispatch-site sweep, ADR-0008 citation fix, health-parity contract test)

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | build | Spec #222's "/journeys and /stories dispatch blocks declare [Use: Fast]" deliverable does not apply — neither skill currently issues a Task-agent fan-out dispatch (journeys runs inline; stories runs agent-browser as separate OS processes, explicitly "not a Task agent"). The contract's Template B "Used by" line naming these two skills as per-journey/per-flow dispatchers is stale against current implementation. | accepted | Verified via grep — no Task()/"Task agent" construction site in either file (confirmed twice, before and after Tasks 1-3 landed). Filing a follow-up for the contract's stale "Used by" line is out of scope for #222 (contract file is #216's, a Non-Goal here) — surfaced instead at Common Step 4.5 (Architecture Alignment). |
| 2 | build | The plan's Global Constraints dropped `skills/specify/red-team.md` from the spec's own Non-Goals list ("`skills/_shared/multi-agent-coordination.md` and `skills/specify/red-team.md` — owned by #220; excluded here") and swept it as part of Task 2. | accepted | Deliberate, correct deviation: #220 closed 2026-08-13, before this build started, and AC1 could not pass with `red-team.md`'s pre-sweep `Tier: **Standard** (Sonnet)` grammar left in place — a live, in-scope dispatch site by the time #222 actually ran, not the design-doc-coupled internals of the *different*, still-excluded file `skills/_shared/multi-agent-coordination.md`. Only the one grammar line was touched, per the plan. Flagged by the final whole-branch review as needing this ledger entry (same class as item #1) — not a defect, a missing audit trail entry. |
| 3 | build | Final whole-branch review found the sweep is not actually "repo-wide" for the model-selection vocabulary axis as the spec's Overview claimed: `skills/_shared/multi-agent-coordination.md:66,205` (owned by #220, correctly a Non-Goal here) still carry the retired `[Use: Standard model — {reason}]` grammar, and `skills/build/SKILL.md` / `build-options.md` (owned by #217/#223, correctly Non-Goals) still expose the `tier=<fast|standard|capable|frontier>` CLI token and a "Model tier override" heading for this same axis — plus four `docs/` sites outside this sweep's `find skills agents bin` scope entirely. All three owning records (#216, #217, #220, #223) are now closed, so nothing currently tracks finishing this. Also folds in two Minor findings from the same review: an intra-file grammar inconsistency in `skills/review/step3-debate-and-refutation.md` (Frontier sites keep an in-bracket reason clause the Capable site's bare-bracket form dropped) and a bespoke citation phrasing at `skills/tidy/SKILL.md:65`. | deferred | Filed as backlog record **#478** via `/claude-tweaks:capture` (routed `keep`, auto default) — scope: the two `multi-agent-coordination.md` sites, the `build/SKILL.md`/`build-options.md` `tier=` CLI-token surface, the four `docs/` sites, and the two Minor grammar-consistency notes above. Includes the observation that AC1-style regexes structurally miss the trailing-reason-before-bracket and hyphenated "model-tier" variants — any successor sweep needs a broader enumerator from the start, not a repeat of Tasks 1-3/6's after-the-fact discovery. |
