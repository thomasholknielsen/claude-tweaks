# Open Items — multi-spec run 1463, 1672, 1673, 1674 (run-lifecycle hardening)

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | test | Batch pre-flight verify sweep against the unmodified base (`2d59491f9`): `npm test` — 6826 tests, 6826 pass, 0 fail. No pre-existing failures for any spec to cite. | observation | Baseline clean; recorded so a later per-spec failure is known to be introduced by this run, not inherited. |
| 2 | review/hindsight | #1463: the plan's discrimination-control test specified a commit on the feature branch but never a merge into the integration branch, so `merge-base --is-ancestor` would have been false and the test would have asserted `shipped-unclosed` against a `cherry`-or-`false` verdict. The implementer caught it and added the merge. | observation | Lesson recorded, no code change owed: a plan asserting a git-state precondition must spell out every command that establishes that state, not only the one the assertion reads. Fed to the batch reflect pass at end-of-run. |
| 3 | review | #1463: `mergedEvidence()`'s two pre-existing git calls (`merge-base --is-ancestor`, `cherry`) pass refs as bare positionals with no `--end-of-options` separator — the same gap fixed on the new call site in `548ae56fa`. | deferred | Out of #1463's scope: this diff never touched those two lines, and fixing them here would widen the record against the surgical-changes rule. Staged as a spec-shaped backlog-record proposal at `spec-1463/staged/backlog-endofoptions-1463.md` for the consolidated Review Console, which owns work-record creation. Not exploitable today — refs come from `git worktree list --porcelain` and git refuses `-`-leading ref names. |
| 4 | build | #1672: the record's Current State asserted a `record-pr` stamp already carried a branch. It did not — `hooks.js` wrote `{number, url}` only — so source 1 of the specified fallback would have been inert. | fixed | Premise corrected rather than built on: `record-pr` now derives and records the branch at stamp time, while the worktree is still live. Scope extension to `plugin/bin/hooks.js` (not in the record's Key Files) accepted under `scope-creep: add-to-plan`. |
| 5 | test | #1672: `npm test` went red at `tests/pr-early-run-lifecycle.test.js` — an unrelated suite byte-pins the exact `record-pr` source line this build changed. Targeted suites were green throughout, so the implementer never saw it. | fixed | Pin re-anchored on the property it is named for (run-state reaches disk only through the sanctioned `writeRunState`) instead of the byte literal, and proved still-discriminating against two mutants. Process lesson: in this repo a targeted-green branch can still be full-suite red, because suites pin source prose repo-wide. |
| 6 | review | #1672: lens 3f's reproduction pair disagreed on every row — agent B returned four findings (one `critical`) having made zero tool calls, citing an assertion API this repo does not use. | acknowledged | All agent-B rows discarded as fabricated, not merely unreproduced; verified by grep that the cited API appears nowhere in either named test file. No code change owed. Recorded because it is direct evidence for the reproduction-pair mechanism: a single-agent dispatch would have surfaced a baseless `critical`. Routed to the end-of-run batch curation pass. |
| 7 | review | Repeated across both specs: several dispatched lens agents put narration before the required first-line status word (the `#606` violation shape). | acknowledged | Recorded, not re-prompted — every such reply carried a complete, unambiguous findings table, so a re-dispatch would return identical content. Logged as a harness signal rather than absorbed silently, since it now spans 2 specs and 10+ dispatches in one run. |
| 8 | review | #1673: the plan I authored specified `closeRunState` **then** `archiveRunDir`, violating the moves-first/close-last invariant documented at `archive-merged.js:185` — an archive failure after a successful close leaves the run `clean` but unmoved, which `iterRunDirsWithState` skips forever. | fixed | Inverted to moves-first, close-last in `20fc05c33`; `closeRunState` now runs against the archived dir so the `close-without-wrapup` event still lands. Regression test proven to go red against the old ordering. Caught by four independent agents across two lenses. Plan-authoring lesson: the same plan's constraints cited that exact blind spot as the reason to archive — I named the hazard and then sequenced into it. |
| 9 | review | #1673: `archiveMerged`'s ownership gate is inert in the path that runs it — the archive check runs only in the detached `reconcile-background` child, which has no hook payload and no reliable `CLAUDE_CODE_SESSION_ID`. | deferred | Half fixed in `20fc05c33` (`reconcile(opts)` now threads `sessionId`; `session-start.js` passes its real payload id). The residual half needs a session id threaded into a detached child's environment — a change to `#820`'s fast/background split contract, not to this record's criterion. Staged as `spec-1673/staged/backlog-bg-sessionid-1673.md`. Not an open hole: auto-close still requires `interrupted` + 24h no self-attributed activity + `shipped-unclosed` evidence. |
| 10 | test | #1674: the record's AC3 (return `false` when no integration branch resolves) turned 20 pre-existing tests in `hooks-bookkeeping-stamps-gate.test.js` red — `false` means *the gate is not armed*, so following AC3 literally disabled IL-131 for every no-remote repo. | fixed | HARD-GATE hit and resolved. Shipped the pre-`#1674` unbounded walk as the fallback instead — a deliberate, logged deviation from the record's own acceptance criterion. Decisive evidence: the fallback needs zero pre-existing tests edited; literal AC3 would need twenty. A design requiring 20 existing tests to be rewritten to accommodate it is the wrong design. |
| 11 | review | #1674: my justification for that fallback ("the false positive cannot arise where no integration branch exists") was wrong — a no-remote repo has a real local integration branch that is merely *unresolvable*. | fixed | Refuted by a lens-3c agent's reproduction, which is the most valuable single result of this run's reviews. Behavior stands (still least-bad, still non-regressive); the code comment and both docs now state that the false positive persists for those repos instead of claiming it cannot occur. Real fix staged as `spec-1674/staged/backlog-integration-resolution-1674.md`. Lesson: "unresolvable" ≠ "nonexistent". |
| 12 | review | #1674: a stale/mistyped `integration-branch:` policy value is truthy but unresolvable — `git log {bogus}..HEAD` exits 128, and the blanket failure branch disarmed the gate on a config typo. | fixed | Reproduced independently before acting (exit 128, `fatal: bad revision`). Both unusable-bound cases now fall back to the unbounded walk. |
| 13 | review | Process: a dispatched lens agent ran mutation probes against the **live** worktree file rather than a scratch copy, while I was editing the same file. | acknowledged | No contamination — verified against `git show HEAD`, not the working tree, and the agent did restore. Root cause is my own dispatch contract: implementer prompts said "scratch backup outside the repo", lens prompts said nothing. Routed to batch curation as a dispatch-contract gap. Also a reminder that an agent's "file is clean, restored" is a claim, not evidence. |
| 14 | test | #1674: one of my own new tests passed for the wrong reason — it wrote `policy.yml` into the main checkout, which the worktree never reads, so `integration` resolved `null` (the case already covered) instead of the bogus name under test. | fixed | Caught only because I mutation-probed the test rather than trusting a green assertion. Fixture corrected to write into the worktree; re-probed and it now goes red without the fix. |
