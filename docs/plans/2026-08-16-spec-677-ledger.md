# Open Items — #677 Rename framing:baked → solution:unjustified

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | build/ops | Sibling live runs touch files this spec also edits — #654/#655 (`bin/lib/issues/backlog.js`, `docs/getting-started.md`, `docs/skill-graph.md`, `skills/backlog/refine-mode.md`), #646 (`skills/capture/SKILL.md`, `skills/feedback/SKILL.md`, `skills/challenge/SKILL.md`) — expect a merge-time re-sync of `main` before the final review/merge (reason-not-auto: concurrent-run-ordering — which lands first is not this run's to decide) | fixed | Merged `origin/main` (#634 + #669 landed) into the branch before the final review — merge commit `b9f9279b`; two conflicts (`bin/lib/issues/record.js` deferReason vs. rename, `skills/backlog/SKILL.md` Next Actions rewrite vs. launcher rename) resolved keeping both sides; full suite 3957/3957 |
| 2 | build/skill | Third facet/label rename in this repo following the same recipe (effort→size, family:parent→parent-issue, now framing:baked→solution:unjustified): new LABELS constant + OR-parse read fallback in record.js, held-aside legacy line in local-store.js parseFrontmatterLines, sharedFacetDefaults key swap, LABELS_JSON row swap + LABEL_BOOTSTRAP_VERSION bump, live-repo migrate-then-delete, permanent [IL-85] comments — no project skill covers it [skill: NEW - work-record-facet-rename] | observation | Recipe now written down three times in commit history; a skill would make the fourth mechanical |
| 3 | review/hindsight | Missing consolidation: parseFrontmatterLines carries three copies of the held-aside legacy-line idiom (effort, family-parent, framing) — extract a helper only when a fourth arrives (staged/reflect-1.md) | observation | Deliberately explicit per the [IL-85] fallback convention; three ~4-line copies is below the threshold where a helper pays for itself |
| 4 | review/hindsight | Tangential: /challenge lacks the bare-#N evidence-or-accept-risk mode #471 designed; #677 ships the --lens=1 #N proxy launcher (staged/reflect-2.md, backlog candidate — Queue writes at the Review Console) | deferred | Origin: #677 spec Gotchas + hindsight; files: skills/challenge/SKILL.md, skills/backlog/overview-mode.md, skills/backlog/refine-lanes.md; trigger: when the needs-you lane's unjustified rows need a one-step remedy |
| 5 | wrap-up | [skill: NEW - work-record-facet-rename] Enrichment for the recipe in item 2 — two near-misses (both systemic, causal chain: no written recipe exists after three renames): (a) the emit side needs a rejection guard for the retired parameter, mirroring the effort guard — the plan carried the read fallbacks but not this, the final review caught it; (b) the vocabulary sweep must re-run after every upstream merge — the second origin/main merge landed a brand-new file (skills/backlog/refine-lanes.md) carrying the old spelling that no plan file list could have named | observation | Folded into the new-skill candidate; nothing to change in this build |
| 6 | wrap-up | [route: D4] Live GitHub label state drifted under concurrent sessions mid-run (spec's carrier list stale, a new carrier, a pre-existing label with a stale description) — re-read carriers/label existence right before any migrate/delete (staged/reflect-3.md) | observation | Memory-bound; the Memory curation row stages the write for the Review Console |
| 7 | wrap-up | Friction: 0 AskUserQuestion stops, 0 gate denials for a 6-task record; two harness shell-shape refusals (compound command with a scratchpad redirect; Write into the shared checkout's gitignored run dir) — both already memory-documented; 8 contract-violation events (agents narrating before the status word) — a known contract weakness. Nothing new to capture | accepted | Already documented (memory: worktree-session-blocks-compound-bash, write-tool-worktree-pinning-vs-hook-exemption; _shared/subagent-output-contract.md notes the SubagentStop unreliability) |
| 8 | wrap-up | Residue: PR #694 (this run's own head branch worktree-flow-spec-677) is open — remedy record: it is closed by this wrap-up's own pr-first merge at Phase 4 execution, not by Phase 1 | observation | Resolved by design at Phase 4 (execution step) — the merge sha is recorded in decisions.md when it lands; no separate record needed |
| 9 | wrap-up | Residue: three merged-but-undeleted remote branches in the blast-radius scan — origin/worktree-flow+spec-654-655, origin/worktree-flow-spec-620-621-622-623-624-625, origin/worktree-flow-spec-646 (remedy auto) | accepted | Sibling runs' branches, not this run's: 654-655's worktree is still locked by a live session (pid 97003) and 620-625 / 646 report pr-open / in-use in this session's start-of-session reconcile; their own settlement and the reconciler own the delete — deleting from this run risks racing a live session's post-merge push. /claude-tweaks:tidy sweeps them if they outlive their sessions |
