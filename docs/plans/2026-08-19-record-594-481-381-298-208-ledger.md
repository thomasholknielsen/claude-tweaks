# Open Items — Bundle 594, 481, 381, 298, 208 (file-overlap dispatch group)

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | test | Pre-existing, unrelated to this bundle's diff: `tests/changelog-coverage.test.js` — "every version that shipped on the release branch has a CHANGELOG entry" and "the record accounts for every version the git walk can still see" both fail because `origin/main` has shipped `6.98.0` (and many other commits) since this worktree's branch point, with `CHANGELOG.md`/`docs/shipped-versions.tsv` not yet reflecting it. None of this bundle's 5 commits touch either file (confirmed via `git log --stat` scoped to those paths) — the branch is simply far behind `origin/main` (189 commits, re-confirmed by second call). | accepted | Defer-reason: pre-existing-outside-diff — fails fix-now (merging 189 commits spans unrelated systems / materially expands scope); does not clear the autonomy floor per deferral-gate.md's floor-mapping table, so no auto-route despite `autonomy: unattended`. No AskUserQuestion channel available in this headless dispatch. Accepted per repo precedent — record #845 resolved an identical-shaped item the same way (commit 12b15ba1). |
