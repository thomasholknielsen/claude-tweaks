# Open Items — flow run #1296, #1336

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | build | #1296 AC1's literal `grep -rn "Reversibility: med" plugin/skills/` also matches two pre-existing, out-of-scope lines: `tidy/step-6-auto.md`'s illustrative "Log entries" example (a format demonstration, not a live policy assertion) and `flow/multispec-freshness.md`'s merge-catchup log line (a different subsystem's own reversibility call, not named in #1296's Deliverables/Key Files). #1296's own target — capture's headless absorb reversibility contradiction — is fully reclassified to `high` in `capture/routing.md`. | accepted | Out of scope for this record — neither file is named in #1296's Deliverables or Key Files; fixing them would be undirected scope creep into two unrelated subsystems' own reversibility judgments. Noted for a future sweep if the reviewer wants the grep to be repo-wide clean. |
