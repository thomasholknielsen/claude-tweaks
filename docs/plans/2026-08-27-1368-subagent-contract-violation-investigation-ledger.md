# Open Items — Investigate subagent contract-line violation observed during #1259's build dispatch

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | build | Identify the dispatch site behind #1259's `contract-violation` event (`plugin/bin/lib/hooks/subagent-stop.js:50`, `firstLine: "Still waiting on the final whole-branch review agent (retry attempt 3) to complete."`) and whether it needs a claude-tweaks-owned fix or is third-party-exempt. | accepted | No fix needed — identified as `/superpowers:subagent-driven-development`'s own final whole-branch review dispatch (invoked from `plugin/skills/build/SKILL.md`'s "Whole-branch review model" step), already documented there as "a third-party dispatch, not one of `_shared/subagent-output-contract.md`'s enumerated Frontier singleton slots" — exempt per that contract's third-party exemption. The captured text is a polling message from a nested retry loop, matching `subagent-stop.js`'s own documented best-effort caveat (SubagentStop fires unreliably, claude-code#27755) rather than a genuine violation. Posted as issue comment: https://github.com/thomasholknielsen/claude-tweaks/issues/1368#issuecomment-5435010296 |
