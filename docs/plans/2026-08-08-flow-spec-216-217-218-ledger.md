# Open Items — Model profile strategy run (specs 216, 217, 218)

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | review/hindsight | Two hand-rolled policy.yml readers (policy-schema.js flat, policy-fragment.js nested) — consolidation candidate when #219 opens policy-schema.js | deferred | Reviewed at consolidated console (2026-08-08), approved as informational. Backing staged/reflect-1.md was lost when the worktree was removed without running the gitignored-scratch transitional guard — content already read and dispositioned before loss; no action pending. |
| 2 | review/hindsight | Mutation-probe discipline: 4/7 implementers independently caught non-discriminating planned tests via scratch-copy probes [skill: subagent-output-contract] | deferred | Reviewed at consolidated console (2026-08-08); no skill edit made this run. Backing staged/reflect-2.md lost with the run dir (see item 1's note) — content already read and dispositioned before loss. |
| 3 | ops | Execute the live label renames (gh label edit effort:{low,medium,high} --name size:{...} + descriptions) at the consolidated branch-finish, after merge | **fixed** | Done 2026-08-09: `size:low`/`size:medium`/`size:high` now live on thomasholknielsen/claude-tweaks with the stated descriptions, verified via `gh label list --search "size:"`. |
| 4 | ops | Add the CHANGELOG stale-installed-version note for the size rename at release time, per the Releasing convention | open | Still genuinely open. v6.73.0 already shipped (by another concurrent session) before this run's live label rename executed, so no release captured it. Rides whichever version bump next touches CHANGELOG.md — not forced here per the Releasing convention (a bump is a separate, deliberate action). |
| 5 | review/hindsight | Controller-side pre-dispatch verification discipline: pre-run a task's own planned acceptance/verification command against live repo state before dispatching it, catching plan gaps ahead of the implementer — reinforces item 2. [route: D5] | deferred | Classified D5 at the console (2026-08-08) — targets `superpowers:subagent-driven-development`, a third-party plugin, which is out of `/claude-tweaks:feedback`'s scope (self-file check). Not filed anywhere; reported to the user instead per the D5 contract. Drafted gap-report body (spec-217/staged/reflect-1.md) was lost with the run dir, but its content is reconstructable from this session's transcript if the user wants it filed manually against superpowers. |
| 6 | review | Architecture: work-record.md doesn't document the effort/size boundary; finding.effort (code-health judge schema) is the same concept as size under a retained name, not a distinct meaning | **fixed** | Addressed 2026-08-08 as part of the Multi-Spec Review Console approvals (commit a93b4b03): added a paragraph to skills/_shared/work-record.md naming the three surviving "effort" meanings (record facet `size`; code-health's `finding.effort`, deliberately unrenamed; `review-effort`/`EFFORT_SCALE`). Backing staged/review-1.md lost with the run dir — no longer needed, change already landed. |
| 7 | review | Test quality: evals/fixtures/git-fixtures.js's seedLocalWorkRecord accepts and silently writes any facet key/value with no validation — the mechanism behind this spec's own eval-fixture regression | **fixed** | Addressed 2026-08-08 as part of the Multi-Spec Review Console approvals (commit a93b4b03): added `TIER_FACET_KEYS`/`assertKnownFacets` validation to evals/fixtures/git-fixtures.js, plus 3 new tests in evals/tests/fixtures.test.js (RED→GREEN verified). Backing staged/review-2.md lost with the run dir — no longer needed, change already landed. |
| 8 | review/hindsight | Pattern observation: tests/routine-record-freshness.test.js's two new tests use two different fixture-isolation strategies (dedicated scoped remote vs. shared fixture) chosen correctly by blast radius, but the rule isn't written down anywhere as a general convention | accepted | Reviewed at consolidated console (2026-08-08), accepted as a documented pattern without a new codified rule this run. Backing spec-218/staged/reflect-1.md lost with the run dir — content already read and dispositioned before loss. |
| 9 | wrap-up | Near-miss: perf/statusline-render.test.js's model fixture sits outside npm test's coverage (deliberate, separate test:perf run) — a future model rename could miss it again unless it's cross-referenced from tests/statusline.test.js | **fixed** | Addressed 2026-08-08 as part of the Multi-Spec Review Console approvals (commit a93b4b03): added cross-reference comments in both perf/statusline-render.test.js and tests/statusline.test.js pointing to each other. Backing spec-218/staged/reflect-2.md lost with the run dir — no longer needed, change already landed. |

**Note on staged/*.md losses (2026-08-08):** the run directory `.claude-tweaks/pipelines/2026-08-08T163319-spec-216-217-218/` (this ledger's own run) had its gitignored `config.yml`, `decisions.md`, `manifest.yml`, and every `staged/*.md` proposal file permanently deleted when the worktree was torn down via `ExitWorktree`'s `discard_changes: true` without first running `wrap-up/cleanup-procedures.md` Section C step 3.5's transitional guard (copy gitignored run content out before worktree removal). All resolutions above were determined and acted on *before* the loss — nothing was decided blind — but the source documents themselves no longer exist on disk. This run has since been closed via `bin/hooks.js close-run` so it no longer surfaces as an unresolved pipeline run.
