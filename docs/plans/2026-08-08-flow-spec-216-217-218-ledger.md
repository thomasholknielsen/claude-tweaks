# Open Items — Model profile strategy run (specs 216, 217, 218)

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | review/hindsight | Two hand-rolled policy.yml readers (policy-schema.js flat, policy-fragment.js nested) — consolidation candidate when #219 opens policy-schema.js | deferred | staged/reflect-1.md, surfaces at consolidated console |
| 2 | review/hindsight | Mutation-probe discipline: 4/7 implementers independently caught non-discriminating planned tests via scratch-copy probes [skill: subagent-output-contract] | deferred | staged/reflect-2.md, wrap-up Step 7 evaluates placement |
| 3 | ops | Execute the live label renames (gh label edit effort:{low,medium,high} --name size:{...} + descriptions) at the consolidated branch-finish, after merge — deferred from #217's build so main's readers aren't broken mid-run (reason-not-auto: requires-signoff — timing gated on the merge itself) | open | — |
| 4 | ops | Add the CHANGELOG stale-installed-version note for the size rename at release time, per the Releasing convention (reason-not-auto: requires-signoff — rides the version bump) | open | — |
| 5 | review/hindsight | Controller-side pre-dispatch verification discipline: pre-run a task's own planned acceptance/verification command against live repo state before dispatching it, catching plan gaps ahead of the implementer — reinforces item 2. [route: D5] | deferred | spec-217/staged/reflect-1.md, targets superpowers:subagent-driven-development |
| 6 | review | Architecture: work-record.md doesn't document the effort/size boundary; finding.effort (code-health judge schema) is the same concept as size under a retained name, not a distinct meaning | deferred | spec-217/staged/review-1.md, surfaces at consolidated console |
| 7 | review | Test quality: evals/fixtures/git-fixtures.js's seedLocalWorkRecord accepts and silently writes any facet key/value with no validation — the mechanism behind this spec's own eval-fixture regression | deferred | spec-217/staged/review-2.md, surfaces at consolidated console |
| 8 | review/hindsight | Pattern observation: tests/routine-record-freshness.test.js's two new tests use two different fixture-isolation strategies (dedicated scoped remote vs. shared fixture) chosen correctly by blast radius, but the rule isn't written down anywhere as a general convention | accepted | spec-218/staged/reflect-1.md, surfaces at consolidated console |
| 9 | wrap-up | Near-miss: perf/statusline-render.test.js's model fixture sits outside npm test's coverage (deliberate, separate test:perf run) — a future model rename could miss it again unless it's cross-referenced from tests/statusline.test.js | deferred | spec-218/staged/reflect-2.md, surfaces at consolidated console |
