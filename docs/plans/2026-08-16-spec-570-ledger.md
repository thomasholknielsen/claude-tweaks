# Open Items — #570: tidy routing: reconcile-converge merged remote-branch deletes; add a routing row for Mark-as-specified

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | build/skill | remote-prune extends the reconcile family with its first pushed mutation — the pattern for outward writes is now: two independent evidence signals (MERGED PR + cherry-equivalence) AND a fetch-prune-first fail-closed freshness precondition | observation | Documented in prune-remote.js header, archive-branches.js header, and docs/plugin-structure.md |
| 2 | build | Spec AC-2 wording (no open PR) contradicted Deliverable 1 (MERGED PR required); implementation shipped the stricter bar and the materialized spec was aligned at Step 4.5 | accepted | Stricter reading is the safe one for a pushed deletion; consequence noted for wrap-up: branches merged without a PR never converge via this check and keep staging in tidy |
| 3 | review | Supplementary delta review (commits 9df908b8, ef39a610, b49ecc43, 8e1fb365 vs cited whole-branch review at 8d0c97d8): commit 8e1fb365 omits `refs #570` unlike its sibling commits in this chain | accepted | Docs-only commit already pushed to origin; rewriting history for a trailing-reference nit is not worth it. All prose claims verified accurate against shipped code. |
| 4 | review/hindsight | Implementation Hindsight (5 evaluations): approach correct, no structural debt regret, no consolidation opportunity, no convention drift, skill-worthy pattern already captured at code-doc altitude (module header + plugin-structure.md + hooks.md, not a new skill file) | accepted | No changes needed — approach is sound |
| 5 | wrap-up | Full-mode reflect: Surprises (stale-cache bug + refname:short git quirk) already captured in code/docs, no new routing | accepted | Already documented in prune-remote.js header + ledger item 1 |
| 6 | wrap-up | Near-misses: two plan-authored tests initially failed to discriminate their target guard, both caught by review and fixed with revert-verified discrimination — causal chain bottoms at a third-party (superpowers writing-plans) self-review gap | accepted | Third-party gap; personal practice of verify-by-reverting already caught both instances in this build |
| 7 | wrap-up | Fresh start: extract prune-remote.js fetch-precondition pattern to a shared helper when a second reconcile check needs it | deferred | Staged (staged/reflect-1.md) — premature with one caller |
| 8 | wrap-up | Friction lens: 2 contract-violation events (subagent status-line drift), rest is bystander/silent noise | accepted | Not avoidable — dispatch prompts already carried the explicit contract |
| 9 | wrap-up | Tradeoff review: stricter MERGED-PR bar vs looser spec AC-2 wording | accepted | Already resolved at build Architecture Alignment — one-off, not a pattern |
