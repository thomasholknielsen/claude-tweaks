# Open Items — 418: Cut the plugin payload over to plugin/ with a git-subdir marketplace source (single release)

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | ops (reason-not-auto: no-cli) | Rotate the Claude Code credential: the Task 1 probe subagent ran `security find-generic-password -g` and printed live OAuth access+refresh tokens into its local transcript file (`/private/tmp/claude-501/.../tasks/af92218f9b31c0b18.output`). Not written elsewhere, not reused — but rotate (sign out/in) to be safe. | open | — |
| 2 | build/skill | Worktree PreToolUse guard blocks git commits in OUT-of-worktree scratch repos too (command-text matching) — Task 1 had to wrap `git commit` for a scratchpad probe repo in a `cd`+`exec` script. Candidate /claude-tweaks:feedback item (guard over-matching). | observation | Noted for wrap-up skills-curation row |
| 3 | build | plugin/bin/lib/issues/initiative-budget.js contains a non-UTF8/NUL byte — git diffs it as binary, plain grep silently skips it (known IL-class hazard). Unowned by the cutover; needs a backlog record (strip the byte + a guard test). | open | — |
| 4 | build | scripts/claude-cloud-setup.sh:90-92 drift check goes permanently false once the catalog entry drops its version field (Task 6 mirror change). Mechanism: it is `expected` that degrades — `(declared && declared.version) || "unversioned"` yields `"unversioned"`, which switches off the `expected !== "unversioned" && installed !== expected` half of the disjunct, leaving `installed === "none"` as the only surviving trigger. `installed` is NOT the degrading side: it reads the install-path manifest and stays correct — so a genuine version drift on a plugin that IS installed is no longer detectable at all. File is /init-generated (Step 14) — needs a follow-up record: teach the generated check a sha-based comparison, then regenerate. | open | — |
| 5 | ops (reason-not-auto: auth-not-configured) | Task 9 could not delete the Probe 1 scratch GitHub repo `thomasholknielsen/ct-subdir-probe` (private, 6 branches): the `gh` token lacks the `delete_repo` scope (`HTTP 403: Must have admin rights to Repository`, four attempts; an initial run returned a transient `HTTP 503` that masked the real error). Granting the scope is an interactive credential change, out of an agent's bounds. Every other scratch artifact is gone and verified. Run:<br>`gh auth refresh -h github.com -s delete_repo`<br>`gh repo delete thomasholknielsen/ct-subdir-probe --yes` | open | — |
| 6 | build/skill | Probe 1 §9's "the probe marketplace was never registered globally" claim was wrong on this machine: it grepped `~/.claude/plugins/known_marketplaces.json` and read 0, but the live file is `$CLAUDE_CONFIG_DIR/plugins/known_marketplaces.json` (`~/.claude-accounts/lipht-thn/...`) — only `plugins/cache` is symlinked between the two, not `plugins/` itself. `probe-marketplace` was registered there and survived Probe 1's own verification. Task 9 removed it (`claude plugin marketplace remove probe-marketplace`, verified 0 hits). Lesson for any future probe: resolve config-dir paths through `CLAUDE_CONFIG_DIR`, never assume `~/.claude`. | observation | Removed at Task 9 cleanup; noted for wrap-up skills-curation row |
| 7 | ops (reason-not-auto: requires-signoff) | Release + catalog flip — before merging PR #793, re-merge `origin/main` and run the full `npm test` (three sibling releases landed during this run; byte-pinning tests can go red only at the merge combination). Then, after merging PR #793, from the MAIN checkout on clean main: `git pull`, then `node plugin/bin/release.js minor "Cut plugin payload over to plugin/ with git-subdir marketplace source"`; the mirror step flips the catalog entry (git-subdir, sha-pinned) in the same run. Then verify a fresh scratch install from the live marketplace works (spec AC; procedure mirrors `.claude-tweaks/pipelines/2026-08-17T150123-spec-418/work/cutover-verification.md` AC 2 without the temp entry). Attended by design (spec Gotcha: no auto:build; worktree session structurally cannot run a release). Companion: #419 lands with or immediately after, never before. | open | — |
| 8 | review/hindsight | Extend #419 with two silent-degradation check classes (repo-root runnable text inside plugin/**; unresolvable journey frontmatter) — staged/reflect-1.md | deferred | Staged for Review Console (comment on #419) |
| 9 | review/hindsight | reason-not-auto closed-vocabulary check before dictating qualifiers [route: D4] — staged/reflect-2.md | deferred | Staged for Review Console (memory update) |
| 10 | review/hindsight | Two-class sweep grep lesson for directory moves — incident-log + donts candidate — staged/reflect-3.md | deferred | Staged for Review Console (doc row) |
