# Open Items — 418: Cut the plugin payload over to plugin/ with a git-subdir marketplace source (single release)

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | ops (reason-not-auto: requires-human) | Rotate the Claude Code credential: the Task 1 probe subagent ran `security find-generic-password -g` and printed live OAuth access+refresh tokens into its local transcript file (`/private/tmp/claude-501/.../tasks/af92218f9b31c0b18.output`). Not written elsewhere, not reused — but rotate (sign out/in) to be safe. | open | — |
| 2 | build/skill | Worktree PreToolUse guard blocks git commits in OUT-of-worktree scratch repos too (command-text matching) — Task 1 had to wrap `git commit` for a scratchpad probe repo in a `cd`+`exec` script. Candidate /claude-tweaks:feedback item (guard over-matching). | observation | Noted for wrap-up skills-curation row |
| 3 | build | plugin/bin/lib/issues/initiative-budget.js contains a non-UTF8/NUL byte — git diffs it as binary, plain grep silently skips it (known IL-class hazard). Unowned by the cutover; needs a backlog record (strip the byte + a guard test). | open | — |
| 4 | build | scripts/claude-cloud-setup.sh:91 drift check degrades to installed==="none" once the catalog entry drops its version field (Task 6 mirror change). File is /init-generated (Step 14) — needs a follow-up record: teach the generated check a sha-based comparison, then regenerate. | open | — |
| 5 | ops (reason-not-auto: requires-human) | Task 9 could not delete the Probe 1 scratch GitHub repo `thomasholknielsen/ct-subdir-probe` (private, 6 branches): the `gh` token lacks the `delete_repo` scope (`HTTP 403: Must have admin rights to Repository`, four attempts; an initial run returned a transient `HTTP 503` that masked the real error). Granting the scope is an interactive credential change, out of an agent's bounds. Every other scratch artifact is gone and verified. Run:<br>`gh auth refresh -h github.com -s delete_repo`<br>`gh repo delete thomasholknielsen/ct-subdir-probe --yes` | open | — |
| 6 | build/skill | Probe 1 §9's "the probe marketplace was never registered globally" claim was wrong on this machine: it grepped `~/.claude/plugins/known_marketplaces.json` and read 0, but the live file is `$CLAUDE_CONFIG_DIR/plugins/known_marketplaces.json` (`~/.claude-accounts/lipht-thn/...`) — only `plugins/cache` is symlinked between the two, not `plugins/` itself. `probe-marketplace` was registered there and survived Probe 1's own verification. Task 9 removed it (`claude plugin marketplace remove probe-marketplace`, verified 0 hits). Lesson for any future probe: resolve config-dir paths through `CLAUDE_CONFIG_DIR`, never assume `~/.claude`. | observation | Removed at Task 9 cleanup; noted for wrap-up skills-curation row |
