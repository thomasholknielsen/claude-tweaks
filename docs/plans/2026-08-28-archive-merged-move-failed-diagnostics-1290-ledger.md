# Open Items — reconcile: move-failed stuck on an already-partially-archived, long-idle multi-spec run dir (#1290)

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | review | Build implemented Task 1 of its own 5-task plan only; Deliverable 1 (OS error capture at every `move-failed` catch site) and Task 5 (finish archiving the real stuck run dir this issue names) were speced but never executed | resolved | fixed inline — commits `bae231d0e` (formatErr/detail threading, +2 tests) and `62991ca23` (plan doc). Task 5's fs-only archival move performed directly against the main checkout; verified `.claude-tweaks/pipelines/2026-08-09T122833-spec-271-267` fully archived, no entries remain outside `archive/`. |
| 2 | review | `archiveMerged`'s skip vocabulary only escalates `move-failed`; a run dir with an archive twin and no resolvable worktree/branch (the exact shape #1290's own subject directory was) — or, more generally, any dir `definitelyAbsent()`'s fail-safe design permanently keeps out of the orphan-mint TTL sweep — is now silently skipped forever (`no-worktree`/`no-branch`) with zero automated visibility | deferred | → backlog #1613 (defer-reason: genuinely-larger — the fix requires new escalation/staleness-threshold design, not a mechanical change; the deliberate "leave in place, never auto-resume" test must keep passing unmodified) |
| 3 | review | PR #1612 (this work's own draft PR, head `worktree-dispatch-record-1290`) — `assess-agent-autonomy merge-check` verdict `needs-human` (item 2's Medium-severity finding is a hard input to that check, per `skills/assess-agent-autonomy/merge-check.md`); left open pending human merge decision | deferred | → left open, no backlog record needed — resolves on human review/merge of #1612, not a separate work item |
