# Open Items — spec 674 + 675 (staged-patch validation + description fallback; curation-judge stagePath verification + shadow sweep)

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | review | #674 low: staged-patch.md partial preamble (Target: or Invariant: missing) unclassified — routed to step 4 (lens 3c, reproduced) | fixed | Auto-applied per review-auto-apply-ceiling: low; commit 665d69d7 |
| 2 | review | #674 high: staged-patch.md:57 fenced gate command (and console fast path) lack `-C "$WORKTREE"` — validates against wrong tree silently (3b/3c, direct-verified) — patch staged at spec-674/staged/review-1.patch | fixed | Applied via commit 3c0e8487 (`git -C "$WORKTREE" apply --check`) |
| 3 | review | #674 high: staged-patch.md:92-96 re-derived edits auto-apply with no human approval under unattended+consoleAutoResolve; needs an unattended floor routing stale diffs to step 4 (3b, direct-verified) — spec-674/staged/review-2.patch | fixed | Applied via commit 3c0e8487 (unattended floor routing stale diffs to step 4) |
| 4 | review | #674 high: staged-patch.md:62 tooling-error branch keyed on `No such file` stderr — a hunk naming a missing target prints the same words (composition error); classify by `test -r "$STAGE_PATH"` instead (3c, empirically probed) — spec-674/staged/review-3.patch | fixed | Applied via commit 3c0e8487 (`test -r "$STAGE_PATH"` classification) |
| 5 | review | #674 medium: staged-patch.md Target: has no containment check — absolute/`..` values must route to step 4 before opening (3b both agents) — spec-674/staged/review-4.patch | fixed | Applied via commit 3c0e8487 (Target: containment check) |
| 6 | review | #674 medium: staged-patch.md log templates at :98 and :108 omit `Reversibility:`/status prefix per auto-decision-log schema (3c, reproduced) — spec-674/staged/review-5.patch | fixed | Applied via commit 3c0e8487 (re-derived from Invariant: at console apply — the diff had gone stale under the earlier-applied patches; Reversibility fields added) |
| 7 | review | #674 medium: staged-patch.md multi-`Target:` artifacts have one `Invariant:` sentence and a singular step 2 (3c, reproduced) — one Invariant line per Target, pair in order — spec-674/staged/review-6.patch | fixed | Applied via commit 3c0e8487 (re-derived from Invariant: — one Invariant line per Target, paired in order) |
| 8 | review | #674 (low-confidence, 3c agent A only) `Staged-at` sha unguarded in the stale-diff log's `git diff --stat` — fallback text when unresolvable — spec-674/staged/review-unconfirmed-1.patch | fixed | Applied via commit 3c0e8487 (re-derived from Invariant: — Staged-at fallback text) |
| 9 | review/docs | #674 3i: registry docs matched by `skills/**` (README, getting-started, plugin-structure, skill-graph, skill-authoring) not updated — the SKILL.md change is one prose sentence with no command surface; wrap-up docs curation to confirm none needs a line | fixed | Docs curation row confirmed and resolved: docs/REGISTRY.md's own Auto-detect cell for plugin-structure.md was the actually-broken instance of this pattern (`skills/**`/`bin/**` segment-local, matched nothing — fixed in commit c2b5e666); docs/skill-authoring.md gained the executable-snippet convention section (commit 093ab098); README/getting-started/skill-graph needed no line |
| 10 | review/skill | [route: D5] flow/manifesto.md Recommendation-defaults table (review-auto-apply-ceiling `low`) contradicts review/step3-routing.md ("Manifesto computes ceiling-conditional `medium` under unattended into config.yml"); this run wrote `low` | accepted | Self-reference collapsed (upstream = this repo) and already fixed on main: the merged v6.89.0 manifesto.md Recommendation-defaults row states the ceiling-conditional default; the disagreement was in the older installed build (6.87.1) the Manifesto was read from at run start. This run's `low` was the more conservative value — nothing to file |
| 11 | wrap-up | residue (spec 674 pass): branch — origin/worktree-flow+spec-654-655 — merged into origin/main, not deleted (remedy: auto; owning session live/locked at sweep time — its own wrap-up/tidy owns deletion) | fixed | Resolved externally — the branch was deleted (owning session/tidy) before this run's gate; residue re-sweep at spec 675 wrap-up no longer lists it |
| 12 | wrap-up | residue (spec 674 pass): branch — origin/worktree-flow-spec-620-621-622-623-624-625 — merged into origin/main, not deleted (remedy: auto; sibling run's branch) | fixed | Resolved externally — the branch was deleted (owning session/tidy) before this run's gate; residue re-sweep at spec 675 wrap-up no longer lists it |
| 13 | wrap-up | residue (spec 674 pass): branch — origin/worktree-flow-spec-646 — merged into origin/main, not deleted (remedy: auto; owning session live/locked at sweep time) | fixed | Resolved externally — the branch was deleted (owning session/tidy) before this run's gate; residue re-sweep at spec 675 wrap-up no longer lists it |
| 14 | wrap-up | residue (spec 674 pass): pr — PR #684 open, head worktree-flow-spec-674-675 (remedy: record) — this run's own draft PR; resolves at the shared-branch finish after spec 675 | open | — |
| 15 | review | #675 high: curation-engine.md sweep — mv failure is silent (loop continues, rmdir swallowed, exit 0) (3c both agents, reproduced) — spec-675/staged/review-1.patch | fixed | Applied via commit c97711eb (loud mv/rmdir diagnostics) |
| 16 | review | #675 medium: sweep reports a headers-only shadow decisions.md as "entries appended" (3c both, reproduced) — review-1.patch | fixed | Applied via commit c97711eb (headers-only decisions.md branch) |
| 17 | review | #675 medium: "could not run" diagnostic logged STAGED with no artifact — should be AUTO + ledger item (3c A, direct-verified) — review-1.patch | fixed | Applied via commit c97711eb (AUTO + ledger item for diagnostics, not STAGED) |
| 18 | review | #675 medium: a second same-basename collision overwrites an existing .shadow-dup — needs .shadow-dup-N (3b A, direct-verified) — review-1.patch | fixed | Applied via commit c97711eb (`.shadow-dup-N` suffix) |
| 19 | review | #675 medium: shadow entries not checked to be regular files — symlink moved / symlinked decisions.md followed (3b B, direct-verified) — review-1.patch | fixed | Applied via commit c97711eb (regular-file/symlink guard) |
| 20 | review | #675 medium: no probes for the elif branch, mv failure, headers-only decisions.md, repeated collision, symlink (3c A) — five probes in review-1.patch | fixed | Applied via commit c97711eb (five new probes: elif branch, headers-only, repeated collision, symlink, mv failure) |
| 21 | review/hindsight | #675 [docs: docs/skill-authoring.md] Pattern observation: executable skill-prose snippets should be byte-pinned + live-probed from a test — no authoring convention names this yet. Staged at spec-675/staged/reflect-1.md. | fixed | Applied to docs/skill-authoring.md as the "Executable snippets in skill prose" section — commit 093ab098 |
| 22 | review/hindsight | #675 Tangential: the anchored-staged-path invariant is stated in both _shared/staged-patch.md and curation-engine.md while _shared/pipeline-run-dir.md (owner) states neither — hoist to one owner. Queue-write proposal at spec-675/staged/reflect-2.md. | deferred | New backlog record created — issue #737 (hoist the anchored-staged-path invariant into pipeline-run-dir.md) |
| 23 | review/hindsight | #675 [route: D4] Memory candidate: KEPT-PROMPT/STAGED status words misapplied twice at plan time — check auto-decision-log's Status Semantics before dictating a log line. Staged at spec-675/staged/reflect-3.md. | fixed | Memory written — auto-decision-log-status-vocabulary-check.md |
| 24 | wrap-up | [route: D4] Batch reflect surprise: `mv -n`/`cp -n` exit 0 on skip — detect collisions with an explicit `-e` test. Staged at staged/reflect-1.md (parent). | fixed | Memory written — mv-n-exits-zero-on-skip.md |
| 25 | wrap-up | Batch reflect tangential: promote the shadow sweep to a `bin/hooks.js sweep-shadow` verb (JS-testable, one implementation). Queue-write proposal at staged/reflect-2.md (parent). | deferred | New backlog record created — issue #738 (promote the shadow sweep to a `bin/hooks.js sweep-shadow` verb) |
| 26 | wrap-up | [route: D4] Batch reflect near-miss (systemic): plan self-check must run each pinned regex against each site's exact replacement text — extend memory `plan-self-check-verification`. Staged at staged/reflect-3.md (parent). | fixed | Existing memory plan-self-check-verification.md extended with a paragraph on phrase-vs-structure self-checks |
