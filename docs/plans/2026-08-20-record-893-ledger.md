# Open Items — archiveRunDir() never moves engine-state.json, leaving orphaned run-dir residue

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | build | Record #893's Deliverables/Acceptance Criteria are already satisfied by `08098fe7` (refs #902), already on `origin/main` — `archiveRunDir()` moved from a fixed file-name list to directory enumeration, which catches `engine-state.json` (and any other current/future wrap-up-engine filename) without a code change. Regression test already present: `tests/reconcile.test.js:337`. No diff was made for this build. | observation | Superseded by #902 — recommend closing #893 as already-resolved (reference #902 / `08098fe7`), not merging as new work. |
| 2 | wrap-up | [skill: build/worktree-setup.md] Reflect (light, Friction lens) found `_shared/pr-early-run-lifecycle.md`'s Run-start procedure did not execute during build/test on this run — no push, no PR, no `run-state.json`, no `decisions.md` log entry (success or documented failure) until review backfilled it by hand. Staged for Skills-row curation. | observation | Staged — see pipeline run dir `staged/reflect-1.md`; routed to the Wrap-Up Review Console's Skill updates section. |
| 3 | wrap-up | branch — `origin/worktree-dispatch-record-916` — merged into `origin/main`, not deleted (residue sweep, `--scope blast-radius`) | fixed | Deleted via `git push origin --delete` (cherry-equivalent, confirmed safe by `bin/hooks.js reconcile --checks archive-branches`). |
| 4 | wrap-up | branch — `origin/worktree-dispatch-record-921` — merged into `origin/main`, not deleted (residue sweep, `--scope blast-radius`) | fixed | Already deleted by a concurrent reconcile pass before this item's own fix attempt ran; confirmed gone via `git ls-remote`. |
| 5 | wrap-up | branch — `origin/worktree-dispatch-record-925` — merged into `origin/main`, not deleted (residue sweep, `--scope blast-radius`) | fixed | Already deleted by a concurrent reconcile pass before this item's own fix attempt ran; confirmed gone via `git ls-remote`. |
| 6 | wrap-up | branch — `origin/worktree-dispatch-record-926` — merged into `origin/main`, not deleted (residue sweep, `--scope blast-radius`) | fixed | Deleted via `git push origin --delete` (cherry-equivalent, confirmed safe by `bin/hooks.js reconcile --checks archive-branches`). |
| 7 | wrap-up | pr — PR #978 (this run's own PR, head `worktree-record-893`) — open, awaiting this run's own Phase 4 merge (residue sweep, `--scope blast-radius`) | accepted | This run's own active PR — resolved by this wrap-up's own merge in Phase 4, not separate residue. |
| 8 | wrap-up | pipeline-run — `.claude-tweaks/pipelines/2026-08-19T203337-record-208` — `run-state.json` status: clean, not archived (residue sweep, `--scope blast-radius`) | accepted | `bin/hooks.js reconcile --checks archive` skipped it (`no-worktree` — the run's worktree/branch is already gone, so `archiveRunDir()`'s PR-state resolution has nothing to key off). Manual Section B archival needs a `git mv`/commit on `main`, which this worktree-isolated session cannot safely perform. Left for `/claude-tweaks:tidy` or a main-checkout session's own periodic sweep — not this run's own residue. |
| 9 | wrap-up | pipeline-run — `.claude-tweaks/pipelines/2026-08-19T203340-record-172` — `run-state.json` status: clean, not archived (residue sweep, `--scope blast-radius`) | accepted | Same as item 8 (`no-branch`) — left for `/claude-tweaks:tidy` or a main-checkout session. |
| 10 | wrap-up | pipeline-run — `.claude-tweaks/pipelines/2026-08-19T204949-record-916` — `run-state.json` status: clean, not archived (residue sweep, `--scope blast-radius`) | accepted | Same as item 8 — left for `/claude-tweaks:tidy` or a main-checkout session. |
| 11 | wrap-up | pipeline-run — `.claude-tweaks/pipelines/2026-08-19T210228-spec-315` — `run-state.json` status: clean, not archived (residue sweep, `--scope blast-radius`) | accepted | Same as item 8 (`no-worktree`) — left for `/claude-tweaks:tidy` or a main-checkout session. |
| 12 | wrap-up | pipeline-run — `.claude-tweaks/pipelines/2026-08-19T210238-record-229` — `run-state.json` status: clean, not archived (residue sweep, `--scope blast-radius`) | accepted | Same as item 8 — left for `/claude-tweaks:tidy` or a main-checkout session. |
| 13 | wrap-up | pipeline-run — `.claude-tweaks/pipelines/2026-08-19T210303-spec-226` — `run-state.json` status: clean, not archived (residue sweep, `--scope blast-radius`) | accepted | Same as item 8 — left for `/claude-tweaks:tidy` or a main-checkout session. |
