# Open Items — Skill-invocation ledger family (#371, #372, #373)

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | flow | Pre-flight divergence: origin/main was 4 ahead (spec #366); merged into shared worktree branch before build | observation | Clean merge — #366's policy-schema.md edits now in base, resolving #373's coordination gotcha |
| 2 | build | Spec-371 deviations from spec text: isFailedCall widened to defensive success===false/is_error===true guard (Task 0 measured failed calls never reach PostToolUse); finding (e) added beyond the spec's four (user-typed slash commands leave no Skill event) | accepted | Both Beneficial — measured-reality corrections, ruled + documented in module header, task0-findings.md, and SDD ledger |
| 3 | build | Journey capture for the skill-invocation-ledger family deferred from spec-371 (no user-visible surface — silent log appends) to spec-372/373's build, where the SessionStart verdict + teardown gate give a walkable surface | fixed | docs/journeys/run-integrity-lifecycle.md — `9632b0b6` |
| 4 | build/skill | Observation: worktree-isolated session refuses compound Bash (&&, heredocs) and main-checkout writes; dispatch prompts that bake "ONE plain command per call + Write/Edit tools" prevented every such failure across 5 subagents. Candidate note for _shared/subagent-output-contract.md's Working Directory Discipline section | open | — |
| 5 | review/hindsight | Task 0's scenario list enumerated input formats (qualified/bare/failed/subagent) but missed the most common invocation *initiator* path — user-typed slash commands — caught only by the final reviewer's independent measurement. Candidate guidance for /specify's spec-template: empirical premise checks should enumerate who initiates (user-typed, model-initiated, subagent, headless), not just input shapes | open | — |
| 6 | review | Spec-372 final review minor: run-integrity's repoRootOf three-levels-up anchoring assumption is unpinned by any test — a run-dir layout change would fail silently. Candidate: one assertion tying it to the anchoring suite (tests/hooks-context-anchoring.test.js or the run-integrity suite). Origin: spec-372 whole-branch review; trigger: next change to _shared/pipeline-run-dir.md's layout | open | — |
