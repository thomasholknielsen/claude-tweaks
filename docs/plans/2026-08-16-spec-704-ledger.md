# Open Items — 704: Consistency polish for the new Next Actions close-out corpus

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | review | Next Actions blocks whose recommended line is omission-only or whose alternative lines are unbackticked: skills/backlog/SKILL.md:66-90 (refine/grant — zero-grant path renders 1-2 unranked command lines; needs a stated 'otherwise' rule like tidy:229's), skills/demo/SKILL.md (same shape + two /claude-tweaks:help lines both renderable on Skip), skills/help/SKILL.md:95-96 (alt-line placeholders lack backticks). Surfaced by #704's final review; requires a design choice (promote a fallback line vs render unranked), out of #704's one-line-fix scope | deferred | → backlog: auto-routed (ledgerNarrowing, blocker needs-human-decision) — staged/ledger-record-next-actions-omission-recommended-slot.md; resolves at the Queue writes surface |
| 2 | review/hindsight | docs/skill-authoring.md's Skill handoffs bullet does not state the conditional-recommendation form (~7 blocks bold (recommended) only under a stated condition, otherwise unranked) that #704's corpus scan had to carve out ad hoc — one sentence would make it self-documenting; staged/reflect-1.md; belongs with item 1's follow-up record | deferred | Staged for the Review Console (staged/reflect-1.md) — resolve at the Queue writes surface together with item 1 |
| 3 | wrap-up | Dispatch briefs that state an exemption's condition after its example get read as the example — Task 7 + its reviewer both dropped 'with qualifiers present' and cleared test/SKILL.md; systemic; one sentence for _shared/subagent-output-contract.md input discipline (staged/reflect-2.md) | deferred | Staged for the Review Console (staged/reflect-2.md) — Docs/Skills curation, D2 |
| 4 | wrap-up | branch — origin/worktree-flow+spec-654-655 — merged into origin/main (PR #656), not deleted (residue sweep, remedy: auto — fix-now candidate) | fixed | remote branch deleted (git push origin --delete) |
| 5 | wrap-up | branch — origin/worktree-flow-spec-620-621-622-623-624-625 — merged into origin/main (PR #634), not deleted (residue sweep, remedy: auto — fix-now candidate) | fixed | remote branch deleted (git push origin --delete) |
| 6 | wrap-up | branch — origin/worktree-flow-spec-646 — merged into origin/main (PR #669), not deleted (residue sweep, remedy: auto — fix-now candidate) | fixed | already deleted on the remote by a sibling session; stale local tracking ref pruned (git fetch --prune) |
| 7 | wrap-up | pr — PR #710 open, head worktree-flow-spec-704 (this work) (residue sweep, remedy: record — this run own PR; resolved by Phase 4 close-via-merge, not Phase 1) | open | — |
