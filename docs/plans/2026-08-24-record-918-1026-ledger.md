# Open Items — #918 residue.js branches probe blast-radius scoping; #1026 residue.js blast-radius worktree-ownership filtering

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | build | #918's entire Technical Approach (give `probeBranches` an ownership predicate so a fallthrough merged branch is `scope: 'observed'`, not `'blast-radius'`) was already implemented by commit `99aa3881` (`refs #499`, 2026-08-20) — before this build ran. `plugin/bin/lib/residue/probes/branches.js` already tags the fallthrough case `scope: 'observed'`; `plugin/skills/wrap-up/residue-sweep.md` already documents it; `tests/bin-lib/residue/probes-git.test.js` already carries the exact regression tests #918's Deliverable 3 asks for. No code change made — re-implementing would duplicate #499's shipped fix. | resolved | Re-verified independently at review (source-read of `branches.js` + `git show --stat 99aa3881`, both confirming the claim). Closed #918 as duplicate of #499. |
| 2 | build | #1026's `branches.js` half is the same #499 fix as item 1. Its `pipeline-runs.js` half was already audited by `#1011`: `probePipelineRuns` intentionally keeps `scope: 'blast-radius'` unconditionally — a `status: 'clean'` run dir is a terminal, inert state regardless of owning session (unlike a merged branch, which could still belong to a live concurrent session). This is documented in a code comment at `plugin/bin/lib/residue/probes/pipeline-runs.js` and pinned by `tests/bin-lib/residue/probes-pipeline-runs.test.js`'s "a clean run dir belonging to an unrelated record is still blast-radius (deliberate divergence from probeBranches)" test — exactly #1026's own Deliverable-1 alternative ("leave both blast-radius when genuinely unattributable, but document why"). `residue-sweep.md`'s `remedy: auto` section already lists the pipeline-run-dir finding as reaching Phase 1 while noting the branch finding is filtered — doc matches code. No code change made. | resolved | Re-verified independently at review (source-read of `pipeline-runs.js`'s code comment, and confirmed issue #1011 closed via merged PR #1159). Closed #1026 as duplicate of #1011. |
| 3 | test | Full `npm test` run (3 consecutive runs) shows the same 14 pre-existing failures, all unrelated to `plugin/bin/lib/residue/**` (confirmed via file-path grep against the failing-test names). All 14 are consistent with running the suite as `root` (`whoami` confirms `root`): chmod/permission-denial tests (root bypasses Unix permission bits so an expected `EACCES` never fires), plus a few environment-specific checks (installed-plugin-version pin, CLI entrypoint flag parsing). This build made zero code changes, so these 14 failures pre-date and are independent of this build's diff. The 33 residue-probe tests directly relevant to #918/#1026 (`tests/bin-lib/residue/probes-git.test.js` + `probes-pipeline-runs.test.js`) pass 33/33. | resolved | Accepted at review: independently re-ran the full suite (fresh `npm test`, not the build step's cached claim) — 6151 pass, 14 fail, 11 skipped, byte-identical failing-test-name list to the build step's report. Diff is docs-only (spec materialization + this ledger), so these 14 pre-date and are structurally unrelated to this dispatch. No separate backlog record filed for the root-sandbox CI gap — below this run's materiality floor and already implicitly known (recurs on every `npm test` run as root). |

## Resolve

Items 1-2: both records' underlying defects were already fixed/audited by prior work (`#499`, `#1011`) before this bundle was dispatched. Closed #918 as duplicate of #499 and #1026 as duplicate of #1011 at wrap-up (2026-08-24) — see each issue's closing comment for the evidence trail. The docs-only diff (materialized specs + this ledger) merges as the audit trail; no `Fixes #{n}` in the merge commit since the issues were already closed explicitly, not by this merge. Item 3: the 14 failing tests are environmental (root-user sandbox) and outside this build's diff — accepted, no action.
