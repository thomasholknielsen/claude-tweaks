# Open Items — dispatch: queue-pull does not exclude issues with an already-open PR (#1224)

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | review | Lens 3e (Architecture, reproduced by both agents A+B): `partitionByOpenLinkedPR` (record.js:545-557) reimplemented the eligible/excluded partition loop instead of reusing the shared `partitionByOpenBlockers` helper its two siblings `partitionByOpenBodyBlockers`/`partitionByOpenNativeBlockers` both build on | fixed | Refactored `partitionByOpenLinkedPR` to call `partitionByOpenBlockers` with an `openBlockerIdsFn` callback, mapping the result's `excluded[].blockedBy[0]` to `{number, linkedPR}` — behavior-preserving, 144/144 record.test.js tests still pass |
| 2 | review | Lens 3d (Performance, direct-verification override — single agent, confirmed by independent source read): `queue-pull-script.md` calls `gh repo view` twice (lines 86, 120) — an avoidable extra API round-trip when `work-links: native` is set and both eligible sets are non-empty | fixed | Cache-and-reuse: the linked-PR block now checks `[ -z "$OWNER_REPO" ]` before refetching, reusing the native block's value when it already ran |
| 3 | review | Lens 3e-A (direct-verification override): SKILL.md's Open-PR-exclusion coverage relies on "same convention... see queue-pull-notes.md" rather than inlining the literal report template, unlike the sibling Blocked-exclusion report two sentences earlier which embeds its template directly in SKILL.md | deferred | SKILL.md is at 40939/40960 bytes (40 KB hard ceiling, `tests/bin-lib/skill-audit/context-cost.test.js`) — only 21 bytes headroom, not enough to inline the template without a broader trim of the surrounding paragraph (out of scope for this record's diff). Routed to the digest per `_shared/materiality-floor.md` (size:low × priority:low × risk:low) — posted to #1426, `Defer-reason: genuinely-larger` |
| 4 | review | Lens 3e-B (direct-verification override, independently confirmed by reading the source): `queue-pull-script.md`'s "Queue-pull notes" trigger sentence (read `queue-pull-notes.md` only under `work-links: native` or a `--limit` cap hit) wasn't updated even though `queue-pull-notes.md` now also documents the unconditional Open-PR-exclusion, which applies regardless of `work-links` | fixed | Updated the trigger sentence to also name the Open-PR-exclusion report's literal template as an unconditional reason to read the file |
| 5 | review | Lens 3a-A (unconfirmed, info, single-agent — not reproduced by agent B): GraphQL `first:10` page-size limit on `closedByPullRequestsReferences` could theoretically miss 10+ linked PRs per issue | observation | Info severity, extremely rare in practice (already acknowledged in queue-pull-notes.md's `includeClosedPrs` note), not reproduced — stays unconfirmed, routes to Wrap-Up Console's Low-confidence subsection, no fix applied |
| 6 | review | Lens 3f-B (unconfirmed, low, single-agent — not reproduced by agent A): no test coverage for a candidate whose `closedByPullRequestsReferences` carries multiple OPEN-state nodes (only the single-OPEN-node case is tested) | observation | Low severity — `.find()` returning the first OPEN match is correct and safe for the exclusion's purpose (any one open linked PR is sufficient to exclude); not reproduced — stays unconfirmed, routes to Wrap-Up Console's Low-confidence subsection, no fix applied |
