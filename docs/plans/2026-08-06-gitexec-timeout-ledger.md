# Open Items — #134 git-exec's 3s timeout makes the worktree.always gate fail open under load

Run: `2026-08-06T153149-record-134` · Mode: auto · Ceremony: standard

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | flow | Step 2.6 shape check returned WARNING: the record touches `git-exec.js` plus its three callers and the test fixtures. Judged cohesive-by-necessity (a shared wrapper's contract change requires updating its callers in the same change, `[IL-60]`) rather than structural coupling. | accepted | Proceeding as one record — decomposition would split a contract change from its callers, which is the failure `[IL-60]` describes |
| 2 | build | Deliverable 5 — decide whether the `tests/hooks-*` e2e suites stay in `npm test` or move to `perf/` as v6.38.1 did for the statusline render budget. | accepted | **Keep them in `npm test`.** v6.38.1's precedent does not transfer: that test asserted a render-*time budget* (a performance claim, which is what `perf/` is for), whereas these assert *behavior* — whether the worktree gate denies. Moving them would remove correctness coverage from the suite that gates merges. Decisive: applying v6.38.1's remedy here reflexively would have silenced the flake while leaving the fail-open enforcement gap live and now undetectable. The tests were not misbehaving; they were correctly reporting a production defect, and the fix belongs at the source (the budget), which is where it landed. |
| 6 | test | AC4 under-load reproduction: 6 clean full runs (2008/2008, exit 0) under three-way concurrency — 3 pre-merge, 3 post-merge — against a pre-fix baseline of 1 failing run in 3 under the identical recipe. | fixed | Exceeds AC4's required 5 |
| 7 | test | One batch of three concurrent post-merge runs aborted with `exit=1`, no TAP summary, and **zero** `not ok` lines across 153 executed tests. Not reproducible: an immediately preceding solo run and an immediately following identical batch were both clean. Disk had 2.3 TiB free and no kill marker appeared in any log. | observation | Unattributed, and deliberately not counted either way — an aborted run is not a failing run, and AC4 asks for *passing* runs. Recording it because "exit=1" and "a test failed" look alike at a glance, which is the same misreading that made #134 expensive in the first place. My initial note that all three "stopped at test 51" was itself wrong: `node --test` flushes TAP per file, so 51 is an output plateau, not an execution point — the healthy retry batch sat at the same number while passing. |
| 8 | review | The indeterminate warning claimed "the worktree.always policy gate was NOT applied", but the branch is reached before `isWorktreeAlwaysOn` can run — `findPolicyFile` proves only that *a* `policy.yml` exists up the chain, not that the flag is on for this repo. The message asserted a policy that may not exist. | fixed | Reworded to "the worktree.always check could not run"; test now asserts the corrected wording and `doesNotMatch` the old over-claim — `743c6692`..HEAD |
| 9 | review | The comment justifying `indeterminateTargets` as an out-parameter gave a reason that does not hold ("so a deny for one target never discards the warning owed for another") — returning both would preserve them equally. | fixed | Replaced with the real reason: an out-param keeps `checkWorktreeRequired`'s return shape at `{}` \| `{exit, json}`, leaving `runInner`'s dispatch untouched. A wrong justification is worse than none where comments are load-bearing. |
| 10 | review/hindsight | The 10000ms budget is justified as ~4x the measured peak, but that peak (2492ms) came from one harness on one machine under synthetic load. The fix lowers the probability of an indeterminate result; it does not eliminate the failure mode, and a slower machine or heavier real load could still exceed it. | accepted | Deliberate. An adaptive or retrying budget adds branching to a hook whose governing invariant is "never break a session", and the record's acceptance criteria asked for headroom and diagnosability, not elimination. Recording it so the residual risk is stated rather than implied — the honest claim is "much rarer and now visible", not "fixed". |
| 4 | review/hindsight | `FAILURE.GIT_ERROR` is treated as a definitive "not a git repo", but it actually covers every non-zero git exit — a corrupt repo or a permissions failure classifies the same way. The label over-claims slightly. | accepted | Behaviour-neutral against the pre-fix code (all of these already resolved to allow), and separating them needs stderr capture, which is deliberately out of this record's scope. Recording it so the imprecision is a known limit rather than an unexamined assumption; the fix is a follow-up if a real corrupt-repo case ever appears. |
| 5 | review/hindsight | `classify()` maps `err.signal === 'SIGTERM'` to `timeout`, so a git process killed by an *external* SIGTERM would be misreported as a budget overrun. | accepted | Deliberate heuristic. `execFileSync`'s timeout kill does not set `err.code` consistently across platforms/Node versions, so signal is a needed signal-of-last-resort; an external SIGTERM to a hook's short-lived git child is not a case worth complicating the classifier for. Both resolve to indeterminate anyway, so no gate decision differs. |
| 3 | build | `session-start.js` also consumes `repoInfo` and skips its worktree nudge when `repoRoot` is falsy — so an indeterminate result silently suppresses the nudge there too. | accepted | Left unchanged, deliberately. It is inform-tier and best-effort (already wrapped in `try/catch`), fires once per session rather than per tool call, and a missed advisory nudge has no enforcement consequence — unlike the pre-tool-use gate, where the same null drove an allow/deny decision. Noting it here so the asymmetry is a recorded choice rather than an oversight. |
