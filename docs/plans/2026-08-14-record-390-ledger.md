# Open Items — Prune docs/superpowers archive — second ADR-0007 application (#390)

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | build/ops | Bulk-prune deliverable (`git rm` ~101 stale `docs/superpowers/{plans,specs}/` files) was already satisfied by five prior `Tidy: remove ...` commits on `main` (`d83f0720`, `f5db30d9`, `70849915`, `3daf94d0`, and the earlier `d5e9a7ba`) that landed the same day, before this worktree branched. Only 12 files remain (9 plans + 3 specs, 124K total) — all either dated today, deliberately kept per a documented `/tidy` audit (`dispatch-tidy-mcp-bridge` — Slice 2 still pending per ADR-0008; `doc-prior-art-detection` — Phase 2 deferred), or actively cited from `docs/skill-graph.md`. None qualify for deletion under ADR-0007's own last-week/active-run/cross-reference criteria. | accepted | Premise superseded by upstream work; no further deletion warranted. |
| 2 | build/ops | `docs/superpowers/plans/2026-07-20-fix-review-findings-data.json` deletion + `bin/lib/docs-health/tests/scope.test.js` fixture-string update | accepted | File already deleted by the same prior tidy pass; the test fixture never actually referenced this path (verified by grep — it references a different, synthetic `.md` name), so no update was needed. |
| 3 | build | Fix dangling ref `bin/lib/routine-template-parser.js:76` → `docs/superpowers/plans/2026-07-05-routine-improvements.md` | fixed | Rewrote comment to the project's "Was {path} — deleted ({sha})" convention (already established at `bin/lib/issues/autonomy.js:7`). |
| 4 | build | Fix dangling ref `bin/lib/skill-audit/tests/fixtures/review-SKILL-pre-2b.md:175` → `docs/superpowers/specs/2026-07-15-cross-spec-promise-tracking-design.md` | accepted | Frozen fixture (verbatim byte-for-byte snapshot per its own test's comment) — left untouched; added an explanatory note in `bin/lib/skill-audit/tests/identifiers.test.js` instead, per the spec's own "decide at build time" guidance. |
| 5 | build/ops | Scope-creep: repo-wide grep (per the record's own Acceptance Criteria) found 8 further dangling `docs/superpowers/` references beyond the 2 the record named, all left over from the same-day tidy commits — `skills/dispatch/mcp-transport.md:7`, `bin/lib/issues/trust.js:7`, `bin/lib/issues/autonomy.js:125`, `skills/wrap-up/skill-curation.md:9`, `bin/lib/code-health/scope.js:153`, `skills/wrap-up/review-console.md:37`, `bin/lib/record-graph/encode.js:3`, `bin/lib/issues/grant-gate.js:17`. | fixed | Applied `add-to-plan` per pipeline config (config.yml). All 8 rewritten to the "Was {path} — deleted/decomposed ({sha})" convention; deletion/decomposition commits identified via `git log --diff-filter=D` / `--follow`. |
| 6 | build | `skills/wrap-up/cleanup-procedures.md:11`'s stale "permanent historical archive" claim | fixed | Reworded to cite ADR-0007's actual retention policy (kept per-build, bulk-pruned only as a separate deliberate action) instead of asserting permanence; corrected the claim that CLAUDE.md documents this (it no longer does — ADR-0007 is now the source). |
| 7 | build | Acceptance Criteria grep: `docs/superpowers/` reference sweep across `skills/ bin/ tests/ docs/*.md agents/ README.md CLAUDE.md` | fixed | Re-ran after all fixes — zero unresolved references remain (all matches are either resolved paths, the noted frozen fixture, or clearly synthetic example paths in test fixtures / doc examples, e.g. `food-graph-design.md`, `migration-design.md`, `notes.md`). `CHANGELOG.md` mentions checked too (per the record's Gotchas) — judged non-load-bearing: `CHANGELOG.md` is explicitly self-documented (line 846) as deliberately excluded from reference sweeps, being a frozen per-release history. |
