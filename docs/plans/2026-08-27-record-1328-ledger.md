# Open Items — 1328: residue.js: pipeline-run-dir `remedy: auto` classification does not check whether the run's worktree is still locked

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | wrap-up | pr — PR #1574 — open, head worktree-dispatch-record-1328 (this work); `gh pr list --state open --json number,title,headRefName --limit 100` (residue-6eb1fc4e), remedy: record | accepted | This run's own active PR, not a forgotten/orphaned one — Phase 4's Review Console + merge-authorization step in this same wrap-up run resolves it next (merge or explicit park), which is exactly the "e.g. a forgotten draft PR from an earlier phase of the same run" case forge.js's residue probe exists to catch, minus the "forgotten" part. |
