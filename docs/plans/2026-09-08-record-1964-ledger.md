# Open Items — #1964: queue-pull-script.md backtick command-substitution

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | review | Lens 3b (Security, low): tests/node-e-snippet-syntax.test.js's `fenceRe` (`/```bash\n([\s\S]*?)```/g`, both the pre-existing `extractNodeEScripts` copy and the new `extractRawDoubleQuotedNodeEBodies` copy) requires a literal LF after the fence opener, so it silently matches zero fences on a CRLF-normalized checkout (core.autocrlf=true, no .gitattributes) — the new #1964 bare-backtick guard and the pre-existing full-corpus syntax sweep never actually executed on this checkout. | fixed | Both `fenceRe` literals changed to `\r?\n`; commit bf75ccc8b. All 17 tests in the file pass, including both sweeps now executing against real corpus content. |
| 2 | wrap-up | Reflect (Near-misses, observation): duplicated `fenceRe` regex silently inherited a pre-existing CRLF-intolerance bug when copied into new code — same class as #2033. | deferred | Staged to Wrap-Up Review Console: staged/reflect-1.md. |
| 3 | wrap-up | Reflect (Friction, tangential): `set-config.js` writes one `config.yml` lever per invocation, making flow's Step 3 case-3 recovery (13 canonical levers) take 13 CLI calls instead of one. | deferred | Staged as backlog candidate: staged/reflect-2.md. |
