# Open Items — dispatch: detect concurrently-authorized records with overlapping unmerged root-cause fixes

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | test | CI's hosted `test` check on PR #1797 failed 3/7158 tests (context-cost.test.js x2, dispatch-budget-drain.test.js) — this build's own commit e05492e55 added a paragraph to `plugin/skills/dispatch/SKILL.md` that tipped it over the 40960-byte per-invocation ceiling (origin/main was already only 15 bytes under it). | fixed | Extracted the new Cross-PR overlap paragraph plus the adjacent Oversized-group report into `cross-pr-overlap-report.md`/`oversized-group-report.md` with one-line pointers, matching this skill's existing sub-file convention. SKILL.md now 40677 bytes (git blob). Verified via targeted test run and a full local suite re-run (7138 tests, 6894 pass, 239 fail, 5 skipped — same pre-existing baseline). Committed 71d4ff1fa; fresh `gh pr checks 1797` shows `test` pass. |
| 2 | review/hindsight | Missing consolidation: `detectCrossPRFileOverlap`'s hub-path computation (fileCounts/threshold/hubPaths) duplicated `groupByFileOverlap`'s existing block almost verbatim in `plugin/bin/lib/issues/grouping.js`. | fixed | Extracted a shared `computeHubPaths(items, getFiles, minCount, fraction)` helper; both functions now call it. Verified: `node --test tests/bin-lib/issues/grouping.test.js` — all `detectCrossPRFileOverlap` and `groupByFileOverlap` tests still pass (the one pre-existing unrelated failure, `#146 and #150 real bodies land in ONE bundle of two, not two singletons`, is unaffected — reproduced without this change per this build's own earlier decisions.md entry). |
