# Open Items — skills/backlog/refine-mode.md is at its 40KB ceiling — 14 bytes headroom, split needed

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | build | Record's stated premise ("14 bytes headroom, emergency risk") is stale: `plugin/skills/backlog/refine-mode.md` was 40,946/40,960 bytes when #845 was filed (confirmed against commit 45006c0b, #764's build tip), but an unrelated later commit (8eba9f2a, refs #311, net -59 lines) already trimmed it to 36,690 bytes — 4,270 bytes headroom (10.4%), below even the 90% WARN_RATIO threshold. AC #1 ("meaningful headroom restored, at least a few KB") is already true on unmodified `main` without performing the Deliverables' split. Decide at review/wrap-up: close #845 as resolved-by-side-effect (no split needed), or still perform the split proactively for modularity. | open | — |
| 2 | test | `npm test` had 2 pre-existing failures unrelated to #845's diff at the time of the pre-change baseline run: `tests/changelog-coverage.test.js` — "every version that shipped on the release branch has a CHANGELOG entry" and "the record accounts for every version the git walk can still see" (`origin/main` reported version 6.98.0, which `docs/shipped-versions.tsv` did not yet list). Not caused by this record. | accepted | Self-resolved upstream between the baseline run (4838 tests, 2 fail) and the post-commit full-suite run (4998 tests, 0 fail) — a concurrent main update added the missing `shipped-versions.tsv` entry. Re-ran `node --test tests/changelog-coverage.test.js` standalone to confirm: 8/8 pass. No action needed from #845. |
