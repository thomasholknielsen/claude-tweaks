# Open Items — #1279, #1700 (materiality digest dedup + MCP fingerprint dual-marker)

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | review/hindsight | Missing consolidation: `materiality-digest.js`'s `parseMaterialityFingerprints` duplicates `digest.js`'s `parseDigestFingerprints` marker-extraction regex loop verbatim (differ only in marker prefix constant). Out of scope for #1279/#1700's own Deliverables — a follow-up simplification candidate. See `staged/reflect-1.md`. | deferred | STAGED to Review Console (`decisions.md`, `hindsight` step). Recommended: extract a shared `parseMarkerFingerprints(text, prefix)` helper into `fingerprint.js`. |
| 2 | review/step3 | Lens 3b (Security) reproduction pair: 0/4 findings confirmed (no shared path:line between the two independent agents) — 1 medium (`FP_RE_WORK_PLAIN` unbounded pattern match risk) and 3 low/info severity. See `staged/review-lowconf-1.md`. | deferred | STAGED to Review Console as low-confidence, per the reproduction-pair contract (unconfirmed findings bypass Step 3 Routing). |
| 3 | wrap-up | Residue sweep: PR #1850 (`worktree-dispatch-record-1279`) open — this run's own head branch. | accepted | `assess-agent-autonomy merge-check` verdict: needs-human for both #1279 and #1700 (a Medium-severity review finding plus a real behavior delta in 8 edited agent-instruction files) — run parks `pending-review`, PR stays open for a human merge decision. Not a defect; the correct outcome of a needs-human verdict under `consoleAutoResolve`. |

Test verdict (independently re-derived at review, fresh `npm test` run at HEAD 4a5622d04): 7369 total, 7365 pass, 4 fail — all 4 individually confirmed pre-existing/environmental (3× macOS `/private/var` realpath symlink quirk in `tests/bin-lib/reconcile/reap-merged.test.js`, 1× Impeccable CLI version-pin mismatch in `tests/impeccable-cli-contract.test.js`), neither failing file touched by this diff.
