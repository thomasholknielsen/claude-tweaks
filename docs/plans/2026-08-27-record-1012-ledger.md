# Open Items — hooks.js mutating verbs: unknown-flag rejection, unambiguous-only implicit run resolution, composite ownership guard

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | review | [3c Error Handling] hooks.js:118-120 comment claims "classifyOwnership can never call a binding match 'foreign'" — false; classifyOwnership's session-id-mismatch check fires before the binding check and DOES return 'foreign' for a step-2 binding hit with a differing session id (proven by tests/bin-lib/hooks/classify-ownership.test.js:18-25). Actual behavior is safe regardless (every step-2 runDir still passes through each verb's own write-time guard — isForeignRun or closeRunState's classifyOwnership check), but the comment could mislead a future maintainer into removing that guard as "dead code". | open | — |
| 2 | wrap-up | [Near-misses, observation] Two lens agents (3a-B, 3f-A) independently misread the shared `runHook` test helper's `PIPELINE_RUN_DIR: ''` default (tests/hooks-dispatcher.test.js:22-43) as a per-call-site spec-compliance gap — both disproven. Suggested resolution: nudge step3-lens-dispatch.md's canonical agent prompt to check shared test-fixture defaults before flagging a "spec text vs. call-site text" gap. | open | — |
| 3 | wrap-up | [Docs row, staleness] docs/hooks.md's "Omitting --run..." bullet and the "Two resolutions, not one" bullet's trailing clause described the pre-#1012 "newest non-terminal run" fallback as current behavior for record-worktree/record-pr/spec-status/close-run/teardown-run — stale as of 98e44d412. Fix staged. | open | — |
| 4 | wrap-up | [Journeys row, staleness] docs/journeys/recover-from-a-bookkeeping-stamp-deny.md Step 3's "Should understand" line quoted docs/hooks.md's pre-#1012 phrasing verbatim ("omitting --run... can silently corrupt") — now stale relative to ledger #3's fix to that same bullet. Fix staged. | open | — |
| 5 | wrap-up | pr — PR #1559 open, head worktree-dispatch-record-1012 (this work) — residue sweep. Expected under pr-first while this run's Review Console approvals are still pending; resolves on merge. | open | — |
