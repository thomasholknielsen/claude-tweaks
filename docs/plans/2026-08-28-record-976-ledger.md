# Open Items — IL-141's git-plumbing PreToolUse-gate bypass has no backlog record beyond the narrow #959 symptom fix (#976)

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | review | Lens 3b (Security): `git-command.js`'s `isReadOnlyApply()` checks only whether ANY of `--check`/`--stat`/`--numstat`/`--summary` is present in a `git apply` invocation's args. Per git's documented semantics, `--apply` explicitly overrides all four of those "turns off apply" flags and re-enables real application. `isReadOnlyApply(['--check', '--apply'])` therefore returns `true`, so `gitTargets('git apply --check --apply x.patch', dir)` resolves `[]` (no write target) even though the command actually applies the patch — a fail-open bypass of the exact gate #976 exists to close. Empirically reproduced: `gitTargets('git apply --check --apply x.patch', '/repo')` → `[]`. | open | Staged as patch — `staged/review-1.patch`. Awaiting Review Console approval. |
| 2 | review | Lens 3b (Security): `gitTargets()`'s `findGitLead` generically recognizes the `env git <verb> ...` lead-in shape (per #590's precedent, established for `commit`/`push`) and correctly classifies `env git mv/rm/apply/update-ref/update-index/commit-tree ...` as writes — confirmed empirically (`gitTargets('env git mv a b', '/repo')` → `[{action:'mv',dir:'/repo'}]`). But `hooks.json`'s PreToolUse and PostToolUse Bash groups only carry `Bash(env git commit *)`/`Bash(env git push *)` predicates (the #590 precedent) — no `Bash(env git {verb} *)` predicate exists for any of the 6 newly-covered plumbing verbs, in either group. The hook process never spawns for the bare `env git {verb} ...` invocation shape (no env-level flags before `git`), so the parser's classification is dead code for that shape — the identical #70/#590 matcher/parser-asymmetry bug this record's own new test (`tests/hooks-gate-coverage.test.js`) pins for commit/push, left unclosed for the 6 new verbs. | open | Staged as patch — `staged/review-2.patch`. Awaiting Review Console approval. |
| 3 | wrap-up | CLAUDE.md & rules curation (don't-repeat candidate): a new `docs/donts.md` rule proposes that widening a gate's covered-verb/action set must cross-check every coverage dimension a prior similar widening for the same gate already established (bare predicate, env-wrapped predicate, dry-run/override-flag detection), not just the new verb list — anchored to items 1-2 above, both caught only by review's empirical reproduction | open | Staged as patch — `staged/claude-md-1.patch`. Awaiting Review Console approval. |
| 4 | wrap-up | Docs curation: `docs/hooks.md`'s bookkeeping-stamps gate description said "Edit/Write/NotebookEdit/commit/push" — stale after #976 widened `GATE_COVERAGE.gitActions`, since `checkBookkeepingStampsGate` reuses that same list via its shared `commandGitTargets` (confirmed by reading `pre-tool-use.js`: `checkBookkeepingStampsGate(ctx, commandGitTargets, ...)` receives the raw `gitTargets()` output, unfiltered) | fixed | Applied directly — `docs/hooks.md` now names the full `GATE_COVERAGE.gitActions` list |
| 5 | wrap-up | Journeys curation: `docs/journeys/recover-from-a-bookkeeping-stamp-deny.md`'s Goal/Entry-point lines enumerated only `git commit`/`git push` as the gate's covered `git` writes — same staleness as item 4, same root cause | fixed | Applied directly — Goal and Entry point lines now name the full verb list |
| 6 | wrap-up | Memory curation (D4): the same don't-repeat lesson as item 3, proposed as a cross-project memory file (`gate-widening-crosscheck-prior-art-dimensions.md`) since it generalizes past this repo — a 3-point checklist for the next gate-coverage widening anywhere | open | Staged — `staged/wrap-up-memory-1.md`. Awaiting Review Console approval. |
| 7 | wrap-up | Residue sweep: PR #1601 (head `worktree-dispatch-record-976`) is still open | accepted | Expected residue at this point in the run — this record's own PR, whose merge/park disposition is Phase 4's own Review Console / merge-authorization decision, not a dangling artifact |
