# Open Items — #1774: Skill best-practice: skill-prose-conformance-tests — Anti-Patterns

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | build/verification | `npm test` shows 3 pre-existing failures in `tests/bin-lib/reconcile/reap-merged.test.js` (`#1793` realpath assertions expect `/var/folders/...`, actual is macOS's `/private/var/folders/...`) — unrelated to this record's diff (one-line Anti-Patterns row trim in `.claude/skills/skill-prose-conformance-tests/SKILL.md`) | accepted | Pre-existing on `main` before this branch: `git merge-base --is-ancestor b47707ca3 669a32470` confirms the port-isolation commit (#1803, which introduced `reap-merged.js`'s `releasePorts` realpath call) is an ancestor of this branch's base commit. This record's diff touches only `.claude/skills/skill-prose-conformance-tests/SKILL.md` — no `plugin/bin/lib/reconcile/reap-merged.js`, `ports.js`, or their tests. Reproduced in isolation (`node --test tests/bin-lib/reconcile/reap-merged.test.js`): same 3/15 failures, not a concurrency flake. Already tracked upstream as open issue #1900. |
| 2 | wrap-up/reflect | Friction lens: dispatch context-pack's canonical `log-decision.js` invocation shape omits the required `--status` flag, causing an exit-2 usage error on first use this session | deferred | Initially staged as an upstream-feedback candidate (`staged/reflect-1.md`); corrected during Phase 2 routing — `_shared/learning-routing.md`'s self-reference check collapses since `git remote get-url origin` resolves to this project (claude-tweaks) itself. Reclassified as an ordinary project skill/doc finding and handed to the Skills curation row instead of the Upstream feedback row. |
| 3 | wrap-up/residue | Residue sweep (`bin/residue.js --scope blast-radius`) flags PR #1935 (head `worktree-dispatch-record-1774`) as an open PR needing disposition | accepted | This is this run's own draft PR under the `pr-first` integration model — its fate (merge, arm, or remain pending-review) is decided by this same wrap-up run's own Phase 4 Review Console / Auto-merge short-circuit, which runs immediately after this gate, not a separate residue concern. No independent action taken here; Phase 4 is authoritative. |
| 4 | wrap-up/verification (re-verify after Skills-row applied edits) | `npm test` re-run after applying the two Skills-row findings shows a 4th failure beyond the 3 already-accepted `reap-merged` ones: `tests/impeccable-cli-contract.test.js` — "the installed CLI matches the pinned version" (pinned `3.6.0`, this machine's globally-installed `impeccable` resolves to `4.0.2` via `npx --no-install impeccable --version`) — unrelated to this record's diff (no file in this diff touches Impeccable, its tests, or its fixtures) | accepted | Reproduced in isolation (`node --test tests/impeccable-cli-contract.test.js`): fails deterministically, not a race. Purely a function of this machine's globally-installed npm package version, not repo state this branch changed. Already tracked upstream as open issue #1900 (same issue as item 1's `reap-merged` failure — its title explicitly covers both: "npm test: reap-merged #1793 macOS realpath mismatch + impeccable-cli-contract version pin"). |
