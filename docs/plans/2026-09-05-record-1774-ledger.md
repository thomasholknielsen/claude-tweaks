# Open Items — #1774: Skill best-practice: skill-prose-conformance-tests — Anti-Patterns

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | build/verification | `npm test` shows 3 pre-existing failures in `tests/bin-lib/reconcile/reap-merged.test.js` (`#1793` realpath assertions expect `/var/folders/...`, actual is macOS's `/private/var/folders/...`) — unrelated to this record's diff (one-line Anti-Patterns row trim in `.claude/skills/skill-prose-conformance-tests/SKILL.md`) | accepted | Pre-existing on `main` before this branch: `git merge-base --is-ancestor b47707ca3 669a32470` confirms the port-isolation commit (#1803, which introduced `reap-merged.js`'s `releasePorts` realpath call) is an ancestor of this branch's base commit. This record's diff touches only `.claude/skills/skill-prose-conformance-tests/SKILL.md` — no `plugin/bin/lib/reconcile/reap-merged.js`, `ports.js`, or their tests. Reproduced in isolation (`node --test tests/bin-lib/reconcile/reap-merged.test.js`): same 3/15 failures, not a concurrency flake. Already tracked upstream as open issue #1900. |
