# Open Items — pre-tool-use: resolve a local default branch so the IL-131 range bound reaches no-remote repos (#1688)

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | review | `preferRemoteTrackingRef`'s upgrade, applied inside the shared `resolveIntegrationBranch` (policy-configured path), breaks the bare-name assumption in 4 existing callers: `post-tool-use.js`'s `checkWorktreeStaleness` (malformed `git fetch origin -- origin/{name}` refspec and `HEAD..origin/origin/{name}` rev-list ref — silently disables the staleness check), `teardown-run.js`'s integration-branch delete-protection (`branch === integration` never matches, silently disabling the "refuse to delete the integration branch" guard), `merge-verification.js`'s `deriveMergeVerification` (`target === fallback` never matches, silently downgrading `merge-when-green` to `off`), and the whole `reconcile/` module (`classify.js`, `mirror-ff.js`, `red-tip.js`, `shared-fetch.js` all construct `origin/${integration}` or fetch `{integration}`, doubling the prefix). Reachable only when a project sets `integration-branch:` in `.claude-tweaks/policy.yml` (not this repo's own config) and a local `refs/remotes/origin/{name}` ref already exists — a common state after any fetch. | open | — |
| 2 | review/hindsight | [skill: _shared/integration-branch.md] Skill-worthy pattern: item #1 above resulted from widening a shared resolver's return-value contract for one new caller without auditing existing consumers for a same-shape assumption. Staged an Anti-Patterns entry proposal for `_shared/integration-branch.md`. | deferred | Staged at `staged/reflect-1.md` — awaits Review Console (Skills curation row). |

