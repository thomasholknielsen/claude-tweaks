# Open Items — pre-tool-use: resolve a local default branch so the IL-131 range bound reaches no-remote repos (#1688)

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | review | `preferRemoteTrackingRef`'s upgrade, applied inside the shared `resolveIntegrationBranch` (policy-configured path), breaks the bare-name assumption in 4 existing callers: `post-tool-use.js`'s `checkWorktreeStaleness` (malformed `git fetch origin -- origin/{name}` refspec and `HEAD..origin/origin/{name}` rev-list ref — silently disables the staleness check), `teardown-run.js`'s integration-branch delete-protection (`branch === integration` never matches, silently disabling the "refuse to delete the integration branch" guard), `merge-verification.js`'s `deriveMergeVerification` (`target === fallback` never matches, silently downgrading `merge-when-green` to `off`), and the whole `reconcile/` module (`classify.js`, `mirror-ff.js`, `red-tip.js`, `shared-fetch.js` all construct `origin/${integration}` or fetch `{integration}`, doubling the prefix). Reachable only when a project sets `integration-branch:` in `.claude-tweaks/policy.yml` (not this repo's own config) and a local `refs/remotes/origin/{name}` ref already exists — a common state after any fetch. | fixed | Applied `review-1.patch` at the Review Console — commit `3b6b3e67f`. Full suite re-verified clean (zero new failures vs. baseline). |
| 2 | review/hindsight | [skill: _shared/integration-branch.md] Skill-worthy pattern: item #1 above resulted from widening a shared resolver's return-value contract for one new caller without auditing existing consumers for a same-shape assumption. | fixed | Applied additive Anti-Patterns entry to `_shared/integration-branch.md` — commit `d07c79b2d`. |
| 3 | wrap-up | Friction lens: this session made 3 avoidable filler Agent-tool dispatches while waiting for an async notification instead of ending the turn with a status message. D4 (memory)-classified operating-pattern lesson. | fixed | Applied at Review Console (consoleAutoResolve) — wrote `agent-tool-async-wait-pattern.md` + MEMORY.md index line. |
| 4 | wrap-up | Docs row: docs/hooks.md's IL-131 bookkeeping-stamps-gate section claimed the no-remote gap "persists" and is "tracked separately" — stale as of #1688. | fixed | Applied fact correction to `docs/hooks.md` — commit `e2a9da440`. |
| 5 | wrap-up | Residue sweep: this run's own open PR #1843 (head `worktree-dispatch-record-1688`) — `gh pr list` blast-radius finding. | fixed | Not left-behind residue — Phase 4's merge/settle step (this same wrap-up run) is PR #1843's designated resolution path, running immediately next. |

