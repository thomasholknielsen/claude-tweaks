# Open Items — resolve-blockers.js: extend to expose per-blocker open state

No existing ledger found when resuming at `review` — creating fresh (per `flow/steps-and-gates.md`'s resume-mode note).

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | review | `queue-pull-script.md`'s native-mode rewrite (calling `bin/resolve-blockers.js` instead of hand-rolled `gh api graphql`) widens the native-blocker-lookup fail-safe blast radius: old code's `partitionByOpenNativeBlockers` degraded fail-open **per-candidate** when a single alias was missing/malformed from the raw GraphQL response; new code routes through `fetchNativeDependencies`, which **throws on any single missing alias** (its #723 "throw on partial result" rule) — `resolve-blockers.js` then exits 3, and `queue-pull-script.md`'s catch-all `else` treats that identically to a total network/auth outage, disabling native filtering for **every** candidate in the run, not just the affected one. Confirmed via independent source trace (`native-dependencies.js`, `record.js`, current `queue-pull-script.md` bash) plus a dispatched Error-Handling lens agent (single-read, low tier) that reproduced the same finding independently — elevated to `confirmed` via the Direct-verification override (this reviewer read the actual current source at each cited file:line and independently confirmed it). Severity: High (Error handling). | accepted | Accept as documented tradeoff (Review Console, Approve-all default per the reviewer's own staged recommendation, `staged/review-1.md`) — net safety effect on dispatch correctness is a wash or mild improvement (the new all-or-nothing throw is the SAME #723 protection already governing every other `fetchNativeDependencies` caller; the old per-candidate silent fail-open had zero warning, the new whole-run fail-open logs one). No code change required to merge #1309. A narrower per-candidate-degrade fix (option 3 in the staged note) is deferred to a future backlog record if a real occurrence is ever observed. **This acceptance does not authorize merging the PR** — `assess-agent-autonomy merge-check` independently returned `needs-human` on a second, unrelated ground (queue-pull-script.md is an agent-instruction file whose behavior-carrying edit fails the refutation test), so the PR still parks `pending-review` regardless of this finding's own disposition. |
| 2 | wrap-up | PR — PR #1615 (this run's own, head `worktree-dispatch-record-1309`) is open. Residue sweep (`bin/residue.js --scope blast-radius`) flags it as outstanding by construction — it is genuinely this run's own in-flight PR, not leftover residue: `assess-agent-autonomy merge-check` returned `needs-human` (item #1 above, High-severity review finding, plus queue-pull-script.md is an agent-instruction file failing the refutation test), so this run parks at `pending-review` with the PR intentionally left open for a human merge decision. | accepted | Accept — the open PR is the correct, by-design state for a `needs-human` merge-check verdict; nothing to fix, nothing to defer. A human decides the actual merge via the Review Console / a direct `gh pr merge` once item #1 is resolved. |

**Note:** `review-effort` resolved to `low` (record labels `risk:low` × `size:low`, no `review-effort-floor` policy set) — Steps 3.5/3.6 (cross-lens debate, refutation, gap-sweep) did not run at this tier; only lenses 3b (Security — no findings) and 3c (Error handling — item #1 above) were dispatched, per operator instruction to independently pay particular attention to this diff's functional-equivalence risk regardless of the low effort tier.
