# Open Items — #551: reconcile-check-authoring docs shape

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | build/docs | `docs/REGISTRY.md` had no row for the new `docs/reconcile-checks.md`, so future `plugin/bin/lib/reconcile/**` changes would never surface it for docs-health review — the unambiguous half of the original finding. | fixed | Added a `docs/reconcile-checks.md` row (Auto-detect: `plugin/bin/lib/reconcile/**`) — `a425b6b0` |
| 2 | review | Whether to also narrow `docs/hooks.md`'s Auto-detect column to drop `plugin/bin/lib/reconcile/**` — `docs/hooks.md`'s own bullet 14 still substantively documents that directory's SessionStart fast/background split, so dropping the pattern there could create a real staleness blind spot rather than just deduplication. This is a documentation-architecture judgment call, not a mechanical fix. | deferred | Staged as a documentation-update proposal for the Review Console — Defer-reason: needs-human-decision |
| 3 | review | `docs/reconcile-checks.md`'s "adding a new check" bullet 4 quoted `reconcile()`'s result shape as `{ mirror, worktrees, claims, runs, branches, remoteBranches, console, skipped }` — this is actually the *fallback's own* (already-incomplete) shape, missing `redTip` from the real `reconcile()` return object (`plugin/bin/lib/reconcile/index.js` line 45). A misleading claim in the exact doc warning about this class of drift. | fixed | Corrected the shape list to include `redTip` and flagged the current fallback's live gap — `a425b6b0` |
| 4 | review | The `redTip` omission itself: `plugin/bin/hooks.js`'s `reconcile-threw` fallback object literal is missing `redTip: null` compared to `reconcile()`'s real result shape — a pre-existing bug outside this record's diff (`bin/hooks.js` untouched by #551). | deferred | Filed thomasholknielsen/claude-tweaks#1009 — Defer-reason: pre-existing-outside-diff |
| 5 | test | `npm test` failed on 2 changelog-coverage assertions (`docs/shipped-versions.tsv` missing the `6.100.0` entry) — origin/main shipped v6.100.0 after this branch's `main` base and after PR #1005's own CI run; not a defect in this record's diff. | fixed | Merged current `origin/main` into the worktree branch (clean, no conflicts); full suite re-ran green (5123/5123) |
