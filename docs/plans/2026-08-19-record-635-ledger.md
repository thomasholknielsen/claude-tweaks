# Open Items — Split skills/_shared/policy-schema.md before it hits the 40 KB sub-file ceiling

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | build | AC1 (`wc -c policy-schema.md` < 30 KB) not fully met — 31,496 B after the split, ~776 B over the literal 30,720 B (30 KiB) target | accepted | Moved the two clearly-scoped coverage blocks (`worktree-always`/teardown gate coverage, `merge-verification` derivation — the issue's own leading candidate) into new `_shared/policy-schema-coverage.md`; the four explicitly-protected categories (canonical read path, `resolveValue` contract, metadata fields, lever tables) stayed in the parent per the issue's own boundary. Closing the remaining ~800 B would require moving lever-table row content (e.g. the `autonomy` row's long Meaning cell) the issue explicitly said to keep in the parent — declined rather than expanding scope past the stated candidate. The real enforced gate (`tests/policy-schema-metadata.test.js`'s new 40,960 B ceiling test, mirroring `github-pr-scan.md`/`review-console.md`'s pattern) passes with ~9.4 KB headroom, up from the original ~400 B. |
| 2 | test | `npm test` has 7 pre-existing failures unrelated to this diff (`impeccable-plugin-contract.test.js` x3 — Impeccable plugin not installed at the pinned root in this sandbox; `hooks-dispatcher.test.js` x2 — root-user bypasses the chmod-based unwritable-fs fault injection two tests rely on; `prune-remote.test.js` x1, `console-execute.test.js` x1 — both need a working git remote/pr-first fixture this sandbox can't reach) | observation | Confirmed via `git stash` / `git stash pop`: the identical 7 tests fail on the unmodified base commit, with the same error messages, before this diff's changes are applied. None of the 7 failing files reference `policy-schema` at all. `npm test` on the diff: 4906 pass, 7 fail (same 7), 10 skipped — zero regressions. |
