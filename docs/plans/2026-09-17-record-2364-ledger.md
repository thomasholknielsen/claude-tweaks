# Open Items — #2364: dispatch close-out mode for approved pending-review PRs

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | wrap-up | Second `/flow` call (`review,polish,wrap-up`) inherited this run's minted directory with no `config.yml`/`staged/`/`work/`, despite the first call (`build,test`) having already built, committed, and pushed PR #2559 — forcing this call to reconstruct the Manifesto's 13-lever config from scratch. See `staged/reflect-1.md`. | observation | Informational — no code fix in this record's scope; flagged as a possible gap in `/claude-tweaks:dispatch`'s two-call handoff for a follow-up look, not actioned here. |
