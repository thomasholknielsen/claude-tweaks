# Open Items — Worktree Concurrency Hardening (specs #192, #307, #308)

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | review | `skills/_shared/worktree-base-ref.md` line 19 claims `worktree.baseRef: fresh` branches from `origin/<default-branch>`. Observed directly contradicted this in this session's own worktree creation (this run's shared worktree branched from local `main` at `1d4a34b6`, 69 commits behind an already-fetched `origin/main` at `cea8caa3`) — see spec-192's `decisions.md`. | fixed | User chose "fix now". Reworded the claim to note the observation and its own unconfirmed status (one observation, harness-internal detail this plugin doesn't own) rather than restate it as settled; also fixed a second, independently-discovered staleness in the same file — its "Runtime verification... surface a mismatch loudly" paragraph still described the exact STOP-on-mismatch mechanism spec #192 removed. Commit `141175a1`. |
| 2 | review | **Security (confirmed via PoC, not just a review agent's claim):** spec #307's `checkWorktreeStaleness` passed the resolved integration-branch value — traceable to hand-editable `.claude-tweaks/policy.yml` — as a bare positional `git fetch` argv element. A `-`-prefixed value (e.g. `--upload-pack=<program>`) is parsed as a git flag, not a refspec; confirmed executing an arbitrary program as a subprocess against a local-path remote (inert against this repo's own `https://` origin specifically). | fixed | Added `--` argv separator, commit `89fec8ab`. Discriminating regression test added and IL-105-verified (reverted the fix, confirmed the test fails; restored, confirmed green) — see spec-307's `decisions.md` for the full PoC writeup. |
| 3 | review | Spec #308's `sibling-sessions.js` self-exclusion compared a `git worktree list --porcelain` path (symlink-resolved by git) against `record-worktree`'s `path.resolve`-only stamp (not symlink-resolved) — the same worktree could read as two different strings, causing a session's own worktree to be misreported as a sibling conflict against itself. Found independently by both Security+ErrorHandling review agents. | fixed | Added `normalizePath()` (reuses `worktree-detect.js`'s `safeReal`, matching `worktree-reap.js`'s existing precedent) on both sides of the comparison, commit `7c296130`. New regression test uses real filesystem symlinks (not this file's usual frozen-string fixtures, since the bug is specifically about string-representation divergence); IL-105-verified. |
