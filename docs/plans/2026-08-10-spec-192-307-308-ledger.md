# Open Items — Worktree Concurrency Hardening (specs #192, #307, #308)

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | review | `skills/_shared/worktree-base-ref.md` line 19 claims `worktree.baseRef: fresh` branches from `origin/<default-branch>`. Observed directly contradicted this in this session's own worktree creation (this run's shared worktree branched from local `main` at `1d4a34b6`, 69 commits behind an already-fetched `origin/main` at `cea8caa3`) — see spec-192's `decisions.md`. `skills/_shared/worktree-setup.md`'s Post-creation catch-up was hardened to not depend on which direction is stale (this record's own fix), so nothing is currently broken by the doc being wrong — but the doc's flat claim about `fresh`'s behavior may itself be inaccurate and worth verifying against multiple observations before restating elsewhere. | open | — |
