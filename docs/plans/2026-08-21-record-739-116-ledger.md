# Open Items — assess-agent-autonomy: router-body trim (#739) + merge-check polish (#116)

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | review | #739's AC "shows the router body's per-session injected volume no longer scaling linearly with record count" is stricter than what a fixed per-record invocation count (explicitly preserved per Deliverables/Gotchas) can ever satisfy — total injected volume necessarily still scales linearly with N, just with a smaller per-invocation constant (21% smaller, 8959->7060 bytes, measured). Direction 1 (harness "previously loaded" suppression) was empirically falsified by the record's own investigation and correctly rejected in favor of Direction 2 (payload trim). Judged as an imprecision in the issue's own AC wording, not an implementation gap — the Deliverables section's explicit "only the per-invocation payload [changes]" framing is the controlling intent. | observation | No code change needed — AC wording note only, non-blocking. |
