# Open Items — record #1470: worktree-setup.md's stale-same-name-branch check misses a remote-only collision

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | review | Step 1.6's `git ls-remote --heads origin "{name}"` command failure (network/auth error) is indistinguishable from a genuine no-match — both produce empty stdout, and the procedure only branched on "No output" vs "A ref comes back," silently reintroducing #1470's own failure mode on the rare occasion the probe itself errors. | fixed | Added an explicit "Command fails" branch mirroring `_shared/worktree-setup.md`'s Post-creation catch-up fail-open convention for the same class of remote failure — `e810f2a72` |
| 2 | review | The best-effort `gh pr list` context lookup's failure/empty case was not distinguished from a confirmed no-open-PR result in the stop-card template, risking a human reading "no open PR found" when the lookup merely failed. | fixed | Added a third "PR status unknown (lookup failed)" state to the stop-card template and adjusted the Interactive-mode recommendation to give no default when status is unknown — `e810f2a72` |
| 3 | review | Narrow TOCTOU race: Step 1.6's remote probe and Step 2's `EnterWorktree` are not atomic — a sibling process could push the same deterministic branch name to `origin` in that gap. | accepted | Narrow window; the existing phase-exit push failure path (`_shared/pr-early-run-lifecycle.md`) already catches this class of collision post-hoc, the same way the equally non-atomic Step 1.5→Step 2 gap already does today. Not blocking. |
| 4 | wrap-up | Residue sweep (`bin/residue.js --scope blast-radius`): PR #1681 — open, head worktree-record-1470 (this work) — needs disposition. | accepted | This is this run's own `pr-first` PR, still open at Phase 3 by design — its final disposition (merge, arm, or leave for review) is resolved by this same wrap-up run's Phase 4 (Review Console / Auto-merge short-circuit), which runs immediately after this gate. |
