# Open Items — Settle retry count splits across issue and PR comment sources, so the retry ceiling never trips (#1963)

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | review | `plugin/skills/dispatch/settle-and-merge.md:108-121` — two coupled defects in step 4's new PR-linked-comment fetch: (a) `require('$DISPATCH_LINKED_PRS')` shell-interpolates the session-tmp path directly into the JS string literal instead of passing it as `process.argv[1]` (the pattern the very next block already uses) — on Windows the backslash-heavy path corrupts silently inside the JS string literal (verified live: `C:\Users\thn\AppData\...` → `C:Users	hnAppData...`), so `require()` throws "Cannot find module" and crashes the whole retry-count step on this platform; (b) the new `gh pr list`/per-PR `gh api` calls have no error handling — a single failed/rate-limited/malformed `gh api` response crashes the whole `node -e` script via an uncaught `JSON.parse`, and a failed `gh pr list` is silently indistinguishable from "record has zero linked PRs" (no `decisions.md` log) — contradicting the explicit degrade-and-log discipline this same diff states one paragraph later for the MCP-absent path. | open | staged/review-1.patch |
| 3 | review | `plugin/skills/dispatch/settle-and-merge.md:121` — `prDir + '/' + f` string concatenation instead of `path.join(prDir, f)`, inconsistent with the codebase's established cross-platform path convention (`atomic-write.js`, `area-type.js`). | fixed | Switched to `path.join()` — see commit |
| 4 | wrap-up | Reflect (Near-misses, systemic): the `process.argv`-vs-string-interpolation bug (item #1) recurred within the same edit that had the safe pattern one block away — no lint/type coverage exists for embedded `node -e` scripts in `plugin/skills/**/*.md` prose. | deferred | Staged as a pattern observation — staged/reflect-1.md |
| 5 | wrap-up | Reflect (Friction): `friction-events.js`'s `contract-violation` detector fired 4 false positives on this run's own intermediate async-wait status turns (documented pattern, not the dispatch's actual final reply). | deferred | Staged as a backlog candidate (tangential) — staged/reflect-2.md — landing: born-ready |
