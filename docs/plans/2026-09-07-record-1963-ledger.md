# Open Items — Settle retry count splits across issue and PR comment sources, so the retry ceiling never trips (#1963)

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | review | `plugin/skills/dispatch/settle-and-merge.md:112` — `require('$DISPATCH_LINKED_PRS')` shell-interpolates the session-tmp path directly into the JS string literal instead of passing it as `process.argv[1]` (the pattern the very next block already uses). On Windows the backslash-heavy path corrupts silently inside the JS string literal (verified live: `C:\Users\thn\AppData\...` → `C:Users	hnAppData...`), so `require()` throws "Cannot find module" and crashes the whole retry-count step on this platform. | open | staged/review-1.patch |
| 2 | review | `plugin/skills/dispatch/settle-and-merge.md:109-121` — the newly-added `gh pr list`/per-PR `gh api` calls have no error handling: a single failed/rate-limited/malformed `gh api` response crashes the whole `node -e` script via an uncaught `JSON.parse`, and a failed `gh pr list` is silently indistinguishable from "record has zero linked PRs" (no `decisions.md` log) — contradicting the explicit degrade-and-log discipline this same diff states one paragraph later for the MCP-absent path. | open | staged/review-2.patch |
| 3 | review | `plugin/skills/dispatch/settle-and-merge.md:121` — `prDir + '/' + f` string concatenation instead of `path.join(prDir, f)`, inconsistent with the codebase's established cross-platform path convention (`atomic-write.js`, `area-type.js`). | fixed | Switched to `path.join()` — see commit |
