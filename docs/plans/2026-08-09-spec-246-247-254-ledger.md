# Open Items — Multi-spec run: #246 record-cache reader, #247 issue-claims trim, #254 writeRunState CI flake

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | flow | Live sibling session (pid 67400, worktree flow-spec-242-243-115-180) may ship to main concurrently — re-verify each record's premise immediately before its own build (IL-109) and re-check main divergence before branch finish | observation | Informational — no record overlap with 242/243/115/180 |
| 2 | flow | origin/main moved 27 commits ahead mid-run (sibling run 242/243/115/180 shipped). File overlap with this branch: none (verified `git diff --name-only 9fdeadc4..origin/main` vs our diff). Rebase onto the new tip before branch finish | acknowledged | Handled at finish step — rebase scheduled, zero-overlap verified |
| 3 | review | Spec 254 LOW finding: a SIGKILL between tmp-write and rename orphans `run-state.json.tmp-<pid>` permanently — no sweep cleans stray tmp files | accepted | Millisecond crash window, gitignored path, run dir is archived/compacted wholesale by wrap-up//tidy; a per-write readdir sweep in a hot hook path costs more than the hygiene gain. Reviewer confirmed no consumer enumerates run-dir files |
