# Open Items — wrap-up Review Console: dead empty-console fast path and undefined no-default-row outcome after Approve all

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | build/ops | Build-time spot check (spec Deliverable 5): sampled `bin/lib/wrap-up/engine-record.js`'s `SCANNED` writes against `_shared/auto-decision-log.md`'s entry-kind vocabulary. `appendDecisionLine` has exactly two call sites, both fed by `buildScannedLine`, which unconditionally prefixes its output with the literal string `SCANNED` — no branch in `engine-record.js` ever emits `AUTO`/`STAGED`/`KEPT-PROMPT`/`REFUSED`. Real per-finding `AUTO`/`STAGED` lines are written separately by the controller (`curation-engine.md` sections 3/4/6, via `log-decision.js`) when a judge's finding is applied or staged — a distinct append path. Confirmed: no decision-bearing outcome is ever emitted under the `SCANNED` prefix. | fixed | Confirmed by direct source read; no code change needed — engine tagging already correct. |
| 2 | build/ops | Deliverable 6: read #798's body and confirmed its repro case (a restructural CLAUDE.md finding staged under "Approve all," which re-prompted mid-flow, reversing the console's stated "no further prompts" contract) is covered by this fix's drills-individually class — restructural stage-only findings (from the CLAUDE.md & rules row) now belong to that class explicitly, drill via `_shared/batched-item-drill.md` chunking as part of the same terminal Approve-all stop, and the console's option descriptions no longer claim unconditional "no further prompts." | fixed | PR #1206 body updated to add `Fixes #798` alongside `Fixes #899`. |
| 3 | wrap-up | Reflect (Friction lens): `bin/materialize.js --run-dir` ambiguity in worktree mode produced a stray main-checkout write (worked around during build). Staged for the Docs/Skills curation row. | deferred | staged/reflect-1.md — awaiting Review Console. |
| 4 | wrap-up | residue sweep — PR #1206 (this run's own draft PR, head `flow-spec-899`) reported open — expected; resolves at Phase 4's merge. | accepted | This run's own in-flight PR — not residue; closes via merge at Phase 4. |
| 5 | wrap-up | residue sweep — `tests/bin-lib/declined-learning/store-concurrency.test.js`: `recordDecline serializes concurrent writers...` fails ~2/3 isolated runs. Diff for this record touches zero files under `bin/lib/declined-learning/`; reproduced identically against the same file content off `main` (3 runs, same pass/fail pattern) — pre-existing timing flake, not introduced here. Both centrally-run full `npm test` invocations for this record passed 5671/5671 clean. | accepted | Pre-existing flake, unrelated module — proof: baseline reproduction + 2x clean full-suite runs on this branch. |
| 6-19 | wrap-up | residue sweep — 14 unarchived `.claude-tweaks/pipelines/*-standalone`/`*-record-{n}` directories, `status: clean`, no attached worktree (`git worktree list` checked) — pre-existing debris from other, unrelated records (dispatch/backlog/tidy/release-review-fixes/trust-table-plugin-root/env-attached-chdir-fix standalone runs, record-499/355/231/951/1011) never archived by their own wrap-ups. None overlaps this record's diff. | accepted | Not this record's blast radius — proper remedy is `/claude-tweaks:tidy`'s archival sweep, not a single-record wrap-up; reported here per residue-sweep.md rather than silently dropped. |
