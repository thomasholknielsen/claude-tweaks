# Open Items — Parent-issue vocabulary rename (#339, #340, #341)

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | ops | Live sibling session (pid 14087, worktree `demo-observation-plan-design`) holds unmerged #324/#325 work (v6.78.0, wrap-up stage) touching `bin/lib/issues/acceptance.js` (comment-only diff vs main) and six skills files #340 sweeps (settle-and-merge.md, review-console.md, execution-and-verification.md, demo/SKILL.md, pending-review-durability.md, visual-review/SKILL.md) (reason-not-auto: requires-judgment — merge-order race with a live human-attended session; this run must re-verify file state at each build [IL-109] and re-check main divergence before finishing the branch) | accepted | Risk consumed — sibling shipped v6.78.0 to origin/main mid-run; merged into this branch cleanly at `76518699` (acceptance.js auto-merged, changelog tests green); [IL-109] re-verifications were performed at spec-339 build |
| 2 | review | `parentGateState`'s `{leaves}` parameter and `leaf` loop variable keep old vocabulary — spec-sanctioned deferral (spec 339 Technical Approach: parameter names are part of #340's prose call sites), but no record owns the eventual rename. Proposal: new backlog record "rename parentGateState's leaves parameter to subIssues across code + prose call sites" once #340 has landed. Needs per-item user decision at the Review Console (work-record creation is never auto-silenced). | open | — |
| 4 | review | Stale "declares a leaf" comment in parseSubIssues test (record.test.js:514), reproduced by both 3f lens agents | fixed | Vocabulary fix — `7b06c788` |
| 5 | review/hindsight | Plan-authoring: plan-prescribed comment text broke the spec's own tombstone grep (AC4) — caught at the Task 6 gate, fixed in 43fa9c34. Lesson: run plan self-check greps over planned comment/prose text, not just code tokens | observation | Informational |
| 6 | review/hindsight | Full-suite npm test runs raced concurrent implementer edits twice (spurious `parseFamilyLeaves is not defined` failures) — hold full-suite runs until tree quiescence in orchestrated builds | observation | Informational |
| 7 | review/hindsight | The circular label-pin test (found+fixed by the simplifier with a verified discrimination check) passed two reviewer reads — mutate-and-confirm-red catches what reading cannot | observation | Informational |
| 10 | test | Intermittent `npm test` runner death observed twice this run (log truncates mid-stream with exit 1, zero failing tests, no TAP summary — first at ~169 tests, later at ~695); both times an immediate re-run on the identical tree passed 3310/3310. Environmental (node 20.12.0 runner), not tree-related | observation | Informational — re-run on truncation; escalate if it ever reproduces twice consecutively |
| 9 | build | Spec 340 AC5 evidence: `grep -rniE "\bleaf\b|\bleaves\b" skills/` = **84** remaining hits (pre-sweep: 396), every one dispositioned per-file in the six task reports (`.superpowers/sdd/2026-08-12-parent-issue-prose-sweep/task-{1..6}-report.md`) — categories: shipped `{leaves, parentLabels}` signature keys (13), live policy key `promise-register-min-leaves` (5), plain-English verb usage (majority), different-sense ("leaf operations", label-namespace) (2). Zero record-class nouns remain. | observation | Informational |
| 8 | build | `skills/_shared/github-pr-scan.md` is at 40,774 of its 40,960-byte soft ceiling (186 bytes headroom) after the dual-fetch additions — record #204 (file split) is now effectively a prerequisite for ANY further addition to that file | open | — |
| 3 | review | Spec #341's verified-red negative sweeps will match this run's own committed pipeline residue: `docs/superpowers/plans/2026-08-12-parent-issue-contract-rename.md`, the three materialized spec files under `.claude-tweaks/pipelines/2026-08-12T092659-*/spec-*/work/`, and the pre-existing consumed plan `docs/superpowers/plans/2026-08-07-parent-acceptance-gate.md` — all dense with `familyGateState`/`parseFamilyLeaves`/`family:parent` literals, none excluded by #341's grep template. #341's build must extend exclusions (or wrap-up must delete consumed plans first) deliberately, not ad hoc. | accepted | Consumed by #341's build: both run plans deleted at their wrap-ups; the pre-existing 2026-08-07 consumed plan deleted by 341 Task 1 (`3752825f`); exclusion extensions (pipelines/, run ledger, docs/superpowers/ history) applied and documented in spec-341 decisions.md; sweeps ran verified-red then green |
