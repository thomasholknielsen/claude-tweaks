# Open Items — Move claim acquisition into flow pre-flight (Step 2.8), with file-overlap warning for direct runs

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | review/hindsight | Claim-blob ownership read appears twice in `claim-targets.md` (skip-guard all-targets check vs. claiming-loop per-target check) — different structural roles, no action needed now. Staged: `staged/reflect-1.md` | accepted | Review Console auto-resolved (consoleAutoResolve, unattended) as no-action per the finding's own recommendation — the duplication is small and the two call sites have different preconditions. |
| 2 | wrap-up | Systemic near-miss: the gh/MCP transport-failure handling gap fixed at `f36c3740` was caught only by adversarial lens review, not the plan or task-level review — no standing rule states transport failure gets all-or-abort treatment. Suggested: add one sentence to `issue-claims.md`'s failure-posture guidance. Staged: `staged/reflect-2.md` | fixed | Phase 2's Skills curation row independently found and applied the exact suggested fix (additive, high-confidence) — added a "Group-claim-all-or-abort exception" paragraph to `issue-claims.md`'s Failure-posture table. Commit `fd65e9f1`. |
| 3 | wrap-up | Residue sweep: branch `origin/worktree-fix-impeccable-cli-pin` is merged into `origin/main` but not deleted (remedy: auto) | fixed | Stale finding — `git fetch origin --prune` confirmed the branch was already deleted upstream (by another session) between the sweep and this check; the sweep's `git branch -r` cache was stale. No action needed. |
| 4 | wrap-up | Residue sweep: branch `origin/worktree-flow-136` is merged into `origin/main` but not deleted (remedy: auto) | fixed | Same as item 3 — already deleted upstream, confirmed via `git fetch origin --prune`. |
| 5 | wrap-up | Residue sweep: PR #468 (this run's own, head `worktree-flow-464`) is still open (remedy: record — resolves via this run's own close-out merge) | fixed | User chose "Fix anyway" at the Phase 2 drill — resolves via this run's own Phase 4 execution step (acceptance labeling + `gh pr merge`), continuing below. |
