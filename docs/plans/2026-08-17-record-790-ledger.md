# Open Items — flow: per-spec run-dir writes recur to worktree-relative shadow copy instead of $RUN_ROOT

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | review | Lens 3b: `resolveRunArg` returned the raw unresolved `candidate` string as `runDir` on accept, instead of the validated `resolved` (cwd-anchored) path — inert today since every caller's `cwd` param equals `process.cwd()`, but a genuine contract inconsistency. Confirmed via direct-verification override (read the source independently; only 1 of 2 reproduction agents flagged it). | fixed | commit cfe22390 — bin/hooks.js:83 now returns `resolved` |
| 2 | review | Lens 3c: `bin/materialize.js`'s anchoring guard called `wtDetect.isAnchoredUnderRoot` directly instead of through `deps`, leaving one gap in the deps-injection seam the prior fix wave otherwise closed (`cwd`/`mainRoot` were already injected). Confirmed via direct-verification override. | fixed | commit cfe22390 — added `deps.isAnchored`, wired into `run()` and both consuming test files' fake deps |
| 3 | review | Lens 3a (info) / 3b (info) / 3f (low): three unconfirmed single-agent findings — (a) `wrap-up-engine.js`'s pre-existing `path.resolve(args.runDir)` doesn't thread the new local `cwd` var (already parked as a nit in this build's SDD final review); (b) a theoretical TOCTOU window between the anchoring `statSync` and the later `mkdirSync`/`writeFileSync` — self-assessed non-blocking by the reporting agent (same-user local CLI, no privilege boundary); (c) new anchoring tests' `mkdtempSync` temp dirs are never `rmSync`'d — verified this matches `tests/helpers/git-fixtures.js`'s existing `gitRepo()` convention (also no cleanup), so it is not a deviation this branch introduced. | accepted | not actionable — (a)/(b) informational and non-blocking per the reporting agent's own reasoning, (c) matches pre-existing codebase convention (verified against tests/helpers/git-fixtures.js) |
| 4 | review | Lens 3i (informational): `docs/REGISTRY.md`'s `bin/**/*.js` Auto-detect pattern matches `docs/hooks.md`, which wasn't updated in this branch's commits. Already judged out of scope at `/build` Common Step 6.5 — the change is below `docs/hooks.md`'s documented level of abstraction (ownership/foreign-session contract, not `--run` argument validation mechanics). | accepted | no update needed — judgment from Common Step 6.5, re-confirmed here |
| 5 | review/hindsight | Skill-worthy pattern: plan authoring under-scoped which call sites carry the same "unvalidated --run/--run-dir input" gap — a repo-wide grep for the flag pattern (not just the issue-named files) would have caught bin/materialize.js AND bin/resolve-profile.js/bin/resolve-policy.js during planning instead of across two later review passes. `[skill: build]` | deferred | staged/reflect-1.md — proposes a "Vulnerability-class completeness check" for build/SKILL.md's Spec Step 3 plan-authoring checklist; surfaces at Wrap-Up Review Console's Skills curation row |
| 6 | wrap-up | Near-miss (reflect full mode): ledger creation was skipped at Flow Step 1.8, only caught when review's Step 1.5 QA Ledger Check ran. CAUSAL: terminal — the downstream gate caught it before any findings were lost. | accepted | no action needed — existing gate (review Step 1.5) already catches this reliably, working as designed; logged for visibility only |
| 7 | wrap-up | Surprise (reflect full mode): final review's third vulnerable call site (bin/resolve-profile.js/bin/resolve-policy.js) could not be folded in — conflicts with docs/journeys/resolve-dispatch-model-profile.md's documented legitimate unanchored use. Correctly filed separately as #853 rather than guessed at inline. | accepted | no action needed — #853 exists and states the design tension explicitly |
| 8 | wrap-up | Friction (reflect full mode): 5 `contract-violation` events this run — SDD task-reviewer/whole-branch-review subagents whose first reply line wasn't the bare required status keyword. Genuine violations, not false-positive gate firings — a claude-tweaks contract (subagent-output-contract.md) wording gap. Initially staged as a D5 upstream candidate (reflect-4.md), but this project IS claude-tweaks, so the Phase 2 Upstream feedback row's self-reference check correctly collapsed it to project scope; the Skills row read it while scanning (see its read list) but produced no separate finding. | accepted | no concrete change this run — single-run 5x recurrence noted for a future pass with more data; staged/reflect-4.md kept as the record |
| 9 | wrap-up | Friction (reflect full mode): events.jsonl for this run contains one ask-user-question entry describing an unrelated worktree/PR (flow-record-81, #837) — apparent cross-run event-logging contamination under concurrent sessions. Same self-reference collapse as row #8 (staged as reflect-5.md, re-routed from Upstream to project scope, read by the Skills row, no finding produced). | accepted | no concrete change this run — single occurrence, not reproduced; staged/reflect-5.md kept as the record |
| 10 | wrap-up | Residue sweep: full-suite probe not run (`--no-suite` — `npm test` already ran earlier in this wrap-up, 4533/4533 pass, no regressions from the doc/skill curation commits). All other blast-radius probes (branches, worktrees, PRs, release-triple) ran clean — zero findings. | observation | no action — suite already verified this run; not a skipped probe with unknown state |
