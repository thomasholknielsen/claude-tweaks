# Open Items — init: pnpm node_modules permission allowlist (#836)

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | review | `plugin/skills/init/bootstrap-steps.md:21` overclaims "idempotent drift-repair on re-run" — a bare `/claude-tweaks:init` re-run does NOT repair a missing Step 8.5 entry when the project's `.claude-tweaks/init-state.yml` marker already matches the installed plugin version (version-check.md's gate skips Steps 1-8.5 entirely in that case); only `/claude-tweaks:init bootstrap` forces the re-check | open | Staged: staged/review-1.patch (unapplied), awaits Review Console |
| 2 | review | `plugin/skills/init/bootstrap/step-08-5-dependency-read-permissions.md:18` — settings.json read step has no malformed-JSON branch, unlike the sibling `init-state.yml` handling in `version-check.md` added in the same diff ("treat as absent if missing or malformed") | fixed | Added malformed-JSON branch — `a205c410` |
| 3 | review | `plugin/skills/init/bootstrap/step-08-5-dependency-read-permissions.md:20` — idempotency merge step doesn't specify handling when `permissions.allow` exists but isn't an array | fixed | Added non-array-type handling — `a205c410` |
| 4 | wrap-up | Fresh-start observation: Step 8.5 self-repair coupled to the whole-bootstrap version-marker gate rather than running unconditionally | deferred | Staged as pattern observation — staged/reflect-1.md, awaits Review Console |
| 5 | wrap-up | Friction: Edit/Write tool refuses edits to the pipeline-run-dir path under the main checkout despite it being the sanctioned location per `_shared/pipeline-run-dir.md` | accepted | Known Claude Code CLI harness limitation outside this repo's fix surface (self-reference check collapses D5); already tracked in operator's local memory |
| 6 | wrap-up | Residue sweep: PR #1552 (this run's own head branch worktree-dispatch-record-836) reported open by `bin/residue.js --scope blast-radius` | accepted | Not orphaned residue — this is the run's own active PR, resolved by this same wrap-up's Phase 4 merge decision (`merge-authorization: ask`), not a separate abandoned artifact |
