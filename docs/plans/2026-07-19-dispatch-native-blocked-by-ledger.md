# Open Items — Record #39: Dispatch native Blocked-by dependency check

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | ops | Pre-flight merge-check: `origin/main` is 9 commits ahead of this worktree's base (unrelated `/demo` session-recall work, no file overlap with `skills/dispatch/SKILL.md`) (reason-not-auto: requires-judgment) | acknowledged | Continued per `auto` mode policy — no file overlap with this record's scope; rebase deferred to wrap-up/merge time if it still matters then |
| 2 | build | Dispatch Step 2's `/tmp/dispatch-*.json` temp files (pre-existing, and now 3 more inherit it: `dispatch-eligible.json`, `dispatch-native-query.graphql`, `dispatch-native-deps.json`) are not namespaced per run, so two concurrent dispatch firings can clobber each other's temp state mid-Step-2 | observation | Flagged by the whole-branch reviewer as pre-existing and out of scope for this record — worth a future `/claude-tweaks:capture` if it ever actually bites |
| 3 | test | Full `npm test` run: 1253/1254 passing. Sole failure is `tests/statusline.test.js`'s render-timing assertion (788ms, threshold 750ms) — this repo's own `assess-agent-autonomy` skill explicitly documents this exact test as a known flake (`failure-check` mode's Transient signatures list). Not touched by this record's diff. | observation | Pre-existing, documented flake — not a regression from this build |
| 4 | review | High: unguarded `gh api graphql` call under `work-links: native` can crash Step 2's entire queue-build on failure, not just native filtering — empirically reproduced (schema-invalid query returns no `data` key, downstream `.data.repository` throws uncaught) | open | Staged: `staged/review-1.patch` — awaiting Review Console decision (fix vs. document fail-loud as deliberate) |
| 5 | review | High: `buildNativeDependencyQuery` hardcodes `blockedBy`, but `capabilities-probe.js` treats it as interchangeable with count-only `issueDependenciesSummary` — a GHE host exposing only the latter would get `work-links: native` enabled by `/init` yet every query would fail schema validation | open | Staged: `staged/review-2.patch` — recommendation to file a follow-up `/claude-tweaks:capture` against `capabilities-probe.js`, not a patch to this diff |
| 6 | review/hindsight | [skill: flow] The aliased-batch GraphQL technique this record proved out has a second consumer: `flow/materialize.md`'s multi-record `work-links: native` resolution currently does one `gh api graphql` call per record instead of one batched call | deferred | Staged: `staged/reflect-1.md` — named, concrete target for a future `materialize.md` edit; not urgent |
| 7 | wrap-up | Near-miss: /review's Step 3 lens pass caught 2 real High findings the earlier whole-branch review had dismissed as Minor — confirms the pipeline's layered-review design works as intended | deferred | Staged: `staged/reflect-2.md` — process observation, no action needed |
