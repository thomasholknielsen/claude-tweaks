# Open Items — Trust fetch: batch native sub-issue enumeration and session-snapshot the result

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | build | Upstream shipped `plugin/bin/lib/issues/native-dependencies.js` (#538 extraction) after this record was shaped: pure query builder stays in `record.js`, network execution + throw-on-partial parse lives behind an injectable runner in a sibling module. The new sub-issues fetch should follow this seam (builder in `record.js` per spec; execution/parse alongside `fetchNativeDependencies`), and its throw-on-partial rule aligns with the spec's error ladder | observation | Pattern noted for the build; spec's Key Files remain correct |
| 2 | build | Residual from final-review fix wave: plugin/bin/fetch-sub-issues.js:16 header comment still asserts the retired "empty parent list through xargs safely" claim, corrected everywhere else — fix in simplify pass | fixed | Reworded in simplify commit 5be6ecaf (also hoisted probeSchema require, closing a deferred Task 3 minor) |
| 3 | build | Follow-up idea (out of scope this record): the two acceptance-scan exit-4 fallback REST loops (github-pr-scan-acceptance.md) still lack per-parent failure capture — pre-existing behavior, no snapshot to poison, but same silent-undercount shape trust-table's fallback now guards against; consider a capture record | accepted | Superseded by item 4's staged/review-1.patch — the same defect gets a fuller in-run fix at the Review Console (ledger narrowing under the unattended ceiling) |
| 4 | review | Three high Error findings (fallback loops unpaginated vs new prose parity claims; acceptance fallback loops lack failure capture — failed parent undercounts or vanishes) staged as staged/review-1.patch — supersedes item 3's capture-record idea with an in-run fix | deferred | Resolves at the Review Console's Pending review section (staged patch, validated with git apply --check) |
| 5 | review | Medium Convention finding auto-fixed: CLI runner now bounds gh calls (timeout 30000), commit 0e0d68c1 | fixed | gh-api-module-pattern's bound-every-remote-call rule |
| 6 | review | Low Error finding auto-fixed: exit 1/2 documented at all three prose exit-code branches, commit 0e0d68c1 | fixed | fail-loud path named instead of raw SyntaxError |
| 7 | review/skill | Lens 3a observation: sibling CLIs resolve-blockers.js and link-records.js share the unbounded-runner gap (pre-existing, out of scope this record) — candidate for a small follow-up sweep | deferred | Queue-write proposal staged as staged/wrap-up-queue-1.md — record creation resolves at the Review Console's Queue writes (ledger routing under the unattended ceiling) |
| 8 | review/hindsight | Structural: retry-ladder + canonicalization triplicated as prose snippets across trust-table + two acceptance sites — fold behind the CLI (staged/reflect-1.md, queue-write candidate); module-level, /claude-tweaks:deepen-shaped follow-up | deferred | Staged for Review Console Queue writes |
| 9 | review/hindsight | [skill: gh-api-module-pattern] Document the {byParent, retry} partial-result envelope vs throw-on-partial (#723) postures and the choice criterion (staged/reflect-2.md) | deferred | Applied directly by the Skills curation judge (commit 8aceabab) — reflect-2.md retained as the audit carrier |
| 10 | wrap-up | Residue: origin/worktree-record-550 merged but not deleted (remedy auto) | fixed | Deleted by a concurrent session before this run's fix; stale tracking ref pruned here |
| 11 | wrap-up | Residue: 5 clean-but-unarchived pipeline run dirs (records 380, 375, spec-1077-1078, record-355, backlog-standalone) — remedy auto | fixed | 2 archived by this run (record-375, backlog-standalone); 3 already archived by concurrent sessions/reconcile |
| 12 | wrap-up | Residue: PR #1133 open, head worktree-flow-spec-1097 (this run's own pr-first draft) | observation | This run's own in-flight artifact — settles via the Review Console's merge path, not residue cleanup |
| 13 | wrap-up | Residue probe: full test suite skipped by the sweep (--no-suite) | observation | Suite ran separately in this same wrap-up: 5420 pass / 0 fail (/tmp/flow-1097-wrapup-suite.log) |
