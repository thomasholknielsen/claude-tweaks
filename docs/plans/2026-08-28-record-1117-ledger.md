# Open Items — Ad-hoc run dirs #500's Friction lens depends on get silently auto-archived by the pre-existing 24h orphan-mint sweep (#1117)

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | review | Lens 3c (Error Handling), reproduction-confirmed by both dispatched agents + direct-verification: `isAdHocStandaloneMint`'s permanent exemption from `isOrphanedMint` has no eventual-cleanup path — once an ad-hoc-standalone run dir's worktree is reaped/removed (or it never gets a PR, or its PR merges without a `console.json` ever being rendered into that exact directory), the directory is permanently un-archivable by any mechanism in this codebase (`decideArchive` requires a resolved console + a still-live worktree registration; nothing writes `console.json` into an ad-hoc dir; `probePipelineRuns` only surfaces `status: 'clean'` dirs, which these can never reach). `#1117`'s own spec text specified "swept only once genuinely superseded" as half of the chosen design — only the permanent-exemption half shipped | deferred | Fails fix-now — needs a design decision among options (same framing #1117's own spec used). Filed https://github.com/thomasholknielsen/claude-tweaks/issues/1604, `Defer-reason: needs-human-decision` |
| 2 | review | Lens 3c (agent B): the residue backstop `probePipelineRuns` (`plugin/bin/lib/residue/probes/pipeline-runs.js:92`) never surfaces a permanently-stuck ad-hoc dir, since it only checks `status: 'clean'` — confirmed via direct source read | deferred | Same root cause as item 1 — folded into #1604's Acceptance Criteria rather than filed separately |
| 3 | review | Lens 3c (agent B): `session-start.js`'s "unfinished pipeline run(s) detected" nag re-fires on every future session for a permanently-stuck ad-hoc dir, with no resolution path — confirmed via direct source read | deferred | Same root cause as item 1 — folded into #1604's Acceptance Criteria rather than filed separately |
| 4 | review | Lens 3b (agent B): `isAdHocStandaloneMint` is a pure basename-suffix match with no corroborating `run-state.json`/`worktree`-field check — a malformed/non-genuine dir with the same suffix would also leak | deferred | Folded into #1604's Deliverables (corroborate the suffix match before exempting) rather than fixed as a standalone patch, since it's entangled with the same redesign |
| 5 | review | Lens 3c: `docs/hooks.md` and `plugin/skills/reflect/full-mode.md`'s new `#1117` prose overstates the merged-PR archival path as already covering "every other run dir" for ad-hoc dirs, which per items 1-4 is not structurally true in the common case | fixed | Softened the claim in both files to acknowledge the gap and point at #1604 — see this review's Actions Performed |
| 6 | review | Lens 3a (agent A): `work/1117-spec.md` (materialized spec file) contains the shaped body twice — once under the normal headings, once again verbatim under `## Original request` | accepted | Not a build defect — `materialize.md`'s documented `## Original request` verbatim-preservation convention (#1240); every materialized spec file has this shape by design |
| 7 | review | Lens 3f (agent B): new `#1117` regression test 1 (`isOrphanedMint` exemption) only exercises an aged dir, not a brand-new one, so it doesn't independently prove "regardless of age" | accepted | Info-severity, non-blocking — the exemption check precedes the age check in production code, so a fresh-dir case is not independently at risk; not worth a dedicated test for this shape |
| 8 | review/hindsight | Skill-worthy pattern: a sweep/TTL exemption needs a verified replacement lifecycle answer, not just removal of the old sweep — the general lesson item 1 illustrates | deferred | Staged: `staged/reflect-1.md`, proposed target `docs/donts.md` — surfaces at the Wrap-Up Review Console |
| 9 | wrap-up | Reflect (full, Friction): 3 `contract-violation` events, 0 `wd-deny`/`gate-denial`/`ask-user-question` (low, proportional volume). One self-inflicted — a placeholder wait-agent spawned mid-run cost a full dispatch for no value and logged a spurious violation | deferred | Routed D4 (memory) — staged: `staged/reflect-2.md`, surfaces at the Wrap-Up Review Console's Memory updates section |
| 10 | wrap-up | Reflect (full, Surprises/Approach/Near-misses/Fresh-start/Tradeoffs): no separate findings — the run's one substantive gap (ad-hoc dir eventual-cleanup) was already fully captured in review's Step 3 (item 1, #1604); no tradeoffs were accepted this run | accepted | Re-deriving would duplicate item 1 — anti-pattern per `reflect/SKILL.md` |
| 11 | wrap-up | Docs row (Phase 2): `docs/hooks.md`'s new `#1117` sentence also carried a stale claim that `config.yml` has a single writer (`/flow`'s Manifesto) — false, `set-config.js`'s `setConfigLever` also creates it | fixed | Applied directly (additive, high confidence) — commit `68534a3d6` |
| 12 | wrap-up | Skills row (Phase 2): the identical stale single-writer claim also exists in `plugin/skills/reflect/full-mode.md`, out of the Docs row's `docs/**` scope and out of the Skills row's `.claude/skills/` scope (`plugin/skills/` is the shipped product payload, not a project guidance skill) — flagged by both rows as orphaned | fixed | Fixed directly outside the registry rows — commit `6d3af3f64` |
| 13 | wrap-up | CLAUDE.md & rules row (Phase 2): staged a new `docs/donts.md` rule — "don't exempt a class from a TTL/orphan sweep on the strength of a named successor cleanup path without tracing that the class can actually reach it" (same lesson as item 8, formalized as a rule) | deferred | Staged: `staged/wrap-up-claude-md-1.md` — surfaces at the Wrap-Up Review Console |
| 14 | wrap-up | Memory row (Phase 2): staged the dummy-wait-agent memory file proposed by item 9's Friction finding | deferred | Staged: `staged/wrap-up-memory-1.md` — surfaces at the Wrap-Up Review Console's Memory updates section |
| 15 | wrap-up | Journeys row (Phase 2): `docs/journeys/route-a-declined-reflection-insight.md` checked against the diff — clean, no drift; no missing-journey gap (this work has no persona-facing flow) | accepted | No action needed |
| 16 | wrap-up | Residue sweep: PR #1603 (head `worktree-dispatch-record-1117`) is open | accepted | Expected residue at this point in the run — this record's own PR, whose merge/park disposition is Phase 4's own Review Console / auto-merge decision, not a dangling artifact |
