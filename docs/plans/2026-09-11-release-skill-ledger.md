# Open Items — release skill (#2251, #2252, #2253, #2254, #2255, #2256, #2258)

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | flow | Pre-flight branch-divergence: `origin/main` was 8 commits ahead of `worktree-design-release-skill` at run start (auto-continued per `auto` mode) | observation | Absorbed by the adopt-path catch-up merge of `origin/main` before any spec built |
| 2 | build | AC 1 names an integration-test fixture proving `git log --first-parent -1 --format=%s` yields `feat: {title} (#N)` after a real squash merge; the branch pins the merge sites by prose conformance instead (an agent performs the merge, not code) — `tests/merge-subject-composer-conformance.test.js` (defer-reason candidate: requires-judgment — whether a real-merge fixture is worth its cost) | deferred | Resolves through `staged/build-deviation-3.md` (console: reword AC 1 to the prose-conformance proof, or the reject branch files the real-merge fixture record) — wrap-up ledger gate |
| 3 | build | `tests/merge-subject-composer-conformance.test.js:23` hard-codes `PR_FIRST_BYTE_CEILING = 30904` from the spec's 30,404 baseline; the spec's Gotchas say the second lander (#2002 or this) re-baselines against the live size | deferred | Backlog proposal `staged/ledger-record-conformance-byte-ceiling-rebaseline.md` (defer-reason: blocked-dependency — the re-baseline belongs to whichever of #2002 / this lands second) — wrap-up ledger gate |
| 4 | build | `plugin/bin/lib/release/subject.js` truncation slices by UTF-16 unit — a hard cut through an astral character leaves a lone surrogate in the subject (theoretical for this repo's titles) | fixed | `cd0ef7785` — `truncateHead` backs the hard cut off one unit when it would split a surrogate pair; surrogate test added in `tests/bin-lib/release/subject.test.js` — wrap-up ledger gate |
| 5 | build | `plugin/bin/lib/compose-subject.js` exit code 1 covers both a malformed invocation and an uncomposable record; a distinct code would let a merge site park a shaping defect instead of treating it as a bad call | deferred | Backlog proposal `staged/ledger-record-compose-subject-cli-hardening.md` with rows 6 and 7 (defer-reason: needs-human-decision — exit-code vocabulary and timeout are contract choices) — wrap-up ledger gate |
| 6 | build | `plugin/bin/lib/compose-subject.js` `GH_TIMEOUT_MS = 5000` is the repo's single-call convention but tight for a `gh issue view` at merge time — a slow response becomes exit 3 | deferred | Folded into the row-5 backlog proposal `staged/ledger-record-compose-subject-cli-hardening.md` — wrap-up ledger gate |
| 7 | build | `plugin/bin/lib/compose-subject.js` `firstSentence` cuts at the first `.` followed by whitespace, so an Overview opening with "e.g. " or "v6.34. " truncates early | deferred | Folded into the row-5 backlog proposal `staged/ledger-record-compose-subject-cli-hardening.md` — wrap-up ledger gate |
| 8 | build | Cosmetic: `tests/bin-lib/compose-subject.test.js` requires `child_process` inline in the eval round-trip test; `parseArgs` number dedupe is untested/undocumented; `shaping-mode-stamping.md` Compatibility bullet quotes CLAUDE.md without an ellipsis for a dropped parenthetical | observation | Whole-branch review triaged all three as fine to defer; no behavior at stake |
| 9 | build/skill | `gh-api-module-pattern` gains a wrinkle: when skill prose consumes a CLI's `--shell` output, the guarded form `SUBJECT_EXPORTS=$(node … --shell) \|\| exit 1` then `eval "$SUBJECT_EXPORTS"` is required — a bare `eval "$(node …)"` swallows the CLI's exit status (`_shared/pr-first-merge.md` Step 3, all six merge sites) | observation | Enrichment candidate for the project skill; wrap-up Skills row evaluates |
| 10 | review | [3c, high] `plugin/skills/_shared/local-merge-auto-finish.md:114` — the composer call's `\|\| exit 1` is a bare, unlogged exit that contradicts the file's own line-110 invariant (every run ends in an explicit, logged outcome); neither the On-abort nor the On-any-failure paragraph covers it | open | — |
| 11 | review | [3c, high] `plugin/bin/lib/compose-subject.js:161` — `## Breaking Change` presence is validated only in aggregate across a bundle (`filter(Boolean)` before the join); a `breaking` sibling with no section is silently dropped when another member supplies a note, contradicting `shaping-mode-stamping.md:222`'s stated invariant; no mixed-bundle test | open | — |
| 12 | review | [3c, medium] `plugin/bin/lib/compose-subject.js:176` + `release/subject.js:19` — blanket `catch` returns exit 1 for any error; `usage()` throws a bare `Error`, so a genuine composer bug is reported as an uncomposable record (sibling CLIs use a named Error subclass and `instanceof`-gate it) | fixed | Named `ComposeSubjectError` raised by the composer; the CLI catch is `instanceof`-gated and rethrows anything else — `347f5782f` |
| 13 | review | [3c, medium] `plugin/skills/dispatch/settle-and-merge.md:330` — the section's recovery branch names only merge conflict and branch-guard abort; a composer failure (exit before any merge) is unnamed | fixed | Recovery trigger list names the composer `\|\| exit 1` failure — `9545d5b88` |
| 14 | review | [3c, medium] `plugin/skills/flow/worktree-merge.md:63` — the Merge Procedure's outcomes (succeeds / conflicts) never name a composer-failure outcome | fixed | New Merge Procedure item 2 names the composer-failure outcome; later items renumbered — `9545d5b88` |
| 15 | review | [3c, medium] `plugin/bin/lib/release/subject.js:46` — the empty-`migrationNote` error cites the subject (lowest) record's number while `breaking` is aggregated across the bundle, so it can blame the wrong issue | fixed | Empty-migrationNote error cites the breaking record numbers via a `breakingRecords` param — `347f5782f` |
| 16 | review | [3c, medium] `plugin/bin/lib/release/subject.js:41,44` — the unresolvable-`type` and empty-`title` errors omit the record number (the `:46` throw includes it), so a bundle's exit 1 cannot say which record failed | fixed | Type/title usage errors cite `for #{number}` — `347f5782f` |
| 17 | review | [3i, low] `docs/releasing.md` covers `plugin/bin/lib/release/**` (REGISTRY auto-detect) but was not updated in this work — build Step 6.5 judged `subject.js` does not change the release procedure until #2259 migrates it | observation | Review in wrap-up; informational |
| 18 | review/hindsight | Missing consolidation: `compose-subject.js:83` adds a third markdown-section extractor beside `decomposition-crossref.js:33` and `grouping.js:328` — staged: `reflect-1.md` (export one `extractSection` from `record.js`) | open | — |
| 19 | review/hindsight | Missing consolidation: duplicate `typeOf(record)` in `record-graph/encode.js:25` and `compose-subject.js:101`, pinned by test rather than shared — staged: `reflect-2.md` (move into `record.js`; fold with row 18) | open | — |
| 20 | review/hindsight | Tangential: lens 3a agent B returned findings with zero tool uses and nothing caught it — staged backlog candidate `reflect-3.md` (dispatcher reads the usage line, discards and re-dispatches once) [route: D3] | open | — |
| 21 | test | Post-fix re-verify at `9748dadd3`: `tests/impeccable-cli-contract.test.js` fails — the installed Impeccable version is 4.1.0 while the repo pins 3.6.0 (`tools/upstream-drift`); the file is outside this diff and the same suite passed 8234/0 twice earlier in this run, so the plugin updated mid-run — environment drift, not a regression. Re-pin by re-recording the fixtures against 4.1.0 (the `upstream-drift` procedure) or reinstall 3.6.0 (reason-not-auto: requires-judgment — re-pin vs reinstall is a maintainer call; changing a global install from a pipeline is out of scope) | deferred | Backlog proposal `staged/ledger-record-impeccable-cli-contract-repin.md` (defer-reason: needs-human-decision — re-pin 4.1.0 vs pin the installed version down) — wrap-up ledger gate |
