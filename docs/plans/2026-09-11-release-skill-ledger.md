# Open Items — release skill (#2251, #2252, #2253, #2254, #2255, #2256, #2258)

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | flow | Pre-flight branch-divergence: `origin/main` was 8 commits ahead of `worktree-design-release-skill` at run start (auto-continued per `auto` mode) | observation | Absorbed by the adopt-path catch-up merge of `origin/main` before any spec built |
| 2 | build | AC 1 names an integration-test fixture proving `git log --first-parent -1 --format=%s` yields `feat: {title} (#N)` after a real squash merge; the branch pins the merge sites by prose conformance instead (an agent performs the merge, not code) — `tests/merge-subject-composer-conformance.test.js` (defer-reason candidate: requires-judgment — whether a real-merge fixture is worth its cost) | deferred | Resolves through `staged/build-deviation-3.md` (console: reword AC 1 to the prose-conformance proof, or the reject branch files the real-merge fixture record) — wrap-up ledger gate |
| 3 | build | `tests/merge-subject-composer-conformance.test.js:23` hard-codes `PR_FIRST_BYTE_CEILING = 30904` from the spec's 30,404 baseline; the spec's Gotchas say the second lander (#2002 or this) re-baselines against the live size | deferred | Backlog proposal `staged/ledger-record-conformance-byte-ceiling-rebaseline.md` (defer-reason: blocked-dependency — the re-baseline belongs to whichever of #2002 / this lands second) — wrap-up ledger gate |
| 4 | build | `plugin/bin/lib/release/subject.js` truncation slices by UTF-16 unit — a hard cut through an astral character leaves a lone surrogate in the subject (theoretical for this repo's titles) | fixed | `cd0ef7785` — `truncateHead` backs the hard cut off one unit when it would split a surrogate pair; surrogate test added in `tests/bin-lib/release/subject.test.js` — wrap-up ledger gate |
| 5 | build | `plugin/bin/lib/compose-subject.js` exit code 1 covers both a malformed invocation and an uncomposable record; a distinct code would let a merge site park a shaping defect instead of treating it as a bad call | deferred | Backlog proposal `staged/ledger-record-compose-subject-cli-hardening.md` with rows 6 and 7 (defer-reason: needs-human-decision — exit-code vocabulary and timeout are contract choices) — wrap-up ledger gate |
| 6 | build | `plugin/bin/lib/compose-subject.js` `GH_TIMEOUT_MS = 5000` is the repo's single-call convention but tight for a `gh issue view` at merge time — a slow response becomes exit 3 | deferred | Folded into the row-5 backlog proposal `staged/ledger-record-compose-subject-cli-hardening.md` — wrap-up ledger gate |
| 7 | build | `plugin/bin/lib/compose-subject.js` `firstSentence` cuts at the first `.` followed by whitespace, so an Overview opening with "e.g. " or "v6.34. " truncates early | deferred | Folded into the row-5 backlog proposal `staged/ledger-record-compose-subject-cli-hardening.md` — wrap-up ledger gate |
| 8 | build | Cosmetic: `tests/bin-lib/compose-subject.test.js` requires `child_process` inline in the eval round-trip test; `parseArgs` number dedupe is untested/undocumented; `shaping-mode-stamping.md` Compatibility bullet quotes CLAUDE.md without an ellipsis for a dropped parenthetical | observation | Whole-branch review triaged all three as fine to defer; no behavior at stake |
| 9 | build/skill | `gh-api-module-pattern` gains a wrinkle: when skill prose consumes a CLI's `--shell` output, the guarded form `SUBJECT_EXPORTS=$(node … --shell) \|\| exit 1` then `eval "$SUBJECT_EXPORTS"` is required — a bare `eval "$(node …)"` swallows the CLI's exit status (`_shared/pr-first-merge.md` Step 3, all six merge sites) | observation | Enrichment candidate for the project skill; wrap-up Skills row evaluates |
| 10 | review | [3c, high] `plugin/skills/_shared/local-merge-auto-finish.md:114` — the composer call's `\|\| exit 1` is a bare, unlogged exit that contradicts the file's own line-110 invariant (every run ends in an explicit, logged outcome); neither the On-abort nor the On-any-failure paragraph covers it | open | — |
| 11 | review | [3c, high] `plugin/bin/lib/compose-subject.js:161` — `## Breaking Change` presence is validated only in aggregate across a bundle (`filter(Boolean)` before the join); a `breaking` sibling with no section is silently dropped when another member supplies a note, contradicting `shaping-mode-stamping.md:222`'s stated invariant; no mixed-bundle test | open | — |
| 12 | review | [3c, medium] `plugin/bin/lib/compose-subject.js:176` + `release/subject.js:19` — blanket `catch` returns exit 1 for any error; `usage()` throws a bare `Error`, so a genuine composer bug is reported as an uncomposable record (sibling CLIs use a named Error subclass and `instanceof`-gate it) | fixed | Named `ComposeSubjectError` raised by the composer; the CLI catch is `instanceof`-gated and rethrows anything else — `347f5782f` |
| 13 | review | [3c, medium] `plugin/skills/dispatch/settle-and-merge.md:330` — the section's recovery branch names only merge conflict and branch-guard abort; a composer failure (exit before any merge) is unnamed | fixed | Recovery trigger list names the composer `\|\| exit 1` failure — `9545d5b88` |
| 14 | review | [3c, medium] `plugin/skills/flow/worktree-merge.md:63` — the Merge Procedure's outcomes (succeeds / conflicts) never name a composer-failure outcome | fixed | New Merge Procedure item 2 names the composer-failure outcome; later items renumbered — `9545d5b88` |
| 15 | review | [3c, medium] `plugin/bin/lib/release/subject.js:46` — the empty-`migrationNote` error cites the subject (lowest) record's number while `breaking` is aggregated across the bundle, so it can blame the wrong issue | fixed | Empty-migrationNote error cites the breaking record numbers via a `breakingRecords` param — `347f5782f` |
| 16 | review | [3c, medium] `plugin/bin/lib/release/subject.js:41,44` — the unresolvable-`type` and empty-`title` errors omit the record number (the `:46` throw includes it), so a bundle's exit 1 cannot say which record failed | fixed | Type/title usage errors cite `for #{number}` — `347f5782f` |
| 17 | review | [3i, low] `docs/releasing.md` covers `plugin/bin/lib/release/**` (REGISTRY auto-detect) but was not updated in this work — build Step 6.5 judged `subject.js` does not change the release procedure until #2259 migrates it | observation | Review in wrap-up; informational |
| 18 | review/hindsight | Missing consolidation: `compose-subject.js:83` adds a third markdown-section extractor beside `decomposition-crossref.js:33` and `grouping.js:328` — staged: `reflect-1.md` (export one `extractSection` from `record.js`) | open | — |
| 19 | review/hindsight | Missing consolidation: duplicate `typeOf(record)` in `record-graph/encode.js:25` and `compose-subject.js:101`, pinned by test rather than shared — staged: `reflect-2.md` (move into `record.js`; fold with row 18) | open | — |
| 20 | review/hindsight | Tangential: lens 3a agent B returned findings with zero tool uses and nothing caught it — staged backlog candidate `reflect-3.md` (dispatcher reads the usage line, discards and re-dispatches once) [route: D3] | open | — |
| 21 | test | Post-fix re-verify at `9748dadd3`: `tests/impeccable-cli-contract.test.js` fails — the installed Impeccable version is 4.1.0 while the repo pins 3.6.0 (`tools/upstream-drift`); the file is outside this diff and the same suite passed 8234/0 twice earlier in this run, so the plugin updated mid-run — environment drift, not a regression. Re-pin by re-recording the fixtures against 4.1.0 (the `upstream-drift` procedure) or reinstall 3.6.0 (reason-not-auto: requires-judgment — re-pin vs reinstall is a maintainer call; changing a global install from a pipeline is out of scope) | deferred | Backlog proposal `staged/ledger-record-impeccable-cli-contract-repin.md` (defer-reason: needs-human-decision — re-pin 4.1.0 vs pin the installed version down) — wrap-up ledger gate |
| 22 | build | #2252 spec premise: proof (b) keyed on a `(#PR)` subject suffix can never match the composer's record-number suffix — built as the confirmed PR's `mergeCommit.oid` on the bounded first-parent history (staged `spec-2252/staged/build-deviation-1.md`) | deferred | Resolves through `spec-2252/staged/build-deviation-1.md` (console: reword #2252's proof to the mergeCommit design, or the reject branch files a record-number mapping follow-up) — wrap-up ledger gate |
| 23 | build | #2252 spec premise: the two-signal proof lacked containment of the branch's current tip — a post-merge push would still be pruned on origin (whole-branch review Critical, reproduced); built with a third condition, `merge-tree --write-tree {mergeCommit^} {branch}` tree-equality (staged `spec-2252/staged/build-deviation-2.md`) | deferred | Resolves through `spec-2252/staged/build-deviation-2.md` (console: reword #2252's proof to three conditions; reject is not recommended — it re-opens the pushed-delete hazard) — wrap-up ledger gate |
| 24 | build | `archive-branches.js`: a squash-merged local branch whose remote ref is gone (screen `null`) and whose tip is younger than 14 days reads `too-young` until it ages — the confirm (where `mergeCommit` arrives) only runs for destructive or MERGED-screened candidates; delayed convergence, safe direction | deferred | Backlog proposal `spec-2252/staged/ledger-record-archive-squash-recall-gap.md` (defer-reason: needs-human-decision — one gh call per young orphaned branch per pass vs a bounded 14-day delay) — wrap-up ledger gate |
| 25 | build | `squash-provenance.js` recall limits, all fail-safe and documented in its header: git < 2.38 (no `merge-tree --write-tree`) and `runGit` indeterminate failures (timeout, no git) both read as not proven, indistinguishable from a real `git-error`; a mismatching recreation writes unreachable tree/blob objects reaped by `gc --auto` | observation | Documented in the module header and `docs/reconcile-checks.md`; no action |
| 26 | build/skill | `/build` runs Common Step 5 (verification) before Step 6 (journeys), so a journey-doc edit is never covered by the build's own suite run — #2252's Origin trailer broke `tests/tidy-report-rules.test.js`'s 100-char rule and surfaced only at `/test`; `_shared/journey-self-review.md` has no conformance-test check either | observation | Fixed at a34e24b83 (section moved); skill-side: consider running the repo's doc-conformance tests (or a targeted `node --test` on tests naming the journey) inside `/journeys` Step 3.5, or ordering Step 6 before Step 5 — surfaces at the console |
| 27 | review | [3f, medium, low-confidence] `tests/bin-lib/reconcile/squash-provenance.test.js:85` — the "main moved" test does not by itself discriminate the tree-equality condition (single-agent finding, not reproduced; controller reads it as a false-negative guard for the merge-recreation implementation) — staged `spec-2252/staged/review-low-confidence-1.md` | open | — |
| 28 | flow | Ceremony source drift in the multi-spec run: `spec-2252/config.yml` (copied from the parent) reads `ceremony-profile: standard` while #2252's label and materialized header read `fast-lane`; build (plan audit, alignment) and review (Steps 1, 1.6, 4) skipped on the label, the wrap-up escape hatch found nothing to downgrade — the skipped steps were covered by the SDD whole-branch review and this wrap-up's promise-register update, but the per-spec config should carry the record's own ceremony | observation | Surfaced for the console; skill-side candidate: `/flow` multi-spec scaffold should write each spec's ceremony into `spec-{N}/config.yml` from the materialized header |
| 29 | build/skill | `run-directory-fact-packs` divergence: `plugin/bin/release-bootstrap.js` (#2253) is a decision CLI — one verdict envelope `{verdict, …}`, exit 0 on every verdict like a fact pack, but its module reads the filesystem directly (only `listTags` is injectable) rather than routing every fs/git call through a `deps` seam; a third consumer of the stack table (units 4/5) should decide whether to lift it into the fact-pack shape | observation | For the batch curation Skills row |
| 30 | build/skill | `parse-signal-discipline` divergence: `rootEntries` in `plugin/bin/lib/init/release-bootstrap.js` swallows a `readdirSync` failure into `[]`, so an unreadable or missing `--root` reads as an empty repo (`fresh`, then `mkdir -p` springs the path into existence) — the exact could-not-read ≡ nothing-there collapse the skill names; the reviewer flagged it Minor (no AC covers it) | open | — |
| 31 | build | #2253 spec gap (whole-branch review Important 3): the design doc's Phase 3 acceptance 3 — this repo's own init run chooses `simple` + `plugin/.claude-plugin/plugin.json` — was dropped at decomposition; under the record's single-row rule this repo resolves `node` seeded from the stale root `package.json` (6.70.0 vs 6.121.0 shipped). Unit 8 (this repo's migration) needs an explicit override — `--release-type`/`--extra-file` on the CLI, or a nested-plugin-manifest-outranks-root-package.json rule (staged `spec-2253/staged/build-deviation-2.md`) | deferred | Backlog proposal `spec-2253/staged/ledger-record-release-type-override.md` (defer-reason: blocked-dependency — parked on #2253 merged; consumed by #2259) — wrap-up ledger gate |
| 32 | build | Conflict-marker gaps vs real tooling, scope-faithful to the spec's enumerated list: bare `goreleaser.yaml`/`.yml` (no leading dot), semantic-release configured under a `release` key in `package.json`, `standard-version`/`.versionrc*` — none detected today | deferred | Backlog proposal `spec-2253/staged/ledger-record-conflict-marker-gaps.md` (defer-reason: genuinely-larger) — wrap-up ledger gate |
| 33 | build | `plugin/skills/_shared/policy-schema.md` is at 40,539 of the 40,960-byte per-file ceiling after #2253's two rows and their PAT/quoting notes — 421 bytes of headroom for the next key; the release family (units 4-6) adds more rows | observation | Warn-only per-file tier since #1990; the next lever addition should plan a split or a move to `policy-schema-coverage.md` |
| 34 | review | [3b, high] `plugin/bin/lib/init/release-bootstrap.js:192` — `branch` is interpolated verbatim into the generated workflow YAML; a newline-bearing value (an unvalidated `--branch`, a raw policy string, CLAUDE.md prose) injects triggers/jobs into a workflow granted `contents: write` + `pull-requests: write` (both agents reproduced) | open | Staged `spec-2253/staged/review-1.patch` (branch-name validation in the renderer, bootstrapRelease and the CLI; Staged-at 76d60a145) — console applies |
| 35 | review | [3b, high] `plugin/bin/lib/init/release-bootstrap.js:250` — on `fresh` the manifest and (pr-first) workflow are pushed unconditionally; only the config is `configShaped`-gated, so a hand-written manifest or workflow with no config yet is silently clobbered (both agents reproduced) | open | Staged `spec-2253/staged/review-2.patch` (never overwrite a file the run did not create → `conflict (partial files)`; Staged-at 76d60a145) — console applies |
| 36 | review | [3b, high] `plugin/bin/lib/init/release-bootstrap.js:256` — `mkdirSync` recursive + `writeFileSync` follow an existing symlink at any segment (a symlinked `.github`), writing outside `root` with no realpath/boundary check while `written` reports the intended relative path (both agents reproduced) | open | Staged `spec-2253/staged/review-2.patch` (lstat every path segment, refuse symlinks, realpath boundary against the real root) — console applies |
| 37 | review | [3c, high, direct-verified] `plugin/bin/lib/init/release-bootstrap.js:104` — `already-bootstrapped` never checks the workflow for the current `integrationModel`: a `local-merge` bootstrap re-run under `pr-first` skips the workflow forever with `written: []` | open | Staged `spec-2253/staged/review-2.patch` (detectReleaseProcess takes integrationModel; a pr-first re-run of a local-merge bootstrap writes only the missing workflow) — console applies |
| 38 | review | [3c, medium, severity contested A medium / B high] `plugin/bin/lib/init/release-bootstrap.js:218` — `defaultListTags` collapses every `git tag -l` failure into `[]`, identical to no tags; the manifest silently seeds from the stack file or `0.1.0` with no signal, and the prose documents only the fallback chain | fixed | `76d60a145` — defaultListTags returns {tags, failure}; the envelope carries `tagsFailure` on fresh; prose and docs name it |
| 39 | review | [3c, medium, direct-verified] `plugin/bin/lib/init/release-bootstrap.js:100` — `readJson` swallows EACCES like ENOENT/malformed, so a present-but-unreadable config is misreported as `conflict / release-please (foreign config)` | fixed | `76d60a145` — readJson distinguishes ENOENT / unparseable / read error; an unreadable config reports `conflict — release-please (config unreadable: <code>)` |
| 40 | review | [3c, low, low-confidence] `plugin/bin/release-bootstrap.js:26` — `--branch ""` passes the value-flag check and is coerced to `main` by the renderer; staged `spec-2253/staged/review-low-confidence-1.md` | open | — |
| 41 | review | [3a, low, low-confidence] `plugin/bin/release-bootstrap.js:16` — the empty string in `VALID_INTEGRATION_MODELS` reads as unreachable; controller: the premise is wrong (an explicit empty value is reachable and maps to `skipped` by design); staged `spec-2253/staged/review-low-confidence-2.md` | open | — |
| 42 | review/hindsight | Structural debt: `bootstrapRelease` accretes write-safety branches across three review rounds — split a writer helper (`writeBootstrapFiles`) via `/claude-tweaks:deepen` once review-2.patch lands; staged `spec-2253/staged/reflect-1.md` | open | — |
| 43 | review/hindsight | Convention drift: name-keyed filesystem checks without an `lstat` type check, twice in one build (directory named `package.json`; symlinked `.github`) — Don't-repeat candidate [claude-md: fs-type-check]; staged `spec-2253/staged/reflect-2.md` | open | — |
| 44 | review/hindsight | Tangential: project skill for the prose-canonical table pinned to a code twin (two instances this run) — staged backlog candidate `spec-2253/staged/reflect-3.md` [skill: NEW - prose-code-twin-pin] [route: D3] | open | — |
| 45 | plan (#2254) | `git describe --contains HEAD` prints `v1.3.0^0` (not `v1.3.0`) when HEAD is the tagged commit itself — probed on git 2.55; the design doc Phase 4 acceptance and the spec AC 2 quote the bare tag name; the acceptance test asserts `/^v1\.3\.0(\^0)?$/` | observation | Plan AC 2 accepts both forms; wording staged as `spec-2254/staged/build-deviation-1.md` |
| 46 | plan (#2254) | plan-audit Check A requires a `Create:` path's parent directory to exist, so a plan introducing a new module directory (`plugin/bin/lib/release-local/`) cannot pass until the directory is created by hand — done empty before the audit | observation | Harness limitation noted for the console; `mkdir -p` before the audit is the workaround |
| 47 | plan (#2254) | Ruling 5: the local engine leaves `java`/`ruby`/`dotnet` manifest edits unsupported (exit 2 naming the type, pointing at `simple` + `extra-files`); `go` is tag-only. Step 21's stack table resolves these types for release-please, which has per-language updaters the local engine does not model | open | Staged as `spec-2254/staged/build-deviation-2.md` — the console picks amend-the-record, a follow-up for the three updaters, or both |
| 48 | build (#2254) | Task 1 review: an unconventional subject whose `BREAKING CHANGE:` footer has empty text yields an empty breaking note (the conventional path falls back to the description) — plan-authored, inert | fixed | Wrap-up fix wave W1 (67eac2c7c) — the footer-empty case falls back to the subject on the unconventional path too |
| 49 | build (#2254) | Task 4 review: `GITHUB_REMOTE_RE` is a false negative on a userinfo-prefixed (`https://token@github.com/o/r`) or uppercase-host origin — silently degrades to the unlinked CHANGELOG form | fixed | W2 (9787f94e0) — userinfo-prefixed and case-insensitive GitHub origins parse; the canonical url never carries credentials |
| 50 | build (#2254) | Task 6 review: `release-hook: false` in policy.yml runs the literal command `false` (the key's schema type is string) — a user meaning "disabled" gets exit 5 | fixed | W3 (e5d9ab6dd) — `release-hook: false/off/none/null` reads as unset |
| 51 | build (#2254) | Task 6 review: the dry-run `manifest:` line is existence-based, so a present file with no version token previews cleanly and throws live (the partial state then names exactly what landed) | fixed | W4 (9d6ead788) — `manifest.plannedWrites` drives the dry-run line from token presence |
| 52 | build (#2254) | Task 6 review: `policy.yml` is read once per key and `remote get-url origin` twice per run — duplicate reads, no behaviour impact | fixed | W5 (955f741bc) — policy.yml read once per run; the origin URL was already read once at 277da3a69 |
| 53 | build (#2254) | Task 3/5 reviews: `spliceMatch` indirection for the `text` kind, the TOML section boundary stops at any column-0 `[` (a multi-line array continuation would end the section early — fails safe), `tsvTip` function/property naming echo | fixed | W6 (6940c1ef1) — spliceMatch indirection removed, TOML boundary matches real table headers only, collectClaims locals renamed |
| 54 | build (#2254) | Task 7 review: `docs/plugin-structure.md`'s line-18 parenthetical CLI list also omits `release-bootstrap` (pre-existing from #2253) | fixed | 7bf7bad7c — `release-bootstrap` added to the plugin-structure CLI roster |
| 55 | build (#2254) | Whole-branch review Critical: the push-failure recovery `git pull --rebase … && git push origin b vX` rebased the release commit and pushed a tag pointing at the orphaned pre-rebase object | fixed | Final fix wave F1 — re-tag after the rebase, force-push the tag |
| 56 | build (#2254) | Whole-branch review Important: `git checkout -- <paths>` restores from the index, leaving the staged bump after a commit failure | fixed | F2 — `git restore --staged --worktree`, plus `rm` for files the run created |
| 57 | build (#2254) | Whole-branch review Important: `spliceJsonKey` is first-occurrence, so a nested `version` key preceding the root one is bumped and the root left stale — silent, compounding | fixed | F3 — depth-aware root-key splice, ruling 9 generalised |
| 58 | build (#2254) | Whole-branch review Important: the generic `x-release-please-version` updater rewrote only the first annotated line | fixed | F4 — every annotated line |
| 59 | build (#2254) | Whole-branch review Important: `python` required `pyproject.toml` though Step 21 selects `python` from `setup.py` too — a `setup.py`-only repo fails every release after writing the manifest | fixed | F5 — optional `pyproject.toml`/`setup.py`/`setup.cfg`, pre-pass requiring one token-bearing file |
| 60 | build (#2254) | Whole-branch review Important: an `origin` whose branch was never pushed made the first release die on `fetch origin main` — the backup-remote shape the spec's Non-goals keep in scope | fixed | F6 — `ls-remote --heads` probe, precheck without origin, push without the ancestry check |
| 61 | build (#2254) | Whole-branch review: no real-git coverage of push/tag-push/hook, and ruling 2 (hidden types render when breaking) pinned only by the implementation's own belief | fixed | F7 — bare-origin acceptance tests; third fixture `release-please-15.0.0.md` (a breaking `refactor`) pinned by sha256 |
| 62 | build (#2254) | Whole-branch review Minor batch: `git add` without `--`, `remoteUrl` blanket catch, config errors exit 1 instead of 2, dead `nextVersionFor` and unused `from` parameter, `extra-files` path containment, single-quoted TOML versions | fixed | F8 |
| 63 | build (#2254) | Whole-branch review Minor: `version.txt` is created without a trailing newline — release-please's `simple` updater behaviour not verified against source this session | fixed | W7 (45254944f) — verified against release-please `DefaultUpdater.updateContent` (`version + \n`, whole-file overwrite): `version.txt` is now written as `X.Y.Z\n` on create and bump |
| 64 | build (#2254) | Whole-branch review Minor: `main()` parses argv twice (the `--root` pre-parse) and discards `opts.error`, which `run` re-derives — harmless duplication | observation | A design nit with no defect — `main()` pre-parses only `--root`; fold into `run` via `deps.cwd` when the file is next touched |
| 65 | review (#2254) | 3b (both agents): the `extra-files` escape check is string-only and the write sink `fs.writeFileSync(path.join(root, p))` follows symlinks — a committed symlink at an extra-files or stack-manifest target writes outside the repo root (IL-150 class) | open | Staged `spec-2254/staged/review-1.patch` — O_NOFOLLOW write + realpath containment of the parent directory, with real-fs tests; the console applies it — patch re-captured at 45254944f after the wrap-up fix wave (both patches `git apply --check` clean alone and chained) |
| 66 | review (#2254) | 3c agent B (reproduced by probe): `applyVersion` throws for an optional one-of target that exists without a version token (a `pyproject.toml` holding only tool config beside a `setup.cfg` that carries the version) — after `.release-please-manifest.json` was already written | open | Staged `spec-2254/staged/review-2.patch` — one-of members present without a token are skipped once another member carries it — patch re-captured at 45254944f after the wrap-up fix wave (both patches `git apply --check` clean alone and chained) |
| 67 | review (#2254) | 3c agent A (traced): a `create:true` `version.txt` that exists without a semver token is silently skipped and the release proceeds with the file out of sync with the tag | open | Same `review-2.patch` — an existing create target without a token throws before any write — patch re-captured at 45254944f after the wrap-up fix wave (both patches `git apply --check` clean alone and chained) |
| 68 | review (#2254) | 3a + 3c (both pairs): `listPlanFiles` used `fs.existsSync` then `fs.readdirSync` (IL-146) — copied from `plugin/bin/release.js:126-130`, which still carries the pattern | fixed | Read-and-catch at the review auto-fix commit; the `release.js` twin is pre-existing and out of scope — Phase 8 (#2259) retires that call site |
| 69 | review (#2254) | 3a agent B only: `--end-of-options` before `origin/{branch}` in `run.js:26` — premise refuted (the value can never start with a dash) | open | Staged `spec-2254/staged/review-low-confidence-1.md` — dismiss or fold into a future `run.js` touch |
| 70 | plan (#2255) | The record's exit-3 wording ("no origin") contradicts its own AC 3; ruling 1 keeps exit 3 structural (not a checkout / run dir not anchored) | observation | Staged `spec-2255/staged/build-deviation-1.md` (update the spec) |
| 71 | build (#2255) | Task 2: the plan's `isReleasePr` title regex matched `chore: release notes` against its own test | fixed | Ruling 9 — the title form requires a version digit |
| 72 | build (#2255) | Task 4 + whole-branch review: the real-binary "gh absent" tests put git's directory on PATH, which on Homebrew also holds gh — gh was spawned for real and failed only for lack of origin | fixed | Fix wave F1 — a scratch bin dir with symlinks to git and node only; the acceptance test asserts the ENOENT text |
| 73 | build (#2255) | Whole-branch review Critical: `hook` tested `release:` and `published` independently over the whole file — a job named `release` was a hook, flow-form and bare `on: release` were not | fixed | F3 — an `on:`-block scanner (ruling 10) |
| 74 | build (#2255) | Whole-branch review Critical: `ciTip` paired the local `origin/{branch}` sha (never fetched) with live check data; the plan's field trace called its freshness "none" | fixed | F4 — check-runs queried by branch name, `per_page=100`, `tipBehind` and `truncated` recorded (ruling 11) |
| 75 | build (#2255) | Whole-branch review Important: `engine` read policy.yml only, ignoring a run's pinned `config.yml` (`_shared/integration-model.md` run-scoped stability); a third reimplementation of `resolvePolicyConfig`'s orchestration | fixed | F5 — `resolvePolicyConfig({runDir})`, run-config source accepted |
| 76 | build (#2255) | Whole-branch review Important: `proposedVersion`'s base diverged from the engine's precheck base and fell to `0.0.0` on this tagless repo (proposing `0.1.0` at 6.121.0) | fixed | F6 — max(highest v* tag, manifest at tipRef, first-parent tag) with `baseSource`, degrade when none (ruling 12 supersedes ruling 7) |
| 77 | build (#2255) | Whole-branch review Important: the default git runner had no maxBuffer/timeout — ENOBUFS on this repo's first-parent log (1,072,505 bytes) today | fixed | F7 — `EXEC_OPTS` on the git runner; verified by a live run against this checkout |
| 78 | build (#2255) | Whole-branch review Important: `openReleasePrConflict` inspected only the last PR commit (bot→human→bot read false) and reported a commit with no authors as `true` (a guess) | fixed | F8 — any non-bot commit flags; missing authorship degrades; dependency errors carry the `X unresolved:` prefix |
| 79 | build (#2255) | Whole-branch review Important: `gh pr list --limit 50` could read `none` on a busy repo | fixed | F9 — limit 200 and an explicit inconclusive error at the page boundary |
| 80 | build (#2255) | Whole-branch review Important: an unexpected throw exited 2 (usage) and the policy/root/tip preamble sat outside the envelope discipline | fixed | F10 — preamble failure degrades every probe with the pack still written; a crash exits 1 like the siblings (ruling 13) |
| 81 | build (#2255) | Whole-branch review Minors: manifest regex → JSON.parse; `breakingNote` dropped from `unreleased`; `--only ,` writes an empty pack; run-directory-fact-packs skill says two packs are shipped | fixed | F11 (the `--only ,` gap also exists in `wrap-up-pack.js` — out of scope, noted) |
| 82 | build (#2255) | Whole-branch review Minor: the scratch fallback is a fresh `os.tmpdir()` mkdtemp per invocation (path on stderr), not `bin/lib/session-tmp.js`'s session scratch the record's wording may have meant | observation | The pack is on stdout; revisit if the skill (#2256) wants a deterministic path |
| 83 | build (#2255) | Whole-branch review recommendation: under pr-first `releasePr.title` carries release-please's own number and nothing reconciles it with `proposedVersion` — two sources, no reconciliation | open | Handed to #2256 through the promise register (F5 amended at #2255's wrap-up): reconcile `proposedVersion` with `releasePr.title` under pr-first, or cut the field there |
| 84 | build (#2255) | Task 4 review (out of scope): `flow-preflight.js` exits 1 on a crash while its header documents 0/2/3 — a pre-existing inconsistency in a sibling this record did not touch; `wrap-up-pack.js` shares `--only ,` | deferred | Backlog proposal `spec-2255/staged/ledger-record-fact-pack-siblings.md` (defer-reason: pre-existing-outside-diff) — wrap-up ledger gate |
| 85 | build (#2255) | Task 2 review Minor: `env` was listed in the pack's `deps` interface but never read (the CLI reads env itself); `execFileAsync` carries an unused `opts` | observation | Interface text only; harmless |
| 86 | build (#2255) | Live run on this checkout: `unreleased` reads 2,469 first-parent commits of which 2,391 are unconventional (`{Verb} {what}` subjects), so `proposedVersion` degrades with "nothing to release" — a truthful reading of a history that predates the merge-time composer; `ciTip.tipBehind` was `true` (the local `origin/main` lags) | observation | #2259's migration decides where the conventional boundary starts (the design doc's bootstrap-version rule); #2256's Step 2 should say "no conventional commits since {lastTag}" rather than "nothing to release" when every commit is unconventional |
| 87 | build (#2255) | Re-review: the `on:` scanner missed a quoted `"on":` / `'on':` key (YAML 1.1 boolean dodge) — a false negative | fixed | 21d4a88e7 (fix-now at the re-review) |
| 88 | build (#2255) | Fix wave F7's premise was 20 KB short — this repo's first-parent log is 1,027,781 B against `execFileSync`'s 1,048,576 default, so no ENOBUFS RED existed; the 32 MB bound was kept | observation | A repo one long-bodied merge away from the default buffer is exactly why the bound stays |
| 89 | boundary 5→6 | origin/main moved 4 commits with 12 overlapping run-modified files and 3 additive content conflicts (this run's `breaking` facet beside main's `bot.parked`; adjacent CLI roster lines) — the registered HARD-GATE fired; the user chose merge + re-validate; merged at de64b4bc2, both sides kept, 1,106 affected tests green, remaining Key Files untouched | observation | Premises for #2256/#2258 hold; the console sees the merge commit in the run's history |
| 90 | plan (#2256) | `--as {version}` under local-merge is unsupported in this unit — `bin/release-local.js` derives the version from history and has no override; `execute.md` refuses `--as` there with a pointer (a breaking commit, or a manual tag) | open | Follow-up: a `--release-as` flag on the local engine (one plan claim to feed precheck, a manifest/CHANGELOG version override) — sized like one engine task |
| 91 | plan (#2256) | `tests/skill-catalog-completeness.test.js` pins every shipped skill into help/reference-card.md, help/context-flow.md and docs/getting-started.md — rows the record assigned to #2257 (/help, README) but which fail the suite the moment `plugin/skills/release/SKILL.md` exists | observation | Ruling 2: the three pinned rows land here; /help's diagrams and README's lifecycle diagram stay #2257's |
| 92 | build (#2256) | The skill description corpus landed 231 chars over the 7,900 ceiling with the release description already trimmed to 260/260 (keywords intact) — `DESCRIPTION_TOTAL_CEILING_CHARS` raised to 8,200 per the file's own per-new-skill precedent (ruling 11); the description itself has zero headroom | fixed | Ceiling raised in Task 7 (a1425d275); the next wording touch on the release description must re-measure |
| 93 | build (#2256) | Task 5 review: the version Steps 6–7 verified and booked was the pack's `proposedVersion`, which neither engine is bound to (the pack never fetches; release-please reads config the pack does not) — a sibling release between Step 1 and Step 5 would have produced a false `PARTIAL` and mis-numbered `Shipped in` comments | fixed | Ruling 14: gating version vs shipped version, reconciled once after the engine returns (cbcf9b8db, 0704f53c7) |
| 94 | build (#2256) | Task 5 review: the release PR was merged on the console's CI row alone — `ciTip` is the integration-branch tip, not the PR, and the console renders CI without gating on it — so a clean review would have merged release-please's PR with its own checks red | fixed | Ruling 15: `execute.md` reads the PR's `statusCheckRollup` and classifies per `pr-first-merge.md` Step 2.5 against the `merge-verification` lever; the release never arms `--auto` (0704f53c7) |
| 95 | build (#2256) | Engine exit `1` was mapped to `PARTIAL` unconditionally although `run.js` uses it for both "nothing written" and a named partial state; the no-origin fallback keyed on a stdout line the engine never prints; `--dry-run` under local-merge skipped the engine and lost its manifest-drift warning | fixed | Rulings 13 and 16: a fifth `failed` outcome; exit 1 splits on its stderr prefix; the fallback keys on `git remote get-url origin`; dry-run passes through to the engine (74dd1cbd5, 0704f53c7) |
| 96 | build (#2256) | The `--as`-under-local-merge refusal (row 90) was placed in Step 5, after the whole-branch review and console, for a run that could never proceed; its log status `REFUSED` would have rendered as an unresolvable queue-write row in the wrap-up console | fixed | Ruling 17: decided in Step 1 right after the pack, logged `SKIP`; `execute.md` keeps a back-stop (cbcf9b8db) |
| 97 | build (#2256) | Re-review of the Task 5 fix round: an exact `git ls-remote --tags origin v{version}` pattern suppresses the peeled `^{}` line, so the annotated-tag sha the hook probe filters on would never appear; the engine ran in the cwd while every probe was anchored to `$RUN_ROOT` | fixed | Glob pattern with an exact-ref filter; `release-local.js --root "$RUN_ROOT"` at both call sites (a91b4eb93) |
| 98 | build (#2256) | Task 8 review: the controller's own resolution ("actionable = `## Step` sections and `## Next Actions`") left `console.md` and `bookkeeping.md` entirely unscanned — neither carries a `## Step` heading — and the reviewer's planted bare `/review` went undetected | fixed | Ruling 18: sub-files are actionable by construction except the preamble and Anti-Patterns; line numbers in violations; controller re-probed `bookkeeping.md:17 (## Inputs): /review` (e5ee3fe7d) |
| 99 | build (#2256) | Task 1's `base:<ref>` argument-hint change and the new skill's Anti-Patterns table tripped two repo-wide pins only visible in the full suite — the reference-card `Takes` column for `/claude-tweaks:review` and the Anti-Patterns row-count pin (403 → 411) | fixed | Both synced at c00c3a11e; lesson: a new skill or an argument-hint edit needs the full suite before its task is called complete, not only the task's own pin tests |
| 100 | build (#2256) | Three SKILL.md sentences (integration-model Consumer table, auto-mode-contract HARD-GATE list, autonomy-ceiling `train` row) were false at Task 3's commit and only became true at Task 7 — no test guards a forward-dated citation | observation | Task 7 closed the merge gate (a1425d275); the reviewer of a multi-task skill build should list forward-dated citations explicitly |
| 101 | build (#2256) | Whole-branch review: `bookkeeping.md` booked the *effective* (gating) version where every other file requires the *shipped* one; the autonomy-ceiling `train` row told the train to stage `release-held.md` on a failed publish hook — the exact misreport AC 9 forbids; a non-`--train` HARD-GATE stop had no outcome word; `$RUN_ROOT` was used in runnable commands but never resolved; the standalone run-dir mint was unregistered in `pipeline-run-dir.md` and the Component-Skill Contract keyed on a `$PIPELINE_RUN_DIR` Step 3 itself sets | fixed | Rulings 19–21 + one fix wave (see the fix-wave commit); scoped re-review ADDRESSED |
| 102 | build (#2256) | The preflight pack (and `bin/lib/release-local/bump.js`) derive `major` for any breaking commit; release-please does not bump `0.x` to `1.0.0` on a breaking change (`bump-minor-pre-major`), so on a `0.x` pr-first repo the console renders a major and the HARD-GATE holds a release that is not one | open | Pack/engine-side (#2254/#2255 behaviour, outside this diff); a `0.x` pre-major rule in `bump.js` + the pack, sized like one engine task — Defer-reason: pre-existing-outside-diff |
| 103 | build (#2256) | Medium/low findings staged by Step 3's whole-branch review are surfaced by no step of the release skill (a release run has no Review Console); they survive only through the ledger row `/review` writes | observation | Acceptable for this unit: `/review` owns its ledger row and the staged files stay in the run directory; a release-run console is #2257's lifecycle wiring to decide |
| 104 | review (#2256) | Lens 3b (direct-verified, refutation stood): the release PR's title is never re-read before the merge — release-please re-renders its PR as commits land, and after Step 3's review plus up to 15 min of check-waiting an unattended run can merge a PR that re-rendered to a major bump the Step 4 HARD-GATE never saw; the `--as` path polls the title, the default path does not (`execute.md` ~L98-102) | open | high → staged as `staged/review-1.patch` (re-read `title` with the PR-check read; a changed bump part re-runs Step 4's gate — major → `HELD`, else proceed on the re-rendered version, logged) |
| 105 | review (#2256) | Lens 3c (direct-verified, refutation stood): an `--as`/shipped-version mismatch under pr-first is reported as `failed` although the branch only runs after the merge landed — contradicting `execute.md`'s own taxonomy and `SKILL.md` Step 8's `failed` definition; the tag is in flight, nothing is verified or booked (`execute.md` ~L34, `SKILL.md` ~L30/L213) | open | high → staged as `staged/review-2.patch` (the outcome is `PARTIAL` with "override did not take" as the named state; Step 6 verifies and Step 7 books the shipped version; the summary names both) |
| 106 | review (#2256) | Lens 3c (direct-verified, refutation stood): a Step 7 per-record `FAILED` write never reaches Step 8 — `released` is defined on Steps 5-6 only, the summary template has no bookkeeping-failure slot, and Next Actions says "the shipped records are closed" unconditionally (`SKILL.md` ~L193-219; `bookkeeping.md` ~L52-69) | open | high → staged as `staged/review-3.patch` (a `bookkeeping: {k} of {n} failed — {list}` line in the summary whenever k > 0, `released` keeps its meaning, and Next Actions leads with the per-record recovery when k > 0 — the recovery is the per-record `gh issue comment`/`close` (or `markShipped`) writes, which are idempotent; a re-run of the skill is not the recovery, since Step 1 then reads an empty unreleased set and Step 2 stops at nothing-to-release) |
| 107 | review (#2256) | Gap-sweep (direct-verified): this repository has **no `v*` tag** (`git ls-remote --tags origin` is empty) but a manifest version — the pack degrades `lastTag`, `proposedVersion` bases on the manifest, the console's first-release gate ("effective version ≥ 1.0.0 with no prior tag") fires on every run, so the train is permanently `HELD` on its own first consumer and Step 3 reviews from the root commit; no recovery names "tag the current manifest version" | open | high → staged as `staged/review-4.patch` (the gate's HELD reason names the recovery — tag the commit that last set the manifest to `{base}` (`git tag v{base} {that sha} && git push origin v{base}`, never the tip, which would fold this run's unreleased set into a version that already shipped) or #2259's bootstrap — and Step 3's base becomes that same manifest commit, never the root, unless no manifest exists either) — hand to #2259 as a promise (F6) |
| 108 | review (#2256) | Gap-sweep (direct-verified): the shipped set keys on the trailing `(#N)` with no PR-vs-issue check; GitHub's default squash subject puts the **PR** number there and `gh issue view` on a PR number succeeds silently (probed on PR #2262) — a pre-#2251 merge would book `Shipped in` on a PR and never on its record | fixed | Medium — the lookup reads the issue and drops any `N` whose payload carries `pull_request` (rendered as "PR-number suffix, not a record"); applied in the gap-sweep fix commit |
| 109 | review (#2256) | Gap-sweep (direct-verified): no step closes or archives a standalone release run directory, so the next `/claude-tweaks:release` re-adopts it as `inherited` — Next Actions suppressed from the second run on, and Step 3's classifier reads the prior run's `## /review` block | fixed | Medium — Step 8 closes a `created` run dir via `hooks.js close-run`, matching `/tidy` and `/backlog`; Step 3 reads only the `## /review` block written after this run's own Step 3 marker; applied in the gap-sweep fix commit |
| 110 | review (#2256) | Gap-sweep (direct-verified): Step 3 invokes `/claude-tweaks:review` with `$PIPELINE_RUN_DIR` set, so the review's auto-apply tier (low, medium at `unattended`) commits fixes to the cwd branch mid-release, after the pack was read | fixed | Medium — Step 0 pins `review-auto-apply-ceiling: none` on the release run dir via `set-config.js` (a whole-branch review over merged history stages, never applies); applied in the gap-sweep fix commit |
| 111 | review (#2256) | Gap-sweep (direct-verified): the local-files `shipped:` edit is a dirty working-tree change no step commits (wrap-up's item 5 commits after `closeRecord`) | fixed | Medium — bookkeeping commits the record files from `$RUN_ROOT` after the loop (`Mark records shipped in v{version}, refs #…`); applied in the gap-sweep fix commit |
| 112 | review (#2256) | Gap-sweep (direct-verified): the `Release-As:` `git commit --allow-empty` commits whatever is already staged in the main checkout's index (only a branch guard precedes it), and no fetch/ancestry check precedes the push although `ciTip.tipBehind` is rendered and never consulted | fixed | Medium ×2 — `status --porcelain` must be empty and `fetch origin {branch}` + local == `origin/{branch}` before the commit; a behind/diverged tip is `failed` with a named recovery, the stranded-commit recovery named; applied in the gap-sweep fix commit |
| 113 | review (#2256) | Gap-sweep (direct-verified): `/claude-tweaks:review`'s Step 1.5 test gate was not adapted for `base:{ref}` — it verifies the cwd HEAD, not the tip being released; and Step 2's "Resolving {base}" rule (`origin/{base}`) precedes the `base:{ref}` paragraph without saying which wins | fixed | Medium + low — Step 1.5 under `base:{ref}` requires the verification tree to be `origin/{integration-branch}` (release Step 3 asserts `$RUN_ROOT` is on `{branch}` at the fetched tip and runs the review from there); the resolution paragraph says the `base:{ref}` rule overrides; applied in the gap-sweep fix commit |
| 114 | review (#2256) | Gap-sweep (direct-verified): the `gh`-absent pr-first stop (merge rendered as a paste-ready command) has no Step 8 outcome word | fixed | Low — it is `HELD` with the reason "no gh — merge rendered for a human", `release-held.md` carrying the command; applied in the gap-sweep fix commit |
| 115 | review/hindsight (#2256) | Skill-worthy pattern: a multi-step *driver* skill (one that merges/tags/pushes/closes through engines it does not implement) needs an outcome taxonomy keyed on what physically landed — `failed` (attempted, nothing landed), `HELD` (a gate fired before the mutating step), `PARTIAL` (landed, a later check missed), `released` (all landed) — plus a gating-vs-shipped split for any value the engine, not the skill, decides; every review finding on this build that survived refutation was a taxonomy or provenance confusion `[skill: NEW - driver-skill-outcome-taxonomy]` | open | Evidence: rulings 13, 14, 19, 20 and review rows 104-106; Cost this session: one fix wave and two review fix commits. Wrap-up's Skills curation row seeds it (batch pass) |
| 116 | review/hindsight (#2256) | A task that adds a skill or edits an `argument-hint` trips repo-wide pins (reference-card `Takes`, the Anti-Patterns row count, the description-corpus ceiling) that its own pin tests never run — the full suite belongs in the task's completion check, not only at Common Step 5 `[route: D4]` | open | Evidence: rows 92 and 99 (`c00c3a11e`); Cost this session: two controller commits after the fact. Extends the existing memory `full-suite-before-merging-markdown-prs` to task granularity — staged for the console via wrap-up's Memory row |
| 117 | review (#2256) | Registering `release/execute.md` in `_shared/pr-first-merge.md`'s consumer roster left the `merge` context-cost bundle (`pr-first-merge.md` + `pr-early-run-lifecycle.md`) **1 byte** under its 60,416 B ceiling; the roster entry was cut to the bare site name and a word dropped from an adjacent descriptor to fit | observation | Any further byte in either file trips `context-cost.test.js`; #2002 tracks the bundle restructure — the next consumer registration must land there first |
