# Open Items — 321: Doc staleness: decisions/0004-worktree-two-domain-convention — Context

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | wrap-up | Residue sweep probe did not run: gh unavailable or not authenticated (repo-scope sweep) | observation | Sandbox has no `gh` — gh-dependent probes (open PR/issue state) could not run for this residue sweep. No action needed for this single-line docs fix; noted for completeness per residue-sweep.md's "unknown — never a silent skip" rule. |
| 2 | wrap-up | Residue sweep probe skipped via --no-suite | observation | `npm test` was already run independently earlier in this wrap-up (2525 pass, 1 skipped, 1 known pre-existing phantom failure) — re-run skipped intentionally to avoid redundant cost. |
