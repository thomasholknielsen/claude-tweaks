# Open Items — #989: pr-early-run-lifecycle: two-call dispatch's build phase silently skipped draft-PR creation

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | review | Reviewed `checkBookkeepingStampsGate`'s new `hasNoUpstreamYet` exemption (plugin/bin/lib/hooks/pre-tool-use.js) against the raw diff and the full test run — narrow, one-shot exemption scoped to Bash push targets only, correctly falls through to deny on an already-tracked branch with no PR recorded, ambiguous git outcomes resolve to false (not exempt) per the file's stated posture for this branch. Two new regression tests (allow case + still-deny case) both pass as part of the full 6244/6244 green suite. | observation | — |
| 2 | review | 3i doc freshness — `docs/hooks.md` (REGISTRY.md-registered against `plugin/bin/lib/hooks/**`) documented the existing degrade-line PR-stamp exemption but not the new #989 `hasNoUpstreamYet` initial-publish-push exemption for the same gate | fixed | Added a matching sentence to docs/hooks.md's Bookkeeping-stamps gate bullet — `7cdfff70` |
| 3 | wrap-up | Friction lens vocab (`plugin/skills/reflect/full-mode.md`) omits `bookkeeping-stamp-deny`, a real denied-action event type this run's own `events.jsonl` shows firing twice against build's initial push — neither included in the pinned vocab nor in the Membership rule's stated exclusion list. Staged: `staged/reflect-1.md` | fixed | Applied at the Review Console (Skills row, `staged/wrap-up-skill-1.md`) — `27e7970a` |
| 4 | wrap-up | Residue sweep (`--scope blast-radius`): PR #1382 is open, head branch `worktree-dispatch-record-989` (this run's own PR) | accepted | This run's own active PR — Phase 4's Review Console / merge decision is the authoritative resolution mechanism for it, not this gate |
| 5 | wrap-up | Residue sweep: unarchived clean pipeline run `.claude-tweaks/pipelines/2026-08-24T142704-backlog-standalone` (unrelated prior run, `run-state.json` status `clean`) | fixed | Archived via `bin/hooks.js archive-run` |
| 6 | wrap-up | Residue sweep: unarchived clean pipeline run `.claude-tweaks/pipelines/2026-08-24T145646-backlog-refine-standalone` (unrelated prior run, `run-state.json` status `clean`) | fixed | Archived via `bin/hooks.js archive-run` |
