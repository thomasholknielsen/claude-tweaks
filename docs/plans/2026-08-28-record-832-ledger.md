# Open Items — record #832 (decomposition-mode.md interactive/mechanical split)

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | review | Stale cross-reference: `design-pre-steps.md:150,197` say "decomposition mode's Step 3 in `decomposition-mode.md`" but Step 3 moved to `decomposition-mode-closeout.md` in this diff | open | — |
| 2 | review | `mechanical-handoff.md`'s Required Inputs table omits the `Visual-reference:` scaffold-path field (Step 2.5b-ii's accepted-variant output), which Step 3 needs when a frontend unit's scaffold was accepted | fixed | Added the row to the table and dispatch prompt template, plus a pinning test — `4d85ebf0` |
| 3 | review | `mechanical-handoff.md`'s Required Inputs table lists `--chained` flag state as a carried input, but `--chained` is a shaping-mode-only flag (`SKILL.md`:48) that decomposition mode never receives — no call site references it anywhere in `decomposition-mode.md`/`decomposition-mode-closeout.md` | fixed | Removed the row from the table and dispatch prompt, plus a pinning test — `4d85ebf0` |
| 4 | review | `tests/specify-mechanical-handoff.test.js` has three test-discrimination gaps: (a) line 43's assertion only checks presence of `decomposition-mode-closeout.md`, not absence of a `decomposition-mode.md` read instruction; (b) line 26's regex `/[Ww]ork.unit list/` uses an unescaped `.` metacharacter, overly permissive; (c) lines 48-52's test name claims "ambiguity" coverage but no assertion checks for it | fixed | Tightened the regex; added the missing "do not read" and "ambiguity" assertions — `4d85ebf0` |
| 5 | wrap-up | Pattern observation (Surprises lens): skill-file splits need a repo-wide cross-reference sweep, not just a sweep of files the build's own commits touched — `design-pre-steps.md`'s stale pointer (item 1) survived because it lives outside the edited file set | open | Excluded from this gate — resolution path is the Wrap-Up Review Console (staged/reflect-1.md, folded into the Skills-row disposition at staged/wrap-up-skill-1.md), same as any other staged reflect finding |
| 6 | wrap-up | Pattern observation (Friction lens): two dispatched review-lens agents violated the OUTPUT FORMAT contract (narration before the status line) with no mandated re-prompt; low cost, no wrong verdict resulted | open | Excluded from this gate — resolution path is the Wrap-Up Review Console (staged/reflect-2.md); reflect's own suggested-resolution text recommends no action at n=1 evidence |
| 7 | wrap-up | Residue sweep: PR #1590 (this run's own PR, head `worktree-dispatch-record-832`) is still open — the probe's own design (`bin/lib/residue/probes/forge.js`) surfaces this run's own open PR unconditionally, even mid-flow before Phase 4 has had a chance to act on it | accepted | Expected mid-run state, not leftover work — Phase 4 (the very next phase) is the governance gate that acts on this PR (Review Console / auto-merge short-circuit); no live human is present in this headless run to drill a genuinely-transient item, so it is accepted here rather than left open indefinitely. Flagged for operator visibility in the final report. |
