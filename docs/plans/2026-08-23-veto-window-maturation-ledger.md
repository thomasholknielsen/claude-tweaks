# Open Items — Veto-window maturation for machine-granted auto:merge (#309)

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | review | `manifesto-authorized-merge.md`'s Applicability section still described the pre-#309 condition ("issue's live labels carry `auto:merge`") without mentioning the matured-`auto:merge-pending` path, even though the actual gate in `auto-merge-short-circuit.md` was already correctly updated — stale cross-reference, not a functional bug. | fixed | Added the matured-pending mention with a pointer to the real gate — `plugin/skills/wrap-up/manifesto-authorized-merge.md` |
| 2 | review | `grant-mode.md`'s audit-comment template documents the marker value as `{true\|false\|pending}` with no note that `true` is now dead for fresh writes (this step only ever applies `auto:merge-pending`, never `auto:merge` directly, post-#309) — ambiguous for a future reader implementing this template. | fixed | Added a one-line clarification that `true` is a pre-#309 legacy value kept only for `fleet-counters.js`'s backward-compat regex match, never written going forward — `plugin/skills/backlog/grant-mode.md` |
| 3 | review | Branch is 253 commits behind `origin/main` (merge-base `fe4e1fff`); `git merge-tree` preview surfaces exactly one real conflict — `tests/policy-schema.test.js`'s pinned `POLICY_KEYS.length` (59→60 here, 59→60 independently on main via #1137, classic N+1 same-line collision). All other overlapping files (`release.js`, `grant-mode.md`, `dispatch/SKILL.md`, `work-record.md`, `cleanup-procedures-execution.md`) spot-checked auto-merge cleanly with no semantic collision — main's independent changes land in different clauses/functions than #309's. | accepted | Real but low-risk conflict, correctly requires a human to resolve the trivial count bump at merge time — reported to the Review Console rather than force-resolved, given this diff touches dispatch's own merge-gate logic. |
| 4 | test | Full `npm test`: two runs showed 3 and 13 failures respectively. Every failure besides `changelog-coverage.test.js` was a timing-sensitive test (`pr-state.test.js`, `reap-merged.test.js`, `release-merged.test.js`, `cli-next-target.test.js`, `cloud-setup-drift-check.test.js`) that passed 100% (69/69, then 18/18) when re-run in isolation — machine-load flake from concurrent sibling sessions, not a regression. | observation | Confirmed via isolated re-runs, both before and after the review's two prose fixes; not caused by #309's diff. |
| 5 | test | `changelog-coverage.test.js`: 2 failures reproduce consistently in isolation — `origin/main` shipped `6.105.0` (2026-08-21) with a CHANGELOG/`shipped-versions.tsv` entry that this branch's stale copy (based on merge-base `fe4e1fff`) doesn't carry. Pre-existing repo drift, unrelated to #309's own diff (confirmed: #309 touches neither `CHANGELOG.md` nor `docs/shipped-versions.tsv`, and the same 2 failures were already noted in the build's own `5ac75abe` simplify-commit message). | accepted | Resolves naturally once this branch merges/rebases onto current `origin/main`; not blocking for this record's own review — same disposition the build's own commit already recorded. |
