# Open Items — Worktree-isolation pin survives worktree removal (#703)

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | build | Task 0's empirical premise check (which teardown path leaves the harness's worktree-isolation pin set) could not be reproduced against the real harness from inside this dispatch — the executing session was explicitly barred from invoking `ExitWorktree`/`git worktree remove` against its own worktree (teardown is the dispatching session's responsibility, not this build's). All three initiator paths the record enumerates (ExitWorktree action:remove; a Bash `git worktree remove` hitting the own-cwd deny and retried via ExitWorktree; a Bash `git worktree remove` targeting a worktree other than the session's literal cwd while pinned to it via an earlier cd/subshell) are stated as inconclusive/unreproducible from this session's own hook-visible events, per AC2's explicit allowance. What IS confirmed: PostToolUse receives a distinguishable event for ExitWorktree(action:remove) and the sanctioned Bash worktree-remove shape (`tests/hooks-post-tool-use-teardown-reanchor.test.js` constructs both payload shapes directly and confirms the new backstop fires) — satisfying the reachability precondition for the mechanical remediation, though not the harness-pin-persistence premise itself. | observation | Documented in commit 02df0a9a's body and here; AC3's case-(c) gate-extension was left uncoded since the gap could not be confirmed real and reachable within this dispatch's constraints — satisfied by this documented finding per AC3's own alternative clause. |
