# Open Items — dispatch can re-select an issue that already has an open PR (#1821)

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | review | Lens 3c (Error handling): queue-pull-script.md's final open-linked-PR filter `node -e` (lines 238-252) had no error/warning path unlike its CLI-call sibling immediately above it — a thrown error there silently left the exclusion unapplied with no signal | fixed | Wrapped in if/else with an explicit stderr Warning + preserved prior `$DISPATCH_GROUPS` on failure (rather than a truncate-on-fail write) — commit pending in this run's worktree |
| 2 | review | Lens 3c (Error handling): `dispatch-open-pr-excluded.json` lacked a pre-initialized safe default unlike every sibling exclusion file in queue-pull-script.md | fixed | Added `echo '[]' > "$DISPATCH_OPEN_PR_EXCLUDED"` before the conditional block — commit pending in this run's worktree |
| 3 | review | Lens 3c (Error handling): queue-pull-script.md's header doc comment claimed the exclusion runs "via record.js's partitionByOpenLinkedPR" but the actual runtime path reimplements the filter inline against `resolve-linked-prs.js`'s already-processed `{openPR}` field — `partitionByOpenLinkedPR` is unit-tested but never required/called from this script (confirmed: matches this file's own existing `partitionByOpenNativeBlockers` precedent, which is likewise never called here) | fixed | Corrected the comment to name the CLI field instead, and noted the precedent explicitly — commit pending in this run's worktree |
| 4 | review | Lens 3i (Documentation Freshness): docs/plugin-structure.md's `plugin/bin/**/*.js` Auto-detect pattern matches this diff's two new files (`plugin/bin/lib/issues/linked-prs.js`, `plugin/bin/resolve-linked-prs.js`) but the doc was not updated | fixed | Added a bin/ CLI list entry, a `bin/lib/issues/linked-prs.js` row (mirroring `native-dependencies.js`'s format), and a `resolve-linked-prs.js` command-reference row (mirroring `resolve-blockers.js`'s format) — commit pending in this run's worktree |
| 5 | review | **Duplicate open PR discovered via independent investigation (not a lens finding):** this run's build cherry-picked commit `1893db5b7` (from abandoned-looking branch `origin/worktree-record-1224`) to implement #1821's fix — but that branch is NOT abandoned: it backs PR **#1852** ("dispatch: queue-pull does not exclude issues with an already-open PR (#1224)"), which is currently OPEN, in draft, has already completed build/test/review (review passed, no findings) and reached merge-check verdict `needs-human` (agent-instruction files touched), and is explicitly "Fixes #1224" — awaiting a human merge decision. This run's own PR (**#1942**, "Fixes #1821") now contains a byte-identical implementation of the same fix, produced independently. Merging either PR first will leave the other stale/conflicting on the exact same file regions (`plugin/skills/dispatch/queue-pull-script.md`, `SKILL.md`, `mcp-transport.md`, `open-pr-exclusion-report.md`, `plugin/bin/lib/issues/linked-prs.js`, `plugin/bin/resolve-linked-prs.js`) — a real, guaranteed reconciliation cost, and ironically the exact failure mode #1821 itself was filed to prevent (dispatch silently duplicating effort already in flight elsewhere), just one layer up (a human/agent manually reusing another issue's open-PR code, rather than dispatch re-selecting the same issue). This is not a code defect in #1821's own diff — 3b/3c lenses and direct verification found the code correct, tested, and consistent with established patterns. It is a merge-coordination decision no amount of further building on this branch can resolve. Needs a human to look at both PR #1852 (issue #1224) and PR #1942 (issue #1821) together and decide: merge one and close/rebase the other as superseded, or some other reconciliation — before either merges. | open | (needs-human-decision: merge-coordination call between PR #1852 and PR #1942 — see finding text) |
| 6 | review/hindsight | Hindsight "Skill-worthy patterns" (Approach correctness finding folded in — same root cause as item #5): `build/worktree-setup.md`'s Step 1.6 "Remote-only stale branch check" only guards *this record's own* deterministically-named branch; it has no equivalent guard for a build session reusing prior-art code from a *different* record's branch whose own PR is still open — the exact gap that let item #5 happen undetected until this review caught it. [skill: build/worktree-setup.md] | deferred | Staged to `{run-dir}/staged/reflect-1.md` for Wrap-Up Review Console — needs its own design pass (trigger condition, UX, auto-mode behavior), not a quick inline fix |
