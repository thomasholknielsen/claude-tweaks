# Open Items — 357: specify/build/design-wrapper: no UI-stack decision point before frontend implementation

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | build | Pre-existing regex hazard in `liftMetadata` (`plugin/bin/lib/issues/materialize-format.js`), shared with `Design-intent:` and not a #357 regression: a bare `Ui-stack:`/`Design-intent:` line with no value swallows the next metadata line into the field (`/^Ui-stack:\s*(.+)$/m` crosses the newline). Final whole-branch review recommended a follow-up record fixing both fields' regex together, since fixing only `Ui-stack:` would introduce a new inconsistency with `Design-intent:`'s identical hazard. | fixed | Review re-derivation confirmed the hazard is real (reproduced: a bare `Design-intent:` line swallowed the literal next line as its value) and fix-now eligible per `_shared/deferral-gate.md` — the file is already in this diff, the fix is localized, no product decision needed. Switched `\s*` to `[ \t]*` across all four `liftMetadata` field regexes (Surface/Design-intent/Ui-stack/Design-seed share the identical pattern). Commit 9ed6f441. |
| 2 | build | Free-form `Ui-stack:` values containing `:` or `#` render unquoted into the materialized header's YAML-shaped block (e.g. `ui-stack: Tailwind: v4 # beta` truncates on read by anything that later parses it as YAML). No active parser reads this block today (all readers are LLM skills), so nothing breaks currently — final whole-branch review flagged it as a latent risk worth a follow-up record rather than blocking this branch. | fixed | Also fix-now eligible ("minor / not load-bearing" is explicitly not a valid defer reason per `_shared/deferral-gate.md`). Added `yamlSafeScalar` to `composeHeader` — double-quotes the `ui-stack` value only when it contains `:`/`#` or leading/trailing whitespace; every other optional header field is a closed enum or opaque token with no such risk. Commit 9ed6f441. |
| 3 | build | `plugin/skills/specify/next-mode.md`'s new Step 2.5c2 headless-resolution text (added in the final-review fix wave, commit ea130713) cites `` `## Decision-log fallback` `` as though it were a markdown H2 heading; the actual text at that location is a bold paragraph label (`**Decision-log fallback.**`), not a heading. Cosmetic — no test depends on it, but misrepresents file structure to a future reader/grep. Caught by the fix wave's own scoped re-review. | fixed | Single-line citation fix — dropped the `## ` prefix, referenced the paragraph by its bold label text instead. Commit 9ed6f441. |
| 4 | build/skill | Confirms `.claude/skills/shared-contract-extraction/SKILL.md`'s documented gotcha ("a later consumer added to an already-extracted contract still needs a sweep step 4 doesn't cover — both go stale the moment a second consumer lands"): adding `Ui-stack:` as a 4th field alongside the existing `Surface:`/`Design-intent:`/`Design-seed:` trio left several stale trio-only citations across `materialize.md`, `materialize-format.js`, `docs/plugin-structure.md` that the final whole-branch review had to catch and sweep (commits ea06bb5f, ea130713) — same pattern as the skill's own record #194 example. No new pattern to document; existing skill already covers this precisely. | observation | — |
| 5 | wrap-up/residue | Residue sweep (`bin/residue.js --scope blast-radius`): PR #1326, head `worktree-dispatch-record-357`, open. | acknowledged | Expected — this run's own draft PR, in flight. Resolves at this same wrap-up's merge step (`_shared/pr-first-merge.md`). |
| 6 | wrap-up/residue | Residue sweep reported "test suite exit 7": `store-concurrency.test.js`'s `recordDecline serializes concurrent writers` and `pr-state.test.js`'s `resolvePrStateAsync: does not block the event loop` both failing. | accepted | Both are concurrency/timing-sensitive tests, in files untouched by this diff. Re-ran both files in isolation immediately after the sweep: 20/20 pass, 0 fail. This session's own full-suite run minutes earlier was 6117/6117 clean. Machine-load flake per CLAUDE.md's documented tolerance — not a regression. |
| 7 | wrap-up/residue | Residue sweep: pipeline run dir `.claude-tweaks/pipelines/2026-08-23T181549-record-357` not yet archived (`run-state.json` status `clean`). | acknowledged | Expected — archival is Phase 4 cleanup item 8, always last. Resolves at execution time. |

## Notes

- These three items were surfaced during `/claude-tweaks:build #357`'s final whole-branch review and its fix-wave re-review, and were deliberately left `open` rather than resolved there — the `build,test` run's step list did not include `wrap-up`, so the nothing-left-behind ledger gate did not fire in that invocation. The `review,polish,wrap-up` continuation run re-derived each item from the raw diff (not from this ledger's own claims), confirmed all three qualify as fix-now under `_shared/deferral-gate.md`'s criteria, fixed them, and verified each fix's regression test actually discriminates (reverted the fix, confirmed the new test failed, restored). See commit 9ed6f441. Item 4 (observation) needs no resolution — it feeds `/wrap-up`'s Skills curation row.
- All 9 plan tasks (docs/superpowers/plans/2026-08-23-ui-stack-decision-point.md) are complete, task-reviewed, and final-whole-branch-reviewed with 0 open Critical/Important findings. `origin/main` (279 commits ahead of this branch's original base) was merged in during this build to keep `npm test` green — see commit 8a88d45d for the merge-conflict resolution record.
