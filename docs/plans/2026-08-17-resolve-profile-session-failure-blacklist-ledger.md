# Open Items — Subagent model-profile resolution ignores live session model state after a same-session credit failure (#763)

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | build/skill | Consulted `.claude/skills/skill-prose-conformance-tests` before editing `skills/_shared/subagent-output-contract.md` and `skills/feedback/session-evaluation.md`'s prose — no pinning test applies per its Decision Framework (new descriptive prose, not a mirrored data structure, executable snippet, or third-party behavioral claim), so none was added; consistent with the skill's own guidance, no divergence found. | observation | No action needed — consistent with skill's Decision Framework |
| 2 | build/docs | Common Step 6.5 (Documentation Sync): changed files matched `docs/REGISTRY.md` Auto-detect patterns for `docs/plugin-structure.md` (`bin/**/*.js`), `CLAUDE.md`/`docs/skill-graph.md`/`docs/getting-started.md`/`docs/skill-authoring.md` (`skills/_shared/*.md`, `skills/**/*.md`). `docs/plugin-structure.md`'s `bin/lib/model-profiles/` entry omits `session-failures.js` and the session-failure resolve stage — confirmed still true, but this exact gap is already item 4 of #841 (filed at final review, `genuinely-larger` defer-reason, bundled with 4 other parked follow-ups) — not re-fixed here to avoid duplicating that already-made deferral decision. `CLAUDE.md`, `docs/skill-graph.md`, `docs/getting-started.md`, `docs/skill-authoring.md`, `README.md` reviewed against the actual prose changes (new `record-failure` guidance in the Dispatching/Frontier paragraphs, `session-evaluation.md`'s degrade-path clause) — none of these docs describe implementation detail at that granularity, no divergence found. | deferred | Tracked in #841 item 4 (docs/plugin-structure.md); no other matched doc needs an update — verified against current file content |
