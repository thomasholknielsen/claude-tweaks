# Open Items — hooks.js subcommands execute --help as a real argument — add a help guard

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | review | [3c Error Handling, medium] USAGE-table guard coverage isn't structurally tied to main()'s dispatch branches — a future subcommand added without a matching USAGE entry would silently get no --help guard, reintroducing the hazard this record closes. Direct-verification confirmed: today's 14 `cmd === '...'` branches match all 14 USAGE keys, but the existing test only iterates `Object.keys(USAGE)`, never the reverse. | fixed | Added a structural test to tests/hooks-help-guard.test.js scanning main()'s source for every `cmd === '...'` branch and asserting each has a USAGE entry — proved red (temporarily dropped the `reconcile` entry, test failed) then green after restore. |
| 2 | review | [coordination, informational] #1012 (PR #1559, open, unmerged, not in this branch's base) independently declares a same-named module-level `const USAGE = {...}` in plugin/bin/hooks.js — different keys (5 verbs vs. #1143's 14) and different value text for overlapping keys (e.g. record-worktree). Merging both as-is is a duplicate top-level identifier, not just a textual diff conflict. | observation | No action in this run — #1012 not yet merged. Whichever PR merges second resolves it via pr-first-merge.md's Conflict path when that day comes; noted here and in the review summary per dispatch instructions. |
