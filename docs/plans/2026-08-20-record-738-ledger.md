# Open Items — Promote the post-fan-out shadow sweep to `bin/hooks.js sweep-shadow` (#738)

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | wrap-up | Residue sweep (`--scope blast-radius`, `remedy: auto`): merged branch `origin/worktree-record-768` not deleted | accepted | Pre-existing, outside #738's diff (record-768's own residue, not this run's). This is a worktree-isolated wrap-up dispatch on a repo currently running a large number of concurrent worktree sessions (30+ live `git worktree list` entries); deleting a shared remote ref here risks racing another session's own teardown. Left to the standing `reconcile-background` sweep (`docs/hooks.md`'s Background convergence: branch archival, merged remote-branch pruning) or `/claude-tweaks:tidy`, which already own this housekeeping category. |
| 2 | wrap-up | Residue sweep (`--scope blast-radius`, `remedy: auto`): 3 un-archived `status: clean` pipeline-run dirs not under `.claude-tweaks/pipelines/archive/` — `2026-08-20T152056-record-663`, `2026-08-20T153010-record-671`, `2026-08-20T153655-record-768` | accepted | Pre-existing, outside #738's diff — orphaned residue from other records' wrap-up runs. Archival requires a write under `$RUN_ROOT` in the main checkout, which this worktree-isolated dispatch cannot safely perform (Write/Edit tooling is pinned to the `record-738` worktree, and this dispatch's own instructions forbid creating or entering another worktree to reach the main checkout). Left to `reconcile-background`'s run-dir-archive convergence (`docs/hooks.md`) or `/claude-tweaks:tidy`. |
