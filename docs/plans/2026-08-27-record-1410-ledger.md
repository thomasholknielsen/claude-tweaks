# Open Items — #1410: hooks: ask-user-question events can be attributed to the wrong concurrent pipeline run's events.jsonl

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | review | Medium: `resolveRun`'s new `isForeignWorktreeCandidate` filter (context.js) silently no-ops when called with a falsy `cwd` — `iterRunDirsWithState(cwd)` internally falls back to `process.cwd()` (line 129), but the ownership-filter calls pass the raw, unresolved `cwd` straight into `classifyOwnership`, whose `if (!cwd) return 'indeterminate'` guard (line 302) can never classify `'foreign'` for that input, silently reverting to the pre-#1410 unguarded fallback for any falsy-cwd caller. The sole current production call site (`hooks.js:775/797`) always resolves `cwd` before calling, so this is not exploitable today — but no test exercises `resolveRun`/`isForeignWorktreeCandidate` under a falsy `cwd`, so a regression here is silent. | fixed | Applied at Review Console from `staged/review-1.patch` — commit `5cb850f1e`. Targeted tests (hooks-run-attribution, hooks-post-tool-use-ask-user-question, bin-lib/hooks) 70/70 green. |
| 2 | wrap-up | Observation (reflect, Near-misses): low-tier direct-verification override caught a real medium bug (item #1) that would otherwise have stayed unconfirmed — confirms the override is pulling real weight at `review-effort: low`. | observation | Informational — see `staged/reflect-1.md`. |
| 3 | wrap-up | Tangential (reflect, Fresh start): dispatch should detect concurrently-authorized records with overlapping unmerged root-cause fixes — #1410 and #1402/PR#1577 independently re-implemented a byte-identical fix in `context.js`. | deferred | Filed as backlog record #1579 (born-ready, type:task, risk:low, size:medium) at the Queue writes gate. |
| 4 | wrap-up | Tangential (reflect, Friction): `set-config.js` requires one call per Manifesto lever (13 calls to write one `config.yml` during this run's inherited-run-dir recovery) — a batch-write mode would remove this friction. | deferred | Filed as backlog record #1580 (born-ready, type:task, risk:low, size:low) at the Queue writes gate. |
| 5 | wrap-up | Residue sweep: PR #1578 (this run's own head branch `worktree-dispatch-record-1410`) is still open — `gh pr list --state open ... --limit 100`, blast-radius scope. | accepted | assess-agent-autonomy merge-check rendered `needs-human` (review's own medium finding is a hard input to that gate) — PR #1578 intentionally stays open pending human merge review. Not filed as a separate backlog record: the PR itself (verdict comment + `bot:in-progress` claim) already fully tracks this; a duplicate "please merge PR #1578" record would be pure bookkeeping noise with no new information. Judgment call, logged to `decisions.md`. |
