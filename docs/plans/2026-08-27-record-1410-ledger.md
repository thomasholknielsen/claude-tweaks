# Open Items — #1410: hooks: ask-user-question events can be attributed to the wrong concurrent pipeline run's events.jsonl

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | review | Medium: `resolveRun`'s new `isForeignWorktreeCandidate` filter (context.js) silently no-ops when called with a falsy `cwd` — `iterRunDirsWithState(cwd)` internally falls back to `process.cwd()` (line 129), but the ownership-filter calls pass the raw, unresolved `cwd` straight into `classifyOwnership`, whose `if (!cwd) return 'indeterminate'` guard (line 302) can never classify `'foreign'` for that input, silently reverting to the pre-#1410 unguarded fallback for any falsy-cwd caller. The sole current production call site (`hooks.js:775/797`) always resolves `cwd` before calling, so this is not exploitable today — but no test exercises `resolveRun`/`isForeignWorktreeCandidate` under a falsy `cwd`, so a regression here is silent. | open | — |
