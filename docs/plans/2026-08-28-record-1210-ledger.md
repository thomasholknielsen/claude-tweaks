# Open Items — materialize.js accepts a main-checkout-anchored --run-dir and never validates it's actually a pipeline run dir

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | review | The #1210 fix only rewrote the write target when `--run-dir` resolved to the main checkout; a `--run-dir` resolving inside a DIFFERENT linked worktree than cwd's own passed #959's existing "inside a linked worktree" acceptance unconditionally and never reached the #1210 rewrite block either — silently writing the spec file into an unrelated worktree's tree (same stray-write class, laterally). Confirmed empirically (two-worktree repro, before/after). | fixed | Unified the rewrite path to cover both mismatches (main-checkout-anchored, or a different worktree) — always targets cwd's own worktree. Commit `b4d27a838`. New regression test in `tests/materialize-run-dir-anchoring.test.js`. |
| 2 | wrap-up | Reflect (near-misses): editing materialize.js's `deps` seam broke a second, independent test fixture (`materialize-drift.test.js`) that relied on original call-laziness — caught before commit only by running the full targeted suite, not just the primary test file. | observation | Staged: `staged/reflect-1.md`. |
| 3 | wrap-up | Reflect (fresh start): `cwdWorktreeRoot`/`runDirWorktreeRoot` are duplicate-bodied deps kept separate only for unrelated-file naming compatibility — collapsible in a future pass. | observation | Staged: `staged/reflect-2.md`. |
| 4 | wrap-up/ops | Residue sweep: PR #1605 (this work) is open, head `worktree-dispatch-record-1210`. | accepted | Not auto-merged: no `auto:merge` label on #1210, and `assess-agent-autonomy merge-check` renders `needs-human` (review pass produced a Medium-severity finding, fixed in-review — the mode never overrides a real review finding). PR stays open pending explicit human merge decision. |
