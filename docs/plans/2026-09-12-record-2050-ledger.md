# Open Items — Flow run: #2050 (Task-tool subagents can't be redirected to a pre-existing worktree via EnterWorktree(path=) in this harness)

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | ops | Deliverable 1's confirm-harness-specificity call, and Acceptance Criterion 1's "avoids the failed pattern by construction, or fails with a clear, actionable message" claim, are unverified against a real two-call Task dispatch that hits this exact shape (a second call joining a first call's worktree via `EnterWorktree(path=)`) (reason-not-auto: live-verification — the only way to confirm or disprove this is a real Task/Agent dispatch creating and joining worktrees against shared harness state; this build was itself policy-restricted from making any Task/Agent dispatch of its own, per this firing's own dispatch prompt) | open | — |
