# Open Items — Policy key naming convention + rename program (#332), worktree.always → worktree-always (#602), run-config direct-read migration (#334)

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | review/3c | facts.js resolveBudgets round-trip untested — configured-budget test added | fixed | commit a3973e43 (auto, low) |
| 2 | review/3f | (low-confidence, unreproduced) no per-pair old+new both-set test for the seven #332 renames — generic coverage exists in resolve-policy-lib.test.js | fixed | commit 7949f77c (wrap-up Phase 3 fix-exhaust — both-set assertion added to the table-driven test) |
| 3 | review/docs | 3i: CLAUDE.md, docs/getting-started.md, docs/plugin-structure.md, docs/skill-authoring.md match changed patterns, not updated — checked, no citations of renamed keys | observation | no update needed |
| 4 | review/hindsight | rename mapping restated in three test literals (convention, low) | fixed | commit 7949f77c (hoisted into one RENAMES_332 fixture) |
| 5 | review/hindsight | auto-mode-contract still says severity floor for the auto-apply cutoff — tangential backlog candidate | deferred | → backlog (auto-routed: expands pipeline scope beyond #332; ledgerRouteRemainder at unattended) — staged/reflect-2.md → Queue writes gate |
| 6 | review/hindsight | unattended-tier alias has no recorded removal condition — tangential backlog candidate | deferred | → backlog (auto-routed: expands pipeline scope beyond #332; ledgerRouteRemainder at unattended) — staged/reflect-3.md → Queue writes gate |
| 7 | wrap-up | residue sweep: PENDING_RENAMES = [worktree.always] allowance in tests/policy-key-naming.test.js — owned by #602, the next spec in this run | observation | resolves when #602 lands (same run) |
