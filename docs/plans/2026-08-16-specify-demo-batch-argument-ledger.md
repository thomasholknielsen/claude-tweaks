# Open Items — #695 specify + demo: accept a #N,#M batch argument so tidy's Yours groups collapse to one paste line

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | ops | Flow Step 2.5 pre-flight: origin/main was 14 commits ahead of local main HEAD 676838ad at run start (auto → continued). Worktree branched from origin/main and caught up to 24b03525 in post-creation catch-up, so the divergence is absorbed for this branch; local main checkout itself remains behind origin. | accepted | absorbed by the worktree's own post-creation catch-up; no residual action |
| 2 | build/deferred-check | Deliverable D (tidy Yours group heads emit `/claude-tweaks:specify #a,#b` / `/claude-tweaks:demo #a,#b`) could not be applied at build time: #685's grouping rule was not on origin/main yet (PR #699 still open). Re-checked at wrap-up Phase 3 (leftover routing): #699 merged 2026-08-17T05:32:06Z, so Task 4 Steps 2-4 were applied — merged origin/main (one conflict in docs/getting-started.md, resolved keeping both intents), updated the Single-ref/Batchable-target bullets and the conformance-scan row, collapsed the journey doc's example paste blocks to batch lines, flipped the mechanical pin test. | fixed | commits fb753c4f (merge), 17e3b41a |
| 3 | build | Two adjacent consumers still emitted a per-record paste line for the now-batchable skills. Re-examined at wrap-up: `skills/_shared/github-pr-scan-acceptance.md`'s acceptance-queue render string genuinely was stale (single-ref only) — fixed to offer the batch form. See row 23 for the second consumer (`skills/backlog/overview-mode.md`), resolved separately since its disposition differs. | fixed | commit 5342e333 |
| 4 | build/observation | `skills/specify/SKILL.md` line 3 frontmatter `description` describes only design-doc decomposition and never mentions shaping mode — the skill's primary path and the text that routes the model to the skill. Pre-existing, out of #695's scope; surfaced by the final review. Wrap-up Phase 2's Skills curation row found the same gap independently and staged a concrete patch (restructural — a rewrite of existing wording, so staged rather than auto-applied). | deferred | staged for the Review Console — Skills curation row, staged/wrap-up-skill-1.md |
| 5 | build/skill | Skill observation: none of the three project skills (gh-api-module-pattern, transcript-payload-verification, upstream-drift) cover skill argument-grammar changes. Learning: extending a skill's argument cardinality (single → list) needs a vocabulary-wide sweep (`per invocation`, `single record`, `one item`, `[#N]`) across skills/, docs/, README — a file-list-scoped deliverable (card + README) missed five live sentences caught only at the final whole-branch review (a sixth surfaced only at wrap-up's Skills row — see row 19). Enriched into docs/skill-authoring.md's argument-hint bullet and its "Adding a mode or argument" rule (Docs curation row). | fixed | commit 20294a81 (Docs row); see rows 11-12 |
| 6 | review | 3c (Error handling): trailing/empty list element left an unnamed offending element — specify batch paragraph now names it (`empty element after #41`) | fixed | commit 28aedc9b |
| 7 | review | 3c (Error handling): demo batch Input had no per-item failure isolation for an unresolvable ref — clause added (reported and skipped, remaining refs still run) | fixed | commit 28aedc9b |
| 8 | review | 3c/3f: mixed-list hard error and free-text fallback rules unpinned — three assertions added to tests/batch-ref-argument.test.js | fixed | commit 28aedc9b |
| 9 | review | 3c (low): `require('./argument-hint-input.test.js')` re-registers that file's 2 tests inside the pin file's process (benign duplication) | accepted | repo convention shared with tests/reference-card-argument-hint.test.js; extracting extractArgumentHint into bin/lib/skill-audit is a bin/ change outside #695's scope — candidate for its own small record |
| 10 | review/hindsight | Hindsight (5 evaluations): approach sound — sequential loop over the unchanged single-item procedure; no new structural debt (test-file require convention already ruled, row 9); consolidation clean (batch grammar stated once per ## Input, pointers elsewhere); convention drift was the 'per invocation' vocabulary, swept in f2c4c37c/28aedc9b; skill-worthy pattern = row 5 (target: docs/skill-authoring.md argument-hint bullet, [skill: docs/skill-authoring.md]). No change-now findings. | observation | — |
| 11 | wrap-up | Reflect: argument-cardinality changes need a vocabulary-wide sweep → docs/skill-authoring.md argument-hint bullet (staged/reflect-1.md; same learning as row 5) | fixed | Docs curation row applied it (additive + high reversibility/confidence), commit 20294a81 |
| 12 | wrap-up | Reflect: per-item loops need an explicit precedence clause over the Interaction-style directive → docs/skill-authoring.md Interaction patterns (staged/reflect-2.md) | fixed | Docs curation row applied it, commit 20294a81 |
| 13 | wrap-up | Reflect near-miss (systemic): prose substring pins are wrap-sensitive — normalize whitespace in tests/batch-ref-argument.test.js read() (staged/reflect-3.md) | deferred | staged for the Review Console — convention finding |
| 14 | wrap-up | Reflect friction: gate-denial burst on an executed compound Bash + contract-violation false positive on SDD's `**Status:** DONE` → backlog candidate (staged/reflect-4.md) | deferred | staged for the Review Console — Queue writes |
| 15 | wrap-up | Reflect fresh-start: extract extractArgumentHint into bin/lib/skill-audit → backlog candidate (staged/reflect-5.md) | deferred | staged for the Review Console — Queue writes |
| 16 | wrap-up | Reflect: not captured — description-ceiling trip (docs/skill-authoring.md:69 already states it), origin/main advancing mid-preflight (catch-up handled it), Write refusing run-dir paths (existing memory) | accepted | already documented elsewhere |
| 17 | wrap-up/curation | Journeys row: `docs/journeys/accept-built-work-via-demo.md`'s Success state still described only the single-record outcome after build added Step 4 (the batch step) — appended the list-path outcome. | fixed | commit cf0ab514 |
| 18 | wrap-up/curation | Docs row (D1): `docs/getting-started.md`'s Polymorphic input paragraph enumerated every accepted `/specify` input except the new `#N,#M` list — added the list form plus its two boundaries. | fixed | commit 20294a81 |
| 19 | wrap-up/curation | Docs row (Registry Maintenance): `docs/plugin-structure.md`'s Auto-detect patterns (`skills/**`, `bin/**`) match zero files under the shared glob matcher (`*` is within-segment only, no `**` construct) — the doc documenting the skills tree scored 0 on a diff touching 7 skill files. Two fixes exist (narrow the patterns, or teach the matcher a real `**`); the code-level one is outside this row's remit. | deferred | staged for the Review Console — Docs curation row, staged/wrap-up-doc-1.md |
| 20 | wrap-up/curation | Skills row: `skills/_shared/work-record.md` still claimed `/demo` resolves one record's brief "per invocation" — a sixth live single-ref sentence the build-time sweep (row 5) missed, not caught until wrap-up. | fixed | commit 487dd29b |
| 21 | wrap-up/curation | Skills row (new-skill candidate): four skills (`flow`, `dispatch`, `specify`, `demo`) now each restate the comma-separated ref-list grammar independently — notation already drifted, the hard cases (mixed-list error, per-item isolation) defined in only two. Candidate: `skills/_shared/batch-ref-argument.md` shared grammar contract. | deferred | staged for the Review Console — Skills curation row, staged/wrap-up-skill-new-batch-ref-argument.md |
| 22 | wrap-up/residue | Residue sweep (`bin/residue.js --scope blast-radius`): PR #718 (this run's own draft PR, head `worktree-flow-spec-695`) is still open — `remedy: record`. Expected at this point in the run — this is the pr-first draft PR that closes at Phase 4 execution. Resolve gate Phase 2: auto-routed to backlog via the `unattended`-tier `ledgerNarrowing` capability, blocker `blocked-dependency`. | deferred | → backlog, staged/ledger-record-pr718-still-open.md (note: proposal states it is very likely already stale by the time it's read) |
| 23 | build | Second half of row 3: `skills/backlog/overview-mode.md`'s Shape-next block (~231-233, one `/claude-tweaks:specify #{N}` per terminal) re-read in full at wrap-up. Its own prose states "Shape lines are priority-ordered, one record per terminal" — each terminal is an independently-ranked unit, the same shape the Dispatch-now block already uses (a chain batches *within* one terminal; terminals never collapse into each other). Not a bug — the original finding conflated "target skill is batchable" with "renderer must collapse across terminals." | accepted | intentional per-terminal rendering — matches the Dispatch-now block's own precedent, no change |
