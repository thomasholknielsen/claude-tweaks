# Open Items — Skill context composer (#1987 decomposition: #1988–#1997)

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | test | Pre-existing (batch pre-flight sweep, unmodified base c392c4da7): `tests/bin-lib/reconcile/reap-merged.test.js` — 3 `#1793` releasePorts tests fail deterministically in isolation (3/15). Root cause: the test fixture builds paths under macOS `/var/folders/...` but `reapMerged` passes the realpath (`/private/var/folders/...`) to `releasePorts`, so `deepStrictEqual` on the recorded path mismatches. Not touched by any spec in this run. | deferred | Already tracked by open #1900 ("npm test: reap-merged #1793 macOS realpath mismatch + impeccable-cli-contract version pin"), found by the resolve gate's Phase 1 re-check at spec 1988's wrap-up — no new record (a duplicate); origin: base sweep; files: tests/bin-lib/reconcile/reap-merged.test.js; trigger: #1900 closes. |
| 2 | test | Batch pre-flight sweep: `tests/statusline.test.js` "transcript_path under .claude-accounts renders the acct segment at the end" failed under the full suite (1/7484) but passed 78/78 in isolation — load-sensitive flake, not a regression. | observation | Flake per `test/verification.md` flake adjudication; no action. |
| 4 | build/skill | Spec 1988: `plugin/bin/compose-context.js` establishes a fourth run-dir guard shape (a run-directory writer taking the resolver family's anchored-or-outside rule with exit 2, justified by callers that never branch on a run-dir code) — registered in `_shared/pipeline-run-dir.md`'s CLI-argument-boundary section, but `.claude/skills/gh-api-module-pattern/SKILL.md`'s "The CLI wrapper contract" bullet still describes only two boundary guards plus the sanctioned-writer third shape. The skill is stale by one shape. [skill: gh-api-module-pattern] | observation | Routed to wrap-up's Skills curation row (multi-spec batch pass). |
| 5 | build/skill | Spec 1988: `compose.js`'s `CANDIDATE_RE` treats any line that starts like a marker but fails the grammar as a hard error rather than content — an application of `.claude/skills/parse-signal-discipline/SKILL.md`'s "could-not-parse must be a distinguishable signal" rule to a new deterministic parser; worth one worked-example line there. [skill: parse-signal-discipline] | observation | Routed to wrap-up's Skills curation row (multi-spec batch pass). |
| 6 | review | Spec 1988, lens 3c (error handling, low): `plugin/bin/compose-context.js` mapped a throwing `--run` stat to exit 2 while its header files failing reads under exit 1. | fixed | Mapped to exit 1, header clause widened, pinning test updated — `82997b2ae`. |
| 7 | review/hindsight | Spec 1988, Missing-consolidation lens: `resolve-conditions.js`'s `gh --version` probe is the sixth near-verbatim gh-availability check in `plugin/bin` (release-claim.js, file-feedback.js, link-records.js, materialize.js, repo-resolve.js) with drifting option objects. Follow-up: one injectable `ghAvailable` helper + migration. Ledger: staged/reflect-1.md (spec-1988). Evidence: whole-branch review + Task 2 review, `grep -rn "'--version'" plugin/bin`. Cost this session: unclear. | open | — (staged proposal awaits the consolidated Review Console) |
| 3 | test | Spec 1988 Task 4 full-suite run: `tests/reconcile.test.js` "FAST_CHECKS runs the preflight and the shared fetch concurrently, not serially (#872)" failed (509ms vs a 200ms wall-clock budget) and again in isolation. Passed on the base sweep. Evidence it is environmental, not a regression: `git diff --name-only c392c4da7..HEAD` lists no file under `plugin/bin/lib/reconcile/` (the code under test is byte-identical to the base where it passed), and `uptime` at adjudication time read load averages 71.64 / 69.66 / 72.18. | observation | Load-sensitive wall-clock assertion on a machine at load ~70; re-confirm green at the run's final verification before merge. |
| 8 | build | Spec 1989, simplify pass: `tests/compose-markers-conformance.test.js`'s `MARKER_SHAPED_RE` restated `compose.js`'s unexported `CANDIDATE_RE` character-for-character — the one place the suite's "cited, not restated" header did not hold, and a silent-drift risk if the parser's candidate shape ever changes. | fixed | `CANDIDATE_RE` exported from `compose.js` and cited by the conformance suite (commit after 6448c3d3a); 4/4 and 45/45 green. |
| 9 | build | Spec 1989, simplify pass: `function* walk(dir)` is byte-identical in `tests/compose-markers-conformance.test.js` and `tests/skill-prose-plugin-root-invocations.test.js` (the only two occurrences under `tests/`). Extracting it means a new shared test helper and an edit to a pre-existing file outside this record's scope. | observation | Below the materiality floor for a record (two-site, ~10-line helper); revisit when a third corpus-walking suite appears — #1990's measurement test is the likely candidate. |
| 10 | review/hindsight | Spec 1989, guidance for the remaining fencing records (#1991-#1994) that materialize later in this run: (a) additive fencing of prose that already exists saved 4.5% of the merge path's bytes (58,612 → 55,995 B) — a sub-issue's byte AC must be set from a measurement of the branch-specific share, not from the file total; (b) fence per paragraph, never a whole section body, whenever other prose cites that section under every condition set ("Root cause above", a degrade table the bookkeeping gate reads) — a fenced-whole section renders as a bare heading, which is fine only when nothing cites its body; (c) mid-line branch-specific clauses stay unfenced (line-anchored grammar) — splitting a sentence is a rewrite; (d) every compose source path in skill prose is `${CLAUDE_PLUGIN_ROOT}`-rooted — the record bodies still spell the repo-relative form. | open | Read by the controller at each later spec's plan-authoring; carried to #1987 as promise F4 for the byte budget. Trigger: #1994 ships. |
| 11 | build | Spec 1990: the per-file 40 KB ceiling becomes a warning tier in `context-cost.test.js`, but two other per-file hard gates on `CEILING_BYTES` stay in force outside that suite — `plugin/bin/lib/merge-size-probe.js` (post-merge per-file bytes at `pr-first-merge.md` Step 2.5, raw blob size, not marker-stripped) and `plugin/bin/lib/plan-audit/checks.js`'s `headroomCheck` (the record names the latter as U10's). Both are #1997's to retarget; until then a fenced `_shared` file that grows past 40 KB raw passes the warning tier and fails the merge probe. | open | Carried to #1997 (retire the per-file pins) — its plan must sweep every `CEILING_BYTES` importer, not just the test file. Trigger: #1997's plan-authoring. |
| 12 | review | Spec 1990, scoped re-review: `parseComposeCallLine`'s placeholder check runs on the token after the `${CLAUDE_PLUGIN_ROOT}/` prefix is stripped, so a source such as `"${CLAUDE_PLUGIN_ROOT}/skills/${x}/y.md"` (a second `${…}`) reads as documentation and goes unmeasured. No such token exists in the corpus and every shipped source is a literal path; the pin test forbids nothing here. | observation | Below the materiality floor; a real call site with an interpolated segment is not a form this decomposition produces. Revisit if a later record parameterizes a source path. |
| 13 | build/hindsight | Spec 1991, fence surveys: the record's premise that four flow-lifecycle files "already contain" mode/attendance prose was wrong for three of them (0 / 0 / 994 B fenceable of 31.6 / 30.3 / 39.1 KB; manifesto.md 1,401 B) — the shared contracts branch on `work-backend`, `work-links`, `ceremony`, `integration-model`, `worktree-policy`, and `transport`, and the composer's `mode` key collides by name with the build execution mode (`worktree`/`current-branch`), never a legal value. A `flow-run` bundle from the two big contracts is ~70 KB under every mode. Guidance for #1992-#1994 (worktree-setup, issue-claims, github-pr-scan): run the four-file survey shape (classification, byte estimate, citation check, pin check, hazards) at plan time, on the axis the file really branches on, and size the call site against the composed-bytes gate before writing a compose sentence. | open | Read by the controller at #1992's, #1993's, and #1994's plan-authoring; carried to #1987 as promise F5. Trigger: #1994 ships. |
| 14 | build | Spec 1991: `plugin/skills/flow/SKILL.md` is 40,810 B after the fix wave and two review fixes (the compose sentence and its adopted-run-only clause cost 672 B together) against `merge-size-probe.js`'s 40,960 B raw merge-time gate on `SKILL.md` files — **150 B of headroom** on the plugin's most-cited skill file. Any later record in this run that touches it (#1995/#1996 move the subagent boundary; #1997 retargets the raw gates) must `wc -c` first and extract a section to a sub-file before adding a byte (the hard-ceiling-headroom rule; `mode-table.md`'s extraction is the precedent). | open | Read at #1995's, #1996's, and #1997's plan-authoring. Trigger: the first later edit to flow/SKILL.md, or #1997 retargeting the raw gate. |
| 15 | review | Spec 1991, scoped re-review: `compose.js`'s `VOCAB.mode` lists `confirm`, but no source `resolve-conditions.js` reads can ever produce it — `config.yml`'s `mode` lever holds only `auto`/`hybrid`/`interactive` (`set-config.js`'s `CONFIG_ONLY_VALUES`; a `confirm` run writes `auto` after its gate). A `<!-- when: mode=confirm -->` fence is therefore unreachable from any run directory and only ever composes in a synthetic conditions object. #1991's fix wave had already unfenced the `confirm` bullet for a different reason (the `hybrid` antecedent). | open | For #1987's vocabulary: either drop `confirm` from `VOCAB.mode` (a composer contract change — expand/contract, with the conformance suite as the sweep) or document it as synthetic-only in `docs/skill-authoring.md`; no later record in this run should fence on `mode=confirm`. Trigger: #1997's plan-authoring (it rewrites the ceiling rules and is the last composer-touching record). |
| 16 | build | Spec 1992, fence survey of `_shared/issue-claims.md`: the mirror-comment step (`## The mirror`'s code block ending in `gh issue comment …`) documents only the `gh` form — the file states no MCP equivalent for posting the claim mirror, so a gh-absent (cloud Routine) reader following "The lock" has no instruction for that step. Not a fencing question (the block stays unconditional precisely because removing it would delete the only guidance) but a contract gap on the exercised MCP path. `_shared/github-write-transport.md`'s CRUD table already maps `Comment` → `add_issue_comment`, so the fix is one documented line, not an open design question — deferred here only because adding it is a content change that would break this record's `[IL-144]` byte-identity proof. | deferred | Proposed unconditionally at #1992's whole-branch review as a backlog record (`spec-1992/staged/record-proposal-1.md`, shared with row 18, `Defer-reason: pre-existing-outside-diff` — a floor-failing reason, so it is a console decision, not an auto-filing) rather than left to triage — CLAUDE.md's no-implicit-deferrals rule. Trigger: the consolidated Review Console files it. |
| 18 | review | Spec 1992, whole-branch review: under `transport=mcp`, `_shared/issue-claims.md`'s "The lock" step 2 tells the reader the file holds "the wrapper object `{content, sha}` from the `gh api` call's `-q` filter" and to `jq -r '.content'` it — but the surviving MCP bullet in step 1 writes the tool's returned content, already the content, so the composed instruction is a wrong action, not merely a dangling reference; the same wrapper-object phrase recurs in the repair section ("never the raw `{content, sha}` wrapper object a fresh `gh api` read produces"). Pre-existing in the raw file; composition makes it the only instruction an MCP reader sees. Same class as row 16, and a content change the byte-identity proof forbids in this record. | deferred | One staged proposal covers rows 16 and 18 (`spec-1992/staged/record-proposal-1.md`): Current State the two passages; Deliverables the `add_issue_comment` mirror form beside the `gh` block and an MCP-side sentence in step 2 (and the "Authoritative: read the blob" paragraph) stating what shape step 1's MCP bullet writes. Trigger: the consolidated Review Console files it. |
| 17 | build | Spec 1992: `plugin/skills/_shared/github-pr-scan.md` is 40,131 B after the fix wave against `merge-size-probe.js`'s 40,960 B raw merge-time gate on `_shared` files — **829 B of headroom**, the tightest `_shared` file in the decomposition (the record's own Gotchas called its pre-fence 915 B the tightest). The composed gate is not what catches the next edit here: the `pr-scan` row's max is the `transport=unresolved` combination (40,192 B), a state the transport probe never produces, and the runtime-reachable max is the `gh` bundle (40,184 B) — both under the composed ceiling, while the raw gate binds first. Same shape as row 14 (flow/SKILL.md, 150 B). `tidy/scan-procedures.md` is 40,100 B but a sub-file — warning tier only. | open | Read at #1995's and #1996's plan-authoring (the subagent boundary touches the PR-scan dispatch) and at #1997's (it retargets the raw gate): `wc -c` first, extract a scope section to a sub-file before adding a byte. Trigger: the first later edit to github-pr-scan.md, or #1997 retargeting the raw gate. |
| 19 | build | Spec 1992, report fidelity: Task 4's proof agent reported "both mcp bundles read end to end: every step/Transport/item pointer resolves" — the whole-branch review then found four pointers that did not (the `gh` bundle's "steps 1-4 below" and "per the rule above", the `mcp` bundle's item 4 by number and `current-pr` item 2 from `repo-wide` item 2). The agent's read was of the `mcp` bundles only, as instructed, and two of the four broke in the `gh` bundle; the other two were in-bundle pointers at absent numbered items that a linear read does not trip over. | observation | Guidance for #1993/#1994's proof tasks: the pointer check is a grep of every "step N", "item N", "above"/"below", and "see X" phrase in each composed bundle against that bundle's own labels, run on every transport's bundle — not a read-through of one. The pin test (`tests/issue-claims-pr-scan-composition.test.js`) now covers the four for these files. |
