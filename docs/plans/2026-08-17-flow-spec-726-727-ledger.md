# Open Items — flow run spec-726-727 (/challenge bare-#N mode + blast-radius ** glob fix)

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | review | #726 AC 1 (live evidence-or-accept-risk run against a throwaway issue) cannot be verified headlessly mid-pipeline — the mode ends in an AskUserQuestion; prose-level correctness is covered by review, the live run needs a human (or /demo) after merge | open | — |
| 2 | review/hindsight | framing-check never reads ## Gotchas, so evidence supplied by the bare-#N mode is invisible to a re-judgment — tangential design gap | deferred | Staged as backlog candidate (staged/reflect-1.md, defer-reason: tangential); resolves at the Review Console's Queue writes gate |
| 3 | review/hindsight | Negative-sweep ACs self-match their own plan file until wrap-up deletes it — authoring-guidance pattern worth one sentence in docs/skill-authoring.md | deferred | Staged as observation (staged/reflect-2.md); routes via wrap-up's Skills curation row |
