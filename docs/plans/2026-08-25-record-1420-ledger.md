# Open Items — dispatch grouping: generic Key Files directory entries merge unrelated records into one mega-group (#1420)

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | wrap-up | PR #1445 — `gh pr list --state open --json number,title,headRefName --limit 100` — open, head worktree-dispatch-record-1420 (this work) | accepted | Expected open state at residue-sweep time — this run's own PR-first draft PR; resolves via this same run's Phase 4 merge decision. |
| 2 | wrap-up | pipeline-run — `.claude-tweaks/pipelines/2026-08-24T183144-record-1094` — run-state.json status: clean, not under `.claude-tweaks/pipelines/archive/` — unrelated pre-existing residue from record #1094, surfaced by this run's residue sweep | deferred | remedy:auto attempted (`node hooks.js archive-run --run {path}`) but failed `git-mv-failed` — this worktree-isolated session cannot perform the required main-checkout git mv. Below the materiality floor (size:low, priority:low, risk:low, purely mechanical) — routed to the digest (#1426) rather than a full backlog record. Defer-reason: blocked-external. |
