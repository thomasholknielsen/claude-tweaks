# Open Items — help policy mode: render a Shape-B derived default (housekeeping-auto-merge) as computed, not the literal false

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | review | Section 3's new snapshot-intrinsic finding source in `skills/help/policy.md` (commit ee895f1a) matches Shape A null-default keys (`integration-model`) as well as Shape B, not just Shape B as intended — duplicate/contradictory finding risk. Staged fix: `staged/review-1.patch`. | fixed | Applied via Review Console (autonomy: unattended, consoleAutoResolve) — see wrap-up commit. |
| 2 | wrap-up | Residue sweep: pr — PR #1031, open, head worktree-dispatch-record-636 (this record's own PR). | accepted | This is record #636's own in-flight PR — not leftover residue. Resolves via this same run's merge decision (Review Console), not a new record. |
| 3 | wrap-up | Residue sweep: pipeline-run — `.claude-tweaks/pipelines/2026-08-20T071039-record-545` — run-state.json status: clean, not archived (remedy: auto — archive under `.claude-tweaks/pipelines/archive/`). | accepted | Unrelated to record #636 (belongs to a different record's wrap-up run, #545). This session is explicitly prohibited from creating/entering a worktree (dispatching session's constraint for this run), so the scratch-worktree remedy in `wrap-up/residue-sweep.md` cannot be safely applied here. Left for the reconciler/`/tidy` sweep, which handles stale run-dir archival as routine housekeeping. Flagged in this run's final report to the dispatching session. |
