# Open Items — #1634: CLAUDE.md drift: CLAUDE — claude-tweaks Pipeline

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | build | Restoring the missing "Auto mode" paragraph pushes CLAUDE.md from 149 to 151 lines, 1 line over the `harness-health-always-loaded-budget` policy value (150, no project override). No test gates on this (the separate byte-budget test still passes at 22586/24576 B), but it will surface as a mechanical `template-conformance` finding on the next `/harness-health` sweep and opens a "CLAUDE.md & rules" curation row at this run's own Review Console (`computeClaudeMdOverBudget`). Flagged by this build's final whole-branch review; out of scope for this record's own Acceptance Criteria (restoring one specific paragraph verbatim), so not silently fixed by trimming unrelated content in this same branch. | resolved | Wrap-up Phase 2's CLAUDE.md & rules curation row (this run) staged a backlog-record proposal (`staged/wrap-up-claude-md-1.md`, Defer-reason: needs-human-decision — no safe mechanical trim identified without editorial judgment). Resolves at the Review Console (Q# auto-resolved under `consoleAutoResolve`, autonomy: unattended) — see decisions.md. |
| 2 | wrap-up | Residue sweep: PR #1913 (this run's own head branch `worktree-dispatch-record-1634`) reported open — expected in-flight state before Phase 4's merge decision, not left-behind residue. | resolved | Resolves via Phase 4's Auto-merge short-circuit / merge decision in this same wrap-up run (see decisions.md). |
