# Open Items — Dispatched review agents leave probe files in the worktree; no mandated scratch location or post-fan-out untracked-file sweep (#2022)

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | build | Architecture deviation: `plugin/skills/review/review-summary-template.md` was modified to add a `### Fan-out leftovers` slot (AC2's "named in the review output" requirement) — not in the spec's original Key Files list. Classification: Update the spec (reality is correct; the spec's Key Files list was incomplete, since AC2 structurally requires a user-facing output slot the spec never named). | open | — |
| 2 | build/skill | `skill-prose-conformance-tests` extension: a test authored against a multi-file concatenated corpus (this repo's `REVIEW_SKILL` pattern — SKILL.md + N lazy-loaded sub-files joined into one string) can silently pass while testing the wrong file, when the same literal appears in more than one concatenated file (Task 2's lens-dispatch SCRATCH test matched `step3-debate-and-refutation.md`'s copy, not `step3-lens-dispatch.md`'s own — caught by the final review, not the task review). Worth adding to that skill's Key Patterns/Anti-Patterns: scope a test to the single source file when a concatenated-corpus constant exists and more than one member file could carry the same literal. | observation | Caught and fixed in this build (commit 61cf71753); not re-filed as a separate record — routed here for /claude-tweaks:wrap-up's Skills curation row per the standard build/skill convention. |
| 3 | review | 3c Error Handling (confirmed via direct-verification override, low-tier single-read): the post-fan-out sweep's `{ctx-dir}` exclusion blindly exempted `agent-scratch/**` from scrutiny, so the Scratch rule's "delete before status word" promise (`_shared/subagent-output-contract.md:33`) had no verification anywhere — an agent that returned `DONE` without cleaning up would leave debris invisible to the only check this record adds. | fixed | Added an "Agent-scratch cleanup check" paragraph to `step3-lens-dispatch.md`'s post-fan-out sweep: `find {ctx-dir}/agent-scratch -type f` after the untracked-path diff, reported the same way as any other leftover. |
| 4 | review | 3c Error Handling (confirmed via direct-verification): `tests/multi-agent-coordination.test.js`'s new SCRATCH-line tests (refutation/gap-sweep blocks) computed their end marker via `indexOf(anchor, start)` with no `!== -1` guard before `.slice()` — a renamed/removed anchor would silently widen the slice to the rest of the concatenated corpus and could still pass against a sibling template's SCRATCH text, masking a real regression. | fixed | Added `assert.notStrictEqual(...End, -1, ...)` guards before the `refutationBlock`/`gapSweepBlock` slices (commit below). |
| 5 | review | 3c Error Handling (confirmed via direct-verification, low priority): same missing `-1` guard on `debateEnd`, but its paired assertion is `assert.doesNotMatch(..., /SCRATCH:/)` — an unbounded slice would still almost certainly contain `SCRATCH:` from a sibling template, so this instance fails loud rather than masking. | fixed | Same guard added alongside item 4, for consistency. |
| 6 | review/hindsight | Skill-worthy pattern (evaluation 5): the Scratch rule's cleanup promise is self-reported and the spec's own Technical Approach names `/design-wrapper`'s craft critics and `/docs-health`'s per-target dispatch as future fan-out sites that would "otherwise re-derive" this rule — but the rule as shipped didn't point future adopters at item 3's verification pattern (`step3-lens-dispatch.md`'s Agent-scratch cleanup check). | fixed | Added a one-sentence pointer to `_shared/subagent-output-contract.md`'s Scratch rule (sized to fit the file's 40KB ceiling — 4B headroom remaining). |
