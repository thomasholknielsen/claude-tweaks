# Open Items — Rotate the Claude Code OAuth credential exposed to a local probe transcript

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | build/ops | Rotate the Claude Code OAuth credential — `claude /logout` then `/login` (reason-not-auto: requires-signoff — an autonomous agent cannot drive an interactive OAuth login flow, and this session's own credential must not be logged out from within itself) | acknowledged | Staged and filed as backlog record #938 — landing: born-ready (defer-reason: blocked-external) |
