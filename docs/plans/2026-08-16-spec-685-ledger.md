# Open Items — spec 685 (tidy report rendering: width discipline, fenced columns, command-grouped Yours)

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | build | `skills/build/SKILL.md` Common Step 1.7 routes only `surface ∈ web|mobile|desktop` to `/claude-tweaks:design-wrapper pre-build`, but `skills/design-wrapper/terminal-routing.md`'s outcomes table says `pre-build` **runs** on the terminal track (loads `_shared/terminal-ux.md` + `_shared/design-craft.md`). A `Surface: terminal` record (this one) never reaches it — the track's pre-build value is unwired. Not this record's scope. | deferred | → backlog (ledger narrowing, unattended): expands pipeline scope / design decision outside this diff — record proposal staged at staged/ledger-record-terminal-prebuild-wiring.md |
| 2 | build/skill | [route: D5] Plan-authoring pattern from #685's final review: when a plan adds a *gate* over an existing output (here a conformance scan over the tidy report), trace the gate's rows against the existing *producers* of that output (`scan-procedures.md`'s Yours routing — record-less `[health]`/`[doctor]` rows) and against the gate's own alternate output shapes (the digest), not only against the template. Two Important findings lived in exactly those cross-file traces. | observation | Staged for Review Console / Upstream feedback row (self-reference → project record: add a gate-over-producers check to build Spec Step 3); see also staged/reflect-5.md |
| 3 | build | Architecture alignment: 2 Beneficial deviations applied to the materialized spec (Aligned conformance row; ref-less closing + record-less `—` rows) — see "Align the materialized spec" commit; 2 Update-the-spec deviations staged (`staged/build-deviation-3.md` — Next Actions AC assumes AskUserQuestion, superseded by #646; `staged/build-deviation-4.md` — the "≤ ~40 lines" AC measures the Yours section, not the whole report). | observation | Staged for Review Console |
| 4 | review | Lens 3c (reproduction pair, both retried after an API session-limit kill): 5 prose gaps in `step-6-auto.md` — digest budget vs Approve-in-full, ==40-line boundary, one-record batch line, batching across differing `PIPELINE_RUN_DIR` prefixes, command-less `[doctor]`/`[health]`/`[pattern]` rows with no group key/closing line. Elevated via direct-verification override (source read: `scan-procedures.md:344-402`); routed AUTO under ceiling `medium`. | fixed | Applied in commit b63062d9 (grouping rule: prefix rows never batched, k=1 batch line, new final `review ({k})` group for command-less rows + conformance exemption; digest: budget yields to Approve, "At 40 lines or fewer"); tests 41/41. |
| 5 | review/hindsight | Observation: fenced rows + per-record paste lines make the 40-line digest the common path for real sweeps (journey example: 58 lines) — by design, but the threshold/digest shape deserves re-evaluation after 3-5 real firings. Staged at staged/reflect-1.md. | observation | Staged for Review Console |
| 6 | review/hindsight | Tangential: a mechanical `bin/` conformance linter over the rendered tidy report (the scan rows are already mechanical). Staged at staged/reflect-2.md as a backlog candidate. | deferred | Staged for the Review Console's Queue writes gate (record proposal with Title/Type/Labels header) |
| 7 | review/hindsight | Tangential (record #685's own named follow-on): `#N,#M` batch argument for `/specify` and `/demo` so their Yours groups collapse to one batch line. Staged at staged/reflect-3.md as a backlog candidate. | deferred | Staged for the Review Console's Queue writes gate |
| 8 | wrap-up | pr — PR #699 — open, head worktree-flow-spec-685 (this work); residue sweep, blast-radius scope | fixed | This run's own draft PR — merged by Phase 4's pr-first execution step in this same wrap-up (merge-when-green) |
