# Open Items — spec 685 (tidy report rendering: width discipline, fenced columns, command-grouped Yours)

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | build | `skills/build/SKILL.md` Common Step 1.7 routes only `surface ∈ web|mobile|desktop` to `/claude-tweaks:design-wrapper pre-build`, but `skills/design-wrapper/terminal-routing.md`'s outcomes table says `pre-build` **runs** on the terminal track (loads `_shared/terminal-ux.md` + `_shared/design-craft.md`). A `Surface: terminal` record (this one) never reaches it — the track's pre-build value is unwired. Not this record's scope. | open | — |
| 2 | build/skill | [route: D4] Plan-authoring pattern from #685's final review: when a plan adds a *gate* over an existing output (here a conformance scan over the tidy report), trace the gate's rows against the existing *producers* of that output (`scan-procedures.md`'s Yours routing — record-less `[health]`/`[doctor]` rows) and against the gate's own alternate output shapes (the digest), not only against the template. Two Important findings lived in exactly those cross-file traces. | observation | Staged for Review Console / Memory curation |
| 3 | build | Architecture alignment: 2 Beneficial deviations applied to the materialized spec (Aligned conformance row; ref-less closing + record-less `—` rows) — see "Align the materialized spec" commit; 2 Update-the-spec deviations staged (`staged/build-deviation-3.md` — Next Actions AC assumes AskUserQuestion, superseded by #646; `staged/build-deviation-4.md` — the "≤ ~40 lines" AC measures the Yours section, not the whole report). | observation | Staged for Review Console |
