# Open Items — 820: Reduce SessionStart reconcile() latency

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | build/skill | `gh-api-module-pattern` documents a sync injectable-runner shape (execFileSync-based); this build's `bin/lib/reconcile/gh-pool.js` establishes a sibling pattern the skill doesn't cover — a concurrency-capped async pool (`execFile`+`Promise`, order-preserving, per-item try/catch so one failure never aborts the batch) for parallelizing `gh` call fan-out. Worth folding into that skill as a documented pattern for future async gh-call work. | observation | Not blocking — noted for a future skill update, not actioned in this build. |
| 2 | ops | This branch (`worktree-issue-820-reconcile-latency`) was 73 commits behind `origin/main` at pre-flight (branch-divergence-check, Step 2.5). Continued per `auto` mode default — only `docs/plugin-structure.md` overlaps this build's file set, low conflict risk. | open | — |
| 3 | review | High-severity performance finding: D5's parallelization was incomplete — `resolvePrState` (a per-active-claim `gh pr list` call) stayed serial inside `release-merged.js`'s Phase 1 for-loop instead of routed through gh-pool, despite firing for every active claim with a resolvable branch. Under the shared 18s budget, this could starve later checks of budget entirely. | open | Staged — `staged/review-1.patch`. |
| 4 | review | Medium-severity architecture finding: `release-merged.js`'s final `writeCache` call spread the `cache` snapshot read at function entry (before Phase 1-3's async gh-pool calls) rather than re-reading immediately before writing — a concurrent reconcile() process's fresher `lastRunAt` write landing inside that window was silently reverted. | open | Staged — `staged/review-1.patch`. |
| 5 | review | Medium-severity architecture finding: `index.js`'s red-tip dispatch was gated only on the shared fetch not having failed (`sharedFetchOk`), never on it having actually run — a future `checks` subset requesting red-tip without mirror/remote-prune would silently read a never-refreshed local ref. | open | Staged — `staged/review-2.patch`. |
| 6 | review | Low-severity architecture finding: `budget.js`'s `createBudget` called `Date.now()` internally rather than accepting an injectable clock, unlike `cache.js`'s documented pure `isFresh` convention, forcing its tests to use real sleeps. | fixed | Auto-applied — commit `74a57f67`. |
