# Open Items — record #835, #834 (feedback: Next Actions plain markdown; gh issue create retry wrapper)

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | review | Security (3b): `withTransientRetry` wraps `gh issue create` (non-idempotent) the same as the idempotent dedup search/read-back — a transient-looking failure whose response was lost after GitHub actually processed the create risks filing a duplicate issue, with no re-check between the failed attempt and the retry. `plugin/bin/lib/feedback/file-feedback.js`'s `fileOne`/`fileDraft`. | open | Staged — `staged/review-1.patch` |
| 2 | review | Error handling (3c): the retry ceiling (1 retry = 2 total attempts) is below record #834's own Gotchas worst case (5 attempts across two hand-rolled loops before a filing succeeded), and neither the code comments nor SKILL.md's new retry prose document why a lower ceiling is acceptable. `plugin/bin/lib/feedback/file-feedback.js`'s `withTransientRetry` default. | open | Staged — `staged/review-1.patch` |
| 3 | review | Error handling (3c, low): `sleepSync`'s `try { Atomics.wait(...) } catch { /* best-effort */ }` silently degrades to a zero-wait immediate retry if `Atomics.wait` fails, instead of the documented 15s backoff. | accepted | Intentional pattern — mirrors the existing precedent in `bin/lib/file-lock.js`'s own `sleepSync` (same try/catch-and-ignore shape, per that file's code comment this diff explicitly follows); not a new risk this diff introduces. |
