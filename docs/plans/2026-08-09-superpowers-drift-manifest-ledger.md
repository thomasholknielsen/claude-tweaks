# Open Items — Add a superpowers entry to tools/upstream-drift/manifest.yml (#262)

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | flow | Run dir lives in the worktree (harness isolation blocks main-checkout writes), contradicting `_shared/pipeline-run-dir.md` anchoring — wrap-up must archive it as tracked files on the feature branch so it survives teardown via merge | open | Committed to applying `cleanup-procedures.md` Section C step 3.5's transitional-guard equivalent at Step 10's archival — not yet executed (Step 10 hasn't run). |
| 2 | flow | Feedback candidate: `hooks.js record-worktree` anchors at the main checkout and latched the stale `2026-08-09T054437-dispatch-standalone` run (IL-96 fallback-latch class; repaired via close-run) — proposal staged at `staged/feedback-record-worktree-anchoring.md` | fixed | `/claude-tweaks:feedback`'s self-reference check fired (this repo IS claude-tweaks) — D5 collapsed per `_shared/learning-routing.md`. Re-checking the premise (IL-71) found it false: `record-worktree --run` already ships. Corrected, narrower finding (resolution has no worktree-local fallback for item 1's own harness-isolation case) filed as thomasholknielsen/claude-tweaks#280. Superseded staged file deleted. |
| 3 | build | `docs/skill-authoring.md` claims brainstorming has a numbered "Step 5/7/8" structure — installed superpowers 6.2.0's `brainstorming/SKILL.md` has no such numbering (confirmed by Task 1's sweep and independently re-confirmed by Task 2); the real coupling (brainstorming's review-gate wait) exists but under an unnumbered "User Review Gate" subsection instead. Independent follow-up, out of #262's scope (about docs/skill-authoring.md, not manifest.yml). | fixed | Corrected to "User Review Gate" / "Spec Self-Review" terminology, verified against installed superpowers 6.2.0. Commit `03bb142a`. |
| 4 | review | `skills/wrap-up/cleanup-procedures.md:118` claims finishing-a-development-branch's Option 2 "only runs `git push` — it never calls `gh pr create`" — verified false against installed superpowers 6.2.0: Option 2's text instructs creating the PR "with the forge's tooling — its CLI if one is available" (finishing-a-development-branch/SKILL.md ~line 121-123). Discovered by the final whole-branch review, out of #262's scope. | fixed | Corrected to describe the real gap (no guarantee the PR body carries a closing keyword), verified against installed artifact. Commit `1785e077`. |
| 5 | review/hindsight | [skill: NEW - self-referential-audit-artifacts] A self-referential audit artifact caused two bugs in this build: the sweep ledger's own committed path matched its own enumeration command (fix round 1), and its hand-transcribed literal table diverged from `manifest.yml`'s authoritative copy with no mechanical link (final-review fix). Candidate `[IL-nn]` CLAUDE.md Don't. Staged at `staged/reflect-1.md` for Wrap-Up Review Console. | deferred | Staged proposal — resolved at Wrap-Up Review Console / Queue writes gate. |
