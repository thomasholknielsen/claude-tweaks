# Open Items — #1806: Several skill files sit within ~150 bytes of the 40KB content-cost ceiling on main

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | review | Three of the five trimmed files (`tidy/step-6-auto.md` 97.9%, `tidy/scan-procedures.md` 95.15%, `specify/SKILL.md` 91.6% of `CEILING_BYTES`, canonical bytes) still sit in the 90-100% WARN band after #1806's trim — AC1/AC2 not fully met for these three, even though the hard ceiling test passes. `ceremony-profile: fast-lane` skips the Spec Compliance gate that would otherwise have caught this at review time; re-verified independently anyway. | deferred | Filed as follow-up #1881 (origin: #1806 review; files: `plugin/skills/tidy/step-6-auto.md`, `plugin/skills/tidy/scan-procedures.md`, `plugin/skills/specify/SKILL.md`; trigger: next content addition to any of the three, especially `step-6-auto.md` at only 858B margin) |
| 2 | review | `splitFrontmatterFence` (`plugin/bin/lib/health-core/frontmatter-list.js`) splits on bare `\n` and never strips `\r`, so it silently returns `null` for every frontmatter-fenced file on this `core.autocrlf=true` checkout — not in #1806's diff (file untouched), discovered while independently re-deriving `context-cost.test.js`'s 3 failures from raw artifacts rather than trusting the prior call's `decisions.md` claim. Affects 6 production call sites beyond this test. | deferred | Filed as follow-up #1882 (origin: #1806 review; files: `plugin/bin/lib/health-core/frontmatter-list.js` and its 6 consumers; trigger: any Windows/`core.autocrlf=true` contributor checkout) |
