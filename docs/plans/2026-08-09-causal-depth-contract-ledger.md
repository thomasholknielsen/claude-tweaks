# Open Items — Causal-depth contract (#264)

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | ops | Pre-flight merge-check: `origin/main` was 24 commits ahead, including v6.74.0 (reason-not-auto: requires-judgment — decided to merge before continuing rather than accept the divergence, since this run's version-bump deliverable would otherwise collide with the shipped tip) | acknowledged | Merged `origin/main` cleanly, no conflicts, before proceeding. |
| 2 | build | Plan file `docs/superpowers/plans/2026-08-09-causal-depth-contract.md` (lines 14, 250) still states the pre-fix, now-incorrect claim that `light-mode.md` inherits the Near-misses Chain Walk binding — the shipped contract (`docs/skill-graph.md:271`) correctly states it does not. | deferred | Historical planning artifact, not live contract prose — no consumer reads the plan file after the build ships. Revisit only if the plan is ever reused as a reference. |
| 3 | build | The original leaf record (#264) scoped a `.claude-plugin/plugin.json` version bump as its own Deliverable 5/Acceptance Criterion 12, which `tests/changelog-coverage.test.js` requires a paired CHANGELOG.md entry + shipped-versions.tsv line for in the same commit — a scoping error caught only by the full test suite, not by task-scoped review. Reverted; versioning happens via `node bin/release.js` at actual release time per CLAUDE.md's Releasing section. | fixed | Reverted in commit a4096509 — see Architecture Alignment Check (Common Step 4.5). |
| 4 | review/hindsight | The `causal-depth[debug]:`/`causal-depth[near-miss]:` bracket-tag decisions.md prefix is a new lexical convention not matching existing entries elsewhere in the codebase — deliberate (required by the final review's path-discrimination finding), not accidental drift. | deferred | Stage path: staged/reflect-1.md. Revisit if a second decisions.md producer wants the same sub-classification shape. |
| 5 | review/hindsight | Skill-worthy pattern: "inline `_shared` contract over new component skill, when the caller already holds full context" — reasoned independently by this build and framing-check, undocumented as a reusable criterion. | deferred | Stage path: staged/reflect-2.md — backlog candidate, surfaces at Queue writes gate. |
