# Open Items — Causal-depth contract (#264)

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | ops | Pre-flight merge-check: `origin/main` was 24 commits ahead, including v6.74.0 (reason-not-auto: requires-judgment — decided to merge before continuing rather than accept the divergence, since this run's version-bump deliverable would otherwise collide with the shipped tip) | acknowledged | Merged `origin/main` cleanly, no conflicts, before proceeding. |
| 2 | build | Plan file `docs/superpowers/plans/2026-08-09-causal-depth-contract.md` (lines 14, 250) still states the pre-fix, now-incorrect claim that `light-mode.md` inherits the Near-misses Chain Walk binding — the shipped contract (`docs/skill-graph.md:271`) correctly states it does not. | deferred | Historical planning artifact, not live contract prose — no consumer reads the plan file after the build ships. Revisit only if the plan is ever reused as a reference. |
| 3 | build | The original leaf record (#264) scoped a `.claude-plugin/plugin.json` version bump as its own Deliverable 5/Acceptance Criterion 12, which `tests/changelog-coverage.test.js` requires a paired CHANGELOG.md entry + shipped-versions.tsv line for in the same commit — a scoping error caught only by the full test suite, not by task-scoped review. Reverted; versioning happens via `node bin/release.js` at actual release time per CLAUDE.md's Releasing section. | fixed | Reverted in commit a4096509 — see Architecture Alignment Check (Common Step 4.5). |
| 4 | review/hindsight | The `causal-depth[debug]:`/`causal-depth[near-miss]:` bracket-tag decisions.md prefix is a new lexical convention not matching existing entries elsewhere in the codebase — deliberate (required by the final review's path-discrimination finding), not accidental drift. | deferred | Stage path: staged/reflect-1.md. Revisit if a second decisions.md producer wants the same sub-classification shape. |
| 5 | review/hindsight | Skill-worthy pattern: "inline `_shared` contract over new component skill, when the caller already holds full context" — reasoned independently by this build and framing-check, undocumented as a reusable criterion. | deferred | Stage path: staged/reflect-2.md — backlog candidate, surfaces at Queue writes gate. |
| 6 | wrap-up | Near-miss: the fix-wave subagent stalled waiting on a Monitor notification before committing, then reported false completion. Causal-depth chain walk (dogfooding the contract this build ships) rendered `terminal` — this recurs the already-ruled `[IL-108]` hazard, not a new gap. | deferred | Stage path: staged/reflect-3.md — evidence IL-108 is still live; not an argument to change anything now. |
| 7 | wrap-up | Residue: worktree `dispatch-240-235-239` — unlocked, not merged into `main`, in reaper domain. Unrelated to #264's own diff. | fixed | The "not merged" hook claim was stale — direct inspection found a clean tree, 0 commits ahead of `origin/main`. Worktree removed, local + remote branch deleted (also resolves item 15). |
| 8 | wrap-up | Residue: worktree `flow+spec-271-267` — locked by a live session (pid 57852). Unrelated to #264's own diff. | acknowledged | Staged as a backlog record — no safe action from this session on another live session's locked worktree. |
| 9 | wrap-up | Residue: worktree `reread-cache-claim-unify` — unlocked, not merged into `main`, in reaper domain. Unrelated to #264's own diff. | fixed | The "not merged" hook claim was stale — direct inspection found a clean tree, 0 commits ahead of `origin/main`. Worktree removed, local + remote branch deleted (also resolves item 16). |
| 10 | wrap-up | Residue: worktree `self-maintaining-fleet-design` — unlocked, in reaper domain, but flagged by this session's own SessionStart hook as holding local content that exists nowhere else. Deleting would be irreversible. | fixed | The hook's warning was stale — direct inspection (via EnterWorktree) found a clean tree, 0 commits ahead of `origin/main`; its work was already merged and archived (refs #265). Removed the worktree and deleted the fully-merged local branch. |
| 11 | wrap-up | Residue: worktree `superpowers-drift-manifest` — locked by a live session (pid 31291). Unrelated to #264's own diff. | open | Awaiting Phase 2 drill. |
| 12 | wrap-up | Residue: worktree `wrap-up-residue-sweep` — unlocked, not merged into `main`, in reaper domain. Unrelated to #264's own diff. | open | Awaiting Phase 2 drill. |
| 13 | wrap-up | Residue: worktree `issue-236-sync-surface-tests` — unlocked, outside the reaper's own domain (no reaper collects it). Unrelated to #264's own diff. | open | Awaiting Phase 2 drill. |
| 14 | wrap-up | Residue: remote branch `origin/issue-236-sync-surface-tests` — merged into `origin/main`, not deleted. | open | Awaiting Phase 2 drill. |
| 15 | wrap-up | Residue: remote branch `origin/worktree-dispatch-240-235-239` — merged into `origin/main`, not deleted. | fixed | Deleted alongside item 7's worktree cleanup. |
| 16 | wrap-up | Residue: remote branch `origin/worktree-reread-cache-claim-unify` — merged into `origin/main`, not deleted. | fixed | Deleted alongside item 9's worktree cleanup. |
