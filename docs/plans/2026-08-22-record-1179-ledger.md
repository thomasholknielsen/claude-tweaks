# Open Items — #1179: consoleAutoResolve default-merge must not override a merge-check needs-human verdict

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | flow/ops | Pre-flight branch-divergence (Step 2.5): origin/main is 83 ahead of local main (local main 50 ahead, diverged both ways) — worktree branched from local HEAD; catch-up merge with origin/main runs in build Step 1 | fixed | Worktree caught up via clean merge of origin/main — commit `9fdbac75`; full suite green post-merge (5920/5920). Local-main↔origin divergence itself is repo-level state outside this run's scope |
| 2 | review | Lens 3e (high, direct-verified): `_shared/console-execution.md`'s reconciler-side `consoleAutoResolve` executor — the second sanctioned caller of the capability — has no needs-human carve-out: it treats every console item as floor-clearing ("no narrower per-item test today") and routes through review-console's On-approval steps 1-6 incl. branch-finish/merge, with zero occurrences of "merge-check"/"needs-human" in the file, so a verdict this record pins as authoritative cannot reach a later reconciler pass. Fix needs a verdict-persistence mechanism (console.json field at post time, or decisions.md read) + executor prose + test — Defer-reason: genuinely-larger | open | Staged as work-record proposal `staged/queue-record-console-execution-needs-human.md` for the Review Console (Q# queue write) |
| 3 | review | Lens 3e (low, auto-applied): docs/skill-graph.md row for `wrap-up/review-console.md` (dispatch section) predated the carve-out — edge description didn't capture that the Layer-2 needs-human verdict now survives into the console short-circuit (CLAUDE.md Cross-references: edge changes land in skill-graph) | fixed | One-sentence edge update appended to the row, committed with review fixes |
| 4 | test | Full-suite re-derivation (second call): 5918/5920. store-concurrency failure is upstream #1192 (byte-identical to origin/main, acquireLock ENOENT fail-open on missing lock parent dir — pre-existing, not this branch); pr-state async failure was a load flake (isolated re-run green) | observation | Caveats informational — TEST_PASSED=true (PASS_WITH_CAVEATS); no action for this record |
