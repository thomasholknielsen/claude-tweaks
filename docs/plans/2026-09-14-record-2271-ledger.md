# Open Items — Warn dispatched subagents that Monitor/background-task notifications never reach them (#2271)

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | wrap-up | Near-miss/Friction: this record's run dir (dispatch's two-call `build,test` then `review,polish,wrap-up` pattern) arrived at the second call with `decisions.md`/`events.jsonl`/`run-state.json` already populated by the first call's build/test phase, but no `config.yml` — flow's Step 3 Manifesto appears not to have written it during the first call despite Step 3 being unconditional. This call initialized `config.yml` in place per the "adopt an empty run dir" branch. Evidence: `ls` of the run dir before this call's Manifesto step showed `context/`, `decisions.md`, `events.jsonl`, `run-state.json` but no `config.yml`. Cost this session: ~5 minutes re-deriving and writing the 13 lever values that a completed first call should have already supplied. | observation | Routed below materiality floor (pre-existing-outside-diff) — digest #1426 comment https://github.com/thomasholknielsen/claude-tweaks/issues/1426#issuecomment-5657007652 |
| 2 | wrap-up | Friction: the working-directory/git-discipline `PreToolUse` guard denied a plain, non-git `node .../log-decision.js --text "..."` call because the `--text` string's prose happened to contain the substring "git show" (part of a sentence describing evidence-gathering method, not an actual git invocation) — the guard's heuristic apparently pattern-matches command text for git-like phrases rather than parsing actual subcommands. Evidence: denial message "this command names git in a form too complex to verify that it stays inside the worktree... Refusing to run it" on a call with zero git subcommand. Cost this session: one retry (~30s) after rewording the `--text` value to avoid the phrase. | observation | Routed below materiality floor (pre-existing-outside-diff) — digest #1426 comment https://github.com/thomasholknielsen/claude-tweaks/issues/1426#issuecomment-5657007652 |
| 3 | wrap-up | residue-sweep (`remedy: record`): this run's own draft PR #2388 (head `worktree-record-2271`) is still open — needs an explicit merge/stay-open decision before this pipeline closes. Record carries `auto:merge`; ran the Auto-merge gate. | accepted | Auto-merge gate ran (no drain-overlap hold; Authorization cleared, auto:merge already live). Content judgment (`assess-agent-autonomy merge-check`) returned `needs-human`: the diff touches an agent-instruction file (`_shared/subagent-dispatch-core.md`) with a genuine new behavior-shaping bullet (Foreground Execution Discipline) — the refutation test could not clear it. Falls back to the normal pending-review path per `settle-and-merge.md`'s "Any layer fails" branch; PR #2388 stays open for a human to merge via the Review Console. This is the run's expected pending-review terminal state, not a defect. |
