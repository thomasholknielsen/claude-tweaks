# Open Items — github-pr-scan's Detection Ladder still hard-skips on missing gh, while /tidy Step 4.7 no longer does (#172)

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | wrap-up | Residue sweep (`node bin/residue.js --scope blast-radius`) found orphaned clean pipeline-run dir `.claude-tweaks/pipelines/2026-08-19T210238-record-229` (sessionId `b469bab9-7023-4d1c-b604-4281d38d9de4`). Its sibling orphan, `2026-08-19T211314-record-310`, archived cleanly (Phase-1 fix-now). This one's archival was refused with `git-mv-failed` — its `work/229-spec.md` is untracked in the main checkout's own git index. This run dir's own decisions.md (from an earlier session in this same worktree) independently flagged a possible orphaned duplicate concurrent #172 build in one of two worktrees (`agent-a195d82021a1eb581` / `agent-ac38faa0798646d5b`) denied by the worktree-pinning gate — record-229's untracked spec is consistent with being exactly that abandoned attempt, but this session did not chase down which worktree it lives in or confirm it is safe to force-archive (risk of cross-session interference with a still-live session, the same class of incident already recorded once in this run). | deferred | Filed #953 (Defer-reason: blocked-external — cannot confirm from this session whether a live sibling session still owns the worktree holding this orphaned spec; forcing resolution risks the same cross-session interference class already recorded once in this run). |
