# Open Items — CLAUDE.md drift fixes (#1429, #993)

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | wrap-up | pr — PR #1525 — open, head worktree-dispatch-record-1429-993 (this work) — residue-sweep `--scope blast-radius` | accepted | This run's own draft PR is the intended terminal state of a `pending-review` outcome (the bundle's Auto-merge gate Authorization layer requires every member to carry `auto:merge`; #1429 does not, so the whole group falls back to the normal pending-review path per `dispatch/settle-and-merge.md`). Not dropped work — it stays open awaiting a human merge decision. |
