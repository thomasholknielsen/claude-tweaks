# Open Items — 257: build: pre-dispatch verification pass over each task's own stated acceptance command

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | test | `npm test` shows 9 pre-existing failures unrelated to this build's diff: 3 in `tests/impeccable-plugin-contract.test.js` (installed Impeccable 4.1.1 vs pinned 4.0.2 — environment mismatch), 2 in `tests/hooks-dispatcher.test.js` (permission-denial simulations that don't fail under root), 2 in `tests/hooks-session-start.test.js` + 1 in `tests/console-execute.test.js` + 1 in `tests/bin-lib/reconcile/prune-remote.test.js` (live-remote/reconcile-cache timing). This build touched only `plugin/skills/build/plan-audit.md` and `plugin/skills/build/SKILL.md`; none of the 9 failing tests reference those files or Common Step 1.5/Check A/B/C content. | accepted | Reproduced in isolation (`node --test` per file, same failures both in the full suite and standalone) both before and unrelated to this diff — baseline environmental failures, not introduced by this build. |
| 2 | review | Spec compliance re-verified independently against raw diff (`git diff main..HEAD -- plugin/skills/build/plan-audit.md plugin/skills/build/SKILL.md`): all 6 acceptance criteria met, including AC6's literal `grep -l "Check C" ...` command re-run and confirmed. Test-failure claim in item #1 independently re-derived: mapped each of the 9 failing subtest names to its source file via `grep -rl`, confirmed the 3/2/2/1/1 file breakdown matches, and confirmed the diff's two touched files (`plan-audit.md`, `SKILL.md`) are referenced by none of the 9 failing tests (`grep` for "plan-audit\|Check A\|Check B\|Common Step 1.5" across `tests/` returns nothing) — structurally rules out this diff as the cause. Code review (Convention/Architecture lenses, `review-effort: medium` from `risk:medium`×`size:medium`, no floor set): Check C's heading matches Check A/B's exact style (`## Check {Letter} — {desc} ({always runs\|conditional})`), no stale "Check A and Check B"/"Two checks" cross-references remain elsewhere in `plugin/`, no merge-provenance contamination (`git log --merges main..HEAD` empty). Non-frontend spec (`surface: backend`) — visual review and Design Quality Pass (Step 6/6.5) skipped by detection, not by omission. Verdict: PASS, zero findings. | resolved | No action needed — clean review. |
