# Open Items — record-worktree without --run corrupts a different live session's run-state.json (#1124)

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | review | Lens 3b (Security): `resolveRunArg` accepts any real anchored directory as a valid `--run` target with no `isInitializedRunDir` check outside the #280 fallback branch — a malformed-but-real `--run` value (e.g. stray `--run .`) silently writes into an arbitrary directory; empirically reproduced | deferred | Filed #1566 (`needs:definition`, `Defer-reason: needs-human-decision`) — a naive unconditional fix broke an existing `record-pr` unit test (dispatch's mint-then-claim two-call handoff legitimately writes to a freshly-mkdir'd, not-yet-initialized run dir), revealing a real design question that needs a human call |
| 2 | review | Lenses 3a/3b/3c: the flag-shaped worktree positional guard (`record-worktree --run <dir> --help`) had no regression test coverage, despite being the exact shape every observed pre-fix incident hit | fixed | Added regression test in `tests/hooks-dispatcher.test.js`; red/green-verified against the guard — commit `de414139` |
| 3 | review | Lens 3c: `plugin/skills/build/worktree-setup.md:91` still quoted a `claude-tweaks: no pipeline run dir found — worktree not recorded` message `record-worktree` can no longer print (that branch was replaced by this fix's `--run` hard-require) | fixed | Updated doc to name the actual current failure messages — commit `de414139` |
| 4 | review | Lens 3c: `hooks.js` comment overclaimed the no-`--run` refusal happens "before `resolveRunArg` even had a chance to read...anything" — `resolveRunArg` runs unconditionally and, on that path, still calls `resolveRunDir` (a real sibling-run scan) before the result is discarded | fixed | Corrected comment to state "no write," not "no read" — commit `de414139` |
| 5 | wrap-up | Reflect (light, Near-misses): this run's own safety nets caught two near-regressions before they landed — (a) an exploratory unconditional-`isInitializedRunDir` fix for item 1 above silently broke `record-pr`'s existing `--run` pinning test, caught only because the targeted suite was run before committing; (b) the dispatched code-simplifier's one edit was to code outside #1124's own diff (`teardown-run`'s ternary, untouched by this branch), caught only by diffing the simplifier's output against the record's own change scope per `simplify/SKILL.md`'s caller-side discipline. Both nets worked as designed this time — flagging as a pattern worth remembering, not a defect | observation | Both existing disciplines (targeted-suite-before-commit; diff-against-own-scope after a file-path-scoped simplify dispatch) are already documented in `simplify/SKILL.md` and this project's own verification practice — no new skill/doc update needed, logged here as confirmation the nets hold |
| 6 | wrap-up | Reflect (light, Fresh start): a from-scratch design of `resolveRunArg`'s shared validation (used by 8 verbs) would likely centralize the "is this actually a run dir" check once, with an explicit exception for the dispatch mint-then-claim first-write case, rather than the current implicit gap (checked only in the #280 fallback branch) | deferred | Same underlying question as ledger item 1 — already filed as #1566; no duplicate record created |
| 7 | wrap-up | CLAUDE.md & rules curation: `docs/donts.md:173`'s don't-probe-`--help` rule names its own removal condition ("remove this rule once the mechanical `--help` guard record ships") — #1124 ships that guard, but only for `record-worktree`; `record-pr` has no guard at all and `close-run` only refuses on a foreign-session-owned target, both still tracked by open #1012. The rule is now factually imprecise (implies all three verbs are still vulnerable) rather than fully obsolete | fixed | Narrowed the rule to name record-worktree as fixed (#1124) and record-pr/close-run as still open (#1012) — applied at the Wrap-Up Review Console (`consoleAutoResolve`, unattended) |
| 8 | wrap-up | Residue sweep: PR #1565 (head `worktree-dispatch-record-1124`) is still open | accepted | Expected residue at this point in the run — this record's own PR, whose merge/park disposition is Phase 4's own Review Console / auto-merge-short-circuit decision, not a dangling artifact |
