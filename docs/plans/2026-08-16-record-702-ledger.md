# Open Items — #702 specify: comma-list batch shaping + argument-hint check rule

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | flow | Step 2.5 branch-divergence-check: origin/main was 14 commits ahead of local main HEAD 676838ad at run start (auto: continued anyway) | fixed | Worktree branch caught up by post-creation catch-up — ff to 2533f1f7, then to 24b03525 (v6.89.0 release) after the baseline run; branch is at origin/main's tip. Local main checkout is the reconciler's to advance |
| 2 | build | Final-review residual (minor): `skills/specify/shaping-mode.md`'s Metadata block section still reads, in isolation, as if the sniff + batched design-intent question happen at that per-record step; the framing paragraph (sniff-first, then loop) is authoritative but the section itself doesn't say "already resolved upfront — this step only writes the values into the composed body". One-sentence tightening; not load-bearing | open | — |
| 3 | build/skill | Enrichment for `docs/skill-authoring.md`'s `argument-hint` bullet: `tests/argument-hint-input.test.js`'s bracket walker opens a group at every `[` — including ones nested inside the leading `<...>` alternation — so a hint like `<#N[,#M...]\|record-id[,id...]\|...>` makes `,#M...` and `,id...` leaves that must appear literally in `## Input` (`#`-led leaves are exempt; `,id...` is not). Bit this build (Task 1) and the plan's pre-flight scan; one sentence in the authoring doc would have prevented it | fixed | Sentence added to `docs/skill-authoring.md`'s `argument-hint` bullet in commit ed7daaae (Common Step 6.5 docs sync, inline) |
| 4 | review | 3c (Error handling), HIGH, confirmed by both reproduction agents: `skills/specify/SKILL.md` batch paragraph (`## Input`) and case-1 Batch branch say unresolvable elements are "reported in one message before any record is shaped, and the whole set then enters shaping mode" — self-contradictory (shape unresolved elements? shape the subset?) and at odds with `shaping-mode.md`'s "already resolved every element" precondition and `flow/materialize.md`'s hard stop. Proposed fix: state the hard stop — nothing shaped when any element fails to resolve | open | staged/review-1.patch (`git apply --check` clean) — awaits Review Console |
| 5 | review | 3i (Doc freshness, informational): `README.md` is registry-matched by `skills/**/SKILL.md` but was not updated in this work — its `/specify` mentions are mode-level ("shaping it in place / decomposing") and verified still accurate | observation | No stale content; nothing to change |
| 6 | review | 3c low-confidence (one of two agents): U1 duplicate-element dedup (medium), U2 empty-element message, U3 no unresolvable-message template, U4 whitespace-after-comma, U5 test-file `require` re-registers sibling tests — see staged/review-unconfirmed.md | observation | Surfaced at the Review Console's Low-confidence subsection; not routed |
