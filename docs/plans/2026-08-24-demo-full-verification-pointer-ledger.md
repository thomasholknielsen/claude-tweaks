# Open Items — demo: cli-surface Observation plan for a decomposed sub-issue skips end-to-end manual verification guidance

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | review/hindsight | `buildNativeParentQuery` triplicates the GraphQL envelope string already used by `buildNativeSubIssuesQuery`/`buildNativeDependencyQuery` (`plugin/bin/lib/issues/record.js`) | deferred | Staged to `staged/reflect-1.md` for the Wrap-Up Review Console. Not collapsed now — record #1194's own spec flags two other open records (#1309, #1224) concurrently touching `record.js`; a shared-helper refactor now risks the merge conflict that warning names. Follow-up backlog record recommended once those land. |
| 2 | wrap-up | Surprises lens: a global `Read(**/tmp/**)` permission deny rule blocked `build-review-context.js --run` pointed at a worktree-local mirror path, blocking 2 of 8 lens-review subagents mid-`/review` | fixed | D4 (memory/environment fact) — written directly to the operator's memory system (`build-review-context-run-flag-tmp-path-deny.md`), not staged — no separate consumer for a personal operational lesson. |
| 3 | wrap-up | Friction lens: this run's `events.jsonl` carries a misattributed `ask-user-question` event from a different record's concurrent session (#1275/#800/#1369); this run's own git-strategy question is absent from the log | deferred | Staged to `staged/reflect-2.md` for the Wrap-Up Review Console — D5/upstream observation on the hooks layer's `"attribution":"fallback"` path. |
