# Open Items — Conversation: sync 6 stale pipeline ledgers + file backlog issue #879

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | wrap-up | Near-miss (systemic, chain-walked 3x): used a bare `git update-ref refs/heads/main origin/main` instead of this project's sanctioned `git merge --ff-only` mechanism (`plugin/bin/lib/reconcile/mirror-ff.js`) to fast-forward the shared main checkout, desyncing the working tree/index from the ref. Caught before any commit/push (proved `git diff --stat HEAD` matched exactly the known prior commit range), recovered via `git reset --hard HEAD`. Existing memory `shared-checkout-branch-volatility.md`'s phrase "prefer checkout-free ff ref updates" is ambiguous enough to have licensed this literal misreading. [route: D4] | deferred | Routed to Memory (D4) by Phase 2's engine, staged at `staged/wrap-up-memory-1.md`. Resolves at this run's own Review Console — Memory updates section — not a genuine 3-way judgment call for the resolve-gate drill (already correctly routed). |
| 2 | wrap-up | Skill-worthy/friction (terminal): `EnterWorktree({path})` re-entry (needed here to push from the worktree after merging in the main checkout) permanently drops this session's "owner" status for that worktree's auto-removal — `ExitWorktree(action:"remove")` then refuses with "not the owner." Tool description warns of this, but no memory names the interaction for the "push main from a worktree under worktree-always" recipe specifically. [route: D4] | deferred | Routed to Memory (D4) by Phase 2's engine, staged at `staged/wrap-up-memory-2.md`. Resolves at this run's own Review Console — Memory updates section. |
| 3 | wrap-up | Residue sweep (`--scope blast-radius`): 65 `pipeline-run` findings — closed (`status: clean`) run dirs never archived under `.claude-tweaks/pipelines/archive/`, per `bin/lib/residue/probes/pipeline-runs.js`'s documented blind spot (#717). `remedy: auto`, no judgment call. | fixed | Batch-archived all 65 via `archiveRunDir` (the same function `bin/lib/reconcile/archive-merged.js` uses), reusing it directly rather than reinventing the mechanic. 64 archived cleanly. 1 (`2026-08-12T054337-dispatch-automerge-checkout-fix-standalone`) hit a pre-existing partial-archive collision from 2026-08-12 — diffed the two `staged/` copies, confirmed the archive copy was the authoritative post-application version (its claimed commit `ec88cfa6` and `IL-125` are both present in current `docs/donts.md`/`docs/incident-log.md`), removed the stale live pre-application residue. Commits: 14 `[reconcile] archive run …` commits on `main` (work/ subdirs only; the rest is gitignored plain moves), merged with 2 concurrent origin commits (no conflicts, `plugin/skills/backlog/overview-mode.md` only), full suite 4752/4752 green, pushed `d529def4`. |
| 4 | wrap-up | Residue sweep: test-suite probe reported `unknown: could not run the project test command` on the first pass (ran without `--no-suite`, from inside the worktree). | accepted | Not investigated separately — the full suite was run directly and independently (twice: once during `finishing-a-development-branch`, once after the archival batch), both 4752/4752 green. The probe's own inability to shell out doesn't indicate an actual suite problem; re-running the CLI probe itself was out of scope for this conversation. |
