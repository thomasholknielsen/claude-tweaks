# Open Items — #1484: record-worktree silently corrupts an unrelated run's run-state.json

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | review/hindsight | Dispatch-adopted, pre-minted-empty run dir: build phase left both `config.yml` (Manifesto) and the PR-early bootstrap (push + draft PR) unrun, with zero `decisions.md` trail for either | deferred | Filed #1858 (Defer-reason: genuinely-larger — root-causing a shared entry guard across `flow/SKILL.md`/`build/worktree-setup.md`/`pr-early-run-lifecycle.md` spans unrelated systems, out of #1484's own diff). Recovered live this run: `config.yml` initialized via `bin/set-config.js`; branch pushed and draft PR #1857 opened. |
