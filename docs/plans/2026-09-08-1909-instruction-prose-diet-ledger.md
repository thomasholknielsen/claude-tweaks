# Open Items — Instruction-prose diet (#1909)

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | build | Deliverables "first trimming pass over the five pipeline skills + `_shared` files" and "scaffolding audit with `bloat.js`'s provenance signals" not attempted this build — full-corpus prose trimming with matched conformance-test updates is a multi-session effort; attempting it inside this build risked an unsafe rush or a half-edited file mid-pass. | deferred | Filed #2063 (`by:capture`, shaped-body, scored `risk:medium`/`size:high`, `ready`) with `Defer-reason: genuinely-larger`. Origin, affected files (the five pipeline skills + `_shared/*.md`, `bloat.js`), and revisit trigger (its own build) are in the issue body. |
| 2 | test | `node bin/verify.js` at `b382c5d5f`: 8047 tests, 8017 pass, 17 fail. | accepted | All 17 verified pre-existing and unrelated to this build's diff — isolated by failing-test-file path per `test/verification.md`'s "Isolating pre-existing failures by file" procedure; none of the 17 failing files intersect this build's changed/added test files. Categories: root-run permission-simulation (8, e.g. `tests/bin-lib/hooks/sweep-shadow.test.js`), Impeccable plugin-contract pins needing an installed plugin at a pinned version (3, `tests/impeccable-plugin-contract.test.js`), shallow-clone-dependent base-SHA pins (2, `tests/session-limit-degrade-conformance.test.js`), and 4 more unrelated to any file this build touches (`tests/bin-lib/code-health/scope.test.js`, `tests/bin-lib/hooks/reconcile-background.test.js`, `tests/bin-lib/visual-decide/cli.test.js`, `tests/console-execute.test.js`). Reproduced identically across two independent full runs. Full detail: PR #2060's Test results section.
