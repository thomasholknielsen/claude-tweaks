# Open Items — specify next: headless selection form + shared headless-self-report extraction (multi-spec run: #967, #968, #969, #970)

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | build/skill | #967's Architecture Alignment Check found `.claude/skills/shared-contract-extraction/SKILL.md` step 4 ("update the edges, once — a new `_shared` file means adding its consumer edges to `docs/skill-graph.md`") was not followed for the new `_shared/headless-self-report.md` extraction or `next-mode.md`'s new `_shared/issue-claims.md` consumption. | fixed | Added both edges to `docs/skill-graph.md` directly (commit `25f9e37b`) — mechanical, unambiguous, verified via full `npm test` (5101/5104, same 3 pre-existing unrelated failures). |
| 2 | test | 3 pre-existing, unrelated `npm test` failures present in this multi-spec run's base (commit `25494a7b`, before any #967-970 work): "every version that shipped on the release branch has a CHANGELOG entry", "the record accounts for every version the git walk can still see" (CHANGELOG version-coverage gap, spanning releases through v6.100.1), and "manifesto.md and multi-spec.md each fit the ~20KB read budget (#724)". Confirmed unrelated to #967's diff — none touch `flow/manifesto.md`, `flow/multi-spec.md`, `CHANGELOG.md`, or release tooling. Recorded retroactively here since flow/multi-spec.md's own pre-flight verify sweep (which normally records this before spec 1's build begins) did not run for this batch. | accepted | Verified independently across every commit in #967's build (Task 1's implementer isolated these 3 at BASE `9d3510a1`; re-confirmed identical 3 at every subsequent commit through `HEAD`, most recently after the docs/skill-graph.md fix). Not this pipeline's responsibility to fix — belongs to whatever work maintains CHANGELOG version coverage and the manifesto/multi-spec.md byte budget. |
