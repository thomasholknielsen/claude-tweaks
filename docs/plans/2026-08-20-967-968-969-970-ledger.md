# Open Items — specify next: headless selection form + shared headless-self-report extraction (multi-spec run: #967, #968, #969, #970)

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | build/skill | #967's Architecture Alignment Check found `.claude/skills/shared-contract-extraction/SKILL.md` step 4 ("update the edges, once — a new `_shared` file means adding its consumer edges to `docs/skill-graph.md`") was not followed for the new `_shared/headless-self-report.md` extraction or `next-mode.md`'s new `_shared/issue-claims.md` consumption. | fixed | Added both edges to `docs/skill-graph.md` directly (commit `25f9e37b`) — mechanical, unambiguous, verified via full `npm test` (5101/5104, same 3 pre-existing unrelated failures). |
| 2 | test | 3 pre-existing, unrelated `npm test` failures present in this multi-spec run's base (commit `25494a7b`, before any #967-970 work): "every version that shipped on the release branch has a CHANGELOG entry", "the record accounts for every version the git walk can still see" (CHANGELOG version-coverage gap, spanning releases through v6.100.1), and "manifesto.md and multi-spec.md each fit the ~20KB read budget (#724)". Confirmed unrelated to #967's diff — none touch `flow/manifesto.md`, `flow/multi-spec.md`, `CHANGELOG.md`, or release tooling. Recorded retroactively here since flow/multi-spec.md's own pre-flight verify sweep (which normally records this before spec 1's build begins) did not run for this batch. | accepted | Verified independently across every commit in #967's build (Task 1's implementer isolated these 3 at BASE `9d3510a1`; re-confirmed identical 3 at every subsequent commit through `HEAD`, most recently after the docs/skill-graph.md fix). Not this pipeline's responsibility to fix — belongs to whatever work maintains CHANGELOG version coverage and the manifesto/multi-spec.md byte budget. |
| 3 | review/hindsight | `dispatch/SKILL.md`'s and `specify/next-mode.md`'s priority-then-age selection ranking scripts are now byte-identical duplicates — the same 2nd-caller duplication threshold that triggered extracting `_shared/headless-self-report.md` earlier in this build. | deferred | Staged as a backlog work-record candidate (Defer-reason: tangential). Stage path: `.claude-tweaks/pipelines/2026-08-20T044958-spec-967-968-969-970/spec-967/staged/reflect-1.md`. Surfaces at the Wrap-Up Review Console's Queue writes section. |
| 5 | review/hindsight | `/claude-tweaks:help`'s Needs Attention table (`plugin/skills/help/status-scan.md`) has no `shaped:headless (no grant)` flag — the third classification #969 added to `/backlog attention`. This leaves `work-backend: local-files` projects (where `/backlog attention` hard-stops entirely) with no surface for this classification at all; `github-issues` projects still have `/backlog attention` itself as a working surface. Surfaced by #969's final whole-branch review and independently re-confirmed by its scoped re-review (both read `status-scan.md`'s actual flag list directly). | deferred | Staged as a backlog work-record candidate (Defer-reason: tangential — extending `/help`'s own table logic is a real, separate unit of work, not a #969 fix). `plugin/skills/backlog/SKILL.md:65` already states the gap honestly (no false parity claim) as part of #969's own fix wave. Surfaces at the Wrap-Up Review Console's Queue writes section, matching item 3's precedent. |
| 4 | build/process | This branch's own `CLAUDE.md` (commit `b679004e`, already in this branch's base before any #967-970 work — predates `origin/main`, which still carries the older text: a local/origin `main` drift, not a change this run made) reads "No phase-plan files; skip `/superpowers:writing-plans`" — contradicting the loaded `/claude-tweaks:build` skill's own Spec Step 2/3, which unconditionally check for and create a plan via `/superpowers:writing-plans` when none exists, with no mention of any bypass. Initially misread this as meaning #968's plan+SDD build (correct, matches the skill) deviated from the "current" rule; re-checked against the actual loaded skill content before acting on that misreading for #969, and found the skill itself doesn't implement CLAUDE.md's line at all — it's either a not-yet-migrated aspiration or describes a narrower scope than I read it for. | accepted | Following the loaded skill's literal, concrete procedure (plan via `/superpowers:writing-plans`, same as #968) for #969 and #970 too — a skill's own documented workflow is more authoritative for "how do I build this" than a compressed CLAUDE.md summary line that the skill doesn't (yet) implement. Flagging the CLAUDE.md/`/build` skill mismatch itself (not just the local/origin `main` drift) for a human look — either the skill needs updating to match CLAUDE.md's stated intent, or CLAUDE.md's line needs correcting/scoping. |
| 6 | build/final-review | `plugin/skills/backlog/SKILL.md:95` states `"skills/backlog/routine-template.yml` doesn't exist (the companion sub-issue blocked on this one ships it)"` — that template exists today and is fleet composition table row 10 (the grant unit). Predates this branch entirely (unrelated to #967-970's diff); surfaced by #970's final whole-branch review while it was independently re-sweeping the routine/fleet doc family for the same stale-enumeration defect class this branch kept finding in its own new content. | deferred | Not fixed as a drive-by — out of scope for #970 (a stale claim in an unrelated skill's prose, not something #970's diff touched or caused). Candidate for a `/claude-tweaks:capture` backlog record with `Defer-reason: tangential`, same pattern as items 3 and 5. Surfaces at the Wrap-Up Review Console's Queue writes section. |
| 7 | build/final-review | `fleet.md:10`'s own claim ("never restated elsewhere as a bare list ... this table is the one place the fleet's membership is enumerated") is now visibly in tension with `routine/SKILL.md:39` and `routine-fleet-on.md:27`, both of which restate the bucket list in prose — and #970's mandated fix (add the shaping unit to both restatements, per the final review's findings 2-3) deepened rather than resolved that tension. Separately, "Six buckets" keeps the same count-drift shape ("Two buckets" before it) that just caused three rounds of stale-number chasing across this build; a count-free phrasing ("Buckets, named explicitly...") would be drift-proof. Surfaced by #970's scoped re-review of its own final-review fix wave. | deferred | Not restructured in this build — the re-review's fixes were correctly scoped to what the final review asked for (add the missing bucket name, correct the stale count), not to redesigning the cardinality-rule's enforcement. Candidate for a `/claude-tweaks:capture` backlog record proposing either removing the two bare-list restatements in favor of a `fleet.md` cross-reference, or de-cardinalizing `fleet.md:10`'s own count. `Defer-reason: tangential`. Surfaces at the Wrap-Up Review Console's Queue writes section. |
| 8 | build/final-review | The final review's finding 5 ("`specify/SKILL.md` has no Routine Configuration section, unlike every other template-shipping skill") had a slightly inaccurate premise: `backlog/SKILL.md` also ships a `routine-template.yml` and also has no such section (confirmed by the scoped re-review via a repo-wide grep) — specify was not the sole outlier. #970 added the section to `specify/SKILL.md` only, per its own scope; `backlog/SKILL.md`'s gap is pre-existing and untouched. | deferred | Not fixed here — out of #970's scope (backlog's gap predates this branch and isn't caused by it). Candidate for a `/claude-tweaks:capture` backlog record (or folding into item 6's backlog/SKILL.md record, since both are backlog/SKILL.md documentation gaps). `Defer-reason: tangential`. Surfaces at the Wrap-Up Review Console's Queue writes section. |
