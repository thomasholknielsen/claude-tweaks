# Open Items — Reconcile decisions.md STAGED lines against staged/'s actual file inventory on resume (#1269)

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | review | `checkStagedInventory` (`plugin/bin/lib/hooks/staged-inventory.js:46-47`) uses `fs.existsSync`-then-`fs.readFileSync` on `decisions.md` — a TOCTOU race: if the file is removed/moved between the two calls (e.g. a concurrent `archive-run` on this same run dir), `readFileSync` throws uncaught; `hooks.js`'s only top-level guard (`main(...).catch(() => process.exit(0))`) swallows it silently, exiting 0 without ever writing the documented stdout line — silently defeating the very MISMATCH detection this check exists to guarantee, at exactly the crash/race class it targets | open | — |
| 2 | wrap-up | Reflect (Near-misses): `existsSync`-then-`readFileSync` TOCTOU recurred a 4th time (#901) despite the documented lens callout — caught at review, not build-time. Staged: `staged/reflect-1.md` | open | — |
| 3 | wrap-up | Reflect (Friction): no explicit rule in `_shared/ledger-format.md` / `wrap-up/nothing-left-behind.md` excludes a review-staged ledger item from wrap-up Phase 3's fix-exhaust before the Console applies it. Staged: `staged/reflect-2.md` | open | — |
| 4 | wrap-up | Residue sweep: PR #1335 (this run's own, head `worktree-dispatch-record-1269`) still open — expected mid-run (merge happens at wrap-up's own close step, not reached yet), not a genuine dangling PR | open | — |
| 5 | wrap-up | Residue sweep: `tests/bin-lib/declined-learning/store-concurrency.test.js` — "recordDecline serializes concurrent writers under an effectively-unbounded lock budget" fails deterministically even in isolation (not a load flake); pre-existing, this record's diff never touches `declined-learning/` (confirmed via `git diff` scope) — outside #1269's scope to fix (remedy: record, genuinely-larger) | open | — |
