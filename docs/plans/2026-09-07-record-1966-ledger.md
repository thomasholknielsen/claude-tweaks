# Open Items — #1966: Review Console consoleAutoResolve merges auto:build-only records inconsistently at autonomy unattended

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | wrap-up | Residue sweep (`bin/residue.js --scope blast-radius`) flagged PR #2044 (head `worktree-record-1966`) as outstanding, remedy `record` | accepted | Not genuine residue — this is this run's own in-flight PR, still open by design at the point the sweep runs (before Phase 4's own merge decision). Resolved by this same wrap-up run's Review Console / Auto-merge short-circuit, not a separate record. |
