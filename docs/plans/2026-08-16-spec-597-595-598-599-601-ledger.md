# Open Items — design-critique dispatch: critics.md roster (#597) + design-critique lever (#595) + review Step 3.8 critic dispatch (#598) + decisions pushback routing (#599) + terminal track (#601)

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | wrap-up | branch — `origin/worktree-deepen+merge-verification` — merged into origin/main, not deleted (residue sweep, spec 597 wrap-up; `remedy: auto`) | fixed | Verified merged, no worktree, no open PR; deleted via `git push origin --delete` (spec 597 wrap-up Phase 1) |
| 2 | wrap-up | pr — PR #609 open, head `worktree-flow-spec-597-595-598-599-601` (this run's own draft PR; residue sweep, `remedy: record`) | open | — (not fixable mid-run: the PR is the bundle's vehicle and merges at the end-of-run settle; resolves at flow Step 5's run-level gate) |
| 3 | wrap-up | residue suite probe skipped via `--no-suite` (spec 597 wrap-up) — full suite ran in build Common Step 5: 3761/3761 pass at `4321eb10` | observation | informational — suite evidence lives in spec-597/decisions.md |
| 4 | wrap-up | residue suite probe skipped via `--no-suite` (spec 595 wrap-up) — full suite ran in build Common Step 5: 3763/3763 pass at `fe1d8379`; PR #609 finding re-surfaced → duplicate of item #2 (not re-added) | observation | informational — suite evidence lives in spec-595/decisions.md |
| 5 | review | 3a Convention (spec 598): `review.md` Step 3.8 (e) used the repo-local `node bin/resolve-profile.js` form (per the contract's §Model Selection parenthetical) where `docs/skill-authoring.md` mandates the model-resolved `${CLAUDE_PLUGIN_ROOT}` placeholder — a consuming project has no repo-local `bin/` | fixed | Placeholder restored — `23aac527` (auto-applied at `review-severity-floor: medium`) |
| 6 | review/hindsight | Convention drift (spec 598): Subagent Contract §Model Selection contradicts skill-authoring.md on the resolve-profile invocation form; ~10 shipped dispatch sites use the repo-local form | fixed | Filed as [#670](https://github.com/thomasholknielsen/claude-tweaks/issues/670) at the consolidated Review Console (user: "Apply everything") |
| 7 | wrap-up | branch — `origin/worktree-flow+spec-332-602-334` — merged into origin/main via PR #603, not deleted (residue sweep, spec 598 wrap-up; `remedy: auto`) | fixed | Verified merged + no open PR; deleted via `git push origin --delete` (spec 598 wrap-up Phase 1). Main also merged into this branch: `95c57109` (3776/3776 green) |
| 8 | wrap-up | residue suite probe skipped via `--no-suite` (spec 598 wrap-up) — full suite ran at `865dd05a` (3763/3763) and on the merged tree `95c57109` (3776/3776); PR #609 finding → duplicate of item #2 | observation | informational — evidence in spec-598/decisions.md |
| 9 | wrap-up | branches — `origin/worktree-flow-spec-580` (PR #587 merged) and `origin/worktree-refine-funnel-design` (PR #653 merged) not deleted; no live worktrees (residue sweep, spec 599 wrap-up; `remedy: auto`) | fixed | Both verified merged + worktree-free; deleted via `git push origin --delete` (spec 599 wrap-up Phase 1) |
| 10 | wrap-up | residue suite probe skipped via `--no-suite` (spec 599 wrap-up) — full suite ran at `9f48dffa` (3832/3832); PR #609 finding → duplicate of item #2 | observation | informational — evidence in spec-599/decisions.md |
| 11 | review | 3a Convention (spec 601, reproduction pair): journey doc `docs/journeys/route-design-critique-findings.md:24`'s red-flag sentence said "the roster has no native or terminal critic yet" — half-stale once this record filled the terminal row | fixed | Corrected directly (low-risk, one line, squarely caused by this record) — `2d9ff889` |
| 12 | review/hindsight | Process finding (spec 601): Step 3.8 track-carve-out task briefs named only the sub-step the author had in mind ("(d) Decisions layer"), missing sub-step (c) Availability — the gating check that actually decides whether a critic dispatches at all; only caught by the whole-branch review's end-to-end trace | fixed | Filed as [#672](https://github.com/thomasholknielsen/claude-tweaks/issues/672) at the consolidated Review Console |
| 16 | review/hindsight | Missing consolidation (spec 595): Manifesto lever set restated by hand in 5 prose files; lever 12 missed all 4 enumeration files until the whole-branch review caught it, same failure mode as lever 11 (#559) | fixed | Filed as [#668](https://github.com/thomasholknielsen/claude-tweaks/issues/668) at the consolidated Review Console |
| 17 | review/hindsight | Convention drift (spec 599): `auto-decision-log.md`'s Entry schema marks `Reversibility:` required but every shipped STAGED-line producer omits it; flagged 3× across 2 records, re-adjudicated "rides" each time | fixed | Filed as [#671](https://github.com/thomasholknielsen/claude-tweaks/issues/671) at the consolidated Review Console |
| 18 | wrap-up | Review Console: 5 "Update the spec" proposals (staged across specs 597/595/598/599/601's `build-deviation-1.md`) presented for sign-off | fixed | User chose "Apply everything (Recommended)" — all 5 GitHub issue bodies (#597, #595, #598, #599, #601) edited to match what was actually built |
| 13 | wrap-up | branch — `origin/worktree-flow-spec-647-648` — merged into origin/main via PR #651, not deleted (residue sweep, spec 601 wrap-up; `remedy: auto`) | fixed | Verified merged + no open PR + no live worktree; deleted via `git push origin --delete` (spec 601 wrap-up Phase 3) |
| 14 | wrap-up | external ceiling breach (spec 601 wrap-up, pre-residue housekeeping): merging 66-commit origin/main drift combined this run's own `design-critique` policy-schema row with sibling record #580's `housekeeping-auto-merge` row — individually under the 40,960 B ceiling, together 102 B over | fixed | IL-70 split-by-unit — extracted `policy-schema.md`'s "Model profiles" table to new sibling `policy-schema-model-profiles.md`, kept a minimal key-name stub inline to satisfy `policy-key-naming.test.js`'s pin — `d937f35d`, 1,996 B headroom, full suite 3876/3876 |
| 15 | wrap-up | residue suite probe (spec 601 wrap-up) — full suite ran twice: post-merge `61487bed` (1 known failure, the ceiling breach above), post-fix `d937f35d` (3876/3876); PR #609 finding → duplicate of item #2 | observation | informational — evidence in spec-601/decisions.md |
