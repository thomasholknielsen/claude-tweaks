# Open Items — Delete consumerless code: four skill-audit modules + _shared/unattended-tier.md

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | build | Record's premise ("four modules have no importer anywhere") did not hold for `skill-catalog.js` on fresh re-verification (spec's own Technical Approach required this re-check). `skill-catalog.js` is actively imported by three kept `bin/lib/skill-audit/tests/*.test.js` files (`csc-registry.test.js`, `context-cost.test.js`, `anti-patterns.test.js`) AND three top-level `tests/*.test.js` files (`skill-catalog-completeness.test.js`, `skill-conventions.test.js`, `argument-hint-input.test.js`) outside the skill-audit directory entirely. Deleting it would have broken 6 surviving test files. | accepted | Kept `skill-catalog.js` — deleted only the 3 modules confirmed genuinely consumerless by fresh grep (`qualified-refs.js`, `identifiers.js`, `relationship-rows.js`). This is a scope correction driven by the record's own re-verification instruction, not scope creep. Acceptance Criteria's "four modules" is satisfied at 3-of-4; the fourth (`skill-catalog.js`) is a false positive in the original audit. |
| 2 | build | Gotcha flagged: `relationship-rows.test.js` "is listed among the tests pinning `_shared/work-record.md`'s config-key table." Investigated — the literal `_shared/work-record.md` string was just fixture text for the parser's own unit tests, not a real pin. The **real** load-bearing content in that file was a different test: `'no skill carries a Relationship section any more'`, a corpus-wide regression guard with no other test covering it, that imported `extractRelationshipRows` from the module being deleted. | fixed | Relocated the guard into `tests/skill-conventions.test.js` (a kept, top-level test file) as `'no skill carries a Relationship section any more'`, reimplemented with a plain `^##\s+Relationship to Other Skills` regex scan instead of the deleted parser. Verified via full `npm test` run (3541 pass / 0 fail / 5 skipped, no regression). |
