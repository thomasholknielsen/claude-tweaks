# Open Items — Flow run: #1503 (qa-agent/browse skill's agent-browser guidance is stale against the pinned agent-browser v0.27)

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | review | `story-examples.md`'s "navigate-to-page-two" example uses `role: link` and its own Locator-type quick-reference table recommends `role` as the "Default for buttons, links, headings" — both now contradicted by this record's own fix, which documents that `find role <role>` only reliably resolves `button`; `link`/`heading` silently fail to match with no error | open | Staged patch — `staged/review-1.patch` |
