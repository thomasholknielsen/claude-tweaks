# Open Items — Flow run: #1503 (qa-agent/browse skill's agent-browser guidance is stale against the pinned agent-browser v0.27)

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | review | `story-examples.md`'s "navigate-to-page-two" example uses `role: link` and its own Locator-type quick-reference table recommends `role` as the "Default for buttons, links, headings" — both now contradicted by this record's own fix, which documents that `find role <role>` only reliably resolves `button`; `link`/`heading` silently fail to match with no error | open | Staged patch — `staged/review-1.patch` |
| 2 | wrap-up | Reflect (Fresh start): spec's Key Files list named only reference-doc files, missing the canonical example file (`story-examples.md`) that also needed the fix — suggest `spec-template.md`'s Key Files guidance prompt a check of example files too, not just reference tables | open | Staged — `staged/reflect-1.md` (Skills curation row) |
| 3 | wrap-up | Residue sweep: PR #1899 (this run's own head branch worktree-dispatch-record-1503) is still open | accepted | This run's own not-yet-merged draft PR — resolved by Phase 4's Auto-merge/Review Console step, not by this ledger; not residue in the "leftover" sense. |
