# Open Items — housekeeping-auto-merge: derive the default from the autonomy ceiling (#580)

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | ops | Open blocker #559 (merge-verification derivation, granted auto:build) shares bin/lib/policy-schema.js + policy-schema.md + resolver tests with this run; user approved proceeding — if #559 is dispatched concurrently, the later merge must rebase over the earlier | open | — |
