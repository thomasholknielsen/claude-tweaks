# Open Items — #883 (test/SKILL.md context overhead) + #881 (suite-count regression stamp)

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | review/hindsight | spec #883 lens 3a: fix-mode.md heading hierarchy skips H2 (H1 -> H3) | fixed | Re-leveled `### Auto mode`/`### Interactive mode` to `## Auto mode`/`## Interactive mode` |
| 2 | review/hindsight | spec #881 lens 3c: verify.js's writeStampAtomic call unguarded -- a stamp-write failure discards the whole report even on a passing run | fixed | Wrapped in try/catch + defensive mkdirSync of parent dir; new discriminating regression test in cli.test.js (verified red against unfixed code, green with fix) |
| 3 | review/hindsight | spec #881 hindsight evaluation 3 (missing consolidation): count-stamp.js's writeStampAtomic duplicated report.js's writeReportAtomic byte-for-byte | fixed | Extracted shared plugin/bin/lib/verify/atomic-write.js (writeJsonAtomic); both call sites delegate to it; new atomic-write.test.js |
| 4 | review/simplify | spec #881: report.js/count-stamp.js kept single-caller pass-through wrappers around writeJsonAtomic instead of calling it directly, with inconsistent (data-first vs path-first) argument order between the two, plus duplicated write-then-rename test coverage in 3 files | fixed | code-simplifier pass: verify.js now calls writeJsonAtomic directly at both call sites (consistently path-first); removed writeReportAtomic/writeStampAtomic; merged readStamp's two identical-handler try/catch blocks; docs/plugin-structure.md corrected to credit atomic-write.js; deleted 2 duplicate wrapper tests. 83/83 pass (independently re-run). |
| 5 | review/simplify | spec #883: fix-mode.md routing stub in SKILL.md flatly asserted "auto mode stages a fix" when auto mode is actually threshold-routed (auto-fix-and-reverify OR stage) — self-contradicting within the same sentence, and inverted the sibling stub convention (condition -> read-pointer -> detail) | fixed | code-simplifier pass: restructured the stub to condition -> read pointer -> one clause per mode, all 4 required citations kept (`staged/test-fix-{n}.patch`, `_shared/staged-patch.md`, `git apply --check`, `_shared/auto-mode-contract.md`). +14 bytes. tests/staged-patch-contract.test.js + pr-early-run-lifecycle.test.js: 36/36 pass (independently re-run). |
| 6 | review/simplify | spec #883: tests/staged-patch-contract.test.js's STAGING_SITES table still names `test/SKILL.md` as the patch-staging site, but the actual staging procedure now lives in fix-mode.md — the test only passes because SKILL.md's stub keeps the 3 required anchors inline | accepted | Not a functional gap today (test passes, stub is accurate) -- a documentation-precision nit flagged by the simplify agent as out of its own file scope. Noted here rather than filed as a separate backlog record: below the materiality floor for a standalone fix (one test's anchor-source column, no behavior change). Revisit if the stub is ever tightened further and stops carrying the anchors inline. |
