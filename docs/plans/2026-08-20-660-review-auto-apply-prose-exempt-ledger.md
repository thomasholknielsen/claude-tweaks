# Open Items — 660: review-auto-apply-ceiling: prose-exempt tier bump

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | wrap-up | Reflect (Near-misses, systemic): no documented convention warns a future boolean-lever author that `policy-schema.js`'s coercion requires literal `true`/`false` (not `on`/`off`) — a fail-open risk on the next boolean lever, caught this run only by whole-branch review. Staged at `staged/reflect-1.md`. | deferred | STAGED for Wrap-Up Review Console — proposes a one-line convention note in `policy-schema.md`/`policy-schema.js`; not a functional blocker for #660, no code fix required to ship this record. |
| 2 | wrap-up | Residue sweep (`residue.js --scope blast-radius`, base f4af16d0, integration origin/main): 14 ownerless-but-unresolved items unrelated to #660's own diff — 3 merged-but-undeleted branches (worktree-dispatch-record-826, worktree-record-486, worktree-record-549) and 10 orphaned `status: clean` pipeline run dirs never archived (records 636, 666, spec-967-968-969-970, 545, 658, 848, 203, 550, 590, 196, 195) — plus PR #1047 (this run's own open PR, not residue; handled by this run's own merge step separately, not counted here). | accepted | Not fixed here: this worktree-restricted subagent session was explicitly instructed not to create/enter another worktree, and `residue-sweep.md`'s own `remedy: auto` fix path requires provisioning a scratch worktree for a write illegal from the current worktree — structurally unavailable here. These 14 items are pre-existing, repo-wide accumulation from other records/sessions, not caused by or specific to #660. `residue-sweep.md` itself notes `--scope repo` (which surfaces the same class of finding) is "the right scope for `/tidy`" — `/claude-tweaks:tidy`'s stated remit is exactly "orphaned plans/worktrees" repo hygiene. Accepted as out of #660's scope; left for `/tidy`'s next pass rather than filing a duplicate backlog record for a job an existing scheduled skill already owns. |
