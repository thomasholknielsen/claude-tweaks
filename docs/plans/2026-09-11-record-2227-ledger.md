# Open Items — reconcile: archiveOrphanedMint fs-renames a git-tracked work/ spec with no git mv or commit (#2227)

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | ops | Pre-flight branch-divergence check: origin/main was 9 commits ahead of local main (e75bdff5) at run start; auto mode continued. The worktree's post-creation catch-up fast-forwarded worktree-record-2227 to e75bdff5, so the build base is current. Main's own mirror-ff was skipped by reconcile (dirty working tree from a sibling session's archive moves). | observation | — |
| 2 | build | Whole-branch review Important: `hasTrackedContent` read an indeterminate `git ls-files` failure (timeout/spawn/no-git) as "untracked", routing a tracked spec back to the bare fs rename under load. | fixed | `isIndeterminate` guard + indeterminate-probe test — `751f22c92` |
| 3 | build | Whole-branch review Minor #3: `archive-run`'s state-less diagnostic, `hooks.js`'s comment, and `_shared/pipeline-run-dir.md` named `archiveOrphanedMint` as the sole owner of a state-less dir, stale once tracked mints route to `archiveRunDir`. | fixed | reworded to "orphaned-mint sweep", byte-pin test still matches — `61aa5d032` |
| 4 | build | Whole-branch review Minor #6: `archiveRunDir`'s archival commit has no `--` pathspec and sweeps whatever else is staged in the main checkout; #2227 widens the routes that reach it. Pre-existing in a function the spec required unchanged (Defer-reason: genuinely-larger). | deferred | Filed as #2241 (origin: this review; files: `plugin/bin/lib/reconcile/archive-merged.js`; trigger: next reconcile-archival change or #1732's consolidation) |
| 5 | build | Whole-branch review Minors #2 (glob-magic `ls-files` pathspecs, three spellings across archive-merged.js and sweep-stray-run-dirs.js) and #4 (`tracked-entry` refusal on the new route has no residue escalation, same as the ad-hoc route). | accepted | Not reachable for RUN_ID_RE-shaped names / identical to existing ad-hoc behavior; recorded as a comment on #1732 for its consolidation rather than as standalone records |
| 6 | build | Whole-branch review Minor #5: this fix stops new uncommitted ` D` spec deletions in main but cannot repair the ~20 already present. | observation | Those deletions are the sibling reconcile-cleanup session's in-flight archive moves (its branch carries the matching `[reconcile] archive run` commits); they clear when that branch lands on main — not this run's cleanup |
| 7 | build | Task 1 review Minor: AC1 test filters `git status --porcelain` to tracked-only lines because the fixture repo has no `.gitignore` for `archive/*/*`; the real repo does, so production satisfies the AC literally. | accepted | Final review triaged as acceptable — the filter still asserts every tracked status line, which is what discriminates against the pre-fix rename |
