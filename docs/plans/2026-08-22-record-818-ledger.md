# Open Items — Multi-spec skipped specs never get manifest status set to not-run, leaking their issue claims (#818)

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | wrap-up | branch — origin/land-record-266 — git branch -r --format=%(refname:short) --merged origin/main — merged, not deleted | fixed | Deleted via `git push origin --delete land-record-266` |
| 2 | wrap-up | branch — origin/land-record-644 — git branch -r --format=%(refname:short) --merged origin/main — merged, not deleted | fixed | Deleted via `git push origin --delete land-record-644` |
