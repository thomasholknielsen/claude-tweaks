# Open Items — Bookkeeping Stamps Gate (record #991)

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | review | `checkBookkeepingStampsGate` (`plugin/bin/lib/hooks/pre-tool-use.js:895`) called `wtDetect.repoInfo(ctx.cwd)` with no `\|\| process.cwd()` fallback, unlike every sibling call site in this file — `nearestExistingDir`'s `path.resolve(undefined)` throws instead of the file's documented "ambiguity resolves to allow" behavior. Reproduced independently by both 3c reproduction-pair agents (one with a live repro). | fixed | Added `\|\| process.cwd()` fallback, matching the file's existing convention (lines 320/325/373/375) — `6c9179b6` |
| 2 | review | `tests/hooks-bookkeeping-stamps-gate.test.js` calls `checkBookkeepingStampsGate()`/`hasLoggedPrDegrade()` directly at 10+ call sites rather than exclusively through `pre.run()` — the only `hooks-*.test.js` file in the repo to do so (3f Test Quality, confirmed via direct verification). | deferred → backlog | Auto-routed by ledger Phase 2 narrowing (`genuinely-larger` clears the floor at `unattended`) — filed as issue #1268 |
| 3 | wrap-up | Reflect (Near-misses, systemic): `decisions.md`'s STAGED lines and `staged/`'s actual file inventory can desync across a session crash, with no reconciliation check on resume. | deferred → backlog | Routed to a record (Defer-reason: genuinely-larger) — filed as issue #1269 |
| 4 | wrap-up | Reflect (Friction): hook-event-simulating tests (`hooks-dispatcher.test.js`, `pr-state.test.js`, others) inherit the ambient `PIPELINE_RUN_DIR`, contaminating the real run's `events.jsonl` with synthetic events. | deferred → backlog | Routed to a record (Defer-reason: genuinely-larger) — filed as issue #1270 |
| 5 | wrap-up | Residue sweep: PR #1256 (this run's own head branch `worktree-dispatch-record-991`) is still open. | accepted | Self-resolves via this run's own Phase 4 merge/close (`pr-first-merge.md`) — expected mid-pipeline state, not independent residue; no judgment call to make |
| 6 | wrap-up | Residue sweep: this run's own pipeline directory (`.claude-tweaks/pipelines/2026-08-22T061958-record-991`) carries `run-state.json` `status: clean` but is not yet archived. | accepted | Self-resolves via this run's own Phase 4 cleanup item 8 (archival), unconditional on every wrap-up run — no judgment call to make |
| 7 | wrap-up | Residue sweep: 8 other sessions' pipeline run directories carry `run-state.json` `status: clean` but were never archived — `2026-08-21T043316-backlog-standalone`, `2026-08-21T043416-tidy-standalone`, `2026-08-21T043718-backlog-standalone`, `2026-08-21T075653-backlog-standalone`, `2026-08-21T222903-backlog-standalone`, `2026-08-21T224547-backlog-standalone`, `2026-08-22T052213-record-818`, `2026-08-22T052301-dispatch-standalone`. | accepted | Not this run's own blast radius (Defer-reason `pre-existing-outside-diff` does not clear the floor at `unattended`) — belongs to `/claude-tweaks:tidy`'s repo-wide sweep (`--scope repo`), not a #991-scoped fix; dropped per user direction on resume |
