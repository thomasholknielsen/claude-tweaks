# Open Items — residue.js branches probe tags every merged remote branch blast-radius (#951)

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | build | #951 duplicates #499 (same defect: `probeBranches` tagging every merged remote branch `blast-radius` unconditionally). #499's fix (commit `99aa3881`, "Fix probeBranches unconditional blast-radius tagging on merged branches") is already present on this branch's base — `plugin/bin/lib/residue/probes/branches.js` already implements the `scope.headBranch` mine-vs-observed distinction, with an explicit comment citing #499. `tests/bin-lib/residue/probes-git.test.js` already carries both regression tests #951's Deliverable 2 asks for ("a merged branch belonging to an unrelated run is observed, not blast-radius" and "multiple merged branches from unrelated runs produce zero blast-radius findings"). Empirically re-verified live in this build (`node plugin/bin/residue.js --base HEAD~1 --scope blast-radius --no-suite --integration-branch origin/main --json`): zero `kind: 'branch'` findings, despite several merged `worktree-dispatch-record-*`/`worktree-*` branches present on origin. `npm test` (5542 tests) passes. No source diff was made — there is nothing left to fix. | observation | All three of #951's Acceptance Criteria are already satisfied by the pre-existing fix; recommend closing #951 as a duplicate of #499 at wrap-up, referencing this build's verification. |
