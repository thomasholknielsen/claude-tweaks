# Open Items — Extend the untrusted-record-content contract to grant-check (#1391)

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | review | refine-mode.md Step 3 makes the identical grant-check call with no caller-side wrap or verdict-source rule (lens 3b, direct-verified); refine-mode.md has 27 B headroom under the 40960 B ceiling, so the paragraph needs a slim-first pass | deferred | Filed as #1442 (Defer-reason: genuinely-larger) — refine-mode.md needs a slim-first pass before the paragraph fits |
| 2 | review/hindsight | grant-check has two callers and no shared invocation file (unlike ceremony-check); #1442 should extract `_shared/grant-check-invocation.md` so refine-mode.md gets a byte-negative citation | deferred | Console auto-resolved: recommendation posted as a comment on #1442 (its Deliverable 1 alternative) |
| 3 | review/hindsight | `[skill: shared-contract-extraction]` add a later-consumer-at-ceiling rule: measure target headroom at plan time; route the caller paragraph through a shared invocation file when a target is at the ceiling | fixed | Skills row applied the rules to .claude/skills/shared-contract-extraction/SKILL.md — c4df264bf |
| 4 | review/hindsight | `[route: D4]` memory: size-check specShapedBody-filed bodies before/after gh issue create until #1435 lands (this run: #1391 body tripled, fixed by hand) | fixed | Memory row M1 applied: specshapedbody-dollar-token-size-check.md written + indexed (console auto-resolve, unattended) |
| 5 | wrap-up | pr — PR #1436 — open, head worktree-flow-spec-1391 (this work); remedy: record — resolves via the Review Console merge decision, not a fix | fixed | Fix anyway (user): merged by the console's Approve-all + merge step this run (PR #1436) |
| 6 | wrap-up | pipeline-run — .claude-tweaks/pipelines/2026-08-24T183144-record-1094 at status clean, never archived (sibling run, blast-radius attributed); remedy: auto | accepted | close-run landed; archive-run refused git-mv-failed because local main is 114 behind origin with the sibling work/ untracked there — the scratch-worktree route is unavailable from inside this run session (native switch is one-way, .worktrees/ not gitignored). Reconcile finishes archival on its next pass; user picked Fix anyway, overridden with this stated constraint |
| 7 | wrap-up | residue suite probe reported unknown: skipped via --no-suite (npm test ran centrally in this wrap-up: 6376/6376 after isolating the pr-state flake) | observation | not a gap — the suite ran in this same run |
| 8 | wrap-up | reflect Surprises: two lens agents died to the account session limit twice; review contract names no degrade path | deferred | Filed as #1449 (born-ready, tangential) by the console auto-resolve |
| 9 | wrap-up | reflect Friction: one ask-user-question (corrupted body) was a genuine judgment call; one contract-violation was a dispatched lens agent narrating before its status word and the dispatcher (this run) not applying the re-prompt-once rule | accepted | operator lapse, rule already exists in step3-lens-dispatch.md; nothing to route |
