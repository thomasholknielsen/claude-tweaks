# Open Items — Materiality digest: proactive-sweep Defer-reason exception, dedup, summary surfacing (#1279)

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | review | High — `tests/materiality-floor-conformance.test.js:201` couples to `record.js`'s `DEFER_REASONS` via unscoped `RECORD_JS.indexOf(']);')` string-slicing instead of importing and testing the already-exported `DEFER_REASONS` constant directly; fragile against any earlier `Object.freeze([...]);` block landing in the file before line 20. | open | — |
| 2 | review | High — `_shared/materiality-floor.md`'s new `## Dedup` fold matches on literal whitespace-collapsed/lowercased free text (`{area}`+finding+`{file refs}`), unlike the proven fingerprint-based `expandDigestFingerprints` (`_shared/health-filing-digest.md`); an LLM-regenerated finding on a later firing is not guaranteed to normalize to a byte-identical match, risking silent dedup misses that reproduce the AC2 bug this section exists to fix. | open | — |
| 3 | review | Medium — the four health-sweep files' new "Stamps `Defer-reason: proactive-sweep` ... and applies its Dedup fold" clause was grammatically attached to the wrong branch (the survivor that *clears* the floor / proceeds to the cap check) instead of the floor-*failing* / digest-routed branch. | fixed | Relocated the clause to attach to the digest-routing sentence in all four files (`code-health/filing.md`, `docs-health/SKILL.md`, `harness-health/filing.md`, `journey-health/SKILL.md`) — commit `e9cc75806`. Zero net byte delta, verified safe against `journey-health/SKILL.md`'s 40960-byte ceiling (unchanged at 40862 bytes post-edit). Full conformance suite re-run 36/36 pass. |
| 4 | review | Spec-compliance gap — the spec's own Key Files list names 6 additional adopters (`review/step3-routing.md`, `wrap-up/residue-sweep.md`, `wrap-up/leftover-routing.md`, `reflect/full-mode.md`, `reflect/hindsight-mode.md`, `visual-review/browser-review.md`) needing the new "Summary surfacing" treatment; only the four health sweeps were actually wired (matches AC3's literal minimum bar, but `materiality-floor.md`'s own new prose states "Every adopter's own summary/report output surfaces this run's routing" — not yet true for these 6). | open | — |
