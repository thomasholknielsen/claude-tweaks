# Open Items — flow run spec-678-680-681-682-683-679

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | build/skill | `bin/lib/release/status.js` + `release.js status` reuse the injectable-runner seam for `git` (deps.git argv-array runner, fake runners that throw on unexpected shapes, deps-wrapped exit-code contract) — `.claude/skills/gh-api-module-pattern` documents the same seam for `gh` only; enrichment: note the pattern is transport-agnostic (`git` modules under `bin/lib/release/` are the second family using it) | observation | input to the Skills curation row (batch pass, multispec-batch-curation.md) — not a defect |
| 2 | review/security | leading-`-` `--merge`/`--ref` values reach git as options — `bin/release.js` parseStatusArgs / `status.js` releaseStatus | fixed | auto-applied — 5c852f92 |
| 3 | review/error | `--ref` with a missing value → `undefined`, exit 1 instead of usage exit 2 — `bin/release.js` | fixed | auto-applied — 5c852f92 |
| 4 | review/error | `isAncestor` swallows fatal git errors (exit 128) as not-an-ancestor — `bin/lib/release/status.js` | fixed | auto-applied — 5c852f92 |
| 5 | review/error | primary manifest read in `iterBumpCommits` lacks commit context on failure — `bin/lib/release/status.js` | fixed | auto-applied — 5c852f92 |
| 6 | review/error | `cat-file -e` catch misreports a bad `--ref` as "no plugin manifest" — `bin/lib/release/status.js` | fixed | auto-applied — 5c852f92 |
| 7 | review/convention | bare `/dispatch`, `/wrap-up`, `/flow` in Step 4.1 body text — `skills/_shared/pr-first-merge.md` | fixed | auto-applied — c5346548 |
| 8 | review/test | Step 4.1-before-reconcile assertion unbounded on the right — `tests/pr-first-merge.test.js` | fixed | auto-applied — 5c852f92 |
| 9 | review/skill | `resolve-policy.js --run "$PIPELINE_RUN_DIR"` returns `source: default` for a multi-spec per-spec subdir because `config.yml` lives only in the parent run dir — `flow/multi-spec.md` / `_shared/pipeline-run-dir.md` gap; every `--run`-reading skill inside a multi-spec run reads defaults instead of the Manifesto values [route: D5] | fixed | flow/multi-spec.md: each spec-{N}/ carries a copy of the parent config.yml (this run: copied into all six spec dirs; resolver now reads run-config) — commit below |
| 10 | review/hindsight | `isBadRefValue` duplicated in `bin/release.js` and `bin/lib/release/status.js` — export once from status.js and import in the CLI | fixed | consolidated by review Step 5 simplify — see commit below |
| 11 | review/hindsight | Pattern observation: a proposal staged after the run's console has closed (post-merge) has no live reader — `_shared/auto-mode-contract.md`'s staged/ section should require a durable out-of-run surface (PR comment kind / record / closing line); staged at `staged/reflect-1.md` for the Skills curation row [skill: _shared/auto-mode-contract.md] | observation | staged for wrap-up curation — reflect-1.md |
| 12 | wrap-up | residue: PR #698 open (this work) — remedy: record; this is the run's own bundle PR, open by design until the consolidated console's Shared teardown merges it (blocked-dependency: end-of-run merge) — re-checked at flow Step 5 | open | — |
