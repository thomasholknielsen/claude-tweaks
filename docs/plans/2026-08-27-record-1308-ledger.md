# Open Items — #1308: pre-tool-use.js teardownTargets env-prefix normalization

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | review/hindsight | Missing consolidation: `findGitLead()` + `skipGlobalFlags()` resolution sequence (find lead token, skip global flags, check `unprovable`/`dir === null`) is now duplicated between `gitTargets()` (git-command.js ~L368-372) and `teardownTargets()` (pre-tool-use.js ~L345-350) — near-identical 4-5 line call sequences in two functions. A shared `resolveGitCommand(toks, cwd)` helper returning `{index, dir}` (or similar) could remove the duplication. | fixed | Resolved by review Step 5's `/claude-tweaks:simplify` pass (code-simplifier subagent): extracted `resolveGitCommand()` into git-command.js, used by both `gitTargets()` and `teardownTargets()`. Behavior-preserving (verified by diff read); full suite re-verified 6534/6534 effective (1 pre-existing isolated-clean flake in pr-state.test.js, unrelated file). Committed 81801e7f. staged/reflect-1.md superseded — no longer needs Review Console attention. |
