# Open Items — #1308: pre-tool-use.js teardownTargets env-prefix normalization

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | review/hindsight | Missing consolidation: `findGitLead()` + `skipGlobalFlags()` resolution sequence (find lead token, skip global flags, check `unprovable`/`dir === null`) is now duplicated between `gitTargets()` (git-command.js ~L368-372) and `teardownTargets()` (pre-tool-use.js ~L345-350) — near-identical 4-5 line call sequences in two functions. A shared `resolveGitCommand(toks, cwd)` helper returning `{index, dir}` (or similar) could remove the duplication. | fixed | Resolved by review Step 5's `/claude-tweaks:simplify` pass (code-simplifier subagent): extracted `resolveGitCommand()` into git-command.js, used by both `gitTargets()` and `teardownTargets()`. Behavior-preserving (verified by diff read); full suite re-verified 6534/6534 effective (1 pre-existing isolated-clean flake in pr-state.test.js, unrelated file). Committed 81801e7f. staged/reflect-1.md superseded — no longer needs Review Console attention. |
| 2 | wrap-up | Fresh-start observation: build's narrow-diff Simplify pass didn't catch the gitTargets()/teardownTargets() duplication that review's whole-change hindsight lens caught (item 1). Defense-in-depth working as intended, matches review-effort-derivation.md's #361 precedent. | accepted | Recommended resolution: Don't capture (n=1, no process change warranted) — staged to Review Console (staged/reflect-2.md) for visibility/override. |
| 3 | wrap-up | Friction observation: 2 of 4 Fast-tier (haiku) review lens agents (3a-A, 3f-A) violated the OUTPUT FORMAT contract (narrated before their status line); resolved via direct-verification rather than the documented re-prompt-once remediation. | accepted | Recommended resolution: Don't capture (n=2, insufficient signal for a policy change; direct-verification is itself a sanctioned override path) — staged to Review Console (staged/reflect-3.md) for visibility/override. |
| 4 | wrap-up/docs | `docs/donts.md` line 171 cites `plugin/bin/lib/hooks/git-command.js:365` for `gitTargets()`'s commit/push classification; this run's simplify commit (81801e7f) shifted that classification to line 389 (the `resolveGitCommand()` extraction added lines above it). Stale line-number pin. | fixed | Applied at the Review Console (staged/wrap-up-docs-1.patch): `:365` -> `:389`. Committed 760993e9. |
| 5 | wrap-up/docs | `docs/REGISTRY.md`'s `docs/donts.md` row Auto-detect is only `CLAUDE.md ## Don'ts` — a change to `plugin/bin/lib/hooks/git-command.js` (the file item 4's stale citation points into) never flags `docs/donts.md` for review, which is how the stale pin in item 4 shipped unnoticed. | fixed | Applied at the Review Console (staged/wrap-up-docs-2.patch): widened Auto-detect to `CLAUDE.md ## Don'ts`, `plugin/bin/lib/hooks/**`. Committed 760993e9. |
| 6 | wrap-up | Residue sweep (`bin/residue.js --scope blast-radius`): PR #1573 (this run's own head branch) is still open, `remedy: record`. | accepted | Close out / Accept — this is the intended pr-first outcome, not orphaned residue: #1308 carries no `auto:merge` label and this run's `merge-authorization` resolves `ask` (config.yml; default `auto` mode never asked to set `pre-authorized` — no Manifesto confirm/hybrid override happened in either of #1308's two /flow calls), so the Auto-merge short-circuit does not apply (`review-console.md`) and the PR is meant to stay open for human merge/close via `gh pr merge`/`gh pr close`. No AskUserQuestion tool is available in this headless dispatch to run Phase 2's per-item drill live; resolved here by policy-derived reasoning (not a live human judgment call) and reported transparently rather than silently — see decisions.md. |
