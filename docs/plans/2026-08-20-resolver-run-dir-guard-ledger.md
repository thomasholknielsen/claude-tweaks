# Open Items — resolve-profile.js/resolve-policy.js: anchored-or-outside guard for --run-dir/--run (#1065)

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | ops | Pre-flight branch-divergence: origin/main was 60 ahead of local main at run start (0 local-only); upstream sha moved mid-preflight (sibling session shipping). Worktree caught up via ff merge to 77ef8c73; local main checkout itself remains behind. | observation | Informational — worktree based on current origin/main; local main ff is settlement-time housekeeping. |
| 2 | build | Follow-up candidate (whole-branch review Minor #8): empty-string `--run`/`--run-dir` argument (e.g. unset `$PIPELINE_RUN_DIR` expanding empty) routes through guards with a blank path across the whole 6-binary run-dir family; a `value.trim()===''` → "requires a value" parse-time check is the family-wide fix. Out of #1065's scope. | open | — |
| 3 | build | Follow-up candidate (whole-branch review Minor #11): the rewritten two-rule paragraph in `_shared/pipeline-run-dir.md` makes testable factual claims about six binaries but has no conformance pin — [IL-127] recurred twice from prose-only defenses. A small test asserting both bullets name their binaries + exit codes would close it. | open | — |
| 4 | build | Whole-branch review minors parked with rulings (SDD breaker-style adjudication, fix wave 8f324e55 covered #1/#2/#4/#6): #3 scan-error reject reuses the no-repo-root message wording (fail-closed behavior correct, wording judgment call on shared helper); #5 adjacent if-blocks/double cwd() cosmetic; #7 AC 11 letter-vs-spirit (mechanism covered + live-probed); #9 `--run-dir /` rule-consistent; #10 flag-precedence placement correct by inspection, unpinned. | accepted | Each individually adjudicated in the SDD run; none affects correctness, and the fail-closed/reject behavior they touch is test-pinned. |
