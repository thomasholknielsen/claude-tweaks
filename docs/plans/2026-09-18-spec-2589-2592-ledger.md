# Open Items — flow run spec-2589, spec-2592

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | build | `plugin/bin/lib/issues/materialize-format.js`'s `REQUIRED_SECTIONS` (line 9) still lists only three sections (`## Current State`, `## Deliverables`, `## Acceptance Criteria`) — no `## Release Note` — while `_shared/work-record.md` (line 276) and `plugin/bin/lib/compose-record/compose.js`'s `REQUIRED_SECTIONS` both require four. This means `/flow`'s own materialize.md hard gate (and `materialize.md`'s own prose, which also only names three sections) does not actually enforce the fourth section either — a genuine drift beyond #2589's three named files, discovered while verifying #2589's premise. Out of scope for #2589 (whose Deliverables name only `shaping-mode.md`/`capture/SKILL.md`/`refine-mode.md`), and out of scope for #2592 (unrelated file). Flagging as a follow-up rather than folding in, per this repo's surgical-changes convention. | resolved | Filed as backlog record #2603 (`ready`, risk:low/size:low) via `/claude-tweaks:capture` per CLAUDE.md's No implicit deferrals rule |
