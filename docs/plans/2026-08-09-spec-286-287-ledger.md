# Open Items — Multi-spec console: engine merge + prose parity (#286, #287)

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | test | `tests/routine-record-freshness.test.js` — 3 assertions failed in a full-suite run ("comparison should be verified against a reachable remote") during spec 286's `/test` step | accepted | Pre-existing, environmental — the file does real `git fetch origin main` and has a dedicated "fails open when remote is unreachable" test path (network-dependent by design). Not related to record #286's diff (bin/lib/wrap-up/**), which this file has no dependency on. Re-ran the file in isolation immediately after: 13/13 passed, including the 3 that failed. Transient network blip during the full-suite run, not a regression. |
| 2 | review | High: `render --spec-state ... --strict` crashes uncaught (exit 1, raw stack trace) on a valid-JSON, non-null object lacking `results` (e.g. `{}`); also closes a related low-severity gap where a top-level array silently produces empty output instead of erroring | fixed | Ledger resolve gate Phase 1: fix qualified as localized/no-product-decision, applied `staged/review-1.patch`, re-verified both repros exit 2 cleanly + 127/127 wrap-up tests pass, committed `4f186073`. |
| 3 | wrap-up | [claude-md: dontCandidate] Pattern observation: input-shape guards checked `typeof`/`null` rather than the actual fields the downstream consumer reads (recurring across the fix-wave's original guard and this /review pass's near-miss finding) | fixed | Ledger resolve gate Phase 2: user chose "Fix anyway". Applied CLAUDE.md Don't + docs/incident-log.md IL-120 (`02a69fdc`); the addition pushed CLAUDE.md over its byte budget, caught by the full-suite run — compressed the bullet, re-verified `tests/claude-md-budget.test.js` passes, committed `31681891`. |
| 4 | review | High (lens 3c, reproduced by both agents): (a) step 4's prose-aggregated section render-order list contradicts the actual template order (Cleanup actions, Queue writes, Memory, Upstream, Issue closures, Translated briefs vs. real order Cleanup actions, Issue closures, Translated briefs, Queue writes, Memory, Upstream) — a fourth self-contradiction found in this file's own review history; (b) no failure-path instruction for a present-but-malformed `engine-state.json` (distinct from the already-fixed "zero specs present" case) — `wrap-up-engine.js` aborts the WHOLE multi-spec render (exit 2, no stdout) on one bad file, dropping all specs' engine-fed sections with no documented recovery, violating this file's own "silently dropping any item is forbidden" rule. Also folded in a single-source (lens 3b agent A) low-severity quoting nit and a single-source (lens 3c agent B) finding that "Approve all"'s description doesn't disclose it also executes Cleanup actions rows, unlike the single-spec console's parity text. | fixed | Ledger resolve gate Phase 1: fix qualified as localized/no-product-decision, applied `staged/review-1.patch`, combined with row #5's consolidation into one edit, re-verified 2923/2923, committed `e158c5d7`. |
| 5 | review/hindsight | Structural debt: multispec-review-console.md's section-order list has no single source of truth — restated independently in 2 places (When-to-run step 4, Numbering rules — the "worked example" this finding also named turned out to be the template itself, not a separate restatement; see the Skills-row agent's independent read), which is why the identical order-list defect class recurred 3 times across this build (Task 1's deferred minor, the final whole-branch review's worked-example fix, and this /review pass's own row #4 finding). Suggested resolution: state the order once in Numbering rules, have the other location reference it. | fixed | Ledger resolve gate Phase 1: same underlying fix as the Skills curation row's `staged/wrap-up-skill-1.md` (now marked RESOLVED there) — qualified as localized/no-product-decision, combined with row #4's patch into one edit, committed `e158c5d7`. |
| 6 | wrap-up | Near-miss: `git checkout -- <file>` — the standard move to revert an intentionally-staged-then-reverted fix under this project's High/Medium routing — triggers Claude Code's harness "modified externally" false-positive reminder. Hit twice in this run (spec 286's and spec 287's own `staged/review-1.patch` reverts). Classified D4 (memory) per learning-routing.md — an environment/harness fact, not a claude-tweaks skill contract. | fixed | Ledger resolve gate Phase 2: user chose "Fix anyway" (per-item drill). Wrote memory file `git-checkout-revert-triggers-false-positive-reminder.md` + `MEMORY.md` index line. |
| 7 | wrap-up | Residue sweep: git worktree `.claude/worktrees/autonomy-unattended-merge-design` (branch `worktree-autonomy-unattended-merge-design`) — lock present but its owning process (pid 57522) is confirmed dead; a stale lock, not a live session. | accepted | User chose to leave untouched despite the stale lock — SessionStart's reaper is the designed mechanism for this case, not a manual ledger-gate fix. |
| 8 | wrap-up | Residue sweep: locked git worktree `.claude/worktrees/flow+spec-271-267` (branch `worktree-flow+spec-271-267`) — confirmed LIVE (pid 57852 actively running). | accepted | User chose to leave untouched — a genuinely live session's worktree, not this run's to touch under any circumstance. |
| 9 | wrap-up | Residue sweep: unlocked, unmerged git worktree `.claude/worktrees/flow-spec-295-296-297` (branch `flow/spec-295-296-297`) — has an active pipeline run tracked elsewhere (`2026-08-09T191318-spec-295-296-297`, status: active). | accepted | User chose to leave untouched — removing it would discard another pipeline run's in-progress, unmerged work. |
