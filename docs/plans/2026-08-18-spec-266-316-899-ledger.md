# Open Items — Bundle #266, #316, #899

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | build/spec-899 | Build-time spot check: sampled `bin/lib/wrap-up/engine-record.js`'s two `appendDecisionLine` call sites (lines 201, 251) — both pass only `buildScannedLine`'s output, unconditionally prefixed `SCANNED `. No AUTO/STAGED/KEPT-PROMPT/REFUSED line is ever emitted under the SCANNED prefix by this module. | accepted | Confirmed by direct code read (see commit for #899); no code change needed — the fix is entirely in the console's own gate text, which now scopes to decision-bearing entries. |
| 2 | build/spec-899 | #798's repro case (restructural CLAUDE.md finding re-prompting after "Approve all") is covered by the new drills-individually class — the CLAUDE.md & rules row's restructural stage-only findings are the named worked example in `review-console-interactive.md`'s Hard requirements. | accepted | Verified by reading #798's live body (fetched 2026-08-18). The eventual closing commit for this branch (not written by this build — /wrap-up was not run in this pass) should carry `Fixes #798` alongside `Fixes #899`. |
