# Open Items — Bundle #266, #316, #899

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | build/spec-899 | Build-time spot check: sampled `bin/lib/wrap-up/engine-record.js`'s two `appendDecisionLine` call sites (lines 201, 251) — both pass only `buildScannedLine`'s output, unconditionally prefixed `SCANNED `. No AUTO/STAGED/KEPT-PROMPT/REFUSED line is ever emitted under the SCANNED prefix by this module. | accepted | Confirmed by direct code read (see commit for #899); no code change needed — the fix is entirely in the console's own gate text, which now scopes to decision-bearing entries. |
| 2 | build/spec-899 | #798's repro case (restructural CLAUDE.md finding re-prompting after "Approve all") is covered by the new drills-individually class — the CLAUDE.md & rules row's restructural stage-only findings are the named worked example in `review-console-interactive.md`'s Hard requirements. | accepted | Verified by reading #798's live body (fetched 2026-08-18). The eventual closing commit for this branch (not written by this build — /wrap-up was not run in this pass) should carry `Fixes #798` alongside `Fixes #899`. |
| 3 | build/spec-266 | #266's Deliverable "sweep the rest of skills/** for the same literal-/tmp/{skill}-* pattern" — a repo-wide grep found 22 more files beyond the 4 named skill families (13 files) plus 2 directly-chained backlog files this build migrated. Full list, priority order, and rationale in the filed follow-up. | deferred | Origin: this build (#266), 2026-08-18. Filed #923 (`Extend session-scoped temp-root convention (#266) to the remaining skills/** sweep`) with the full file list, priority order (health-sweep skills first — scheduled-Routine concurrent-firing risk, same class as #266's own dispatch evidence), and technical approach. Defer-reason: scope-larger-than-single-pass. Trigger to revisit: #923 itself, whenever picked up. |
| 4 | build/spec-266 | `_shared/trust-table.md` writes `/tmp/trust-table-records.json` and `/tmp/trust-table-git-log.txt`, read by `backlog/trust-signal.md` (migrated in this build for its own downstream `backlog-refine-trust.json` output, but this specific upstream pair left unmigrated as a scope boundary). | deferred | Same origin as item 3 — folded into #923's Gotchas as the `_shared/trust-table.md` migration's specific closing note (removes the exception from `backlog/trust-signal.md` once done). |
| 5 | build/spec-266 | `backlog/refine-mode.md` is at 40,313/40,960 bytes after this build's edits — 647 B headroom, the tightest of any file this build touched. No test currently fails, but any further edit to this file needs a fresh `wc -c` check before committing. | accepted | Noted for #923's Gotchas (any future edit to this file from the deferred sweep must re-measure) and for this repo's own near-ceiling awareness generally; no action needed now since `tests/bin-lib/harness-health/skill-md.test.js` passes at the current size. |
