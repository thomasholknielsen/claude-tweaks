# Open Items — build: copy surviving SDD deferred-minor ledger lines into the run ledger before workspace deletion

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | build | New directive in `dispatch.md` cites SDD's `## Finish` step (`rm -rf <workspace>`); `tools/upstream-drift/manifest.yml` pins `## Final Review` specifically for that same upstream file but has no matching pin for `## Finish` — already covered by the file's existing whole-file watch (line 156-157), so not a gap, just a possible future hardening. | observation | Not folded in — outside this record's Deliverables; the whole-file watch already catches drift in that section. |
| 2 | wrap-up | Friction (reflect, light mode): no Task-tool dispatch primitive available in this runtime, forcing every subagent-shaped pipeline step (build Common Step 2/3, review Step 3/5) to degrade to a direct manual pass instead. Evidence: decisions.md's own repeated AUTO entries at 12:34:21, 12:36:17, and this call's 12:51:08/12:51:18 all cite the identical constraint independently. Cost this session: no retries or failures — each degrade was already anticipated in skill prose (`/build`'s and `/review`'s own text names the fallback explicitly) and executed cleanly first try. | observation | Self-reference check (`_shared/learning-routing.md`): `git remote get-url origin` resolves to this repo itself (claude-tweaks) — D5 collapses, re-classify from rule 4. Already an anticipated, gracefully-handled runtime condition (every citing skill step already names its own fallback) — no D1/D2/D3 capture warranted; not a new discovery. |
| 3 | wrap-up | Friction (reflect, light mode): raw `git push` (not just `gh` CLI) is denied in this sandbox — HTTP 403 directly from GitHub's `git-receive-pack` endpoint, confirmed via GIT_CURL_VERBOSE trace (TLS/CONNECT succeed, info/refs 200, only the POST is rejected) and cross-checked against the CCR proxy's own `/__agentproxy/status` (zero github.com entries in `recentRelayFailures`, ruling out an egress-policy denial) — distinct from and wider than plain `gh`-CLI absence. Evidence: this call's 12:52:50 log-decision entry; `_shared/pr-early-run-lifecycle.md`'s existing "Push at run start fails" degrade row already covers the outcome correctly (local-only run, continue) without needing to know the root cause. Cost this session: zero — the existing degrade path applied cleanly, no retry loop, no stall. | observation | Self-reference check: same repo, D5 collapses, re-classify from rule 4. The existing degrade behavior is already correct and sufficient (this row IS the capture — a documentation nuance for a future reader of `decisions.md`, not a functional gap); below the materiality floor for a standalone doc PR on its own. |
