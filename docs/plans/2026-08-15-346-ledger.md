# Open Items — claude-md-conformance: splitSections() finds zero headings on CRLF input

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | build | Materialized spec file (`work/346-spec.md`) cites a stale test path `bin/lib/init/tests/claude-md-conformance.test.js` — the actual file lives at `tests/bin-lib/init/claude-md-conformance.test.js` after a repo-wide test-relocation sweep merged into this branch. The plan and implementation used the correct live path; only the run-dir's materialized snapshot (frozen at fetch time) carries the stale text. | observation | No code impact — materialized file is an audit snapshot, not a live reference. |
| 2 | wrap-up | Reflect (light) near-misses finding: per-task review missed a `.trim()`-masked AC gap in the CRLF regression tests; the final whole-branch review caught and fixed it. | deferred | Staged as pattern observation — `staged/reflect-1.md`, resolved at Wrap-Up Review Console. |
| 3 | wrap-up | Residue sweep (blast-radius): remote branch `origin/worktree-flow-464` — merged into origin/main, not deleted. | fixed | Already deleted by another session before this run's fix pass reached it — re-verified via `git ls-remote` (no longer present). |
| 4 | wrap-up | Residue sweep (blast-radius): remote branch `origin/worktree-wrapup-473-476` — merged into origin/main, not deleted. | fixed | Deleted via `git push origin --delete worktree-wrapup-473-476`. |
| 5 | wrap-up | Residue sweep (blast-radius): PR #489 — open, head `worktree-flow-346` (this run's own draft PR). | observation | Expected in-flight state — this pipeline run's own PR, resolved by this same wrap-up's Phase 4 merge/close execution, not a residue defect. |
