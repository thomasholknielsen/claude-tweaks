# Open Items — #2043: bin/verify.js tests cmd example fails on Windows under cmd.exe

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | review | `tools/run-tests.js`: an empty `listTestFiles()` result (both `ROOTS` entries missing/empty) was passed straight to `spawnSync(process.execPath, ['--test', ...files])` with no length check — `node --test` with zero file args auto-discovers its own default patterns and can exit 0 having run nothing, silently degrading `npm test` to a false-green instead of failing loudly (docs/incident-log.md:421's "the suite gets quieter, not redder"). Review lens 3c, reproduced (severity contested: high/medium — took medium). | fixed | Added an explicit `files.length === 0` guard before spawning — prints an error to stderr naming both `ROOTS` entries and exits 1. Regression-tested via `listTestFiles` returning `[]` for a missing root. Commit: 05cab99b3 |
| 2 | review | `tools/run-tests.js:38`: `fs.existsSync(dir)`-then-`fs.readdirSync(dir)` inside `collectTestFiles`/`listTestFiles` was a check-then-act TOCTOU pair on a directory path — a race the project's own convention (`docs/donts.md`, `[IL-146]`) explicitly bans. Review lens 3c, reproduced (severity contested: low/medium — took low). | fixed | `collectTestFiles` now reads the directory directly and catches `ENOENT`, treating it as "absent" (re-throwing any other error) — no separate existence check. Regression-tested. Commit: 05cab99b3 |
| 3 | review | `tests/package-json-test-script.test.js:48`: the regression test guarding AC1 (file-list parity with the old `find`-based enumeration) only asserts `files.length > 50`, not a tight/exact count — a large accidental directory loss (e.g. all of `tests/bin-lib/`) would still clear the threshold and pass silently, which is exactly the "suite gets quieter, not redder" failure mode this spec's own Gotchas section names as the reason AC1 exists. Review lens 3f, reproduced (severity contested: critical/high — took high). | open | Staged — see `staged/review-1.patch`. Awaiting Review Console approval (ceiling `medium` stages High). |
| 4 | review/hindsight | Missing-consolidation finding: `package.json`'s `test:perf` script still uses the same POSIX-only `$(...)` shell substitution #2043 just removed from `test` — out of scope for #2043 (test:perf is excluded from npm test/CI per CLAUDE.md), but a real adjacent Windows-portability gap. | deferred | Tangential — staged as backlog candidate at `staged/reflect-1.md`. Surfaces at Wrap-Up Review Console's Queue writes gate. |
