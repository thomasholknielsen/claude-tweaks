# Open Items — backlog overview funnel redesign (#513, #514, #515, #516)

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | build | (#513) `notPlanned` shared facet has no local-files writer/parser — label-derived only today; add symmetric `local-store.js` lines or document as label-only in `facet-shape.js`'s header (final-review Minor; review lens 3c confirmed via direct-verification) | fixed | Added not-planned parse/serialize + round-trip test — `2e7a7263` |
| 2 | review | (#513) reference-card lens-list drift was invisible to a green suite — consider a test pinning `skills/help/reference-card.md`'s argument column to each skill's `argument-hint` frontmatter (final-review recommendation) | open | — |
| 3 | review/hindsight | (#513) funnelBuckets' inline blocker precedence (`r.blockedBy ?? facets.blockedBy`) predates #514's canonical `blockersOf` helper — consolidate or document the divergence when #514 builds (staged/reflect-1.md) | deferred | Carried into #514's plan — built next in this run |
| 4 | review | (#516) the `(Recommended)` MUST has no matching menu option at the fallback ladder's grant rung or the `Next: backlog is empty` terminal case (pre-existing from #515; the MUST is now stronger than the menu satisfies — noted in SKILL.md) — follow-up record candidate | open | — |
