# Open Items — Policy read-path unification (#329, #330, #331)

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | review | policy.js wrappers loosen `worktree.always` on pathological shapes (indented line, true-then-false duplicate) — fail-open shift with no audit signal | accepted | Deliberate adoption of the schema parser's documented column-0 semantics; consistent with hooks' ambiguity-resolves-to-allow posture; gate/audit/resolver now agree (reviewer finding 2, spec 329) |
| 2 | review | List-typed keys have asymmetric envelope shapes (unset → `[]` array default; configured → raw comma string per `resolveValue`'s pinned contract) — prose consumers must split | fixed | Documenting line added to `_shared/policy-schema.md`'s Canonical read path — `bb1e256e` |
| 4 | build | `health-open-cap` documented in `_shared/policy-schema.md` (default 10) since #235 but never registered in `POLICY_KEYS` — resolver returned unknown-key for a documented lever; Task A's agent caught it by executing the resolver before migrating | fixed | Registered with default 10 + pin test, digest site migrated — `f72c3d43` |
| 3 | review | Requesting an alias's old name resolves the replacement key — with `dispatch-pick-max-concurrent` still a live POLICY_KEYS row, a request for it answers with `dispatch-batch-size`'s value (both defaults 3, no divergence today) | observation | Carried to spec 331 (collapse leaf) for awareness (reviewer note, spec 329) |
| 5 | build | `skills/flow/SKILL.md` (22 bytes) and `skills/dispatch/SKILL.md` (4 bytes) sit under the 40 KB ceiling with almost no headroom — the next addition to either trips `context-cost.test.js`; durable fix is a sub-file extraction (IL-72 shape) | open | — (resolve at consolidated console — propose a backlog record) |
| 6 | review | Two run-config direct-read sites with inline defaults (`tidy/step-6-auto.md:6` tidy-aggressiveness, `specify/decomposition-mode.md:56` overlap) read `config.yml` without the resolver — outside #330's policy.yml scope but the same drift class; step-6-auto's read also skips the policy.yml level | open | — (resolve at consolidated console — small follow-up or fold into #331) |
| 7 | review | Dual-set execution keys (`execution.always` + `execution-strategy` in one file) silently drop the lock under the uniform new-key-wins alias rule (spec 331 review finding 1, MEDIUM) | fixed | Documented in the deprecations entry + schema row — audit/init surface the conflict; uniformity kept over a special-cased alias — `80402c02` |
| 8 | review | AC 3's sweep hits frozen eval/test fixtures carrying retired lever lines — an allowed class the AC's own list didn't name | accepted | Recorded exception (frozen fixtures are historical snapshots; no live citation is stale) — decisions.md SCANNED entries carry the classification |
| 9 | review | `migratableKeys` is schema-restricted, so a downstream CLAUDE.md-era stray of a retired key is now invisible to `/init --update` drift | accepted | Consistent with existing renamed-key behavior; the policy.yml-side audit still reports retirements (spec 331 review finding 3) |
| 10 | review | `docs/decisions/0013:30` references `review-diff-heuristic-thresholds` in present tense | observation | Decision-record history — docs-health's staleness sweep owns ADR drift if it matters (spec 331 reviewer note) |
