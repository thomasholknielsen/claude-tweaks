# Open Items — Harden track-issue-fixes.yml edge cases (#326)

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | review | Build hand-edited `.github/workflows/track-issue-fixes.yml` directly but never updated `plugin/bin/lib/issue-branch-tracking.js`'s `generateWorkflowYaml()` — the actual `/init` single-source-of-truth generator every consuming project reads from. All 4 code-level hardening deliverables were absent from the shipped `plugin/` payload. | fixed | Ported all 4 changes into the generator, regenerated the committed YAML from it (byte-identical, diff-verified), updated `tests/issue-branch-tracking.test.js` (replaced stale SHA-dedup pin, added 4 new assertions, added 2 revert-commit end-to-end fixture tests). Verified test discrimination by reverting the generator change (6/6 new tests failed as expected). Full suite 6097/6097 green, actionlint clean. Commit `f33f9b14`. |
| 2 | wrap-up | Systemic near-miss: fast-lane's ceremony profile skips /review's Step 1 (Spec Compliance Check), which is exactly the step that would normally cross-check a spec's "Key Files" list against the codebase. Nothing in `/claude-tweaks:specify`'s materialize procedure currently checks whether a target file (`.github/workflows/track-issue-fixes.yml`) has a known generator/source-of-truth module before writing the spec's Key Files section — this is discoverable via the target file's own header comment ("No network here — /init writes the generated YAML") or a `tests/` file that imports the generator. A future fast-lane spec touching a generated artifact could ship the same gap undetected, since the finding here only surfaced from an unprompted "read before you write" search, not from any gate. | deferred | Filed as a backlog work-record candidate — see Queue writes at the Review Console. Origin: reflect (wrap-up) from #326. Trigger: any future spec whose Key Files include a generated/derived artifact. |
