# Open Items — bin/ composer CLI for record-body composition (#800)

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | build/skill | flow/materialize.md's "Multi-record layout" diagram states single-record work files land at {run-dir}/work/{n}-spec.md (i.e. nested under .claude-tweaks/pipelines/{run-id}/work/), but every observed real commit (e.g. work/252-spec.md, de6aaf5d) and .gitignore's own allow-list only cover .claude-tweaks/pipelines/*/work/** — the actual, consistently-used convention is a plain top-level work/{n}-spec.md at the repo root, matching CLAUDE.md's Anchoring-section aside ("work/{n}-spec.md is the exception and stays inside the worktree"). The doc's illustrative diagram appears stale against the real convention. [route: D2] | fixed | Corrected the diagram line to `work/{n}-spec.md ← single-record run (repo-root, git-tracked in the worktree — pipeline-run-dir.md's exception; reaches the main checkout by merge)`, matching pipeline-run-dir.md's documented exception (line 78). Commit 7273a93e. |
| 2 | test | npm test: 2 stable failures in tests/changelog-coverage.test.js ("every version that shipped on the release branch has a CHANGELOG entry", "the record accounts for every version the git walk can still see") — origin/main (tip 55a378f8, fetched fresh) shipped version 6.105.0 without a matching docs/shipped-versions.tsv entry. Confirmed pre-existing and unrelated to this branch: `git diff acb57a7f..HEAD` touches no changelog/version file, and the assertion compares against live origin/main, not this branch's own commits. | accepted | Pre-existing repo-state drift on origin/main, outside record #800's scope — proof: git diff isolation (zero hits on shipped-versions.tsv/CHANGELOG.md/plugin.json across every #800 commit) plus a fresh `git fetch origin main` reconfirming the same 6.105.0 gap on both test-suite runs during this build. |
