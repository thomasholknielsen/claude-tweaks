# Open Items — Flow run: specs 1608, 1492, 1489, 1490, 1491, 1493, 1494, 666

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | build | [spec 1608] `plugin/skills/_shared/issue-claims.md` sits at 39117/40960 bytes (95.5% of the 40 KB ceiling) with no byte-ceiling test pinning this file — headroom note has no red guard | deferred | Staged queue-write proposal `spec-1608/staged/leftover-issue-claims-ceiling-test.md` (defer-reason: tangential) — consolidated console decides |
| 2 | build | [spec 1608] `--link` in reclaim mode reaches only decisions.md/JSON envelope (claimPayload has no link field); doc bullet lists it unqualified | observation | Accepted by whole-branch review as minor; doc clause candidate for a future touch |
| 3 | build | [spec 1608] reclaim mode mints a 72h-TTL live claim owned by basename(--run) with no release guidance in the doc bullet | observation | Accepted by whole-branch review as minor; release via release-claim.js when done |
| 4 | build | [spec 1608] exit 3/4 semantics differ between release-claim.js and repair-claim.js (both documented; trap for a script handling both) | observation | Accepted by whole-branch review as observation |
| 5 | build | [spec 1608] repair.js reuses writeTombstone/tombstoneContent naming for claim content in reclaim mode | observation | Accepted; revisit only if a third consumer appears |
| 6 | build | [spec 1608] release-claim.js has the same anchoring-check-after-write ordering the fix wave corrected in repair-claim.js (pre-existing, out of this spec's scope) | deferred | Staged queue-write proposal `spec-1608/staged/leftover-release-claim-anchoring.md` (defer-reason: tangential) — consolidated console decides |
| 7 | build | [spec 1492→1489/1490] `backlog/grant-mode.md:3` and `backlog/SKILL.md:46` still cite "/dispatch next's headless-unit shape" as the canonical descriptor — post-#1492 that is the Routine-fired bare drain (`next` = deprecated alias). Specs 1489/1490 own those files; fold the reword into their builds | fixed | backlog/SKILL.md half landed with spec 1489; grant-mode.md's descendant line (refine-headless.md:46) reworded in spec 1490's fix wave (b1a3ce03f) — both halves pinned |
| 8 | build | [spec 1492] `_shared/headless-self-report.md` title + remaining prose still "next form" saturated beyond the two normative sentences the fix wave corrects; `bin/lib/policy-schema.js` dispatch-batch-size summary lacks drain-default meaning (borderline) | observation | Accepted by whole-branch review as deferred terminology; follow-up sweep candidate |
| 9 | review/hindsight | [spec 1492→1491] `--budget <n\|all>` is defined locally in dispatch/SKILL.md (`n` = attempt count, `all` = drain to empty). Spec 1491's specify `--budget` must read identically — reuse the same sentence shapes, and cross-cite rather than fork the definition | fixed | Canonical definition extracted to _shared/record-batch-input.md (spec 1491 Task 1); both skills cite it; dispatch swap byte-negative |
| 10 | review/hindsight | [spec 1489→1493] attention-mode.md tidy-row launcher re-point once --approve ships | fixed | Consumed by spec 1493 Task 2: launcher now /claude-tweaks:tidy --approve with the honestly-scoped targets-newest asymmetry, pinned |
| 11 | build | [spec 1494] `node --test <directory>` (a bare dir arg, no glob) silently discovers and runs ZERO tests from inside that directory on this repo's Node v22.23.2 — it doesn't error loudly, it just reports success on the OTHER named files while quietly skipping the directory's suite entirely. Repo's own `npm test` avoids this via `find ... -name '*.test.js'` pre-expansion, but any ad hoc dispatch/plan text using the shorthand (this plan's Task 3 brief did) reproduces the footgun. Discovered by Task 3's reviewer independently re-deriving the brief's literal command. | observation | Not a code defect — no source fix applies. Candidate for a `/claude-tweaks:capture` backlog record: teach plan-authoring / dispatch-prompt conventions to always expand test-dir args to an explicit file list (or a `find`-piped form), never a bare directory, since a silent zero-tests-run is worse than an explicit error |
| 12 | build | [spec 666] Record's own Acceptance Criteria #1 (a documented hard precondition) requires the build date be on or after 2026-11-16 before any edit — today is 2026-08-30, ~2.5 months early | not-run | No file modified, per the record's own explicit instruction ("stops without modifying any file and reports the record as not yet actionable — this is not a normal 'build failed' outcome"). Manifest transitioned `666: not-run`. Re-run this record via `/claude-tweaks:flow #666` on or after 2026-11-16 |
| 13 | build | [spec 1494, fix-wave regression] Fix wave 1's `.gitignore` carve-out widening pushed `_shared/auto-mode-contract.md` (already 40954/40960, 6B headroom) to 40986 — over the 40 KB ceiling; separately, sweep/SKILL.md's two `invalidateSnapshot` snippets used `process.env.CLAUDE_PLUGIN_ROOT` directly instead of the required `${CLAUDE_PLUGIN_ROOT}` string-literal placeholder — a bug that originated in the controller's own plan draft, was faithfully transcribed through Task 1, and pinned as-is by Task 4's own test. Both caught only by the CENTRAL full `npm test` run, not any subagent's targeted checks | fixed | Controller fixed both directly (commit 9cd4cc43c) after the post-fix-wave full suite surfaced them; re-verified 37/37 + 283/283 targeted, then 6971/6971 full suite green. Reinforces this repo's own rule: never trust a subagent's self-reported pass/fail — the full suite is the only thing that actually exercises every consumer of a shared near-ceiling file |
