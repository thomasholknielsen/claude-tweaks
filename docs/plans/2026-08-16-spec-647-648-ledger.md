# Open Items — spec 647 + 648 (permittedGrants per-grant reasons; feedback frontier --unattended)

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | ops | Pre-flight divergence: origin/main was 14 ahead of local HEAD (2a20919a) at Step 2.5; auto-continued. Worktree post-creation catch-up merged to c47d47de, and origin/main advanced again during setup (93454595 → c47d47de). | acknowledged | Post-creation catch-up merged origin/main (c47d47de) into the shared worktree before any commit; main-checkout ref sync happens via reconcile at merge time. |
| 2 | test | Pre-existing baseline failure (batch pre-flight sweep, unmodified base c47d47de): tests/reference-card-argument-hint.test.js — specify's reference-card Takes cell lacks the `[--chained]` token its argument-hint gained (refs #564). 3829/3830 pass otherwise. Not introduced by this run. | fixed | Fixed in this branch (wrap-up Phase 1 fix-exhaust): added `[--chained]` to the specify row in skills/help/reference-card.md — required for merge-when-green, since CI (test.yml) runs the full suite. Test now 3/3. Commit: see "Align reference card" commit. |
| 3 | review/hindsight | [skill: NEW - installed-build-skew-fallback] Pattern observation from #647: skill-text snippets reading a widened module return shape carry a one-release read-side fallback tagged with the module's removal condition; convention undocumented in CLAUDE.md/docs/skill-authoring.md. Staged at spec-647/staged/reflect-1.md for wrap-up Skills curation. | observation | Staged for Review Console / Skills curation |
| 4 | review | tests/frontier-unattended-literal.test.js allowlist compares forward-slash Set entries against platform-specific path.relative output — false positive on Windows (lens 3f, confirmed via direct-verification override). One-line normalize fix staged at spec-648/staged/review-1.patch. | fixed | Applied at the consolidated Review Console (path.sep normalization); test 1/1. Commit: see "Normalize allowlist path separators" commit. |
| 5 | review/hindsight | [route: D4] Memory candidate from #648: a record's own sweep grep (`grep "frontier --unattended"`) provably missed the primary offender because the literal wrapped mid-command across a line break — sweep greps over prose need whitespace-spanning scans (grep -z / regex \s+). Staged at spec-648/staged/reflect-1.md. | observation | Staged for Review Console / Memory curation |
