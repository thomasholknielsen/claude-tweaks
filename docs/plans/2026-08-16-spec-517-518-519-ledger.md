# Open Items — Multi-spec run 517, 518, 519 (tidy report redesign + reconcile-backed auto-apply)

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | flow/ops | Pre-flight branch-divergence: origin/main was 3 ahead of HEAD (worktree-record-506-design) at run start | fixed | Post-creation catch-up merged origin/main into shared worktree — `ace2d090` |
| 2 | review | archive/* tag namespace D/F collision: branches `build/foo` and `build/foo/bar` produce conflicting tag paths; second reports `tag-failed` forever (fails closed, low frequency) — final-review Recommendation, spec #517 follow-up candidate | deferred | → backlog #548 (auto-routed: blocker clears floor — design decision + scope expansion) |
| 3 | review/hindsight | Spec #517's literal tag-aging rule (age by tagged commit's committer date) inverted its own safety rationale — a >90d-old branch lost its archive tag in the creating pass; caught only by the final whole-branch review's empirical repro. Lesson: for retention/aging mechanisms, trace the retained artifact's full lifecycle (create→age→delete within one pass) at spec time | observation | Ruled + fixed in `ca17a26b` (annotated tags, taggerdate aging) |
| 4 | review/hindsight | Plan's execution tests assumed no-remote temp repos would yield null PR states; real resolver returns 'network-failure' → fail-closed skip. Lesson: I/O-edge modules need their injectable test seam (resolvePr param) designed in from the start, not retrofitted at dispatch | observation | Ruled + fixed pre-dispatch (injectable resolvePr) |
| 5 | review/hindsight | Spec #518's Key Files missed `scan-procedures.md` — the Collection routing table binds every scan tag to the report sections the spec renamed; the Critical finding was the renamed surface's un-listed consumer. Lesson: a spec that renames a contract surface must grep for that surface's consumers when composing Key Files | observation | Fixed in-wave (`97363bca`, `6d8cf6ac`) |
| 6 | wrap-up | Release-note obligation (spec #519 deliverable): the release that ships this branch must announce the `tidy-aggressiveness` default flip `conservative` → `moderate` — a distributed behavior change under expand-contract discipline. Carry into the release step; do not bury (reason-not-auto: requires-judgment — release timing and notes are the maintainer's call) | deferred | → backlog #549 (auto-routed: blocker clears floor — awaits the release step's maintainer approval) |
