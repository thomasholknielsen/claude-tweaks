# Open Items — Add an early-warning tier to the 40KB SKILL.md ceiling check before it's hit

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | test | Pre-existing baseline failure: `tests/impeccable-cli-contract.test.js:35` "the installed CLI matches the pinned version" — locally installed Impeccable CLI is 3.6.0, repo pins 3.5.0 | accepted | Environmental, not caused by this record's changes — branch diff (`git diff --stat 107ef8c2..dfb3ca8`) touches only `bin/lib/skill-audit/context-cost.js`, `bin/lib/skill-audit/tests/context-cost.test.js`, and the materialized spec file; nothing Impeccable-related. Independently confirmed by the final whole-branch reviewer (opus) via `git log` on the failing test file. Full suite otherwise 3575/3576 passing; this record's own target file (`context-cost.test.js`) is 13/13 clean. |
| 2 | review | Final review (Important #2): the new near-ceiling warning only surfaces in raw `npm test` output, not wired into `bin/lib/skill-audit/bloat.js`'s CLI report — spec's Acceptance Criteria explicitly names "test output" as satisfying, but the record's stated intent (catch a file before an unrelated edit forces a reactive extraction) is better served by a second, more visible surface | deferred | Origin: record #336 follow-up. Files: `bin/lib/skill-audit/bloat.js` (has its own `CEILING_BYTES` import already; would need `nearCeiling`/`WARN_RATIO` added and a report line). Trigger: revisit when `bloat.js` is next touched, or proactively if the near-ceiling list grows past its current 11 entries. To be staged as a new backlog work record at the Review Console. |
| 3 | review | Final review (Minor #4): a file at exactly `CEILING_BYTES` (40,960 bytes) is neither warned (`nearCeiling` uses strict `<`) nor failed differently than today — a 1-byte/1-in-40960 blind spot | accepted | The existing hard-fail behavior at exactly the ceiling is unchanged and correct (`overCeiling` already excludes it, per Task 1's approved boundary test); only the missing warning is a gap, and it's a plan-level boundary choice already reviewed and approved in Task 1. Negligible probability, not worth reopening approved code for. |
| 4 | review | Final review (Minor #5): `CEILING_BYTES * WARN_RATIO` threshold expression is computed independently in both `context-cost.js` and `context-cost.test.js` (DRY) | accepted | Genuine minor; fixing would re-open already-reviewed Task 1 code for a non-functional refactor. Easy future cleanup if `bloat.js` wiring (item 2 above) happens — that work would naturally consolidate the expression into an exported helper. |
| 5 | review | Final review (Minor #6): the spec's Technical Approach said "percent-of-ceiling," the shipped warning prints KB instead | accepted | Cosmetic. KB matches the surrounding tests' existing `kb()` display convention — consistency with the file outweighs literal spec wording, and the header line already states the percent threshold ("90%+"). |
| 6 | review | Lens 3i (Doc Freshness): `docs/plugin-structure.md` matches this diff's `bin/**` pattern but wasn't updated in this work's commits | accepted | Already judged during `/build` Common Step 6.5: the doc's existing line-19 summary ("Corpus measurement and guards for the shipped skill payload: the 40 KB per-file ceiling...") already generically covers this addition — no textual change needed. |
| 7 | wrap-up | Reflect (light, Near-misses): dormant `${s.skill}/${s.file}` formatter bug was invisible until this record's new warning test finally exercised it — general pattern worth remembering (staged: `staged/reflect-1.md`) | observation | Informational, non-blocking — surfaces at the Wrap-Up Review Console's Pending review section directly from `staged/reflect-1.md`; no further ledger action needed. |
| 8 | wrap-up | Residue sweep: `origin/worktree-archive-463` — merged into `origin/main`, not deleted (remedy: auto) | fixed | Deleted via `git push origin --delete worktree-archive-463`. Zero data loss — fully merged into `origin/main` already. |
| 9 | wrap-up | Residue sweep: `origin/worktree-dispatch-398` — merged into `origin/main`, not deleted (remedy: auto) | fixed | Already deleted upstream by an unrelated session before this fix-exhaust ran — `git fetch --prune` confirmed the remote ref no longer exists. Stale local remote-tracking ref only. |
| 10 | wrap-up | Residue sweep: `origin/worktree-dispatch-399` — merged into `origin/main`, not deleted (remedy: auto) | fixed | Same as item 9 — already deleted upstream, stale local remote-tracking ref only, confirmed via `git fetch --prune`. |
