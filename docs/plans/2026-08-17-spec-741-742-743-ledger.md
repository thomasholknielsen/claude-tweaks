# Open Items — Sequential flow run: #741, #742, #743

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | ops | Local `main` was 73 commits behind `origin/main` at pre-flight (branch-divergence-check, Step 2.5). Continued per `auto` mode default; the shared worktree's post-creation catch-up merged `origin/main` directly, so the worktree itself is current. The main checkout itself still carries the divergence. | open | — |
| 2 | review | #741: high-severity finding — `refine-mode.md` Step 5's closing summary claims to derive its tally/failure data from `decisions.md`, but Step 5 has no failure-case log template and no exit-status checks on its `gh`/`writeRecord` calls, so "0 failed" is vacuous rather than derived. Staged at `spec-741/staged/review-1.patch`. | open | — |
| 3 | review | #741: unconfirmed medium finding (single review agent, not reproduction-confirmed) — the Dependency-repair write type is absent from both the pre-existing `decisions.md` log templates and the new closing-summary's tally categories. | open | — |
| 4 | test | #743's Common Step 5 full-suite run hit 2 failures in `tests/changelog-coverage.test.js`, both the same root cause: `origin/main` shipped v6.93.0 (commit ccab3c55, a concurrent session, unrelated to this run) after this worktree's base, and `docs/shipped-versions.tsv`/CHANGELOG.md on `origin/main` haven't been merged into this branch yet. Confirmed via `git fetch origin main` + `git diff HEAD origin/main -- docs/shipped-versions.tsv .claude-plugin/plugin.json`. Not caused by any of #741/#742/#743's changes — no file this run touched is implicated. Resolves itself when the shared worktree catches up with `origin/main` at the finish-branch step (multi-spec.md's "re-check main divergence immediately before" the final finish). | open | — |
| 5 | review | #743: high-severity finding, reproduction-confirmed by 3 independent agents (convention-B, security-A, security-B) — `refine-mode.md`'s close-run instruction told the agent to reuse "the same path named in the closing summary's audit-trail line above" as `--run <absolute-run-dir>`, but that line renders the `decisions.md` *file* path, not a directory; `close-run`'s `resolveRunArg` rejects non-directory paths and silently no-ops, defeating #743's entire purpose. Staged at `spec-743/staged/review-1.patch`. | open | — |
| 6 | review | #743: unconfirmed medium finding (single review agent, error-handling-B; the reproduction pair disagreed — error-handling-A found nothing here) — the close-run paragraph overstates the risk of omitting `--run` ("silently disarm that session's own worktree enforcement"); `close-run` already refuses a foreign-owned run without explicit `--run` when `sessionId` is stamped, so the real unprotected case is narrower (a fallback landing on an unowned run). | open | — |
| 7 | ops | **Human attention recommended.** This parent run directory (`.claude-tweaks/pipelines/20260817T082334-spec-741-742-743/`) never received its Manifesto-time scaffolding (`config.yml`, `manifest.yml`, `decisions.md`, `staged/`, `run-state.json`) per `multi-spec.md`'s documented layout — discovered only at #743's end-of-run wrap-up, root cause not diagnosable in retrospect (predates the current context window). Effect: no `run-state.json` existed anywhere in this run's tree at any level (not even per-spec) — hooks-layer E1/E2/E3 enforcement and resume/reconcile classification never covered this run; the batch registry/reflect passes ran via `curation-engine.md`'s prose fallback instead of the engine (no `manifest.yml` `baseSha`, no per-spec `engine-state.json` to aggregate). `decisions.md`/`staged/` created retroactively at the parent level during this wrap-up so the consolidated console has somewhere to read from. This is a gap in the pipeline's own tracking-integrity contract, not a code defect in the 3 records' deliverables — flagging for human review of why the Manifesto-time creation step was skipped, since I cannot diagnose it from inside this run. | open | — |
