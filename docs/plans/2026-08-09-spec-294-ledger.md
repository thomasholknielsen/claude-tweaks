# Open Items — Batch upstream-feedback (U#) filing into one multiSelect decision, collapse /feedback's double-ask

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | review/convention | `⚠` emoji in dedup-flag format string (`upstream-feedback-batch.md`, quoted in journey file) — CLAUDE.md "no emojis in skill files" | fixed | Commit 59966708: replaced with `**possible duplicate:** #{N}` |
| 2 | review/error-handling | Step 0's batch loop claims Step 4 (dedup) runs non-interactively, but Step 4's three-way interactive ask was never updated — "comment on existing issue instead" has no batch-UI expression | open | Staged: `staged/review-1.patch` — folds dedup match into shared contract's flag + check/uncheck; "comment instead" routes via free-text edit |
| 3 | review/error-handling | Steps 2/3/6's "stop" has no per-candidate-vs-whole-batch scoping when reused inside Step 0's loop | open | Staged: `staged/review-1.patch` — "stop" now scopes to the one candidate, matching Step 7's per-item isolation |
| 4 | review/error-handling | `--pre-confirmed`: scrub-rerun-vs-drift-fallback ordering unstated; no per-item isolation on a scrub hard-stop mid-batch | open | Staged: `staged/review-1.patch` — scrub rerun now stated to run first; per-item isolation stated |
| 5 | review/error-handling | `--pre-confirmed`: no staged-file cleanup on success — resume-after-interruption risks re-filing an already-filed item (duplicate public issue) | open | Staged: `staged/review-1.patch` — Step 8 deletes the staged file on success; drift check's "file not found" now means "already filed, skip" |
| 6 | review/error-handling | `upstream-feedback-batch.md`'s "Editing an item" has no handling for a free-text edit request naming no matching item | open | Staged: `staged/review-2.patch` — list open titles, ask again; never guess or nearest-match |
| 7 | review/security | `--pre-confirmed` caller-legitimacy is prose-enforced, not structural — a future non-console caller with a fabricated matching snapshot could skip the human confirm | accepted | Self-documented, deliberate, repo-wide Component-Skill Contract pattern (skills are prose, not code) — not a defect novel to #294; flagged for visibility per both reproduction agents |
| 8 | review/error-handling | Materialized spec file's Acceptance Criteria text ("all pre-checked") contradicts the shipped "unchecked by default" design | accepted | Already resolved at `/build` Common Step 4.5 (Architecture Alignment Check) — spec is a frozen historical record, not hand-edited to match reality |
| 9 | review/docs | Lens 3i (doc freshness): `CLAUDE.md` matches the `skills/_shared/*.md` Auto-detect pattern (new file `upstream-feedback-batch.md`) but wasn't updated in this work's commits | observation | Informational per lens design ("review in wrap-up") — no enumerated CLAUDE.md claim goes stale; Wrap-up's Docs curation row can pick it up |
| 10 | review/hindsight | 5-evaluation hindsight pass (approach, structural debt, consolidation, convention, skill-worthy) | accepted | No changes needed beyond Step 3's own findings (items 1-6 above) — approach is sound; consolidation with sibling #290's `batched-item-drill.md` was considered and already deliberately rejected (parent #288's body); the shared contract itself is the intended skill-worthy pattern, already captured as `skills/_shared/upstream-feedback-batch.md` |
| 11 | wrap-up | Locked worktree `autonomy-unattended-merge-design` (branch `worktree-autonomy-unattended-merge-design`) — another live session's in-progress work, not this run's | accepted | User chose "Fix anyway"; verified pid 57522 alive (5.5h), branch unmerged into main — no safe non-destructive action available; not this run's worktree to touch |
| 12 | wrap-up | Locked worktree `flow+spec-271-267` (branch `worktree-flow+spec-271-267`) — another live session's in-progress work, not this run's | accepted | User chose "Fix anyway"; verified pid 57852 alive (12.5h), branch unmerged into main — no safe non-destructive action available; not this run's worktree to touch |
| 13 | wrap-up | Locked worktree `flow+spec-286-287` (branch `worktree-flow+spec-286-287`) — another live session's in-progress work, not this run's | accepted | User chose "Fix anyway"; verified pid 87406 alive (5h), branch unmerged into main — no safe non-destructive action available. **Note:** its latest commit already claims `[IL-120]` for a different topic ("shallow input-shape guards") — collides with #294's own staged IL-120 proposal; neither branch merged yet, re-check/renumber before #294 merges to main |
| 14 | wrap-up | Unlocked worktree `flow-spec-295-296-297` (branch `flow/spec-295-296-297`), in `SessionStart` reaper domain | accepted | Residue sweep, remedy: auto — `[IL-58]`: `SessionStart`'s reaper collects abandoned unlocked worktrees automatically; not this run's worktree to remove manually |
