# Open Items — Pre-flight the vocabulary guard at record time and add an amend verb

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | wrap-up | residue — merged-but-undeleted remote branch origin/worktree-issue-820-reconcile-latency (unrelated to #252's own worktree/blast radius; residue.js --scope blast-radius surfaced it anyway) — `git push origin --delete` returned HTTP 403 | deferred | → backlog, staged/ledger-item-1.md (Defer-reason: blocked-external — auto-routed via ledgerNarrowing at autonomy: unattended) |
