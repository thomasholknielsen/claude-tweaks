# Open Items — spec 705, 782 (multi-spec run 2026-08-17T145819-spec-705-782-785)

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | build/skill | `.claude/skills/skill-prose-conformance-tests`'s "prove the assertion can go red before trusting its green" convention was not explicitly exercised as a discrete build step for the new `tests/specify-range-form-readback.test.js` file (Task 3 wrote it as a transcription task after its target prose already existed, so it passed immediately rather than red-then-green). Verified post-hoc at Common Step 4.5: every new marker assertion is genuinely absent from the pre-Task-1 file content (`git show 61df3e44:...`) and the two "must-still-be-true" negative controls are genuinely present there, confirming real discrimination — no defect found, but the project skill's discrimination-proof step should be folded into future prose-pin task briefs proactively rather than verified after the fact. | observation | Verified via `git show` diff against pre-change content; no code change needed — informational for `/claude-tweaks:wrap-up`'s Skills curation row. |
| 2 | review/hindsight | Implementation Hindsight (5-evaluation action gate, spec #705): approach correctness sound (narrowed scope matched the concurrent-merge discovery); no structural debt (pure prose, same shape as the existing comma-list batch form); no missing consolidation (the one adjacent staleness was already deferred to #822 rather than folded in); no convention drift; the read-back-verification shape (re-fetch after write, assert invariants, isolate failure) considered but not prescriptive enough to name a target skill/section. | accepted | No changes needed — approach is sound. Reasoning stated in body; see spec-705/decisions.md's `/review` Step 4 entry for the full per-evaluation breakdown. |
| 3 | wrap-up | Residue sweep (`bin/residue.js --scope blast-radius`, spec #705's wrap-up): PR #812 (head `worktree-flow-spec-705-782`) is open — remedy: record. | accepted | Not residue — expected mid-run state. This is the shared multi-spec run's own draft PR; spec #782 has not yet built/tested/reviewed in the same run. Will close normally via merge once both specs finish. |
| 4 | wrap-up | Residue sweep: the project test-command probe could not run (`ran: false, reason: "could not run the project test command"`) from the sweep's invocation context. | observation | Not investigated further — the full suite already ran and passed (4469/4469) via `/claude-tweaks:test` earlier in this run; the sweep's own probe failing to resolve a runnable command does not indicate a real test failure. |
