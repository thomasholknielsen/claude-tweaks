# Open Items — local-merge + auto mode: finishing-a-development-branch auto-finish path (#771)

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | review | Lens 3c (confirmed, reproduced): `local-merge-auto-finish.md`'s conflict-detection (Procedure step 4) only recognizes conflict-marker/`CONFLICT` output — other `git merge` failure modes (dirty working tree, unrelated histories) are unrouted | fixed | Broadened to a catch-all: any non-zero `git merge` exit routes to abort-if-`MERGE_HEAD`-exists-then-Park, matching `pr-first-merge.md`'s own catch-all pattern |
| 2 | review | Lens 3c (confirmed, reproduced): Procedure step 2's `close-run` call has no described failure branch before the (effectively irreversible) merge attempt proceeds | fixed | Added a one-line fail-open note matching the identical, pre-existing gap in `auto-merge-short-circuit.md`'s own local-merge branch — consistent behavior, now stated rather than implicit |
| 3 | review | Lens 3c (confirmed, reproduced): Procedure step 3's `run-state.json` `worktree`-field read (`require(...).worktree`) has no missing/empty-field guard — a missing field silently substitutes the string `undefined` into subsequent `git -C` commands | fixed | Added an explicit guard-and-fail-loud step, mirroring step 4's existing "abort with a clear stderr message" pattern for the branch-mismatch case |
| 4 | review | Lens 3c (confirmed, reproduced): push failure (Procedure step 6) has no rollback or Park routing — step 7's `log-decision.js` call unconditionally asserts `"...pushed. [outcome: merged]"` regardless of step 6's actual result, a false-success claim | open | Staged `staged/review-1.patch` for Review Console — requires a design call (retry? park with a distinct outcome? leave local commit and park?) beyond a one-line fix |
| 5 | review | Lens 3c (confirmed, direct-verification): Procedure step 4's "verify main checkout is on the resolved base branch" abort (`exit 1`) has no `log-decision.js` call and isn't routed to Park — a third, unhandled outcome contradicting the file's own "every run ends in an explicit, logged outcome (`merged` or `pending-review`)" invariant | open | Staged `staged/review-2.patch` for Review Console — proposes folding this case into the Park branch (nothing has been touched yet, so `pending-review` is the correct outcome) |
| 6 | review | Lens 3c (confirmed, direct-verification, cross-checked against `bin/lib/hooks/pre-tool-use.js:396-424`): Park branch's "the worktree, feature branch, and issue claim all stay exactly as they were" claim is inaccurate — `close-run` (Procedure step 2) unconditionally lifts the worktree's teardown-gate protection *before* the merge attempt, so a parked run's worktree is reap-eligible even though the doc implies it's protected | open | Staged `staged/review-3.patch` for Review Console — this is a correctness/safety gap in the actual procedure (not just prose), so the proposal both corrects the claim and flags the design question of whether Park should re-establish protection |
| 7 | review | Lens 3b (unconfirmed, not reproduced): commit-message shell interpolation of unescaped placeholders (Procedure step 4) — same pattern already accepted in `auto-merge-short-circuit.md` | observation | Low-confidence, staged to Review Console; not actioned inline given the second agent didn't reproduce it and it extends an already-accepted convention |
| 8 | review | Lens 3f (unconfirmed, not reproduced): `tests/local-merge-auto-finish.test.js` has 3 test-hygiene notes (undefended `indexOf`/`slice` boundary detection in tests 3-6; no revert-verification evidence for the `doesNotMatch` assertion; fragile `'3.5.'` boundary marker) | observation | Low-confidence, staged to Review Console; second agent found nothing — not actioned inline |
