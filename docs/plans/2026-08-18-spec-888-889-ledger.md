# Open Items — assess-agent-autonomy hardening (#888, #889)

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | test | Multi-spec pre-flight sweep: 1/4752 failed — `resolvePrStateAsync: does not block the event loop` (tests/bin-lib/reconcile/pr-state.test.js), a timing-sensitive concurrency assertion. Isolated re-run: 7/7 pass. Machine-load flake per CLAUDE.md's varying-failure-count rule, not a regression. | observation | Pre-existing/load-sensitive; cite this item if it recurs in per-spec test runs. |
| 2 | test/888 | Spec 888 Common Step 5 verification: 2/4770 failed, both in `tests/changelog-coverage.test.js` — "1 version(s) shipped on origin/main with no CHANGELOG entry: 6.97.0". Confirmed origin/main shipped v6.97.0 after this shared worktree branched (post-dates the pre-flight sweep in item #1, which predates it and found no such failure). Spec 888's diff touches neither `CHANGELOG.md` nor `shipped-versions.tsv` — verified via `git diff --name-only` against the spec 888 range. | accepted | Not this spec's regression — shared-worktree branch divergence. `flow/multi-spec.md`'s Shared-worktree Step 3 (finish once at the end) is the owner: re-check main divergence and rebase before finishing the branch, after both specs complete. Proceeding with spec 888's own verification as passed for build purposes. |
| 3 | review/888 | Step 3 lens 3b (Security): `blast-radius-cli.js`'s two revision-resolving `git` calls (`rev-parse --verify`, `merge-base`) pass `base`/`integrationBranch` without an end-of-options separator — a value starting with `-` could be read as a flag. | fixed | Low severity — auto-applied. Fixed with `--end-of-options` (empirically verified against `--` first, which breaks `rev-parse --verify`/`merge-base` by switching them into pathspec mode; `--end-of-options` is the correct separator for these two subcommands). Discriminating tests added (reverted to confirm red). |
| 4 | review/888 | Step 3 lens 3c (Error handling), finding 3c-A: `resolveConfig`'s `readFile` calls swallow ALL read errors (not just ENOENT) into `null`, silently degrading `mergeSensitivePaths` to `[]` on a real read failure (permission denied, IO error, corrupt config) — the exact silent-approval hazard #888 exists to eliminate, one layer down in the policy read. | fixed | Medium severity — auto-applied under `review-auto-apply-ceiling: medium`. `defaultReadFile` now rethrows non-ENOENT errors; `resolveConfig` wraps its two `readFile` calls and converts any such error to `BlastRadiusError`, matching the CLI's fail-loud contract. Discriminating test added (reverted to confirm red). |
| 5 | review/888 | Step 3 lens 3f (Test quality)/3f: `blast-radius.js`'s header comment claimed "no logic lives here", but `--run` path validation (added in the build-phase fix wave) is real logic in that file. | fixed | Low severity — auto-applied. Comment corrected to name argv parsing and `--run` validation as the logic that does live in the shell. |
| 6 | review/888 | Step 3 lens 3c: `fail()` in `blast-radius.js` writes `err.message` verbatim to stderr; a wrapped `execFileSync` error message can contain embedded newlines, violating the CLI's own documented single-line-stderr contract. | fixed | Low severity — auto-applied. `fail()` now takes only the first line of the message. |
| 7 | review/888 | Step 3 lens 3e (Architecture): `resolveConfig()` in `blast-radius-cli.js` duplicates the root-resolution + policy.yml/config.yml-read pathway `resolve-policy.js`'s `main()` already implements — two independent implementations of the same read-fail-safe judgment. | deferred | Genuinely larger than #888's scope (a shared-helper extraction touching both CLIs' read paths). Filed as #916, `Defer-reason: genuinely-larger`, born-ready, `risk:low size:medium`. |
| 8 | review/888 | Step 3 lens 3f-B: claimed `--run` path validation was missing from `blast-radius-cli.js`. | accepted | False positive, no action needed — the reviewing agent missed that this validation lives by design in `blast-radius.js` (the CLI shell), not in the library. Verified by direct read of `blast-radius.js:41-49`. |
| 9 | review/888 | Step 3 lens 3c-B: claimed `resolvePolicyKeys`/`classifyDiffFiles`/`blastRadiusSummary` inside `resolveConfig` could throw non-`BlastRadiusError` exceptions, and that `resolved['auto-merge-max-lines']` could be `undefined`. | accepted | False positive, no action needed — verified by grepping `policy-schema.js` for `throw` (zero results; "never throws" is a stated design invariant) and re-reading `globToRegExp` (escapes all regex metacharacters). `resolvePolicyKeys` always returns an entry for every requested key. |
| 10 | review/hindsight | Step 4 Implementation Hindsight (5 evaluations, inline/component-invoked): approach correctness sound; structural debt already filed as #916; no consolidation opportunity found (grep confirms no other numstat-parsing site); convention drift (typed-error/injectable-deps split vs. other bin/*.js's scattered process.exit style) judged deliberate, not accidental; skill-worthy pattern deferred — single instance only, revisit once #889 confirms whether it repeats. | accepted | No changes needed — approach is sound. |
| 11 | build/889 | Common Step 3 (Code Simplification) for spec 889: code-simplifier subagent dispatch hit 4 consecutive `API Error: 529 Overloaded` terminations over ~35 minutes (initial + 2 resumes + 1 resume after a 25-min backoff). Confirmed via status.claude.com: an active, unresolved "investigating elevated errors" incident across multiple Claude models as of this run. Tooling failure, not a content judgment — the spec 889 diff itself was never assessed for simplification opportunities. | fixed | Retried during /review Step 5 once the platform incident cleared — succeeded. 3 changes: wrapped two unwrapped ~470-char paragraphs, fixed a `/claude-tweaks:backlog refine` reference split mid-token across a line break, collapsed two byte-identical pin tests into a loop. Commit 401783e2. |
| 12 | review/hindsight | Step 4 Implementation Hindsight (5 evaluations, inline/component-invoked): approach correctness sound; no structural debt; missing consolidation found and merged with skill-worthy pattern — `grant-check.md`/`failure-check.md`/`merge-check.md` now carry 3 near-duplicate instances of the same gather-resilience shape (MCP fallback + two could-not-gather short-circuits), not yet extracted to a shared fragment; convention drift none. | deferred | Routed to backlog at the multi-spec Review Console (consoleAutoResolve): created #927 "Extract assess-agent-autonomy's gather-resilience shape into a shared fragment", Defer-reason: genuinely-larger. [skill: assess-agent-autonomy] |
| 13 | wrap-up | Residue sweep (`bin/residue.js --base 7525f5e5 --integration-branch origin/main --scope blast-radius --no-suite`, run at spec 889's wrap-up — the multi-spec run's final spec): PR #894 (this run's own draft PR, head `worktree-flow-spec-888-889`) flagged outstanding, remedy: record, NEEDS DISPOSITION. | fixed | Resolved by the run's own finish step: origin/main merged into the branch (commit cac07a7c, 3 conflicts resolved by intent, full suite green — 4996/4997 + the known pr-state machine-load flake passing 7/7 in isolation, and item 2's changelog-coverage failure gone as predicted), PR #894 merge executed immediately after this ledger's deletion commit and verified via `gh pr view` before run-dir archival (parent decisions.md carries the verification line). |
| 14 | wrap-up | Residue sweep: 2 probes reported `unknown` (not a clean pass) — `--no-suite` skipped the full test-suite re-run (already ran via `/claude-tweaks:test` earlier this wrap-up); release-triple check reported not applicable to this project's convention. | observation | No action — both are expected `unknown`s per residue-sweep.md's "never a silent skip" rule, not findings. |
