# Open Items — #2014: Make the multi-spec artifact-overwrite check distinguish an append-only shared journey from an overwrite

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | review | Lens 3b (Security): `checkArtifactOverwrite`'s top-level `git log` call interpolates the CLI-supplied `${base}..${head}` range with no `--end-of-options` guard before it, unlike this repo's own sibling convention (`plugin/bin/lib/verify/changed-files.js`). A `--base`/`--head` value starting with `-` could be parsed as a git option rather than a revision, letting the HARD-GATE integrity check silently misreport. Severity: high. | open | Staged as `staged/review-1.patch` for the Wrap-Up Review Console (`review-auto-apply-ceiling: medium` stages High, does not auto-apply it) |
| 2 | review | Lens 3c (Error handling): `check-artifact-overwrite.js`'s catch-all masked any exception (including a real bug in `checkArtifactOverwrite`) as exit 3 "walk failed", unlike sibling CLIs (`merge-size-probe.js`, `blast-radius-cli.js`, `flow-preflight.js`) which rethrow unrecognized errors. Severity: medium. | fixed | Auto-applied under `review-auto-apply-ceiling: medium` — added `ArtifactOverwriteCheckError` (mirrors `MergeSizeProbeError`), thrown only from `defaultGit` on an actual git-command failure; the CLI's catch now rethrows anything else uncaught. Commit: c9d88ee66. |
| 3 | review | Lens 3c (Error handling): `checkArtifactOverwrite({ paths: [] })` silently widened to "match every path" (git's own "no pathspec after `--`" meaning) instead of the module's stated fail-closed intent — the CLI wrapper already worked around this for its own call site, but the exported library function itself had no guard. Severity: medium. | fixed | Auto-applied under `review-auto-apply-ceiling: medium` — an empty array now falls back to the default scope (`docs/journeys/`, `stories/`), same as `paths` being unset. Commit: c9d88ee66. Step 5 (Simplify) then retired `check-artifact-overwrite.js`'s now-dead `if (opts.paths.length === 0) delete opts.paths` workaround (the library-side guard supersedes it) and fixed a non-discriminating regression test for this row: the fixture originally passed even with the guard reverted, because its out-of-scope file resolved as a same-spec self-edit either way — remediated by giving it a real cross-spec-overwrite shape plus an explicit `clean === true` assertion. |
| 4 | wrap-up | Reflect (Surprises + Approach): the build's plan doc cited `merge-size-probe.js` as the model for this CLI's exit-code contract, but only carried over its shape, not its `--end-of-options` git-argv hardening or its error-class separation — both gaps survived build + full test suite and were caught only by review's lens dispatch (ledger items 1-2 above). | open | Staged as `staged/reflect-1.md` for the Wrap-Up Review Console |
| 5 | wrap-up | Reflect (Near-misses, CAUSAL: terminal): the `paths: []` regression test (ledger item 3) was non-discriminating on first write — its fixture satisfied an unrelated same-spec self-edit exemption regardless of the guard under test, a live instance of lens 3f's own Discrimination diagnostic. Caught by a follow-up code-simplifier pass. | open | Staged as `staged/reflect-2.md` for the Wrap-Up Review Console |
| 6 | wrap-up | Reflect (Near-misses/Friction): two self-corrected execution lapses this run — auto-applying the HIGH-severity finding alongside the two Medium ones before re-checking `review-auto-apply-ceiling` per-item (unwound before commit); not re-prompting the 3c lens agent whose reply put narration before its status word, per `step3-lens-dispatch.md`'s own re-prompt-once rule. | accepted | Don't capture — already covered by existing contract text (the routing table; the re-prompt rule) and CLAUDE.md's Checkpoint/Fail-loud principles; no new rule warranted |
