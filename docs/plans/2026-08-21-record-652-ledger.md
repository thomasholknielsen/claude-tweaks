# Open Items — session-start reconciliation: leaves tracked-file deletions uncommitted after archiving pipeline runs (#652)

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | review | revertWorkMoves ignored `git reset`'s result before unconditionally running `fs.renameSync`, risking disk/index desync on a compound reset+commit failure (critical, converged across 3 independent review lenses) | fixed | Gated the disk move on reset success; added a compound-failure test — commit `50b9ad5a` |
| 2 | review | Adjacent gaps: `git-mv-failed` sibling path doesn't revert already-succeeded pairs in a multi-batch; no multi-spec revert test coverage (single-sourced, unconfirmed within its reproduction pair) | deferred | Out of #652's stated scope (commit-failure path only) — filed as backlog issue #1214 |
| 3 | wrap-up | Near-miss: `materialize.js --run-dir` must point at the worktree's *mirrored* copy of the run-dir path, not the `$RUN_ROOT`-anchored one, when an orchestrating agent drives it directly (rather than via a packaged skill's own env threading) — the first invocation wrote `work/652-spec.md` into the main checkout instead of the worktree, caught and cleaned up before committing anything | accepted | Cheap catch (one `rm -rf` + re-run), no lasting harm — noted for future manually-orchestrated flow runs; `materialize.md`'s "When this runs" section already documents the worktree-first ordering but not this specific `--run-dir` value gotcha for a non-skill caller |
| 4 | wrap-up | Friction: `EnterWorktree` refuses to create a worktree from a subagent session with a pinned/overridden cwd ("cannot create a worktree from a subagent with a cwd override") — required falling back to manual `git worktree add` + `env -C` for every cwd-scoped command for the rest of the build, duplicating the tool's own branch-naming convention (`worktree-{name}`) by hand | observation | Harness-level constraint (Agent SDK's `EnterWorktree`, not a claude-tweaks skill defect) — worth a documentation note in `build/worktree-setup.md` for future subagent-dispatched `/flow` runs so this isn't rediscovered each time |
| 5 | wrap-up | Live incident: this run's own `run-state.json` was corrupted to `{status: clean, worktree: null}` mid-session by a sibling process sharing this session's `CLAUDE_CODE_SESSION_ID` (`events.jsonl` shows a `wd-deny` at 14:53:08Z from a commit attempt in a *different* worktree, `land-record-266`, under this same session id) — matches the documented "record-worktree without an explicit `--run` corrupts another session's state" hazard | accepted | Worktree/branch/commits were unaffected — only the bookkeeping stamp was wrong. Repaired via `record-worktree --run` (the sanctioned write path). Already tracked upstream by open issues #1096, #1012, #1099 (run-state ownership hardening) — no new issue filed |
| 6 | wrap-up/ops | Residue sweep (`--scope blast-radius`) found 16 unarchived `status: clean` pipeline-run dirs, dated 2026-08-20 and earlier-in-day 2026-08-21, from unrelated prior sessions: `2026-08-20T174925-dispatch-standalone`, `2026-08-20T175000-record-499`, `2026-08-20T193508-dispatch-standalone`, `2026-08-20T193627-record-355`, `2026-08-20T193642-record-231`, `2026-08-20T204530-record-951`, `2026-08-20T211123-record-1011`, `2026-08-21T042252-backlog-standalone`, `2026-08-21T043154-release-review-fixes-standalone`, `2026-08-21T043316-backlog-standalone`, `2026-08-21T043416-tidy-standalone`, `2026-08-21T043718-backlog-standalone`, `2026-08-21T043900-trust-table-plugin-root-standalone`, `2026-08-21T045014-env-attached-chdir-fix-standalone`, `2026-08-21T075653-backlog-standalone`. Per `bin/lib/residue/probes/pipeline-runs.js`'s own audited design (#1011), a `status: clean` run dir is inert regardless of which session produced it and archival is a non-destructive move — remedy `auto` | deferred | Excluded `2026-08-21T143434-spec-316` from this batch deliberately: same suspect window as item 5's corruption incident (the `land-record-266` commit attempt belongs to that record's session), so its `clean` status may be the same corruption rather than genuine completion — left untouched for its own session to resolve, not archived by this run. The remaining 15 are auto-archived at Phase 4 cleanup (this run's own housekeeping pass, per the probe's documented cross-session design) |
