# Open Items — record #2016: friction-events.js Friction Lens vocabulary filter

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | wrap-up | Reflect (light mode, Friction lens): the worktree-isolation Bash guard repeatedly refused multi-line/multi-flag commands as "too complex to verify" during this run even when non-mutating (e.g. `git rev-parse --git-common-dir` combined with other reads, long `log-decision.js --text` calls), forcing command splitting and retries. | accepted | Already tracked upstream — duplicate of open issues #1798, #1785, #1876 (same guard false-positive class). No new capture; this run's occurrence is confirming evidence, not a new finding. |
| 2 | wrap-up | Residue sweep (blast-radius scope): PR #2085 — `gh pr list --state open --json number,title,headRefName --limit 100` — open, head `worktree-record-2016` (this work). | accepted | This run's own open PR — expected residue for a `pr-first` run until it merges (`bin/lib/residue/probes/forge.js`'s own doc comment names this exact case as "worth surfacing," not a defect). Disposition resolves at this same run's Phase 4 CLOSE step (Review Console / Auto-merge short-circuit), not separate leftover work. |
| 3 | wrap-up | Residue sweep: full test-suite probe reported `unknown: skipped via --no-suite` — this wrap-up's own `/claude-tweaks:test` run already covered it earlier in the same session (8042 tests, 288 fail, matches documented baseline). | observation | Skip was intentional (`--no-suite`), not a silent gap — full-suite result already recorded in this run's `decisions.md` test-step entry. |
