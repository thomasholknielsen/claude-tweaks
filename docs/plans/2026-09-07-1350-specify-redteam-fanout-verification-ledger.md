# Open Items — Verify /specify red-team fan-out batching at next live multi-sub-issue run (#649 AC2)

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | build | Record #1350's only deliverable is observing a live `/specify` decomposition run producing 2+ `ceremony:standard` sub-issues (to inspect that session's transcript for red-team fan-out batching, per `_shared/subagent-output-contract.md`'s single-assistant-message rule). No such qualifying run occurred between the prior attempt's release (2026-08-29T080612, claim tombstone reason: "not actionable now — standing watch-item for a future live /specify decomposition run, no code to build") and this attempt (2026-09-07). There is no code to build and no transcript to inspect yet — pending external work per `build/SKILL.md` Common Step 4. | deferred | → backlog (ledger Phase 2 narrowing, ceiling: unattended, defer-reason: blocked-external). Staged `staged/ledger-record-1350-fanout-watch.md` — flagged as a likely duplicate of #1350 itself; recommend closing as duplicate during Review Console triage. |
| 2 | test | Residue sweep (`bin/residue.js --scope blast-radius`) flags 16 `npm test` failures at this run's HEAD as outstanding — independently re-derived from the raw `tests.log` (7485 tests, 7457 pass, 16 fail; every failing test's file:line mapped) and confirmed unrelated to this run's own diff (2 markdown-only files). All 16 match the exact signature (chmod/permission-simulation no-ops under a root-privileged test runner; one Impeccable plugin version-pin mismatch) documented by an already-open, unrelated-prior-run record. | accepted | Already tracked by existing open record #1853 ("npm test: 16 deterministic failures under a root-privileged test runner", filed 2026-09-04) — same 16-test signature, confirmed by name/count match. No new record filed; nothing this run can fix (sandbox-environment-level, not this diff). |
