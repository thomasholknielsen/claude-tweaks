# Open Items — Verify /specify red-team fan-out batching at next live multi-sub-issue run (#649 AC2)

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | build | Record #1350's only deliverable is observing a live `/specify` decomposition run producing 2+ `ceremony:standard` sub-issues (to inspect that session's transcript for red-team fan-out batching, per `_shared/subagent-output-contract.md`'s single-assistant-message rule). No such qualifying run occurred between the prior attempt's release (2026-08-29T080612, claim tombstone reason: "not actionable now — standing watch-item for a future live /specify decomposition run, no code to build") and this attempt (2026-09-07). There is no code to build and no transcript to inspect yet — pending external work per `build/SKILL.md` Common Step 4. | open | — |
