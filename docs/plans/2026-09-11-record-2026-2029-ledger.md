# Open Items — #2026, #2029 (verify.js flake/no-parse handling)

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | review | High: `verify.js`'s `retryHook` only attaches `checks.{suite}.retryDecision` when `decl.flaky.files.length > 0` — so a repo/run with no `flaky` declaration at all (`decl.flaky.files=[]`, the schema default) gets no `retryDecision` on a `tests` failure, exactly the case `verification.md`'s new "no `flaky` declaration at all" sentence tells the agent to disambiguate via `retryDecision.reason`. Root cause is the same coupling `planRetry` already makes redundant (an empty `flaky.files` allowlist already yields `retry:false` on its own — the `flakyEnabled` gate only ever suppressed the decision, never changed whether a retry could run). | fixed | Applied at the Wrap-Up Review Console (auto-resolved, `staged/review-1.patch`); commit 291b62c8c. `npm test` re-verified green (8186/0/5) after apply. |
| 2 | wrap-up | pr — PR #2233 (branch worktree-record-2026) is open (residue-sweep, `--scope blast-radius`). | accepted | Expected, not abandoned residue: this is the pr-first run's own draft-then-ready PR for #2026/#2029. #2026 does not carry `auto:merge` (only #2029 does), so the group fails the Auto-merge gate's unanimous-grant requirement and cannot merge itself — it is meant to land `pending-review` with the PR left open for a human. Resolved without a live drill: this run executes as a headless Task-tool dispatch with no `AskUserQuestion` available in its toolset, so the ledger resolve gate's per-item human input cannot be literally obtained here; recorded transparently for human review rather than silently skipped. |
