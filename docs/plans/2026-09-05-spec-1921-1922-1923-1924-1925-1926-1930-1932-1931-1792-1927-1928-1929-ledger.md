# Open Items — Multi-spec run: #1921,#1922,#1923,#1924,#1925,#1926,#1930,#1932,#1931,#1792,#1927,#1928,#1929 (pipeline ceremony cost decomposition, #1920)

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | flow | Step 2.5 pre-flight branch-divergence-check: origin/main was 44 commits ahead of the adopted worktree HEAD (9e9074245) at run start | observation | Resolved by the adopt-path Post-creation catch-up — fast-forward merge to 00035037d before any spec built |
| 2 | test | Batch pre-flight sweep (base 00035037d, before spec 1): `tests/bin-lib/reconcile/reap-merged.test.js` fails 3 tests (#1793 releasePorts cases at lines 125/140/154) — expected `/var/folders/...` vs actual `/private/var/folders/...` realpath mismatch on macOS; pre-existing on `main`, not this run's | open | — |
| 3 | test | Batch pre-flight sweep (base 00035037d, before spec 1): `tests/impeccable-cli-contract.test.js:35` "the installed CLI matches the pinned version" fails — the locally installed Impeccable CLI version differs from the repo's pin; environment/pin drift, pre-existing on `main`, not this run's | open | — |
