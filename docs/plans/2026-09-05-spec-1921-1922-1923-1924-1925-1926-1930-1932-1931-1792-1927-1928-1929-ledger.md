# Open Items — Multi-spec run: #1921,#1922,#1923,#1924,#1925,#1926,#1930,#1932,#1931,#1792,#1927,#1928,#1929 (pipeline ceremony cost decomposition, #1920)

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | flow | Step 2.5 pre-flight branch-divergence-check: origin/main was 44 commits ahead of the adopted worktree HEAD (9e9074245) at run start | observation | Resolved by the adopt-path Post-creation catch-up — fast-forward merge to 00035037d before any spec built |
| 2 | test | Batch pre-flight sweep (base 00035037d, before spec 1): `tests/bin-lib/reconcile/reap-merged.test.js` fails 3 tests (#1793 releasePorts cases at lines 125/140/154) — expected `/var/folders/...` vs actual `/private/var/folders/...` realpath mismatch on macOS; pre-existing on `main`, not this run's | deferred | Already tracked by open record #1900 ("npm test: reap-merged #1793 macOS realpath mismatch + impeccable-cli-contract version pin") — defer-reason: pre-existing-outside-diff; no new record (spec #1921 wrap-up, Phase 1 re-check) |
| 3 | test | Batch pre-flight sweep (base 00035037d, before spec 1): `tests/impeccable-cli-contract.test.js:35` "the installed CLI matches the pinned version" fails — the locally installed Impeccable CLI version differs from the repo's pin; environment/pin drift, pre-existing on `main`, not this run's | deferred | Already tracked by open record #1900 — defer-reason: pre-existing-outside-diff; no new record (spec #1921 wrap-up, Phase 1 re-check) |
| 4 | build | Spec #1921 whole-branch review, pre-existing and out of scope: `plugin/bin/lib/verify/report.js`'s `writeReportAtomic` (the pid-suffixed atomic writer added to fix a shared-`.tmp` collision) is exported and tested but never called — `verify.js` writes `report.json` through `writeJsonAtomic`'s plain `.tmp`, and the new `stamp.js` twin write does the same; the foreground rule forbids concurrent runs so no live collision, but the dead export should be wired in or removed (follow-up candidate, not this spec's) | deferred | → backlog (needs:definition) — auto-routed by ledger narrowing at `unattended` (defer-reason: needs-human-decision); staged at `staged/ledger-record-write-report-atomic-dead-export.md` for the Queue writes gate |
| 5 | build | Spec #1921 whole-branch review, spec-level observation for Phase 2 (#1922/#1923 in this run): `/review` Step 1.5's belt-and-braces stamp read re-triggers `/claude-tweaks:test` on `match: false`; a project that commits or dirties between `/test` and `/review` buys a fourth full-suite run — safe and per spec, but the scoping engine should revisit whether that read should re-trigger or merely warn | deferred | → backlog (needs:definition) — auto-routed by ledger narrowing at `unattended` (defer-reason: blocked-dependency on #1923); staged at `staged/ledger-record-review-stamp-belt-and-braces-retrigger.md` for the Queue writes gate |
| 6 | build/skill | Spec #1921 extends the `gh-api-module-pattern` CLI wrapper contract's exit-code vocabulary with a "status-as-data" mode: `verify.js --stamp-status` exits 0 in every state (absent/mismatch/no checkout) because the JSON is the answer — worth one sentence in that skill's exit-code section so the next bin CLI with a read-only status mode follows the same shape (enrichment) | observation | Skill-enrichment candidate for wrap-up's Skills curation row |
| 7 | review | Spec #1921 review lens 3b (Security), high, confirmed by direct verification: a pass stamp written against a dirty tree (`dirty: true`) reads `match: true` from `--stamp-status` once the uncommitted edit is discarded — `plugin/bin/verify.js` `stampStatus()` recomputes `dirty` from the live tree but never consults the stamp's own stored `dirty`, so a HEAD state that was never verified pristine is trusted by `/test` Skip-if-recent and `/review` Step 1.5. Fix: `match` must also require the stored `stamp.dirty === false`. Staged under the `medium` auto-apply ceiling (high stages) — `staged/review-7.patch` | open | — |
| 8 | review | Spec #1921 review lens 3b (Security), medium, confirmed (both agents): `--stamp-status --git-dir <dir>` reads the stamp from `<dir>` but `gitInfo()` computes `head`/`dirty` from the invoking cwd, so a sibling checkout at the same commit can read `match: true` for a verification it never ran. Fix: when `--git-dir` is supplied and its realpath differs from the cwd's own git dir, force `match: false` (the nine-field shape unchanged) | fixed | Auto-applied under the `medium` ceiling — `268c91934` (test added: foreign-checkout `--git-dir` → `match: false`) |
| 10 | wrap-up | Spec #1921 wrap-up Phase 4 verify: `wrap-up-engine.js verify` has no `MULTISPEC_REVIEW_DEFER` awareness — its `run-dir-archived` check reads `fail` on every per-spec wrap-up of a multi-spec run (item 8 is deferred to the parent by `cleanup-procedures.md`'s own table), so the closure line is withheld by contract on a spec that did everything it was allowed to. Candidate: `verify` skips (or reports `deferred`) for the five deferred rows when the run dir's basename is `spec-{N}` under a multi-spec parent | observation | Plugin-defect candidate for the batch wrap-up pass (this repo is the plugin — a backlog record, not an upstream filing) |
| 11 | build/skill | Spec #1922 extends the `gh-api-module-pattern` CLI wrapper contract with two wrinkles worth one sentence each in its git-seam section: a module-level domain error class (`ChangedFilesError`) that the CLI maps onto the usage-class exit 2 rather than the runtime-failure exit 1 (an unresolvable base is an invocation problem, not a check failure), and the quiet `stdio: ['ignore','pipe','pipe']` posture applied once on the module's `git()` seam so benign probe failures (`merge-base --is-ancestor`, `rev-parse --verify`) never leak `fatal:` to the caller's stderr on an exit-0 path (enrichment) | observation | Skill-enrichment candidate for wrap-up's Skills curation row |
| 12 | review/skill | Spec #1922 review Step 1.6 (`review/cross-spec-promise-check.md` item 2): when the newly-seeded forward assumption's owner sibling (#1923) is itself blocked by the record under review (#1922), the instruction to add a `Blocked by #{sibling}` line to this record's body would create a dependency cycle for `resolve-blockers.js`; the line was withheld and only the parent row + seeding comment were written. The skill should carve out the "owner is downstream of this record" case (promise row only, no Blocked-by line) — plugin-defect candidate (this repo is the plugin) | observation | Plugin-skill enrichment candidate for wrap-up's Skills curation row |
| 13 | review | Spec #1922 review lens 3c (Error handling), info, confirmed by direct verification: `plugin/bin/verify.js`'s `--scope` flow calls `changedFiles()` unguarded, so a `git diff`/`git status` failure after the base was already verified exits 1 through `main().catch` with a raw stack instead of a `--scope:` message | accepted | Fail-safe as-is (loud exit 1, never a fabricated pass); wrapping it in the usage-error exit would misclassify a runtime failure as an invocation error — no change |
| 14 | review/hindsight | Spec #1922 hindsight, Missing consolidation: "the stamp's own anchor" (`fullSha`, or `sha` for a legacy stamp) is derived twice — `scope.js`'s `anchorOf` and `changed-files.js`'s `usableAnchor` each inline the same ternary; one exported `anchorOf(stamp)` on `stamp.js` (the module that owns the stamp shape) read by both would keep the two from drifting. Evidence: `scope.js:8-13` vs `changed-files.js:44-47` (direct read during Step 3's override). Cost this session: none yet — flagged by the 3b lens as the seed of a real divergence (fixed in Step 3 routing) | fixed | Change now — applied by Step 5's simplify pass: one exported `anchorOf(stamp)` on `stamp.js`, imported by `scope.js` and `changed-files.js` (commit after 15cc17f26); verify suites 173/173 |
| 15 | review | Spec #1922 review lens 3b (Security), medium, confirmed by direct verification: `changed-files.js`'s `usableAnchor` ran `git merge-base --is-ancestor {anchor} HEAD` without `--end-of-options`, so a dash-prefixed anchor read from the JSON stamp was flag-injectable (failed closed via `tryGit`, but broke the file-wide guard convention) | fixed | Auto-applied under the `medium` ceiling — 15cc17f26 (test: a `--bogus` anchor resolves null with `--end-of-options` in the argv) |
| 16 | review | Spec #1922 review lens 3b, low, confirmed by direct verification: `stamp.js`'s `readStamp` JSON branch accepted any string `sha` (the legacy bare-file branch enforced `SHA_RE`), letting an unvalidated value reach the anchor logic | fixed | Auto-applied (low → AUTO) — 15cc17f26 (JSON `sha`/`fullSha` must be 40-hex or the stamp reads as absent; one existing test's placeholder sha replaced with a valid one) |
| 17 | review | Spec #1922 review lens 3b, low, confirmed by direct verification: `verify.js` passed the raw prior stamp to `selectScope` even when `usableAnchor` found its anchor stale (history rewrite), so a non-full run could stamp `fullSha: {stale}` with `base: {merge-base}`, breaking the `base === fullSha` invariant | fixed | Auto-applied (low → AUTO) — 15cc17f26 (a stale anchor now forces a `full` run; test: post-amend `--scope` run stamps `full` at the new HEAD) |
| 18 | build/skill | Spec #1923 Task 1: a Standard implementer ran a bare `git stash` / `git stash pop` (unprompted) to reconstruct the plan's Expected-FAIL probe after implementing ahead of it — on this repo the stash stack is shared across every worktree and live session, so a pop can apply a sibling's WIP. No damage this time (stack verified intact), but `_shared/subagent-output-contract.md`'s Working Directory Discipline should name `git stash`/`git checkout --`/`git reset` as forbidden in implementer dispatches, and `build/dispatch.md`'s task prompt should carry the `git show HEAD:<path>` alternative for before/after measurements (enrichment) | observation | Skill-enrichment candidate for wrap-up's Skills curation row |
| 19 | build | Spec #1923 Task 4 pushed `plugin/skills/flow/multi-spec.md` over the #724 ~20 KB read budget (`tests/run-dir-timestamp-utc.test.js:75`, limit 20480 B) — the plan measured the 40 KB skill ceiling, not this file's own tighter budget, and the file already sat at 20291 B. Trimmed to 20470 B after the wording correction (10 B spare): the file has no room for any later spec's edit — #1928 names it as a Key File, so that spec's plan must split it first; a split of `multi-spec.md` (e.g. the Execution section to a sub-file) is the durable fix | observation | Plan-authoring + skill-structure candidate for wrap-up (headroom check must use the file's own budget pin when one exists, not the generic ceiling) |
| 20 | build | Spec #1923 whole-branch review, pre-existing and out of scope (finding 12): `plugin/skills/test/design-gate.md` still resolves the Design CLI gate's changed-file list from `git diff --name-only`, which is empty for every committed pipeline diff — so in-pipeline the gate's Layer 3 sniff sees zero files and returns `decision: "skip"` on every committed frontend diff (the #808 merge precondition's own gate silently self-skips). The same skill now states "never hand-roll `git diff --name-only`"; the gate should read `verify.js --changed-files` (with the exit-1 → full-list fallback) like the QA filter does | deferred | → backlog (born-ready: one file, one command swap, pinned by a prose test) — defer-reason: pre-existing-outside-diff would not clear the floor, but the defect is material (a safety gate that never fires in pipelines), so routed as `genuinely-larger`'s sibling: a separate record so the fix gets its own review; staged at `staged/ledger-record-design-gate-changed-files.md` for the Queue writes gate |
| 21 | review/hindsight | Spec #1923 hindsight, Approach correctness: the record's own Red-team Gotcha ("standalone = no run dir OR no `--source`") encoded a false premise — `/claude-tweaks:flow` never passes `--source` to `/test` — so the plan copied it verbatim and every scoped site shipped unreachable until the whole-branch review probed it; a second Gotcha (`match` as the re-verify signal) produced a re-trigger loop the same way. Evidence: whole-branch review findings 1-2 (`grep -rn -- '--sourc' plugin/skills/flow/` empty; probe D2 loop). Cost this session: two fix waves (~25 min) plus a promise amendment on #1920. Lesson: a Gotcha written as a "Red-team resolution" is a premise to verify against the code at plan-authoring time, not a rule to transcribe — `[route: D4]` (extends the existing `empirical-premise-check-before-contract-text` memory with this instance) | accepted | Recorded; memory note extended in-session; no code change beyond the fix waves already landed |
| 22 | review | Spec #1923 review lens 3c (Error handling), medium, confirmed by direct verification (both agents flagged it at medium/high): `test/SKILL.md`'s explicit `qa affected` steps read `verify.js --changed-files` with no action on exit 1, so a resolver failure read as "No QA stories affected" | fixed | Auto-applied under the `medium` ceiling — dfc816f92 (exit 1 → stop and report the runner's message, never an empty set) |
| 23 | review | Spec #1923 review lens 3c, low, reproduced by both agents: the Step 1 `affected` bullet stated that exit 1 exists but not the action | fixed | Auto-applied (low → AUTO) — dfc816f92 (stop, report, offer the standard suite instead of a narrowed guess) |
| 24 | review | Spec #1923 review lens 3c, info, confirmed by direct verification: `verify.js` calls `changedFiles()` unguarded after `resolveBase` in both `--changed-files` and `--scope` modes | accepted | Same ruling as item 13: a post-validation git failure exits 1 loudly through `main().catch`, never a fabricated pass — no change |
| 25 | review | Spec #1923 review lens 3f (Test quality), low, confirmed by direct verification: the `--changed-files` CLI tests covered a committed change and an untracked file only — no staged or modified-tracked case | fixed | Auto-applied (low → AUTO) — dfc816f92 (new test: staged + modified-tracked + untracked + committed → sorted, deduplicated set) |
| 26 | review | Spec #1923 review lens 3f, low, single-agent: the `--changed-files` output order is asserted alphabetically without stating whether order is contractual | accepted | It is contractual — `changed-files.js` returns `[...set].sort()`; a comment above the assertion now says so (dfc816f92) |
| 27 | build | Spec #1924 whole-branch review, measurement note for the parent (#1920 Group B): this repo's `.claude-tweaks/verify-scope.json` is inert until `main`'s suite is green — `verify.js` writes a pass stamp only on a green full run, the reap-merged baseline (#1900, items 2-3) keeps every run red, so no `fullSha` anchor exists and every `--scope` run resolves `full` (probed: no `claude-tweaks-verify-pass.json` in this worktree's git dir after four full runs). Once green, the only delta shape the declaration cheapens is a bookkeeping-only delta since the anchor (ledger rows, run-dir spec files, consumed plans/specs); every `plugin/**`, `tests/**`, `tools/**`, and unmatched path stays `full`. The same baseline gap already inert-ed #1921's stamp (item 9) | observation | Carry into the batch reflect + the parent's Group B measurement; the durable fix is #1900 (or a baseline-aware stamp, item 9's open choice) |
| 28 | review | Spec #1924 review lens 3b (Security), single-read at low tier, confirmed by direct verification, re-severitied high → low: `suiteCommand` interpolates `pkg.name`/`pkg.path` verbatim into a command `verify/run.js` runs with `shell: true`, and `readPackageAt` validated only `typeof name === 'string'`. Low, not high: the same package.json's `scripts.test` already runs through that shell verbatim, so a crafted member package.json is no new trust boundary — but a name with a space or a `;` still produced a broken or surprising suite command | fixed | Auto-applied (low → AUTO) — 3796f1d9e (names outside npm's grammar and paths outside `[A-Za-z0-9@._-/]` are skipped with `package name or path is not shell-safe — not proposed as a suite`; test pins it) |
| 29 | review | Spec #1924 review lens 3b, medium, fact confirmed by direct verification: `step-06-6-verify-scope.md` writes the starter in `auto` mode without the interactive table's approval click | accepted | Intentional per `_shared/auto-mode-contract.md`: a reversible, project-owned, tracked file written under `AUTO` with a log line, reviewed like code at the PR; the interactive path's question is the mode's own stop, not a security gate. No change |
| 30 | review | Spec #1924 review lens 3b, info (below the lens floor, fixed anyway because the module header promises exhaustive `skipped` reporting): `readdirSync(withFileTypes)` lists a symlinked workspace member as a non-directory, so it vanished from both `dir/*` and `dir/**` with no `skipped` entry | fixed | 3796f1d9e (`expandGlob` returns `symlinks`; each becomes `skipped: {path, reason: 'symlinked entry not followed'}`; the walk still never follows a link; test pins both shapes) |
| 31 | review | Spec #1924 review lens 3c (Error handling), single-read at low tier, confirmed by direct verification, re-severitied high → medium: `detectWorkspace` was the module's one `existsSync`-then-`readFileSync` site (pnpm-workspace.yaml), so a file pruned between the two calls threw out of a report-only CLI with a stack trace instead of exit 0/2 (docs/donts.md's TOCTOU rule, #901/#1269 recurrence). Medium, not high: no state is corrupted, the CLI aborts loudly | fixed | Auto-applied under the `medium` ceiling — 3796f1d9e (`readText` read-and-catch; a present-but-unreadable file is absent; test pins EACCES on the pnpm file) |
| 32 | review | Spec #1924 review lens 3c, low, confirmed by direct verification: an unparseable root `package.json` was merged with "no workspace" (`readJson` → null) while every member package.json parse failure got a `skipped` reason | fixed | Auto-applied (low → AUTO) — 3796f1d9e (`detectWorkspace` pushes `skipped: {path: 'package.json', reason: 'unparseable package.json'}`; the CLI prints it with the other warnings; Step 6.6 prose lists the three new reasons) |
| 33 | review/hindsight | Spec #1924 hindsight, Missing consolidation: `init-verify-scope.js` and `detectWorkspace` each read the root `package.json` — the CLI for `scripts`, the module for `workspaces` — with different error semantics (silent catch vs a `skipped` reason), which is exactly the divergence review finding 32 flagged. Evidence: `init-verify-scope.js:128-130` (pre-fix) beside `verify-scope-starter.js`'s `readText` block. Cost this session: one review finding + its fix (ledger 32) that a single read would have made impossible. Evaluations 1, 2, 4, 5: no finding (the spec's false premises were caught at plan time and staged as build-deviation-1; flat sibling dir and zero-dependency parsing follow the repo's conventions; the shell-safe rule now lives in code with a test rather than a new don't) | fixed | Change now — 6bc121431 (`detectWorkspace` returns `rootScripts`, `composeStarter` defaults to `workspace.rootScripts`, the CLI's own read removed; AC2 test asserts the default) |
| 34 | review | Spec #1925 review lens 3c (Error handling), low, single-agent → confirmed by direct verification: `retryLogName`'s dash slug collided `tests/a-b.test.js` with `tests/a/b.test.js`, so a later retry silently overwrote the earlier file's log while `retryAttempts`/the CAVEAT still pointed at it — the module's own "never share a log path" invariant broken | fixed | Auto-applied (low → AUTO) — 3df329528 (slug `/` → `+`, a character the extracted-path charset never admits; collision test) — deviation from the record's `/` → `-` Gotcha noted in staged/build-deviation-1.md |
| 35 | review | Spec #1925 review lens 3f (Test quality), medium, single-agent → confirmed by direct verification: the retry hook's unreadable-log branch (`retryDecision.reason: unreadable-log`) had no test | fixed | Auto-applied under the `medium` ceiling — 3df329528 (CLI test: the check unlinks its own log before the hook reads it → ordinary failure, reason recorded, no retry, no stamp) |
| 36 | review | Spec #1925 review lens 3f, low, confirmed by direct verification: `flakyCaveatLines`' `, ` join for two retried files was unexercised | fixed | Auto-applied (low → AUTO) — 3df329528 |
| 37 | review | Spec #1925 review lens 3f, low, confirmed by direct verification: `nextFlakyHits` with a null allowlist was tested with no retried files only | fixed | Auto-applied (low → AUTO) — 3df329528 (a new retried file under an unknown allowlist counts alongside the carried-forward map) |
| 38 | review | Spec #1925 review lens 3f, low, single-agent: `fakeRunOne` extracts the file via a test-local `run ` prefix | accepted | The fake serves `runRetries` with a hand-built plan object, not `planRetry`'s output — the coupling is to the test's own plan, which is the unit under test's input; no change |
| 39 | review | Spec #1925 review lens 3f, info, single-agent: `stripAnsi` test "has no negative control" | accepted | False premise — `extract.test.js:214` already asserts two ESC sequences are removed and a bracket that is not one survives; no change |
| 40 | review/hindsight | Spec #1925 hindsight, Skill-worthy pattern: the whole-branch review's one real bug (a run with no declaration wiping `flakyHits`) was "could-not-determine collapsed into legitimately-empty" — the same discipline the parse-signal skill states for parsers, applied to an *absent optional config file*. Evidence: `verify.js` count-stamp block (`allowlist = decl ? decl.flaky.files : []` before 515825245) and the review's probe (`RUN 2 (canonical, no --scope): "flakyHits": {}`). Cost this session: one Important finding, one fix-wave item, one probe. Enrichment for the skill: name "no declaration/config was read" as a distinct signal from "the declaration says none" — pass `null`, never `[]` `[skill: parse-signal-discipline]` | observation | Routed to wrap-up's Skills curation row via the tag; no code change (the fix already shipped) |
| 41 | review | Spec #1926 review lens 3c (Error handling), medium, single-agent → confirmed by direct verification: `flow/steps-and-gates.md`'s phase-exit paragraph said the polish PR row is "never added" for `no-polish`/fast-lane while its next sentence said fast-lane removes it — and `_shared/pr-early-run-lifecycle.md` omits the row only for a backend surface, so the pre-existing `no-polish` clause was wrong too | fixed | Auto-applied under the `medium` ceiling — 399981c52 (row omitted only for backend surface; no-polish and fast-lane get it unchecked and remove it at polish's would-be exit, no push; roster Mentions row follows the reworded line) |
| 42 | review | Spec #1926 review lens 3c, low, reproduced by both agents: a malformed Mentions row crashed `tests/ceremony-profile-roster.test.js` with an opaque TypeError | fixed | Auto-applied (low → AUTO) — 399981c52 (named assertion on the row's cell count) |
| 43 | review | Spec #1926 review lens 3a (Convention), medium, both agents (different line cites) → confirmed by direct verification: `_shared/ceremony-profile.md` was missing from `docs/plugin-structure.md`'s curated `_shared` row | fixed | Auto-applied under the `medium` ceiling — 399981c52 |
| 44 | review | Spec #1926 review lens 3f (Test quality), low (agent: medium), confirmed by direct verification: the multi-task/batched `--count-tasks` CLI cases asserted stdout only and the never-runs-Checks case asserted the exit code only | fixed | Auto-applied — 399981c52 (whole-result `deepStrictEqual`) |
| 45 | review | Spec #1926 review lens 3f, low, confirmed by direct verification: `stripFencedCodeBlocks` handled backtick fences only | fixed | Auto-applied — 399981c52 (`~~~` fences too; test extended) |
| 46 | review | Spec #1926 review lens 3f, medium (agent), single-agent: the roster test's exemption "relies on the exact `roster tag \`…\`` citation form" | accepted | That form is the contract the roster states and Tasks 3-5 wrote; a looser match would let untagged restatements pass — no change |
| 47 | review | Spec #1926 review lens 3f, low ×2, single-agent: no case-variation tests for the batched markers; `rows()` filters by header names | accepted | The markers are defined literals (`**Execution:** batched`, `[batch]` case-insensitive by design); the roster's table shape is pinned by the same test — no change |
| 48 | review/hindsight | Spec #1926 hindsight, Approach correctness: the record's Deliverable 4 asserted a runtime effect ("the downgrade means the next run runs `standard`") that no code path produces, the plan copied it into three files, and only the whole-branch review's probe caught it — the same shape as #1923's false Gotchas (ledger 21). Evidence: whole-branch review I1 (`grep ceremony:fast-lane` — the only writer is `specify/shaping-mode-stamping.md`). Cost this session: one Important finding, three-file rewrite in the fix wave. Change now: `build/plan-authoring-checks.md` gains a **Behavioral-claim check** — prose asserting a runtime effect names the code path that produces it or is marked unverified `[skill: build]` | fixed | Applied inline (review Step 4) — committed with the simplify pass |
| 9 | review/hindsight | Spec #1921 hindsight, Approach correctness: the pass stamp is inert on this repo's non-green baseline — `verify.js` stamps only on `report.pass === true`, `main` carries 4 pre-existing failures, so every `--stamp-status` read is `present: false` and none of the full-suite runs the stamp exists to shed were shed (4 full runs for this one spec). Evidence: `--stamp-status` → `present:false` after build Step 5, the test phase, and the review re-verify. Cost this session: ~20 min of redundant suite time. Names an open choice (fix the baseline vs a baseline-aware stamp) → Capture (needs:definition) | deferred | → backlog candidate, staged at `staged/reflect-1.md` (Queue writes gate; `Defer-reason: tangential`) |
