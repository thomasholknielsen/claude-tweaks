# Open Items — Flow run: #1903 (Add conformance test banning process.exit() inside require.main guards)

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | wrap-up | Reflect (light, Friction lens): composing the PR-early body during a case-3 run-dir recovery (missing `config.yml`) hit two consecutive Write-tool denials -- worktree write blocked pending PR bookkeeping (`[IL-131]`), then main-checkout write blocked by the worktree-session guard -- before landing on `gh pr create --body-file -` via a stdin heredoc as the workaround. `pr-early-run-lifecycle.md` Step 3's recipe assumes a plain file write succeeds; it doesn't name this recovery-time chicken-and-egg or document the stdin escape hatch. Evidence: two `tool_use_error` Write-tool denials on `pr-early-body-1903.md`, then a successful `gh pr create --body-file -` heredoc. Cost this session: ~3 extra tool round-trips to discover the workaround -- low, but likely reproducible on any other case-3 recovery. | deferred | Staged to Review Console (`staged/reflect-1.md`) as a pattern observation for the Skills curation row -- candidate: document the stdin-body workaround in `pr-early-run-lifecycle.md`'s Step 3. |
| 2 | wrap-up | Residue sweep: PR #2052 (this run's own head branch `worktree-record-1903`) is still open | accepted | This run's own not-yet-merged draft PR -- resolved by Phase 4's Auto-merge/Review Console step, not by this ledger; not residue in the "leftover" sense. |
