# Open Items — record #711

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | wrap-up | Residue sweep (`bin/residue.js --scope blast-radius`): PR #770 — head `worktree-flow-record-711` (this run's own PR) still open | accepted | No `auto:merge` grant on issue #711 (only `auto:build`) — the Auto-merge short-circuit (`review-console.md`) never applies; run terminates at `pending-review` by design. PR #770 stays open for human merge review; worktree removal, claim release, and run-dir archival are deferred to the reconciler on merged-PR evidence. |
