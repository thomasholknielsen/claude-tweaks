# Open Items — #1910: Fast-lane bundling: dispatch packs up to three non-overlapping fast-lane records into one multi-spec run

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | review | Spec Deliverable "The dispatch Reporting section names the bundle and its members" is unmet: `queue-pull-script.md` writes `dispatch-fastlane-bundles.json` but nothing reads it — `grep -rln "dispatch-fastlane-bundles"` finds only the write site. Attempted a minimal one-line pointer fix in `plugin/skills/dispatch/SKILL.md`'s `## Reporting` section (matching the existing `oversized-group-report.md`/`open-pr-exclusion-report.md` convention) but reverted it: that file was already at 40945/40960 bytes (15 B of headroom) before this record touched it, so `tests/timing-prose-conformance.test.js`'s two byte-ceiling pins (`#1928 AC6`, `#1929 AC5`) fail on any addition, however small — fixing this properly requires trimming existing unrelated prose first, which is out of this record's surgical scope. All 3 stated Acceptance Criteria (bundling behavior, priority/auto-merge/overlap isolation, `bundleCap: 0` byte-for-byte) are met and tested; this is a completeness gap on a Deliverable, not a correctness defect — the shipped feature works, its Reporting-section visibility does not. | deferred | Blocked on already-open #2009 (dispatch/SKILL.md Reporting/Configuration split, filed from a prior spec's identical near-miss) — added corroborating evidence rather than filing a duplicate. Once #2009 lands, this record's Reporting-section pointer can be added with headroom to spare. |
| 2 | wrap-up | Friction: two `contract-violation` events in this run's own `friction-events.js` output are false positives — `bin/lib/hooks/subagent-stop.js`'s check fires on every Task-agent turn (including this call's own mid-task narration turns), not only the final reply, per `reflect/full-mode.md`'s Friction Lens documented ambiguity. Staged as a pattern observation for the Review Console. | deferred | Staged: `staged/reflect-1.md` (suggests narrowing the check to the actual final reply, or filtering narration-shaped turns on the read side in `friction-events.js`). |
| 3 | wrap-up | Residue sweep (`bin/residue.js --scope blast-radius --no-suite`) reports PR #2055 (this run's own head branch, `worktree-record-1910`) as outstanding, kind `pr`, remedy `record`. | accepted | This is the run's own `pr-first` draft/ready PR, not orphaned residue — Phase 4's auto-merge short-circuit / Review Console disposes of it (merge, arm, or leave pending-review) as the normal end of this run, not as leftover cleanup. |
